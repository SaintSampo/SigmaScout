# WR-03 (03.1-REVIEW.md): the repo is currently a single root package with
# flat subdirectories under apps/ and packages/ — none of them has its own
# package.json, so pnpm does not treat them as workspace members today.
# These globs are reserved for the planned multi-package split (a later
# phase); until that split happens, leave them commented so this file
# states a fact about the repo rather than an aspiration.
# packages:
#   - "apps/*"
#   - "packages/*"
allowBuilds:
  better-sqlite3: true
  esbuild: true
