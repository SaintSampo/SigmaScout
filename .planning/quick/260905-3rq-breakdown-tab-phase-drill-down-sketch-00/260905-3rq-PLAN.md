---
quick_id: 260905-3rq
description: Breakdown tab phase drill-down (sketch 009-A) with sortable columns
date: 2026-09-05
mode: quick
executed_by: orchestrator-inline (design context from sketch 009 + findings skill loaded in main context; visual verification needs the main context's browser tooling)
---

# Quick Task 260905-3rq: Breakdown tab phase drill-down + sorting

## Design source

Sketch 009 winner **A — phase drill-down** (`.planning/sketches/009-breakdown-usability/`),
plus the user's follow-up decisions (2026-09-05):

- **Expansion state is NOT URL-shareable** — plain `useState`, no search-param schema change.
- **OPR gets no changes** — its Breakdown stays the current flat table (Total only).
- **Sorting (sketch B's idea) folds in** — click any metric header to sort, click again to
  flip; reuse the Teams table's exact affordance (button-in-th, `aria-sort`, accent ▲/▼).

## Data facts (verified against live artifacts 2026-09-05)

- Event artifact team rows carry `phaseAuto`/`phaseTeleop`/`phaseEndgame` with value,
  spread AND percentile (checked `2026alhu` vpr@8.0.0+rolling-2026-09b) — collapsed phase
  columns get real tier boxes with no pipeline change.
- `withDerivedGroupMetrics` (lib/metricGroups.ts) is the stale-cache fallback: a cached
  pre-260904-7id EPA artifact has no published group entries; the derived entry is
  value-only (no spread/tier), deliberately.
- Grouping is single-sourced in `packages/core/algorithms/breakdown/groups.ts`;
  `componentsInGroup(season, groupId)` gives each group's member components.
  Ungrouped components (e.g. `foulsCommitted`; `adjust` already filtered from
  `metricKeysFor` since 96c59a77) keep their own trailing columns.

## Tasks

1. **BreakdownTab.tsx**: for `hasGroupedTeamsView(algorithmId)` algorithms, default the
   metric columns to `[Total, Auto, Teleop, Endgame, <ungrouped>]`; a group-band header
   row above the column-label row carries one toggle button per phase
   (`aria-expanded`, accent ink, `breakdown-group-toggle-{id}` testid); expanding swaps
   the phase column for its component columns in place. Local sort state
   (default Total desc, missing-last, team-number tie-break — the existing
   `buildBreakdownRows` rules generalized to any key); collapsing the group that owns
   the active sort key resets sort to Total desc. Rows pass through
   `withDerivedGroupMetrics`. OPR path renders exactly what it does today.
2. **BreakdownTabSkeleton**: collapsed-default headers for grouped algorithms; OPR
   skeleton unchanged.
3. **Tests**: update BreakdownTab.test.tsx (default column set, expand/collapse swap,
   sort click/toggle/reset, missing-last, partial-data via expanded endgame, derived
   EPA fallback renders value-only, OPR unchanged) and event.$eventKey.test.tsx
   (header count 15 → collapsed 7).

## Constraints

- apps/web only; a retune/republish agent shares this checkout — no pipeline/publish
  code, stage commits by explicit path only.
- Tokens only (D-06); accent = interactive/active only; tier rendering untouched.
- Verify visually (dev server + Playwright) at desktop and 390px before finalizing.

## Done

- Event Breakdown opens scroll-free at desktop widths with Total + three tiered phase
  columns; phases expand/collapse in place; every metric column click-sorts; OPR
  untouched; targeted suites green; screenshots confirm desktop + mobile.
