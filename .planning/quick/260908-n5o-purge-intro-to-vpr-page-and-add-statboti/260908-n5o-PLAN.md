---
phase: quick-260908-n5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/epaVsStatbotics.ts
  - scripts/epaVsStatbotics.test.ts
  - scripts/publishEpaComparison.ts
  - scripts/publishEpaComparison.test.ts
  - packages/harness/pageArtifacts.ts
  - package.json
  - apps/web/src/lib/api/epaComparison.ts
  - apps/web/src/lib/api/epaComparison.test.ts
  - apps/web/src/components/methodology/epaComparisonContent.ts
  - apps/web/src/components/methodology/epaComparisonContent.test.ts
  - apps/web/src/components/methodology/EpaComparisonPage.tsx
  - apps/web/src/routes/methodology.epa-vs-statbotics.tsx
  - apps/web/src/routes/methodology.epa-vs-statbotics.test.tsx
  - apps/web/src/components/methodology/methodologyCardData.ts
  - apps/web/src/components/methodology/MethodologyCards.tsx
  - apps/web/src/routeTree.gen.ts
  - docs/models/epa-vs-statbotics.md
  - docs/models/epa-divergences.md
autonomous: false
requirements: [QT-260908-n5o]

estimate:
  tokens: 190000
  raw_tokens: 95000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "The /methodology hub shows exactly three cards, and the retired VPR intro is not one of them."
    - "/methodology/epa-vs-statbotics renders exactly the four locked differences and none of the four excluded ones."
    - "Every statistic on the new page comes from a fetched R2 artifact. No slope, Pearson, mean absolute difference, accuracy or Brier value is a literal anywhere in apps/web source."
    - "Both agreement arms published in one artifact carry one EPA version stamp, and a publish attempt that mixes two versions fails loudly instead of publishing."
    - "The page renders a skeleton while the artifact is pending and an error state when the fetch or parse fails."
    - "The head-to-head block states plainly, from the data rather than from a hardcoded sentence, how many seasons Statbotics is ahead on."
    - "No em dash character appears in any shipped page string."
  artifacts:
    - packages/harness/pageArtifacts.ts (epaComparisonKey + EpaComparisonArtifactSchema + EpaComparisonArtifact)
    - scripts/publishEpaComparison.ts
    - apps/web/src/lib/api/epaComparison.ts
    - apps/web/src/components/methodology/epaComparisonContent.ts
    - apps/web/src/components/methodology/EpaComparisonPage.tsx
    - apps/web/src/routes/methodology.epa-vs-statbotics.tsx
  key_links:
    - "epaComparisonKey() is the ONE spelling of the R2 key, imported by both scripts/publishEpaComparison.ts (writer) and apps/web/src/lib/api/epaComparison.ts (reader). Two spellings would be a silent permanent 404, exactly as preScheduleKey's own doc comment warns."
    - "EpaComparisonArtifactSchema is the ONE shape, parsed on the publish side before upload and again on the client after fetch."
    - "METHODOLOGY_CARDS is the ONE source of hub card titles, blurbs and routes. methodology.index.test.tsx already iterates it, so a correct card swap needs no test edit."
    - "epa.version (packages/core/algorithms/epa.ts) is the ONE source of the version stamp. Never a hand-typed version string."
---

<objective>
Two halves, in order.

1. Purge the Intro to VPR page from this project entirely: route, route test, component, content module, hub card, the route union member, and any generated route-tree entry.
2. Ship a new methodology page explaining how Statbotics' EPA and SigmaScout's EPA differ, aimed at high school students in the FRC community, carrying real published measurements of how far apart the two ratings actually land.

Purpose: the VPR intro is being retired, and the question it half-answered ("why does this number not match the other site") gets a real page with real measured numbers behind it instead of prose.

Output: one new published R2 artifact, one new fetching methodology page in the hub's first card slot, four deleted files, and a re-measurement that retires the stale `epa@2.0.0+baseline` figures currently sitting in the docs.

Out of scope, stated so nobody widens it: `epa.ts`, `sigma1/index.ts`, and the rating maths. This task documents and measures what already exists. It does not retune, repromote, or alter a model.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260908-n5o-purge-intro-to-vpr-page-and-add-statboti/260908-n5o-CONTEXT.md
@.claude/CLAUDE.md

Read before Task 1:
@docs/models/epa-vs-statbotics.md
@packages/harness/epaStatboticsCompare.ts

Read before Task 3:
@apps/web/src/lib/api/compare.ts
@apps/web/src/components/methodology/AcknowledgmentsPage.tsx
</context>

<sandbox_boundary>
## READ THIS BEFORE RUNNING ANYTHING

**An executor subagent's sandbox denies ALL network Bash on this machine.** That is a property of the sandbox, not of the code. It has bitten this project before, and the failure shape is an executor reporting a plan step as broken when the step was fine.

Tasks 1 through 4 are executor work: code, schema, page, tests. Every verify command in those tasks is offline (vitest, tsc, vite build, git grep). Run them.

Task 5 is ORCHESTRATOR-RUN. It fetches Statbotics and it writes to R2. **The executor must not attempt it and must not report a failure for it.** The executor stops at the Task 5 checkpoint and hands back. The orchestrator runs those commands from the main context.

Two more traps this repo has already paid for:

- Run vitest from the REPO ROOT, never from `apps/web`. Root sees 167 test files, `apps/web` sees 77, and an eight day red CI once hid in that gap.
- Root `tsc --noEmit` does NOT cover `apps/web`. Typecheck the web tsconfig separately.
- Do not wrap a pnpm command in `timeout`. It swallows all output and exits 0. Use `npx vitest run` directly and judge by output, not by exit code.
- Never `Read`, `cat`, or echo `.env`. Publish tooling takes `--env-file=.env` and reads it itself.
</sandbox_boundary>

<locked_decisions>
Every item below came from `260908-n5o-CONTEXT.md` and is not open for revisiting.

1. **Four differences make the page, and only these four:** offseason matches, win-probability scale, component maps, no per-year tweaks.
2. **Four are excluded and must NOT be added back for completeness:** fouls handling, EPA carrying no plus-or-minus of its own, the now-closed elimination-match divergence, and the rebuilt-from-scratch framing (the acknowledgments page owns that one and keeps it).
3. **Stats are FETCHED from R2, never baked into the page.**
4. **All three stat blocks ship:** per-season agreement, offseason on versus off, head-to-head accuracy.
5. **The hub keeps exactly three cards.** The retired card's slot is taken by the new page, in first position. Not two cards. Not four.
6. **The offseason-excluded arm is re-measured, never copied.** The figures in the docs are `epa@2.0.0+baseline` over 2022-2025. The shipping model is `epa@6.0.0+baseline`. Publishing two model versions in one A/B table is exactly the documentation drift this project's failure log names as its defining past failure.
7. **Voice:** no em dash characters anywhere in shipped page copy. Short declarative sentences. No "it's worth noting", "importantly", "in essence". Never oversell. Where Statbotics is ahead, say so. Explain a term the first time it appears, in the same sentence. Neutral between the two ratings.
</locked_decisions>

<planner_deviation>
## One deliberate deviation from CONTEXT, with its reason

CONTEXT says the new key belongs in `artifactKey`'s switch in `pageArtifacts.ts`. **Do not put it there.** Declare it as its own exported key function instead, following `districtsIndexKey` / `districtDetailKey` / `preScheduleKey`, which sit immediately below `artifactKey` in that same file and carry doc comments explaining exactly why.

The reason is structural. `PageKind` is the union that `apps/worker/src/artifactWriter.ts`'s exhaustive `SCHEMA_BY_PAGE` record and `packages/harness/payloadBudget.test.ts`'s `PAGE_KINDS` size gate are both keyed on. Widening `PageKind` forces a live Worker change for an artifact the Worker must never write, and pulls a cross-season document into a per-season size budget it does not belong in. Three existing artifacts already live outside `PageKind` on purpose for this reason.

The substance of the locked decision is honored in full: the key and the Zod schema and the inferred type all live in `pageArtifacts.ts`, the publisher writes it, and a new fetcher in `apps/web/src/lib/api/` reads it exactly the way `compare.ts` does. Only the declaration form changes, and it changes toward the file's own documented convention.
</planner_deviation>

<tasks>

<task type="tracer">
  <name>Task 1: Re-measure under the shipping model, with a version stamp and a win-probability arm</name>
  <files>scripts/epaVsStatbotics.ts, scripts/epaVsStatbotics.test.ts</files>
  <read_first>scripts/epaVsStatbotics.ts (whole file), packages/harness/score.ts lines 187 to 300 (HarnessPredictionInput, ScoreSlice, AggregateScoresOptions, aggregateScores, ELIGIBILITY_NOT_CLAIMED), packages/harness/publish.ts lines 2225 to 2245 (the existing records-to-HarnessPredictionInput mapping, which this task mirrors rather than reinvents), packages/harness/statbotics.ts lines 30 to 60 and 240 to 300 (StatboticsReference, statboticsReference)</read_first>
  <action>
This is the thin end-to-end slice: it proves one measurement run can produce every number the finished page needs, under one model version, before any artifact, publisher, fetcher or page exists.

Widen the report `scripts/epaVsStatbotics.ts` already writes so it carries two new things.

First, a version stamp. Add `epaVersion` to `EpaVsStatboticsReport`, read from the `epa` algorithm module's own `version` field. Never a hand-typed version string. This one field is what makes the mixed-version publish detectable in Task 2, so it is load-bearing rather than decorative.

Second, a win-probability arm, per season. The script's `replayEpaSeasonFinals` already runs a threaded chronological replay and already throws away the `PredictionRecord[]` that `WalkForwardSimulator.runAll` returns. Keep those records per season. Map them to `HarnessPredictionInput[]` with the same field-for-field mapping `publish.ts` already performs at its own call site, then call `aggregateScores` with `corpusSeasons` set to the run's own requested season list and `selectedOnSeasons` set to the `ELIGIBILITY_NOT_CLAIMED` sentinel. That sentinel is correct here rather than a convenience: this measurement makes no headline-eligibility claim, and the sentinel is the strictest available answer rather than a permissive default. Select the `combined` `compLevelView` slice for each season and carry its `winnerAccuracy`, `brierScore` and `scoredCount` onto that season's report entry.

Pair each season's figures with Statbotics' own published accuracy and Brier from `statboticsReference(season, { cachePath: ... })`. That function attempts a live fetch and falls back to a dated constant, and it never throws for a Statbotics-side failure, so the script stays runnable when Statbotics is down. Carry the returned `value`, `mse`, `capturedAt` and `fetched` fields onto the same report entry, so a reader of the published document can always tell whether a Statbotics figure was live or dated.

Note that `aggregateScores` excludes offseason matches by default and counts them as an explicit exclusion. That is correct and must not be overridden: scoring has always excluded offseason play on both sides, which is why the accuracy comparison was never affected by the offseason-inclusion divergence. Do not pass any option that changes this.

Export the pure pieces so they can be unit tested without a corpus and without a network: the records-to-inputs mapping, and the combined-slice selection. Keep the impure driver unexported as it is today.

Refresh the script's own usage comment block so the offseason-excluded arm's documented invocation names the full 2022-2026 range rather than 2022-2025. The 2022-2025 restriction was written when 2026 was still in progress. 2026 is now the season the production tables already report, so the two arms must cover the same seasons or they are not an A/B at all.

Add `scripts/epaVsStatbotics.test.ts`. Cover the mapping (a synthetic record produces the expected input fields, including `eventKey` carried as a real field rather than derived by splitting the match key), the combined-slice selection (given slices for all three views, the combined one is returned; given none, the failure is loud rather than a silent null), and the version stamp (the report's `epaVersion` equals the `epa` module's own `version`, by equality, never by pattern match). No test in this file may open the corpus or call the network.

Do NOT run this script in this task. It fetches Statbotics. Task 5 runs it.
  </action>
  <verify>
    <automated>npx vitest run scripts/epaVsStatbotics.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The report type carries `epaVersion` plus per-season win-probability figures for both sides. The new test file passes from the repo root and touches neither the corpus nor the network. Root typecheck is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish key, schema, and a publisher that refuses to mix model versions</name>
  <files>packages/harness/pageArtifacts.ts, scripts/publishEpaComparison.ts, scripts/publishEpaComparison.test.ts, package.json</files>
  <read_first>packages/harness/pageArtifacts.ts lines 1590 to 1650 (districtsIndexKey and its doc comment on why it sits outside PageKind), scripts/publishDistricts.ts lines 1 to 120 and its main/upload section (parseArgs shape, dry-run behavior, artifacts-before-index ordering, and how putObject reads its own credentials)</read_first>
  <behavior>
    - Two arm reports carrying the same `epaVersion` compose into one artifact that parses against the schema.
    - Two arm reports carrying DIFFERENT `epaVersion` values throw a named error and publish nothing.
    - Two arm reports covering different season sets throw a named error and publish nothing.
    - An arm report whose `includeOffseason` flag does not match the slot it was passed in throws rather than silently publishing a mislabelled arm.
    - `--dry-run` composes, validates and prints the byte size without ever calling putObject.
  </behavior>
  <action>
In `packages/harness/pageArtifacts.ts`, add an exported `epaComparisonKey()` returning the literal `v1/methodology/epa-vs-statbotics.json`. One key, not year-scoped: this measurement is a single cross-season document. Declare it as its own function immediately after the districts and pre-schedule key functions, with a doc comment stating why it is not a `PageKind` member (the Worker never writes it, and the per-season size budget does not apply to a cross-season document), and stating that this function is the ONE spelling of the key, imported by both the publisher and the client fetcher.

Add `EpaComparisonArtifactSchema`, extending `PagePreambleSchema` (not the algorithm-scoped preamble; there is no algorithm segment here, matching the reasoning `CompareArtifactSchema` already carries). Do not bump `PAGE_ARTIFACT_SCHEMA_VERSION`. This file's established convention is that an additive new artifact kind never bumps it, and bumping would invalidate every already-published artifact of every kind.

Fields, and each one exists for a reason:
  - `measuredAt`, `epaVersion`, `minMatches`: the provenance stamp. `epaVersion` is what makes a version-mixed table impossible to publish.
  - `agreement`: an array of rows, each `{ season, includeOffseason, joinedCount, ordinaryLeastSquaresSlope, pearson, meanAbsoluteDifference }`. Stat block one reads the offseason-inclusive rows. Stat block two reads both arms and pairs them by season. One array, not two, so the two arms structurally cannot come from two different documents.
  - `headToHead`: an array of `{ season, ourWinnerAccuracy, ourBrierScore, scoredCount, statboticsWinnerAccuracy, statboticsBrierScore, statboticsCapturedAt, statboticsFetched }`. Our two figures are nullable, matching `ScoreSlice`'s own contract. Statbotics' Brier is nullable, matching `StatboticsReference.mse`'s own optionality.

Export the inferred type as `EpaComparisonArtifact`.

Add `scripts/publishEpaComparison.ts`, shaped like `scripts/publishDistricts.ts`: `parseArgs` from `node:util`, deep relative imports with explicit `.js` extensions, a `main()` guarded on being the process entry point, non-zero exit on failure, and `putObject` reading its own credentials from `process.env`. This file never touches `process.env` directly and never reads, prints or interpolates `.env`.

Flags: `--inclusive <path>` and `--excluded <path>` for the two arm report JSONs, `--bucket`, and `--dry-run`.

The composition is where the anti-drift gates live, and they are the point of this task rather than a nicety. Before composing anything, assert that the two reports agree on `epaVersion` and on their season set, and that each report's own `includeOffseason` flag matches the slot it was passed in. Each failure throws its own named error class. Never a console warning, never a best-effort merge. A silent version mix here is precisely the failure this whole task exists to avoid.

Parse the composed object through `EpaComparisonArtifactSchema` before upload, so an out-of-shape document fails at compose time rather than becoming a client parse error.

Add a `publish:epa-comparison` entry to the root `package.json` scripts, using `tsx --env-file=.env`, matching every other publish entry.

Write `scripts/publishEpaComparison.test.ts` first, from the behavior list above, with small hand-built fixture reports declared inline. The mismatch cases are the tests that matter most: each must assert the specific named error, and must assert nothing was uploaded. No test in this file may call the network.

Do NOT run the publisher in this task.
  </action>
  <verify>
    <automated>npx vitest run scripts/publishEpaComparison.test.ts packages/harness/pageArtifacts.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The key function and schema are exported from pageArtifacts.ts. The publisher composes, gates and validates. A version-mismatched pair throws and uploads nothing, proven by a passing test. Root typecheck is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The explainer page, its fetcher, and its prose as data</name>
  <files>apps/web/src/lib/api/epaComparison.ts, apps/web/src/lib/api/epaComparison.test.ts, apps/web/src/components/methodology/epaComparisonContent.ts, apps/web/src/components/methodology/epaComparisonContent.test.ts, apps/web/src/components/methodology/EpaComparisonPage.tsx, apps/web/src/routes/methodology.epa-vs-statbotics.tsx, apps/web/src/routes/methodology.epa-vs-statbotics.test.tsx</files>
  <read_first>apps/web/src/lib/api/compare.ts (the closest analog fetcher: no algorithm segment, no version segment, therefore no useAlgorithmVersion resolution and no enabled gate), apps/web/src/routes/methodology.compare.tsx (pending and error branch ordering), apps/web/src/components/methodology/AcknowledgmentsPage.tsx and the head of acknowledgmentsContent.ts (the content-as-data shape this page follows), apps/web/src/components/compare/AccuracyTable.tsx (the table markup and classes to mirror rather than invent)</read_first>
  <behavior>
    - The content module exports exactly four difference entries, and their ids equal the expected list by equality, not by iteration. A fifth entry added later fails this test loudly.
    - No exported string in the content module contains an em dash character.
    - The route renders a skeleton while the query is pending.
    - The route renders an error state when the fetch rejects, with a retry affordance.
    - Given a mocked artifact, the route renders one agreement row per season, both offseason arms paired per season, and one head-to-head row per season.
    - The head-to-head summary sentence's season count is derived from the mocked data, so a fixture where Statbotics leads in three seasons renders three rather than a hardcoded four.
  </behavior>
  <action>
Invoke `Skill("sketch-findings-sigmascout")` before writing any markup. It carries the decided palette, the uncertainty display rules and the chart craft rules, and it is not optional for UI work on this repo.

Naming carries a real hazard on this machine. A data module and its component must not differ from each other by case alone, or Rolldown collapses them on the case-insensitive filesystem and the build fails with a confusing missing-export error. The filenames above already satisfy this, following the established `coverageRows.ts` / `DataCoverageTable.tsx` shape. Do not rename toward a colliding pair.

**Fetcher** (`epaComparison.ts`): mirror `compare.ts` exactly. Import `epaComparisonKey` and the schema from `pageArtifacts.js` at the same five-levels-up relative depth `compare.ts` uses. There is no workspace alias in this repo. Fetch, check `res.ok`, throw `ArtifactFetchError` on a bad status, parse through the schema, call `markArtifactParsed()` immediately after the parse resolves, throw `ArtifactValidationError` on a parse failure. Export a query-options factory. This artifact has no algorithm segment and no version segment, so like `compare.ts` it needs no `useAlgorithmVersion` resolution and no `enabled` gate on an unresolved manifest. Do not add one back by symmetry with the other fetchers. Say so in the file's doc comment, pointing at `compare.ts`'s own note.

**Content module** (`epaComparisonContent.ts`): prose as data, the way `acknowledgmentsContent.ts` is. Export a lead paragraph, and exactly four difference entries with stable ids `offseason-matches`, `win-probability-scale`, `component-maps`, `no-per-year-tweaks`, each with a heading and its paragraphs. Also export the short framing sentences that introduce each of the three stat blocks.

Write the four difference explanations for a high school FRC audience, from the source material in `docs/models/epa-divergences.md` sections 7, 4, 6 and 3 respectively. Do not paste the doc's prose; it is written for a maintainer, not a student. Cover, in your own words:
  - Offseason matches. We replay offseason events and let them move a rating. Statbotics never ingests them. This is the single largest source of disagreement. Include the 2026-09-08 narrowing: the cross-season carry is now taken at a season's last official match, so an exhibition result in November no longer seeds the following February. Within-season offseason play still moves ratings.
  - Win-probability scale. Statbotics divides the score margin by one season-level standard deviation constant. We use an expanding window standard deviation folded match by match, so a Week 1 prediction never borrows variance from matches that had not been played yet. Explain that this is what makes walk-forward replay possible at any point in a season, and explain "walk-forward" in the same sentence you first use it.
  - Component maps. We built our own per-season score-breakdown groupings against our own data rather than porting Statbotics' table, so the two split a match score into different pieces in some seasons. 2024 is the concrete example: we kept five separate note components rather than collapsing them.
  - No per-year tweaks. Statbotics applies per-season post-processing on top of the raw rating, such as a 2018 sigmoid and per-year clamps. We run the raw output with no season-specific patches, as standing policy.

The four excluded differences are excluded. Do not add a fifth entry for completeness.

Voice rules are binding on every string in this module. No em dash characters. Short declarative sentences. No hedging openers. No sentence that restates the previous sentence in different words. Neutral between the two ratings. Explain a term the first time it appears.

For the em dash gate, assert at runtime over the exported string constants rather than grepping the file, because this repo's file header comments legitimately use that character and a file-wide grep would fire on a comment. The test walks the exported values, not the source text.

<!-- planner-discipline-allow: em dash -->

**Page component** (`EpaComparisonPage.tsx`): takes the parsed artifact as a prop and renders content plus three stat blocks. Never fetches. Never declares its own query.

  - Block one, per-season agreement. Rows from the `agreement` entries where `includeOffseason` is true. Columns: season, teams compared, OLS slope, Pearson correlation, mean absolute difference. Explain each statistic in one short sentence above the table, in plain terms: what a slope below 1 means, what a correlation near 1 means, what the mean absolute difference is measured in.
  - Block two, offseason on versus off. Pair the two arms per season. Layout is your call, one table or two, but the pairing must be visually adjacent so a reader can see the size of the effect at a glance. State that this one choice explains most of the gap.
  - Block three, head-to-head accuracy. Winner accuracy and Brier score for our EPA next to Statbotics' own published figures, per season. Explain what a Brier score is and that lower is better, in the same sentence you first use it. Add a summary line whose season count is DERIVED by counting the seasons where Statbotics' accuracy exceeds ours, never a hardcoded number, so the page cannot go stale against its own table. Where Statbotics is ahead, say Statbotics is ahead.
  - Surface the version stamp and measurement date somewhere small and honest near the tables, and mark any Statbotics figure that came from a dated constant rather than a live fetch, using the artifact's own flag.

Mirror `AccuracyTable.tsx`'s table markup and classes. Do not invent a new table treatment. Numeric precision follows the docs: slope and correlation to three decimals, mean absolute difference to two with a points unit, accuracy and Brier to four.

**Route** (`methodology.epa-vs-statbotics.tsx`): `createFileRoute("/methodology/epa-vs-statbotics")`. One `useQuery` against the fetcher. One page-level state in the same fixed branch order `methodology.compare.tsx` uses: 404 empty state, other error state with retry, pending skeleton, populated. Title renders from first paint regardless of query state. Declare no `validateSearch` of its own.

This route is NOT linked from the hub yet. Task 4 does that. Leaving it unlinked here keeps the two file surfaces from colliding.

**Tests**: the content test and the route test, both from the behavior list. The route test builds a small self-contained route tree the same way `methodology.compare.test.tsx` and `methodology.index.test.tsx` do, with the real exported `Route` object under test and `fetch` mocked. Pin the four ids with a single `toEqual` on the full array. Do not iterate a hardcoded list and assert per item: an iterating test silently skips a new entry, and this repo has already been bitten by exactly that.
  </action>
  <verify>
    <automated>npx vitest run apps/web/src/routes/methodology.epa-vs-statbotics.test.tsx apps/web/src/components/methodology/epaComparisonContent.test.ts apps/web/src/lib/api/epaComparison.test.ts</automated>
    <automated>npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>The route renders pending, error and populated states against a mocked artifact. The content module exports exactly four differences with the pinned ids and no em dash in any exported string. The head-to-head summary count is derived from the fixture. The web tsconfig typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 4: Purge the Intro to VPR page and hand its hub slot to the new page</name>
  <files>apps/web/src/components/methodology/methodologyCardData.ts, apps/web/src/components/methodology/MethodologyCards.tsx, apps/web/src/routeTree.gen.ts, and four deletions</files>
  <read_first>apps/web/src/components/methodology/methodologyCardData.ts, apps/web/src/components/methodology/MethodologyCards.tsx, apps/web/src/routes/methodology.index.test.tsx</read_first>
  <action>
Delete these four files outright:
  - `apps/web/src/routes/methodology.vpr.tsx`
  - `apps/web/src/routes/methodology.vpr.test.tsx`
  - `apps/web/src/components/methodology/VprGuide.tsx`
  - `apps/web/src/components/methodology/vprGuideContent.ts`

<!-- planner-discipline-allow: VprGuide -->
<!-- planner-discipline-allow: vprGuideContent -->
<!-- planner-discipline-allow: methodology/vpr -->

The deleted component owns the only closing link to `/methodology/compare` outside the hub, so removing it strands nothing.

In `methodologyCardData.ts`, replace the first card with the new page's card and swap `"/methodology/vpr"` out of the `to` union for `"/methodology/epa-vs-statbotics"`. The hub keeps exactly three cards, and the new card takes the first position. Title and blurb are yours; keep the blurb short, plain, and free of em dashes, in the same register the other two cards use. Something in the shape of "Our EPA vs Statbotics' EPA" with a blurb naming that both sites publish a rating called EPA and that this page shows where the two numbers come apart and by how much.

In `MethodologyCards.tsx`, the component destructures exactly three cards by position and returns null if any is undefined. Update the destructured names and the three explicit `Link` elements to the new card set. Keep the explicit-links-not-a-map shape: each `Link`'s `to` needs its own literal route path for the router's typed search prop to type-check, which is why this file does not map over the descriptor array. That reasoning is already in the file's doc comment and still holds.

Describe the removed page by concept in any comment you write, for example "the former first card". Do not name the deleted component or its content module in any surviving comment, or the leftover gate below will fire on your own comment rather than on real leftover code.

Regenerate the route tree by running a web build. The tree is generated by the router's Vite plugin, so a stale generated entry pointing at a deleted route counts as a leftover and will fail the gate.

`methodology.index.test.tsx` iterates `METHODOLOGY_CARDS` and asserts the rendered link count equals that constant's length, so a correct card swap needs no edit there. If that test fails, the card data and the component have drifted apart. Fix the drift rather than the test.
  </action>
  <verify>
    <automated>pnpm --filter web build</automated>
    <automated>! git grep -nE 'VprGuide|vprGuideContent|methodology/vpr' -- apps packages scripts</automated>
    <automated>npx vitest run apps/web/src/routes/methodology.index.test.tsx</automated>
    <automated>npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Four files deleted. The hub shows three cards with the new page first. The leftover gate finds nothing in apps, packages or scripts, including the regenerated route tree. The hub's own route test passes unedited.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: ORCHESTRATOR-RUN. Measure both arms, publish, and retire the stale doc figures</name>
  <what-built>Tasks 1 to 4 shipped the measurement code, the artifact key and schema, the publisher, the page and the purge. Nothing has been measured and nothing has been published. Every key under `v1/methodology/` returns 404 right now, and the new page correctly shows its error state against live R2.</what-built>
  <action>
STOP. This task is ORCHESTRATOR-RUN and every step below needs network access the executor sandbox denies. The executor must not attempt any command here and must not report a failure for this task. Hand back to the orchestrator with Tasks 1 to 4 committed, and let the orchestrator perform the steps in `<how-to-verify>` from the main context.
  </action>
  <verify>
    <automated>MISSING by design — every gate in this task requires network access (Statbotics fetch, R2 write, live page load). The orchestrator verifies by the steps below, from the main context.</automated>
  </verify>
  <done>Both arms measured under one model version, the artifact live in R2, the page rendering real numbers with one version stamp across both arms, and both model docs updated so no `epa@2.0.0+baseline` figure is presented as current.</done>
  <instructions>
**The executor stops here. These commands need network access that the executor sandbox denies. Run them from the main context.**

Step 1, measure both arms under the shipping model. Both runs read the corpus read-only and touch no credential. Neither takes `--env-file`.

    npx tsx scripts/epaVsStatbotics.ts --check
    npx tsx scripts/epaVsStatbotics.ts --seasons 2022-2026 --no-offseason --out reports/epa-vs-statbotics-nooff

The first run is the production offseason-inclusive arm and also gates the committed tolerance bands. If `--check` fails, follow the documented procedure in `docs/models/epa-vs-statbotics.md`: re-run without `--check`, rebuild the bands with the unchanged half-width formula recentred on the fresh values, commit the refreshed baseline, and record the before-and-after table with an honest direction verdict whether or not it matches any prediction. Do not widen a band to make a run pass.

The second run is the offseason-excluded arm over the SAME seasons as the first. The stale table in the docs covers 2022-2025 under `epa@2.0.0+baseline`. Two different season sets is not an A/B either, so both arms must be 2022-2026.

Confirm before publishing: both report files carry the same `epaVersion`, and it is the version the site is actually serving. If they differ, something re-ran against a different model and the publish must not proceed. The publisher will refuse anyway, which is the gate working.

Step 2, publish. Artifacts before manifest is the load-bearing ordering rule in this repo. This artifact has no manifest of its own, so the ordering constraint here is simply that the object lands before anyone is told to look for it.

    pnpm publish:epa-comparison -- --inclusive reports/epa-vs-statbotics/epa-vs-statbotics.json --excluded reports/epa-vs-statbotics-nooff/epa-vs-statbotics.json --dry-run
    pnpm publish:epa-comparison -- --inclusive reports/epa-vs-statbotics/epa-vs-statbotics.json --excluded reports/epa-vs-statbotics-nooff/epa-vs-statbotics.json

Read the dry run's byte size and composed shape before doing the real publish.

Step 3, verify the page against live data. Start the web dev server with the artifact origin set to local so the `/v1` proxy is active, since R2 CORS blocks localhost direct. Use a fresh port. Load `/methodology` and confirm three cards with the new one first, then follow it and confirm all three stat blocks render with real numbers, that both offseason arms carry the same version stamp, and that the head-to-head summary count matches its own table.

Step 4, retire the stale figures in the docs, which is the whole reason the re-measurement was mandatory.

  - `docs/models/epa-vs-statbotics.md`: replace the offseason A/B table's `epa@2.0.0+baseline` 2022-2025 numbers with the freshly measured 2022-2026 pair, and stamp the section with the model version and measurement date. Update the head-to-head accuracy table to the freshly measured figures under the shipping version, or state plainly which version each column was measured under. Two versions in one table presented as a controlled A/B is the thing being fixed here, so do not reintroduce it in the doc while removing it from the page.
  - `docs/models/epa-divergences.md`: the header's "Version status of the quoted figures" paragraph names the offseason-excluded figure as "the one stale number on this line". That sentence is now false. Update it.
  - Add a line to both docs pointing at the published artifact as the live source the site reads.
  </instructions>
  <verification>
Confirm all five before resuming: (1) both report JSONs carry the same `epaVersion` and it matches the version the site serves; (2) the real publish printed a success for `v1/methodology/epa-vs-statbotics.json`; (3) `/methodology` shows three cards with the new one first; (4) the new page renders all three stat blocks with real numbers, one version stamp across both agreement arms, and a head-to-head summary count that matches its own table; (5) neither model doc presents an `epa@2.0.0+baseline` figure as current.
  </verification>
  <resume-signal>Type "published" once both arms are measured, the artifact is live, the page renders real numbers, and both docs are updated. Or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Statbotics API to pipeline | Third-party JSON crosses into a measurement that gets published as fact |
| Pipeline to R2 | A publish writes a public object using live credentials from `.env` |
| R2 to browser | Untrusted-from-the-client's-perspective JSON is parsed and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-n5o-01 | Tampering | `scripts/publishEpaComparison.ts` composition | high | mitigate | Named-error gates on `epaVersion` equality, season-set equality, and arm-flag correctness. A mixed-version or mislabelled-arm publish throws and uploads nothing. Proven by tests, not by review. |
| T-n5o-02 | Information disclosure | `.env` handling in the new publish script | high | mitigate | The script never touches `process.env` directly and never reads, prints or interpolates `.env`. `putObject` reads its own credentials, matching every other publish tool here. The script is invoked through a `--env-file=.env` package script. Never `Read` or `cat` that file. |
| T-n5o-03 | Tampering | Statbotics response shape drift | medium | mitigate | `statboticsReference` already Zod-parses the response and falls back to a dated constant on any failure. The published artifact carries a `statboticsFetched` flag per season, so a dated figure is visible as dated rather than passing as live. |
| T-n5o-04 | Information disclosure | New public R2 object | low | accept | The object contains only aggregate published measurement statistics that already appear in committed documentation. No credential, no per-user data, no unpublished model parameter. |
| T-n5o-05 | Tampering | Client-side artifact parse | medium | mitigate | The fetcher parses through `EpaComparisonArtifactSchema` and throws `ArtifactValidationError` on any mismatch, so a malformed or stale-shaped object renders the error state rather than a partially wrong table. |
| T-n5o-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable. This plan adds no package-manager install task. No new dependency is introduced by any task. |
</threat_model>

<verification>
Run from the REPO ROOT, after Task 4 and before the Task 5 checkpoint.

    npx vitest run
    npx tsc --noEmit
    npx tsc --noEmit -p apps/web/tsconfig.json
    pnpm --filter web build

Judge by output, never by exit code alone, and never wrap these in `timeout`.

Post-publish human check, orchestrator-run per Task 5 step 3: `/methodology` shows three cards with the new page first, and the new page renders all three stat blocks from live R2 with one version stamp across both agreement arms.
</verification>

<success_criteria>
- The four Intro to VPR artifacts are deleted and `git grep` finds no reference in `apps`, `packages` or `scripts`, including the regenerated route tree.
- The hub has exactly three cards. The new EPA comparison page holds the first slot.
- The new page explains exactly the four locked differences. None of the four excluded ones appears.
- Every statistic on the page is fetched from `v1/methodology/epa-vs-statbotics.json`. No statistic is a literal in `apps/web` source.
- Both agreement arms in the published artifact were measured under the same `epa` version, and the publisher provably refuses a pair that was not.
- The offseason-excluded arm was re-measured over 2022-2026 under the shipping model. The `epa@2.0.0+baseline` figures no longer appear as current anywhere.
- No em dash character appears in any shipped page string.
- The head-to-head block reports Statbotics' lead honestly, with the season count derived from the data.
- Full root vitest, both typechecks, and the web build all pass.
</success_criteria>

<output>
Create `.planning/quick/260908-n5o-purge-intro-to-vpr-page-and-add-statboti/260908-n5o-SUMMARY.md` when done.

Note for the executor: `Write` is blocked against SUMMARY files for subagents in this repo. Return the summary text to the orchestrator and let the orchestrator write it. Do not route around the block with a Bash heredoc.
</output>
</content>
</invoke>
