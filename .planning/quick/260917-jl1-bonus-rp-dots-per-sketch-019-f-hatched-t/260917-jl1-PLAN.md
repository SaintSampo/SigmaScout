---
phase: quick-260917-jl1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/theme.css
  - apps/web/src/lib/bonusRp.test.ts
  - apps/web/src/lib/bonusRp.ts
  - apps/web/src/components/team/BonusRpDots.tsx
  - apps/web/src/components/team/BonusRpDots.test.tsx
  - apps/web/src/components/team/MatchTable.tsx
  - apps/web/src/components/event/EventMatchTable.tsx
autonomous: true
requirements: [QUICK-260917-jl1]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A predicted likely dot (p > 2/3) renders with the same ground and border as an actual earned dot (D-01)."
    - "A predicted toss-up dot (1/3..2/3) renders a 135deg diagonal hatch of the 30% alliance tint against the surface colour (D-02)."
    - "A predicted unlikely dot stays a full-size empty outline carrying its letter (D-03)."
    - "An actual missed dot renders as a 6px letterless pip that keeps the 14px slot, while unknown stays full-size dashed with its letter (D-04)."
    - "Every non-unknown dot's letter uses the site's primary text token (D-05)."
  artifacts:
    - apps/web/src/styles/theme.css
    - apps/web/src/lib/bonusRp.test.ts
  key_links:
    - "BonusRpDots.tsx's emitted class names <-> the theme.css rules that style them, pinned by the selector test in bonusRp.test.ts."
    - "Tier cutoffs in bonusRp.ts's bonusDotTier <-> the per-tier CSS rules (unchanged mapping, D-06)."
---

<objective>
Restyle the per-bonus RP dots to sketch 019 variant F (as revised 2026-09-17): predicted
likely draws exactly as an actual earned dot, predicted toss-up draws a diagonal hatch,
an actual "not earned" shrinks to a letterless pip, and one dark letter replaces the
per-alliance letter inks.

Purpose: at 14px the shipped three tiers (empty / 25% tint / 60% fill) are too close to
tell apart, and the actual earned dot sits between them.
Output: updated theme.css rules and tokens, extended selector pins, refreshed comments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/sketches/019-bonus-dot-tier-contrast/README.md
@apps/web/src/styles/theme.css
@apps/web/src/lib/bonusRp.test.ts
@apps/web/src/components/team/BonusRpDots.tsx

Facts already established, do not re-derive:
- The tossup/likely/unlikely CSS rules live at theme.css ~701-724; the dot base, earned,
  missed and unknown rules at ~652-732; the tier token block in `@theme` at ~63-86.
- The site's primary text token is `--color-text-primary` (theme.css line 28) and the
  surface token is `--color-bg-surface` (line 21). The 30% tints already exist as
  `--alliance-red-soft` / `--alliance-blue-soft` (lines 48, 50).
- A repo-wide grep over apps/ and packages/ shows the six tokens at theme.css 81-86 have
  NO consumer outside theme.css itself, so the deletion condition in the task is met.
- `BonusRpDots.tsx` already emits `bonus-dot--missed` for actual missed dots only
  (predicted dots get `bonus-dot--predicted` plus a tier class), so the pip needs no new
  marker class and no component logic change.
- `BonusRpDots.test.tsx` asserts each dot's `textContent` is its letter, so the pip must
  keep its letter in the DOM and hide it visually only.
- The sketch's reference rendering is `index.html` lines 92-106 (`.vF`, `.dot.fa`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle the dot rules and retire the superseded tokens</name>
  <files>apps/web/src/lib/bonusRp.test.ts, apps/web/src/styles/theme.css</files>
  <behavior>
    - The existing pin "theme.css carries a rule for every tier x side combination" keeps
      passing unchanged against the rewritten rules.
    - New pin, same style: theme.css contains a rule selecting the actual-missed pip
      (`.bonus-dot--missed`) that sets a 6px width and 4px side margins.
    - New pin, same style: for each side, the toss-up rule's declaration block contains
      `repeating-linear-gradient` and that side's `-soft` token.
    - New pin, same style: for each side, the likely rule's declaration block names that
      side's `-soft` token, i.e. the same ground an actual earned dot uses.
  </behavior>
  <action>
    Write the four pins above into bonusRp.test.ts first, alongside the existing selector
    pin and reusing its `findUpward` helper and read of the stylesheet, and confirm the
    three new ones fail. Then change theme.css:

    (a) Predicted likely (D-01): give it the same ground and border colour an actual
    earned dot of that side uses, via the existing 30% soft custom property. Do not
    introduce a second name for that colour.

    (b) Predicted toss-up (D-02): ground becomes
    `repeating-linear-gradient(135deg, var(--alliance-{side}-soft) 0 2px, var(--color-bg-surface) 2px 4px)`.
    Set `background-image` (and reset `background-color` to transparent) or `background`
    consistently across both sides; whichever you pick, both sides match.

    (c) Predicted unlikely (D-03): unchanged geometry and transparent ground; only its
    letter colour changes under (e). The actual earned dot keeps its 30% ground.

    (d) Actual missed pip (D-04): add a rule for `.bonus-dot--missed` setting
    `width: 6px; height: 6px; margin: 0 4px;` so the drawn mark plus margins still spans
    the 14px slot and no row reflows, keeping the 1px alliance border it already has.
    Hide the letter with `font-size: 0` rather than removing it, so the element's
    `title`/`aria-label` still names the bonus and the DOM text stays intact. This rule
    must NOT match `.bonus-dot--unknown` (still full size, dashed, with its letter) and
    must NOT match any predicted dot.

    (e) One letter colour (D-05): every non-unknown dot rule — earned, missed, and all
    three predicted tiers, both sides — sets `color: var(--color-text-primary)`. Lift it
    to a shared rule if that reads cleaner than repeating it. `.bonus-dot--unknown` keeps
    its muted dashed colours untouched.

    (f) Delete the six custom properties defined in the `@theme` block at lines 81-86 —
    the 60%-opacity pair, the 25%-opacity faint pair, and the per-alliance letter-ink pair
    — now that nothing references them. Rewrite that block's comment (lines 63-86) to
    describe what actually ships: likely = the same 30% ground as an actual earned dot,
    toss-up = a 135deg hatch of that same 30% tint against the surface, unlikely = outline
    only, one dark letter everywhere. Do not name the removed custom properties anywhere in
    the replacement comment. Record in it the measured contrast of `--color-text-primary`
    on each 30% ground composited on white — compute it yourself; expect approximately
    11.0:1 on red (composited #F4BEBE) and 11.5:1 on blue (composited #BED0F9), and about
    17.9:1 on the bare surface between the hatch stripes. If your figures differ from those,
    trust your own and note the composited hexes you used.

    (g) Cutoffs, `bonusDotTier`, `data-tier` and the tooltip percentage are untouched
    (D-06). Every colour stays a custom property; no literal colour value in any rule.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/lib/bonusRp.test.ts src/components/team/BonusRpDots.test.tsx</automated>
    <automated>grep -c 'dot-ink' apps/web/src/styles/theme.css   # expect 0</automated>
    <automated>grep -cE '\-\-alliance-(red|blue)-fill' apps/web/src/styles/theme.css   # expect 0</automated>
  </verify>
  <done>All four pins pass; the three greps print 0, 0; no literal colour appears in the
  `.bonus-dot` rules.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the stale descriptions and run full verification</name>
  <files>apps/web/src/lib/bonusRp.ts, apps/web/src/components/team/BonusRpDots.tsx, apps/web/src/components/team/BonusRpDots.test.tsx, apps/web/src/components/team/MatchTable.tsx, apps/web/src/components/event/EventMatchTable.tsx</files>
  <action>
    Find every comment or doc string describing the superseded look and rewrite it to match
    what now ships. Known locations: `bonusRp.ts`'s `BonusDotTier` type comment and
    `bonusDotTier`'s doc block (they describe an "empty, faintly tinted, or solid" read and
    a solid top tier); `BonusRpDots.tsx`'s component doc block (it says an actual dot is
    "solid when earned and hollow when not" and describes the middle tier as faintly
    tinted); the describe-block and header prose in `BonusRpDots.test.tsx`. Then grep
    MatchTable.tsx and EventMatchTable.tsx for the same vocabulary and fix any hit; if
    neither mentions it, leave them alone.

    New wording should say: an actual earned dot and a predicted likely dot draw the same
    30% alliance ground, a predicted toss-up draws a diagonal hatch of that tint, a
    predicted unlikely draws an outline only, an actual "not earned" draws a small
    letterless pip, and `unknown` stays dashed and muted. Keep the existing explanation of
    WHY tiers beat a continuous fill and why `unknown` is not `missed`. No decision ids
    (D-01 and the like) and no quick-task id in any source comment. Do not restate the
    numeric contrast figures here; they live in theme.css only.

    Then run the full verification below from the worktree root and from apps/web. If the
    web typecheck reports a missing `routeTree.gen`, run `npx vite build` in apps/web once
    first, then re-run it. Never wrap a command in `timeout` and never route through pnpm.
    Judge each run by its printed output, not by its exit code.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run</automated>
    <automated>npx vitest run</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Both vitest runs report zero failures (apps/web ~77 files, root ~167 — a smaller
  root count means the run was scoped wrong); both typechecks print no errors; no comment
  in the five files still describes a 60% fill, a 25% tint, or a hollow "not earned" dot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Presentation-only change: CSS declarations, custom-property deletions, and comments. No new input, dependency, or data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-jl1-01 | Information Disclosure | BonusRpDots accessible names | low | mitigate | The pip hides its letter with `font-size: 0`, never `display: none` or removal, so the bonus name survives in `title`/`aria-label` and in the DOM text the tests pin. |
| T-jl1-02 | Tampering | none | low | accept | No package installs, no network access, no secret read. `.env` is never opened. |
</threat_model>

<verification>
- `npx vitest run` green from apps/web and from the worktree root.
- `npx tsc --noEmit` green at root and against apps/web/tsconfig.json.
- The dot rules reference only custom properties.
- The orchestrator performs the browser check after merge; no visual task here.
</verification>

<success_criteria>
All five must-have truths hold, the six superseded tokens are gone, and no comment in the
touched files still describes the superseded look.
</success_criteria>

<output>
Create `.planning/quick/260917-jl1-bonus-rp-dots-per-sketch-019-f-hatched-t/260917-jl1-SUMMARY.md` when done
</output>
