# Quick Task 260904-2i9: move the pin, then republish - Context

**Gathered:** 2026-09-04
**Status:** Ready for planning

<domain>
## Task Boundary

`vpr@7.0.0+rolling-2026-09.json` is promoted and committed (quick task `260904-100`) but nothing
reads it: `PROMOTED_VPR_VERSION_PATH` still resolves to `tuned-2026-08`. Move the pin, re-run the
harness, and republish artifacts to R2.

**The push already happened and is verified.** 54 commits are on `main`, the Cloudflare Pages
deploy succeeded, and the live site returns 200. Live artifacts are still 5.0.0 and DO carry
`seasonLabel`; the newly deployed client treats that field as optional, so it reads both today's
artifacts and the post-republish shape. That ordering was mandatory — `v1/compare/{year}.json` is
NOT version-scoped, so a republish overwrites it live, and the previously deployed client REQUIRED
`seasonLabel`.

</domain>

<decisions>
## Implementation Decisions — LOCKED

### D-1 — SIX copies of the pin path exist. Five move together; ONE deliberately does not.

Verified 2026-09-04:

| file | constant | action |
|---|---|---|
| `packages/harness/cli.ts:153` | `PROMOTED_VPR_VERSION_PATH` | **move** |
| `packages/harness/manifests.ts:224` | `PROMOTED_VPR_VERSION_PATH` | **move** |
| `packages/harness/selectionProvenance.ts:53` | `PROMOTED_VPR_VERSION_PATH` | **move** |
| `scripts/measureRewindGap.ts:416` | `PROMOTED_VPR_VERSION_PATH` | **move** |
| `packages/harness/fixtures/extract-digest-slice.ts:65` | `DEFAULT_VERSION_PATH` | **move** |
| `packages/harness/manifests.test.ts:32` | (test mirror) | **move** |
| `packages/harness/tune.ts:1341` | `INCUMBENT_VERSION_PATH` | **DO NOT MOVE** |

**`tune.ts` stays on `tuned-2026-08` and this is not an oversight.** It is the D-T7 acceptance
BASELINE. All ten recorded verdicts were measured against `tuned-2026-08`; repointing it would
silently redefine what "the incumbent" meant in results already written down. Add a comment saying
exactly that, so the next person does not "fix" the inconsistency.

**This is the duplicated-fact pattern that produced every green-but-wrong defect this session.**
Five constants that must agree, updated by hand, with each file internally consistent either way —
a missed one gives a system where the scorer, the manifest, the eligibility flag and the rewind
measurement each believe a different set is live, very likely with a green suite. Strongly prefer
collapsing them to ONE exported constant over updating five in place. If a genuine import cycle
forbids that for a specific file, say which and why, and add a test pinning agreement across all
of them.

### D-2 — Publish range stays `--seasons 2022-2026`

Eligibility now derives from the CORPUS, not the CLI range (`260903-n2o` D-4), so the same range
yields correct per-season flags. 2019/2020 are selection-only and D-5 keeps them off Compare
regardless. Publishing them would create team and event pages for those years — a product
expansion, not part of this republish. Recorded as a separate open question, not done here.

### D-3 — Expected live changes, so an unexpected one is visible

After the republish:

- manifest `vpr`: `5.0.0+tuned-2026-08` -> `7.0.0+rolling-2026-09`
- `v1/compare/{year}.json`: `seasonLabel` GONE (field deleted from `ScoreSlice`)
- `headlineEligible`: **2022 becomes true**; 2025/2026 stay true; 2023/2024 stay false
- every published figure changes for 2022 and 2026 (new parameter sets); 2023/2024/2025 keep the
  incumbent's params but their numbers still move, because season carry threads through 2022

**Anything else changing is a signal, not noise.**

### D-4 — `apps/web/src/routes/__fixtures__/compare-*.json` MUST change this time

Every prior task in this arc gated on those fixtures being byte-identical. **This is the republish
they were waiting for.** A diff is the success signal. But the diff must be confined to D-3's list
— check it rather than accepting it wholesale.

### Claude's Discretion

- Whether the harness re-run and the publish are one command or two.
- Where the single shared pin constant lives, if D-1's collapse is taken.

</decisions>

<specifics>
## Specific Ideas

**Order of operations:**
1. Move the pin (D-1), suite green.
2. Full harness re-run for the accuracy record.
3. `pnpm publish:seasons` — writes to PRODUCTION R2, roughly 56k PUTs / ~25 min.
4. Re-commit the changed fixtures (D-4), after checking the diff.
5. Verify live: manifest version, `seasonLabel` absent, `headlineEligible` true for 2022.

**Credentials:** present and correct (`R2_ACCESS_KEY_ID` len 32, `R2_SECRET_ACCESS_KEY` len 64).
Use `tsx --env-file=.env` — the established pattern. **Never read, echo, log, or interpolate
`.env`'s contents** (CLAUDE.md's secrets rule; it exists because it was broken once).

**Out of scope, follow-ups each:** re-measuring SC-3, the rewind gap and the baseline fingerprint;
rewriting `docs/models/sigma1-tuning-results.md`; the manual `docs/publish-budget.md` transcription;
the 2027 preseason `--seasons` run.

**Known traps** (project memory): never `timeout <n> pnpm <cmd>` — swallows output, exits 0. Run
`npx vitest run` from the REPO ROOT; `npx tsc --noEmit` separately in `apps/web`. A quiet log during
a long run is NOT evidence of death — check process memory and output-file growth.

</specifics>

<canonical_refs>
## Canonical References

- `data/algorithm-versions/vpr@7.0.0+rolling-2026-09.json` — the target
- `.planning/todos/pending/retune-sigma1-rolling-origin.md` — the RESULTS section
- `.planning/quick/260904-100-.../260904-100-SUMMARY.md` — what built the representation
- `packages/harness/pageArtifacts.ts:154` — `v1/compare/{year}.json`, the unversioned key

</canonical_refs>
