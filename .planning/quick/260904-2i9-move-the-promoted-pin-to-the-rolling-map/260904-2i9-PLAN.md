---
quick_id: 260904-2i9
phase: quick-260904-2i9
slug: move-the-promoted-pin-to-the-rolling-map
date: 2026-09-04
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: false
depends_on: []
source: .planning/quick/260904-2i9-move-the-promoted-pin-to-the-rolling-map/260904-2i9-CONTEXT.md
requirements: [D-1, D-2, D-3, D-4]
files_modified:
  - packages/harness/promotedVersionPath.ts
  - packages/harness/cli.ts
  - packages/harness/promotedOverrides.test.ts
  - packages/harness/cli.promotedStaleness.test.ts
  - packages/harness/tune.ts
  - packages/harness/seasonParamSets.test.ts
  - packages/harness/selectionProvenance.ts
  - packages/harness/selectionProvenance.test.ts
  - packages/harness/publish.test.ts
  - packages/harness/manifests.ts
  - packages/harness/manifests.test.ts
  - packages/harness/fixtures/extract-digest-slice.ts
  - scripts/measureRewindGap.ts

estimate:
  tokens: 70000
  raw_tokens: 140000
  tasks: 5
  confidence: high

must_haves:
  truths:
    - "Every harness/publish path that resolves `vpr` loads `vpr@7.0.0+rolling-2026-09.json` (D-1)"
    - "Exactly ONE editable constant decides which promoted `vpr` file is live; no second copy of that path can be hand-edited into disagreement (D-1)"
    - "`tune.ts`'s `INCUMBENT_VERSION_PATH` still resolves `tuned-2026-08`, and a comment says that is the D-T7 acceptance baseline for ten already-recorded verdicts (D-1)"
    - "`warnIfNewerPromotedVpr` emits nothing for the live pin, and a test holds it there"
    - "After the orchestrator's republish, live R2 shows manifest `7.0.0+rolling-2026-09`, no `seasonLabel`, and `headlineEligible` true for 2022 (D-3)"
  artifacts:
    - packages/harness/promotedVersionPath.ts
  key_links:
    - "`promotedVersionPath.ts` -> `cli.ts`'s `applyPromotedOverrides` (what RUNS)"
    - "`promotedVersionPath.ts` -> `manifests.ts`'s `buildAlgorithmsManifest` (what is PUBLISHED)"
    - "`promotedVersionPath.ts` -> `selectionProvenance.ts` (what the eligibility flag CLAIMS)"
    - "`promotedVersionPath.ts` -> `scripts/measureRewindGap.ts` (what the rewind gap MEASURES)"
    - "`selectionProvenance.test.ts`'s independently-typed literal -> the shared constant (the single agreement pin)"
  prohibitions:
    - "The executor never runs `pnpm publish:seasons`, `pnpm promote`, or any tuning search"
    - "The executor never touches `apps/web/src/routes/__fixtures__/compare-*.json` (D-4 — those change during the ORCHESTRATOR's publish step)"
    - "The executor never reads, echoes, logs, or interpolates `.env`'s contents (`.claude/CLAUDE.md` secrets rule)"
    - "`packages/harness/tune.ts`'s `INCUMBENT_VERSION_PATH` is not repointed"
---

<objective>
`vpr@7.0.0+rolling-2026-09.json` is promoted and committed, but nothing reads it — five
independent copies of the promoted-version path still resolve `tuned-2026-08`, and a sixth
(`tune.ts`) deliberately must not move. Collapse the five to ONE exported constant in a new
leaf module, point that constant at `rolling-2026-09`, and fix the three tests that had the
old pin's facts written into them.

Purpose: this is the duplicated-fact pattern that produced every green-but-wrong defect this
session. Five constants that must agree, each file internally consistent either way, means a
missed copy gives a system where the scorer, the manifest, the eligibility flag and the rewind
measurement each believe a different parameter set is live — plausibly with a green suite. One
constant makes that state unrepresentable rather than merely tested-for.

Output: `packages/harness/promotedVersionPath.ts` (new leaf module), five call sites importing
it, three corrected test files, one new staleness canary, and a documented republish procedure
the ORCHESTRATOR executes by hand against production R2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260904-2i9-move-the-promoted-pin-to-the-rolling-map/260904-2i9-CONTEXT.md
@.claude/CLAUDE.md

Findings verified at planning time, 2026-09-04 — the executor does not need to re-derive these:

**1. There is no import cycle, for any of the five.** `packages/core/algorithms/sigma1/params.ts`
(the home of `SIGMA1_CODE_VERSION`) imports only its own sigma1 siblings, `../carryover.js`, and
`zod` — nothing from `packages/harness`. A new harness module importing only `node:path` and
`params.js` is therefore a genuine leaf that every one of the five can import: `cli.ts`,
`manifests.ts` (which already imports `cli.ts`), `selectionProvenance.ts` (which `cli.ts` imports —
the direction that made a `cli.ts` import impossible), `scripts/measureRewindGap.ts`, and
`fixtures/extract-digest-slice.ts`. **All five collapse.** No file genuinely cannot import it.

**2. `selectionProvenance.ts`'s recorded cycle rationale is obsolete, not wrong.** Its header says
importing `PROMOTED_VPR_VERSION_PATH` back from `cli.ts` would close a cycle — true, and that is
exactly why the shared constant goes in a THIRD module neither of them owns. That paragraph must
be rewritten in Task 2, not left standing as a justification for a mirror that no longer exists.

**3. `warnIfNewerPromotedVpr` self-resolves.** Committed `provenance.promotedAt` values:
`rolling-2026-09` = `2026-09-04T05:33:17.863Z`, `tracer-check` = `2026-09-03T17:29:19.185Z`,
`tuned-2026-08` = `2026-09-03T17:29:12.725Z`. Once pinned to `rolling-2026-09` the pin IS the
newest, so the scan finds no later candidate and returns before logging. **No behavior change is
needed for the warning to go quiet.** Two things still follow from the move, both in scope:
its warning message hardcodes the instruction "edit PROMOTED_VPR_VERSION_PATH in
packages/harness/cli.ts", which names the wrong file after the collapse (Task 1); and the fact
that the pin is the newest is worth pinning as a test, since "a promotion landed and nothing
moved" is the precise defect this whole task exists to fix (Task 4).

**4. THREE more copies exist than `260904-2i9-CONTEXT.md`'s D-1 table lists.** All three encode the
OLD pin's facts and go red on the move — which is the correct, loud outcome, but the executor must
expect them rather than debug them:
  - `packages/harness/promotedOverrides.test.ts:48` — `PROMOTED_VERSION_IDENTITY =
    ${SIGMA1_CODE_VERSION}+tuned-2026-08`, asserted at lines 385/441/442 against
    `applyPromotedOverrides`' output. A version IDENTITY, not a path. Task 1.
  - `packages/harness/selectionProvenance.test.ts:19-23` —
    `INDEPENDENTLY_RESOLVED_VPR_VERSION_PATH`, deliberately independent (see Task 2). Task 2.
  - `packages/harness/selectionProvenance.test.ts:58-64` — asserts `vpr`'s selected-on set equals
    the committed file's flat `provenance.tuneSeasons`. The rolling file has NO
    `provenance.tuneSeasons` at all (its provenance carries only `corpusIdentity`, `promotedAt`,
    `objectiveDefinition`), and its per-season answers genuinely differ by season. Task 2.

**5. Two tests that look like copies are NOT, and stay put.**
`packages/harness/seasonParamSets.test.ts:27` mirrors `tune.ts`'s incumbent (stays on
`tuned-2026-08`, gets a comment in Task 1), and `packages/harness/promoteOverride.test.ts:601`
reads the tuned file as a flat-params carry SOURCE fixture for `buildParamSetsBySeason` (stays,
untouched).

**6. `publish.test.ts`'s eligibility assertion survives; only its docstring is stale.** The rolling
map records `selectedOnSeasons` `[2022,2023,2024]` for both 2024 and 2025, so "2024 NOT
headline-eligible, 2025 IS" still holds. Its comment block at ~line 2215 describes a flat
`provenance.tuneSeasons` read that no longer exists. Task 2.

**7. No season can throw, and the digest fixture does not need regenerating.** The corpus holds
event years `2019, 2020, 2022, 2023, 2024, 2025, 2026`; the rolling file's `paramSetsBySeason`
covers exactly `2019, 2020, 2022, 2023, 2024, 2025, 2026`. `resolveParamSets(...).forSeason(s)`
throws for an uncovered season, so this exact agreement is what keeps every season loop safe — it
is the highest-consequence NEW failure mode (the live pin becomes a per-season file for the first
time) and Task 3 gates on it explicitly. Separately, both files record the IDENTICAL digest slice
(`sliceSeason` 2022, `["2022alhu","2022azfl","2022azva"]`, 265 matches) and `digest.test.ts` matches
fixture to version by slice, never by version string — so `packages/harness/fixtures/digest-slice.json`
is correct as committed. **Do not regenerate it.**

Relevant sources, read as needed rather than up front:
@packages/harness/cli.ts
@packages/harness/selectionProvenance.ts
@packages/harness/manifests.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one shared constant decides what actually runs</name>
  <files>packages/harness/promotedVersionPath.ts, packages/harness/cli.ts, packages/harness/promotedOverrides.test.ts, packages/harness/tune.ts, packages/harness/seasonParamSets.test.ts</files>
  <read_first>packages/harness/cli.ts (lines 128-160 and 220-300), packages/harness/promotedOverrides.test.ts (lines 28-52 and 375-450), packages/harness/tune.ts (lines 1330-1345)</read_first>
  <action>
Create `packages/harness/promotedVersionPath.ts` as a genuine leaf: it imports `join` from
`node:path` and `SIGMA1_CODE_VERSION` from `../core/algorithms/sigma1/params.js`, and NOTHING
else — no corpus, no schema, no sibling harness module. Import cycles are what forced five
mirrors to exist; a leaf that imports nothing importable-from is the property that makes them
collapsible, and any future import added here can reopen the cycle it exists to avoid. Say that
in the module header.

Export two constants: `ALGORITHM_VERSIONS_DIR`, the committed version-file directory
(`data/algorithm-versions`), and `PROMOTED_VPR_VERSION_PATH`, built by joining that directory with
a filename derived from `SIGMA1_CODE_VERSION` and the param-set name `rolling-2026-09` — the same
derived-not-retyped construction every one of the five call sites already used, so the next
`SIGMA1_CODE_VERSION` bump still moves the pin by construction (per D-1). Document in the
constant's own doc comment that this is now the SINGLE place a promoted-version re-pin is edited,
that `tune.ts`'s incumbent is a deliberate exception, and that the previous value was
`tuned-2026-08`.

In `cli.ts`: delete both module-private constants (`PROMOTED_VPR_VERSION_PATH` at ~153 and
`ALGORITHM_VERSIONS_DIR` at ~155) and import them from the new module instead. Preserve the
existing re-pin history comment above the old constant by MOVING it into the new module rather than
deleting it — it records four prior re-pins and why derivation replaced hand-editing. Then correct
`warnIfNewerPromotedVpr`'s final `console.log` message: its closing instruction currently names
`PROMOTED_VPR_VERSION_PATH in packages/harness/cli.ts`, which is the wrong file once the constant
moves; point it at the new module's path. Change nothing else about that function — per finding 3
it goes quiet on its own once the pin is the newest committed file.

In `promotedOverrides.test.ts`: `PROMOTED_VERSION_IDENTITY` (~line 48) is a hand-typed version
identity whose param-set half is now wrong. Replace the retyped literal with a value READ from the
pinned file itself — parse `PROMOTED_VPR_VERSION_PATH` through `PromotedVersionSchema` (already
imported there) and take its own `version` field. What that test actually defends is unweakened
and stated in its own doc comment: that `vpr-defaults` and `vpr-adapt` resolve to identities
DISTINCT from the promoted one. Those two suffixes stay literal. Update the doc comment to say the
promoted half is now read rather than retyped, and why.

In `tune.ts`: do NOT repoint `INCUMBENT_VERSION_PATH` (~line 1341). Extend its existing doc comment
with an explicit do-not-move paragraph: this stays on `tuned-2026-08` deliberately because it is
the D-T7 acceptance BASELINE against which ten verdicts are already recorded, so repointing it
would silently redefine what "the incumbent" meant in results already written down — it is not an
oversight left behind by the pin move, and it must not be "fixed" into agreement with
`promotedVersionPath.ts`. Add a one-line comment above `seasonParamSets.test.ts:27`'s
`INCUMBENT_VERSION_PATH` saying it mirrors `tune.ts`'s incumbent for the same reason and likewise
does not follow the live pin.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/promotedOverrides.test.ts packages/harness/cli.promotedStaleness.test.ts</automated>
  </verify>
  <done>`applyPromotedOverrides` resolves the `vpr` module to the committed `rolling-2026-09` file's own `version` string, asserted end to end by `promotedOverrides.test.ts` reading that file rather than a retyped literal. `cli.ts` holds no promoted-version path of its own. `tune.ts` still resolves `tuned-2026-08` and says in prose why it must stay there.</done>
  <reversibility rating="reversible">The pin is one constant in one leaf module; reverting is a one-line edit plus the three test reads that follow it.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Retire selectionProvenance's deliberate mirror and its flat-provenance expectations</name>
  <files>packages/harness/selectionProvenance.ts, packages/harness/selectionProvenance.test.ts, packages/harness/publish.test.ts</files>
  <read_first>packages/harness/selectionProvenance.ts (lines 1-90), packages/harness/selectionProvenance.test.ts (lines 1-90), packages/harness/publish.test.ts (lines 2205-2230)</read_first>
  <action>
In `selectionProvenance.ts`: delete the module-private `ALGORITHM_VERSIONS_DIR` and
`PROMOTED_VPR_VERSION_PATH` (~lines 44-53) and import both from `./promotedVersionPath.js`. Then
rewrite the header paragraph that justifies the mirror. That paragraph's reasoning is correct and
now obsolete: importing the constant back from `cli.ts` really would close a cycle (`cli.ts`
imports `aggregateScoresForRun` from here), which is precisely why the shared constant lives in a
third leaf module neither file owns. Leaving the old justification standing would read as a live
reason not to share. Replace it with the resolved version, and note that this closes the gap the
old text explicitly said the mirror still carried — the same structural-sharing argument the file
already makes one paragraph later for `vpr-adapt` delegating to `resolveOnSearchWinner`, now
extended to `vpr`.

In `selectionProvenance.test.ts`, `INDEPENDENTLY_RESOLVED_VPR_VERSION_PATH` (~line 19-23) is the
ONE place that keeps a hand-typed literal, and that is deliberate — its doc comment states its
whole purpose is proving the module reads the same committed file the harness loads, "rather than
merely asserting a value this test also computed the same (wrong) way." Importing the shared
constant here would make that assertion tautological and delete the only independent witness in
the repo. So: retype its literal to name `rolling-2026-09`, keep it independent, and ADD one
assertion that it strictly equals `PROMOTED_VPR_VERSION_PATH` imported from
`./promotedVersionPath.js`. That single agreement pin is what protects the collapse — it goes red,
naming both values, if the shared constant is ever moved without this witness following.

Then fix the first case (~line 58-64), which asserts `vpr`'s selected-on seasons deep-equal the
committed file's flat `provenance.tuneSeasons` for every season asked. The rolling file carries no
`provenance.tuneSeasons` at all, and its per-season answers genuinely differ. Rewrite it to read
each season's expectation RAW from the parsed file's `paramSetsBySeason` map — the map entry's own
`selectedOnSeasons`, indexed by the season's string key — rather than through `resolveParamSets`,
which is the code path under test. Assert 2019's answer and 2026's answer each match their own
map entry, and assert the two answers DIFFER from each other: a per-season file's whole point is
that one flat list can no longer stand in for every season, and only a differing pair proves the
function is resolving per season rather than returning one set for all of them. Retitle the case,
dropping its "today's legacy params file" qualifier.

In `publish.test.ts`, the comment block at ~line 2210-2225 explains its expectation is pinned to
the committed file's flat `provenance.tuneSeasons` of `[2022, 2023, 2024]`. The assertion itself
("2024 NOT headline-eligible, 2025 IS") is unchanged and still correct, because the rolling map
records `[2022, 2023, 2024]` as the selected-on set for BOTH of those seasons' governing param
sets. Update only the prose: name the per-season source it now reads, and keep the existing
sentence saying a future re-tune that changes the selected-on set is SUPPOSED to redden this test.
Do not touch the assertions.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/selectionProvenance.test.ts packages/harness/publish.test.ts</automated>
  </verify>
  <done>`selectionProvenance.ts` owns no version path. Exactly one hand-typed promoted-version literal remains in the repo, in `selectionProvenance.test.ts`, and one assertion pins it to the shared constant. `vpr`'s selected-on answers are asserted per season, from the file's own map, and are proven to differ between two seasons.</done>
</task>

<task type="auto">
  <name>Task 3: Point the manifest, the rewind measurement and the fixture extractor at the shared constant</name>
  <files>packages/harness/manifests.ts, packages/harness/manifests.test.ts, scripts/measureRewindGap.ts, packages/harness/fixtures/extract-digest-slice.ts</files>
  <read_first>packages/harness/manifests.ts (lines 205-265), packages/harness/manifests.test.ts (lines 28-34), scripts/measureRewindGap.ts (lines 410-420), packages/harness/fixtures/extract-digest-slice.ts (lines 50-80)</read_first>
  <action>
Four call sites, one change each: delete the module-local constant and import from
`./promotedVersionPath.js` (or `../promotedVersionPath.js` / `../packages/harness/promotedVersionPath.js`
as the file's own depth requires).

`manifests.ts` (~lines 209-225): drop both `PROMOTED_VPR_VERSION_PATH` and its
`ALGORITHM_VERSIONS_DIR`, and replace the doc comment that explains why they were reimplemented
rather than imported — its premise was that `cli.ts`'s copies are module-private, which no longer
holds. Keep `warnIfNewerPromotedVpr` imported from `cli.ts` exactly as it is; only the two
constants move.

`manifests.test.ts` (~line 32): import the shared constant rather than keeping a derived mirror.
Its comment currently justifies deriving rather than hardcoding; the derivation argument is
satisfied more strongly by the import, and what the file's assertions actually defend — that the
manifest's id and version are READ from the committed file rather than written as literals at the
construction site — is untouched by where the path comes from. The independent-witness job now
belongs to `selectionProvenance.test.ts` alone (Task 2); say so in the replacement comment so the
next reader does not reintroduce a second mirror thinking this one was lost by accident.

`scripts/measureRewindGap.ts` (~line 416): its comment justifies duplication across "an isolation
boundary" by citing `replayRig.ts`'s precedent. A `node:path`-only leaf crosses no boundary —
replace the constant with the import and say the boundary argument does not apply to a leaf.

`fixtures/extract-digest-slice.ts` (~line 65): its `DEFAULT_VERSION_PATH` becomes the shared
constant (keep the local name if the `--version` override reads better that way, but its value must
come from the import). Its existing comment argues that only the PATH moves and the committed
`digest-slice.json` fixture does not need regenerating, because `digest.test.ts` matches fixture to
version by `sliceSeason` + `sliceEventKeys`, never by version string. That argument still holds and
was checked at planning time (finding 7) — extend the comment to record that the rolling file's
recorded slice is byte-identical on both fields and the match count. Do not regenerate the fixture.

Then run the one check that only exists because the live pin is a per-season file for the first
time: confirm the corpus's own event years and the rolling file's `paramSetsBySeason` keys are the
same set, so no season loop can hit `resolveParamSets`' uncovered-season throw. Report both lists in
the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/manifests.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>No file outside `promotedVersionPath.ts` constructs a live promoted-version path. `npx tsc --noEmit` is clean at the repo root, which is what proves the script and fixture imports resolve (neither is covered by a test). The corpus's event years and the rolling map's covered seasons are confirmed equal.</done>
</task>

<task type="auto">
  <name>Task 4: Pin the pin — staleness canary, repo-wide sweep, full suite</name>
  <files>packages/harness/cli.promotedStaleness.test.ts</files>
  <read_first>packages/harness/cli.promotedStaleness.test.ts (lines 1-70 and 130-160)</read_first>
  <behavior>
    - `warnIfNewerPromotedVpr(ALGORITHM_VERSIONS_DIR, PROMOTED_VPR_VERSION_PATH)`, called against
      the REAL committed directory and the REAL live pin, logs nothing at all.
    - The case fails loudly, naming the newer file, the day a promotion lands without the pin
      following it — which is exactly the defect this whole task exists to repair.
  </behavior>
  <action>
Add one case to `cli.promotedStaleness.test.ts`'s existing describe block, reusing the
`vi.spyOn(console, "log")` harness already in that file. Unlike every other case there, it runs
against the real `data/algorithm-versions` directory and the real `PROMOTED_VPR_VERSION_PATH`
imported from `promotedVersionPath.js`, and asserts the spy was never called.

Give it a doc comment carrying the escape hatch, because this case can be legitimately wrong:
`warnIfNewerPromotedVpr` deliberately WARNS rather than throws so that pinning an older version to
compare it against a newer one stays possible, and a test turns that soft signal into a hard CI
stop. So the comment must say that if a future pin deliberately targets a version that is not the
newest, this case is updated with a note explaining why — it is a speed bump on an unnoticed
re-pin, not a prohibition on a deliberate one.

Then sweep for anything the collapse missed. Two set-equality checks over `*.ts` under `packages/`
and `scripts/`, excluding `node_modules`:

  - Files still constructing a path from `SIGMA1_CODE_VERSION` and the old param-set name must be
    exactly `packages/harness/tune.ts`, `packages/harness/seasonParamSets.test.ts`, and
    `packages/harness/promoteOverride.test.ts` — the D-1 incumbent, its test mirror, and the
    flat-params carry-source fixture. Any fourth file is a copy that did not move.
  - Files naming the new param-set name must be exactly `packages/harness/promotedVersionPath.ts`,
    `packages/harness/selectionProvenance.test.ts` (the deliberate independent witness), and
    `packages/harness/baselineFingerprint.test.ts` (which already enumerated all three committed
    version filenames before this task). Any fourth file is a hand-typed copy that crept back in
    instead of importing.

Record both file lists verbatim in the SUMMARY — a bare pass/fail hides which member changed. Then
run the full suite from the REPO ROOT, not from `apps/web`: a root run covers ~167 files against
`apps/web`'s ~77, and that gap has hidden an eight-day red suite before. Also run `npx tsc --noEmit`
inside `apps/web` separately. Never wrap either in `timeout <n> pnpm ...` — that swallows all output
and exits 0.
  </action>
  <verify>
    <automated>npx vitest run && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>The full repo-root suite and both typechecks are green. Both sweep file lists match their expected sets exactly and are recorded in the SUMMARY. A future promotion that lands without a re-pin now reddens `cli.promotedStaleness.test.ts` by name.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: ORCHESTRATOR republishes to production R2 and verifies live</name>
  <precondition>Tasks 1-4 are committed and the repo-root suite is green — the pin must be on `rolling-2026-09` in git before any artifact is written to production R2, or the published manifest and the committed source disagree.</precondition>
  <action>STOP. The executor performs no part of this task — it runs no `publish:seasons`, no `promote`, and no tuning search, and it does not touch `apps/web/src/routes/__fixtures__/compare-*.json`. Hand control back to the orchestrator, which executes the four-step procedure in `<how-to-verify>` below by hand against production R2.</action>
  <what-built>
Every harness and publish path now resolves `vpr@7.0.0+rolling-2026-09.json` through one shared
constant. Nothing has been published — live R2 still serves `5.0.0+tuned-2026-08` artifacts.
  </what-built>
  <how-to-verify>
**This step is deliberately NOT an executor action.** It writes roughly 56k objects to PRODUCTION
R2 over ~25 minutes and overwrites `v1/compare/{year}.json`, which `pageArtifacts.ts:154` shows is
NOT version-scoped — a mistake here is live immediately and has no versioned predecessor to fall
back to. The orchestrator runs it under supervision.

**Step 1 — full harness re-run, for the accuracy record.** Run the harness across
`--seasons 2022-2026` before publishing, so the recorded accuracy figures come from the same
parameter sets the artifacts will. Use the established `tsx --env-file=.env` invocation. Do not
read, echo, or interpolate `.env` — let the tool read it.

**Step 2 — publish.** `pnpm publish:seasons` with `--seasons 2022-2026` (D-2: the range stays
2022-2026 — eligibility now derives from the CORPUS, not the CLI range, so this range yields
correct per-season flags; 2019/2020 are selection-only and publishing them would create team and
event pages for those years, which is a product expansion recorded as a separate open question).
Roughly 56k PUTs, ~25 minutes. A quiet log is NOT evidence the run died — check output-file growth
and process memory before concluding anything. Never `timeout <n> pnpm ...`.

**Step 3 — check the fixture diff, then commit it.** `apps/web/src/routes/__fixtures__/compare-*.json`
MUST change this time (D-4) — every prior task in this arc gated on those being byte-identical, and
this is the republish they were waiting for. A diff is the success signal. But confine the diff to
D-3's list before committing it:
  - manifest `vpr`: `5.0.0+tuned-2026-08` -> `7.0.0+rolling-2026-09`
  - `seasonLabel` GONE from `v1/compare/{year}.json` (the field was deleted from `ScoreSlice`)
  - `headlineEligible`: **2022 becomes true**; 2025/2026 stay true; 2023/2024 stay false
  - every published figure moves for 2022 and 2026 (new parameter sets), and 2023/2024/2025 keep
    the incumbent's params but their numbers still move, because season carry threads through 2022

**Anything else changing is a signal, not noise.** Investigate before committing.

**Step 4 — verify live.** Fetch the deployed manifest and confirm the `vpr` entry reads
`7.0.0+rolling-2026-09`; fetch a `v1/compare/{year}.json` and confirm `seasonLabel` is absent; and
confirm `headlineEligible` is true for 2022.

**Explicitly NOT part of this step, each a follow-up:** re-measuring SC-3, the rewind gap and the
baseline fingerprint; rewriting `docs/models/sigma1-tuning-results.md`; the manual
`docs/publish-budget.md` transcription (`publish:seasons` prints its summary but does not write
that file); and the 2027 preseason `--seasons` run.
  </how-to-verify>
  <instructions>Run the four steps in `<how-to-verify>` strictly in order — the harness re-run precedes the publish so the recorded accuracy figures come from the same parameter sets the artifacts do, and the fixture diff is inspected against D-3's list BEFORE it is committed. Abort at the first observed change that D-3 does not list, and investigate rather than committing it.</instructions>
  <verification>The deployed algorithms manifest's `vpr` entry reads `7.0.0+rolling-2026-09`; a live `v1/compare/{year}.json` carries no `seasonLabel` field; and `headlineEligible` is true for 2022, true for 2025/2026, false for 2023/2024.</verification>
  <resume-signal>Type "published" once live verification passes, or describe what diverged from D-3's list</resume-signal>
  <reversibility rating="costly">`v1/compare/{year}.json` is not version-scoped, so the republish overwrites the live object with no versioned predecessor — recovery means re-publishing from the previous pin, not restoring a key.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed version file -> every replay driver, the manifest, and the eligibility flag | One constant decides what the site predicts and what its accuracy page is allowed to claim |
| executor -> production R2 | A publish is live immediately and `v1/compare/{year}.json` carries no version scope |
| `.env` -> any command or transcript | Live TBA and R2 credentials; the git-side protections pass while a secret escapes into a transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2i9-01 | Tampering | the five duplicated pin constants | critical | mitigate | Tasks 1-3 collapse all five into one leaf-module export, so a scorer/manifest/flag/rewind disagreement becomes unrepresentable rather than merely tested-for; Task 4's set-equality sweep names any file that did not move |
| T-2i9-02 | Repudiation | `tune.ts`'s `INCUMBENT_VERSION_PATH` | high | mitigate | Task 1 leaves it on `tuned-2026-08` and records in prose that it is the D-T7 baseline for ten already-recorded verdicts, so a future reader cannot "fix" the inconsistency and silently redefine what those verdicts compared against |
| T-2i9-03 | Spoofing | a promotion that lands without a re-pin | high | mitigate | Task 4's canary asserts `warnIfNewerPromotedVpr` stays silent against the real versions directory, converting today's soft console warning into a named CI failure, with a documented escape hatch for a deliberate older pin |
| T-2i9-04 | Tampering | production `v1/compare/{year}.json` | high | mitigate | Task 5 is a blocking human-action checkpoint, not an executor action; the D-3 expected-change list is checked against the fixture diff BEFORE the commit, and live keys are verified after |
| T-2i9-05 | Information disclosure | `.env` (TBA + R2 credentials) | high | mitigate | No task reads `.env`; Task 5's commands use `tsx --env-file=.env` so the tool reads it directly, per `.claude/CLAUDE.md`'s secrets rule (which exists because it was broken once, during Phase 4 plan 04-01) |
| T-2i9-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this plan — no new dependency appears in any task |
</threat_model>

<source_audit>
| Source | Item | Covered by |
|--------|------|------------|
| CONTEXT | D-1 — five copies move, `tune.ts` does not, collapse preferred | Tasks 1, 2, 3 (collapse); Task 1 (`tune.ts` guard comment); Task 4 (sweep) |
| CONTEXT | D-2 — publish range stays `--seasons 2022-2026` | Task 5, Step 2 |
| CONTEXT | D-3 — the four expected live changes, anything else is a signal | Task 5, Step 3 (diff check) and Step 4 (live verification) |
| CONTEXT | D-4 — the compare fixtures MUST change, confined to D-3's list | Task 5, Step 3; executor prohibited from touching them (`must_haves.prohibitions`) |
| CONTEXT | Discretion — one command or two for re-run + publish | Two, Task 5 Steps 1 and 2: the accuracy record must come from the same params the artifacts do, and a 25-minute production write earns its own confirmation point |
| CONTEXT | Discretion — where the shared constant lives | `packages/harness/promotedVersionPath.ts`, a `node:path` + `params.js` leaf (finding 1) |
| ORCHESTRATOR | Can the five collapse to one? Import cycles? | Yes, all five — finding 1; the cycle `selectionProvenance.ts` documents is real and is why a third leaf module owns the constant (Task 2) |
| ORCHESTRATOR | Any file that genuinely cannot import it? | None on cycle grounds. `selectionProvenance.test.ts` keeps its literal by DESIGN (an independent witness would be destroyed by importing) — Task 2 adds the agreement assertion D-1 asks for |
| ORCHESTRATOR | What must `warnIfNewerPromotedVpr` do once the pin is newest? | It self-resolves; finding 3. Only its message text needs correcting (Task 1), and Task 4 pins the now-silent behavior |
| CONSTRAINT | Publish is operational, not an executor action | Task 5 is `checkpoint:human-action`; `must_haves.prohibitions` bars the executor from `publish:seasons`, `promote`, and any tuning search |
| CONSTRAINT | Re-measurements, tuning-results rewrite, publish-budget, 2027 run | Explicitly listed as out of scope in Task 5's closing paragraph |

No unplanned items.
</source_audit>

<verification>
- `npx vitest run` from the REPO ROOT is green (~167 files — an `apps/web` run covers ~77 and has hidden a red suite for eight days before).
- `npx tsc --noEmit` is clean at the repo root AND separately inside `apps/web`.
- Files still constructing the old param-set path are exactly `tune.ts`, `seasonParamSets.test.ts`, `promoteOverride.test.ts`; files naming the new one are exactly `promotedVersionPath.ts`, `selectionProvenance.test.ts`, `baselineFingerprint.test.ts`.
- The corpus's event years equal the rolling file's `paramSetsBySeason` keys, so no season loop can hit the uncovered-season throw.
- `packages/harness/fixtures/digest-slice.json` is unchanged — the recorded slice is identical across both version files.
- `apps/web/src/routes/__fixtures__/compare-*.json` are unchanged by the EXECUTOR, and changed by the ORCHESTRATOR's publish within D-3's list.
- `git status --porcelain` shows only the intended source changes.
</verification>

<success_criteria>
One constant in one leaf module decides which promoted `vpr` file is live, and it names
`rolling-2026-09`; the scorer, the manifest, the eligibility flag and the rewind measurement can no
longer believe different parameter sets are live, because there is no longer a second value to
disagree with. `tune.ts` still resolves the incumbent and says in prose why it must. The staleness
warning is quiet, and a named test keeps it that way. After the orchestrator's supervised
republish, live artifacts read `7.0.0+rolling-2026-09`, carry no `seasonLabel`, and mark 2022
headline-eligible — with every other observed change accounted for against D-3's list.
</success_criteria>

<output>
Create `.planning/quick/260904-2i9-move-the-promoted-pin-to-the-rolling-map/260904-2i9-SUMMARY.md` when done.
</output>
</content>
</invoke>
