---
phase: quick-260913-l8q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Task 1: em dashes out of user-facing sentences
  - apps/web/src/components/compare/RpCalibrationSection.tsx
  - apps/web/src/components/districts/DistrictLocksTab.tsx
  - apps/web/src/components/districts/DistrictLocksTab.test.tsx
  - apps/web/src/components/event/EventMatchTable.tsx
  - apps/web/src/components/team/MatchTable.tsx
  - apps/web/src/components/event/StartMatchPicker.tsx
  - apps/web/src/components/methodology/acknowledgmentsContent.ts
  - apps/web/src/components/methodology/methodologyCardData.ts
  - apps/web/src/routes/event.$eventKey.tsx
  - packages/core/districts/qualification.ts
  - packages/core/districts/qualification.test.ts
  - packages/harness/pageArtifacts.ts
  - packages/harness/pageArtifacts.test.ts
  - scripts/publishDistricts.test.ts
  # Task 2: Locks tab cut line uses the lock verdicts' own pool
  - packages/core/districts/locks.ts
  - packages/core/districts/locks.test.ts
  - scripts/publishDistricts.ts
  - scripts/publishDistricts.test.ts
  # Task 3: Beta tag after the ribbon wordmark
  - apps/web/src/components/ribbon/Ribbon.tsx
  - apps/web/src/components/ribbon/Ribbon.test.tsx
autonomous: true
requirements:
  - 260913-l8q

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "No user-facing sentence, label or aria-label on the listed surfaces contains an em dash (U+2014); each was replaced by a comma, period, colon or parentheses, never an en dash or a double hyphen"
    - "Every lone em dash that means 'nothing here' still renders: DistrictLocksTab's unknown verdict, empty awards, missing event points and null cut line, and both match tables' Call-column badge glyph"
    - "The 2025fsc champ verdict cell reads 'special allocation, not modeled', and every test that quotes that note agrees"
    - "The Locks tab's 'Current cut line' is the point total of the last points-qualifying team in the SAME pool and slot count the lock verdicts use (award-qualified and prequalified teams removed, ranked award qualifiers subtracted from slots), so for 2026fnc the composed champ cut line is 231 (published today: 172) and the DCMP cut line is 76 (published today: 75)"
    - "In every composed district-year 2016-2020 and 2022-2026, no points-pool team with more points than its tier's cut line is 'eliminated', and no points-pool team with fewer points than the cut line is 'locked'"
    - "A smaller grey 'Beta' tag sits right after the ribbon wordmark on desktop and mobile, outside the home link, so the wordmark link's text is still exactly the Sigma glyph followed by igmaScout"
    - "Only this task's files land in its commits; nothing is published to R2, deployed or pushed"
  artifacts:
    - path: "packages/core/districts/locks.ts"
      provides: "cutLinePointsWithQualifiers, sharing one pool/pointsSlots derivation with computeLocksWithQualifiers"
      contains: "export function cutLinePointsWithQualifiers"
    - path: "scripts/publishDistricts.ts"
      provides: "buildDistrictArtifact computing both tiers' cut lines through cutLinePointsWithQualifiers"
      contains: "cutLinePointsWithQualifiers("
    - path: "packages/core/districts/qualification.ts"
      provides: "specialAllocationNote without an em dash"
      contains: "special allocation, not modeled"
    - path: "apps/web/src/components/ribbon/Ribbon.tsx"
      provides: "wordmark group with a sibling Beta span in the ribbon's muted ink"
      contains: "Beta"
  key_links:
    - from: "scripts/publishDistricts.ts buildDistrictArtifact"
      to: "packages/core/districts/locks.ts cutLinePointsWithQualifiers"
      via: "called once per tier with the same inputs and qualifier sets as that tier's computeLocksWithQualifiers call"
      pattern: "cutLinePointsWithQualifiers\\("
    - from: "insights.cmpCutLinePoints / insights.dcmpCutLinePoints"
      to: "apps/web/src/components/districts/DistrictLocksTab.tsx 'Current cut line'"
      via: "published district artifact (the orchestrator republishes districts; this plan does not)"
      pattern: "CutLinePoints"
---

<objective>
Three independent, user-reported fixes in one quick task:

1. Take em dashes (U+2014) out of user-facing sentences and labels, keeping every lone em dash that stands for "nothing here" (locked user decision: prose only).
2. Fix the Locks tab cut line. It is read off the raw district ranking at rank `slots`, while the lock verdicts use a narrowed pool (award-qualified and prequalified teams removed) and a narrowed slot count. So NC 2026's Championship tab shows 172 even though GearCats (6500) was eliminated at 216. The correct value is 231.
3. Add a grey, smaller "Beta" tag right after the ribbon wordmark.

Purpose: the cut line is a published number that contradicts the verdicts next to it, which is a published-honesty defect. The other two are copy and brand polish the user asked for.
Output: three commits (one per task). The orchestrator writes SUMMARY.md, updates STATE.md, republishes districts and ships the web app. None of that is in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@packages/core/districts/locks.ts
@scripts/publishDistricts.ts

Orchestrator findings are established. Do not re-research them.

## Execution hazards (read before touching anything)

- **One checkout, several concurrent Claude sessions, and no worktrees.** Before editing ANY file, run `git status --short -- <file>`. If the file already shows modifications you did not make, do NOT edit it. List it in your returned summary as deferred for the orchestrator. If a deferred file breaks a pair (for example a source string plus the test asserting it), defer the whole pair rather than committing half of it. Re-read each file right before editing it. Ribbon.tsx was changed by another session minutes before planning (d52e5ced).
- **Stage by explicit path only** (`git add <path> <path>`). Never use `git add -A`, `git add .` or `git add -u`. After each commit, run `git show --stat HEAD` and confirm only your files landed. If a foreign file slipped in, say so in the summary. Do not rewrite history.
- **Tests:** from the repo root, run `npx vitest run <paths>`. Never use `timeout ... pnpm ...`, which swallows output and exits 0. Verify by reading the printed pass/fail counts, not the exit code.
- **Typecheck both projects:** `npx tsc --noEmit -p tsconfig.json` (root) AND `npx tsc --noEmit -p apps/web/tsconfig.json` (root tsc misses web errors). Failures in files other sessions are editing are not yours: note them and move on.
- **Never Read, cat, head, tail or echo `.env`.** If a command needs it, let tsx load it with `--env-file=.env`.
- **Commit per task** with prefix `fix(260913-l8q):` (Tasks 1-2) or `feat(260913-l8q):` (Task 3), ending with the attribution trailer from the system reminder. Do NOT commit PLAN.md, SUMMARY.md or STATE.md. Do NOT run `gsd-tools quick-tasks-append`. Do NOT push, publish to R2, deploy or reseed D1.
- **Summary:** RETURN the summary as text in your final message. Subagent Write to SUMMARY.md is blocked, so the orchestrator writes the file. Never write long markdown through a Bash heredoc.
- Do not type an em dash, en dash or `--` substitute into any new user-facing string.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace em dashes in user-facing sentences and labels (placeholders stay)</name>
  <files>apps/web/src/components/compare/RpCalibrationSection.tsx, apps/web/src/components/districts/DistrictLocksTab.tsx, apps/web/src/components/districts/DistrictLocksTab.test.tsx, apps/web/src/components/event/EventMatchTable.tsx, apps/web/src/components/team/MatchTable.tsx, apps/web/src/components/event/StartMatchPicker.tsx, apps/web/src/components/methodology/acknowledgmentsContent.ts, apps/web/src/components/methodology/methodologyCardData.ts, apps/web/src/routes/event.$eventKey.tsx, packages/core/districts/qualification.ts, packages/core/districts/qualification.test.ts, packages/harness/pageArtifacts.ts, packages/harness/pageArtifacts.test.ts, scripts/publishDistricts.test.ts</files>
  <action>
Scope is the locked user decision: only user-facing sentences and labels change. Out of scope: code comments, test NAMES (the first argument of `it(...)`/`describe(...)`), and every lone em dash used as a "nothing here" placeholder. The placeholders that must stay untouched are:
- DistrictLocksTab.tsx: the unknown-verdict return, the two empty fragments (no awards, missing event points) and the null cut-line ternary.
- EventMatchTable.tsx and MatchTable.tsx: both call-badge glyph children in each file.

Run the git status dirty-file check on every file first (see hazards). This file list was clean at planning time.

Make each replacement exactly as below. The wording is the planner's discretion choice, following precedent commit b89c68a0, which swapped a mid-sentence dash for a comma. Swap only the em dash and its surrounding spaces unless a rewording is stated.
- RpCalibrationSection.tsx line 66: "...every qualification match in the corpus, including offseason events, unlike the win-probability calibration above." (comma)
- DistrictLocksTab.tsx line 127 (the caveat): "...has not been eliminated: declines, waitlist movement and wildcard slots can only ever help a team's chances, never hurt them." (colon)
- DistrictLocksTab.tsx line 215 (schedule-strip chip): render `{event.eventName}: {event.played ? "Played" : "Upcoming"}` (colon). Parentheses were rejected because the chip already appends a " (N max)" parenthetical, and a double parenthetical reads badly. DistrictLocksTab.test.tsx lines 335/337 use toContain("Played"/"Upcoming") and keep passing. Grep the test for any exact-textContent assertion on the chip and update it if one exists.
- EventMatchTable.tsx line 366 and MatchTable.tsx line 451: change the aria-label to "Not scored (no prior data)". The span's glyph child stays exactly as is.
- StartMatchPicker.tsx line 84 (pre-schedule caption template): "Before the schedule is released: how the field is likely to rank when nobody yet knows who plays whom. Computed ahead of time..." (colon; the template interpolations stay unchanged)
- acknowledgmentsContent.ts line 106: "SigmaScout is built on work other people did first: the data, the ratings it measures itself against, and the ideas behind several of its pages. ..." (colon)
- acknowledgmentsContent.ts line 126: "...not Statbotics' own code. It was done that way so EPA can be replayed walk-forward at any point in a season. ..." (period, with "It was" added so the new sentence stands)
- acknowledgmentsContent.ts line 135: "The idea behind SigmaScout's Districts page (showing whether a team's district championship spot is already mathematically locked, or already out of reach) came from FRC Locks." (the paired dashes become one pair of parentheses)
- methodologyCardData.ts line 91: "The projects and data SigmaScout is built on: The Blue Alliance, Statbotics, and more." (colon)
- routes/event.$eventKey.tsx line 449 (disabled Simulation tab title; unreachable today, and VPR is retired): replace the whole string with "Simulation is only available on SPR. Switch the algorithm selector to SPR."
- packages/core/districts/qualification.ts line 139, the return value of specialAllocationNote: "special allocation, not modeled" (comma). This text renders in the Locks tab verdict cell for 2025fsc. Update every assertion that quotes the old note to the new text: packages/core/districts/qualification.test.ts (~115), packages/harness/pageArtifacts.test.ts (~1489), scripts/publishDistricts.test.ts (~394), apps/web/src/components/districts/DistrictLocksTab.test.tsx (~307 and ~315). Also update the doc comment near packages/harness/pageArtifacts.ts:1769 that quotes the note, so the comment does not describe text that no longer exists.

After the edits, grep each changed file's sibling *.test.ts(x) and apps/web/e2e for any other assertion quoting an old string, and update the assertion (not the test name). Planning-time grep found none beyond the allocation-note sites above, but confirm it.

Commit: stage exactly the files you changed by path, using message `fix(260913-l8q): remove em dashes from user-facing sentences`. Then run `git show --stat HEAD`.
  </action>
  <verify>
    <automated>cd C:/Users/Jacob/Documents/GitHub/SigmaScout && node -e 'const fs=require("fs");const D=String.fromCharCode(8212);const s=(a,b)=>a+" "+D+" "+b;const W="apps/web/src/components/";const c=[[W+"compare/RpCalibrationSection.tsx",s("corpus","including")],[W+"districts/DistrictLocksTab.tsx",s("eliminated","declines")],[W+"districts/DistrictLocksTab.tsx",s("{event.eventName}","{event.played")],[W+"event/EventMatchTable.tsx",s("scored","no prior")],[W+"team/MatchTable.tsx",s("scored","no prior")],[W+"event/StartMatchPicker.tsx",s("released","how")],[W+"methodology/acknowledgmentsContent.ts",s("first","the data")],[W+"methodology/acknowledgmentsContent.ts",s("own code","done")],[W+"methodology/acknowledgmentsContent.ts",s("Districts page","showing")],[W+"methodology/acknowledgmentsContent.ts",s("out of reach","came")],[W+"methodology/methodologyCardData.ts",s("built on","The Blue")],["apps/web/src/routes/event.$eventKey.tsx",s("VPR algorithm","switch")],["packages/core/districts/qualification.ts",s("allocation","not modeled")],["packages/core/districts/qualification.test.ts",s("allocation","not modeled")],["packages/harness/pageArtifacts.ts",s("allocation","not modeled")],["packages/harness/pageArtifacts.test.ts",s("allocation","not modeled")],["scripts/publishDistricts.test.ts",s("allocation","not modeled")],[W+"districts/DistrictLocksTab.test.tsx",s("allocation","not modeled")]];let bad=0;for(const [f,p] of c){if(fs.readFileSync(f,"utf8").includes(p)){console.log("STILL PRESENT",f,JSON.stringify(p));bad++}}const g=(x,y)=>x.split(y).length-1;const t=fs.readFileSync(W+"districts/DistrictLocksTab.tsx","utf8");const keep=g(t,"<>"+D+"</>")>=2&&g(t,"return \""+D+"\"")>=1&&g(t,"? \""+D+"\"")>=1&&g(fs.readFileSync(W+"event/EventMatchTable.tsx","utf8"),"{\""+D+"\"}")>=2&&g(fs.readFileSync(W+"team/MatchTable.tsx","utf8"),"{\""+D+"\"}")>=2;console.log(bad===0&&keep?"EM-DASH PROSE CLEAN, PLACEHOLDERS KEPT":"FAIL bad="+bad+" keep="+keep);process.exit(bad===0&&keep?0:1)' && npx vitest run packages/core/districts/qualification.test.ts packages/harness/pageArtifacts.test.ts scripts/publishDistricts.test.ts apps/web/src/components/districts apps/web/src/components/event apps/web/src/components/team apps/web/src/components/methodology apps/web/src/components/compare apps/web/src/routes</automated>
  </verify>
  <done>The scan prints "EM-DASH PROSE CLEAN, PLACEHOLDERS KEPT". Any fragment still reported must belong to a file listed as deferred (foreign edits), and the summary names it. The vitest output shows 0 failed tests in files this task touched. Both tsc projects report no new errors in touched files. One commit exists whose `git show --stat` lists only this task's files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive the Locks tab cut line from the lock verdicts' own pool and slot count</name>
  <files>packages/core/districts/locks.ts, packages/core/districts/locks.test.ts, scripts/publishDistricts.ts, scripts/publishDistricts.test.ts</files>
  <behavior>
    - cutLinePointsWithQualifiers(teams, null, qualifiers) returns null
    - With no qualifiers it returns the pointTotal of the slots-th team sorted by pointTotal descending, even when the input array is NOT in points order (the helper sorts; it does not trust input order)
    - NC-2026-shaped regression: teams a100 b90 c80 d70 e60 f50 g40, slots 4, awardQualified {f, g}. The naive rank-slot answer would be 70; pointsSlots = 2, so the result is 90. With maxRemaining 0 for everyone, computeLocksWithQualifiers reports c (80) "eliminated", which is consistent with a cut line of 90 and inconsistent with 70
    - Prequalified teams leave the pool but do NOT consume slots: a100 prequalified, b90 c80 d70, slots 3 gives 70
    - Award qualifiers that are not in `teams` (unranked) do not reduce slots, mirroring awardQualifiedRankedCount
    - Ranked award-qualified count >= slots gives pointsSlots 0 and returns null; an empty pool returns null
    - A pool smaller than pointsSlots returns the last (lowest) pool team's pointTotal, mirroring the old clamp
    - Consistency property over ~200 deterministic pseudo-random fixtures (seeded LCG, no Math.random) with random pointTotal, maxRemaining (including 0), slots and qualifier subsets. For every non-null cut line c, every computeLocksWithQualifiers result with status locked/eliminated/contending satisfies both: pointTotal > c implies status is not "eliminated", and pointTotal < c implies status is not "locked"
    - buildDistrictArtifact: in a district whose DCMP (eventType 2) awards a consuming type (e.g. awardType 1 Winner, as the existing fixture near line 335 does) to a team ranked below cmpSlots, insights.cmpCutLinePoints equals every team's champLock.cutLinePoints and sits ABOVE the old rank-slot value. The equivalent holds for dcmp with a district-event (eventType 1) Impact award (awardType 0). 2025fsc still publishes a null champ cut line
  </behavior>
  <action>
Root cause (established): `cutLinePointsFor` in scripts/publishDistricts.ts reads the raw ranking at rank `slots`. `computeLocksWithQualifiers` in packages/core/districts/locks.ts judges teams against a pool with award-qualified and prequalified teams removed, and against `pointsSlots = max(slots - awardQualifiedRankedCount, 0)`. Live 2026fnc proof: cmpSlots 15 minus 8 champ lockedAward teams leaves 7 points slots. The pool's 7th team is 8429 at 231 and the next is 6500 at 216 (eliminated), yet the site publishes 172. DCMP tier: published 75, pool-consistent 76.

RED first. In packages/core/districts/locks.test.ts, add a `describe("cutLinePointsWithQualifiers", ...)` block covering every behavior bullet except the last. Reuse the file's existing `team(teamKey, pointTotal, maxRemaining)` helper and import the new export. Run it and confirm it fails (missing export). Commit nothing yet.

GREEN in locks.ts:
- Factor the pool derivation out of computeLocksWithQualifiers into one private helper, e.g. `qualifierPool(teams, slots: number, qualifiers)` returning `{ pool, pointsSlots }`. Use exactly the existing arithmetic: ranked award-qualified count, `Math.max(slots - count, 0)`, and a pool that excludes both sets. Have computeLocksWithQualifiers call it, so the verdicts and the cut line cannot drift apart. The null-slots branch and the qualified-status mapping stay behaviorally identical.
- Add `export function cutLinePointsWithQualifiers(teams: readonly LockTeamInput[], slots: number | null, qualifiers: QualifierSets): number | null`. Return null for null slots, pointsSlots 0 or an empty pool. Otherwise sort a copy of the pool's pointTotals descending and return index `Math.min(pointsSlots, pool.length) - 1`.
- Give it a doc comment stating the invariant it guarantees (the consistency property) and why it must share the pool with the verdicts (the 2026fnc 172-vs-231 bug).

Wire it in scripts/publishDistricts.ts buildDistrictArtifact:
- dcmp cut line: `cutLinePointsWithQualifiers(districtLockInputs, district.dcmpSlots, { awardQualified: districtAwardQualified, prequalified: new Set() })`.
- champ cut line: `cutLinePointsWithQualifiers(champLockInputs, district.cmpSlots, { awardQualified: dcmpAwardQualified, prequalified: champPrequalified })`, still forced to null when `champAllocationNote !== null`.
- Hoist each qualifier-sets object into a named const shared by that tier's computeLocksWithQualifiers call and its cut-line call, so both calls provably get identical inputs.
- Delete `cutLinePointsFor` and its doc comment. Add the new import. Drop any import that becomes unused. CorpusDistrictRanking is still used elsewhere in the file, so check before removing it.

In scripts/publishDistricts.test.ts:
- Remove `cutLinePointsFor` from the import and delete its `describe("cutLinePointsFor")` block (~lines 419-436). Its cases now live in locks.test.ts.
- Add the buildDistrictArtifact-level tests from the last behavior bullet, using the file's existing `ranking`, `district`, `districtEvent` and `eventAward` helpers.
- Check that no existing test asserted a cut-line value that the new semantics change. If one did, update the expected value and explain why in the summary.

Run both test files and confirm they are green. Commit with message `fix(260913-l8q): Locks cut line uses the lock verdicts' own pool and slot count`, staging exactly these four files by path.

Corpus-wide check (local, no network, nothing committed):
- Write a scratch script in the session scratchpad directory, not the repo. It imports `composeYear` from the absolute file URL of scripts/publishDistricts.ts (its main() is entry-point guarded, so importing is safe) and `openCorpusReadOnly` from packages/corpus/db.ts. It opens data/corpus.sqlite read-only and composes each season in 2016-2020 and 2022-2026 with computedAt set to the current ISO time.
- For every detail artifact it asserts three things:
  - insights.dcmpCutLinePoints equals every team's districtLock.cutLinePoints, and the cmp equivalent holds.
  - The consistency property holds on both tiers, over teams whose status is locked, eliminated or contending.
  - It prints 2026fnc's dcmp and cmp cut lines.
- Run it from the repo root with `npx tsx <scratch path>`.
- Expected: 2026fnc champ 231 and dcmp 76. If the local corpus differs from the live snapshot the orchestrator measured, recompute the expected values from that composed artifact's own team statuses and report both, but any property violation is a real failure.
- Then run the real CLI end to end: `npx tsx scripts/publishDistricts.ts --years 2016-2020,2022-2026 --dry-run`. Add `--env-file=.env` only if the module refuses to load without it. The dry run composes, Zod-validates and prints sizes without calling putObject, so it needs no network. If it unexpectedly attempts network access, stop it, skip it and say so.
- Never run it without `--dry-run`.
  </action>
  <verify>
    <automated>cd C:/Users/Jacob/Documents/GitHub/SigmaScout && npx vitest run packages/core/districts/locks.test.ts scripts/publishDistricts.test.ts packages/core/districts && grep -c "cutLinePointsWithQualifiers(" scripts/publishDistricts.ts && node -e 'const s=require("fs").readFileSync("scripts/publishDistricts.ts","utf8");const hit=/function cutLinePointsFor\b/.test(s);console.log(hit?"FAIL: old helper still defined":"OK: old helper removed");process.exit(hit?1:0)'</automated>
  </verify>
  <done>The RED run failed before the implementation and the GREEN run passes, with 0 failed tests in locks.test.ts, publishDistricts.test.ts and packages/core/districts. scripts/publishDistricts.ts calls cutLinePointsWithQualifiers exactly twice, and the old helper is gone. The scratch corpus check reports zero property violations across every district-year, and 2026fnc composes to champ 231 and dcmp 76 (or the summary explains a corpus-snapshot difference with the self-consistent values). The dry run either composed every season without error or was skipped with the reason stated. Both tsc projects are clean for the touched files. One commit lists only the four task files. The summary tells the orchestrator that `pnpm publish:districts` (a network step) is owed before the live Locks tab shows 231.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grey "Beta" tag after the ribbon wordmark</name>
  <files>apps/web/src/components/ribbon/Ribbon.tsx, apps/web/src/components/ribbon/Ribbon.test.tsx</files>
  <behavior>
    - A "Beta" text node renders in the ribbon on desktop and on mobile (matchMedia matches: true, mirroring the existing mobile test's stub)
    - "Beta" is not inside any link: its element's closest("a") is null
    - The existing link assertions still pass unchanged: 6 links, and the first five link texts are the wordmark, Teams, Events, Locks, Methodology
    - "Beta" is the wordmark link's next element sibling, inside one shared wrapper, so the ribbon row keeps the same number of flex children
  </behavior>
  <action>
First load the UI skill: `Skill("sketch-findings-sigmascout")`. Confirm the ribbon's muted ink token (planning found `--ribbon-ink-muted: rgba(255, 255, 255, 0.72)` in apps/web/src/styles/theme.css, used by GitHubLink and the nav links) is the sanctioned grey-on-ribbon ink. If the skill names a different token for secondary ribbon text, use that one and say so in the summary.

Run `git status --short -- apps/web/src/components/ribbon/` and re-read Ribbon.tsx immediately before editing. Another session changed it minutes before planning.

RED: add the behavior assertions to Ribbon.test.tsx, reusing its renderRibbonAt helper and the existing never-resolving fetch stub and matchMedia stub pattern. Run it and confirm it fails.

GREEN: in Ribbon.tsx, change the `wordmark` constant (currently a bare Link with classes `text-role-display shrink-0 truncate text-[var(--ribbon-ink)]`) into a wrapper `div` with `flex shrink-0 items-baseline gap-[var(--spacing-sm)]`:
- The wrapper's first child is the existing Link, unchanged: same `to`, `search={preserveSearch}`, classes and Sigma accent span, so the link's textContent is untouched.
- Its second child is a sibling `span` reading "Beta" with `text-role-label text-[var(--ribbon-ink-muted)]` (12px, one step smaller than the 28px display wordmark, grey on the pine bar). Use the skill's token instead if it says otherwise.
- Keep "Beta" outside the Link, because the home link's accessible text must stay the wordmark alone.
- Both the mobile and desktop branches already render `{wordmark}` as one flex child, so no layout code outside the constant changes.
- Extend the constant's existing dated comment with one line saying the Beta tag is a user request on 2026-09-13 and sits outside the link on purpose (Ribbon.test pins the link text).

Mobile width: the wrapper stays shrink-0 and adds roughly 40px to the first mobile row, next to GlobalSelects. Do not change GlobalSelects. The orchestrator checks a 375px screenshot (see verification).

Run the Ribbon test and confirm it is green. Typecheck the web project. Commit with message `feat(260913-l8q): grey Beta tag after the ribbon wordmark`, staging exactly the two files by path.
  </action>
  <verify>
    <automated>cd C:/Users/Jacob/Documents/GitHub/SigmaScout && npx vitest run apps/web/src/components/ribbon && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>The RED run failed before the change. The ribbon test files pass with 0 failed tests, including the new Beta assertions and the unchanged six-link and link-order assertions. The web tsc run reports no errors in Ribbon.tsx or Ribbon.test.tsx. One commit lists only those two files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local corpus -> published district artifact | buildDistrictArtifact output becomes a public number on the Locks tab once the orchestrator republishes |
| repo -> transcript | executor commands run beside a live `.env` (TBA key, R2 token pair) |
| shared checkout -> commit | concurrent sessions' uncommitted edits sit in the same working tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l8q-01 | Tampering (integrity of a published figure) | scripts/publishDistricts.ts cut line | high | mitigate | The cut line and the verdicts share one qualifierPool derivation in locks.ts. The seeded property test plus the corpus-wide scratch check prove no team above the line is eliminated and none below is locked, in every district-year. |
| T-l8q-02 | Information disclosure | `.env` during the dry run | high | mitigate | Never Read or cat `.env`. The dry run runs without `--env-file` unless the module will not load, and then tsx reads the file itself. No secret is interpolated into output. |
| T-l8q-03 | Tampering | git staging in a shared checkout | medium | mitigate | Run a git status dirty check per file before editing, defer foreign-dirty files, stage by explicit path, and inspect `git show --stat HEAD` after every commit. |
| T-l8q-04 | Denial of service | accidental real R2 publish | medium | mitigate | Only `--dry-run` may run. The publish, deploy and push are owed to the orchestrator, which runs network steps. |
| T-l8q-05 | Spoofing | Beta tag inside the home link | low | accept | Purely presentational. The test pins it outside the link, so the link's accessible name is unchanged. |
</threat_model>

<verification>
Executor (after all three tasks):
- `npx vitest run packages/core/districts scripts/publishDistricts.test.ts packages/harness/pageArtifacts.test.ts apps/web/src/components/districts apps/web/src/components/event apps/web/src/components/team apps/web/src/components/methodology apps/web/src/components/compare apps/web/src/components/ribbon apps/web/src/routes` shows 0 failed tests in files this plan touched. Read the counts, not the exit code. Name any failure in a file another session is editing as pre-existing or foreign.
- `npx tsc --noEmit -p tsconfig.json` and `npx tsc --noEmit -p apps/web/tsconfig.json` are both clean for touched files.
- `git log --oneline -3` shows the three `260913-l8q` commits, and each `git show --stat` lists only that task's files.

Orchestrator (network or visual, not the executor):
- Run `pnpm publish:districts`, then read https://data.sigmascout.org/v1/district/2026fnc.json. Expect `insights.cmpCutLinePoints` 231 and `insights.dcmpCutLinePoints` 76.
- Locally, screenshot the ribbon at desktop width and at 375px. Confirm "Beta" is grey, smaller, baseline-aligned after the wordmark, and the first mobile row does not overflow horizontally.
- Ship the web app so the copy and Beta changes go live.
</verification>

<success_criteria>
- Zero em dashes remain in the listed user-facing strings, and every placeholder dash still renders.
- The Locks tab cut line and lock verdicts come from one shared pool/slot derivation. 2026fnc composes to champ 231 and dcmp 76, and no district-year violates the above-the-line/below-the-line consistency property.
- "Beta" renders grey and smaller right after the wordmark, outside the home link, on both ribbon layouts.
- Three commits, path-staged, with no foreign files, and no publish, deploy or push.
</success_criteria>

<output>
Return the summary as text in the final message, with: commits (hash and files), any deferred files and why, test and tsc counts, the 2026fnc cut lines the scratch check produced, the dry-run outcome (or why it was skipped), and the owed orchestrator steps (districts republish, web ship, ribbon screenshots). The orchestrator writes `.planning/quick/260913-l8q-remove-em-dashes-from-user-facing-text-f/260913-l8q-SUMMARY.md` from it.
</output>
