---
id: 260925-m7e
slug: ledger-follow-ups-keep-scroll-position-o
kind: quick
mode: inline
created: 2026-09-25
description: "Ledger follow ups: keep scroll position on drawer and rewind navigation, print every week tick, tilde before every open cell figure"
files_modified:
  - apps/web/src/components/districts/DistrictLedger.tsx
  - apps/web/src/components/districts/DistrictLedger.test.tsx
  - apps/web/src/components/districts/districtLedgerCopy.ts
  - apps/web/src/components/districts/districtLedgerCopy.test.ts
  - apps/web/src/styles/theme.css
autonomous: true
---

# Quick task 260925-m7e: three ledger follow ups from Jacob's first look at the restyle

Jacob, 2026-09-25, three messages after the restyle shipped:

1. "the rewind feature seems broken for years before 2026" and "when I click a blue box, the
   page scrolls back to the top and it shouldnt". Headless probes of the live site showed the
   rewind loading artifacts and reopening cells on 2025 PNW and 2024 FiM through the real
   interactive path (year, district pick, drag), with no errors and no bad responses, so the
   breakage is the scroll reset: every slider move and every blue box click navigates with a
   search param change, and the router reset the page scroll to the top on each one. Fix: pass
   the router's keep-scroll option on both ledger navigations.
2. "there are more than 4 weeks. it should adapt." The jump chips already derive from the
   weeks present (2026 FiM shows After week 0 to 5), but the restyle's tick rail dropped any
   week label within 10% of a neighbour, so the rail read "start, wk 1, now". Fix: print every
   week and stagger a colliding label onto a second row.
3. "for the big number in all the blue boxes, put a ~ symbol before the number". Fix: the bold
   line of every open cell (median form and chance form, including the grand total) starts
   with "~"; the legend explainer now reads "~ = this site's prediction, not a number TBA
   published".

Executed inline in the main context (three small edits with the tests already in place).
