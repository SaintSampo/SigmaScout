---
phase: 260914-53h-resolve-todo-finish-nvn-held-back-trims-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Wave 1 (W1-1 rename)
  - packages/harness/sigmaScoutLayer.ts
  - packages/harness/preSchedule.ts
  - packages/harness/publish.ts
  - packages/harness/sigmaScore.ts
  - packages/harness/preSchedule.test.ts
  - packages/harness/rpSeed.test.ts
  - packages/harness/sigmaScoutLayer.matchBand.test.ts
  - packages/harness/sigmaScoutLayer.sigmaFor.test.ts
  - packages/harness/sigmaSeed.test.ts
  - scripts/measureFieldAveragedRanks.ts
  # Wave 1 (W1-2 doc fix)
  - docs/first-paint-measurement.md
  # Wave 2 (comment trims, W2-1..W2-6, file lists in each task)
  - packages/harness/publish.test.ts
  - packages/harness/stateSnapshot.ts
  - packages/harness/stateSnapshot.test.ts
  - packages/harness/generatedSchedules.ts
  - packages/harness/generatedSchedules.test.ts
  - packages/harness/sigmaScoutLayer.outcomeArms.test.ts
  - packages/harness/sigmaScoutLayer.bandGuard.test.ts
  - packages/harness/sigmaScoutLayer.meanShift.test.ts
  - packages/harness/sigmaScoutLayer.lattice.test.ts
  - scripts/measureRpCalibration.ts
  - scripts/measureRpCalibration.test.ts
  - scripts/measureFieldAveragedRanks.test.ts
  - packages/core/rankingPoints/**
  - apps/worker/src/scheduled.ts
  - apps/worker/src/stateProbe.ts
  - apps/worker/test/scheduled.rp.test.ts
  - apps/worker/test/stateProbe.test.ts
  - apps/web/src/styles/theme.css
  - apps/web/src/lib/bonusRp.ts
  - apps/web/src/lib/bonusRp.test.ts
  - apps/web/src/components/team/BonusRpDots.tsx
  - apps/web/src/components/team/BonusRpDots.test.tsx
autonomous: true
requirements: [QUICK-260914-53h]

estimate:
  tokens: 700000
  raw_tokens: 1400000
  tasks: 8
  confidence: high

must_haves:
  truths:
    - "`git grep -n consistencyByTeam -- ':!.planning'` prints nothing: the layer method is `sigmaScoreByTeam()`, the `FieldContributionInputs` field is `sigmaScoreByTeam`, and every caller, JSDoc reference and the two sigmaFor test names use the new name"
    - "The rename changes no behaviour and no published byte: root `npx tsc --noEmit` error count equals its pre-rename baseline, and the scoped harness/scripts tests keep their pre-rename pass counts with no new failing names"
    - "docs/first-paint-measurement.md carries no line-number citation and no code-symbol citation that fails to resolve at HEAD"
    - "Every Wave 2 file passes `comments.mjs guard` against its executor's task-start BASE (no FAIL line names a file in that task's list) and shows dCode = 0 in `comments.mjs delta`"
    - "The five sealed files (packages/spr/model.ts, evaluate.ts, data.ts, cli.ts, packages/core/algorithms/spr.ts) are byte-identical to the Wave 1 start commit"
    - "Provenance markers (the tool's `prov` column) summed over each Wave 2 task's file list drop by at least 80%; every survivor is a one-line pointer to a still-live decision"
    - "Each Wave 2 task's scoped tests, typechecks (and for W2-6 the web build) match its S1 baselines"
    - "Every commit touched only its own task's files, was made with explicit `-- <paths>`, and no foreign uncommitted edit was absorbed; no .env content was read"
    - "The todo sits in .planning/todos/completed/ with a Resolution section, and SUMMARY.md exists"
  artifacts:
    - path: "packages/harness/sigmaScoutLayer.ts"
      provides: "Renamed Sigma Score accessor"
      contains: "sigmaScoreByTeam(): ReadonlyMap<string, number>"
    - path: "packages/harness/preSchedule.ts"
      provides: "Renamed field-averaged input"
      contains: "readonly sigmaScoreByTeam: ReadonlyMap<string, number>"
    - path: "packages/harness/publish.ts"
      provides: "Seed-passenger, walk-forward and generation-addressing rationale kept; provenance narration removed"
      contains: "withRpMeanShift(rows, rpMeanShift)"
    - path: "apps/web/src/styles/theme.css"
      provides: "Palette, contrast, tier-token and bonus-dot rationale kept; sketch/quick-task narration removed"
      contains: "--tier-common-edge"
    - path: "docs/first-paint-measurement.md"
      provides: "Citations that resolve at HEAD"
      contains: "markStartAndSetQuery"
  key_links:
    - from: "W1-1 rename commit"
      to: "every Wave 2 executor BASE"
      via: "Wave 2 executors record BASE only after the refactor(260914-53h) commit is on HEAD, because publish.ts, preSchedule.ts, sigmaScoutLayer.ts, sigmaScore.ts and measureFieldAveragedRanks.ts are also trim targets"
      pattern: "refactor\\(260914-53h\\)"
    - from: "comments.mjs guard (this session's scratchpad)"
      to: "per-executor BASE plus that executor's own file list"
      via: "guard output filtered with grep -F -f $LOG/<TASK>-files.txt"
      pattern: "GUARD OK"
    - from: "packages/harness/rpSeed.test.ts, sigmaSeed.test.ts, publish.test.ts"
      to: "packages/harness/publish.ts source text"
      via: "regex blocks from `const state = finalSeasonStates.get(algorithm.id);` to `emitSeedSql(`, and the `buildCompareArtifact({` call-site count"
      pattern: "readFileSync\\(new URL\\(\"./publish.ts\""
---

<objective>
Resolve the pending todo `.planning/todos/pending/finish-nvn-held-back-trims-and-sigma-rename.md`: (1) rename `consistencyByTeam` to `sigmaScoreByTeam`, (2) comment-trim every file quick task 260913-nvn held back for other sessions, (3) fix the rotted citations in `docs/first-paint-measurement.md`.

Nothing here changes behaviour or published bytes, so nothing is published, deployed or pushed.

Dispatch shape (for the orchestrator):
- **Wave 1** (both tasks parallel-safe, disjoint files; one executor may do both in sequence): W1-1 rename, W1-2 doc fix. W1-1 must be committed before any Wave 2 executor records its BASE.
- **Wave 2** (six tasks, all file-disjoint, all parallel-safe): W2-1 publish.ts; W2-2 harness layer, snapshot and schedules; W2-3 publish.test.ts and measure scripts; W2-4 rankingPoints season rules; W2-5 rankingPoints analytic machinery; W2-6 Worker and web.

Why six and not three or four: the held-back set totals 10,408 comment lines at HEAD (harness and scripts 5,618, rankingPoints 3,072, Worker 1,051, web 667). At the ~2,000-lines-per-executor cap that needs six executors; fewer would push one executor past half context, which is the failure mode that left garbled half-edits in nvn. Recommended concurrency cap 4 (two interactive sessions share this checkout and the corpus-heavy suites have a 30s test timeout): start W2-1, W2-2, W2-4, W2-6; start W2-3 and W2-5 as slots free (each shares a heavy test file with W2-1 and W2-4 respectively).

Tracer-first is realised per executor, as in nvn-04: step S2 trims, guards, tests and commits one file alone before scaling out.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/finish-nvn-held-back-trims-and-sigma-rename.md
@.planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-04-PLAN.md
@.claude/CLAUDE.md

## Environment facts (verified 2026-09-14, plan on them)

- Both "Wait for" conditions in the todo are met: 260914-01x is complete and pushed; `quick/260913-pnp` merged to main (b990a7fc). `scheduleTemplates.ts` and two of the three measure scripts were deleted by pnp; `packages/core/rankingPoints/2021.ts` does not exist (no 2021 RP module).
- `workflow.use_worktrees` is false. Every executor works on the main checkout `C:/Users/Jacob/Documents/GitHub/SigmaScout`. Two other interactive Claude sessions are active on it.
- `packages/harness/algorithmIdentity.test.ts` no longer exists; drop it from every command copied from nvn-04.
- Executors cannot Write SUMMARY.md and heredocs break on long markdown: executors RETURN report text; the orchestrator writes SUMMARY.md.

## Tool: comments.mjs (this session's scratchpad, NOT in the repo)

- CMT = `C:/Users/Jacob/AppData/Local/Temp/claude/c--Users-Jacob-Documents-GitHub-SigmaScout/5084c558-8583-40c8-8798-8c91dab02553/scratchpad/53h-comments/comments.mjs`
- LOG = `C:/Users/Jacob/AppData/Local/Temp/claude/c--Users-Jacob-Documents-GitHub-SigmaScout/5084c558-8583-40c8-8798-8c91dab02553/scratchpad/53h-comments/logs` (exists)
- Do NOT use the nvn-04 scratchpad path (it is another session's temp dir). If CMT is missing, STOP and report; do not reimplement it.
- Subcommands, columns and scope rules: exactly as nvn-04-PLAN.md `## Tool: comments.mjs`. Regions: A = apps/web; B = packages/harness, scripts, packages/ingest, packages/corpus, vitest.config.ts; C = packages/core, packages/spr, apps/worker. Name every log `$LOG/<TASK>-*.txt` (e.g. `W2-3-guard.txt`).
- `delta` rows print `dComment dCode dBlank before->after comment path`, only for files changed since BASE.

## Comment policy

Use nvn-04-PLAN.md `## Comment policy (applies to all three tasks)` verbatim: the maintainer question, DELETE D1-D8 and KEEP K1-K7. Do not re-derive a policy. The NEVER list is pasted here because it must not be missed:

NEVER
- N1 Never edit the sealed files: packages/spr/model.ts, packages/spr/evaluate.ts, packages/spr/data.ts, packages/spr/cli.ts, packages/core/algorithms/spr.ts. Their git blob shas are what the published holdout attests to (`packages/spr/sealedPaths.ts`); the guard fails if any changes.
- N2 Never change a non-comment character: string and template literals, test names, JSX text, CSS values, types, imports, and the formatting or indentation of code lines. Never rewrap code. JSON, CSV, snapshot, fixture, .md, .toml, .sql files are out of scope.
- N3 Never type a retired algorithm id (sigma1, sigma1-*, bpr) into a new or reworded comment: packages/harness/algorithmIdentity.test.ts sweeps every file and fails on unmarked mentions. Delete such lines rather than rewording them.
- N4 Never invent. A rewritten comment may state only what the original said about live behaviour or what the code in view visibly does. Do not substitute a current algorithm name for a deleted one unless the code on screen shows it.
- N5 Never delete or create a file; never touch files outside your region, `.planning/` (including STATE.md) or `docs/`. Do not fix bugs you notice (audit section F is out of scope); list them in the report.
- N6 No network, no deploy, no push, no republish. Never read, cat or echo `.env`.

53h readings of that list: N3 still applies although that sweep test is deleted (retired ids must not re-enter comments). In N5, "your region" means "your task's file list"; W1-2 is the only task allowed to edit `docs/`, and W1-1 is the only task allowed to change code (the rename). N2's "test names" rule has one exception: W1-1 renames the method inside two sigmaFor test names.

New-since-nvn KEEP facts (live after 260914-01x, K1): lattice marginals and the RP mean shift ship (state shape 16); `withRpMeanShift` is a LEAGUE-row seed passenger alongside `withSigmaBeliefs`, `withSigmaPopulation`, `withRpBeliefs`; bonus dots fill to the predicted odds (F10). Keep the live constraint each implies, in present tense; delete the quick-task ids and dates.

## Known source readers (must not be missed)

nvn-04's list, corrected for HEAD:
- apps/web/src/styles/theme.css is read by components/MetricValue.test.tsx, components/compare/comparePalette.test.ts (first-match regex on `--compare-algo-*: value;`, so a comment quoting a declaration can shadow or unshadow the real one), components/team/MatchTable.test.tsx and styles/theme.scrollbar.test.ts (selector bodies after stripping comments), lib/simAxis.test.ts (first-match regex on `--sim-band-overlay: color-mix(...)`, `--sim-hist-bar:`, `--sim-median-tick:` over raw CSS), favicon.test.ts, and lib/bonusRp.test.ts (strips comments, then reads the bare `.bonus-dot` rule's width and border).
- packages/harness/publish.ts is read by publish.test.ts (`buildCompareArtifact({` call-site count must equal its `});`-terminated block count), sigmaSeed.test.ts and rpSeed.test.ts (block regex from `const state = finalSeasonStates.get(algorithm.id);` to the first `emitSeedSql(`; that block must contain `withSigmaBeliefs(`, `withSigmaPopulation(`, `withRpBeliefs(`, `withRpMeanShift(`, `withRpMeanShift(rows, rpMeanShift)`; the file must contain `layers.get(algorithm.id)!.rpMeanShiftState()`). Never delete or add comment text containing any of those tokens.
- packages/harness/preSchedule.ts by preSchedule.test.ts: `/from\s+"([^"]+)"/g` over RAW source, so a comment containing `from "..."` counts as an import specifier. Never delete or add such a comment.
- packages/harness/sigmaScoutLayer.ts by sigmaScoutLayer.bandGuard.test.ts (three pinned code strings; code anyway).
- scripts/measureRpCalibration.ts by its own test via `SOURCE` over RAW text: match counts of `new SigmaScoutLayer(`, `new WalkForwardSimulator(`, `buildSeasonStream(`, `function brier(`, `function rate(`, `function meanPredicted(`; negative matches on `rpMeanShift|latticeRuleModule` and `--allow-reporting|--force`; `SOURCE.indexOf("async function main(")`; positive `into.lattice += from.lattice;`. Never delete or add comment text matching any of those. It is also read as a baseline/doc consumer (data/baselines, docs/models/rp-attribution.md), which are out of scope.
- packages/core/rankingPoints/analyticPmf.ts by analyticPmf.test.ts (export set by regex over lines starting with `export`; a code-only filter drops lines starting with `//`, `*`, `/*`). Never touch an `export` line.
- apps/worker/src/stateProbe.ts and apps/worker/src/scheduled.ts by apps/worker/test/stateProbe.test.ts (strips comments first; also walks the import graph).
- Cross-region: packages/harness/browserSafeSchemas.test.ts and packages/core/isomorphic.test.ts read core source; scripts/secrets-boundary.test.ts reads the whole repo.
- Run `node "$CMT" readers --region R` at S1 anyway (over-inclusive watch list) and, before deleting a comment containing a distinctive token, `git grep -nF "<token>" -- "*.test.ts" "*.test.tsx"`. A new failure in a source-reading test means restore the deleted text, never change the test.

## Shared procedure

Use nvn-04-PLAN.md `## Shared procedure (every task)` S0-S6 and its Report format, with these 53h overrides (they win where they differ):

- O1 Paths: CMT and LOG above. Commit subjects `docs(260914-53h): trim comments in <area>`, body line `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- O2 S0 BASE: first confirm `git -C C:/Users/Jacob/Documents/GitHub/SigmaScout log --oneline -20 | grep -c "refactor(260914-53h)"` prints 1 or more (W1-1 landed); if 0, STOP. Then write BASE to `$LOG/<TASK>-base.txt` and write your task's file list (one repo-relative path per line, from your task's table) to `$LOG/<TASK>-files.txt`. Run `selftest` for each region your task spans; require `failures=0`.
- O3 S1 counts: `node "$CMT" count --region R --all`, filtered with `grep -F -f "$LOG/<TASK>-files.txt"`; record comment/code/prov/dead per file and the sums. Re-rank from these numbers (another session may have changed a file since planning).
- O4 Ownership check before editing each file: `git status --short -- <file>` must print nothing and `git log --oneline "$BASE"..HEAD -- <file>` must show only your own commits. Otherwise skip the file and report it as foreign-held. Never edit, stage, restore or revert any file outside your list.
- O5 Guard verdict: `node "$CMT" guard --base "$(cat "$LOG/<TASK>-base.txt")" --region R [--region R2] > "$LOG/<TASK>-guard.txt"`, then `grep -E "^FAIL" "$LOG/<TASK>-guard.txt" | grep -F -f "$LOG/<TASK>-files.txt"` must print nothing and `grep -c "sealed file changed" "$LOG/<TASK>-guard.txt"` must print 0. Other Wave 2 executors share your region, so a final `GUARD FAILED` caused only by files outside your list is not yours: rerun once after about 60 seconds (a peer may be mid-edit); if it persists, check `git log`/`git status` for that file and name it in the report as foreign.
- O6 Garble check before every commit (nvn lost work to this): list hunks with `git diff -U0 -- <file>`, then Read about 15 lines around each hunk and confirm every surviving comment is a whole, grammatical sentence, no two comments merged, no dangling list marker or half table row, and `*/` closers intact. The guard does not catch garbled prose.
- O7 Commit: `git -C C:/Users/Jacob/Documents/GitHub/SigmaScout commit -m "<subject>" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>" -- <explicit paths>`. Never `add -A`, `add .`, `commit -a`. On `index.lock` or `cannot lock ref`, wait about 5 seconds and retry once. Afterwards `git status --short -- <committed paths>` must print nothing.
- O8 Tests: use `npx vitest run ...` exactly as written in your task, never `pnpm test`, never wrapped in `timeout` (it swallows output and exits 0). Give long runs the Bash tool's own timeout (up to 600000 ms). Save output to LOG and read the `Test Files` and `Tests` lines; never judge by exit code. A failure that appears only under parallel load counts as new only if it reproduces when that test file is run alone.
- O9 Batches: one commit per file, except publish.ts and publish.test.ts, which commit per window batch of about 1,000 source lines (guard, garble check, scoped tests, commit). Production files before tests; within each, higher `prov` first.
- O10 S5 stop rule unchanged: past about half context, commit what is done, never leave uncommitted edits, return the remaining files with counts.
- O11 Report (returned as text, never written to a file): commits (short sha + subject), per-file and summed before/after comment/code/prov/dead, baseline vs final test/typecheck/build counts, comments kept on purpose that a reader might expect gone (and why), guard NOTEs and foreign files, bugs noticed (not fixed), stopped-at list.
</context>

<tasks>

<task type="auto">
  <name>W1-1 (wave 1, parallel-safe with W1-2): rename the Sigma Score accessor and input to sigmaScoreByTeam</name>
  <wave>1</wave>
  <files>packages/harness/sigmaScoutLayer.ts, packages/harness/preSchedule.ts, packages/harness/publish.ts, packages/harness/sigmaScore.ts, packages/harness/preSchedule.test.ts, packages/harness/rpSeed.test.ts, packages/harness/sigmaScoutLayer.matchBand.test.ts, packages/harness/sigmaScoutLayer.sigmaFor.test.ts, packages/harness/sigmaSeed.test.ts, scripts/measureFieldAveragedRanks.ts</files>
  <read_first>
    - The todo, section "1. Rename"
    - `git grep -n consistencyByTeam -- ':!.planning'` output (22 hits at planning time, listed below)
  </read_first>
  <action>
<!-- planner-discipline-allow: consistencyByTeam -->
Todo item 1. The module rename already landed in bf7f38fa (`sigmaMetric.ts`, `sigmaMetricByTeam`); this finishes it. The new name is `sigmaScoreByTeam` because the method delegates to `SigmaScoreAccumulator.scoreByTeam()` and returns finished Sigma Scores; it collides with nothing (`git grep -n sigmaScoreByTeam` is empty at planning time; `sigmaByTeam` is a different, existing parameter name in publish.ts and sigmaScore.ts and stays).

Before editing: baseline `npx tsc --noEmit` from the repo root (save to `$LOG/W1-1-tsc-before.txt`, count `error TS` lines) and the test command in verify (save to `$LOG/W1-1-tests-before.txt`, read the counts). Apply O4 to every file; if one has foreign uncommitted edits, STOP and report rather than renaming around it (a partial rename does not compile).

Rename every occurrence of the identifier consistencyByTeam outside `.planning/`, in code, JSDoc and backticked comment references alike:
- sigmaScoutLayer.ts: the method declaration (about line 166), the JSDoc references (about lines 80, 181, 230), and the local of the same name plus its two uses in the match-band path (about lines 340-342).
- preSchedule.ts: the `FieldContributionInputs` field and its JSDoc (about 494-495), the prose reference in `buildFieldContributions`'s doc comment (about 519), the destructure and both uses (about 542, 549, 554). Rename the scalar local `consistency` read from that map (about 554, used at 563) to `sigmaScore`.
- publish.ts: the call assigning `sigmaByTeamForAlgo` (about 3153) and the backticked reference in the read-after-fold comment (about 2950).
- sigmaScore.ts: the backticked JSDoc reference (about 164).
- scripts/measureFieldAveragedRanks.ts: rename the local `consistency` (about 871, passed to `makeRankingPointFiller`) to `sigmaScores`, and the object key at about 946 becomes `sigmaScoreByTeam: sigmaScores`.
- Tests: preSchedule.test.ts object keys and property reads (about 258, 267, 269, 309, 391); rpSeed.test.ts (213, 233); sigmaScoutLayer.matchBand.test.ts (294); sigmaSeed.test.ts (143, 155); sigmaScoutLayer.sigmaFor.test.ts JSDoc (6), the calls (87, 92), the map local `consistency` at 79 renamed to `sigmaScores`, and the method name inside the two `it(...)` titles at 64 and 85 (the one allowed test-name change: the title names the method being renamed).

Change nothing else: no prose rewording of "consistency figure" phrases (Wave 2 trims those files), no reformatting, no other identifiers. Do not touch `apps/`, `docs/`, or the sealed files. The rename changes no serialized key, so no published byte changes and no publish is needed.

Commit once: subject `refactor(260914-53h): rename consistencyByTeam to sigmaScoreByTeam`, Co-Authored-By trailer, explicit paths (the ten files), per O7.
  </action>
  <verify>
    <automated>From C:/Users/Jacob/Documents/GitHub/SigmaScout: (1) `git grep -n consistencyByTeam -- ':!.planning'` prints nothing. (2) `git grep -c "sigmaScoreByTeam" -- packages/harness/sigmaScoutLayer.ts packages/harness/preSchedule.ts` shows both files with 1 or more. (3) `npx tsc --noEmit > "$LOG/W1-1-tsc-after.txt" 2>&1; grep -c "error TS" "$LOG/W1-1-tsc-after.txt"` equals the before count. (4) `npx vitest run --project node packages/harness/preSchedule.test.ts packages/harness/rpSeed.test.ts packages/harness/sigmaScoutLayer packages/harness/sigmaSeed.test.ts packages/harness/sigmaScore.test.ts packages/harness/publish.tracer.test.ts packages/harness/publish.test.ts scripts/measureFieldAveragedRanks.test.ts` (Bash timeout 600000) shows the same `Test Files` and `Tests` passed counts as the before run and no new failing test names. (5) `git diff --name-only -- packages/spr/model.ts packages/spr/evaluate.ts packages/spr/data.ts packages/spr/cli.ts packages/core/algorithms/spr.ts` prints nothing. (6) `git status --short -- packages/harness scripts` shows none of the ten files.</automated>
  </verify>
  <done>
    - One `refactor(260914-53h)` commit touching exactly the ten files; no remaining occurrence of the old identifier outside `.planning/`
    - Root tsc error count and the scoped test pass counts equal their pre-rename baselines
    - Report returned as text: sha, before/after tsc and test counts, any foreign-held file
  </done>
</task>

<task type="auto">
  <name>W1-2 (wave 1, parallel-safe with W1-1): fix rotted citations in docs/first-paint-measurement.md</name>
  <wave>1</wave>
  <files>docs/first-paint-measurement.md</files>
  <read_first>
    - docs/first-paint-measurement.md lines 20-75 and 205-250 (Read with offset/limit)
    - apps/web/src/components/search/SearchBox.tsx lines 215-240 and apps/web/src/lib/perfMarks.ts lines 60-80
  </read_first>
  <action>
<!-- planner-discipline-allow: handleValueChange -->
Todo item 3. This is a dated measurement log: leave every measured figure, artifact name and historical statement as written; change only citations that no longer resolve at HEAD. Apply O4 to the file first.

Findings at planning time (re-check each against HEAD):
1. Two citations of `docs/publish-budget.md` "line 28" (about doc lines 34 and 69). The budget doc has been rewritten since (its teams row is now a ceiling table at about line 23, and the 2,721,887-byte row no longer exists there). Replace each line-number pointer with a line-free pointer: at line 34 the budget-table row is already quoted verbatim, so cite it as `docs/publish-budget.md`'s budget table as it stood at measurement time; at line 69 point back to that quoted row ("the `docs/publish-budget.md` row quoted in Run metadata"). Do not re-quote current budget numbers.
2. About line 214: `SearchBox.tsx`'s handleValueChange no longer exists (it only ever lived in a comment, removed by 332e5b17). At HEAD the input's `onValueChange` handler is `markStartAndSetQuery`, which calls `markSearchKeystroke()` from `lib/perfMarks.ts`, and that calls `performance.mark`. Rewrite only that clause so the symbol names resolve; keep the sentence's meaning.
3. Then scan the whole file for any other line-number citation: `grep -n -E "line [0-9]+|lines [0-9]+|\.(md|ts|tsx|css|html):[0-9]+|#L[0-9]+" docs/first-paint-measurement.md` and, for every backticked `*.ts`/`*.tsx` file or symbol cited, confirm it exists at HEAD with `git grep -n` (verified present at planning time: `routes/teams.tsx` `teams-parse-to-paint`, `lib/search-index.ts` `matchTeams`/`matchEvents`, `search-index.test.ts`, `MetricHistoryTab.tsx` `React.lazy`, `MetricHistoryChart.tsx`, `vite.config.ts` `tanstackRouter`). Fix any further miss the same way: symbol name instead of a line number, or a corrected pointer; never invent history.

Commit: subject `docs(260914-53h): replace stale citations in first-paint-measurement.md`, Co-Authored-By trailer, path `docs/first-paint-measurement.md` only, per O7.
  </action>
  <verify>
    <automated>From the repo root: `grep -n -E "line [0-9]+|lines [0-9]+|\.(md|ts|tsx|css|html):[0-9]+|#L[0-9]+" docs/first-paint-measurement.md` prints nothing; `grep -c "handleValueChange" docs/first-paint-measurement.md` prints 0; `grep -c "markStartAndSetQuery" docs/first-paint-measurement.md` prints 1 or more; `git show --stat HEAD -- docs/first-paint-measurement.md` (or the task's commit) shows only that file with a single-digit to low-double-digit line change; `npx vitest run --project node packages/harness/payloadBudget.test.ts` still passes (it reads docs/publish-budget.md, which this task must not touch).</automated>
  </verify>
  <done>
    - No line-number citation remains; every cited code symbol resolves at HEAD; measured figures untouched
    - One `docs(260914-53h)` commit touching only docs/first-paint-measurement.md
    - Report returned as text: sha and each citation changed (before, after)
  </done>
</task>

<task type="auto">
  <name>W2-1 (wave 2, parallel-safe with W2-2..W2-6): trim publish.ts</name>
  <wave>2</wave>
  <files>packages/harness/publish.ts</files>
  <read_first>
    - This plan's context sections, nvn-04-PLAN.md "Comment policy" and "Shared procedure", and nvn-04 Task B's publish.ts KEEP/DELETE bullet
    - `node "$CMT" readers --region B` output
  </read_first>
  <action>
Region B. File list (comment/code, prov, dead at planning time): 1902/1869, prov 264, dead 36 `packages/harness/publish.ts`. About a quarter of all provenance markers left in the repo.

Follow S0-S6 with O1-O11. Work in Read windows of about 300 lines with Edit calls per window; commit per O9 window batch (about four commits). The first window batch is the S2 tracer: trim, guard (O5), garble check (O6), tests, commit.

KEEP (K1, compressed): walk-forward predict-before-update ordering; season carry; generation and version addressing of R2 keys; upload concurrency and the pre-upload budget ceilings; D1 seed-row limits; why each seed passenger (`withSigmaBeliefs`, `withSigmaPopulation`, `withRpBeliefs`, `withRpMeanShift`) must be chained, one line each (LEAGUE-row passengers are the easy ones to forget); read-after-fold for `sigmaFor` and never scoring every team per match; the all-or-nothing roster rule's reason in one line; Match Band is display-only and win odds keep their own variance.
DELETE: quick-task, phase, plan, D-/F-/PD- ids and dates (D1); cutover, retirement and rename history including the removed `--event` path, VPR promotion, tune/promote, Swing (D2, D3); measured-incident narration beyond one figure plus one docs/ pointer (D5); line-number cross references (D7).
Source-reader tokens in the context section: never delete or add comment text containing them. The rename landed in Wave 1; do not touch the renamed call.

Tests per batch and at S1/S6 (repo root, Bash timeout 600000): `npx vitest run --project node packages/harness/publish.test.ts packages/harness/publish.tracer.test.ts packages/harness/rpSeed.test.ts packages/harness/sigmaSeed.test.ts packages/harness/sigmaScoutLayer.matchBand.test.ts`. Typecheck at S1 and S6 only: `npx tsc --noEmit` (count `error TS`).
  </action>
  <verify>
    <automated>O5 guard over `--region B` with W2-1's BASE: no FAIL line names packages/harness/publish.ts and the sealed count is 0; `node "$CMT" delta --base "$(cat "$LOG/W2-1-base.txt")" --region B --top 500 | grep -F "packages/harness/publish.ts"` shows dCode 0; `node "$CMT" count --region B --all | grep -F "packages/harness/publish.ts"` shows prov at or below 52 (80% drop from 264); the test command above matches its S1 counts; root tsc error count not above S1.</automated>
  </verify>
  <done>
    - publish.ts comment-only commits, each guarded and garble-checked; dCode 0; prov down at least 80%
    - Scoped tests and root tsc match baselines
    - Report returned as text per O11
  </done>
</task>

<task type="auto">
  <name>W2-2 (wave 2, parallel-safe): trim the harness layer, snapshot and schedule modules</name>
  <wave>2</wave>
  <files>packages/harness/preSchedule.ts, packages/harness/generatedSchedules.ts, packages/harness/sigmaScore.ts, packages/harness/stateSnapshot.ts, packages/harness/sigmaScoutLayer.ts, packages/harness/sigmaScoutLayer.matchBand.test.ts, packages/harness/sigmaScoutLayer.outcomeArms.test.ts, packages/harness/stateSnapshot.test.ts, packages/harness/preSchedule.test.ts, packages/harness/generatedSchedules.test.ts, packages/harness/sigmaScoutLayer.meanShift.test.ts, packages/harness/sigmaScoutLayer.lattice.test.ts, packages/harness/sigmaScoutLayer.bandGuard.test.ts, packages/harness/sigmaScoutLayer.sigmaFor.test.ts</files>
  <read_first>
    - This plan's context sections, nvn-04 "Comment policy" and "Shared procedure", nvn-04 Task B bullets for stateSnapshot.ts and sigmaScore.ts/sigmaScoutLayer.ts
    - `node "$CMT" readers --region B` output
  </read_first>
  <action>
Region B. 14 files, 1,840 comment lines at planning time. Order and counts (comment/code, prov, dead; "2nd" = had one earlier nvn pass, expect a smaller cut):
Production: 267/386 p11 preSchedule.ts (S2 tracer; never trimmed, pnp-held); 119/278 p1 generatedSchedules.ts (never trimmed); 413/207 p1 d10 sigmaScore.ts (2nd, still 67% comment); 321/576 p5 stateSnapshot.ts (2nd); 248/168 p1 sigmaScoutLayer.ts (2nd, F4 added lattice and mean-shift docs).
Tests (only where narration is heavy): 75/200 p10 sigmaScoutLayer.matchBand.test.ts; 51/132 p5 sigmaScoutLayer.outcomeArms.test.ts; 154/1084 p6 d2 stateSnapshot.test.ts; 56/406 p4 preSchedule.test.ts; 42/303 p3 generatedSchedules.test.ts; 31/260 p3 sigmaScoutLayer.meanShift.test.ts; 15/103 p2 sigmaScoutLayer.lattice.test.ts; 34/46 p1 sigmaScoutLayer.bandGuard.test.ts; 14/76 p1 sigmaScoutLayer.sigmaFor.test.ts.

Follow S0-S6 with O1-O11, one commit per file.

KEEP: preSchedule.ts's all-or-nothing roster rule (one or two lines: deciding once per event, and why a null is the ordinary answer), the one-team `momentsFor` identity (scaling factor is exactly 1) in one or two lines, cold teams counted as zeros and why, sort-for-determinism; the Match Band formula meaning (band = sqrt(rosterSize x sum Sigma^2), display-only) and that win odds keep their own variance; the two-sided band guard's reason; read-only `sigmaFor` never creating a belief (one line, not the war story); raw seed state vs finished figures and the silent-divergence reason (one line each); D1 statement-size and write-cap reasons in stateSnapshot.ts; state shape numbers only where code checks them. In tests, one line per pinned regression (K6); digest provenance becomes "pinned digest" with no ids.
DELETE per D1-D8: plan 09-09 rung and D-16/D-17 banners, measured-on-event dates, Swing/Sigma1 history, pnp and 01x narration. Never add or delete a comment containing `from "` in preSchedule.ts (import-specifier test). Test names untouched (N2).

Tests per file and at S1/S6 (repo root, Bash timeout 600000): `npx vitest run --project node packages/harness/sigmaScore.test.ts packages/harness/sigmaScoutLayer packages/harness/stateSnapshot.test.ts packages/harness/preSchedule.test.ts packages/harness/generatedSchedules.test.ts packages/harness/sigmaSeed.test.ts packages/harness/rpSeed.test.ts scripts/measureFieldAveragedRanks.test.ts`. Typecheck at S1 and S6: `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>O5 guard over `--region B` with W2-2's BASE: `grep -E "^FAIL" "$LOG/W2-2-guard.txt" | grep -F -f "$LOG/W2-2-files.txt"` prints nothing, sealed count 0; `node "$CMT" delta --base "$(cat "$LOG/W2-2-base.txt")" --region B --top 500 | grep -F -f "$LOG/W2-2-files.txt"` shows dCode 0 on every row; `node "$CMT" count --region B --all | grep -F -f "$LOG/W2-2-files.txt"` prov column sums to 10 or less (from 54); the test command above and root tsc match S1.</automated>
  </verify>
  <done>
    - All 14 files trimmed (or listed in the S5 stopped-at list), comment-only, dCode 0, summed prov down at least 80%
    - Scoped tests and root tsc match baselines
    - Report returned as text per O11
  </done>
</task>

<task type="auto">
  <name>W2-3 (wave 2, parallel-safe): trim publish.test.ts and the RP measure scripts</name>
  <wave>2</wave>
  <files>scripts/measureFieldAveragedRanks.ts, scripts/measureRpCalibration.ts, packages/harness/publish.test.ts, scripts/measureRpCalibration.test.ts, scripts/measureFieldAveragedRanks.test.ts</files>
  <read_first>
    - This plan's context sections, nvn-04 "Comment policy" and "Shared procedure", nvn-04 Task B bullet on the measure scripts
    - scripts/measureRpCalibration.test.ts lines 85-95, 240-300, 530-540, 895-965 (the SOURCE assertions)
  </read_first>
  <action>
Region B. 5 files, 1,876 comment lines at planning time. Order (comment/code, prov, dead):
446/1221 p12 d1 scripts/measureFieldAveragedRanks.ts (S2 tracer; never trimmed, pnp-held); 635/1074 p17 d2 scripts/measureRpCalibration.ts (2nd pass, but F4 grew it by about 200 comment lines); 551/4202 p60 d9 packages/harness/publish.test.ts (commit per O9 window batch); 174/998 p23 scripts/measureRpCalibration.test.ts; 70/284 p3 d1 scripts/measureFieldAveragedRanks.test.ts.

Follow S0-S6 with O1-O11.

Measure scripts: keep what a re-runner needs (inputs, flags, output path, each arm's and metric's definition, the walk-forward and one-simulator constraints the SOURCE assertions pin, and why reporting-slice overrides do not exist); delete result write-ups, figure tables, experiment diaries and arm-verdict narration (the numbers live in docs/models/rp-attribution.md and data/baselines). measureRpCalibration.ts is SOURCE-read over raw text: never delete or add comment text matching the tokens listed in the context section. measureFieldAveragedRanks.ts: one line on what the baked and field-averaged arms compare; delete PD-04 and plan ids (the error-message string literal that names PD-04 is code, N2: leave it).
publish.test.ts: K6, one line per pinned regression; delete plan-history describe narration in comments only (describe and it titles are strings, N2). Its `buildCompareArtifact({` test reads publish.ts, not this file; W2-1 owns publish.ts.

Tests per file and at S1/S6 (repo root, Bash timeout 600000): `npx vitest run --project node packages/harness/publish.test.ts scripts/measureRpCalibration.test.ts scripts/measureFieldAveragedRanks.test.ts scripts/secrets-boundary.test.ts`. Typecheck at S1 and S6: `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>O5 guard over `--region B` with W2-3's BASE: filtered FAIL lines empty, sealed count 0; `delta ... --region B --top 500 | grep -F -f "$LOG/W2-3-files.txt"` shows dCode 0 on every row; `count --region B --all | grep -F -f "$LOG/W2-3-files.txt"` prov sums to 23 or less (from 115); the test command above and root tsc match S1.</automated>
  </verify>
  <done>
    - All 5 files trimmed (or in the stopped-at list), comment-only, dCode 0, summed prov down at least 80%
    - Scoped tests and root tsc match baselines
    - Report returned as text per O11
  </done>
</task>

<task type="auto">
  <name>W2-4 (wave 2, parallel-safe): trim rankingPoints season rule modules and constants</name>
  <wave>2</wave>
  <files>packages/core/rankingPoints/2025.ts, packages/core/rankingPoints/2026.ts, packages/core/rankingPoints/2023.ts, packages/core/rankingPoints/2024.ts, packages/core/rankingPoints/2022.ts, packages/core/rankingPoints/rules.ts, packages/core/rankingPoints/constants.ts, packages/core/rankingPoints/2016.ts, packages/core/rankingPoints/2017.ts, packages/core/rankingPoints/2018.ts, packages/core/rankingPoints/2019.ts, packages/core/rankingPoints/2020.ts, packages/core/rankingPoints/rules.test.ts, packages/core/rankingPoints/reconciliation.test.ts</files>
  <read_first>
    - This plan's context sections, nvn-04 "Comment policy" and "Shared procedure", nvn-04 Task C bullet on rankingPoints/20xx.ts and constants.ts
    - `node "$CMT" readers --region C` output
  </read_first>
  <action>
Region C. 14 files, 1,425 comment lines at planning time (1,278 without the tail file). Order (comment/code, prov, dead):
Never trimmed: 139/167 p12 d5 2025.ts (S2 tracer); 70/103 p10 d2 2026.ts; 75/98 p8 d1 2023.ts; 84/130 p6 d2 2024.ts; 56/97 p5 d1 2022.ts; 35/54 p4 rules.ts.
Second pass (trimmed before F4; F4 touched them again): 319/199 p3 d1 constants.ts; 190/158 p1 2016.ts; 68/108 p1 2017.ts; 63/102 p1 2018.ts; 64/77 p1 2019.ts; 55/65 p1 2020.ts.
Tests: 60/397 p9 rules.test.ts; tail only if narration is heavy: 147/359 p0 d1 reconciliation.test.ts.

Follow S0-S6 with O1-O11, one commit per file or per three small season modules.

KEEP: each season's game-manual RP rules and thresholds with their manual source in one line; TBA field quirks and per-season field renames; roll-up hazards reconciliation tests guard (one or two lines); in constants.ts each threshold's source in one line. The "dead" hits in these files are mostly plain-English "spread"/"tune"; delete only real deleted-feature narration.
DELETE: F4/F10 quick-task ids, D-/F- ids, dates, calibration-run narration and before/after figures (one figure plus a docs/models pointer at most, D5).

Tests per batch: `npx vitest run --project node packages/core/rankingPoints/rules packages/core/rankingPoints/reconciliation packages/core/rankingPoints/analyticPmf.seasons packages/core/rankingPoints/predictThresholdsGolden`. At S1 and S6 (Bash timeout 600000): `npx vitest run --project node packages/core/rankingPoints packages/core/isomorphic.test.ts packages/harness/browserSafeSchemas.test.ts` and `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>O5 guard over `--region C` with W2-4's BASE: filtered FAIL lines empty, `sealed file changed` count 0; `delta ... --region C --top 500 | grep -F -f "$LOG/W2-4-files.txt"` shows dCode 0 on every row; `count --region C --all | grep -F -f "$LOG/W2-4-files.txt"` prov sums to 12 or less (from 62); the S6 test command and root tsc match S1.</automated>
  </verify>
  <done>
    - All files trimmed (reconciliation.test.ts may be skipped with a reason), comment-only, dCode 0, summed prov down at least 80%
    - rankingPoints suite, isomorphic and browserSafeSchemas tests and root tsc match baselines
    - Report returned as text per O11
  </done>
</task>

<task type="auto">
  <name>W2-5 (wave 2, parallel-safe): trim rankingPoints analytic machinery and its tests</name>
  <wave>2</wave>
  <files>packages/core/rankingPoints/marginals.ts, packages/core/rankingPoints/empiricalMoments.ts, packages/core/rankingPoints/meanShift.ts, packages/core/rankingPoints/moments.ts, packages/core/rankingPoints/analyticPmf.ts, packages/core/rankingPoints/fieldAveraged.ts, packages/core/rankingPoints/analyticPmf.test.ts, packages/core/rankingPoints/analyticPmfGolden.test.ts, packages/core/rankingPoints/fieldAveraged.test.ts, packages/core/rankingPoints/analyticPmf.seasons.test.ts, packages/core/rankingPoints/empiricalMoments.test.ts, packages/core/rankingPoints/analyticPmf.universal.test.ts, packages/core/rankingPoints/predictThresholdsGolden.test.ts, packages/core/rankingPoints/marginals.test.ts, packages/core/rankingPoints/marginals.lattice.test.ts, packages/core/rankingPoints/analyticPmf.lattice.test.ts, packages/core/rankingPoints/meanShift.test.ts</files>
  <read_first>
    - This plan's context sections, nvn-04 "Comment policy" and "Shared procedure", nvn-04 Task C bullet on analyticPmf.ts and marginals.ts
    - packages/core/rankingPoints/analyticPmf.test.ts lines 420-470 (export-set and code-only assertions)
  </read_first>
  <action>
Region C. 17 files, 1,647 comment lines at planning time. Order (comment/code, prov, dead):
Production: 296/446 p2 d1 marginals.ts (S2 tracer; lattice marginals landed in F4); 160/118 p0 empiricalMoments.ts; 90/115 p3 d1 meanShift.ts (new in F4); 56/8 p5 d6 moments.ts (88% comment); 314/534 p3 analyticPmf.ts (2nd pass); 212/164 p1 d6 fieldAveraged.ts (2nd pass).
Tests: 177/538 p12 analyticPmf.test.ts; 81/116 p5 analyticPmfGolden.test.ts; 76/244 p4 d1 fieldAveraged.test.ts; 39/221 p3 d1 analyticPmf.seasons.test.ts; 37/227 p2 d2 empiricalMoments.test.ts; 29/98 p4 d1 analyticPmf.universal.test.ts. Tail, only if narration is heavy: 26/34 p2 predictThresholdsGolden.test.ts; 24/334 p2 marginals.test.ts; 12/289 p1 marginals.lattice.test.ts; 9/143 p3 analyticPmf.lattice.test.ts; 9/190 p1 meanShift.test.ts.

Follow S0-S6 with O1-O11, one commit per production file; tests may batch two or three per commit.

KEEP: numerical-stability reasons (clamps, epsilons, log-space, lattice support bounds), gaussian-family and lattice-marginal reasons, the mean shift's direction and what it corrects in one or two lines, the negative-binomial path in one line stating it is unused by every current season, `tieProbability` running from the `varianceD > 0` branch; golden tests: what a digest pins and how to regenerate it, one line each.
DELETE: PCM, tuning and calibration-arm narration, F4/F10 ids and dates, before/after figure series (one figure plus `docs/models/rp-bonus-gap-attribution.md` or `rp-attribution.md` pointer at most). analyticPmf.ts: never touch a line starting with `export`. moments.ts: its dead hits are deleted-feature narration; delete, do not reword (N3, N4).

Tests per commit: `npx vitest run --project node packages/core/rankingPoints/<basename without .ts>` for the files in the batch plus `packages/core/rankingPoints/analyticPmf.test.ts`. At S1 and S6 (Bash timeout 600000): `npx vitest run --project node packages/core/rankingPoints packages/core/isomorphic.test.ts packages/harness/browserSafeSchemas.test.ts` and `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>O5 guard over `--region C` with W2-5's BASE: filtered FAIL lines empty, sealed count 0; `delta ... --region C --top 500 | grep -F -f "$LOG/W2-5-files.txt"` shows dCode 0 on every row; `count --region C --all | grep -F -f "$LOG/W2-5-files.txt"` prov sums to 10 or less (from 53); the S6 test command and root tsc match S1.</automated>
  </verify>
  <done>
    - Production files and the six named tests trimmed (tail tests optional), comment-only, dCode 0, summed prov down at least 80%
    - rankingPoints suite, isomorphic and browserSafeSchemas tests and root tsc match baselines
    - Report returned as text per O11
  </done>
</task>

<task type="auto">
  <name>W2-6 (wave 2, parallel-safe): trim the Worker tick files and the web bonus-dot and theme files</name>
  <wave>2</wave>
  <files>apps/web/src/lib/bonusRp.ts, apps/web/src/components/team/BonusRpDots.tsx, apps/web/src/styles/theme.css, apps/worker/src/scheduled.ts, apps/worker/src/stateProbe.ts, apps/worker/test/scheduled.rp.test.ts, apps/worker/test/stateProbe.test.ts, apps/web/src/lib/bonusRp.test.ts, apps/web/src/components/team/BonusRpDots.test.tsx</files>
  <read_first>
    - `Skill("sketch-findings-sigmascout")` BEFORE touching theme.css, BonusRpDots.tsx or bonusRp.ts, so palette, contrast, interval and chart-craft rationale is recognised as K2
    - This plan's context sections, nvn-04 "Comment policy" and "Shared procedure", nvn-04 Task A theme.css bullet and Task C Worker bullet
    - `.claude/CLAUDE.md` "Cloudflare Storage & Compute Topology" and the "What NOT to Use" Cron row (sustained 10ms CPU, 50 subrequests, D1 batching)
  </read_first>
  <action>
Regions A and C (pass both to the tool: `--region A --region C`). 9 files, 1,718 comment lines at planning time. Order (comment/code, prov, dead):
Web: 101/81 p1 lib/bonusRp.ts plus 41/55 p1 components/team/BonusRpDots.tsx (S2 tracer pair, one commit); 492/537 p2 d5 styles/theme.css (2nd pass, still 48% comment).
Worker: 597/936 p2 d7 apps/worker/src/scheduled.ts (2nd pass); 202/586 p0 apps/worker/src/stateProbe.ts (2nd pass).
Tests: 138/857 p4 apps/worker/test/scheduled.rp.test.ts; 114/560 p1 apps/worker/test/stateProbe.test.ts; 27/132 p2 d1 lib/bonusRp.test.ts; 6/95 p1 BonusRpDots.test.tsx.

Follow S0-S6 with O1-O11, one commit per file (the tracer pair together).

Web KEEP (K2): contrast ratios, rarity-tier token constraints, colour-blind rules, scrollbar/overflow/touch reasons, that bonus dots fill to the predicted odds and why the fill uses the dot's inner size; never let a comment quote a `--compare-algo-*` or `--sim-*` declaration (first-match regex readers). Web DELETE: sketch numbers, F10/quick-task ids, colour-change history.
Worker KEEP (K1): sustained-CPU and subrequest arithmetic, D1 batching, tick ordering, partial-roster gates, why the live tier folds one algorithm, the shape-16 seed resume in one line. Worker DELETE: multi-algorithm cutover, retired-id and live-tier history, dates. Do not fix the known partial-state re-pricing or backlog-budget bugs (N5); an existing one-line warning may stay, no new TODOs.

Commands (save to `$LOG/W2-6-*.txt`, read counts):
- Worker tests per file and S1/S6 (repo root, Bash timeout 600000): `npx vitest run --project node apps/worker`; worker typecheck at S1/S6: `npx tsc --noEmit -p apps/worker/tsconfig.json`.
- Web tests per file and S1/S6 (from apps/web; this runs the web project only): `npx vitest run src/lib/bonusRp.test.ts src/lib/simAxis.test.ts src/styles src/components/team src/components/MetricValue.test.tsx src/components/compare/comparePalette.test.ts src/favicon.test.ts`.
- Web typecheck at S1/S6 from the repo root: `npx tsc --noEmit -p apps/web/tsconfig.json` (the root tsc does not cover apps/web; compare `error TS` counts with S1, not with zero).
- Web build at S1/S6 from apps/web: `npx vite build` (must end with `built in`).
  </action>
  <verify>
    <automated>O5 guard over `--region A --region C` with W2-6's BASE: filtered FAIL lines empty, sealed count 0; `delta ... --region A --region C --top 800 | grep -F -f "$LOG/W2-6-files.txt"` shows dCode 0 on every row; `count --region A --region C --all | grep -F -f "$LOG/W2-6-files.txt"` prov sums to 2 or less (from 14); worker vitest, worker tsc, the web vitest command, web tsc and `vite build` match S1.</automated>
  </verify>
  <done>
    - All 9 files trimmed (or in the stopped-at list), comment-only, dCode 0, summed prov down at least 80%
    - Worker tests and tsc, scoped web tests, web tsc and build match baselines
    - Report returned as text per O11
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor edit -> committed source | A "comment" edit could silently change code, a literal, a type or a pragma; the rename is the only intended code change |
| six executors plus two interactive sessions -> one git index | Concurrent commits on a shared checkout can absorb another writer's edits |
| repo -> transcript | `.env` holds live TBA and R2 credentials |
| sealed files -> published holdout claim | Blob shas of five files attest a published number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53h-01 | Tampering | Every Wave 2 file | high | mitigate | `comments.mjs guard` per executor BASE, filtered to the task's file list (O5); `delta` dCode = 0; O6 garble re-read before each commit; scoped tests and typechecks against S1 baselines |
| T-53h-02 | Tampering | W1-1 rename | medium | mitigate | Old identifier grep must be empty; root tsc (covers packages, scripts and their tests) and the scoped harness/scripts suites equal pre-rename baselines; no serialized key renamed |
| T-53h-03 | Tampering | Sealed SPR files | high | mitigate | N1; guard fails on any diff; W1-1 verify diffs the five paths |
| T-53h-04 | Repudiation | Shared git index | medium | mitigate | O4 ownership check before editing, `git commit -- <explicit paths>` only (O7), retry once on index.lock, `git status` after each commit; Wave 2 file lists are disjoint |
| T-53h-05 | Information disclosure | `.env` | high | mitigate | N6; no task needs it (no network, no publish, no deploy) |
| T-53h-06 | Tampering | Tests that read source text | medium | mitigate | Known source-reader list with the exact tokens; restore text on failure, never edit the test |
| T-53h-07 | Tampering | docs/first-paint-measurement.md history | low | mitigate | W1-2 changes citations only; measured figures and historical statements stay as written |
</threat_model>

<verification>
Orchestrator, after Wave 1 returns:
- `git log --oneline -3` shows the `refactor(260914-53h)` commit (and the `docs(260914-53h)` doc commit); `git grep -n consistencyByTeam -- ':!.planning'` prints nothing. Only then dispatch Wave 2 (cap 4 concurrent, W2-1/W2-2/W2-4/W2-6 first).

Orchestrator, after all Wave 2 tasks return:
- Take the earliest Wave 2 BASE. `node "$CMT" guard --base <it> --region A --region B --region C` shows no FAIL line naming any file from the six task lists and no sealed-file line (foreign FAILs are reported, not fixed).
- `node "$CMT" delta --base <it> --region A --region B --region C --top 800` shows dCode = 0 on every 53h file row; record the combined comment and prov drop.
- For any S5 stopped-at list, dispatch a follow-up executor with the same task text restricted to the remaining files, before closing.
- Full check from the repo root: `npx vitest run` (both projects; Bash timeout 600000), `npx tsc --noEmit`, `npx tsc --noEmit -p apps/worker/tsconfig.json`, `npx tsc --noEmit -p apps/web/tsconfig.json`; nothing new failing relative to the executors' baselines (failures belonging to other sessions' uncommitted work are named, not fixed).
</verification>

<success_criteria>
- The old accessor name is gone outside `.planning/`; tsc and tests prove the rename.
- docs/first-paint-measurement.md citations resolve at HEAD.
- Every held-back file from the todo is comment-trimmed under the nvn-04 policy with zero non-comment changes, provenance markers down at least 80% per task, and live rationale kept.
- Tests, typechecks and the web build match baselines; nothing published, deployed or pushed.
</success_criteria>

<output>
Orchestrator close-out checklist (executors return text only):

1. Write `.planning/quick/260914-53h-resolve-todo-finish-nvn-held-back-trims-/260914-53h-SUMMARY.md` with the Write tool (never a heredoc) from the eight reports: commits, per-task before/after comment/prov totals, test/typecheck/build baselines vs final, kept-on-purpose notes, foreign files, bugs noticed, follow-up dispatches.
2. Resolve the todo: `git mv .planning/todos/pending/finish-nvn-held-back-trims-and-sigma-rename.md .planning/todos/completed/` FIRST, then Edit the moved file to add frontmatter `resolved_date: 2026-09-14` and `resolved_by: quick task 260914-53h (<rename sha>, <doc sha>, <trim commit range>)` and a `## Resolution (2026-09-14)` section (one short paragraph per todo item). Then `git add .planning/todos/completed/finish-nvn-held-back-trims-and-sigma-rename.md` (git mv before editing, or the edit is silently left out of the commit).
3. Before touching STATE.md, re-read its tail (another session may have appended). Confirm `grep -c "LOCAL PATCH (SigmaScout" ~/.claude/gsd-core/bin/gsd-tools.cjs` prints 1, then `gsd-tools quick-tasks-append --task "<description with no pipe character>" --dir 260914-53h-resolve-todo-finish-nvn-held-back-trims- --commit <close-out sha>`.
4. Commit with explicit paths only: `docs(260914-53h): summary, resolve todo finish-nvn-held-back-trims-and-sigma-rename` covering the SUMMARY.md, both todo paths and STATE.md, with the Co-Authored-By trailer; then `git status --short -- .planning` shows none of them.
5. Do not push unless Jacob asks; if he does, check `git log --oneline origin/main..main` first, because a push deploys every other session's commits too.
</output>
