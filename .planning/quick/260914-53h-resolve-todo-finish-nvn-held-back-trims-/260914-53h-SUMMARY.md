---
quick_id: 260914-53h
slug: resolve-todo-finish-nvn-held-back-trims-
date: 2026-09-14
status: complete
resolves_todo: finish-nvn-held-back-trims-and-sigma-rename
commits:
  - 3e71a43e refactor(260914-53h): rename consistencyByTeam to sigmaScoreByTeam
  - 99aa8e45 docs(260914-53h): replace stale citations in first-paint-measurement.md
  - 51 docs(260914-53h) comment-trim commits (W2-1..W2-6), listed below
---

# 260914-53h: finish the nvn held-back trims and the Sigma rename

This closes the pending todo `finish-nvn-held-back-trims-and-sigma-rename`. Nothing changes behaviour or published bytes, so no publish, deploy or push was needed.

## Result

- **Rename (W1-1, 3e71a43e):** `SigmaScoutLayer.consistencyByTeam()` and the `preSchedule.ts` input of the same name are now `sigmaScoreByTeam`, across 10 files. That name matches `SigmaScoreAccumulator.scoreByTeam()`, which the method delegates to. No occurrence remains outside `.planning/`. Root tsc stays at 0 errors, and the scoped tests pass 355/355 before and after.
- **Doc citations (W1-2, 99aa8e45):** `docs/first-paint-measurement.md` cited `docs/publish-budget.md` by line number twice; both now use pointers without line numbers. The `handleValueChange` reference now names `markStartAndSetQuery` → `markSearchKeystroke()`. Measured figures are untouched.
- **Comment trims (W2-1..W2-6):** 60 files, guarded as comment-only against 3e71a43e across regions A+B+C: `GUARD OK`, 60 checked, 0 failed, no sealed file changed. From 3a6ff8ca to HEAD, comment lines fell by 5,650, with code lines unchanged and 4 blank lines removed.
- **Full root suite after everything:** 237 test files passed, 5,304 tests passed, 1 skipped.

| Task | Files | Comment lines | Provenance markers |
|---|---|---|---|
| W2-1 publish.ts | 1 | 1,902 → 609 | 264 → 0 |
| W2-2 harness layer, snapshot, schedules + tests | 14 | 1,840 → 946 | 54 → 0 |
| W2-3 publish.test.ts, RP measure scripts + tests | 5 | 1,876 → 790 | 115 → 0 |
| W2-4 rankingPoints season modules, constants, rules + tests | 14 | 1,425 → 606 | 62 → 0 |
| W2-5 rankingPoints analytic machinery + tests | 17 | 1,647 → 837 | 53 → 0 |
| W2-6 Worker tick files, web theme and bonus dots + tests | 9 | 1,718 → 970 | 14 → 2 (both are the test's own "Phase 1/2" step labels) |

Each executor matched its own baselines exactly:
- **W2-6:** Worker tests 190/190, web tests 458/458, Worker and web tsc 0 errors, `vite build` succeeds.
- **W2-4 and W2-5:** rankingPoints, isomorphic and browserSafeSchemas tests 781/781.
- **W2-1:** 240/240.
- **W2-3:** 369/369.
- **W2-2:** 229/229.

## Wrong comments corrected or removed (comment-only)

- **2016.ts:** claimed 2016 was not on the site (`FIRST_SEASON` is 2016 now).
- **constants.ts `predictThresholds`:** claimed 2025 `autoBonus` is untracked and that no bonus overstates.
- **sigmaScore.ts:** `fold()`'s JSDoc was misplaced above `fromBeliefs`.
- **Worker `buildOutcomeInput`:** its doc said "fifth" argument; `toSimMatchInput` takes four.
- **theme.css:** three stale colour notes (slate-300 on a slate-200 border, green-50 surfaces, a 4.56:1 figure that is 5.13:1 on white).
- **bonusRp.ts:** "per-bonus data not published yet".
- **Deleted-module pointers:** the rankingPoints analytic files still pointed at deleted modules (`linkFunctions.ts`, `distribution.ts`, `rpPmfForMatch`). fieldAveraged.ts listed callers that no longer exist, and the universal test said `pRedWin` is never read.
- **measureRpCalibration.test.ts header:** said one `SigmaScoutLayer` construction; the test asserts two.

## Left alone on purpose (strings and test names are out of scope for a comment trim)

- Planning ids inside `describe`/`it` titles across publish.test.ts, measureRpCalibration.test.ts and the rankingPoints tests.
- Runtime strings with planning ids:
  - publish.ts `buildPreScheduleSidecarForEvent` logs `(PD-06)` and `(PD-04 ...)`.
  - The stateProbe.ts `rp=0` warning says "plan 09-08" and "predate Phase 9".
  - measureFieldAveragedRanks.ts `renderDoc`, its banner and one error message carry D-16/D-17/PD-04/A-FA ids.
- `untrackedGate.note` strings in rankingPoints 2023/2024/2025 tell readers to run `pnpm rp:conservative-branch`, which no package.json defines. They also cite `docs/models/sigma1-rp-verification.md`, which exists under its retired name.
- publish.test.ts `eventArtifactParams()` still sets `algorithmId: "vpr"` in a fixture literal.
- stateSnapshot.test.ts: the "Missing league row" banner sits above the unknown-passenger-key block.

These are candidates for a later string-and-test-name cleanup; none is a behaviour bug.

## Notes

- **Planning-time corrections to the todo:** pnp had already deleted `scheduleTemplates.ts`, `rankingPoints/2021.ts` never existed, and `algorithmIdentity.test.ts` is gone.
- **Guard tool location:** it was copied into this session's scratchpad (`53h-comments/comments.mjs`) and is not in the repo.
- **No interference:** no foreign commits landed on any of these files during the run, and no executor skipped a file for foreign edits.
