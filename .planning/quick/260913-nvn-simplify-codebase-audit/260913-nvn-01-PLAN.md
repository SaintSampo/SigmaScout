---
phase: quick-260913-nvn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Task 1: deleted
  - packages/gbr/
  - packages/pcm/
  - docs/models/gbr.md
  - packages/spr/tune.ts
  - packages/spr/ablate.ts
  - packages/spr/score.ts
  - packages/spr/slice.ts
  - packages/spr/innovations.ts
  - packages/spr/carryover.ts
  - packages/spr/equivalence.ts
  - scripts/deleteRetiredAlgorithmObjects.ts
  - scripts/deleteRetiredAlgorithmObjects.test.ts
  - scripts/deleteOrphanedDemoTeamObjects.ts
  - scripts/deleteOrphanedDemoTeamObjects.test.ts
  - scripts/mockRankDistribution.ts
  - scripts/verifyAllianceUncertaintyIdentity.ts
  - docs/ui/rank-distribution-mock.md
  - docs/models/alliance-uncertainty-identity.md
  - docs/models/sigma1-identifiability.md
  - docs/models/sigma1-reparameterization.md
  - docs/models/sigma1-sensitivity-screen.md
  - docs/models/sigma1-tuning-results.md
  - scripts/recon-tba-fields.ts
  - scripts/recon-rocket-rp-2019.ts
  - scripts/statboticsComponentMaps.ts
  - scripts/statboticsComponentMaps.test.ts
  - scripts/verify-native-module.ts
  - packages/harness/eventScopeDiagnostic.ts
  - packages/harness/eventScopeDiagnostic.test.ts
  - data/diagnostics/opr-event-scope-2026-08.json
  - packages/harness/identifiability.ts
  - packages/harness/identifiability.test.ts
  - packages/harness/baselineFingerprint.ts
  - packages/harness/baselineFingerprint.test.ts
  - packages/harness/rpConservativeBranch.ts
  - packages/harness/algorithmIdentity.test.ts
  - apps/worker/src/bundleSmoke.ts
  - scripts/measureEpaDeviations.ts
  - scripts/measureEpaDeviations.test.ts
  - scripts/measureAllianceReconstruction.ts
  - scripts/measureAllianceReconstruction.test.ts
  # Task 1: edited
  - package.json
  - .gitignore
  - scripts/rpPredictThresholdsGolden.ts
  - packages/spr/holdout.ts
  - packages/spr/holdout.test.ts
  - packages/spr/sealedPaths.ts
  - packages/core/rankingPoints/marginals.ts
  - packages/core/rankingPoints/analyticPmf.ts
  - packages/core/rankingPoints/analyticPmf.test.ts
  - packages/core/rankingPoints/analyticPmf.universal.test.ts
  - packages/core/rankingPoints/constants.ts
  - packages/core/rankingPoints/2019.ts
  - packages/core/algorithms/types.ts
  - packages/core/algorithms/breakdown/2016.ts
  - packages/core/algorithms/breakdown/2017.ts
  - packages/core/algorithms/breakdown/index.ts
  - packages/core/algorithms/simulation/rankSimulation.ts
  - packages/harness/eventBootstrap.ts
  - packages/harness/eventBootstrap.test.ts
  - scripts/pruneR2Generations.ts
  - packages/core/algorithms/epa.test.ts
  - packages/core/algorithms/epaCarryScale.ts
  - packages/core/algorithms/epaCarryScale.test.ts
  - packages/core/districts/pointModel.ts
  - packages/core/districts/pointModel.test.ts
  - apps/worker/src/stateStore.ts
  - apps/worker/src/scheduled.ts
  - apps/worker/src/stateProbe.ts
  - apps/worker/wrangler.toml
  - packages/harness/publish.ts
  - packages/harness/publish.test.ts
  - packages/harness/payloadBudget.test.ts
  - packages/harness/stateSnapshot.ts
  - scripts/replayRig.ts
  - scripts/recon-shared.ts
  - scripts/recon-breakdown-fields.ts
  - scripts/publishAlgorithmsManifest.test.ts
  - scripts/measureRpCalibration.ts
  - scripts/measureRpCalibration.test.ts
  - scripts/measureMatchBandCoverage.ts
  - scripts/scriptHelpers.ts
  - docs/publish-budget.md
  - docs/worker-operations.md
  - docs/models/sigma1-rp-verification.md
  - .planning/todos/pending/algorithm-identity-sweep-reads-all-of-data.md
  - .planning/todos/completed/algorithm-identity-sweep-reads-all-of-data.md
  - .planning/todos/pending/narrow-offseason-population-ablation-entry.md
  - .planning/todos/completed/narrow-offseason-population-ablation-entry.md
  # Task 2: publish.ts --event mode
  - packages/harness/sigmaScoutLayer.ts
  - packages/harness/sigmaScoutLayer.sigmaFor.test.ts
  - packages/harness/sigmaScore.ts
  - packages/harness/percentiles.ts
  - packages/harness/scheduleTemplates.ts
  - packages/harness/fixtures/schedule-templates/README.md
  - docs/models/rp-layer-config-arms.md
  - .planning/todos/pending/event-mode-replays-cold-and-writes-different-numbers.md
  - .planning/todos/completed/event-mode-replays-cold-and-writes-different-numbers.md
  # Task 3: harness backtest CLI cluster (deleted)
  - packages/harness/cli.ts
  - packages/harness/cli.season-carry.test.ts
  - packages/harness/cli.seasonsSpec.test.ts
  - packages/harness/report.ts
  - packages/harness/report.test.ts
  - packages/harness/artifact.ts
  - packages/harness/artifact.test.ts
  - packages/harness/predictions.ts
  - packages/harness/predictions.test.ts
  - packages/harness/metricHistory.ts
  - packages/harness/metricHistory.test.ts
  # Task 3: edited
  - packages/core/algorithms/opr.test.ts
  - packages/core/algorithms/opr.ts
  - packages/core/algorithms/epa.ts
  - packages/core/algorithms/breakdown/constants.ts
  - packages/harness/selectionProvenance.test.ts
  - packages/harness/selectionProvenance.ts
  - packages/harness/rounding.test.ts
  - packages/harness/rounding.ts
  - packages/harness/score.ts
  - packages/harness/seasonBoundary.ts
  - packages/harness/pageArtifacts.ts
  - packages/harness/metricHistorySchema.ts
  - apps/worker/src/artifactWriter.ts
  - packages/harness/manifestSchemas.ts
  - scripts/measureGeneratedSchedules.ts
  - scripts/measureFieldAveragedRanks.ts
  - scripts/measureRandomSchedules.ts
autonomous: true
requirements:
  - 260913-nvn

estimate:
  tokens: 110000
  raw_tokens: 220000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every approved non-web delete path in AUDIT.md section A, section B row 3, and the parts of section B rows 1-2 that survived re-verification is gone from the tree, and no live code in packages, scripts, apps/worker, package.json or .gitignore names a deleted file, script or symbol (each task's gate grep prints nothing), with the sealed SPR files and lines marked 260913-nvn the only exemptions"
    - "No published number changes: publishSeasons is untouched except for exporting its registry and comment edits, so packages/harness/level1Digest.test.ts and every surviving publish.test.ts suite pass"
    - "publish.ts runs only in --seasons mode; buildSingleEventPublish, runEventMode and their parity tests are gone, and the pending event-mode todo is closed as resolved by deletion"
    - "The harness backtest CLI (cli.ts, report.ts, artifact.ts, predictions.ts, metricHistory.ts and their tests) is gone; the one algorithm registry left is publish.ts's exported BASE_PUBLISH_ALGORITHMS, and all five former ALGORITHMS importers (opr.test.ts, selectionProvenance.test.ts, measureGeneratedSchedules.ts, measureFieldAveragedRanks.ts, measureRandomSchedules.ts) read it instead"
    - "Root npx tsc --noEmit and npx tsc --noEmit -p apps/worker report no error that is not also in the baseline captured before Task 1; the final npx vitest run --project node fails no test that passed in that baseline, and its Test Files total is the baseline total minus the 20 deleted test files"
    - "Items the audit got wrong are NOT deleted and are named in the executor's return: measureRandomSchedules.ts, measureFieldAveragedRanks.ts plus its test, the field-averaged builder in preSchedule.ts, FieldAveragedPreScheduleArtifactSchema, and statbotics.ts"
    - "Nothing in apps/web or .planning/STATE.md is touched, nothing is published, deployed or pushed, and .env is never read"
  artifacts:
    - path: "packages/harness/publish.ts"
      provides: "the single algorithm registry, now exported"
      contains: "export const BASE_PUBLISH_ALGORITHMS"
    - path: "scripts/rpPredictThresholdsGolden.ts"
      provides: "the golden-grid generator, reading mulberry32 from core's rank simulation"
      contains: "simulation/rankSimulation.js"
    - path: ".planning/todos/completed/event-mode-replays-cold-and-writes-different-numbers.md"
      provides: "the closed event-mode todo with its resolution note"
      contains: "CLOSED 2026-09-13"
  key_links:
    - from: "scripts/measureGeneratedSchedules.ts"
      to: "packages/harness/publish.ts"
      via: "BASE_PUBLISH_ALGORITHMS lookup by algorithm id, replacing cli.ts's ALGORITHMS"
      pattern: "BASE_PUBLISH_ALGORITHMS"
    - from: "packages/core/rankingPoints/predictThresholdsGolden.test.ts"
      to: "packages/core/algorithms/simulation/rankSimulation.ts"
      via: "scripts/rpPredictThresholdsGolden.ts imports mulberry32 (byte-identical to the deleted identifiability.ts copy, verified by diff at planning time)"
      pattern: "mulberry32"
    - from: "packages/spr/holdout.ts"
      to: "packages/spr/cli.ts"
      via: "the design-year rejection message names the surviving design-year entry point instead of the deleted score.ts"
      pattern: "spr/cli\\.ts"
---

<objective>
Delete the dead and closed-experiment code outside apps/web that the owner approved in `260913-nvn-AUDIT.md`
sections A and B, so the repo has fewer lines, files and moving parts. Nothing that affects a published number or
the Worker's behavior changes.

Purpose: the owner's first goal is fewer lines, files and less complexity. This plan removes about 23,500 lines and
about 70 files. It also fixes every live pointer that would be left aiming at a deleted file.

Output: three atomic commits (dead set; publish.ts `--event` mode; harness backtest CLI), three todos moved to
`.planning/todos/completed/`, and a summary returned as text. The executor does not write SUMMARY.md.

Re-verification against HEAD `d3b304b0` changed the audit's list in these ways. The executor must follow this list,
not the audit's:

1. **DROPPED from section B row 1: `scripts/measureFieldAveragedRanks.ts` + test, the field-averaged builder in
   `packages/harness/preSchedule.ts` (`FieldContributionInputs`, `buildFieldContributions`,
   `FieldAveragedPreScheduleBuildParams`, `buildFieldAveragedPreScheduleArtifact`), and
   `FieldAveragedPreScheduleArtifactSchema`.** `scripts/measureGeneratedSchedules.ts` is not in the delete set. It
   imports 18 symbols from `measureFieldAveragedRanks.js` (`replaySeason`, `measureSeedNoiseFloor`,
   `evaluateRungOneCriterion`, the CLAUSE_* constants and more), and the two modules form a cycle that
   `measureFieldAveragedRanks.test.ts` pins. The pending high-priority todo `drop-licensed-schedule-templates` needs
   `measureGeneratedSchedules.ts` before the generator can be wired in, so deleting the field-averaged script is not a
   pure delete.
2. **DROPPED from section B row 1: `scripts/measureRandomSchedules.ts`.** The same pending todo tells its reader to
   re-derive the 2025cur / n=1000 floor from this script (its "What to do" step 1). The audit kept only the doc.
3. **DROPPED from section B row 2: `packages/harness/statbotics.ts` (and its test).** `scripts/epaVsStatbotics.ts`
   (the `compare:epa-statbotics` script) imports `statboticsReference` and `fetchStatboticsTeamYears` from it.
4. **Registry move:** instead of making a new module, publish.ts's existing `BASE_PUBLISH_ALGORITHMS` is exported
   and all five surviving `ALGORITHMS` importers use it. Those importers are `packages/core/algorithms/opr.test.ts`,
   `packages/harness/selectionProvenance.test.ts`, `scripts/measureGeneratedSchedules.ts`,
   `scripts/measureFieldAveragedRanks.ts` and `scripts/measureRandomSchedules.ts`. The last two are the kept scripts
   from items 1-2. All three scripts already import `makeRankingPointFiller` from publish.js. This adds no file and
   removes one of the four duplicate registries.
5. **ADDED to Task 2's test edits:** the audit's publish.test.ts line ranges would also delete tests that do not use
   `--event`. Those are the OPR no-band/no-RP gate, the SPR RP-pmf sanity check and the OPR/EPA no-sidecar gate.
   Task 2 lists by name exactly which `it` blocks go.
6. **`ComponentPrediction.variance`** moves from the "small dead items" batch into Task 3, because its only reader
   is `predictions.test.ts`.
7. **ADDED todo closure (flag it in the return):** `narrow-offseason-population-ablation-entry` can only be done by
   running `pnpm measure:epa-deviations`. That script is deleted here with owner approval, so the todo is closed with
   a note that the committed JSON is now a frozen record.
8. **Todo folder convention** is `.planning/todos/completed/`. There is no `done/` folder. Each closed file gets a
   `## CLOSED 2026-09-13` section at the end, following the precedent of commit `0770513d`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-AUDIT.md

## Shared rules for every task (read before touching anything)

**Shared checkout.** A plan-02 executor is editing `apps/web` in this same working tree right now, and other
sessions may be active too.
- Never touch `apps/web/**` or `.planning/STATE.md`.
- Never run `git add -A`, `git add .`, `git commit -a`, `git stash`, `git reset`, or `git checkout -- <path>`.
- Delete files with plain `rm` / `rm -rf`, not `git rm`, so nothing is staged in the shared index early.
- For a NEW path (a todo moved into `completed/`), run `git add <that explicit path>` first. Then commit with
  `git commit -m "<subject>" -m "<body>" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>" -- <explicit paths>`.
  A deleted tracked file or a wholly deleted directory can be passed directly as a path. Pass modified files as
  individual file paths, never as a parent directory.
- Before each commit, run `git status --short`. Build the path list only from entries this task created. Leave
  foreign dirty entries alone and mention them in the return.
- If the commit hits an `index.lock` error, wait a few seconds and retry once.
- After each commit, run `git status --short` again and `git show --stat HEAD`. Confirm every intended path landed
  and nothing foreign did.
- Move todos with `mv` then edit, never `git mv` then edit. An Edit made before a `git mv` can silently miss the
  commit.
- Do not put commit messages or long prose in heredocs, because long markdown breaks Git Bash heredocs on this
  machine. Use several `-m` flags.

**Never edit sealed files:** `packages/spr/model.ts`, `packages/spr/evaluate.ts`, `packages/spr/data.ts`,
`packages/spr/cli.ts`, `packages/core/algorithms/spr.ts`. Never change `SEALED_CODE_PATHS` in
`packages/spr/sealedPaths.ts`; its header comment is editable. `packages/spr/cli.ts` has a comment naming
`tune.ts`/`ablate.ts`. Leave it, because it is sealed.

**Secrets:** never read, cat or echo `.env`. Nothing in this plan needs credentials or the network. Do not publish,
deploy or push.

**Comment rule (applies in every task):** a comment in live code (packages, scripts, apps/worker, package.json,
.gitignore) that names a deleted file, script command or symbol must be reworded or removed. Rewrite the sentence so
it stands without the pointer, and do not add narration. There is one exception: a comment that is provenance for a
shipped number, such as the EPA carry-rescale threshold chosen by a measurement script, may keep the deleted name only
if `(deleted in 260913-nvn)` sits on the SAME line. When a comment lists several parser or copy sites and some are
deleted, remove the enumeration entirely rather than editing the list. Do NOT edit quoted string literals in the RP
rule modules `packages/core/rankingPoints/{2018,2019,2023,2024,2025}.ts`. They attribute a dated measurement to
`pnpm rp:conservative-branch` and stay as recorded provenance. Do NOT edit files under `data/`. Do NOT edit
historical dated records in `docs/`, meaning measurement write-ups and dated "Delete pass" sections. Edit docs only
where a task names the exact spot: an instruction a reader would follow today, or a clickable link to a deleted doc.

**Verification discipline:** read pass/fail counts in the output and never rely on an exit code. Never wrap a
command in `timeout`. Use `npx vitest run`, not `pnpm test`. Put long outputs in your session scratchpad directory,
called SCRATCH here, and read summaries with `tail` / `grep`. A full `npx vitest run --project node` from the repo
root takes several minutes because some tests read the local corpus. Run it with a Bash timeout of 600000. If it
could exceed that, run it in the background with output redirected to a SCRATCH file and wait for the completion
notice. A test that newly fails only because it imports an `apps/web` path, such as
`scripts/measureFieldAveragedRanks.test.ts` pulling `apps/web/src/lib/simQuantile.js`, may be the concurrent plan-02
executor's doing. Check `git log -3 -- apps/web` and report it rather than "fixing" web code.

**Return, not SUMMARY.md:** the final message must contain:
- BASE sha and each commit sha with its subject
- `git diff --shortstat BASE..HEAD` overall and per commit
- baseline vs final tsc (root and worker) and vitest node summary lines, with any failure attributed
- every audit item skipped and why, including the five dropped above
- the added todo closure
- stale mentions left for plan 02 in apps/web: `apps/web/src/components/event/RankDistributionTable.tsx` names
  `docs/ui/rank-distribution-mock.md`, and `apps/web/src/lib/searchParams.test.ts` names the algorithmIdentity sweep
- fields left without any consumer: the breakdown maps' and RP modules' `diagnosticKeys`

<!-- planner-discipline-allow: bundleSmoke -->
<!-- planner-discipline-allow: algorithmIdentity -->
<!-- planner-discipline-allow: identifiability.ts -->
<!-- planner-discipline-allow: baselineFingerprint -->
<!-- planner-discipline-allow: eventScopeDiagnostic -->
<!-- planner-discipline-allow: rpConservativeBranch -->
<!-- planner-discipline-allow: measureEpaDeviations -->
<!-- planner-discipline-allow: measureAllianceReconstruction -->
<!-- planner-discipline-allow: deleteRetiredAlgorithmObjects -->
<!-- planner-discipline-allow: deleteOrphanedDemoTeamObjects -->
<!-- planner-discipline-allow: mockRankDistribution -->
<!-- planner-discipline-allow: statboticsComponentMaps -->
<!-- planner-discipline-allow: readAndDeserializeScopedState -->
<!-- planner-discipline-allow: pmfStandardDeviation -->
<!-- planner-discipline-allow: maxRookieBonus -->
<!-- planner-discipline-allow: runEventMode -->
<!-- planner-discipline-allow: buildSingleEventPublish -->
<!-- planner-discipline-allow: SingleEventPublishResult -->
<!-- planner-discipline-allow: deriveSeasonFromEventKey -->
<!-- planner-discipline-allow: --event -->
<!-- planner-discipline-allow: PredictionRecordSchema -->
<!-- planner-discipline-allow: HarnessArtifactSchema -->
<!-- planner-discipline-allow: reportBreakdownParseFailures -->
<!-- planner-discipline-allow: breakdownParseFailureCountOf -->
<!-- planner-discipline-allow: runSeasons -->
<!-- planner-discipline-allow: pnpm harness -->
<!-- planner-discipline-allow: cli.ts -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline, then delete the verified-dead set (gbr, pcm, spr research files, one-shot scripts, harness diagnostics, sigma1 docs, bundleSmoke, small core/worker dead items, algorithmIdentity.test.ts, the two closed EPA/alliance measure scripts)</name>
  <files>package.json, .gitignore, scripts/rpPredictThresholdsGolden.ts, packages/spr/holdout.ts, packages/spr/holdout.test.ts, packages/spr/sealedPaths.ts, packages/core/rankingPoints/marginals.ts, packages/core/rankingPoints/analyticPmf.ts, packages/core/rankingPoints/analyticPmf.test.ts, packages/core/rankingPoints/analyticPmf.universal.test.ts, packages/core/districts/pointModel.ts, packages/core/districts/pointModel.test.ts, apps/worker/src/stateStore.ts, docs/publish-budget.md, docs/worker-operations.md, docs/models/sigma1-rp-verification.md, plus the deleted paths and comment-only edits listed in frontmatter</files>
  <read_first>
    - .planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-AUDIT.md (sections A and B; this plan's objective overrides it where they differ)
    - package.json (scripts block)
    - packages/spr/sealedPaths.ts (confirm SEALED_CODE_PATHS is still model/evaluate/data/cli plus core spr.ts before deleting any spr file)
    - packages/spr/holdout.ts (`resolveYears`) and packages/spr/holdout.test.ts (describe "resolveYears")
    - scripts/rpPredictThresholdsGolden.ts (header + imports)
    - docs/publish-budget.md section "### Version-retirement procedure (routine, for the next algorithm bump)" and section "## Baseline provenance (D-09/D-10/D-11)"
  </read_first>
  <action>
Step 0, BASELINE, before any change. Record `BASE=$(git rev-parse HEAD)` and `git status --short`, and note any
foreign dirty files. From the repo root, run each of these into SCRATCH and record the summary lines plus the
failing test-file list or error list:
- `npx vitest run --project node` into SCRATCH/vitest-baseline.txt. Record the "Test Files" and "Tests" lines and
  every FAIL line.
- `npx tsc --noEmit` into SCRATCH/tsc-root-baseline.txt.
- `npx tsc --noEmit -p apps/worker` into SCRATCH/tsc-worker-baseline.txt. The file `apps/worker/tsconfig.json`
  exists.

Step 1, DELETE with rm / rm -rf. Per AUDIT section A and section B rows 1 (reduced) and 3, delete exactly these:
- `packages/gbr/`, the whole directory including `results/` and `HOLDOUT-RESULT.txt`, and `docs/models/gbr.md`
- `packages/pcm/`, the whole directory
- `packages/spr/{tune,ablate,score,slice,innovations,carryover,equivalence}.ts`
- `scripts/deleteRetiredAlgorithmObjects.ts` and its `.test.ts`
- `scripts/deleteOrphanedDemoTeamObjects.ts` and its `.test.ts`
- `scripts/mockRankDistribution.ts`, `scripts/verifyAllianceUncertaintyIdentity.ts`,
  `docs/ui/rank-distribution-mock.md`, `docs/models/alliance-uncertainty-identity.md`
- `docs/models/sigma1-identifiability.md`, `docs/models/sigma1-reparameterization.md`,
  `docs/models/sigma1-sensitivity-screen.md`, `docs/models/sigma1-tuning-results.md`. KEEP
  `docs/models/sigma1-rp-verification.md`.
- `scripts/recon-tba-fields.ts`, `scripts/recon-rocket-rp-2019.ts`. KEEP every `docs/data/*.md`.
- `scripts/statboticsComponentMaps.ts` and its `.test.ts`
- `scripts/verify-native-module.ts`
- `packages/harness/eventScopeDiagnostic.ts` and its `.test.ts`, and
  `data/diagnostics/opr-event-scope-2026-08.json`. KEEP `data/diagnostics/epa-deviation-ablation.json`.
- `packages/harness/identifiability.ts` and its `.test.ts`
- `packages/harness/baselineFingerprint.ts` and its `.test.ts`
- `packages/harness/rpConservativeBranch.ts`
- `packages/harness/algorithmIdentity.test.ts`
- `apps/worker/src/bundleSmoke.ts`
- `scripts/measureEpaDeviations.ts` and its `.test.ts`
- `scripts/measureAllianceReconstruction.ts` and its `.test.ts`

That is 14 deleted test files. Do NOT delete `scripts/measureRandomSchedules.ts`,
`scripts/measureFieldAveragedRanks.ts`, its test, anything in `packages/harness/preSchedule.ts`, or
`packages/harness/fixtures/extract-digest-slice.ts`. The last one regenerates the live `digest-slice.json` fixture.

Step 2, EDITS THAT KEEP THINGS COMPILING:
- `package.json`: remove these 17 script entries and nothing else: verify:native, recon:tba, identifiability,
  event-scope-diagnostic, fingerprint, gbr:eval, gbr:holdout, gbr:tune, pcm:eval, pcm:holdout,
  rp:conservative-branch, measure:alliance-reconstruction, measure:epa-deviations, mock:rank-distribution,
  cleanup:retired-objects, cleanup:orphaned-demo-teams, recon:rocket-rp. Keep `measure:field-averaged` and
  `harness`; Task 3 removes `harness`.
- `scripts/rpPredictThresholdsGolden.ts`: change the `mulberry32` import to come from
  `../packages/core/algorithms/simulation/rankSimulation.js`. The two function bodies were diffed byte-identical at
  planning time; re-run that diff before deleting identifiability.ts if you want to be sure. Fix any header comment
  that names identifiability.ts.
- `packages/spr/holdout.ts`: `resolveYears`'s design-year error becomes
  `holdout: ${y} is not a holdout year - design years (2016-2022) are evaluated by packages/spr/cli.ts`, and its doc
  comment says the message points at cli.ts. In `packages/spr/holdout.test.ts`, rename the test to "rejects a design
  year, pointing the caller at cli.ts" and change the second regex to match `cli\.ts`.
- `packages/spr/sealedPaths.ts` header comment: "every holdout/pcm run" becomes "every holdout run". Leave the list
  alone.
- `packages/core/rankingPoints/marginals.ts` erf header: drop the `packages/pcm/model.ts:135` entry and fix the stated
  copy count.
- `packages/core/rankingPoints/analyticPmf.ts`: delete `pmfStandardDeviation`. In `analyticPmf.test.ts`, remove
  "pmfStandardDeviation" from the pinned export Set. In `analyticPmf.universal.test.ts`, delete its three
  `pmfStandardDeviation` `it` blocks and its import, retitle the describe to pmfMean only, and fix the header comment.
  Fix the `packages/core/algorithms/types.ts` comment that names it.
- `packages/core/districts/pointModel.ts`: delete `maxRookieBonus`, and drop its name from the
  `UnknownDistrictSeasonError` doc comment. In `pointModel.test.ts`, delete the `maxRookieBonus` describe and the
  import name.
- `apps/worker/src/stateStore.ts`: delete `readAndDeserializeScopedState` and its doc comment. Rewrite the header
  paragraph that names it so it states that the Worker reads rows through `stateSnapshot.ts`'s `deserializeState`
  and never grows a second deserializer. Remove imports left with no use in the file, checked with grep: the
  `deserializeState` value import and the EpaState/OprState/SprState type imports. Keep the `StateRow` type exports.
  In `apps/worker/src/scheduled.ts`, reword the comment that names the deleted function so it points at
  `deserializeState` throwing `MissingLeagueRowError`.

Step 3, DOCS THAT WOULD NOW MISLEAD, and only these:
- `docs/publish-budget.md`: replace the BODY of "### Version-retirement procedure (routine, for the next algorithm
  bump)" with two or three sentences. They say the per-id retirement tool was deleted in 260913-nvn, and that the
  routine for any superseded generation is now `pnpm cleanup:r2-generations`, described in the "Delete pass —
  2026-09-12" section above. In "## Baseline provenance", turn the markdown link to
  `models/sigma1-tuning-results.md` into plain code-span text marked `(deleted in 260913-nvn; in git history)`. Leave
  every dated "Delete pass" record and the `json budget` fenced block untouched, because payloadBudget.test.ts parses
  it.
- `docs/worker-operations.md`: in "Check the entrypoint before deploying", delete the bundleSmoke sentences and keep
  the `main = "src/scheduled.ts"` check.
- `docs/models/sigma1-rp-verification.md`: replace the "Reproduce with:" fenced `pnpm rp:conservative-branch` block
  with one sentence. It says the generator was deleted in 260913-nvn and can be restored from git history at BASE.
  Leave the provenance sentences.

Step 4, COMMENT SWEEP. Run the gate command from `<verify>` and fix every hit under the Comment rule in `<context>`.
Known hits to expect:
- breakdown 2016.ts and 2017.ts: the "Its single consumer" sentences about diagnosticKeys. Say the field has no
  consumer today; do NOT remove the field.
- publish.ts `parseSeasonsRange` doc: delete the sentence about the sibling module-private parsers and the
  parenthetical naming the retired-objects script. Leave the cli.ts sentences for Task 3.
- scripts/recon-shared.ts `parseReconArgs` doc: drop the enumeration.
- scripts/recon-breakdown-fields.ts header: its paragraph about recon-tba-fields.
- scripts/publishAlgorithmsManifest.test.ts header.
- payloadBudget.test.ts, stateSnapshot.ts and scripts/replayRig.ts: the fingerprint mentions.
- publish.test.ts: the algorithmIdentity mention in the rp-calibration baseline describe.
- .gitignore: the comment above `apps/web/.tanstack/`. Keep the ignore entries.
- apps/worker/src/stateProbe.ts and apps/worker/wrangler.toml: the header comment. Keep `main` and every binding.
- scripts/measureRpCalibration.ts plus its test, scripts/measureMatchBandCoverage.ts and scripts/scriptHelpers.ts:
  the "same idiom as" mentions.
- packages/core/algorithms/epa.test.ts, epaCarryScale.ts and epaCarryScale.test.ts: provenance, so use the
  same-line `(deleted in 260913-nvn)` form.
- packages/core/rankingPoints/constants.ts: the "Re-running `pnpm rp:conservative-branch` is what would produce a
  current one" sentence becomes "the generator was deleted in 260913-nvn; restore it from git history to re-measure".
  Mark the attribution lines. packages/core/rankingPoints/2019.ts: the comment only.
- packages/core/algorithms/breakdown/index.ts: two "bare `catch` precedent at `identifiability.ts:239-249`" comments.
  State the catch rationale without the pointer.
- packages/core/algorithms/simulation/rankSimulation.ts: the `mulberry32` header naming its "existing two copies".
  After this task it is the source that rpPredictThresholdsGolden.ts imports, so say that.
- packages/harness/eventBootstrap.ts and eventBootstrap.test.ts: the mulberry32 lineage comments. Point at
  rankSimulation.ts's copy instead.
- scripts/pruneR2Generations.ts: its header's "WHY THIS EXISTS" and standalone-shape mentions of the retired-objects
  script. Keep the reason: per-id enumeration missed generations, listing does not.
- scripts/rpPredictThresholdsGolden.ts: the header's shape comparison.
- packages/core/algorithms/opr.test.ts: the export-pin `it` title "a symbol epa.ts/identifiability.ts depend on"
  becomes "a symbol epa.ts depends on". Leave the pinned list itself unchanged.

Step 5, TODOS. `mv` `.planning/todos/pending/algorithm-identity-sweep-reads-all-of-data.md` into
`.planning/todos/completed/`. Append "## CLOSED 2026-09-13 — the test is deleted (quick task 260913-nvn)",
explaining that the rename guard it ran had finished on 2026-08-29 and the owner approved deleting it.
`mv` `.planning/todos/pending/narrow-offseason-population-ablation-entry.md` into `completed/`. Append
"## CLOSED 2026-09-13 — the generator is deleted (quick task 260913-nvn)", explaining that
`scripts/measureEpaDeviations.ts` was removed with the closed EPA-gap work and that
`data/diagnostics/epa-deviation-ablation.json` is now a frozen record whose `offseason-population` status predates
the 260911-r7e measurement. `git add` both new paths.

Step 6, VERIFY as in `<verify>`, then COMMIT with the subject
`chore(260913-nvn): delete dead research packages, one-shot scripts, harness diagnostics and closed EPA measure scripts`.
The body lists the deleted groups and the dropped audit items, and ends with the Co-Authored-By line. Pass explicit
paths only, per `<context>`.
  </action>
  <verify>
    <automated>git grep -n -E "packages/gbr|packages/pcm|pcm/model|spr/(tune|ablate|score|slice|innovations|carryover|equivalence)\.ts|deleteRetiredAlgorithmObjects|deleteOrphanedDemoTeamObjects|cleanup:(retired-objects|orphaned-demo-teams)|mockRankDistribution|mock:rank-distribution|verifyAllianceUncertaintyIdentity|recon-tba-fields\.ts|recon:tba|recon-rocket-rp-2019|recon:rocket-rp|statboticsComponentMaps|verify-native-module|verify:native|eventScopeDiagnostic|event-scope-diagnostic|identifiability\.ts|pnpm identifiability|baselineFingerprint|pnpm fingerprint|rpConservativeBranch|rp:conservative-branch|bundleSmoke|algorithmIdentity|readAndDeserializeScopedState|pmfStandardDeviation|maxRookieBonus|measureEpaDeviations|measure:epa-deviations|measureAllianceReconstruction|measure:alliance-reconstruction|opr-event-scope-2026-08|sigma1-(identifiability|reparameterization|sensitivity-screen|tuning-results)|rank-distribution-mock|alliance-uncertainty-identity|gbr:(eval|holdout|tune)|pcm:(eval|holdout)" -- packages scripts apps/worker package.json .gitignore vitest.config.ts ':!packages/spr/cli.ts' ':!packages/spr/model.ts' ':!packages/spr/evaluate.ts' ':!packages/spr/data.ts' ':!packages/core/algorithms/spr.ts' | grep -v "260913-nvn" | grep -v -E '^packages/core/rankingPoints/20[0-9]{2}\.ts:[0-9]+:\s*"'   (must print nothing)</automated>
    <automated>npx tsc --noEmit > SCRATCH/tsc-root-t1.txt 2>&1; npx tsc --noEmit -p apps/worker > SCRATCH/tsc-worker-t1.txt 2>&1; then diff each against its baseline file (no new error lines)</automated>
    <automated>npx vitest run --project node > SCRATCH/vitest-t1.txt 2>&1 (Bash timeout 600000 or background); compare "Test Files"/"Tests" lines and FAIL lines to SCRATCH/vitest-baseline.txt: Test Files total = baseline - 14, and no FAIL that the baseline did not have</automated>
    <automated>git show --stat HEAD; git status --short   (every intended path committed; no foreign path in the commit)</automated>
  </verify>
  <done>
All listed paths are deleted and the 17 package.json scripts are gone. The gate grep prints nothing. The root and
worker tsc runs add no errors over baseline. The node vitest run has 14 fewer test files and no new failures. Both
todos sit in `.planning/todos/completed/` with CLOSED sections. One commit contains only this task's paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete publish.ts --event mode, its parity tests and its todo</name>
  <files>packages/harness/publish.ts, packages/harness/publish.test.ts, packages/harness/sigmaScoutLayer.ts, packages/harness/sigmaScoutLayer.sigmaFor.test.ts, packages/harness/sigmaScore.ts, packages/harness/percentiles.ts, packages/harness/scheduleTemplates.ts, packages/harness/fixtures/schedule-templates/README.md, docs/models/rp-layer-config-arms.md, .planning/todos/completed/event-mode-replays-cold-and-writes-different-numbers.md</files>
  <read_first>
    - .planning/todos/pending/event-mode-replays-cold-and-writes-different-numbers.md (option 3 "Delete `--event`" is what this task does)
    - packages/harness/publish.ts: file header (lines 1-41), the CLI section from `deriveSeasonFromEventKey` through `main()`
    - packages/harness/publish.test.ts: describes named in the action (find them by title with grep; line numbers shift)
    - packages/harness/sigmaScoutLayer.ts header, section "WHY THIS IS A MODULE AND NOT A LOOP BODY"
  </read_first>
  <action>
This task closes the pending todo `event-mode-replays-cold-and-writes-different-numbers` by its own option 3, per
AUDIT section A row "publish.ts --event mode". It has no caller, and it wrote cold-start numbers on 86 of 89 rows.

`packages/harness/publish.ts`:
- Delete `deriveSeasonFromEventKey` and its one-line doc, the `SingleEventPublishResult` interface, and
  `buildSingleEventPublish` with its doc comment and the long leading comment inside it. Delete `runEventMode`.
- In `main()`: remove the `event` parseArgs option and the `values.event` branch. With no seasons value it throws
  `--seasons is required`. Node's strict parseArgs will then reject a stray `--event` flag, which is what we want.
- In the file header: replace the two usage lines and the paragraph starting "`--event` is the single-event
  republish path" with the single `--seasons` usage line. State that a single event is refreshed by a full
  `pnpm publish:seasons`. Remove the words "Two modes".
- Reword three comments so no clause names the deleted mode:
  - the comment above the `SigmaScoutLayer` import ("BOTH orchestrations in this file drive it")
  - the "same band `--event` now publishes too" clause near `matchBandFields`
  - the "because `--event` below runs the identical thing" clause in `publishSeasons`
- Then check every name that `buildSingleEventPublish` alone might have used. The planning-time check found every
  helper still used by `publishSeasons`. Re-confirm with grep counts: for each top-of-file import name, if the import
  line is its only remaining occurrence, drop that name from the import.

`packages/harness/publish.test.ts`:
- Remove `buildSingleEventPublish` from the `./publish.js` import.
- Delete the describe "--event replays the season exactly once — structural (plan 07-09 Task 2)" and the doc comment
  above it.
- In describe "publishSeasons and --event agree on the SigmaScout layer (2026-09-09)":
  - Delete the doc comment above it.
  - Delete these `it` blocks: "emits a band at all on the late event — the precondition…", "--event publishes the
    SAME band…", "--event publishes the same ranking-point fields…", "--event publishes the SAME standings
    percentiles…" and "agrees for OPR too — neither path publishes…".
  - KEEP "OPR publishes no band and no ranking-point field on any row of any event, team or teams artifact…" and
    "SPR still publishes ranking-point pmfs on the same fixture…".
  - Retitle the describe "publishSeasons — OPR publishes no band or ranking-point odds (quick tasks 260913-g66 and
    260913-it4)".
- In describe "publishSeasons — event standings carry the season-final Sigma entry (quick task 260913-jkp Task 1)",
  delete only the `it` "the --event path publishes the same teams[].metrics.sigma entries as the seasons path for
  the same event".
- In describe "publishSeasons and --event agree on the presim sidecar (2026-09-09)":
  - Delete the doc comment above it.
  - Delete the `it` "both paths write a sidecar carrying ranking points for the same event".
  - KEEP "OPR and EPA build no pre-schedule sidecar on the fixture where SPR builds one (quick task 260913-it4)".
  - Retitle the describe "publishSeasons — the pre-schedule sidecar is SPR-only (quick task 260913-it4)".
- Then check the test-local helpers and imports: `seedTwoEventSeason`, `findEventArtifact`, `upsertEvent`,
  `upsertMatch`, `seasonEvent`, `seasonMatch`, `OFFSEASON_EVENT_TYPE` and `EventArtifact`. Drop an import name only
  if its import line is now its only occurrence.

Comments elsewhere that describe two write paths:
- `sigmaScoutLayer.ts`: cut the "WHY THIS IS A MODULE AND NOT A LOOP BODY" section to at most six lines. Level-2
  per-match math lives only here and `publishSeasons` calls it. Adding a level-2 field in a caller's loop instead
  recreates the drop-a-field defect a second write path caused on 2026-09-09.
- `sigmaScoutLayer.sigmaFor.test.ts` header and `sigmaScore.ts` (the insert-on-read accessor comment): keep the
  order-dependence reason. Drop the `--event` naming and the pointer to the deleted sidecar-parity test.
- `percentiles.ts`: end the sentence at "every event artifact standing".
- `scheduleTemplates.ts` and `fixtures/schedule-templates/README.md`: drop the "`publishSeasons`/`--event` parity"
  gate from the list of gates. Write "those tests" instead of "those 16 tests", because the count changed.
- `docs/models/rp-layer-config-arms.md`: "neither `publishSeasons` nor `runEventMode` (…) nor any production
  `SigmaScoutLayer` construction site passes an override" becomes "neither `publishSeasons` (…) nor any production
  `SigmaScoutLayer` construction site passes an override".

Todo: `mv` the pending todo into `.planning/todos/completed/`. Append "## CLOSED 2026-09-13 — resolved by option 3,
the mode is deleted (quick task 260913-nvn)". Say that a single-event refresh is now a full `pnpm publish:seasons`,
which carries every season boundary, and that no cold-start path to R2 remains in publish.ts. `git add` the new path.

Verify per `<verify>`. Then commit with the subject
`refactor(260913-nvn): delete publish.ts --event mode and its parity tests`. The body names the closed todo and the
kept OPR/SPR/sidecar gates, and ends with the Co-Authored-By line. Pass explicit paths.
  </action>
  <verify>
    <automated>git grep -n -E -e "--event\b|runEventMode|buildSingleEventPublish|SingleEventPublishResult|deriveSeasonFromEventKey|single-event republish" -- packages/harness docs/models/rp-layer-config-arms.md ':!packages/harness/cli.ts'   (must print nothing; cli.ts is deleted in Task 3)</automated>
    <automated>npx tsc --noEmit > SCRATCH/tsc-root-t2.txt 2>&1 and diff against SCRATCH/tsc-root-baseline.txt (no new errors)</automated>
    <automated>npx vitest run --project node packages/harness/publish.test.ts packages/harness/publish.tracer.test.ts packages/harness/sigmaScoutLayer packages/harness/percentiles.test.ts packages/harness/scheduleTemplates.test.ts packages/harness/level1Digest.test.ts > SCRATCH/vitest-t2.txt 2>&1; read the summary: no FAIL beyond the baseline's for these files, and the kept tests "OPR publishes no band and no ranking-point field", "SPR still publishes ranking-point pmfs" and "OPR and EPA build no pre-schedule sidecar" appear as passed (use --reporter=verbose if needed)</automated>
    <automated>git show --stat HEAD; git status --short</automated>
  </verify>
  <done>
publish.ts has one mode, `--seasons`. The five parity `it` blocks and the structural source-range test are gone,
and the three non-parity gates still pass. The gate grep prints nothing. The todo is in `completed/` with a CLOSED
section. One commit contains only this task's paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete the pnpm harness backtest CLI cluster; publish.ts's registry becomes the only one; final full verification</name>
  <files>packages/harness/publish.ts, packages/core/algorithms/opr.test.ts, packages/harness/selectionProvenance.test.ts, scripts/measureGeneratedSchedules.ts, scripts/measureFieldAveragedRanks.ts, scripts/measureRandomSchedules.ts, packages/harness/rounding.test.ts, packages/core/algorithms/types.ts, package.json, docs/worker-operations.md, plus the deleted CLI files and comment-only edits listed in frontmatter</files>
  <read_first>
    - packages/harness/cli.ts lines 40-100 (imports and the `ALGORITHMS` registry) only
    - packages/harness/publish.ts `BASE_PUBLISH_ALGORITHMS` declaration and its doc comment
    - packages/harness/rounding.test.ts (the `it` using `PredictionRecordSchema`)
    - packages/core/algorithms/types.ts (`ComponentPrediction`, `breakdownParseFailureCountOf`)
  </read_first>
  <action>
This implements AUDIT section B row 2, re-verified. The registry move comes first.

1. Registry move:
   - `packages/harness/publish.ts`: add `export` to `const BASE_PUBLISH_ALGORITHMS` and nothing else. Its keys are
     already opr, epa, spr in that order.
   - `packages/core/algorithms/opr.test.ts`: import `BASE_PUBLISH_ALGORITHMS` from `../../harness/publish.js`, and
     retitle the identity test to name `BASE_PUBLISH_ALGORITHMS.opr (packages/harness/publish.ts)`.
   - `packages/harness/selectionProvenance.test.ts`: import it from `./publish.js` and use
     `Object.keys(BASE_PUBLISH_ALGORITHMS)`.
   - `scripts/measureGeneratedSchedules.ts`, `scripts/measureFieldAveragedRanks.ts` and
     `scripts/measureRandomSchedules.ts`: in each, add it to the existing `../packages/harness/publish.js` import
     (the one that already brings in `makeRankingPointFiller`), delete the `cli.js` import, and replace the single
     `ALGORITHMS[algorithmId]` lookup with `BASE_PUBLISH_ALGORITHMS[algorithmId]`.
   - Run `npx tsc --noEmit` once before deleting anything, to prove the repoint alone compiles.

2. Delete with rm:
   - `packages/harness/cli.ts`, `cli.season-carry.test.ts`, `cli.seasonsSpec.test.ts`
   - `report.ts`, `report.test.ts`
   - `artifact.ts`, `artifact.test.ts`
   - `predictions.ts`, `predictions.test.ts`
   - `metricHistory.ts`, `metricHistory.test.ts`

   That is 6 deleted test files. KEEP `statbotics.ts` and `statbotics.test.ts`, because `scripts/epaVsStatbotics.ts`
   imports them. KEEP `metricHistorySchema.ts`, because apps/web and pageArtifacts.ts import it. The
   cli.season-carry test's cross-season leak guard targeted cli.ts's own `runSeasons`. Predict-before-update and
   replay determinism stay covered by `replay.season.test.ts`, and publishSeasons carry by publish.test.ts's "EPA
   carries from the last official match" suite. Say so in the return.

3. Keep-compiling edits:
   - `publish.ts`: `import type { MetricHistoryRow }` now comes from `./metricHistorySchema.js`.
   - `package.json`: remove the `harness` script.
   - `rounding.test.ts`: delete the `it` "a rounded pmf still satisfies PredictionRecordSchema's own 1e-9 sum
     tolerance" and its import. The `it` above it already asserts the 1e-9 sum directly.
   - `packages/core/algorithms/types.ts`: delete `breakdownParseFailureCountOf` and its doc comment, whose only
     caller was cli.ts. KEEP the `BreakdownParseTelemetry` interface, which EpaState extends. Remove `variance?` and
     its doc line from `ComponentPrediction`; its only reader was predictions.test.ts. Confirm with grep that no
     code sets it.

4. Comment and doc sweep. Run the gate from `<verify>` and fix every hit under the Comment rule. Known hits to
   expect:
   - `publish.ts`:
     - the header clause "mirroring `cli.ts`'s `runSeasons` … `tune.ts`'s file header"
     - the `splitAlgorithmVersion` mention
     - the `HarnessArtifactSchema` / `artifact.ts` mention near brierScore rounding
     - the `publishSeasons` doc "Mirrors `cli.ts`'s `runSeasons` orchestration … `tune.ts`": keep only that
       `buildSeasonStream`/`WalkForwardSimulator` are the shared leak-proof primitives
     - the compare-slice comment naming `runSeasonsMode`
     - the `parseSeasonsRange` doc sentences saying cli.ts mirrors the grammar
   - `publish.ts` `resolvePublishAlgorithms` one-line doc: the "`cli.ts`'s harness runs" clause.
   - `publish.test.ts`: the "mirrors artifact.ts's" comment.
   - `selectionProvenance.test.ts`: the doc naming "the single derivation `cli.ts` and `publish.ts` used to
     independently" build.
   - `manifestSchemas.ts`: the schema-version comment naming `artifact.ts`'s `ARTIFACT_SCHEMA_VERSION`. Keep the
     independence from `PAGE_ARTIFACT_SCHEMA_VERSION`.
   - `pageArtifacts.ts`: the mentions of artifact.ts and predictions.ts. State the rule itself, for example "a pmf
     sums to 1 within 1e-9", without a pointer.
   - `rounding.ts`, `score.ts`, `seasonBoundary.ts`, `selectionProvenance.ts` (registry and call-site mentions),
     `opr.ts` (the `runSeasons` mention).
   - `epa.ts`: the two `reportBreakdownParseFailures` mentions and the `buildArtifact` / `pnpm harness` version-shape
     comment. Keep the rule that version carries `{codeVersion}+{paramSetName}`, which publish and manifests still
     split.
   - `breakdown/constants.ts`: "discovered running `pnpm harness`" becomes a real tsx run.
   - `metricHistorySchema.ts` header: it is the node-free home of the metric-history row schema.
   - `apps/worker/src/artifactWriter.ts`: the two artifact.ts mentions.
   - `docs/worker-operations.md`: drop "(or `pnpm harness`)".

   Leave dated measurement records in docs/ alone, such as epa-vs-statbotics.md, offseason-inclusion-remeasurement.md
   and opr-baseline-change.md.

5. Final verification per `<verify>`. This is the full run. Then commit with the subject
   `refactor(260913-nvn): delete the pnpm harness backtest CLI; publish.ts owns the algorithm registry`. The body
   notes that statbotics.ts is kept and the registry was repointed, and ends with the Co-Authored-By line. Pass
   explicit paths.

6. Return the summary text described in `<context>` "Return, not SUMMARY.md".
  </action>
  <verify>
    <automated>git grep -n -E "harness/cli\.ts|\`cli\.ts\`|cli\.ts's|cli\.ts:[0-9]|runSeasons\b|runSeasonsMode|reportBreakdownParseFailures|breakdownParseFailureCountOf|harness/(report|artifact|predictions|metricHistory)\.ts|\`(report|artifact|predictions|metricHistory)\.ts\`|artifact\.ts's|PredictionRecordSchema|HarnessArtifactSchema|\bARTIFACT_SCHEMA_VERSION|buildArtifact\b|pnpm harness|\"harness\":|harness/cli\.js|\{ ALGORITHMS \}|\bALGORITHMS(\[|\.|\))|\`ALGORITHMS\`" -- packages scripts apps/worker package.json docs/worker-operations.md ':!packages/ingest' ':!packages/spr' | grep -v "260913-nvn"   (must print nothing; this pattern was dry-run against HEAD at planning time and hits only the sites this task lists, with no false positives from "SIGMA ALGORITHMS ONLY" comments)</automated>
    <automated>npx tsc --noEmit > SCRATCH/tsc-root-final.txt 2>&1; npx tsc --noEmit -p apps/worker > SCRATCH/tsc-worker-final.txt 2>&1; diff each against its baseline (no new error lines)</automated>
    <automated>npx vitest run --project node > SCRATCH/vitest-final.txt 2>&1 (Bash timeout 600000 or background); Test Files total = baseline total - 20; every FAIL line also appears in SCRATCH/vitest-baseline.txt; level1Digest.test.ts passes</automated>
    <automated>git diff --shortstat BASE..HEAD; git log --oneline BASE..HEAD; git show --stat HEAD; git status --short</automated>
  </verify>
  <done>
The CLI cluster and its 6 test files are gone. `BASE_PUBLISH_ALGORITHMS` is exported and read by all five former
`ALGORITHMS` importers. statbotics.ts and metricHistorySchema.ts survive. The gate grep prints nothing. Final tsc
(root and worker) shows no new errors. The final node vitest run has 20 fewer test files than baseline and no new
failures. Three commits are on main, unpushed. The return text carries the commits, shortstat, test comparison,
skipped items and web-side leftovers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor shell → .env | Live TBA and Cloudflare/R2 credentials sit in the checkout; nothing in this plan needs them |
| shared checkout → git history | A concurrent plan-02 executor and other sessions keep uncommitted edits in the same tree and index |
| offline pipeline → published artifacts | Any change to publishSeasons behavior reaches visitors on the next republish |
| sealed holdout files → published BPR/SPR holdout figure | Editing a sealed path invalidates the seal that attributes a published number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nvn-01 | Information disclosure | every shell command | high | mitigate | No command uses `--env-file` or reads `.env`; the CLAUDE.md secrets rule is restated in context; no network, publish or deploy |
| T-nvn-02 | Tampering | git commits on the shared checkout | high | mitigate | rm instead of git rm; explicit-path `git commit -- <paths>` only; `git status --short` before and `git show --stat HEAD` after every commit; apps/web and STATE.md are off-limits |
| T-nvn-03 | Tampering (published numbers) | publish.ts, core algorithm modules | medium | mitigate | publishSeasons logic is untouched (export keyword, import path and comments only); level1Digest.test.ts plus the surviving publish.test.ts suites must pass; baseline-vs-final vitest comparison |
| T-nvn-04 | Repudiation | sealed SPR holdout provenance | high | mitigate | The five sealed paths and `SEALED_CODE_PATHS` are excluded from every edit and gate; only non-sealed spr files are deleted |
| T-nvn-05 | Repudiation | deleted measurement generators behind kept numbers | low | accept | Committed outputs (`epa-deviation-ablation.json`, docs/data recon records, sigma1-rp-verification.md) are kept; provenance comments carry `(deleted in 260913-nvn)`; BASE sha in the return is the restore point |
</threat_model>

<verification>
- Gate greps for all three tasks print nothing.
- Root `npx tsc --noEmit` and `npx tsc --noEmit -p apps/worker` add no errors over the pre-Task-1 baseline.
- Final `npx vitest run --project node`: Test Files = baseline - 20. No failure absent from the baseline.
  level1Digest.test.ts passes.
- `git log --oneline BASE..HEAD` shows the three `260913-nvn` commits. `git status --short` shows no leftover change
  from this plan.
- The rejected items still exist: `scripts/measureRandomSchedules.ts`, `scripts/measureFieldAveragedRanks.ts`
  (with its test), `buildFieldAveragedPreScheduleArtifact` in preSchedule.ts,
  `FieldAveragedPreScheduleArtifactSchema`, and `packages/harness/statbotics.ts`.
- Nothing is published, deployed or pushed. apps/web and .planning/STATE.md are untouched by these commits.
</verification>

<success_criteria>
- About 23,500 lines and about 70 files are removed outside apps/web, measured by `git diff --shortstat BASE..HEAD`.
  The estimate is about 19,800 for Task 1, about 550 for Task 2 and about 3,100 for Task 3.
- No live code points at a deleted file, script or symbol.
- publish.ts has one mode, and the repo has one algorithm registry outside manifests.ts.
- Three todos are closed in `.planning/todos/completed/` with CLOSED notes.
- The audit's wrong claims are reported back with evidence, not acted on.
</success_criteria>

<output>
Do NOT create a SUMMARY.md; subagent Write is blocked for it on this machine. The executor returns the summary text in
its final message. The orchestrator writes
`.planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-01-SUMMARY.md` and updates STATE.md by a hand-verified
script, not with quick-tasks-append.
</output>
