---
phase: 260913-nvn-simplify-codebase-audit
plan: 03
type: execute
wave: 2
depends_on: ["260913-nvn-01"]
files_modified:
  - packages/harness/publish.ts
  - packages/harness/publish.test.ts
  - packages/harness/teamRanks.ts
  - packages/harness/teamRanks.test.ts
  - packages/harness/uploadQueue.ts
  - packages/harness/uploadQueue.test.ts
  - packages/harness/publishBudget.ts
  - packages/harness/publishBudget.test.ts
  - packages/harness/payloadBudget.test.ts
  - docs/publish-budget.md
  - package.json
  - packages/harness/stateSnapshot.ts
  - packages/harness/stateSnapshot.test.ts
  - packages/harness/manifests.ts
  - packages/harness/manifests.test.ts
  - packages/harness/seasonBoundary.ts
  - packages/harness/seasonBoundary.test.ts
  - packages/harness/sigmaScore.ts
  - packages/harness/sigmaScore.test.ts
  - packages/harness/score.ts
  - packages/harness/score.test.ts
  - packages/harness/selectionProvenance.ts
  - packages/harness/selectionProvenance.test.ts
  - packages/harness/seasonSpec.ts
  - packages/harness/sigmaScoutLayer.ts
  - packages/harness/consistencyMetric.ts
  - packages/harness/consistencyMetric.test.ts
  - packages/harness/sigmaMetric.ts
  - packages/harness/sigmaMetric.test.ts
  - packages/harness/preSchedule.ts
  - packages/harness/metricDirection.ts
  - scripts/replayRig.ts
  - scripts/epaVsStatbotics.ts
  - scripts/publishDistricts.ts
  - scripts/verifySubsetPublish.ts
  - scripts/measureGeneratedSchedules.ts
autonomous: true
requirements: [QUICK-260913-nvn]

estimate:
  tokens: 220000
  raw_tokens: 440000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every published object body for seasons 2025-2026 x opr/epa/spr (teams, team, events, event, compare) hashes identically (sha256 per key, fixed generation and computedAt) before Task 1 and after each of Tasks 1, 2 and 3; objectCount, totalBytes and per-kind size stats are identical; the algorithms manifest body is identical"
    - "OPR and EPA never enter the pre-schedule sidecar builder; SPR sidecars are still uploaded before their own event artifact"
    - "Every team's World/Country/District/State rank scopes equal the pre-change per-team implementation on seeded random rosters with tied totals, missing totals, missing regions and non-real team keys, and publish sorts each scope pool once per (season, algorithm)"
    - "The events/{year}/{algo} rows are built and validated once per season and reused for the three existing keys; keys and bytes are unchanged"
    - "A publish run's summary prints wall-clock timings: per season (replay, fold, compare) and per (season, algorithm) (build, sidecars, uploadWait), plus a run total"
    - "Uploads drain through a bounded queue (default concurrency 48) while the next (season, algorithm) block builds; queued-plus-in-flight bodies never exceed the queue cap; a put that fails after r2Client's own retries fails the run"
    - "An artifact larger than its page kind's ceiling throws before that artifact is uploaded, in --dry-run and real runs alike; a breach on the run's first artifact produces zero puts"
    - "pnpm publish:seasons (via --write-budget) rewrites only the json budget block of docs/publish-budget.md from its own measurements; the ceilings live in one constant and the committed doc must equal it"
    - "docs/publish-budget.md keeps only the machine block and still-true runbook sections; every section a test or apps/* file cites by name survives"
    - "The approved harness/core leftovers are renamed, deduplicated or removed without changing any published byte, and the node suite, root tsc, apps/worker tsc and apps/web tsc are green"
  artifacts:
    - path: "packages/harness/uploadQueue.ts"
      provides: "UploadQueue: bounded-concurrency, backpressured, first-error-wins upload queue"
      contains: "export class UploadQueue"
    - path: "packages/harness/publishBudget.ts"
      provides: "PAGE_BUDGET_MAX_BYTES, computeSizeStats, parse/render/replace of the json budget block, the per-object ceiling assertion"
      contains: "export const PAGE_BUDGET_MAX_BYTES"
    - path: "packages/harness/teamRanks.ts"
      provides: "buildTeamRankScopesByTeam: sort-once rank scopes for every row"
      contains: "export function buildTeamRankScopesByTeam"
    - path: "packages/harness/teamRanks.test.ts"
      provides: "Seeded randomized equivalence test against a test-local copy of the pre-change per-team implementation"
    - path: "docs/publish-budget.md"
      provides: "Machine-written json budget block plus still-true runbook sections only"
      contains: "```json budget"
  key_links:
    - from: "packages/harness/publish.ts publishSeasons"
      to: "packages/harness/publishBudget.ts ceiling assertion"
      via: "called on every page-kind object before it is recorded or enqueued, dry-run included"
      pattern: "PAGE_BUDGET_MAX_BYTES"
    - from: "packages/harness/publish.ts uploader"
      to: "packages/harness/uploadQueue.ts UploadQueue"
      via: "each put (and each sidecar-then-event pair, as ONE task) is awaited through enqueue; drain before the summary"
      pattern: "UploadQueue"
    - from: "packages/harness/publish.ts CLI --write-budget"
      to: "docs/publish-budget.md json budget block"
      via: "replace-in-place of the fenced block after a successful run"
      pattern: "write-budget"
    - from: "packages/harness/payloadBudget.test.ts"
      to: "packages/harness/publishBudget.ts"
      via: "parser import plus doc-ceilings-equal-constant assertion"
      pattern: "PAGE_BUDGET_MAX_BYTES"
---

<objective>
Cut republish wall-clock time and remove harness/core leftovers from the 260913-nvn audit (AUDIT.md sections C and D) WITHOUT changing any published artifact byte other than the per-run `generation`/`computedAt` stamps.

Purpose: the last full republish took ~1h55m with no per-phase timing, a manual budget-transcription step, builds and uploads that never overlap, and dead work (OPR/EPA sidecar probes, O(N^2) rank scopes). Removing these makes the next republish faster and self-documenting, and removes naming/knob debris that misdescribes the live system.

Output: three atomic commits (Task 1 perf, Task 2 perf, Task 3 refactor), two new harness modules (`uploadQueue.ts`, `publishBudget.ts`) with tests, a shrunk `docs/publish-budget.md`, and byte-identity evidence from an uncommitted scratch digest harness run before and after every task.

Scope guards: NOT in scope are apps/worker, apps/web, D1 seeding changes, `--emit-from-season`, schema field removals, sidecar caching, and the walk-forward leak investigation (AUDIT F5 — note it in the returned summary, do not investigate). Sealed files `packages/spr/{model,evaluate,data,cli}.ts` and `packages/core/algorithms/spr.ts` are never edited.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-AUDIT.md

Source (read in targeted ranges; publish.ts is ~3.5k lines after plan 01 — use Grep to locate symbols, then Read with offset/limit):
@packages/harness/publish.ts
@packages/harness/teamRanks.ts
@packages/harness/payloadBudget.test.ts
@packages/harness/r2Client.ts

Facts established during planning (verified against HEAD d3b304b0; re-confirm by grep, since plan 01 edits publish.ts first):

- `publishSeasons` replays each season ONCE for all algorithms (`simulator.runAll`), folds records through one `SigmaScoutLayer` per algorithm, then loops `for (const algorithm of options.algorithms)` building teams -> events -> event (plus sidecar) -> team artifacts, collecting promises and awaiting `Promise.all` at the end of the block. Compare is built and awaited once per season after the loop; manifests and seeds are written after all seasons unless `skipState`.
- The module-private `BoundedUploader` class records `{pageKind,key,bytes}` into `records` (sidecars into `sidecarRecords`) REGARDLESS of dry-run and only calls `putObject` when not dry-run. Its semaphore starts puts immediately, but the build loop is synchronous, so no network callback runs until the block's `Promise.all` — build and upload never overlap, and every body in the block is held in memory until then.
- Sidecar call site: `presimEnabled ? buildPreScheduleSidecarForEvent({...}) : undefined`; the event put is chained behind the sidecar put. For OPR/EPA at HEAD the call always returns undefined, but only after loading a schedule template, building schedule 0 and probing one prediction: `SigmaScoutLayer`'s constructor creates the RP accumulator only when `publishesRankingPoints(algorithmId)`, so `makeRankingPointFiller` returns undefined, `opr.ts`/`epa.ts` emit no `redRpPmf` (grep count 0), and `buildPreScheduleArtifact` returns null at its first-match probe. `publishesRankingPoints(id)` is `usesSigmaScore(id)` (true for spr only).
- `publish.test.ts`'s presim describe ("pre-event walk-forward state, scheduleless events, and the presim sidecar") uses a fake RP-modeling algorithm registered under the id "epa" (version "9.9.9+presim-test"). Gating on the algorithm id will stop those tests getting a sidecar, so the fake must be re-registered under the spr id.
- `buildEventsArtifact` stamps `algorithmId` and `algorithmVersion` into the body, so the three `events/{year}/{algo}` bodies are NOT byte-identical to each other. Only the rows (built from `eventMeta` + `eventCounts`, both algorithm-independent) and the validated `events` array can be shared.
- `teamRanks.ts` is browser-safe and imported by apps/web (`compareTeamsByTotal`, `percentileForRank`, `USA_COUNTRY_VALUE`, `isRealPublishedTeamKey`). `buildTeamRankScopes` has no importer outside publish.ts and teamRanks.test.ts. Array.prototype.sort is stable, so sorting a pool once and indexing it yields exactly the rank `rankWithin`'s copy-sort-findIndex yields, provided each scope pool keeps `realRows` iteration order.
- `payloadBudget.test.ts` parses ONLY the fenced block matched by the pattern for a code fence tagged `json budget` in `docs/publish-budget.md` (fields `measuredAt`, `run`, `pages.{teams,team,events,event,compare}.{count,medianBytes,p95Bytes,maxBytes,budgetMaxBytes,largestKey}`). `publish.test.ts` (compare rpCalibration test) reads the same block's `pages.compare.budgetMaxBytes`. No other test reads the doc. `apps/worker/test/liveAlgorithmTier.test.ts` and `apps/worker/src/scheduled.ts` cite the doc's "Worker runtime budget (D-21/D-23, plan 04-07)" section by name (apps/* cannot be edited, so that heading must survive verbatim). Current committed ceilings: teams 3,500,000; team 500,000; events 108,000; event 350,000; compare 20,000.
- `docs/publish-budget.md` (1,993 lines) records that R2 DELETEs rate-limited at concurrency 16 during a cleanup pass; PUTs ran fine at 16. r2Client's `putObject` retries transient statuses with exponential backoff (`PUT_MAX_ATTEMPTS` 5) — leave it untouched.
- `serializeState` accepts `EpaState | OprState | SprState`; publish.ts's seed block casts `state as EpaState | OprState` (missing SprState).
- `buildAlgorithmsManifest` destructures `paramsSeason` and never uses it; callers are publish.ts, scripts/replayRig.ts, manifests.test.ts. `manifestSchemas.ts` keeps its optional `paramsSeason` schema field (schema removals are out of scope).
- `coldStartSeason`: after plan 01 deletes cli.ts, only `PublishSeasonsOptions` and `seasonBoundaryFor`'s optional third parameter (exercised by seasonBoundary.test.ts) remain.
- `SigmaScoreOptions.scale` (always 1, a multiplier on the reported figure) and `.talentPrior` (always true) are set only by sigmaScore.test.ts; apps/worker imports `SigmaScoreAccumulator` from sigmaScore.ts, so worker tsc must be run after changing the options type.
- `selectionProvenance.ts`'s `SELECTED_ON_SEASONS_SOURCES` maps opr/epa/spr to `() => []`, so publish's `headlineEligible` depends only on `selectCorpusSeasons(db)` and `MIN_PRIOR_SEASONS_FOR_HEADLINE`. `scripts/epaVsStatbotics.ts` passes the `ELIGIBILITY_NOT_CLAIMED` sentinel (forces every slice false).
- `splitVersion` (publish.ts, module-private, used by `buildCompareArtifact`) duplicates `manifestSchemas.ts`'s exported `splitManifestVersion` except for the error class/message.
- `parseSeasonsRange` (publish.ts) and `scripts/publishDistricts.ts`'s `parseYearsSpec` implement the same grammar; publishDistricts' header says it avoided importing the large publish.ts. The lenient `parseSeasons` copies in scriptHelpers.ts, measureRpCalibration.ts (feeds a sealed-slice guard) and measureRpMeanDeficit.ts have DIFFERENT semantics — leave them.
- `scripts/verifySubsetPublish.ts` composes `PUBLISHED_SUBSET` from `PRE_RENAME_EVENT_SUBSET` (15 sigma1 absence controls + 2 opr/epa arms), `RENAMED_EVENT_SUBSET` (the same 17 mapped sigma1 -> spr with the "present" -> "partial" pmf downgrade), `NEW_2024AUWARP_ENTRY`, and two retired-vpr absence layers; `PUBLISHED_TEAM_SUBSET` carries sigma1/vpr absence entries too. The tables ARE read, but every absence entry asserts a prefix the 2026-09-12 full-listing census already deleted. `pruneR2Generations.ts` imports only `ALGORITHMS_MANIFEST_KEY`, `DEFAULT_ARTIFACT_ORIGIN`, `fetchArtifactFresh` from it.
- `consistencyByTeam()` (SigmaScoutLayer method), `consistencyMetric.ts`'s `consistencyMetricByTeam`/`ConsistencyMetricEntry` have importers only in packages/harness and scripts (none in apps/*).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Byte-identity harness end to end, then stop dead work (sidecar gate, sort-once rank scopes, shared events rows) and add phase timers</name>
  <files>packages/harness/publish.ts, packages/harness/publish.test.ts, packages/harness/teamRanks.ts, packages/harness/teamRanks.test.ts</files>
  <precondition>Plan 01 has landed on main: packages/harness/cli.ts does not exist and grep finds no runEventMode in packages/harness/publish.ts; data/corpus.sqlite exists. If either fails, STOP and report — do not plan around a half-applied plan 01.</precondition>
  <behavior>
    - Rank-scope equivalence: for 5 seeds, a ~600-row roster (totals drawn from a small integer set so ties are common, ~10% rows with no total, countries among USA/Canada/Israel/absent, stateProv among CA/TX/ON/absent including a non-USA row with a stateProv, districtKey among fim/ne/ont/absent, a handful of non-real team keys, rows in shuffled order), the new all-teams function's entry for EVERY row key deep-equals the test-local copy of the pre-change per-team implementation, and a key absent from the rows maps to nothing (while the per-team reference returns an empty array for it).
    - The new function returns an entry (possibly an empty array) for every row key, including non-real keys and rows with no total.
    - Sidecar gate: publishSeasons over the existing two-event fixture with the real opr, epa and spr modules writes a presim key for spr and none for opr or epa; the fake RP-modeling algorithm, now registered under the spr id, still gets its sidecar priced from the pre-event state, and the sidecar put still precedes its event put.
    - A fake RP-modeling algorithm registered under a non-RP id (opr or epa) now gets NO sidecar, documenting that the gate is by id, not by probe.
  </behavior>
  <action>
Step 0 — baseline before touching code. Record `BASE=$(git rev-parse HEAD)` and use the executor's own session scratchpad directory as SCRATCH (written `<scratch>` below; never a repo path) for every capture, comparison script and one-off in this plan. Run `npx vitest run --project node` from the repo root and record the pass/fail/skip file and test counts from the output text (never the exit code; never wrap in `timeout`). Run `npx tsc --noEmit` at root and record the result.

Step 1 — create the UNCOMMITTED scratch digest harness at `packages/harness/zz-byte-identity.scratch.test.ts` (inside the node project include glob; never staged, deleted at the end of Task 3). Its contract, in prose:
(a) the whole describe runs only when env var BYTE_IDENTITY_OUT is set (describe.runIf), so other sessions' suite runs skip it;
(b) it mocks `./r2Client.js` via importOriginal, spreading the real module and replacing `putObject` with a recorder that stores sha256(body) per key in a vi.hoisted Map and counts duplicate puts of the same key;
(c) one test with a 45-minute timeout opens `data/corpus.sqlite` with `openCorpusReadOnly`, and calls `publishSeasons` with seasons [2025, 2026], algorithms from `resolvePublishAlgorithms(undefined)`, bucket "byte-identity", `dryRun` true only when env BYTE_IDENTITY_DRY is "1", `skipState: true` (MANDATORY — without it the run overwrites reports/publish/seed-*.sql, which a pending D1 import may still need), `includeOffseason: true`, `preScheduleFromSeason: 2027` (no sidecars: at 1,000 schedules a single SPR season's sidecars take over an hour), fixed `generation` "byte-identity-gen" and `computedAt` "2026-01-01T00:00:00.000Z", and `rpCalibration` from `loadRpCalibrationMeasurement(RP_CALIBRATION_MEASUREMENT_PATH)`;
(d) it also hashes `JSON.stringify(buildAlgorithmsManifest(...))` with generation "g" and computedAt "c" (pass paramsSeason 2026 through an untyped cast so the same file keeps working after Task 3 removes the parameter);
(e) it writes to BYTE_IDENTITY_OUT a JSON object holding objectCount, totalBytes, pages, sidecarRecords count, duplicate-put count, manifestDigest and the digests as key-sorted entries, and closes the db.
Also write a tiny comparison script in the executor's session scratchpad (not the repo) that loads two such JSON files and prints: changed/added/removed key counts, the first 10 differing keys, and whether objectCount, totalBytes, pages and manifestDigest match; with a third argument "sizes-only" it compares only objectCount/totalBytes/pages (for dry-run captures, which have no digests).
Run the harness TWICE at this pre-change tree (BYTE_IDENTITY_OUT pointing at two different files in the session scratchpad, e.g. `BYTE_IDENTITY_OUT=<scratch>/baseline-a.json npx vitest run --project node packages/harness/zz-byte-identity.scratch.test.ts`, Bash timeout 600000) and compare them: they MUST be identical, which proves the capture is deterministic before any change is judged against it. If one run exceeds ~9 minutes, switch the harness to seasons [2026] only for EVERY capture in this plan (both baselines re-run) and say so in the summary. If the two baselines differ, STOP and report the differing keys — do not proceed on a nondeterministic guard.

Step 2 — sidecar gate (AUDIT D1). In publishSeasons' event loop, only call `buildPreScheduleSidecarForEvent` when `presimEnabled && publishesRankingPoints(algorithm.id)`; OPR and EPA then never load a template, build schedule 0, or probe. Add a one-line comment naming why (the RP accumulator exists only for RP-publishing ids, so every other id's probe returns null by construction). The OPR/EPA "presim skip" log lines disappear; that is a log change, not an artifact change. In publish.test.ts, re-register the presim describe's fake RP-modeling algorithm under `spr.id` (keeping its version string) and update its key assertions from the epa id to the spr id; fix anything the SPR layer now does to that fake (the filler leaves an already-present pmf untouched, so the sidecar pmf bytes should still encode the fake's state). Ensure a seasons-mode test asserts real opr/epa produce no presim key while spr does (plan 01 may have deleted the old parity-describe version of it — re-home it into the presim describe if so), and add the non-RP-id fake test from the behavior list.

Step 3 — sort-once rank scopes (AUDIT D2). In teamRanks.ts add exported `buildTeamRankScopesByTeam(rows)` returning a Map from teamKey to the ordered scope array for EVERY row: filter real rows once; sort the world pool once; group real rows by country, by districtKey (defined only) and, for USA rows, by stateProv — each group preserving real-row iteration order — and sort each group once with `compareTeamsByTotal`; build teamKey -> index maps per sorted pool (first occurrence wins, matching findIndex); then emit, per row, exactly the scopes and gating the old function emitted (no total or non-real key -> empty array; world always; country when defined; district when defined; state only for USA with stateProv). Reimplement `buildTeamRankScopes` as a thin wrapper over the new function (lookup, defaulting to an empty array) and delete `rankWithin`. Keep the module dependency-free and browser-safe (browserSafeSchemas.test.ts scans it). In teamRanks.test.ts, paste the pre-change `rankWithin` + `buildTeamRankScopes` bodies verbatim as test-local reference functions and add the seeded equivalence tests from the behavior list (seeded PRNG — a tiny inline mulberry32 is fine). In publish.ts replace the per-team map construction with one `buildTeamRankScopesByTeam(rankableTeamRows)` call; `rankableTeamRows` is built 1:1 from `teamsRows`, so every teamsRows key still gets an entry and team-artifact `ranks` stays byte-identical.

Step 4 — shared events rows (AUDIT D3). The three existing `events/{year}/{algo}` keys stay exactly as they are (a key change would need a coordinated republish and web change). Hoist the `eventsRows` construction above the per-algorithm loop (once per season). Build and validate the artifact once per season with `buildEventsArtifact` for the first algorithm, and for each algorithm derive its artifact by spreading that base and overwriting only `algorithmId` and `algorithmVersion` (spread keeps key positions, so serialized key order is unchanged), validating those two fields with a picked preamble schema so nothing unparsed reaches an upload (T-04-22). Record in a comment that the bodies differ only by those two stamps.

Step 5 — phase timers (AUDIT D4). Add a small local timing accumulator in publish.ts (performance.now based, milliseconds keyed by label) and wrap: per season — the `runAll` replay, the records fold/grouping pass, the compare aggregate+build+upload; per (season, algorithm) — artifact build time excluding sidecar builds, total sidecar build time, and time spent awaiting uploads (in this task, the block's final await). Add a run total. Print them in the run summary after the size lines as `timing:` lines (seconds, one decimal), and add a `timings` record (label -> ms) to `PublishSummary`.

Step 6 — verify, capture, commit. Run the targeted tests, the full node suite, root tsc, and `npx tsc --noEmit -p apps/web/tsconfig.json` (teamRanks.ts is imported by the web app — read-only check, never edit apps/web). Re-run the digest harness to `<scratch>/after-task1.json` and compare against baseline-a: 0 changed, 0 added, 0 removed, identical objectCount/totalBytes/pages/manifestDigest, and copy the harness's printed `timing:` lines for the summary. Commit only these paths: `git commit -m "perf(260913-nvn): skip sidecar probes for non-RP algorithms, sort rank pools once, share events rows, time publish phases" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>" -- packages/harness/publish.ts packages/harness/publish.test.ts packages/harness/teamRanks.ts packages/harness/teamRanks.test.ts` (retry once if index.lock exists), then `git status` to confirm the scratch file is still untracked and nothing else was swept in.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/harness/teamRanks.test.ts packages/harness/publish.test.ts packages/harness/browserSafeSchemas.test.ts</automated>
    <automated>npx vitest run --project node</automated>
    <automated>npx tsc --noEmit && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
    <automated>node "$SCRATCH/compare-digests.mjs" "$SCRATCH/baseline-a.json" "$SCRATCH/after-task1.json"  (must print 0 changed, 0 added, 0 removed and all-match)</automated>
  </verify>
  <done>Two identical baseline captures exist; the after-Task-1 capture matches them byte for byte; the equivalence, gate and presim tests pass; the node suite's failure count is no higher than the Step 0 baseline (any pre-existing failure named); both tsc runs are clean; one commit containing exactly the four listed paths; the run summary prints timing lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overlap build and upload through a bounded queue, and make publish write and enforce its own budget</name>
  <files>packages/harness/uploadQueue.ts, packages/harness/uploadQueue.test.ts, packages/harness/publishBudget.ts, packages/harness/publishBudget.test.ts, packages/harness/payloadBudget.test.ts, packages/harness/publish.ts, packages/harness/publish.test.ts, docs/publish-budget.md, package.json</files>
  <behavior>
    - UploadQueue caps concurrency: 200 tasks gated on deferreds released in random order never exceed the cap in flight, reach the cap, and all complete.
    - Completion is ordering-independent: tasks finishing in reverse order still drain cleanly with every task run exactly once.
    - Backpressure: with concurrency 2 and maxPending 4, the fifth enqueue does not resolve until a running task settles.
    - Error propagation: one rejecting task makes drain reject with that error, makes every later enqueue reject with it, never starts tasks still waiting for a slot, and leaves no unhandled rejection.
    - drain on an empty queue resolves.
    - Ceiling assertion throws a named error (naming kind, key, bytes, ceiling) above the ceiling and passes at exactly the ceiling.
    - render -> parse round-trips a summary's pages into the block shape payloadBudget.test.ts reads, with ceilings taken from the constant; replacing the block in a fixture document leaves every byte outside the fence unchanged; a document with no block makes replace throw the named parse error; a summary missing any of the five page kinds makes render throw.
    - The committed doc's budgetMaxBytes for each of the five kinds equals the constant.
    - publishSeasons with the teams ceiling lowered to 1 rejects with the ceiling error and putObject is never called, in dry-run and in a real (mocked) run; with a mocked putObject rejecting once, publishSeasons rejects with that error.
  </behavior>
  <action>
Step 1 — `packages/harness/uploadQueue.ts`: export class UploadQueue constructed with `{ concurrency, maxPending? }` (maxPending defaults to concurrency x 2 and must be at least concurrency). `enqueue(task: () => Promise<void>): Promise<void>` resolves once the task is ACCEPTED — immediately while fewer than maxPending tasks are queued or running, otherwise after one settles (this await is the build loop's yield point and its memory bound). Accepted tasks start as slots free. The first task error is stored; later enqueues reject with it; tasks still waiting for a slot are dropped without starting; every task promise is caught internally so nothing surfaces as an unhandled rejection. `drain(): Promise<void>` waits for everything accepted to settle and rejects with the first error. No dependency on r2Client — the caller's task does the put, so r2Client's retry/backoff is untouched. Write uploadQueue.test.ts first (RED), then the class (GREEN).

Step 2 — `packages/harness/publishBudget.ts` (pure, no fs): move `PublishedObjectRecord`, `PageKindSizeStats`, `percentileOf` and `computeSizeStats` here from publish.ts and re-export them from publish.ts so existing importers keep working. Add `PAGE_BUDGET_MAX_BYTES` (a frozen record over the five PageKinds holding the current committed ceilings: teams 3,500,000; team 500,000; events 108,000; event 350,000; compare 20,000) — from now on the ONE home for ceilings; the doc mirrors it. Add the named errors `PublishBudgetParseError` (moved from payloadBudget.test.ts, same message) and `PublishBudgetExceededError`; `assertWithinPageBudget(pageKind, key, bytes, ceilings)` taking the ceilings record as a parameter so tests can mock the imported constant; the block regex (moved from the test) and `parsePublishBudget(markdown)`; `renderPublishBudgetBlock({ measuredAt, run, pages })` producing the fenced `json budget` block as 2-space-indented JSON with key order measuredAt, run, pages in kind order teams/team/events/event/compare, each kind's fields in the order count, medianBytes, p95Bytes, maxBytes, budgetMaxBytes, largestKey (the committed doc's order), throwing if any kind is missing; and `replacePublishBudgetBlock(markdown, block)`. Write publishBudget.test.ts for these behaviors. In payloadBudget.test.ts delete the local parser/error/regex copies, import them from publishBudget.ts, and add the doc-ceilings-equal-constant test; everything else in that file stays.

Step 3 — wire publish.ts. Replace `BoundedUploader`'s semaphore with an UploadQueue. Its page-kind publish method computes bytes, calls `assertWithinPageBudget(pageKind, key, bytes, PAGE_BUDGET_MAX_BYTES)` BEFORE recording or enqueueing (so dry-run fails identically), records, then in a real run returns the queue's enqueue of a task that calls putObject with the existing contentType/cacheControl headers; in dry-run it returns immediately. Sidecar-plus-event becomes ONE queued task that puts the sidecar and then the event artifact sequentially (artifacts-before-index ordering; the sidecar has no page-kind ceiling, the event artifact's ceiling is asserted before enqueue). In publishSeasons, `await` every publish call inline where the old code pushed promises, delete the per-block pending arrays and `Promise.all`, and `await` the queue's drain once after the season loop (before manifests, seeds and the summary). On any thrown error, let in-flight puts settle (ignoring their outcomes) and rethrow the original error. Re-point Task 1's uploadWait timer at the awaited enqueues plus the final drain (report the drain as its own `timing:` line). Set DEFAULT_CONCURRENCY to 48 and update the header usage line. Design note to leave as a comment: the ceiling is checked per object at enqueue, so an over-budget object is never uploaded, but objects from EARLIER blocks may already be in R2 when a later block fails (the same exposure as any mid-run network failure today; the next successful run overwrites them in place). `--dry-run` is therefore the complete budget pre-flight; the stricter "nothing uploads until everything is built" reading would forfeit the build/upload overlap this task exists to add.

Step 4 — CLI budget write. Add a boolean `--write-budget` flag. After `publishSeasons` returns successfully and only when the flag is set, `runSeasonsCliMode` reads docs/publish-budget.md, replaces the block with `renderPublishBudgetBlock` built from the summary's pages and a `run` string assembled from `process.argv.slice(2)` joined by spaces (never environment variables; the `--env-file` flag belongs to tsx and never appears there) plus the generation, objectCount, totalBytes, presim sidecar count with median/p95/max, start and finish UTC and duration, and a "(dry-run: nothing uploaded)" marker when applicable; `measuredAt` is the finish time. Write the file back and print the path. Append `--write-budget` to the `publish:seasons` script in package.json (after the existing flags, so the presim-flag drift tripwire's regex still matches). Narrow ad-hoc `publish:artifacts` runs do not pass the flag and never clobber the doc.

Step 5 — publish.test.ts. Add a module-level mock of `./publishBudget.js` that spreads the real module and replaces `PAGE_BUDGET_MAX_BYTES` with a mutable copy; add the two ceiling-gate tests and the put-failure test from the behavior list (restore the ceiling in a finally). Keep the existing presim ordering test green.

Step 6 — shrink docs/publish-budget.md. First grep the whole repo excluding .planning and node_modules for publish-budget and record every section each hit cites. Rewrite the doc to at most ~250 lines containing only: the title plus a "What this document is" section saying the block is machine-written by `pnpm publish:seasons` (`--write-budget`), ceilings live in `PAGE_BUDGET_MAX_BYTES` and are enforced per object before upload, and every dated run log lives in `git log -p docs/publish-budget.md`; a "Payload budget (D-05)" section with the current ceilings table, how to change a ceiling (edit the constant; payloadBudget.test.ts fails until doc and constant agree; the test's absolute ceilings sit above it), the standing rule against widening a ceiling to make a run pass, and one sentence that the team ceiling was raised 400,000 -> 500,000 on 2026-09-09 on Jacob's instruction; a short "Upload concurrency" note (default 48, `--concurrency N` override, r2Client retries transient failures, R2 DELETEs rate-limited at 16 during a cleanup pass so drop to 16 if a run shows sustained retries); the presim sidecar section reduced to its still-true facts (not a PageKind, no budget row, SPR only, 1,000 schedules x 50 draws, gated by `--presim-from-season`); "Storage and write volume (DATA-05)" reduced to the free-tier allowances and the local-counter vs dashboard caveat; the "Re-baseline cadence" runbook with its command list corrected to the files publish actually emits (`seed-opr.sql`, `seed-epa.sql`, `seed-spr.sql`) and its historical note dropped; a cleanup pointer to `pnpm cleanup:r2-generations` (list-driven; never trust a sampled census); the "Worker runtime budget (D-21/D-23, plan 04-07)" section kept VERBATIM with its subsections (apps/worker cites it by name); and "The machine-readable block" last with the block's current values unchanged. Drop the dated run logs, investigations, team-page delta, every dated delete pass, D1 read-back, version-retirement procedure, baseline provenance, state-row shape history, pre-run projection, and the long ceiling-change entry. Any comment in packages/harness or scripts that cites a dropped section by name gets reworded to cite the doc's git history (comment-only edit; add those paths to the commit and list them in the summary); citations in apps/* stay as they are (note them in the summary). If packages/harness/algorithmIdentity.test.ts still exists and fails only because the doc no longer contains a retired id its exclusion list expects, drop that list entry and its pinned length by one, add the file to the commit, and note it.

Step 7 — verify, capture, commit. Targeted tests, full node suite, root tsc. Digest harness to `<scratch>/after-task2.json` (compare with baseline-a: identical, and duplicate-put count 0), then once with BYTE_IDENTITY_DRY=1 to `<scratch>/after-task2-dry.json` (compare sizes-only: identical). Keep both runs' `timing:` lines, including the drain line, for the summary. Commit with the explicit path list above (plus any conditional paths from Step 6): message `perf(260913-nvn): drain uploads through a bounded queue while building, and publish writes and enforces its own budget` with the Co-Authored-By trailer; retry once on index.lock; `git status`.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/harness/uploadQueue.test.ts packages/harness/publishBudget.test.ts packages/harness/payloadBudget.test.ts packages/harness/publish.test.ts</automated>
    <automated>npx vitest run --project node</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node "$SCRATCH/compare-digests.mjs" "$SCRATCH/baseline-a.json" "$SCRATCH/after-task2.json" && node "$SCRATCH/compare-digests.mjs" "$SCRATCH/baseline-a.json" "$SCRATCH/after-task2-dry.json" sizes-only</automated>
    <automated>wc -l docs/publish-budget.md && grep -c "Worker runtime budget (D-21/D-23, plan 04-07)" docs/publish-budget.md && grep -c "json budget" docs/publish-budget.md</automated>
  </verify>
  <done>Queue and budget modules are tested green; publish uploads through the queue at default concurrency 48 with a final drain; ceiling breaches fail before the offending upload in both modes; `pnpm publish:seasons` carries `--write-budget`; the doc is at most ~250 lines, still parses, still carries the cited Worker section, and its ceilings equal the constant; real-mode digests and dry-run sizes match the baseline; the node suite is no worse than baseline; one commit with explicit paths.</done>
</task>

<task type="auto">
  <name>Task 3: Harness and core leftovers — SPR names, inert knobs, eligibility collapse, dedupes, subset tables, Sigma naming</name>
  <files>packages/harness/stateSnapshot.ts, packages/harness/stateSnapshot.test.ts, packages/harness/publish.ts, packages/harness/publish.test.ts, packages/harness/manifests.ts, packages/harness/manifests.test.ts, packages/harness/seasonBoundary.ts, packages/harness/seasonBoundary.test.ts, packages/harness/sigmaScore.ts, packages/harness/sigmaScore.test.ts, packages/harness/score.ts, packages/harness/score.test.ts, packages/harness/selectionProvenance.ts, packages/harness/selectionProvenance.test.ts, packages/harness/seasonSpec.ts, packages/harness/sigmaScoutLayer.ts, packages/harness/consistencyMetric.ts, packages/harness/consistencyMetric.test.ts, packages/harness/sigmaMetric.ts, packages/harness/sigmaMetric.test.ts, packages/harness/preSchedule.ts, packages/harness/metricDirection.ts, scripts/replayRig.ts, scripts/epaVsStatbotics.ts, scripts/publishDistricts.ts, scripts/verifySubsetPublish.ts, scripts/measureGeneratedSchedules.ts</files>
<!-- planner-discipline-allow: BprState -->
<!-- planner-discipline-allow: promoted VPR -->
  <action>
Work item by item; after each, run the directly affected test files and root tsc before moving on. If an item grows past its stated file set, touches apps/*, or would edit a sealed file (packages/spr/{model,evaluate,data,cli}.ts, packages/core/algorithms/spr.ts), revert that item's edits and record it as skipped with the reason. Files named by the AUDIT A/B delete lists (e.g. deleteRetiredAlgorithmObjects.ts, measureEpaDeviations.ts, measureRandomSchedules.ts, algorithmIdentity.test.ts) belong to the other plans in this quick task: if one still exists and references a symbol you change, make the minimal compiling edit there, add it to the commit, and note it.

Before starting, capture offline evidence for item 8: a scratchpad tsx one-off that imports `PUBLISHED_SUBSET` and `PUBLISHED_TEAM_SUBSET` from scripts/verifySubsetPublish.ts and writes to `<scratch>/subset-before.json` the non-absence entries (expectAbsent not true) with `note` removed, sorted by eventKey/teamKey then algorithmId, de-duplicated by that pair. (Importing is side-effect free: the file only runs main() as the entry point.)

1. SPR serializer names. In stateSnapshot.ts rename the module-private BPR-era serializer and deserializer to `serializeSprState` / `deserializeSprState` (dispatch sites and the doc comment that names the deserializer), and retitle the stateSnapshot.test.ts describe that names it. Seed bytes are unaffected.

2. publish.ts truths. Rewrite the `resolvePublishAlgorithms` doc comment to what it does now: resolve the requested ids (default `PUBLISHED_ALGORITHM_IDS`) against the single module registry, throwing on an unknown id — delete the promoted VPR swap narrative and the cli.ts reference. Change the seed block's state cast to include SprState (import the type from core spr.ts; importing a type does not edit the sealed file).

3. One registry. If plan 01 created a standalone algorithm-registry module, make it the single home; otherwise export `PUBLISHED_ALGORITHM_MODULES` from manifests.ts (Node-side, already imports opr/epa/spr; publishedAlgorithms.ts must stay ids-only — manifests.test.ts pins its exports and apps/web imports it). Point `buildAlgorithmsManifest` and `resolvePublishAlgorithms` (keep its exact unknown-id message, publish.test.ts pins it) and any surviving registry copy at it; delete the publish.ts `BASE_PUBLISH_ALGORITHMS` literal and the manifests.ts local `modules` literal.

4. Inert knobs.
(a) Remove `paramsSeason` from `BuildAlgorithmsManifestOptions` and its destructure, and from the calls in publish.ts, scripts/replayRig.ts and manifests.test.ts (drop or retitle any manifests test whose only point was the parameter). Leave manifestSchemas.ts's optional schema field alone.
(b) Remove `coldStartSeason` from `PublishSeasonsOptions` and the log branch that names it; remove `seasonBoundaryFor`'s optional third parameter and the seasonBoundary.test.ts cases that exercise it — only if no remaining caller passes three arguments (grep first).
(c) Remove the `--rp-calibration` and `--no-rp-calibration` flags from main() and their parameters from `runSeasonsCliMode`, which then always loads `RP_CALIBRATION_MEASUREMENT_PATH` — only after grepping package.json, scripts, docs and packages (excluding .planning) for callers passing those flags; if a doc names them, reword that line. `PublishSeasonsOptions.rpCalibration` stays (tests pass it).
(d) Remove `scale` and `talentPrior` from `SigmaScoreOptions` and `DEFAULT_SIGMA_SCORE_OPTIONS`, the multiplier on the reported figure (it was always 1, and multiplying by 1 is exact, so no Sigma value changes), the talent-independent branch, the doc lines describing them, and the sigmaScore.test.ts control test. Then run `npx tsc --noEmit -p apps/worker/tsconfig.json` and `npx tsc --noEmit -p apps/web/tsconfig.json` (both import sigmaScore.ts); if either passes these options, revert this sub-item and record it as skipped.
(e) `includeOffseason` default: SKIP deliberately. Flipping `PublishSeasonsOptions`' default to true removes no branch (every call site still derives the exclusion flag from it either way) and would silently change the fixtures of the many publish.test.ts tests that rely on the default. Record this in the summary.

5. Eligibility collapse. Delete selectionProvenance.ts and selectionProvenance.test.ts. In score.ts, `isHeadlineEligible` takes (season, corpusSeasons) and applies only the distinct-prior-seasons rule; `AggregateScoresOptions` keeps `corpusSeasons` required and replaces the per-algorithm selected-on map with a REQUIRED eligibility field whose only values are "from-corpus-seasons" and the existing `ELIGIBILITY_NOT_CLAIMED` sentinel (keeping D-2's no-silent-default rule); remove the missing-algorithm-entry check. publish.ts calls `aggregateScores(harnessPredictions, { corpusSeasons: selectCorpusSeasons(db), eligibility: "from-corpus-seasons" })`; scripts/epaVsStatbotics.ts passes the sentinel under the new field name. Trim score.test.ts's selected-on cases, keeping prior-seasons and sentinel coverage; fix the compare-eligibility tests in publish.test.ts. Compare artifact bytes (which carry `headlineEligible`) are proven unchanged by the digest capture. If this item needs edits outside score.ts, score.test.ts, the two deleted files, publish.ts, publish.test.ts, scripts/epaVsStatbotics.ts and a surviving plan-01 importer, revert it and record it as skipped.

6. Version split. Delete publish.ts's module-private `splitVersion` and use `splitManifestVersion` from manifestSchemas.ts in `buildCompareArtifact`; if a test pins the old publish-specific error message, update it to the manifest error class.

7. Season spec parser. Create `packages/harness/seasonSpec.ts` (no imports) exporting `parseSeasonSpec(spec, flagName)` with publish.ts's term grammar (single year, range, comma list; ascending and de-duplicated; empty, bad-term and reversed-range errors that embed flagName). publish.ts's exported `parseSeasonsRange` becomes a one-line wrapper for "--seasons" and scripts/publishDistricts.ts's `parseYearsSpec` a one-line wrapper for "--years" (replace its header note about avoiding the large module). Keep every existing parser test green, loosening a message assertion only where it pinned wording that now differs by flag name. Leave the lenient `parseSeasons` copies in scriptHelpers.ts, measureRpCalibration.ts and measureRpMeanDeficit.ts untouched — their semantics differ (one feeds a sealed-slice guard).

8. Subset tables. In scripts/verifySubsetPublish.ts replace the rename-control composition with literal current tables: the event table holds the 15 premier entries written with `algorithmId` "spr" and the effective RP-pmf expectation the rename mapping produced ("present" became "partial"), the two opr/epa 2024casf arms once each, and the 2024auwarp entry; delete the sigma1 absence controls, the renamed-duplicate mapping, both retired-vpr absence layers, their helper, the premier-id constant if it goes unused, and the "non-control" sigma1 filters in the two generation-uniformity reports. Apply the same rule to the team subset (drop sigma1/vpr absence entries, keep the live-algorithm entries). Keep `expectAbsent`/`version` support and `assertSubsetEntryShape`, since absence entries remain a valid tool for a future retirement. Rewrite the header to describe the current tables in a few lines. Re-run the item-8 capture to `<scratch>/subset-after.json` and compare: the non-absence entry sets must be identical. The script needs network to run for real; say in the summary that the next `pnpm verify:subset` after a publish is its first live exercise.

9. Sigma naming (contained change only). If grep still shows no importer under apps/*: `git mv packages/harness/consistencyMetric.ts packages/harness/sigmaMetric.ts` and the same for its test FIRST (git mv before editing: an edit followed by git mv can silently drop content), then rename `consistencyMetricByTeam` -> `sigmaMetricByTeam`, `ConsistencyMetricEntry` -> `SigmaMetricEntry`, and the layer method `consistencyByTeam()` -> `sigmaByTeam()` (grep sigmaScoutLayer.ts for a name collision first), updating call sites in publish.ts, sigmaScoutLayer.ts and its tests, sigmaSeed.test.ts, preSchedule.ts and its test (including a surviving field-input property of the same name), metricDirection.ts's comment, and scripts/measureGeneratedSchedules.ts (plus a surviving measureRandomSchedules.ts). If this exceeds ~15 files or reaches apps/*, revert it and record it as skipped.

Final verification: full node suite, root tsc, apps/worker tsc, apps/web tsc; digest harness to `<scratch>/after-task3.json` compared against baseline-a (identical, including manifestDigest). Then delete `packages/harness/zz-byte-identity.scratch.test.ts`. Confirm with `git diff --stat "$BASE" -- packages/spr/model.ts packages/spr/evaluate.ts packages/spr/data.ts packages/spr/cli.ts packages/core/algorithms/spr.ts` (BASE from Task 1 Step 0; compares against the working tree, so it also catches uncommitted edits) that it prints nothing. Commit ONLY the paths actually changed, listed explicitly, including deletions and both sides of each rename (git records a rename only when both paths are in the commit): message `refactor(260913-nvn): SPR serializer names, one algorithm registry, drop inert publish knobs and selected-on plumbing, dedupe parsers, current verify:subset tables` with the Co-Authored-By trailer; retry once on index.lock; then `git status`, and `git show --stat HEAD` to confirm the renamed files carry their edited content.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/harness/stateSnapshot.test.ts packages/harness/manifests.test.ts packages/harness/seasonBoundary.test.ts packages/harness/sigmaScore.test.ts packages/harness/score.test.ts packages/harness/publish.test.ts scripts/publishDistricts.test.ts</automated>
    <automated>npx vitest run --project node</automated>
    <automated>npx tsc --noEmit && npx tsc --noEmit -p apps/worker/tsconfig.json && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
    <automated>! grep -rq "BprState" packages/harness scripts</automated>
    <automated>! grep -q "promoted VPR" packages/harness/publish.ts</automated>
    <automated>node "$SCRATCH/compare-digests.mjs" "$SCRATCH/baseline-a.json" "$SCRATCH/after-task3.json"</automated>
    <automated>test ! -f packages/harness/zz-byte-identity.scratch.test.ts && test -z "$(git diff --stat "$BASE" -- packages/spr/model.ts packages/spr/evaluate.ts packages/spr/data.ts packages/spr/cli.ts packages/core/algorithms/spr.ts)" && echo sealed-untouched</automated>
  </verify>
  <done>Each item is either done with its tests green or recorded as skipped with a reason; the after-Task-3 digest capture equals the baseline; the subset before/after comparison is identical; node suite no worse than the Step 0 baseline; root, worker and web tsc clean; no sealed file changed; the scratch harness is deleted and was never committed; one refactor commit with explicit paths, verified by git status and git show --stat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local publisher -> R2 (S3 API) | Signed PUTs with credentials from .env; partial state is served to the public from the bucket |
| .env -> process/transcript | Live TBA and R2 secrets loaded by tsx; must never reach logs, the budget doc, commits or transcripts |
| shared checkout <-> concurrent sessions | One working tree, one git index, reports/publish seed files a pending D1 import may still need |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nvn03-01 | Tampering | publishSeasons per-object budget gate with overlapping uploads | medium | mitigate | Ceiling asserted before record/enqueue in dry-run and real runs; a first-artifact breach is proven to make zero puts; the doc and a code comment name `--dry-run` as the complete pre-flight and state that earlier blocks may already be uploaded (overwritten in place by the next successful run) |
| T-nvn03-02 | Denial of Service | UploadQueue memory | low | mitigate | enqueue blocks at maxPending (default concurrency x 2), so at most that many bodies are held; covered by the backpressure unit test |
| T-nvn03-03 | Denial of Service | R2 throttling at concurrency 48 | medium | mitigate | r2Client retry/backoff unchanged; `--concurrency N` override kept; the doc's upload-concurrency note records the concurrency-16 DELETE throttling and says to drop to 16 on sustained retries; the summary flags the next real republish as the first live exercise |
| T-nvn03-04 | Information Disclosure | budget doc `run` string, logs, transcripts | high | mitigate | run string built only from process.argv.slice(2) and summary fields, never environment variables; the executor never reads, cats or echoes .env; no real publish, network or deploy happens in this plan |
| T-nvn03-05 | Tampering | scratch digest harness vs reports/publish seed files | medium | mitigate | harness hard-codes skipState true (no seed or manifest writes) and mocks putObject (no R2 writes); it runs only when BYTE_IDENTITY_OUT is set, is never staged, and is deleted in Task 3 |
| T-nvn03-06 | Tampering | sealed SPR holdout files | high | mitigate | never edited; importing a type from core spr.ts only; `git diff --stat` over the sealed paths is part of Task 3 verification |
| T-nvn03-07 | Repudiation | shared git index | medium | mitigate | commits use `git commit -- <explicit paths>` only (never add -A, add ., or commit -a); `git status` after each commit; apps/*, .planning/STATE.md and other plans' files untouched unless a compile break forces a minimal, noted edit |
</threat_model>

<verification>
- Two pre-change digest captures are identical (determinism), and the captures after Tasks 1, 2 and 3 each equal them: 0 changed, 0 added, 0 removed keys; identical objectCount, totalBytes, pages and manifestDigest; the Task 2 dry-run capture matches sizes.
- `npx vitest run --project node` from the repo root after each task: failure count no higher than the Step 0 baseline, counts read from the output text.
- `npx tsc --noEmit` at root after each task; apps/web tsc after Tasks 1 and 3; apps/worker tsc after Task 3.
- Exactly three new commits, each with explicit paths, and the scratch harness absent from all of them.
- No sealed file, apps/* file or .planning/STATE.md changed.
</verification>

<success_criteria>
- OPR/EPA sidecar probing gone; rank scopes sort once per pool (randomized equivalence test green); events rows built once per season; per-phase timings printed and returned.
- Uploads overlap builds through a bounded, backpressured queue at default concurrency 48; put failures and ceiling breaches fail the run, with ceiling breaches caught before the offending upload in both modes.
- `pnpm publish:seasons` writes its own json budget block; ceilings live in one constant the doc must equal; docs/publish-budget.md is shrunk to still-true content.
- Harness/core leftovers resolved or explicitly skipped with reasons; published bytes provably unchanged.
</success_criteria>

<output>
Do NOT write a SUMMARY.md (the orchestrator writes it). Return in the final message:
- the three commit SHAs with subjects and `git show --shortstat` per commit;
- baseline and per-task node-suite counts, and tsc results;
- byte-identity evidence: season set used, object count, comparison output for baseline-a vs baseline-b and vs after-task1/2/3 (plus the dry-run sizes check), and the subset before/after result;
- the `timing:` lines from the after-task1 and after-task2 captures (real and dry-run), including the drain line;
- every item skipped with its reason (includeOffseason default is skipped by design), every conditional file touched (other-plan files, comment citations, algorithmIdentity.test.ts), and apps/* citations of dropped doc sections left as they were;
- notes for the orchestrator: (1) AUDIT F5 (possible walk-forward leak in presim sidecars) was not investigated; (2) the next real republish is the first live exercise of the upload queue at concurrency 48 and of `--write-budget`, so it should be attended, dropping to `--concurrency 16` on sustained retries; (3) the next `pnpm verify:subset` is the first live run of the rewritten tables; (4) the memory entry saying publish-budget is a manual step is now stale; (5) the last committed budget block reports 641 presim sidecars from before OPR/EPA stopped publishing RP odds, so the next republish will report roughly a third of that regardless of this plan.
</output>
