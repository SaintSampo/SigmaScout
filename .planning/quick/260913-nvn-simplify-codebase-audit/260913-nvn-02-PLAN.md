---
phase: quick-260913-nvn
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  # Task 1: web orphans + Swing/VPR/spread leftovers (incl. audit bug F2)
  - apps/web/src/components/team/rpMoments.ts
  - apps/web/src/components/team/rpMoments.test.ts
  - apps/web/src/components/ui/separator.tsx
  - apps/web/src/components/ui/input.tsx
  - apps/web/src/components/ui/textarea.tsx
  - apps/web/src/components/ui/avatar.tsx
  - apps/web/src/components/ui/command.tsx
  - apps/web/src/components/ui/dialog.tsx
  - apps/web/src/components/ui/input-group.tsx
  - apps/web/src/components/ui/select.tsx
  - apps/web/src/components/ui/sheet.tsx
  - apps/web/src/components/ui/table.tsx
  - apps/web/src/lib/api/compare.compat.test.ts
  - apps/web/src/routes/__fixtures__/compare-2022-legacy-seasonLabel.json
  - apps/web/src/styles/theme.css
  - apps/web/src/components/MetricValue.tsx
  - apps/web/src/components/MetricValue.test.tsx
  - apps/web/src/components/team/SeasonHeader.test.tsx
  - apps/web/src/lib/metricGroups.ts
  - apps/web/src/lib/metricGroups.test.ts
  - apps/web/src/lib/metricKeys.ts
  - apps/web/src/lib/metricKeys.test.ts
  - apps/web/src/lib/officialSnapshot.ts
  - apps/web/src/lib/officialSnapshot.test.ts
  - apps/web/src/components/compare/MethodologyNote.tsx
  - apps/web/src/components/compare/MethodologyNote.test.tsx
  - apps/web/src/components/compare/calibrationSeries.ts
  - apps/web/src/components/compare/comparePalette.test.ts
  - apps/web/src/components/event/StartMatchPicker.tsx
  - apps/web/src/components/event/useSimulationRun.ts
  - apps/web/src/components/methodology/sigmaContent.ts
  - apps/web/src/components/methodology/sigmaContent.test.ts
  - apps/web/src/lib/searchParams.ts
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/src/components/teams-table/columns.tsx
  - apps/web/src/components/teams-table/columns.test.tsx
  - apps/web/src/components/event/AlliancesTab.tsx
  - apps/web/src/components/event/SimulationTab.tsx
  - apps/web/src/routes/event.$eventKey.tsx
  - apps/web/src/routes/event.$eventKey.test.tsx
  - apps/web/src/components/event/eventMatchAxis.ts
  - apps/web/src/components/event/eventMatchAxis.test.ts
  - apps/web/src/components/event/EventMatchTable.tsx
  - apps/web/src/components/event/EventMatchTable.test.tsx
  - apps/web/src/routes/team.$teamNumber.tsx
  - apps/web/src/lib/query-client.ts
  # Task 2: cheap page-load wins
  - apps/web/vite.config.ts
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/routes/__root.tsx
  - apps/web/public/_headers
  - apps/web/public/alfredo-systems.png
  - apps/web/public/alfredo-systems.webp
  - apps/web/src/lib/api/manifests.test.ts
  - apps/web/src/favicon.test.ts
  # Task 3: test + component dedup
  - apps/web/src/test/helpers.ts
  - apps/web/src/components/event/AlliancesTab.test.tsx
  - apps/web/src/components/event/BreakdownTab.test.tsx
  - apps/web/src/components/event/InsightsTab.test.tsx
  - apps/web/src/components/event/RankDistributionTable.test.tsx
  - apps/web/src/components/teams-table/TeamsTable.test.tsx
  - apps/web/src/components/teams-table/TeamsFilters.test.tsx
  - apps/web/src/components/teams-table/teamFilterModel.test.ts
  - apps/web/src/components/ribbon/AlgorithmSelect.test.tsx
  - apps/web/src/components/ribbon/Ribbon.test.tsx
  - apps/web/src/components/ribbon/YearSelect.test.tsx
  - apps/web/src/components/search/SearchBox.test.tsx
  - apps/web/src/components/team/MatchTable.tsx
autonomous: true
requirements:
  - 260913-nvn

estimate:
  tokens: 120000
  raw_tokens: 240000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "apps/web carries none of the audit's verified web orphans: rpMoments, ui/separator, ui/input, ui/textarea, the compare compat test and its legacy fixture, the dead spread-superscript CSS, the listed dead exports and the unused shadcn sub-components"
    - "apps/web/package.json no longer lists @fontsource-variable/geist or shadcn, and the site still builds and its tests still pass"
    - "SPR has a Compare calibration colour: theme.css declares a compare-algo token for every id in PUBLISHED_ALGORITHM_IDS, pinned by comparePalette.test.ts (fixes audit bug F2)"
    - "Every teams-table non-Total metric column uses the one 88px wide-viewport width; no algorithm-dependent spread width remains"
    - "The Simulation tab trigger is enabled for SPR and disabled (with the existing SPR-only title) for OPR and EPA, and the pre-schedule sidecar is never fetched for OPR/EPA"
    - "Event match rows carry the Match Band variance under its own name end to end; no row field relabels it"
    - "index.html paints a Pine-green ribbon before JS, carries no comments, and the built HTML preconnects to and preloads the algorithms manifest on the resolved artifact origin"
    - "main.tsx starts the algorithms manifest query before the first render with the same query options useAlgorithmVersion uses"
    - "The footer sponsor logo ships as a ~2x-display-size WebP with explicit width/height and lazy loading; dist/_headers marks /assets/* immutable"
    - "The event route test file covers the per-tab 404/500/pending/panel/scroll-sibling/click-preserves-search behaviour through it.each rather than per-tab copies, with no assertion lost"
    - "mockNarrowViewport, makeQueryClient and the genuinely identical artifact builders live once in src/test/helpers.ts"
    - "EventMatchTable renders through the team MatchTable's shared leaf components (alliance marks, axis header, chip, score lines, call badge) instead of private twins, with identical test ids and behaviour"
  artifacts:
    - path: "apps/web/src/test/helpers.ts"
      provides: "shared test helpers: mockNarrowViewport, makeQueryClient, makeEventArtifact, makeTeamsArtifact"
    - path: "apps/web/public/_headers"
      provides: "immutable Cache-Control for hashed /assets/*"
    - path: "apps/web/public/alfredo-systems.webp"
      provides: "resized footer logo (replaces the 81 KB PNG)"
    - path: "apps/web/src/components/team/MatchTable.tsx"
      provides: "exported shared match-row leaf components consumed by EventMatchTable and StartMatchPicker"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/lib/api/manifests.ts"
      via: "queryClient.prefetchQuery(algorithmsManifestQueryOptions()) before createRoot().render"
      pattern: "prefetchQuery\\(algorithmsManifestQueryOptions\\(\\)\\)"
    - from: "apps/web/vite.config.ts"
      to: "dist/index.html"
      via: "transformIndexHtml injects preconnect + preload as=fetch crossorigin for the resolved VITE_ARTIFACT_ORIGIN"
      pattern: "rel=\"preload\""
    - from: "apps/web/src/components/compare/CalibrationSection.tsx"
      to: "apps/web/src/styles/theme.css"
      via: "var(--compare-algo-${algorithmId}) must resolve for spr"
      pattern: "--compare-algo-spr"
    - from: "apps/web/src/components/event/EventMatchTable.tsx"
      to: "apps/web/src/components/team/MatchTable.tsx"
      via: "imports AllianceRow, AxisHeader, AllianceChip, PredictedScoreLine, ActualScoreLine, CallBadge, teamNumberLabel"
      pattern: "from \"../team/MatchTable.js\""
---

<objective>
Shrink apps/web: delete verified orphans and Swing/VPR/spread leftovers (fixing audit bug F2, SPR has no Compare colour), land four cheap page-load wins, and collapse duplicated tests and the team/event match-table twin components.

Purpose: the owner's first goal is fewer lines, files and less complexity; the page-load items are one-liners that remove a request waterfall hop, an 81 KB image, a cold-load colour flash and needless revalidation of hashed assets.

Output: three atomic commit groups (Task 1, Task 2, Task 3 as tests + components), a returned summary text (NOT a SUMMARY.md file), and an orchestrator visual-check list.

Decomposition note: tracer-first does not apply. This is a pure simplification of an already-proven architecture, and the orchestrator fixed the task order (orphans/leftovers, page-load wins, dedup). All three tasks are `auto`.

Planner re-verification (done 2026-09-13 against HEAD d3b304b0 with an import-graph script from main.tsx plus a per-export reference scan, comments stripped). Every audit claim in scope held, with these corrections the executor must follow:
- "unused exports": `groupMetricKey`, `publishesGroupMetrics`, `officialSnapshotMetrics`, `formatSeasonList` and `SIGMA_STEADY_ALLIANCE_EXAMPLE_SIGMAS` have zero production references but ARE exercised by tests, so deleting them means editing those tests too. `START_MATCH_PICKER_MAX_H_PX`, `SimulationRunStatus` and `CompareCalibrationBin` have zero references anywhere.
- "4 search types": the dead four are `EventsSearch`, `EventSearch`, `DistrictsSearch`, `MatchSearch`. `TeamsSearch` is imported by `routes/__root.test.tsx` and `routes/teams.test.tsx`, so KEEP it.
- `ui/input.tsx` and `ui/textarea.tsx` are imported only by `ui/input-group.tsx`'s `InputGroupInput`/`InputGroupTextarea`, which nothing uses. They become dead only once those two are removed.
- "makeArtifact x12": only two groups genuinely match. (a) The schema-parsed event-artifact builder in BreakdownTab/InsightsTab/AlliancesTab tests. (b) The teams-artifact builder in TeamsFilters/teamFilterModel tests. ElimsTab/QualsTab use cast builders with different event keys and names, and the other seven build different artifact types. Do not force-fit those. `mockNarrowViewport` (5 copies) and `makeQueryClient` (4 copies) are byte-identical.
- "5 twin components": the five leaf twins are identical. `EventAllianceRow` differs only by drawing the dot when `played`, and the caller can express that by passing `actual` undefined. The two ROW components are NOT identical: the team row has a Result column, own-team highlight classes and different roster-number classes, so they stay separate. The Call cell is behaviourally identical (the event copy's explicit tie branch returns the same "Prediction incorrect" badge the fallthrough already returns for a tie), so it is shared.
- `SIMULATION_AVAILABLE`: resolved as `isSimulationDisabled = !usesSigmaScore(algorithm)` rather than deleting the disabled branch. The route already carries the SPR-only title copy. Since 260913-it4 OPR/EPA publish no RP pmfs and no pre-schedule sidecar, so today they open a tab that 404s its sidecar fetch. Gating makes the copy true, stops that wasted request, and still deletes the constant and its 25-line doc.
- `compare.compat.test.ts` is referenced by three comments in `packages/harness/pageArtifacts.ts` (~1577, ~1641, ~1663). That file is outside this plan's boundary, so report it for plan 04 and do not edit it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-AUDIT.md
@.claude/skills/sketch-findings-sigmascout/SKILL.md

## Shared-checkout rules (a plan-01 executor is deleting non-web code in this same checkout, concurrently)

- Touch ONLY files under `apps/web/` plus `pnpm-lock.yaml`. Never touch `.planning/STATE.md`, root `package.json`, `packages/`, `scripts/`, `docs/` or `.claude/`. Never read `.env`.
- Commit ONLY with explicit paths: `git commit -m "<subject>" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>" -- <path> <path> ...`. Deleted tracked files are committed by naming them in the pathspec. NEW files must first be staged by explicit path (`git add -- <newfile>`), then also named in the commit pathspec. Never use `git add -A`, `git add .`, `git commit -a`, heredoc commit messages, or `git mv`.
- On an `index.lock` error: wait a few seconds, retry once. After EVERY commit run `git status --short` and confirm none of your paths are still modified and no foreign path was swept in (`git show --stat HEAD`).
- No network, no deploy, no push, no e2e/playwright runs.
- Test failures in files that import from `packages/` may be caused by the concurrent plan-01 deletion. If a failure's stack points outside apps/web and the same test passed at baseline, report it and do not fix it.
- Commit subjects: `refactor(260913-nvn): ...`, `perf(260913-nvn): ...`, `test(260913-nvn): ...`.
- Do NOT write SUMMARY.md. Return the summary as text in the final message.
- Comment trimming is plan 04's job. Only edit a comment when it names a symbol, class or file this plan deletes. Then reword it minimally so no dangling name remains.

## Verification protocol (read counts, never trust an exit code, never wrap anything in `timeout`)

- Tests (from `apps/web`): `npx vitest run > <your-scratchpad>/vitest-<label>.txt 2>&1; grep -E "Test Files|Tests  |FAIL" <your-scratchpad>/vitest-<label>.txt`. Use Bash timeout 600000. Record the passed/failed file and test counts. A removed test file lowers the file count, and that is expected. Any NEW failing test is a regression.
- Typecheck (root tsc misses apps/web): from `apps/web`, `npx tsc --noEmit -p tsconfig.json; echo "exit=$?"`. It was clean at plan time (exit 0, no output). The root tsconfig does not enable noUnusedLocals, so remove imports that a deletion orphans by hand.
- Build (from `apps/web`): `npx vite build 2>&1 | grep -E "index-.*\.js|index-.*\.css|MetricHistoryChart|built in|warn"`. Record the main `index-*.js` size and gzip size. The audit baseline was 899.9 KB / 263.5 KB gzip. Re-measure it yourself, since other sessions have committed since.
- The executor cannot screenshot (the artifact proxy needs network). Every visual change goes on the orchestrator visual-check list in the final message and is never claimed as verified.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete web orphans and Swing/VPR/spread leftovers; give SPR its Compare colour (F2)</name>
  <files>apps/web/src/components/team/rpMoments.ts, apps/web/src/components/team/rpMoments.test.ts, apps/web/src/components/ui/{separator,input,textarea,avatar,command,dialog,input-group,select,sheet,table}.tsx, apps/web/src/lib/api/compare.compat.test.ts, apps/web/src/routes/__fixtures__/compare-2022-legacy-seasonLabel.json, apps/web/src/styles/theme.css, apps/web/src/components/MetricValue.tsx, apps/web/src/components/MetricValue.test.tsx, apps/web/src/components/team/SeasonHeader.test.tsx, apps/web/src/lib/{metricGroups,metricKeys,officialSnapshot}.ts + their .test.ts, apps/web/src/components/compare/{MethodologyNote.tsx,MethodologyNote.test.tsx,calibrationSeries.ts,comparePalette.test.ts}, apps/web/src/components/event/{StartMatchPicker.tsx,useSimulationRun.ts,AlliancesTab.tsx,SimulationTab.tsx,eventMatchAxis.ts,eventMatchAxis.test.ts,EventMatchTable.tsx,EventMatchTable.test.tsx}, apps/web/src/components/methodology/sigmaContent.ts + .test.ts, apps/web/src/lib/searchParams.ts, apps/web/package.json, pnpm-lock.yaml, apps/web/src/components/teams-table/columns.tsx + .test.tsx, apps/web/src/routes/event.$eventKey.tsx + .test.tsx, apps/web/src/routes/team.$teamNumber.tsx, apps/web/src/lib/query-client.ts</files>
  <action>
STEP 0 (baseline, before any edit): run the vitest, typecheck and build commands from the context block, and save the vitest output as `vitest-baseline.txt`. Record the counts and main-chunk size/gzip. Also record `git diff --shortstat HEAD -- apps/web` (should be empty) and the HEAD sha as the plan-02 start point.

A. Orphan deletes (git rm-equivalent: delete the files; they get committed by pathspec):
1. Delete `components/team/rpMoments.ts` and `rpMoments.test.ts`.
2. Delete `components/ui/separator.tsx`.
3. Delete `lib/api/compare.compat.test.ts` and `routes/__fixtures__/compare-2022-legacy-seasonLabel.json`. Do not touch the other compare-*.json fixtures.
4. theme.css: delete the `.metric-spread-superscript` rule and its whole preceding doc comment block. `MetricValue.test.tsx`: in the "renders no plus-minus superscript element at all" test, delete only the class-selector assertion and KEEP the no-plus-minus-text assertion. `SeasonHeader.test.tsx`: delete the whole "renders no plus-minus superscript anywhere in the header" test, since its only assertion is the class absence. `MetricValue.tsx` ~line 56: reword the comment so it no longer names the deleted class.
5. Dead exports (delete the declaration, then fix any orphaned import and any comment naming it):
   - `lib/metricGroups.ts` `groupMetricKey`. In metricGroups.test.ts, drop it from the import, delete the test(s) that exist only to exercise it, and drop `COMPONENT_GROUP_METRIC_KEYS` from the import if it becomes unused.
   - `lib/metricKeys.ts` `publishesGroupMetrics`. In metricKeys.test.ts, remove it from the import and delete its assertions/test, keeping the `hasGroupedTeamsView` coverage in that describe.
   - `lib/officialSnapshot.ts` `officialSnapshotMetrics`. Rewrite the three officialSnapshot.test.ts cases to call the surviving production function `officialSnapshotRow(history, events)?.metrics`, so the coverage of the real code path is kept.
   - `components/compare/MethodologyNote.tsx` `formatSeasonList`. Delete its describe block in MethodologyNote.test.tsx, and reword the ~line 192 doc comment that names it.
   - `components/event/StartMatchPicker.tsx` `START_MATCH_PICKER_MAX_H_PX` and its doc comment. Leave `apps/web/e2e/` untouched, even though a stale e2e comment names it.
   - `components/event/useSimulationRun.ts` type `SimulationRunStatus`.
   - `components/compare/calibrationSeries.ts` type `CompareCalibrationBin`.
   - `components/methodology/sigmaContent.ts` `SIGMA_STEADY_ALLIANCE_EXAMPLE_SIGMAS`. Delete the sigmaContent.test.ts assertions/test that use it, and drop it from the import.
   - `lib/searchParams.ts` types `EventsSearch`, `EventSearch`, `DistrictsSearch`, `MatchSearch`. KEEP `TeamsSearch`, because two route tests import it.
6. Unused shadcn sub-components. Delete the function and its export-list entry for each of these:
   - avatar: `AvatarBadge`, `AvatarGroup`, `AvatarGroupCount`
   - command: `CommandSeparator`, `CommandShortcut`
   - dialog: `DialogClose`, `DialogFooter` (keep `DialogOverlay`/`DialogPortal`, which `DialogContent` uses internally, and keep the `Button` import that `DialogContent` still uses)
   - select: `SelectGroup`, `SelectLabel`, `SelectSeparator` (keep the scroll buttons, which are used internally)
   - sheet: `SheetClose`, `SheetDescription`
   - table: `TableFooter`, `TableCaption`
   - input-group: `InputGroupButton`, `InputGroupText`, `InputGroupInput`, `InputGroupTextarea`, plus the now-dead `inputGroupButtonVariants` cva and the `Button`, `Input` and `Textarea` imports
   Then delete `ui/input.tsx` and `ui/textarea.tsx`. Before deleting each, re-run `git grep -n "<Name>" -- apps/web/src` to confirm only its own file matches.
7. Deps: remove `@fontsource-variable/geist` and `shadcn` from `apps/web/package.json` dependencies. Keep `components.json`. Lockfile, from the repo root:
   - First run `git status --short -- package.json pnpm-lock.yaml apps/worker/package.json`. If ANY of those is already modified by another session, skip the lockfile, and report "lockfile left for orchestrator: foreign edits present".
   - Otherwise run `pnpm install --lockfile-only --offline` and judge the result by `git diff --numstat -- pnpm-lock.yaml`, not by exit code. Commit the lockfile only if it shows deletions and ZERO additions and the diff mentions the two removed packages.
   - If the command fails or additions appear, restore the file (`git checkout -- pnpm-lock.yaml`, safe only because the pre-check proved it was unmodified) and report "lockfile left for orchestrator: needs network or re-resolves".
   - Never run a plain `pnpm install`.

B. Swing/VPR/spread leftovers:
8. F2 palette fix. theme.css: rename the `--compare-algo-vpr` custom property to `--compare-algo-spr` (same #0D9488 value) and rename the `.compare-algo-vpr` utility class to `.compare-algo-spr`. comparePalette.test.ts: rename the EXPECTED_TOKENS key and any header-comment mention. Add a test that loops over `PUBLISHED_ALGORITHM_IDS` (import from `packages/harness/publishedAlgorithms.js`, the same relative-depth pattern other compare files use) and asserts that `declaredValue(css, "--compare-algo-" + id)` is defined, so a future algorithm id without a colour fails loudly.
9. columns.tsx: delete `algorithmPublishesSpread`, `metricColumnWidth` and the 120px spread-sized constant, together with their doc comments. Rename the 88px constant to `METRIC_COLUMN_WIDTH_PX` (value 88), keeping one short line of its measurement rationale. `metricColumnWidthFor` returns `TOTAL_SIGMA_COLUMN_WIDTH_PX` for a Sigma Total, else `isNarrow ? 120 : METRIC_COLUMN_WIDTH_PX`. columns.test.tsx: replace the "metricColumnWidth — D-1 spread-carrying vs spread-less" describe with one test: wide non-Total metric column size equals `METRIC_COLUMN_WIDTH_PX` for BOTH spr and epa, and the narrow size stays the literal 120. Point the ~line 289 Total assertion at `METRIC_COLUMN_WIDTH_PX`. Run `git grep -n "120" -- apps/web/src/components/teams-table/TeamsTable.test.tsx` and fix any wide-mode SPR width pin the same way. AlliancesTab.tsx ~393-406: reword the comments that name the deleted predicate or constant. Its own Sigma-pill pick/combined widths are a different mechanism and stay.
10. Simulation gating. In `routes/event.$eventKey.tsx`, set `isSimulationDisabled = !usesSigmaScore(algorithm)`, importing `usesSigmaScore` from `../../../../packages/harness/sigmaScore.js`. Drop `SIMULATION_AVAILABLE` from the import, and fix the ~line 47-51 and ~201-205 comments that still say "VPR-only" so they say SPR / Sigma algorithms. In `SimulationTab.tsx`, delete the `SIMULATION_AVAILABLE` constant and its doc block, plus the preceding D-04 doc paragraph that describes it (~lines 63-102). In `routes/event.$eventKey.test.tsx`'s Simulation describe, rewrite the three tests:
    (a) The trigger is enabled for spr and disabled for epa and opr, with the artifact fetch pending.
    (b) The enabled spr trigger has no title, aria-label or aria-describedby and textContent "Simulation". For opr, the wrapper span's title is the existing SPR-only sentence.
    (c) `?algorithm=opr&tab=simulation` shows the Insights panel while the URL tab param still reads "simulation". `?algorithm=spr&tab=simulation` shows the Simulation panel.
    Delete the stale "every algorithm" comments there.
11. Band relabel. In `eventMatchAxis.ts`:
    - Remove the two `*ScoreVarianceOwn` fields from `EventMatchRow` and from `toRow`, and delete the post-merge `.map` that copies the Match Band variance into them (with its comment). `mergeEventMatches` returns the merged values sorted.
    - `computeEventAxisDomain` reads `redMatchBandVariance`/`blueMatchBandVariance`.
    - Reword the EventMatchRow doc paragraph that described the removed pair.
    In `EventMatchTable.tsx`, pass `sd` from `row.redMatchBandVariance`/`row.blueMatchBandVariance`, and pass `variance={row.redMatchBandVariance}` (and blue) to the predicted-score lines. Update `EventMatchTable.test.tsx` and `eventMatchAxis.test.ts` row fixtures and case names to use `redMatchBandVariance`/`blueMatchBandVariance` wherever they build an `EventMatchRow`. Leave tests that build ARTIFACT match objects (ElimsTab.test.tsx, team MatchTable.test.tsx, matchAxis.test.ts) alone: the artifact schema still carries the algorithm's own variance field, and those tests prove it is ignored.
12. `routes/team.$teamNumber.tsx`: delete the `TeamSeasonArtifactWithActiveYears` alias and its doc block (~lines 35-49), and read `data.activeYears` directly, since the schema type already has it. Drop the `TeamSeasonArtifact` type import if it becomes unused.
13. `lib/query-client.ts`: replace the stale doc comment with 2-3 accurate lines. Artifacts change at most once per publish or live Worker tick. The 5-minute staleTime keeps browsing cheap. No query in apps/web sets refetchInterval, so live pages refresh only when staleTime lapses (audit F3). Comment only; do NOT add polling.

C. Verify (see below), then commit Task 1 as ONE commit, `refactor(260913-nvn): delete web orphans and Swing/VPR leftovers, give SPR its compare colour`. The pathspec lists every Task-1 path (deleted and modified), plus `pnpm-lock.yaml` only when step 7 cleared it.
  </action>
  <verify>
    <automated>From apps/web: `npx tsc --noEmit -p tsconfig.json; echo "exit=$?"` prints exit=0 with no diagnostics. `npx vitest run` (output saved as vitest-task1.txt) shows zero failing tests that passed at baseline. Deleted-name sweep: `git grep -n -w -E "rpMoments|groupMetricKey|publishesGroupMetrics|officialSnapshotMetrics|formatSeasonList|START_MATCH_PICKER_MAX_H_PX|SimulationRunStatus|CompareCalibrationBin|SIGMA_STEADY_ALLIANCE_EXAMPLE_SIGMAS|EventsSearch|EventSearch|DistrictsSearch|MatchSearch|algorithmPublishesSpread|SIMULATION_AVAILABLE|TeamSeasonArtifactWithActiveYears|InputGroupInput|InputGroupTextarea|DialogFooter|TableCaption" -- src; echo "sweep-exit=$?"` prints sweep-exit=1 (no matches). `git grep -n -E "metric-spread-superscript|compare-algo-vpr|METRIC_COLUMN_WIDTH_SPREADLESS_PX|fontsource-variable/geist" -- src package.json; echo "sweep2-exit=$?"` prints sweep2-exit=1. Region-scoped: `git grep -n "ScoreVarianceOwn" -- src/components/event/eventMatchAxis.ts src/components/event/EventMatchTable.tsx; echo "sweep3-exit=$?"` prints sweep3-exit=1. `ls src/components/ui/input.tsx src/components/ui/textarea.tsx src/components/ui/separator.tsx 2>&1 | grep -c "No such file"` prints 3. `npx vite build` succeeds.</automated>
  </verify>
  <done>One Task-1 commit exists touching only apps/web paths (plus pnpm-lock.yaml when cleared). The typecheck is clean. The vitest failure set is no larger than baseline. All three sweeps are empty. theme.css declares a compare-algo token for opr, epa and spr, pinned by a PUBLISHED_ALGORITHM_IDS loop. The teams table has one wide metric width. Simulation is SPR-gated. Event rows read the Match Band variance directly. `git status --short` after the commit shows none of these paths.</done>
</task>

<task type="auto">
  <name>Task 2: Cheap page-load wins (manifest preconnect/preload + prefetch, logo, _headers, Pine shell ribbon)</name>
  <files>apps/web/vite.config.ts, apps/web/index.html, apps/web/src/main.tsx, apps/web/src/routes/__root.tsx, apps/web/public/_headers, apps/web/public/alfredo-systems.png, apps/web/public/alfredo-systems.webp, apps/web/src/lib/api/manifests.test.ts, apps/web/src/favicon.test.ts</files>
  <action>
1. Manifest hints (audit E1, first half; route loaders and defaultPreload are OUT of scope).
   - In `vite.config.ts`, switch to the function form `defineConfig(({ mode }) => ...)`. Resolve `artifactOrigin = loadEnv(mode, process.cwd(), "VITE_").VITE_ARTIFACT_ORIGIN ?? DEFAULT_ARTIFACT_ORIGIN`, where `DEFAULT_ARTIFACT_ORIGIN` is a module const `"https://data.sigmascout.org"`. Reuse it as the proxy target too. loadEnv also picks up process-env VITE_* values, which is how playwright's webServer.env sets the local origin.
   - Add a tiny inline plugin whose `transformIndexHtml` returns two head-prepend tags: `link rel=preconnect href=<artifactOrigin> crossorigin` and `link rel=preload as=fetch href=<artifactOrigin>/v1/manifest/algorithms.json crossorigin` (crossorigin as the empty-string attribute, which means anonymous and matches a default-credentials cross-origin fetch()).
   - Do NOT import from `src/` into vite.config.ts: `artifactOrigin.ts` reads `import.meta.env` at module top, which is undefined in the config's Node context. Local-origin dev and e2e mode then emit same-origin hints, and a bare `vite dev` emits the production origin, same as today's fetch target, so neither mode breaks.
   - Drift guard: in `src/lib/api/manifests.test.ts`, add one test that reads `../../../vite.config.ts` as text (node:fs, resolved from `import.meta.url`) and expects it to contain `ALGORITHMS_MANIFEST_KEY`'s value.
2. Prefetch. In `src/main.tsx`, import `algorithmsManifestQueryOptions` from `./lib/api/manifests.js` and call `void queryClient.prefetchQuery(algorithmsManifestQueryOptions());` before `createRoot(...)`. These are the same options object `useAlgorithmVersion` passes to useQuery, so the query dedupes on the key `["algorithms-manifest"]`. Do not add loaders, defaultPreload or refetchInterval.
3. Logo (audit E2).
   - `public/alfredo-systems.png` is 1707x608 and displays 32px tall (about 90px wide). Resize it with the sharp copy already in the workspace store (verified loadable offline at plan time). Run a Node one-liner from the repo root that requires `./node_modules/.pnpm/sharp@0.35.2/node_modules/sharp`, resizes to height 64 (2x display, width about 180, aspect kept) and writes `apps/web/public/alfredo-systems.webp` at quality 90 (try `nearLossless: true` if the text edges look soft). Target a few KB, and report the byte size.
   - Do NOT use `convert` on this machine: it resolves to Windows' disk-conversion tool, not ImageMagick.
   - If sharp fails to load, fall back to PowerShell System.Drawing writing a 180x64 PNG named `alfredo-systems.png`, and report the fallback.
   - Delete the old PNG when the WebP replaces it.
   - In `src/routes/__root.tsx`, the footer img becomes `src="/alfredo-systems.webp"` with `width={90} height={32} loading="lazy" decoding="async"`, keeping `className="h-8 w-auto"` and the alt text.
4. `public/_headers` (audit E3, new file) has exactly two lines. Line one is `/assets/*`. Line two is two-space-indented `Cache-Control: public, max-age=31536000, immutable`. Vite's `public/` files land at the dist root, and only hashed build output lives under /assets.
5. index.html (audit E7).
   - Delete the big HTML comment and every CSS comment inside the style block.
   - Make the shell ribbon match the real Pine ribbon (`Ribbon.tsx` header: `bg-[var(--ribbon-bg)]`, ink `--ribbon-ink`, `px-lg py-md` desktop and `px-md` below 768px). Concretely, `.shell-ribbon` gets background `#14532d` (theme.css `--ribbon-bg`) and `.shell-wordmark` gets color `#ffffff` (`--ribbon-ink`). Inside the existing `max-width: 767px` media block, set `.shell-ribbon` horizontal padding to 16px.
   - Drop the now-unused shell custom properties. Keep the wordmark text exactly `SigmaScout` (an e2e spec matches it), the `#root` markup shape, the favicon link, and the module script.
   - Drift guard: in `src/favicon.test.ts`, add one test asserting the index.html text contains theme.css's `--ribbon-bg` value, reusing that file's `extractToken`.
6. Optional one-liner (audit E8). In vite.config.ts's `tanstackRouter({...})` options, add `routeFileIgnorePattern: "\\.test\\.tsx?$"`. Verify it after the build: `grep -c "RouteImport } from" src/routeTree.gen.ts` must still print 17 (the file is gitignored, so check the count, not a diff). If the count changes, revert just this option and report it.
7. Verify, then commit Task 2 as ONE commit, `perf(260913-nvn): preload the algorithms manifest, shrink the footer logo, immutable assets, Pine shell ribbon`. First run `git add -- apps/web/public/_headers apps/web/public/alfredo-systems.webp`. The pathspec includes those two, the deleted PNG, and the modified files.
  </action>
  <verify>
    <automated>From apps/web: `npx vite build` succeeds. `grep -o '<link rel="preconnect"[^>]*>' dist/index.html` and `grep -o '<link rel="preload"[^>]*>' dist/index.html` each print one tag, naming https://data.sigmascout.org with v1/manifest/algorithms.json on the preload. `grep -c "<!--" dist/index.html` prints 0. `grep -c "#14532d" dist/index.html` prints at least 1. `ls dist/_headers dist/alfredo-systems.webp` both exist. `node -e "console.log(require('fs').statSync('public/alfredo-systems.webp').size)"` prints a value under 10000. `grep -c "prefetchQuery(algorithmsManifestQueryOptions())" src/main.tsx` prints 1. `grep -c "RouteImport } from" src/routeTree.gen.ts` prints 17. `npx tsc --noEmit -p tsconfig.json; echo "exit=$?"` prints exit=0. `npx vitest run src/lib/api/manifests.test.ts src/favicon.test.ts` passes, then the full `npx vitest run` (output saved as vitest-task2.txt) shows no new failures versus vitest-task1.txt. Record the main index-*.js size/gzip.</automated>
  </verify>
  <done>One Task-2 commit exists. The built HTML carries the preconnect and preload for the resolved origin, no comments, and a Pine ribbon. main.tsx prefetches the manifest with the shared query options. The logo is a small WebP with width/height/lazy. dist/_headers marks /assets/* immutable. The two drift-guard tests pass. The route tree is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse duplicated tests and merge the match-table twin leaf components</name>
  <files>apps/web/src/test/helpers.ts, apps/web/src/routes/event.$eventKey.test.tsx, apps/web/src/components/event/{AlliancesTab,BreakdownTab,InsightsTab,RankDistributionTable}.test.tsx, apps/web/src/components/teams-table/{TeamsTable.test.tsx,TeamsFilters.test.tsx,teamFilterModel.test.ts}, apps/web/src/components/ribbon/{AlgorithmSelect,Ribbon,YearSelect}.test.tsx, apps/web/src/components/search/SearchBox.test.tsx, apps/web/src/components/team/MatchTable.tsx, apps/web/src/components/event/EventMatchTable.tsx, apps/web/src/components/event/StartMatchPicker.tsx</files>
  <action>
COMMIT 3a (tests). Before editing, record the `it(` count and line count of every test file you touch.

1. Create `src/test/helpers.ts`, with no JSX and no import of `routerHarness.tsx`, which builds a router at import time. It exports:
   - `mockNarrowViewport(): () => void`, byte-identical to the five current copies.
   - `makeQueryClient(): QueryClient` with retry false, identical to the four copies.
   - `makeEventArtifact(teams, overrides = {})`, the schema-parsed builder currently duplicated in BreakdownTab.test.tsx and InsightsTab.test.tsx (spr, 2.0.0+tuned-2026-08, 2024casf, season 2024, empty matches/upcoming).
   - `makeTeamsArtifact(teams: unknown[])`, the TeamsFilters/teamFilterModel builder.
   Replace the local copies:
   - mockNarrowViewport in AlliancesTab, BreakdownTab, InsightsTab, RankDistributionTable and TeamsTable tests
   - makeQueryClient in AlgorithmSelect, Ribbon, YearSelect and SearchBox tests
   - the event builder in BreakdownTab and InsightsTab tests
   - AlliancesTab.test.tsx keeps a one-line local wrapper that only adds `alliances` to overrides when it is defined, so the key stays absent otherwise, exactly as today
   - the teams builder in TeamsFilters and teamFilterModel tests
   Leave ElimsTab/QualsTab (cast builders, different event key and name) and the other artifact-type builders untouched. Remove imports orphaned by the moves.
2. `routes/event.$eventKey.test.tsx`. Collapse the per-tab copies without dropping any assertion.
   - Replace the nine per-describe `originalFetch`/`afterEach` blocks with ONE file-level `afterEach`.
   - Add a local `stubFetch(artifact)` helper. The manifest URL returns `manifestResponse()`. Otherwise it returns a never-resolving promise for `"pending"`, a `new Response(..., { status })` for a number, or the given Response factory.
   - Define `TAB_CASES` for breakdown, insights, quals, alliances and elims. Each case carries `tab`, `triggerName` (Breakdown, Insights, Qualifications, Alliances, Playoffs), `scrollTestId` (`<tab>-table-scroll`) and a resolved-artifact factory. Alliances needs `alliances: [{ allianceNumber: 1, picks: ["frc254"] }]`. Elims needs the existing single qf1m1 played match. The others use `eventArtifactResponse()`.
   - Write `it.each(TAB_CASES)` tests for:
     (i) 404: the "No published results for 2024casf yet" empty state with no button.
     (ii) 500: "Couldn't load event 2024casf for 2024." plus a Retry button.
     (iii) Pending: at least one `[data-slot="skeleton"]` and no progressbar. If a tab's pending state renders its table-scroll testid instead of a skeleton slot, carry that tab's original assertion as a per-case field rather than weakening it.
     (iv) `?tab=<tab>` with the resolved artifact: that panel has hidden=false, and a different registered panel has hidden=true.
     (v) The tab-strip scroll region and the case's table scroll region are DOM siblings in both directions.
     (vi) Starting from `?algorithm=spr&year=2024&tab=<insights if the case is breakdown, else breakdown>` with the resolved artifact, waiting for the trigger to be enabled, then `fireEvent.mouseDown(trigger, { button: 0 })`, the search params read tab, algorithm spr and year 2024.
   - Merge the duplicated "six tabs in order from first paint" tests (Insights describe, Elims describe, the Elims-fifth test and the Breakdown first-paint test) into one test asserting the six names in order with the artifact pending.
   - Keep as-is: the invalid-key test; the DEFAULT_EVENT_TAB and Test 5/6/7 tests; the Insights pending column-header assertion (keep it as its own test); the Insights ranked/unranked banner test; the SPR Breakdown column test; the EPA expansion test; the Alliances disabled-trigger tests (543-633 region); the identity-header tests; and the Simulation tests rewritten in Task 1. Use `stubFetch` inside them where it shortens them.
   - Target roughly 350-450 fewer lines.
3. Run the touched test files (`npx vitest run src/routes/event.$eventKey.test.tsx src/components/event src/components/teams-table src/components/ribbon src/components/search`), then the full suite (output saved as vitest-task3a.txt). Then commit 3a, `test(260913-nvn): shared test helpers and it.each per-tab event route coverage`, running `git add -- apps/web/src/test/helpers.ts` first.

COMMIT 3b (components).

4. In `components/team/MatchTable.tsx`, export these existing module-private leaf components and helpers:
   - `teamNumberLabel`, `AllianceRow`, `AxisHeader`, `AllianceChip`, `PredictedScoreLine`, `ActualScoreLine`, `matchBandSd`
   - a new `CallBadge({ played, coldStart, winnerCorrect })`, extracted from the Call cell's inner conditional (unplayed dash aria-hidden; cold-start dash with aria-label "Not scored (no prior data)"; hit check; miss cross). The cell's `<td>` stays in each row view.
   Type the `compLevel` props so both callers type-check. Both schemas use the same qm/ef/qf/sf/f enum, so `Parameters<typeof isBonusRpCompLevel>[0]` or the existing type works.
5. In `components/event/EventMatchTable.tsx`, delete `rosterNumberLabel`, `EventAllianceRow`, `EventAxisHeader`, `EventAllianceChip`, `EventPredictedScoreLine` and `EventActualScoreLine`, and import the shared ones from `../team/MatchTable.js`. `EventMatchRowView` must:
   - pass `actual={row.played ? row.actualRedScore : undefined}` (and blue), which preserves "dot only when played"
   - pass `sd={matchBandSd(row.redMatchBandVariance)}` (and blue)
   - render `<CallBadge played={row.played} coldStart={row.coldStart === true} winnerCorrect={winnerCorrect} />`. The explicit tie branch goes away, because a tie already makes winnerCorrect false, which renders the same miss badge.
   `MatchRow` in MatchTable.tsx uses `CallBadge` too. The two ROW components and their different Result column, own-team highlight classes and roster-number classes stay separate. All data-testids stay byte-identical.
6. In `components/event/StartMatchPicker.tsx`, replace its local team-number label helper (~line 118) with the shared `teamNumberLabel` import.
7. No test edits should be needed; the existing EventMatchTable, MatchTable, QualsTab, ElimsTab, StartMatchPicker and match-route tests are the proof. If one fails, fix the component, not the assertion, unless the assertion pinned a private name that no longer exists. Run the typecheck and the full suite (output saved as vitest-task3b.txt), then `npx vite build` and record the final main-chunk size/gzip. Commit 3b, `refactor(260913-nvn): EventMatchTable reuses the team MatchTable leaf components`.

FINAL MESSAGE (returned text, no file): list every commit sha and subject. Include `git diff --shortstat <plan-02-start-sha> HEAD -- apps/web` and the same for pnpm-lock.yaml. Include vitest counts at baseline and after each commit, typecheck results, and main index-*.js size/gzip before and after. Include the per-file it() and line counts for the test files touched in 3a. State the lockfile outcome, anything skipped and why, and any failure attributed to the concurrent plan-01. Include the pageArtifacts.ts comment references to the deleted compat test (for plan 04). End with the orchestrator visual-check list from `<verification>` below.
  </action>
  <verify>
    <automated>From apps/web: `npx tsc --noEmit -p tsconfig.json; echo "exit=$?"` prints exit=0. The full `npx vitest run` (output saved as vitest-task3b.txt) shows no test failing that passed at baseline. The twin-removal sweep `git grep -n -E "function (EventAllianceRow|EventAxisHeader|EventAllianceChip|EventPredictedScoreLine|EventActualScoreLine|rosterNumberLabel)" -- src; echo "twin-exit=$?"` prints twin-exit=1. `git grep -l -E "function (mockNarrowViewport|makeQueryClient)\(" -- src` lists exactly one file, src/test/helpers.ts. `grep -c "it.each" src/routes/event.\$eventKey.test.tsx` prints at least 1. `wc -l src/routes/event.\$eventKey.test.tsx` is at least 300 lines below its pre-task count. `npx vite build` succeeds.</automated>
  </verify>
  <done>Two Task-3 commits exist (3a tests, 3b components). The shared helpers live once in src/test/helpers.ts. The event route test uses it.each for the per-tab states with no assertion dropped (every original assertion maps to a retained test or an it.each row). EventMatchTable and StartMatchPicker use the team MatchTable's exported leaf components. Testids and behaviour are unchanged. The typecheck is clean and the suite is no worse than baseline. The summary text, with the visual-check list, is returned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare Pages edge to browser cache | `_headers` sets how long browsers and CDNs keep /assets/* |
| page origin to artifact origin (data.sigmascout.org R2) | cross-origin preload/preconnect and the manifest fetch |
| npm registry to lockfile | dependency removal rewrites pnpm-lock.yaml |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nvn02-01 | Denial of Service | `public/_headers` immutable rule | medium | mitigate | The rule is scoped to `/assets/*` only, where Vite emits content-hashed filenames. `index.html`, `_headers`, the favicon and the logo live at the dist root and keep default revalidation, so a deploy is never masked by a stale immutable HTML entry point. Verified by checking dist/ layout after the build. |
| T-nvn02-02 | Information Disclosure | preload/preconnect `crossorigin` | low | accept | Anonymous CORS matches the existing `fetch()` default (no credentials sent cross-origin). The manifest is public data already fetched on every page. |
| T-nvn02-03 | Tampering | `pnpm install --lockfile-only --offline` | low | mitigate | Only removals are allowed. The lockfile is committed only when `git diff --numstat` shows zero additions and the pre-check proved no foreign edits. Otherwise it is restored and handed to the orchestrator. No new package is installed, so no package-legitimacy checkpoint applies. |
| T-nvn02-04 | Tampering | shared checkout with the concurrent plan-01 executor | medium | mitigate | Explicit-path commits only, a `git status` and `git show --stat HEAD` check after each commit, no edits outside apps/web and pnpm-lock.yaml, and failures outside apps/web reported, not fixed. |
| T-nvn02-05 | Repudiation | Simulation gating change | low | accept | A behaviour change for OPR/EPA readers (the tab is disabled with the existing SPR-only title). It is surfaced on the orchestrator visual-check list rather than asserted as verified. |
</threat_model>

<verification>
Automated (executor): typecheck exit=0 after each task; full vitest suite no worse than baseline after each commit; the Task 1 deleted-name sweeps are empty; the built dist/index.html carries both hints, no comments and the Pine ribbon; dist/_headers exists; the routeTree still has 17 route imports; main-chunk size/gzip is recorded before and after.

Orchestrator visual-check list (the executor must NOT claim these verified; it cannot reach the artifact proxy):
1. Teams table, SPR, desktop, grouped and components views: every non-Total metric column is now 88px wide (was 120). Values and tier boxes must not clip or wrap. Also check EPA unchanged.
2. `/methodology/compare` calibration charts and legends (win and RP sections): the SPR series and swatch now render teal #0D9488, where before the colour was missing.
3. Event page Simulation tab: enabled and working under SPR. Under OPR and EPA the trigger is disabled with the "Simulation is only available on SPR" tooltip, and `?tab=simulation&algorithm=opr` lands on Insights. Note that live OPR/EPA artifacts may still carry RP pmfs until the owed republish; the gate is intentional regardless.
4. Quals, Playoffs and `/match/$matchKey` tables plus the team page match table: bands, ticks, dots, the rounded ` ± N` suffix, bonus-RP dots and call badges look identical to before. Unplayed event rows show no dot. A tied played match shows the miss badge.
5. Cold load with throttled network: the static shell ribbon paints Pine green with a white wordmark (no light-gray flash), and its height and padding match the mounted Ribbon on desktop and on a 390px phone.
6. Footer sponsor logo: crisp at 1x and 2x/3x DPR, same 32px height, no layout shift.
7. Production build in DevTools Network: exactly one `v1/manifest/algorithms.json` request (the preload is consumed, with no "preloaded but not used" console warning), started before the main JS finishes. With `VITE_ARTIFACT_ORIGIN` set to the local origin, the hints point at the local origin and dev still loads.
8. After the next deploy only: `/assets/*` responses carry `Cache-Control: public, max-age=31536000, immutable`, and `/` and `/index.html` do not.
</verification>

<success_criteria>
- Task 1, Task 2 and Task 3 (3a + 3b) commits exist, each containing only apps/web paths (plus pnpm-lock.yaml when cleared), with the prescribed subjects and the Co-Authored-By trailer.
- apps/web net line count drops substantially (expected roughly 1,200-1,500 lines across TS/TSX/CSS/HTML plus a 14 KB fixture and an 81 KB PNG), reported as a `git diff --shortstat` from the plan-02 start sha.
- Typecheck is clean, and the vitest failure set is no larger than baseline.
- Audit bug F2 is fixed and pinned by a PUBLISHED_ALGORITHM_IDS-driven test.
- The final message returns commits, shortstat, test and bundle numbers, the lockfile outcome, skipped items, plan-04 follow-ups and the 8-item visual-check list. No SUMMARY.md is written, and STATE.md is untouched.
</success_criteria>

<output>
Do NOT create a SUMMARY.md (subagent write block). Return the summary text described at the end of Task 3 in the final message. The orchestrator writes `.planning/quick/260913-nvn-simplify-codebase-audit/260913-nvn-02-SUMMARY.md`.
</output>
