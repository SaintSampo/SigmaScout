---
quick_id: 260906-8kd
phase: quick-260906-8kd
slug: registry-proof-the-breakdown-season-list
date: 2026-09-06
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: true
depends_on: []
requirements:
  - TRAP-1
  - TRAP-2
  - EXPORT
  - PIN
  - BEHAVIOUR-PRESERVED
  - TODO-CORRECTION
files_modified:
  - packages/core/algorithms/breakdown/index.ts
  - packages/core/algorithms/breakdown/groups.test.ts
  - packages/core/algorithms/breakdown/reconciliation.test.ts
  - .planning/todos/pending/extend-corpus-2018-2017-2016.md

estimate:
  tokens: 28000
  raw_tokens: 56000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Registering a new season in SEASON_COMPONENT_MAPS (packages/core/algorithms/breakdown/index.ts) automatically extends BOTH groups.test.ts and reconciliation.test.ts to exercise it, with no second edit to either file's iteration source."
    - "Registering a new season without a conscious acknowledgement still fails LOUDLY: exactly one pinned equality assertion (in groups.test.ts) compares the registry tuple to an explicit literal, mirroring the single pin rules.test.ts provides for all four RP_REGISTERED_SEASONS consumers."
    - "The set of seasons actually exercised is IDENTICAL before and after -- 2019, 2020, 2022, 2023, 2024, 2025, 2026 -- proven by a machine diff of vitest's per-test fullName list captured before and after the edits, not by inspection."
    - "A reader of .planning/todos/pending/extend-corpus-2018-2017-2016.md learns that the trap was TWO files (not three), that both are now registry-driven and therefore closed, and that pointModel.test.ts was never a defect but the already-correct exemplar -- recorded as a dated correction, not a silent rewrite."
    - "No new season is registered anywhere, no corpus file is touched, and no TBA call is made."
  artifacts:
    - "packages/core/algorithms/breakdown/index.ts -- exports BREAKDOWN_REGISTERED_SEASONS"
    - "packages/core/algorithms/breakdown/groups.test.ts -- registry-driven iteration plus the single loud pin"
    - "packages/core/algorithms/breakdown/reconciliation.test.ts -- registry-driven describe.each and it.each"
    - ".planning/todos/pending/extend-corpus-2018-2017-2016.md -- corrected THE TRAP section"
  key_links:
    - "SEASON_COMPONENT_MAPS (module-private) -> BREAKDOWN_REGISTERED_SEASONS (exported) -> both breakdown test files' iteration"
    - "groups.test.ts's pinned equality -> the one conscious edit a season registration still demands"
---

<objective>
Close the silent trap in the score-breakdown test suite before the corpus is extended back to
2018/2017/2016.

Two test files iterate a HARDCODED season list, so registering a new season in
`breakdown/index.ts`'s `SEASON_COMPONENT_MAPS` today ships an entirely unproven component map
with a fully green suite. The fix is to export a registered-seasons tuple from the breakdown
dispatch table and point both tests at it, mirroring exactly what the RP dispatch table one
directory over already does.

Purpose: pay a one-time cost that removes the same silent failure mode from all three remaining
corpus-extension passes (2018, then 2017, then 2016). The todo file itself names this as
investment #1 to make up front.

Output: one new production export, two registry-driven test files, one corrected todo. No new
season is registered, no behaviour changes, no data is touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/extend-corpus-2018-2017-2016.md

THE PATTERN TO COPY -- read both, they are the exemplar this task mirrors:
@packages/core/algorithms/sigma1/rp/rules.ts
@packages/core/algorithms/sigma1/rp/rules.test.ts

THE FILES TO CHANGE:
@packages/core/algorithms/breakdown/index.ts
@packages/core/algorithms/breakdown/groups.test.ts
@packages/core/algorithms/breakdown/reconciliation.test.ts

OUT OF SCOPE -- read only if you need to confirm the exemplar claim, DO NOT EDIT:
@packages/core/districts/pointModel.test.ts
</context>

<verified_facts>
Verified against live source on 2026-09-06. Do not re-derive these; DO re-confirm the line
numbers with `grep -n` before each edit, because another session is committing to this same
checkout and line numbers may have drifted.

**The real trap is TWO files, not three.** The distinction that matters is *iteration list*
(silent) vs *equality assertion* (loud):

1. `packages/core/algorithms/breakdown/groups.test.ts:11` -- a module-level hardcoded season
   array, consumed by `for (const season of ...)` at lines 14, 35 and 50. SILENT TRAP: a season
   registered in `SEASON_COMPONENT_MAPS` but absent from that literal is simply never tested and
   the suite stays green.
2. `packages/core/algorithms/breakdown/reconciliation.test.ts:32` -- a module-level hardcoded
   season tuple, consumed by `describe.each(...)` at line 80 and `it.each(...)` at line 249.
   SILENT TRAP, same mechanism.
3. `packages/core/districts/pointModel.test.ts` -- NOT A TRAP. Line 21 already iterates the
   registry and line 37 is a deliberate loud pin that fails the instant a season is registered.
   It is the target shape, not a defect. Out of scope for this task.

**One pin, many consumers -- the precedent is measured, not assumed.** `RP_REGISTERED_SEASONS`
has four consuming files (`rules.test.ts`, `sigma1/rp/reconciliation.test.ts`,
`distribution.test.ts`, `rpConservativeBranch.ts`) and exactly ONE pinned equality assertion, at
`rules.test.ts:23-26`. That pin lives in the pure unit-test file -- the one whose own header
says "no corpus access". So the breakdown pin belongs in `groups.test.ts` (pure unit test, no
corpus) and NOT in `reconciliation.test.ts` (corpus-gated, skips wholesale when
`data/corpus.sqlite` is absent). Two pins would mean two edits per future season registration;
that is the duplication the RP precedent already declined.

**Non-vacuity is already handled -- do not add a second guard.** `reconciliation.test.ts:89`
already asserts `expect(rows.length).toBeGreaterThanOrEqual(SAMPLE_SIZE)`. Once the iteration is
registry-driven, registering a season whose data has NOT been ingested fails loudly on that
floor rather than passing vacuously. That is the desired behaviour and it needs no new code. Do
not "helpfully" add an emptiness check.

**Corpus is present** (`data/corpus.sqlite`, 468 MB), so `reconciliation.test.ts` really runs
rather than skipping -- its 7 seasons x 2 sides x 2000 matches take a couple of minutes. Budget
for that; do not read a long run as a hang.

**The checkout is live.** At planning time the working tree carried another session's in-flight
edits to `packages/core/algorithms/sigma1/index.ts` and `kalman.ts`, and HEAD moved during the
planning session itself. Every commit below stages by explicit path AND asserts the staged set,
because a plain `git add <paths>` still commits whatever the other session left staged. This
exact failure occurred on the immediately preceding quick task (260906-813).
</verified_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Export the registry, drive groups.test.ts from it, prove the season set is unchanged</name>
  <files>packages/core/algorithms/breakdown/index.ts, packages/core/algorithms/breakdown/groups.test.ts</files>
  <precondition>`data/corpus.sqlite` exists (check with `ls -la data/corpus.sqlite`). If absent, reconciliation.test.ts skips wholesale and the Task 2 baseline diff proves nothing -- halt and report rather than proceeding on a vacuous baseline.</precondition>

  <action>
This is the tracer: one production export wired through to one test consumer and proven
end-to-end by a real run, before Task 2 expands it to the second consumer.

STEP A -- capture the baseline BEFORE touching any file. Run from the REPO ROOT
(`C:/Users/Jacob/Documents/GitHub/SigmaScout`), never from `apps/web`:

    WORK="C:/Users/Jacob/AppData/Local/Temp/gsd-8kd"
    mkdir -p "$WORK"
    npx vitest run packages/core/algorithms/breakdown/groups.test.ts packages/core/algorithms/breakdown/reconciliation.test.ts --reporter=json --outputFile="$WORK/before.json"
    node -e "const fs=require('fs');const r=JSON.parse(fs.readFileSync(process.argv[1],'utf8'));console.log(r.testResults.flatMap(f=>f.assertionResults.map(a=>a.fullName)).sort().join('\n'))" "$WORK/before.json" > "$WORK/before.txt"
    wc -l "$WORK/before.txt"
    npx tsc --noEmit 2>&1 | sort > "$WORK/tsc-before.txt"
    wc -l "$WORK/tsc-before.txt"

Use `npx vitest`, never a `pnpm` wrapper under `timeout` -- the wrapper swallows output and
exits 0. The WORK path is a `C:/`-style absolute path deliberately: both bash and node.exe
accept it unmangled, and it lives outside the repo so nothing lands in git.

The tsc baseline is captured as a SET, not a count-against-zero, because the other session's
in-flight sigma1 edits may already be red and four apps/worker errors are known pre-existing.

Confirm the baseline is real: `before.txt` must be non-empty and
`grep -c "season 2019" "$WORK/before.txt"` must return a positive number. If reconciliation
skipped, stop -- see the precondition.

STEP B -- `packages/core/algorithms/breakdown/index.ts`. Immediately after the
`SEASON_COMPONENT_MAPS` const (currently ending at line 55; re-confirm with `grep -n`), add an
exported `BREAKDOWN_REGISTERED_SEASONS` derived the same way `rules.ts:60-62` derives its own:
`Object.keys(SEASON_COMPONENT_MAPS).map(Number).sort((a, b) => a - b) as readonly number[]`.
Keep `SEASON_COMPONENT_MAPS` itself module-private -- only the seasons tuple becomes public.

Doc-comment it in the register of `rules.ts:54-59`: state that it is the sorted, readonly tuple
of every registered season and the single source of both `groups.test.ts`'s and
`reconciliation.test.ts`'s iteration, so registering a new season automatically extends both
suites without a second edit. Add one sentence `rules.ts` does not have: that this export exists
specifically to close a failure mode in which a season registered above but missing from a
test-local list shipped an unproven component map with a green suite. Name the one conscious
edit that remains -- the pinned equality in `groups.test.ts`.

The `readonly number[]` typing is deliberate and already proven to compile against vitest's
`describe.each`/`it.each` overloads: `rules.test.ts:29` resolves against exactly that type.

STEP C -- `packages/core/algorithms/breakdown/groups.test.ts`. Import
`BREAKDOWN_REGISTERED_SEASONS` from `./index.js` -- that import statement already exists at line
2 for `componentMapForSeason`, so extend it rather than adding a second one. Remove the
module-level hardcoded season array at line 11 and repoint all three iteration sites (lines 14,
35, 50) at the imported tuple.

Nothing else in the file changes. Every assertion body, failure message and `it()` title stays
byte-identical, so the only test-name delta in Step D is the pin you are about to add.

Add the single loud pin, mirroring `rules.test.ts:23-26` in shape and tone: a top-level
`describe("BREAKDOWN_REGISTERED_SEASONS")` whose one `it` asserts `toEqual` against the explicit
literal `[2019, 2020, 2022, 2023, 2024, 2025, 2026]`, with a title stating the tuple is sorted
and that 2021 is absent because no standard FRC season was played that year. Place it above the
existing describe blocks. Do NOT add a second pin in `reconciliation.test.ts` -- see the
one-pin-many-consumers fact above.

STEP D -- prove behaviour is preserved. Re-run the identical vitest command with `after.json`,
build `after.txt` the same way, then `diff "$WORK/before.txt" "$WORK/after.txt"`.

The ONLY acceptable diff is added lines belonging to the new pin describe block. ZERO removed
lines. Every season-labelled name -- the 2019/2020/2022..2026 grouping test names and the
`season NNNN component map reconciliation` names -- must appear identically on both sides. If
any season-labelled line is removed or altered, the change is NOT behaviour-preserving: stop and
report rather than committing.

Then `npx tsc --noEmit 2>&1 | sort > "$WORK/tsc-after.txt"` and
`diff "$WORK/tsc-before.txt" "$WORK/tsc-after.txt"`. It must be empty -- no NEW errors. A
pre-existing red set carried in from the other session is expected and fine; a new line is not.

STEP E -- commit, staging by explicit path AND asserting the staged set:

    git add -- packages/core/algorithms/breakdown/index.ts packages/core/algorithms/breakdown/groups.test.ts
    STAGED=$(git diff --cached --name-only | sort)
    EXPECTED=$(printf '%s\n' packages/core/algorithms/breakdown/index.ts packages/core/algorithms/breakdown/groups.test.ts | sort)
    [ "$STAGED" = "$EXPECTED" ] && echo STAGING_OK || { echo STAGING_MISMATCH; git diff --cached --name-only; }

If the sets differ, the other session staged something: `git restore --staged --` each foreign
path and re-assert until they match. Only on STAGING_OK run
`git commit -m "refactor(quick-260906-8kd): derive the breakdown season list from the registry"`.
Never commit on a mismatch.
  </action>

  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && [ "$(grep -c 'BREAKDOWN_REGISTERED_SEASONS' packages/core/algorithms/breakdown/index.ts)" -ge 1 ] && [ "$(grep -vE '^\s*(\*|//|/\*)' packages/core/algorithms/breakdown/groups.test.ts | grep -c 'const SEASONS')" = "0" ] && [ "$(grep -vE '^\s*(\*|//|/\*)' packages/core/algorithms/breakdown/groups.test.ts | grep -c 'for (const season of BREAKDOWN_REGISTERED_SEASONS)')" = "3" ] && npx vitest run packages/core/algorithms/breakdown/groups.test.ts</automated>
  </verify>

  <done>
`BREAKDOWN_REGISTERED_SEASONS` is exported from `breakdown/index.ts` with a doc comment naming
the failure mode it closes. `groups.test.ts` has no module-level season array; all three
iteration sites read the imported tuple; one pinned `toEqual` asserts the literal
`[2019, 2020, 2022, 2023, 2024, 2025, 2026]`. The before/after test-name diff shows only the
pin's added lines and zero removed lines, and the tsc error-set diff is empty. Committed with a
verified staged set of exactly the two intended paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drive reconciliation.test.ts's describe.each and it.each from the same registry</name>
  <files>packages/core/algorithms/breakdown/reconciliation.test.ts</files>
  <precondition>Task 1 committed, and `$WORK/after.txt` from Task 1 Step D still exists — it is this task's baseline. If it is missing, regenerate it by re-running Task 1 Step A's command pair against the current (post-Task-1) tree before editing anything here.</precondition>

  <action>
Expansion from Task 1's proven slice: the same export now feeds the second, corpus-backed
consumer. No new production code.

STEP A — re-confirm the three sites with `grep -n`, since another session may have shifted them:
the module-level hardcoded season tuple (was line 32), the `describe.each(...)` (was line 80),
and the `it.each(...)` in the prototype-poisoning block (was line 249).

STEP B — extend the existing `import { componentMapForSeason, FOULS_COMMITTED_COMPONENT,
parseBreakdown } from "./index.js";` (was line 47) to also pull in
`BREAKDOWN_REGISTERED_SEASONS`. Do not add a second import statement from the same module.

STEP C — remove the module-level hardcoded season tuple and repoint both `.each` call sites at
the imported tuple.

Preserve the provenance the removed declaration's doc comment carries — which plan or quick task
registered each season (02-01 for 2024, then 2022/2023 via its Task 1, 2025/2026 via its Task 2,
2020 and 2019 via quick task 260903-4fs) and that 2021 is deliberately absent because no
standard FRC season was played. Re-anchor it as a comment at the import site. Two edits to it:
drop the "all seven seasons" count phrasing, which goes stale the moment 2018 lands, and state
that the list is now derived from the dispatch table rather than restated here.

Do NOT add a pinned equality assertion in this file. The single pin lives in `groups.test.ts` —
see the one-pin-many-consumers fact in `<verified_facts>`.

Do NOT add an emptiness or non-vacuity guard. The existing
`expect(rows.length).toBeGreaterThanOrEqual(SAMPLE_SIZE)` floor (was line 89) already turns a
registered-but-not-ingested season into a loud failure, which is exactly the desired behaviour;
a second guard would be redundant machinery. This paragraph exists so a later reader does not
"helpfully" add one.

Typing note, so nothing surprises you: the tuple arrives as `readonly number[]` rather than a
`const` literal union, so the `.each` callback parameter widens from a season literal union to
`number`. Every consumer in this file already takes `number` — `sampleBreakdowns`,
`componentMapForSeason` — and the `year === 2026` ternary inside an `it.each` title still
compiles against `number`. `rules.test.ts:29` and `sigma1/rp/reconciliation.test.ts:211` both
already resolve `describe.each` against exactly this type.

STEP D — prove behaviour is preserved. Task 1's `after.txt` is the baseline. Re-run the same
two-file vitest command writing `after2.json`, build `after2.txt` with the identical node
one-liner, then `diff "$WORK/after.txt" "$WORK/after2.txt"`.

That diff must be EMPTY — this task adds and removes no test, so every one of the seven
`season NNNN component map reconciliation` describe names, both per-side reconciliation names
per season, and the seven prototype-poisoning `it.each` names must be byte-identical. Any
non-empty diff means the season set changed: stop and report rather than committing.

Then `npx tsc --noEmit 2>&1 | sort > "$WORK/tsc-after2.txt"` and
`diff "$WORK/tsc-before.txt" "$WORK/tsc-after2.txt"` — still no NEW error lines.

STEP E — commit with the same staged-set assertion, this time for a single path:

    git add -- packages/core/algorithms/breakdown/reconciliation.test.ts
    STAGED=$(git diff --cached --name-only)
    [ "$STAGED" = "packages/core/algorithms/breakdown/reconciliation.test.ts" ] && echo STAGING_OK || { echo STAGING_MISMATCH; echo "$STAGED"; }

Unstage any foreign path with `git restore --staged --` and re-assert. Only on STAGING_OK run
`git commit -m "refactor(quick-260906-8kd): drive reconciliation.test.ts from the breakdown registry"`.
  </action>

  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && [ "$(grep -vE '^\s*(\*|//|/\*)' packages/core/algorithms/breakdown/reconciliation.test.ts | grep -c 'const REGISTERED_SEASONS')" = "0" ] && [ "$(grep -c 'each(BREAKDOWN_REGISTERED_SEASONS)' packages/core/algorithms/breakdown/reconciliation.test.ts)" = "2" ] && npx vitest run packages/core/algorithms/breakdown/reconciliation.test.ts packages/core/algorithms/breakdown/groups.test.ts</automated>
  </verify>

  <done>
`reconciliation.test.ts` declares no module-level season list; both `describe.each` and
`it.each` read `BREAKDOWN_REGISTERED_SEASONS`; the provenance comment survives at the import
site without a stale count. The `after.txt` vs `after2.txt` test-name diff is empty and the tsc
error-set diff is empty. Committed with a verified staged set of exactly one path. No pin and no
emptiness guard were added to this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct THE TRAP in the corpus-extension todo — two files, both now closed</name>
  <files>.planning/todos/pending/extend-corpus-2018-2017-2016.md</files>

  <action>
The todo currently asserts something this task has just disproved, and it is the document the
2018 job will be executed from. Leaving it wrong is the expensive part.

Follow the correction habit the predecessor file already established — a visible, DATED
correction rather than a silent rewrite. The predecessor's own phrasing to mirror in register:
"CORRECTED 2026-09-03 — the flags DO get recomputed; the earlier instruction here was wrong."

EDIT 1 — the "THE TRAP" section. Retitle it to say TWO hardcoded test season lists, both now
closed. Open the section with a dated correction naming this quick task (260906-8kd) and stating
plainly that the section previously claimed three files, that `pointModel.test.ts` was one of
them, and that that claim was wrong.

Rewrite the table to the two real traps — `breakdown/groups.test.ts` and
`breakdown/reconciliation.test.ts` — and mark both CLOSED, naming
`BREAKDOWN_REGISTERED_SEASONS` (exported from `breakdown/index.ts`) as what now drives them.

Then recast `packages/core/districts/pointModel.test.ts` in prose, out of the table: it was never
a defect. It already iterates `DISTRICT_REGISTERED_SEASONS` and its equality assertion is a
deliberate LOUD pin that fails the instant a season is registered. It is the exemplar the two
breakdown files were just brought up to.

Also fix the section's closing prose, which is wrong in the same way — it claims only
`rules.test.ts` is self-correcting "and the other three are not". Replace it with the real
distinction this task established: an *iteration list* fails silently, an *equality assertion*
fails loudly, and a registry-driven suite needs exactly one of the latter.

EDIT 2 — add, in that same section, the list a 2018 executor actually needs: **which pins will
fail, by design, when 2018 is registered.** There are three, one per registry family, and each
is a one-line literal edit rather than a bug:

- `packages/core/algorithms/breakdown/groups.test.ts` — the new `BREAKDOWN_REGISTERED_SEASONS`
  pin (added by this task)
- `packages/core/algorithms/sigma1/rp/rules.test.ts` — the existing `RP_REGISTERED_SEASONS` pin
- `packages/core/districts/pointModel.test.ts` — the existing `DISTRICT_REGISTERED_SEASONS` pin

State the payoff explicitly: three loud, self-announcing failures replace what used to be two
silent green passes.

EDIT 3 — the "Then 2017, then 2016" section, investment #1 ("Make the three hardcoded test
season lists registry-driven"). Mark it DONE, dated 2026-09-06, done by this quick task, and
correct its count from three to two. Leave investment #2 (generalizing the recon scripts)
untouched — it is still open and is not this task's job.

EDIT 4 — the "nine registries" table, row 1 (score components). Append that adding the
`SEASON_COMPONENT_MAPS` entry now automatically extends both breakdown test suites, with the
`groups.test.ts` pin as the single remaining conscious edit. Re-confirm the line number cited in
that row with `grep -n` — Task 1 inserted an export into that file and anything cited below the
insertion point has shifted. Fix any other line-number citation in the table that this task
moved; leave citations into files this task did not touch alone.

Do not change any measured number, any 2018 finding, the additivity analysis, the RP threshold
options, or the district-key notes. This edit is scoped to the trap claim and its consequences.

COMMIT with the same staged-set assertion:

    git add -- .planning/todos/pending/extend-corpus-2018-2017-2016.md
    STAGED=$(git diff --cached --name-only)
    [ "$STAGED" = ".planning/todos/pending/extend-corpus-2018-2017-2016.md" ] && echo STAGING_OK || { echo STAGING_MISMATCH; echo "$STAGED"; }

Only on STAGING_OK run
`git commit -m "docs(quick-260906-8kd): correct THE TRAP — two files, both now registry-driven"`.
  </action>

  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && f=.planning/todos/pending/extend-corpus-2018-2017-2016.md && grep -q 'CORRECTED 2026-09-06' "$f" && grep -q 'BREAKDOWN_REGISTERED_SEASONS' "$f" && grep -q 'RP_REGISTERED_SEASONS' "$f" && grep -q 'DISTRICT_REGISTERED_SEASONS' "$f" && git diff --quiet HEAD -- packages/core/districts/pointModel.test.ts && echo TODO_CORRECTION_OK</automated>
  </verify>

  <done>
The todo's trap section is retitled to two files, opens with a dated 2026-09-06 correction
naming quick task 260906-8kd, marks both breakdown files CLOSED against
`BREAKDOWN_REGISTERED_SEASONS`, and recasts `pointModel.test.ts` in prose as the exemplar rather
than a defect. The three pins a 2018 registration will trip are listed by path. Investment #1 in
"Then 2017, then 2016" is marked DONE and its count corrected. Registry table row 1 carries the
new automatic-extension note with a re-confirmed line number. No measured 2018 finding was
altered, and `pointModel.test.ts` is untouched in git.
  </done>
</task>

</tasks>

<verification>
Run from the REPO ROOT, never from `apps/web` — root vitest sees ~200 test files, `apps/web`
sees ~77, and an 8-day red suite has hidden in that gap before.

1. **The behaviour-preservation proof (the load-bearing one).**
   `diff "$WORK/before.txt" "$WORK/after2.txt"` shows ONLY the added lines of the
   `BREAKDOWN_REGISTERED_SEASONS` pin describe block from Task 1. Zero removed lines. Every
   season-labelled test name present before is present after, unchanged. This is what proves the
   same season set — 2019, 2020, 2022, 2023, 2024, 2025, 2026 — runs before and after.

2. **The two suites are green:**
   `npx vitest run packages/core/algorithms/breakdown/groups.test.ts packages/core/algorithms/breakdown/reconciliation.test.ts`

3. **No new type errors:** `diff "$WORK/tsc-before.txt" "$WORK/tsc-after2.txt"` is empty. Judge
   by set difference, never against zero — the four `apps/worker` errors are known pre-existing
   and the concurrent session's `sigma1/` edits may add more that are not yours.

4. **Scope was honoured:** `git diff --stat HEAD~3..HEAD` touches exactly the four
   `files_modified` paths and nothing else. `packages/core/districts/pointModel.test.ts` does not
   appear. No file under `data/` appears. No new season literal (2018, 2017, 2016) was added to
   any dispatch table.

5. **The registries themselves are unchanged:** `git diff HEAD~3..HEAD -- packages/core/algorithms/breakdown/index.ts`
   shows only the added export and its doc comment — `SEASON_COMPONENT_MAPS`'s entries are
   untouched.
</verification>

<success_criteria>
- `BREAKDOWN_REGISTERED_SEASONS` is exported from `packages/core/algorithms/breakdown/index.ts`,
  derived from `SEASON_COMPONENT_MAPS` by the same expression `rules.ts` uses, and doc-commented
  with the failure mode it closes.
- Neither breakdown test file declares a module-level season list; all five consumption sites
  (three `for...of` in `groups.test.ts`, one `describe.each` and one `it.each` in
  `reconciliation.test.ts`) read the imported tuple.
- Exactly one pinned equality assertion exists for this registry, in `groups.test.ts`.
- The before/after test-name diff proves an identical season set, and the tsc error-set diff is
  empty.
- The todo's trap section is corrected with a visible dated entry, names two traps rather than
  three, and recasts `pointModel.test.ts` as the exemplar.
- Three commits, each with a verified staged file set. `packages/core/districts/pointModel.test.ts`,
  `data/corpus.sqlite`, and every `sigma1/` file the concurrent session is editing are untouched.
</success_criteria>

<output>
Create `.planning/quick/260906-8kd-registry-proof-the-breakdown-season-list/260906-8kd-SUMMARY.md`.

Known environment constraint: the `Write` tool is blocked for subagents on `SUMMARY.md`. Do NOT
route around it with Bash. Return the summary body as your final message text and let the
orchestrator write the file.

The summary must record, specifically: the export name and where it lives; that the trap was two
files and not three, with `pointModel.test.ts` named as the exemplar that was wrongly accused;
the exact before/after test-name diff result that proves the season set is unchanged; that
exactly one pin exists and where; and that no season was registered and no corpus touched.
</output>

