---
phase: quick-260904-4aa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/harness/statbotics.ts
  - packages/harness/statbotics.test.ts
  - packages/harness/epaStatboticsCompare.ts
  - packages/harness/epaStatboticsCompare.test.ts
  - scripts/epaVsStatbotics.ts
  - data/baselines/epa-vs-statbotics-2026-09.json
  - docs/models/epa-vs-statbotics.md
  - docs/models/epa-divergences.md
  - packages/core/algorithms/epa.ts
  - package.json
autonomous: true
requirements: [SC-2]

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One command measures SigmaScout's per-team season-final EPA against Statbotics' published per-team EPA for every season 2022-2026, and prints per-season agreement statistics."
    - "Both sides of the comparison mean the same quantity: our foulsCommitted component is excluded, because Statbotics' epa.total_points is a no-foul number."
    - "Demo team keys never enter the join, on either side."
    - "A documented tolerance band exists and the script exits non-zero when a measured statistic falls outside it."
    - "The year-level Statbotics reference row reports Statbotics' actually-published accuracy, not the unverified 0.70/0.71 estimate."
    - "SC-2 is recorded as measured-and-closed with its evidence, replacing the blocked-on-external-dependency record."
  artifacts:
    - scripts/epaVsStatbotics.ts
    - packages/harness/epaStatboticsCompare.ts
    - packages/harness/epaStatboticsCompare.test.ts
    - data/baselines/epa-vs-statbotics-2026-09.json
    - docs/models/epa-vs-statbotics.md
  key_links:
    - "Join key: Statbotics numeric `team` -> corpus `frc{team}` team_key."
    - "Comparable quantity: our `total` metric MINUS our `foulsCommitted` component <-> Statbotics `epa.total_points`."
    - "Live v3 year shape `metrics.win_prob.season.acc` <-> `StatboticsYearResponseSchema` (currently parses a field the API no longer returns)."
    - "Season-final state: `WalkForwardSimulator.runAll(...).finalStates` -> `epa.teamMetrics(state)`."
---

<objective>
Close SC-2 — "EPA runs walk-forward at any point in a season, and spot-checked teams land within a documented tolerance of published Statbotics numbers" — which has been recorded blocked-on-external-dependency since 2026-08-13 because `api.statbotics.io` returned HTTP 500.

That blocker is gone. Verified live 2026-09-04: `/v3/year/{2022..2026}`, `/v3/team_year/254/2024`, and `/v3/team_years?year=2025&limit=1000&offset=N` all return HTTP 200, and 2025 paginates to 3,690 team rows.

Purpose: replace an ad-hoc, un-re-runnable measurement (epa.ts's header cites "OLS slope vs Statbotics 0.489 -> 0.841" from quick task 260901-is2, produced by a script that was never committed) with a committed, re-runnable comparison plus a documented tolerance — and correct the year-level reference row, which is silently wrong.

Output: a comparison script, a pure tested statistics module, a committed tolerance baseline, and `docs/models/epa-vs-statbotics.md` recording the verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/STATE.md

@docs/models/epa-divergences.md
@packages/harness/statbotics.ts
@packages/core/algorithms/epa.ts
@scripts/measureRewindGap.ts
@packages/core/algorithms/demoTeams.ts
</context>

<discovered_facts>
Established live during planning (2026-09-04) — do not re-derive, but DO re-confirm the API is up before Task 1's fetch work:

**Statbotics v3 shapes**
- `/v3/team_years?year={y}&limit={<=1000}&offset={n}` returns a JSON array. 2025 -> 3,690 rows (1000+1000+1000+690). Row fields: `team` (number), `year`, `name`, `country`, `state`, `district`, `rookie_year`, `epa`, `record`.
- `row.epa` fields: `total_points`, `unitless`, `norm`, `breakdown`, `stats`, `ranks`.
- `row.epa.total_points` is a NO-FOUL number: for `frc254`/2024 it is 51.71, and `auto_points 15.94 + teleop_points 29.48 + endgame_points 6.28 = 51.70`. The year endpoint corroborates by exposing `breakdown.total_points_mean`, `breakdown.foul_mean` and `breakdown.no_foul_mean` as three separate figures.
- `row.record` = `{wins, losses, ties, count, winrate}`. Every 2025 row sampled had `count > 0`.
- `/v3/year/{y}` top-level keys are `year, score_mean, score_sd, percentiles, breakdown, metrics`. There is NO `epa_acc` key. Winner-prediction accuracy now lives at `metrics.win_prob.season.acc`, alongside `metrics.win_prob.season.{count,conf,mse}`.

**Measured live year-level figures** (use these to sanity-check Task 3's fetch; they are not to be hardcoded as the shipped values — the shipped values come from the live fetch that task performs):
| season | `metrics.win_prob.season.acc` | `metrics.win_prob.season.mse` |
|--------|-------------------------------|-------------------------------|
| 2022 | 0.7815 | 0.1502 |
| 2024 | 0.7627 | 0.1620 |
| 2025 | 0.7839 | 0.1537 |
| 2026 | 0.7978 | 0.1483 |

`STATBOTICS_REFERENCE_FALLBACK` currently claims 0.70/0.70/0.71/0.71/0.71. The real figures are 6-9 points higher, so every report that rendered the fallback understated the target SigmaScout is trying to beat.

**SigmaScout shapes**
- Corpus team keys are `frc{number}` (`frc1`, `frc2783`). Corpus covers 2019, 2020, 2022, 2023, 2024, 2025, 2026 (no 2021).
- `TOTAL_METRIC_KEY` is `"total"`; `FOULS_COMMITTED_COMPONENT` is `"foulsCommitted"` (`packages/core/algorithms/breakdown/constants.ts`).
- `epa.teamMetrics()` sums EVERY component into `total`, including `foulsCommitted`. `foulsCommitted` holds points a team's own fouls GAVE AWAY to opponents, so it is not part of that team's own scoring contribution.
- Demo keys: `frc9970`-`frc9999` (`isDemoTeamKey`) plus the pseudo-key `demo-pseudo-unregistered` (`DEMO_PSEUDO_TEAM_KEY`).
- `WalkForwardSimulator.runAll(algorithms, teams, initialStates, onMatchComplete)` returns records carrying a `.finalStates` `ReadonlyMap<algorithmId, unknown>`.
- `seasonBoundaryFor(seasons, index, coldStartSeason?)` — cold start is POSITIONAL (index 0) when the third argument is omitted.
- Production replay window is 2022-2026 with `includeOffseason: true` (what `publish:seasons` uses).

**Environment**
- This work needs NO credentials. Statbotics is unauthenticated; the corpus is a local read-only SQLite file. Run scripts with plain `npx tsx`, NOT `tsx --env-file=.env`. Do not read, print, or copy `.env` — see CLAUDE.md "Secrets handling".
- `reports/` is gitignored (correct home for fetch caches and generated reports). `data/baselines/` IS tracked (correct home for the committed tolerance baseline).
- Scoped test runs: use `npx vitest run <path>` and judge by printed output. Do NOT wrap `pnpm` in `timeout` — that swallows output and exits 0 regardless.
</discovered_facts>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one season compared" — the thinnest real path</name>
  <files>packages/harness/statbotics.ts, scripts/epaVsStatbotics.ts</files>
  <precondition>`https://api.statbotics.io/v3/team_years?year=2025&limit=1&offset=0` returns HTTP 200 with a one-element JSON array. If it does not, halt and report the outage — SC-2 goes back to blocked and the rest of this plan cannot run.</precondition>
  <read_first>
    - `scripts/measureRewindGap.ts` lines 495-575 and 640-655 — the established season-loop shape: `openCorpusReadOnly(CORPUS_PATH)`, `buildSeasonStream(db, season, { includeOffseason: true })`, `seasonBoundaryFor(seasons, seasonIdx)`, `carrySeason` threading, then `records.finalStates`. Mirror this; do not invent a second replay orchestration.
    - `scripts/measureRewindGap.ts` lines 43-64 and its `isEntryPoint` guard at the file's end — the deep-relative-import-with-`.js`-suffix convention and the entry-point guard every `scripts/*.ts` here uses.
    - `packages/harness/statbotics.ts` in full — the caching and fetch-failure conventions to extend rather than duplicate.
  </read_first>
  <action>
Wire ONE path all the way through, for season 2025 only, and print one line of output.

In `packages/harness/statbotics.ts`, add `fetchStatboticsTeamYears(season, options)`. It pages `/v3/team_years?year={season}&limit=1000&offset={n}`, advancing offset by the page size until a page returns fewer rows than the limit, and concatenates. Validate each row with a new Zod schema that picks only the fields this comparison consumes — `team`, `epa.total_points`, `epa.breakdown.{auto_points, teleop_points, endgame_points}`, `record.count` — and let unknown fields be stripped rather than passed through, matching this file's existing boundary discipline. Unlike `statboticsReference`, this function MUST throw on a fetch or validation failure: there is no honest fallback for a per-team reference series, and a silent empty result would read as "perfect agreement over zero teams". Support an optional on-disk cache path so re-runs during development do not re-page the API; write the cache under `reports/`.

Create `scripts/epaVsStatbotics.ts`. For this tracer it hardcodes season 2025 and does exactly one thing end to end:
  1. Fetch 2025's Statbotics team-years via the function above.
  2. Open the corpus read-only and replay `epa` across 2022, 2023, 2024, 2025 in order, offseason-inclusive, threading `carrySeason` at each boundary exactly as `measureRewindGap.ts` does, cold-starting positionally at index 0.
  3. Take `records.finalStates.get("epa")` after the 2025 season and call `epa.teamMetrics(finalState)`.
  4. Build our per-team comparable value as the team's `total` metric MINUS its `foulsCommitted` component (treat an absent `foulsCommitted` entry as zero). This is the whole point of the tracer: Statbotics' `epa.total_points` is a no-foul quantity, so comparing our raw `total` against it would compare two different things and quietly inflate every residual.
  5. Inner-join on `frc{statboticsRow.team}`, dropping any key for which `isDemoTeamKey` is true and dropping `DEMO_PSEUDO_TEAM_KEY`.
  6. Print one line: joined team count, our-side count, their-side count, and the Pearson correlation.

Add a `compare:epa-statbotics` script to the root `package.json` invoking `tsx scripts/epaVsStatbotics.ts` with NO `--env-file` flag.

Keep it to the single path. No multi-season output, no report file, no tolerance checking, no statistics beyond the one correlation — those are Task 2. Real error handling on this one path only.
  </action>
  <verify>
    <automated>npx tsx scripts/epaVsStatbotics.ts</automated>
  </verify>
  <done>The command prints a joined team count in the low thousands (2025 has 3,690 Statbotics rows and a comparably-sized corpus team pool) and a Pearson correlation that is a finite number well above 0.5. A correlation near zero, or a joined count in the single or double digits, means the join key or the metric extraction is wrong — fix it here, before Task 2 builds on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand to every season, with tested statistics and a tolerance gate</name>
  <files>packages/harness/epaStatboticsCompare.ts, packages/harness/epaStatboticsCompare.test.ts, scripts/epaVsStatbotics.ts, data/baselines/epa-vs-statbotics-2026-09.json</files>
  <behavior>
`epaStatboticsCompare.ts` holds the pure, network-free, corpus-free calculation. Tests drive it with hand-computed synthetic fixtures — no fetching, no SQLite:
    - Given paired series, `ordinaryLeastSquaresSlope` returns the known slope of an exactly-linear fixture (e.g. theirs = 2*ours + 3 yields slope 0.5 when regressing ours on theirs, in whichever direction the module documents — fix the direction in the doc comment and test THAT direction).
    - `pearson` returns 1 for a perfectly ascending pair, -1 for a perfectly descending pair, and a hand-computed value for a third fixture.
    - `meanAbsoluteDifference` returns the hand-computed mean of absolute residuals.
    - `sampleStandardDeviation` matches a hand-computed value and is undefined (or a documented sentinel) for fewer than two observations.
    - `joinTeams` drops demo keys from both sides, drops teams present on only one side, and reports `ourCount`, `theirCount`, `joinedCount` so coverage loss is visible rather than silent.
    - `joinTeams` applies a minimum-match filter when given one, keeping only teams whose Statbotics `record.count` meets the threshold.
    - `compareSeason` composes the above into one result object and, on an empty join, throws rather than returning vacuous perfect agreement.
    - `checkAgainstTolerance` returns the list of statistics that fall outside a supplied band, and an empty list when every statistic is inside it.
  </behavior>
  <action>
Extract the tracer's arithmetic into `packages/harness/epaStatboticsCompare.ts` as pure functions over plain arrays, per the behavior block. The module must not import the corpus, `node:fs`, or anything that fetches — that is what makes it testable without network or a 460MB database.

Widen `scripts/epaVsStatbotics.ts` to accept `--seasons <start>-<end>` (default 2022-2026), `--min-matches <n>`, `--no-offseason`, `--out <dir>`, and `--check`. Replay once across the full requested range, capturing season-final metrics at EVERY season in the range rather than only the last — the replay is chronological and already visits each boundary, so one pass produces all five comparisons.

For each season report: joined/our/their counts, OLS slope, Pearson, mean absolute difference in points, our rating SD, their rating SD, and a spot-check table of named teams — the top 15 by Statbotics EPA plus a deterministic sample of 15 more drawn by a fixed seed, each row carrying team key, their value, our value, and the difference. SC-2 asks for spot-checked teams by name; the aggregate statistics alone do not satisfy its wording.

Report every season twice: once over all joined teams, once filtered to `--min-matches` (default 12, the point at which `epaPercentFunc` has decayed to its floor and a rating has stopped moving fast). Low-match teams are noisy on both sides and would otherwise dominate the mean absolute difference.

Write the full result to `{out}/epa-vs-statbotics.json` (default `reports/epa-vs-statbotics/`) and print a per-season table to stdout.

`--check` loads `data/baselines/epa-vs-statbotics-2026-09.json` and exits non-zero, naming each offending statistic and its band, when a measured value falls outside its tolerance.

Then RUN it for real, twice, and create the baseline from what you measure:
  - `npx tsx scripts/epaVsStatbotics.ts` (full range, offseason-inclusive — production parity)
  - `npx tsx scripts/epaVsStatbotics.ts --seasons 2022-2025 --no-offseason --out reports/epa-vs-statbotics-nooff`

The second arm exists to settle a real comparability question empirically rather than by assumption: our production replay includes offseason events, which run after championships, while Statbotics' team-year EPA may not. Record the 2025 delta between the two arms; do not assume it is negligible and do not assume it is large.

Write `data/baselines/epa-vs-statbotics-2026-09.json` from the offseason-inclusive run: per season, the measured slope, Pearson, mean absolute difference and both SDs, each with a tolerance band centred on the measurement. Set bands wide enough that re-running today passes unchanged, and tight enough that a real regression in EPA's attribution arithmetic would break them — slope and Pearson to roughly +/-0.05, mean absolute difference to roughly +/-1.0 points, subject to what you actually observe. Record the band rationale in the file.

Do NOT tune EPA to move these numbers. This task measures; it does not adjust the model. If a season's agreement looks poor, report it — a documented divergence is the honest outcome, and epa.ts's header already predicts a slope near 0.84 rather than 1.00 because of six deliberate divergences.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/epaStatboticsCompare.test.ts</automated>
    <automated>npx tsx scripts/epaVsStatbotics.ts --check</automated>
  </verify>
  <done>The unit tests pass against synthetic fixtures with no network access. The full run prints a five-season table and writes the JSON report. `--check` passes against the committed baseline. The baseline file records a measured band per statistic per season, with its rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the year-level reference row and record the SC-2 verdict</name>
  <files>packages/harness/statbotics.ts, packages/harness/statbotics.test.ts, docs/models/epa-vs-statbotics.md, docs/models/epa-divergences.md, packages/core/algorithms/epa.ts</files>
  <read_first>
    - `packages/harness/statbotics.ts` lines 32-90 — `StatboticsYearResponseSchema` and the fallback constants being corrected.
    - `packages/harness/report.ts` — find every consumer of `statboticsReference` before changing what it returns, so the corrected value lands in the right places rather than breaking a report renderer.
  </read_first>
  <action>
**The schema fix.** `StatboticsYearResponseSchema` parses `{ epa_acc: number }`. Live v3 returns no such field; winner-prediction accuracy is at `metrics.win_prob.season.acc`. The consequence is that `statboticsReference` has been catching its own parse failure and returning the fallback on EVERY call — the API being back up changed nothing, because the parse fails before the fallback is reached. Repoint the schema at the live shape, reading accuracy from `metrics.win_prob.season.acc`. Also surface `metrics.win_prob.season.mse` on `StatboticsReference` as a new optional field: that is Statbotics' own Brier score, directly comparable to ours, and it is the single most useful number this endpoint publishes for a project whose stated core value is beating Statbotics on Brier.

Keep the existing failure-tolerant contract intact — a Statbotics outage must still not fail a report run — and keep `fetched` honest.

**The fallback correction.** Replace `STATBOTICS_REFERENCE_FALLBACK`'s 0.70/0.71 estimates with the values a live fetch returns today, per season, recorded with today's date and a `sourceLabel` that says they were fetched and verified rather than estimated. Drop the "KNOWN STUB" caveat from the doc comment once the values are real. These figures are 6-9 points higher than the estimates they replace, which makes the target SigmaScout is measured against materially harder — that is the correction, not a problem with it.

Add tests to `packages/harness/statbotics.test.ts` covering: a live-shaped payload parsing to the right accuracy and Brier, a payload missing `metrics` falling back rather than throwing, and every fallback constant carrying a fetched-and-verified label. Use an injected `fetchImpl` with inline fixtures — the existing `StatboticsReferenceOptions.fetchImpl` seam exists for exactly this. No test may hit the network.

**The verdict document.** Write `docs/models/epa-vs-statbotics.md` recording:
  - What SC-2 asks for, and that it was blocked from 2026-08-13 until this task on an API outage that has since resolved — with the resolution date and the endpoints re-verified.
  - The per-season measured table from Task 2 (both the all-teams and min-matches arms), and the named spot-check rows SC-2's wording requires.
  - The documented tolerance and where the baseline lives, so the claim is re-runnable rather than a dated assertion.
  - The comparability boundary, stated plainly: our value excludes `foulsCommitted` because Statbotics' `total_points` is a no-foul figure; the offseason-inclusive vs offseason-excluded delta measured in Task 2; and a pointer to `epa-divergences.md` for why an exact match is neither expected nor wanted.
  - A short section comparing SigmaScout's own EPA Brier against Statbotics' now-readable `metrics.win_prob.season.mse` per season. Report it as measured, in both directions, wherever we lose as well as where we win.
  - The verdict: whether SC-2 is met, in one sentence, with the tolerance it is met at.

**Flag, do not silently rewrite, any affected claim.** If the corrected accuracy figures contradict a comparative claim made anywhere in the repo or on the site, do NOT quietly restate that claim to fit. Record the contradiction in the verdict document under a clearly-titled section and add a pending todo under `.planning/todos/pending/` naming what needs the developer's decision. Retracting a published claim is the developer's call, not this task's.

**Cross-references.** Add a line to `docs/models/epa-divergences.md` pointing at the new document as the measurement those divergences predict. In `packages/core/algorithms/epa.ts`'s header, keep the measured figures but replace the un-re-runnable provenance with a pointer to the committed script and baseline, so a future reader can regenerate the numbers instead of trusting a quoted result from a script that no longer exists.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/statbotics.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`statboticsReference` returns live-fetched accuracy and Brier for a live-shaped payload and still falls back without throwing on a malformed one, proven by tests that never touch the network. The fallback constants carry today's verified values. `docs/models/epa-vs-statbotics.md` states the SC-2 verdict with its tolerance, its spot-check rows, and its comparability boundary. Any claim the corrected figures contradict is flagged with a pending todo rather than rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Statbotics API -> pipeline | Third-party JSON of unbounded shape crosses into our process, over five paged requests per season |
| `.env` -> transcript | Not crossed by design: this plan is credential-free and must stay that way |
| local corpus -> script | Read-only SQLite, already-ingested and validated upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4aa-01 | Tampering | `fetchStatboticsTeamYears` | medium | mitigate | Zod-validate every row at the fetch boundary, picking only consumed fields and stripping unknowns; throw on validation failure rather than degrading to a partial series |
| T-4aa-02 | Information disclosure | script invocation | high | mitigate | Run with plain `npx tsx`, never `--env-file=.env`; the script reads no environment variable and touches no credential. Per CLAUDE.md, never render `.env` contents into any output stream |
| T-4aa-03 | Denial of service | Statbotics paging | low | accept | At most ~4 requests per season across 5 seasons against a public unauthenticated API, with an opt-in local cache for re-runs; well inside any reasonable courtesy budget |
| T-4aa-04 | Repudiation | the SC-2 claim | medium | mitigate | The verdict is backed by a committed script and a committed tolerance baseline, so the measurement can be reproduced rather than taken on trust — the specific failure that made the prior ad-hoc measurement unverifiable |

No package-manager installs are introduced by this plan (Zod, tsx and vitest are already direct dependencies), so no package legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx vitest run packages/harness/epaStatboticsCompare.test.ts packages/harness/statbotics.test.ts` passes with no network access.
- `npx tsc --noEmit` is clean.
- `npx tsx scripts/epaVsStatbotics.ts --check` exits zero against the committed baseline.
- The comparison excludes `foulsCommitted` on our side and demo keys on both sides — visible in the report's own counts, not merely asserted.
- `docs/models/epa-vs-statbotics.md` names spot-checked teams, states a tolerance, and gives a one-sentence SC-2 verdict.
</verification>

<success_criteria>
SC-2's per-team Statbotics tolerance check moves from blocked-on-external-dependency to measured, with a committed, re-runnable script and a documented tolerance — and the year-level reference row reports Statbotics' real published accuracy instead of a silently-failing parse falling back to an unverified estimate.
</success_criteria>

<output>
Create `.planning/quick/260904-4aa-verify-epa-matches-statbotics/260904-4aa-SUMMARY.md` when done.

Record explicitly: the measured per-season agreement figures, the tolerance chosen and why, the offseason-inclusive vs offseason-excluded delta, the corrected year-level accuracy figures against the estimates they replace, and any comparative claim the correction calls into question.
</output>
