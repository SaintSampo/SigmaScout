---
phase: quick-260912-2uz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/rankingPoints/constants.ts
  - packages/core/rankingPoints/marginals.ts
  - packages/core/rankingPoints/marginals.test.ts
  - packages/core/rankingPoints/analyticPmf.ts
  - packages/core/rankingPoints/analyticPmf.test.ts
  - packages/harness/sigmaScoutLayer.ts
  - scripts/measureRpCalibration.ts
  - scripts/measureRpCalibration.test.ts
  - docs/models/rp-attribution.md
  - .planning/todos/pending/restore-negative-binomial-to-retest-it.md
autonomous: false
requirements:
  - restore-negative-binomial-to-retest-it

estimate:
  tokens: 180000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Production behaviour is bit-identical after the restore: analyticPmfGolden.json and predictThresholdsGolden.json both pass with zero edits to either JSON file (D-1 of the specifics: season modules stay gaussian)."
    - "familyForClauseSum refuses a negative-binomial scaled-sum clause with an explicit throwing case naming closure under scaled addition, per CONTEXT D-4 — the guard is stronger after this task, never weaker."
    - "The measurement refuses to run on any season at or above 2023, enforced in code at the CLI boundary, per CONTEXT D-1."
    - "Both arms score the identical observation set: per-bonus observation counts are asserted equal in-flight, per CONTEXT D-2."
    - "The reachable/unreachable partition is computed at runtime from ruleModule.bonusPredicates and printed with the results, per CONTEXT D-4 — never hardcoded from the context table."
    - "The negativeBinomial resolution counter is live and the fraction of NB-arm fits that genuinely resolved to negative binomial is reported, per CONTEXT D-6."
  artifacts:
    - packages/core/rankingPoints/marginals.ts
    - packages/core/rankingPoints/analyticPmf.ts
    - scripts/measureRpCalibration.ts
    - .planning/quick/260912-2uz-restore-negative-binomial-to-retest-it/260912-2uz-SUMMARY.md
  key_links:
    - "variant rule module (object spread over thresholdVariables) -> new SigmaScoutLayer(variant, algorithmId) -> allianceBonusRpPmf -> fitAllianceMarginals honours marginalFamily (CONTEXT D-3 seam)"
    - "one WalkForwardSimulator replay per season -> folded through two layers per algorithm (CONTEXT D-2: arms multiply layers, never replays)"
    - "runtime eligibility derivation from bonusPredicates -> which variables the variant flips to negative-binomial"
---

<objective>
Restore `"negative-binomial"` to the `MarginalFamily` union and to `marginals.ts`'s fit path
(deleted, not disabled, in `2731bfab`), then re-run plan 09-06's marginal-family attribution
through the one published scorer — on the selection slice only.

Purpose: 09-06's recorded verdict ("negative-binomial does not help") rests on a measurement in
which 80% of cells were structurally incapable of moving, because `clauseProbability` refit
combined moments as a hardcoded Gaussian. Quick task 260911-w7k removed that hardcode, but the
family had already been deleted, so nothing can exercise the fix. The verdict is unsupported, not
disproven. This task makes the question answerable and answers it on 21 of 33 cells instead of 0.

Output: the restored family under test; a measurement-only arm seam in
`scripts/measureRpCalibration.ts`; and a three-category result recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260912-2uz-restore-negative-binomial-to-retest-it/260912-2uz-CONTEXT.md
@.planning/todos/pending/restore-negative-binomial-to-retest-it.md
@.claude/CLAUDE.md

Recover deleted code with `git show 2731bfab -- <path>` (or `git show 2731bfab^:<path>`), never
from memory — the deleted text carries its own reasoning and that prose is worth more than a
paraphrase.
</context>

<environment_rules>
Binding for every task below; each was recorded after being broken once.

- **Another session is writing to this checkout.** Stage by explicit path. Never `git add -A`,
  never `git add .` — commit `f0c7af48` absorbed a foreign session's edits exactly that way.
  Run `git status --short` before every commit and confirm only this task's paths are staged.
- **No network.** The corpus (`data/corpus.sqlite`, gitignored, 582MB) is read-only input. Do not
  publish, do not write R2, do not run a live-origin check.
- **Never render `.env`.** Nothing here needs a secret; if something appears to, stop and ask.
- **Run tests from the repo root**, not `apps/web`. Prefer `npx vitest run <path>`; do not wrap
  test commands in `timeout`, which swallows output and exits 0 on this machine. Read the OUTPUT,
  not the exit code.
- Worktrees are already disabled and must stay disabled — the corpus would not exist in one.
</environment_rules>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore the negative-binomial family and its tests — one commit, provably inert in production</name>
  <files>packages/core/rankingPoints/constants.ts, packages/core/rankingPoints/marginals.ts, packages/core/rankingPoints/marginals.test.ts, packages/core/rankingPoints/analyticPmf.ts, packages/core/rankingPoints/analyticPmf.test.ts, packages/harness/sigmaScoutLayer.ts</files>
  <read_first>
    `git show 2731bfab -- packages/core/rankingPoints/constants.ts packages/core/rankingPoints/marginals.ts packages/core/rankingPoints/analyticPmf.ts packages/harness/sigmaScoutLayer.ts packages/core/rankingPoints/marginals.test.ts`
    — the exact deleted text, with its documentation intact. Restore from this diff, not from memory.
  </read_first>
  <behavior>
    - `fitMarginal(mean, variance, "negative-binomial")` with `variance > mean > 0` resolves to negative binomial and carries `r = mean^2/(variance-mean)` and `p = mean/(mean+r)`, with no `fallbackReason`.
    - `fitMarginal` with a declared negative binomial and `mean <= 0` resolves to gaussian with reason `non-positive-mean`; with `variance <= mean` it resolves to gaussian with reason `variance-le-mean`. Both are Gaussian fallbacks, both are counted as fallbacks.
    - `probAtLeast` on an NB-resolved marginal is monotone non-increasing in threshold and stays inside [0, 1] — the same asserted invariant the Gaussian and degenerate arms carry.
    - `probAtLeast(m, k) + probAtMost(m, k - 1)` equals 1 to within 1e-12 for an NB-resolved marginal at integer `k` (the discrete identity the degenerate arm deliberately does not satisfy).
    - `familyForClauseSum` over a clause whose terms all declare negative binomial THROWS, with a message naming the season, the bonus, and closure under scaled addition as the violated precondition.
    - A mixed clause (one term declaring gaussian, one declaring negative binomial) still throws on the distinct-families check — the pre-existing `declared.size !== 1` arm, now reachable from the real union instead of only through a cast.
    - The resolution tally counts an NB-resolved fit on its own `negativeBinomial` axis, separately from `gaussian`, `degenerate`, and `fallbacks`.
  </behavior>
  <action>
Restore exactly what CONTEXT.md's Specific Ideas section lists, and nothing beyond it.

`constants.ts`: grow `MarginalFamily` to the two-member union. The comment block above it currently
explains why the union has one member and why the declaration site survives anyway — that
explanation is now false. Rewrite it: state that the union has two members again, that the
2026-09-11 refusal was measured on a run whose reach was one predicate shape, that 260911-w7k
removed the hardcode limiting that reach, and that this restore exists to re-measure under the
wider reach. Keep the D-02 per-variable-declaration framing, which is unchanged and is exactly
what makes the measurement seam possible.

`marginals.ts`: restore `NB_MAX_TAIL_TERMS`, the `"negative-binomial"` member of
`ResolvedMarginalFamily`, the `non-positive-mean` and `variance-le-mean` members of
`MarginalFallbackReason`, `FittedMarginal`'s optional `r` and `p`, `fitMarginal`'s fourth rung,
and `nbTailSum` / `nbMode` / `negativeBinomialAtLeast` verbatim from `2731bfab` — the log-space
recurrence, the lower-sum-then-switch ordering that avoids catastrophic cancellation, and the
bounded loop are all load-bearing numerics that must not be re-derived. Restore the
`"negative-binomial"` arms of `probAtLeast` and `probAtMost`. Update `fitMarginal`'s ladder
docblock, which currently says the fourth rung went away with the family.

`analyticPmf.ts`: restore the `negativeBinomial` counter on `MarginalResolutionTally`, its zero in
`emptyMarginalResolutionTally`, and its arm in `accumulateMarginalResolution`. Update the tally
docblock, which currently says the counter is gone. Then fix `familyForClauseSum`: its `default:`
arm is a `never` exhaustiveness check and will stop compiling the moment the union grows. The
correct fix is an explicit `case "negative-binomial":` that THROWS, naming closure under scaled
addition as the violated precondition, with the season and bonus name in the message — a sum of
independent NB variables is NB only when every `p` matches, and a divided term is not even
integer-supported.

FORBIDDEN, and this is the whole reason the re-test exists: do not widen the `never`, do not
delete the exhaustiveness arm, do not return a family from the NB case, and do not add any
Gaussian fallback for an NB clause sum. Any of those reintroduces the hardcode 260911-w7k removed
and makes this measurement meaningless a second time. The guard must come out of this task
stronger than it went in.

`sigmaScoutLayer.ts`: wherever the tally is accumulated, ensure the NB resolution reaches the
counter — the layer passes its running tally into the pmf call, so confirm the restored
`accumulateMarginalResolution` arm is on the path and that `rpMarginalResolutionTally`'s returned
copy carries the new field.

Tests: restore the deleted NB tests from `2731bfab`'s version of `marginals.test.ts`, and add the
`familyForClauseSum` NB-throws test. Then find the tests that 260911-w7k had to reach through an
explicit cast (they carry a comment saying the guard is unreachable from real data under a
one-member union) and convert them to use the real union member — the guards are live now, and
that comment is no longer true.

Do NOT restore anything on CONTEXT.md's "Do NOT restore" list: `RpLayerConfig` and its unions,
`RP_LAYER_CONFIG_DEFAULT`, `describeRpLayerConfig`, `assertSupportedRpLayerConfig`,
`resolveDeclaredFamily`, the layer constructor's config parameter, the `p-red-win` win source, the
`discrete-margin` tie model, `splitOutcomeProbabilities`, the inertness golden, or the `--arms`
registry. All 34 season-module declarations stay as they are.

Commit this task alone, staging only the six files above by explicit path:
`feat(260912-2uz): restore the negative-binomial marginal family and its tests`.
  </action>
  <verify>
    <automated>npx vitest run packages/core/rankingPoints/marginals.test.ts packages/core/rankingPoints/analyticPmf.test.ts packages/core/rankingPoints/analyticPmfGolden.test.ts packages/core/rankingPoints/predictThresholdsGolden.test.ts packages/core/rankingPoints/analyticPmf.seasons.test.ts packages/core/rankingPoints/analyticPmf.universal.test.ts</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <automated>git diff 2731bfab..HEAD --stat -- packages/core/rankingPoints/analyticPmfGolden.json packages/core/rankingPoints/predictThresholdsGolden.json</automated>
  </verify>
  <done>
Both golden suites pass with ZERO edits to `analyticPmfGolden.json` and
`predictThresholdsGolden.json` — the third verify command must print no changed lines for either
file. If a golden appears to need editing, that is a BUG IN THE RESTORE, not a stale golden: stop,
diagnose why a production number moved when every season module still declares `"gaussian"`, and
fix the restore. Editing a golden to make this task pass is failing the task.

Typecheck is clean, the restored NB tests pass, `familyForClauseSum` throws for an NB clause sum,
and the NB resolution counter exists. One commit, six files, nothing from the "Do NOT restore"
list present in the diff.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the measurement-only NB arm seam, with the reporting slice made unreachable in code</name>
  <files>scripts/measureRpCalibration.ts, scripts/measureRpCalibration.test.ts</files>
  <read_first>
    `scripts/measureRpCalibration.ts` lines 1-130 (the SAME-SCORER FIX header, `parseSeasons`, and
    the `brier` / `rate` / `meanPredicted` helpers) and lines 461-560 (the season loop: one
    `buildSeasonStream`, one `WalkForwardSimulator(...).runAll`, the per-algorithm
    `Map<string, SigmaScoutLayer>`, and the tally fold).
    `packages/core/rankingPoints/constants.ts` lines 230-345 (`RpLinearTerm`, `RpThresholdClause`,
    and the seven-member `BonusPredicate` union) — the registry the eligibility partition is
    derived from.
  </read_first>
  <behavior>
    - Given a seasons spec containing 2023, 2024, 2025, or 2026 while the NB arm is requested, the script throws before opening the corpus, with a message citing the recorded decision that the reporting slice stays unspent.
    - Given a range spec that merely spans the reporting slice (for example 2016 through 2026), the script throws rather than silently trimming — the guard reads the PARSED season list before any rule-module filter.
    - Given the 2018 rule module, the derived eligibility partition marks `autoRunPoints`, `autoSwitchOwnershipSec`, and `endgamePoints` eligible and poisons none.
    - Given the 2017 rule module, the partition poisons all four fuel/rotor variables, because each appears in a `linearCombination` of scaled terms.
    - Given the 2016 rule module, the partition poisons `teleopChallengePoints` and `teleopScalePoints` and leaves the five crossing variables plus `attackedTowerEndStrength` eligible.
    - The variant rule module flips `marginalFamily` only for eligible variables and leaves every poisoned variable declaring gaussian; the original rule module object is not mutated.
  </behavior>
  <action>
Add the smallest possible seam to `scripts/measureRpCalibration.ts` that scores a second arm off
ONE replay through ONE scorer. Suggested surface (Claude's discretion per CONTEXT.md): a
`--marginal-arm` flag that, when present, runs the control arm and the NB arm side by side.

**The slice guard comes first, and it is structural.** Immediately after `parseSeasons` returns and
BEFORE the rule-module filter, before `openCorpusReadOnly`, and before any replay: if the NB arm is
requested and any parsed season is 2023 or later, throw. The message must say the 2023-2026
reporting slice was already spent on this question on 2026-09-11, that Jacob's 2026-09-12 decision
forbids spending it again, and that the arm runs on 2016-2020 plus 2022 only. Reading the parsed
list rather than the filtered one is deliberate: a wide range must be REFUSED, not silently
trimmed, so nobody can reach the reporting slice by accident. This guard is the reason the wrong
run is impossible rather than merely discouraged — do not downgrade it to a warning, a default, or
a documentation note.

**Derive the eligibility partition at runtime from `ruleModule.bonusPredicates`.** CONTEXT.md D-4's
table is what you EXPECT to see, not source to paste — the season modules are the truth and the
derivation must track them. Walk every predicate and enumerate every clause it contains:
`singleThreshold` contributes one implicit single-term clause over its `variable`;
`linearCombination` contributes one clause made of its `terms`; `conjunctionDistinct` contributes
each of its `clauses`; `countOfIndicators` contributes each of its `indicators`;
`dataDependentMixture` contributes its `selector`, `whenSelectorTrue`, and `whenSelectorFalse`;
`constant` contributes none; `nestedSameVariable` contributes a single unscaled reference to its
`variable`, since it routes through interval enumeration, which calls `probAtLeast` on the fitted
marginal directly and therefore honours the declared family.

A variable is ELIGIBLE only if every clause it appears in has exactly one term and that term's
`divisor` is absent or 1. A variable appearing in ANY multi-term clause or ANY divisor-bearing term
is POISONED and must keep declaring gaussian — including when it also appears alone in some other
clause, because a mixed clause throws on `familyForClauseSum`'s distinct-families check. Handle the
predicate union exhaustively so a future predicate kind fails to compile here rather than being
silently skipped.

**Build the variant rule module by the CONTEXT.md D-3 seam**, spreading the rule module and mapping
`thresholdVariables` so eligible variables declare negative binomial and poisoned ones do not. It
has been verified against HEAD that no season module's `parse` or `evaluateBonuses` uses `this`, so
an object spread is safe; re-confirm with a grep before relying on it, and clone differently rather
than abandoning the seam if that has changed.

**The arms multiply LAYERS, never replays.** Keep exactly one `buildSeasonStream` and one
`WalkForwardSimulator(...).runAll` per season. Construct the NB layer the same way the publisher
constructs the control layer — TWO arguments, the variant rule module and the algorithm id.
Omitting the second argument selects a different band variance and is the exact defect that
manufactured a phantom regression on this question once already. Fold the same `records` list
through both layers.

**Assert the identical observation set in flight**, per CONTEXT.md D-2: after each season, throw if
any bonus's observation count differs between arms. The layer's eligibility gates run before any
family branch so they cannot legitimately differ; an inequality means the seam is wrong and the
comparison is void.

**Score both arms with the existing `brier` / `rate` / `meanPredicted` helpers.** Never add a
second scoring implementation — see the SAME-SCORER FIX block in the script's own header for what
happens when two scorers disagree.

**Report the resolution tally per arm**, including the restored `negativeBinomial` count, and the
fraction of the NB arm's fits that genuinely resolved to negative binomial rather than falling back
to gaussian on non-positive mean or on under-dispersion. Print the derived partition — which
variables are eligible, which are poisoned and why, and which bonuses can move, cannot move because
a clause is a scaled sum, and cannot move because the predicate is constant — alongside the Brier
table.

Add tests in `scripts/measureRpCalibration.test.ts` for the behaviours above, exporting only what
the tests need. Do not restore the `--arms` registry, the deleted arm vocabulary, or the
attribution record's writer half; `--attribution-out`'s reader half and its digest builder stay
exactly as they are.

Commit this task alone, staging only the two script paths:
`feat(260912-2uz): add the measurement-only negative-binomial arm seam`.
  </action>
  <verify>
    <automated>npx vitest run scripts/measureRpCalibration.test.ts</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <automated>npx tsx scripts/measureRpCalibration.ts --marginal-arm --seasons 2020 --algorithm bpr</automated>
  </verify>
  <done>
The slice guard and the partition-derivation tests pass. The smoke run on 2020 alone completes,
prints the derived partition (2020 `endgamePoints` eligible, `shieldOperational` able to move), and
reports a per-arm resolution tally. Observation counts are equal across arms for every bonus. No
2023-2026 figure was produced at any point. One commit, two files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the selection-slice measurement, report the three categories separately, and take the correct exit path</name>
  <files>docs/models/rp-attribution.md, .planning/todos/pending/restore-negative-binomial-to-retest-it.md, .planning/quick/260912-2uz-restore-negative-binomial-to-retest-it/260912-2uz-SUMMARY.md</files>
  <precondition>`data/corpus.sqlite` exists and is readable, and Task 2's smoke run completed — the measurement reads the corpus read-only and needs no network.</precondition>
  <action>
Run the comparison on the SELECTION SLICE ONLY: seasons 2016-2020 plus 2022, across the published
algorithms. 2021 has no rule module and drops out of a range automatically; prefer the explicit
gapped spec so the intent is legible in the command itself. If the run takes long, let it run and
check progress by output rather than assuming it died.

**2023, 2024, 2025 and 2026 are FORBIDDEN.** Task 2's guard makes the run impossible, and that is
the point — do not work around it, do not add an override flag, and do not report a reporting-slice
figure even incidentally, even if one would be cheap. This is Jacob's recorded decision of
2026-09-12 and no result, however promising, authorises spending that slice.

**Report three categories, never pooled.** Pooling them is precisely what made the 09-06 verdict
worthless:

1. REACHABLE cells — those the derived partition says can move (expect roughly 21 of 33; report
   what the code actually derived, not the expected count). The gain or loss is evaluated on these
   cells and only these.
2. STRUCTURALLY UNREACHABLE cells — those that cannot move, each with its reason: a clause that is
   a scaled sum and so keeps its gaussian declaration, or a `constant` predicate that is inert by
   construction. These tie with control BY CONSTRUCTION. Say that as a structural fact and never
   present their ties as evidence about the family.
3. FALLBACK TIES — reachable cells that tied because the NB fit fell back to gaussian on
   non-positive mean or on under-dispersion. State what fraction of the NB arm's fits genuinely
   resolved to negative binomial. A run where most fits fell back is a result about the FIT'S
   APPLICABILITY and must be reported as that, not as a verdict on the family.

**Then take exactly one of two exit paths, per CONTEXT.md D-1. There is no third.**

PATH A — no real gain on the reachable cells: the question CLOSES. A family that cannot beat the
incumbent on the data it was chosen against will not be rescued by the reporting slice. Write the
measurement up in the SUMMARY with all three categories and the resolution fractions. Update
`docs/models/rp-attribution.md` by ADDING a section recording that the 09-06 verdict was re-tested
under the wider reach and what was found — do not rewrite its existing figures, and do not touch
`data/baselines/rp-attribution-2026-09.json`, whose digest sync test must stay green. Move the todo
from `.planning/todos/pending/` to resolved per the project's todo convention, citing this task and
the measured numbers.

PATH B — a real gain on the reachable cells: STOP. Report the magnitude to Jacob and end the task
there. Do NOT reach for 2023-2026 to confirm it. Do NOT promote anything, do NOT flip any season
module's declaration, and do NOT edit `docs/models/rp-attribution.md`'s verdict — a selection-slice
result cannot promote anything on its own, which is the entire purpose of the slice split.
Re-spending the reporting slice is a fresh decision for Jacob to make with the magnitude in hand.
Leave the todo pending, write the SUMMARY with the magnitude and the three categories, and hand
back.

Under BOTH paths the code changes from Tasks 1 and 2 stay in the tree: the family is restored and
the seam exists, and whether the question closed or escalated, the next person can re-run it.

Keep the written-up numbers in the SUMMARY — a report under `reports/rp/` is gitignored and is not
the durable record. Commit this task alone, staging only the doc and planning paths touched:
`measure(260912-2uz): negative-binomial re-test on the selection slice`.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/baselineFingerprint.test.ts scripts/measureRpCalibration.test.ts apps/web/src/lib/bonusRp.test.ts</automated>
    <human-check>The SUMMARY reports reachable cells, structurally-unreachable cells with reasons, and fallback ties as three separate categories; states the NB resolution fraction; contains no 2023-2026 figure; and names which exit path was taken and why.</human-check>
  </verify>
  <done>
The measurement ran on 2016-2020 plus 2022 only. The three categories are reported separately with
the derived partition printed. The NB resolution fraction is stated. Exactly one exit path was
taken and is named in the SUMMARY. `data/baselines/rp-attribution-2026-09.json` is unmodified and
its digest test is green. Nothing was promoted, and no season module declaration changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell/tool output -> transcript | `.env` holds the live TBA key and the R2 token pair; rendering it into any output stream leaks it to disk even though git stays clean |
| shared checkout -> git index | another session is writing to this working tree concurrently; a wildcard stage absorbs its edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2uz-01 | Information Disclosure | `.env` secrets reaching a transcript | high | mitigate | No task reads, cats, or interpolates `.env`; the measurement needs only the read-only corpus and no network. CLAUDE.md's secrets convention binds every task. |
| T-2uz-02 | Tampering | git index, shared checkout | medium | mitigate | Every commit stages explicit paths only; `git add -A` and `git add .` are forbidden; `git status --short` is checked before each commit. |
| T-2uz-03 | Tampering | 2023-2026 reporting slice | high | mitigate | Task 2's in-code guard throws on any parsed season at or above 2023 before the corpus is opened, making the prohibited run impossible rather than merely discouraged. |
| T-2uz-04 | Tampering | published RP probabilities | high | mitigate | Season modules keep declaring gaussian; both goldens must pass with zero edits, and a golden needing an edit is treated as a restore bug, not a stale golden. |
| T-2uz-SC | Tampering | package installs | low | accept | No package-manager installs in this task — every symbol restored comes from `2731bfab` in this repo's own history. |
</threat_model>

<verification>
- Both goldens green with zero JSON edits after Task 1 (production behaviour provably unchanged).
- `familyForClauseSum` throws for a negative-binomial clause sum; no Gaussian fallback was added.
- The NB arm cannot be pointed at 2023-2026 — the guard throws, and a test proves it.
- Both arms score equal per-bonus observation counts, asserted in flight, through one replay and
  one scorer constructed with two arguments.
- The reachable/unreachable partition is derived from `bonusPredicates` at runtime and printed.
- Three commits, in order: restore, seam, result.
</verification>

<success_criteria>
The negative-binomial family is restored under test with zero movement in any published number; the
re-test ran on the selection slice across roughly 21 reachable cells instead of 09-06's 0; the
result is reported in three separate categories with the NB resolution fraction stated; and exactly
one of the two recorded exit paths was taken, with nothing promoted and the 2023-2026 reporting
slice still unspent.
</success_criteria>

<output>
Create `.planning/quick/260912-2uz-restore-negative-binomial-to-retest-it/260912-2uz-SUMMARY.md` when done.
</output>
