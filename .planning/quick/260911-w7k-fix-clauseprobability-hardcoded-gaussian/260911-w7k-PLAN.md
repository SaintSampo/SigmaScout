---
phase: quick-260911-w7k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/rankingPoints/analyticPmf.ts
  - packages/core/rankingPoints/analyticPmfFixtures.ts
  - packages/core/rankingPoints/analyticPmfGolden.json
  - packages/core/rankingPoints/analyticPmfGolden.test.ts
  - packages/core/rankingPoints/analyticPmf.test.ts
  - packages/core/rankingPoints/constants.ts
  - docs/models/rp-attribution.md
  - docs/models/rp-layer-config-arms.md
  - .planning/quick/260911-w7k-fix-clauseprobability-hardcoded-gaussian/260911-w7k-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260911-w7k]

estimate:
  tokens: 47500
  raw_tokens: 95000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`clauseProbability` no longer names a marginal family as a string literal. The family a multi-term clause's combined moments are fitted with is DERIVED from the families its contributing terms declare."
    - "A single-term clause whose only term has `divisor === 1` reuses that variable's own `FittedMarginal` verbatim instead of refitting it — the clause's random variable IS the variable's, so no closure argument is needed and no family derivation applies. Its `resolved` and `fallbackReason` survive."
    - "A clause whose terms do not all declare the same family, or whose declared family is not closed under scaled addition, THROWS naming the season, the bonus and the violated precondition — the house style `assertIndependencePrecondition` / `assertPairwiseDisjoint` / `groupContribution`'s multi-bonus throw already use. It never silently falls back."
    - "C-1 NUMERICAL INERTNESS IS PROVEN, NOT ASSUMED: a committed golden file captured from `analyticPmf.ts` at HEAD *before* the change pins every registered season × three event tiers × eight moment patterns of `allianceBonusRpPmf`'s bonus probabilities, pmf and resolved-family tally at full double precision, and it is green both before and after the change with EXACT equality (`toBe`, no tolerance anywhere)."
    - "That golden is proven non-vacuous automatically, not by a manual perturb-and-revert: a 1.01x mean perturbation makes at least one bonus probability differ for every registered season, and every season has at least one bonus probability strictly inside (0.001, 0.999) so the grid is not a wall of saturated 0/1s blind to a Gaussian-fit change."
    - "C-2 NO SECOND FAMILY IS REINTRODUCED: `MarginalFamily` still reads `\"gaussian\"` and nothing else; no `resolveDeclaredFamily`, no `RpLayerConfig`, no selectable surface. `analyticPmf.test.ts`'s dead-symbol scan and its pinned export-set equality both stay green UNCHANGED — the new helper is NOT exported."
    - "C-3 BOTH NEW GUARDS ARE EXERCISED BY A TEST, not merely written. With a one-member union both are unreachable from real data, so each is reached through an EXPLICIT `as` cast in the test, and the cast carries a comment saying it is the honest way to reach a branch a second family would make reachable naturally. No coverage is faked and no guard is left untested."
    - "The `MarginalResolutionTally` counts are BIT-IDENTICAL before and after. `clauseProbability`'s combined fit was never fed to `accumulateMarginalResolution` (the sole call site iterates `marginalsByName.values()`), and this change neither adds nor removes a tally call. The golden pins the counts so the claim is checked rather than asserted."
    - "Floating-point evaluation order is preserved EXACTLY for the multi-term path: same left-to-right sum in declared term order, same `/divisor` and `/(divisor * divisor)`, never a precomputed multiplier — `RpLinearTerm`'s own doc comment says that order is load-bearing. `resolveRpThreshold` stays AFTER the per-term marginal lookups so the missing-marginal throw still surfaces first."
    - "No accuracy measurement, no retune, no republish, no R2 write, no corpus read, no BPR/SPR contact, no sealed holdout spent. There is nothing to measure — the value of this change is structural."
    - "Every present-tense claim in the tree that `clauseProbability` hardcodes a Gaussian is corrected to past tense with a dated superseded note, WITHOUT overstating: the change moves no published number, alters nothing that was measured, and only changes what a future measurement could reach."
  artifacts:
    - "packages/core/rankingPoints/analyticPmfGolden.json — the pre-change behaviour oracle, with `generatedFromCommit` and a regeneration prohibition"
    - "packages/core/rankingPoints/analyticPmfGolden.test.ts — exact-equality replay plus the two automated non-vacuity assertions"
    - "packages/core/rankingPoints/analyticPmfFixtures.ts — gains the deterministic grid builder; still carries no assertions of its own"
    - "packages/core/rankingPoints/analyticPmf.ts — `clauseProbability` rewritten, one new NON-exported family-derivation helper"
    - "packages/core/rankingPoints/analyticPmf.test.ts — the two cast-reached guard tests and the three-branch reuse-equivalence test"
  key_links:
    - "analyticPmfGolden.test.ts -> analyticPmfFixtures.ts (grid builder lives in the designated non-test sibling; importing a `.test.ts` re-executes its `describe()` calls and silently duplicates every test in it)"
    - "the new helper -> marginals.ts `FittedMarginal.declared` (the per-variable declaration site D-02 locked; the derivation READS it, it does not reintroduce a global switch)"
    - "the single-term fast path -> marginals.ts `fitMarginal`'s three-rung ladder (reuse is inert because refitting a fit's own (mean, variance) reproduces its `resolved` and `mean` on every rung)"
    - "docs/models/rp-attribution.md + docs/models/rp-layer-config-arms.md + constants.ts header -> the same measured finding, stated in three places and corrected in all three together"
---

<objective>
`clauseProbability` in `packages/core/rankingPoints/analyticPmf.ts` sums each term's scaled
moments and then fits the combined variable with a marginal family written as a STRING LITERAL.
Whatever the contributing `RpThresholdVariable`s declared is dropped on the floor. Every
predicate kind except `nestedSameVariable` routes through it, so 24 of 30 season/bonus cells were
structurally incapable of responding to a marginal-family change — a finding the 09-06 measurement
discovered and recorded but deliberately did not act on.

Make the seam real: derive the clause's family from its terms' declarations, reuse a single
unscaled term's own fit verbatim, and refuse loudly when a combination has no exact closed form.

Purpose: if a second family is ever introduced it reaches all 30 cells rather than the 6 that
`nestedSameVariable` covers. That is the ENTIRE value. There is nothing to measure.

Output: a behaviour-identical `analyticPmf.ts`, a committed pre-change oracle proving it is
behaviour-identical, two tested guards, and three documentation sites corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@packages/core/rankingPoints/analyticPmf.ts
@packages/core/rankingPoints/marginals.ts
@packages/core/rankingPoints/analyticPmfFixtures.ts
</context>

<preflight>

Read before writing anything. These were verified against HEAD during planning; re-confirm the
line numbers have not moved before editing.

1. **`packages/core/rankingPoints/constants.ts:75`** reads `export type MarginalFamily = "gaussian";`
   — a SINGLE-MEMBER union. Its `"negative-binomial"` sibling and the exact discrete CDF were
   deleted 2026-09-11 in commit `2731bfab`. `marginals.ts:184-196` carries that deletion's
   tombstone. **Do not reintroduce a second member, a resolver symbol, or any selectable surface.**

2. **`packages/core/rankingPoints/analyticPmf.test.ts`** carries two scans this plan must keep
   green with NO edit to either:
   - an **exported-surface set equality** built from `/^export (?:function|const|interface|type|class) (\w+)/gm`
     over `analyticPmf.ts`. Adding ANY new export to that file turns it red. The new helper is
     therefore NOT exported.
   - a **dead-symbol code-line scan** forbidding `resolveDeclaredFamily` (among seven others) on
     any line of `analyticPmf.ts` that does not begin with `//`, `*` or `/*`. Do not name the new
     helper that, and do not name it anything on that list.

3. **`fitMarginal`'s three-rung ladder** (`marginals.ts:148-158`) is what makes the single-term
   reuse inert. Read all three rungs before writing Task 2's equivalence test.

4. **`accumulateMarginalResolution`'s only call site** is the `for (const marginal of
   marginalsByName.values())` loop inside `allianceBonusRpPmf` (`analyticPmf.ts:511-513`).
   `clauseProbability`'s combined fit has NEVER been tallied. Confirm this by grep before
   claiming it in the SUMMARY.

5. **`packages/core/rankingPoints/predictThresholdsGolden.test.ts`** is the house pattern Task 1
   follows: a committed oracle captured from code at HEAD *before* the change, with an explicit
   regeneration prohibition in its header. Read its header for the wording convention.

6. **Test invocation.** Repo-root `npx vitest run` is the honest full-suite command. Running
   vitest from `apps/web` covers only a subset. `timeout <n> pnpm <cmd>` swallows output and
   exits 0 — never use it and never read its exit code as a pass. Verify by OUTPUT, not exit code.

</preflight>

<forbidden>

- **Do not add a second `MarginalFamily` member.** Not even commented out, not even in a test
  fixture type. The union stays one member.
- **Do not name anything `resolveDeclaredFamily`, `RpLayerConfig`, `RP_LAYER_CONFIG_DEFAULT`,
  `assertSupportedRpLayerConfig`, `describeRpLayerConfig`, `splitOutcomeProbabilities`,
  `tieProbability` or `TIE_MARGIN_HALF_WIDTH`** anywhere in `analyticPmf.ts`.
- **Do not export anything new from `analyticPmf.ts`.** The export set is pinned by a set equality.
- **Do not edit the pinned export set or the dead-symbol list** to accommodate this change. If
  either goes red, the change is wrong, not the pin.
- **Do not regenerate `analyticPmfGolden.json` to make a red test green.** The oracle exists to
  catch exactly that. Regeneration is gated on the GRID's own shape changing and on nothing else.
  If the golden goes red in Task 2, STOP and report — a red oracle here means the change is not
  numerically inert and the whole premise fails.
- **Do not add a tolerance to the golden comparison.** Exact `toBe` on every number. A tolerance
  would hide precisely the mistake the oracle exists to catch.
- **Do not weaken the non-vacuity assertion** if a season fails it. Name that season in an
  explicit exception list with its reason and assert the list is exactly that.
- **Do not run any accuracy measurement, retune, publish, R2 write, D1 write, or corpus read.**
  Do not touch BPR/SPR. Do not spend the sealed holdout. There is nothing to measure here.
- **Do not overstate the fix in any document.** It does not change what was measured. It changes
  what a future measurement could reach. Those are different sentences.
- **Do not touch `.env` in any way.** No `Read`, no `cat`, no interpolation. Nothing in this plan
  needs a secret.
- **Do not delete the unused `ruleModuleWithDeclaredFamily` helper** in `analyticPmf.test.ts`
  (line ~54). It is genuinely dead and its doc comment describes a deleted mechanism, but removing
  it is unrelated scope. Record it in the SUMMARY as a noticed-and-left follow-up.

</forbidden>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-change behaviour oracle, from code at HEAD, before touching anything</name>
  <files>packages/core/rankingPoints/analyticPmfFixtures.ts, packages/core/rankingPoints/analyticPmfGolden.json, packages/core/rankingPoints/analyticPmfGolden.test.ts</files>
  <precondition>`git status` is clean and `packages/core/rankingPoints/analyticPmf.ts` is unmodified at HEAD. If it is not, stop — an oracle captured from already-changed code proves nothing.</precondition>
  <action>
Build and commit the behaviour oracle that Task 2's numerical-inertness claim rests on. This task
must land as its own commit with `analyticPmf.ts` UNTOUCHED, so git history shows the pin predates
the fix.

**Step 1 — extend `analyticPmfFixtures.ts` with a deterministic grid builder.**

This file is the designated non-test fixture home precisely so a test can import from it without
re-executing another file's `describe()` registrations. Keep it assertion-free. Add:

- `export const GOLDEN_GRID_VERSION = 1;` — bumped only when the grid's own shape changes, never
  in response to a red test.
- `export const GOLDEN_EVENT_TYPES = [0, 2, 3] as const;` — the three real TBA `event_type` values
  exercising `eventTierFor`'s base / districtChampionship / championship mapping. Never name an
  `EventTier` directly, so the mapping stays on the tested path. Matches
  `scripts/rpPredictThresholdsGolden.ts`'s `GRID_EVENT_TYPES` convention.
- `export const GOLDEN_SCALES = [3, 10, 30, 100, 300] as const;` — five orders of magnitude, so
  every season's thresholds (counts in the single digits through points in the hundreds) fall
  somewhere inside the sweep rather than the grid being uniformly saturated.
- `export interface BonusPmfGoldenRow { readonly pattern: string; readonly bonusProbabilities: readonly number[]; readonly pmf: readonly number[]; readonly tally: { readonly gaussian: number; readonly degenerate: number; readonly fallbacks: number }; }`
- `export function buildBonusPmfGoldenRows(ruleModule: RpRuleModule, eventType: number, meanScale = 1): BonusPmfGoldenRow[]`

`buildBonusPmfGoldenRows` builds eight moment patterns per (season, eventType), each through the
existing `buildRuleModuleMoments` with its default `scoreMean`/`scoreVariance` (the bonus half
never reads them), calls `allianceBonusRpPmf(moments, ruleModule, eventType, freshTally)` with a
per-row fresh `emptyMarginalResolutionTally()`, and records the row. Multiply EVERY pattern's
`mean` by `meanScale` before building the moments; leave `variance` alone. The eight patterns:

  - `scale-${s}` for each `s` in `GOLDEN_SCALES` — every declared threshold variable gets
    `{ mean: s, variance: (s / 4) ** 2 + 1 }`.
  - `asym-30` — threshold variable at index `i` gets
    `{ mean: (30 * (i + 1)) / (i + 2), variance: (i + 2) * 3 }`. This pattern exists because
    every other numeric pattern gives all variables the SAME mean, under which a swapped divisor
    or a reordered term sum can cancel and leave the grid blind to it. Asymmetry is what makes
    the multi-term path observable.
  - `zero-variance` — every variable `{ mean: 30, variance: 0 }`, driving `fitMarginal`'s rung 2.
  - `non-finite` — variable index 0 gets `{ mean: Number.NaN, variance: 1 }`, every other variable
    `{ mean: 30, variance: 9 }`, driving `fitMarginal`'s rung 1. This is the ONLY pattern where
    Task 2's reuse and the current refit produce a differently-LABELLED fit (`fallbackReason`
    `"non-finite"` versus `"zero-variance"`), and it must be in the grid so that claim is checked
    against real output rather than argued.

The builder must NOT catch. If any (season, tier, pattern) throws, stop and report the exact
triple and the message — do not swallow it, and do not silently drop the pattern. If the throw is
genuine and unavoidable for a pattern, replace that pattern with one that does not throw and
record the substitution in the golden file's `note` field, then say so in the SUMMARY.

**Step 2 — generate the golden.**

Write a THROWAWAY generation script into the session scratchpad (not into the repo, not into
`scripts/`) that imports `RP_REGISTERED_SEASONS` / `rpRuleModuleForSeason` from
`packages/core/rankingPoints/rules.ts` and the new builder from `analyticPmfFixtures.ts`, iterates
every registered season × `GOLDEN_EVENT_TYPES` at `meanScale = 1`, and writes
`packages/core/rankingPoints/analyticPmfGolden.json` with this shape:

```
{
  "gridVersion": <GOLDEN_GRID_VERSION>,
  "generatedFromCommit": "<git rev-parse HEAD>",
  "note": "<one sentence: captured from analyticPmf.ts UNMODIFIED at the commit above, before quick task 260911-w7k changed clauseProbability; regeneration is gated on the grid changing and never on a red test>",
  "cells": { "<season>|<eventType>": [ <BonusPmfGoldenRow>, ... ], ... }
}
```

Run it with `npx tsx`. Serialize numbers as plain JSON numbers — JS emits the shortest
round-tripping representation for a double, so `JSON.parse` recovers the exact bit pattern and
exact equality is available. No rounding, no `toFixed`, no digest.

If the resulting file exceeds 200 KB, and only then, reduce `GOLDEN_SCALES` to `[3, 30, 300]` and
regenerate. Do not reduce it for any other reason.

**Step 3 — write `analyticPmfGolden.test.ts`.**

Header comment, in this module's established voice, stating: this is a characterization oracle,
its values WERE produced by running the module (unlike its `analyticPmf.test.ts` sibling, whose
D-07 discipline requires hand-computed values — the difference is deliberate and the two files
fail for different reasons), and carrying the regeneration prohibition verbatim in spirit from
`scripts/rpPredictThresholdsGolden.ts`'s own header.

Assertions:

  a. `golden.gridVersion` equals the live `GOLDEN_GRID_VERSION`.
  b. `Object.keys(golden.cells)` is exactly the full cross product of `RP_REGISTERED_SEASONS` and
     `GOLDEN_EVENT_TYPES` — a set equality, so both a missing cell and a stray one fail readably.
  c. For every cell, `describe.each`-style: the live `buildBonusPmfGoldenRows(...)` rows match the
     committed rows one at a time — same `pattern` strings in the same order, then every
     `bonusProbabilities[i]`, every `pmf[i]` and all three `tally` counts by EXACT `toBe`. Each
     assertion carries a message naming season, eventType, pattern and index, so a failure names
     the cell rather than just the cell's hash.
  d. **Non-vacuity, automated.** For every registered season at `eventType` 0, rebuild with
     `meanScale = 1.01` and assert at least one `bonusProbabilities` entry differs from the
     committed value in a numeric-ladder row. A characterization test that has never been observed
     to be sensitive to its own inputs is evidence of nothing.
  e. **Non-saturation, automated.** For every registered season, assert that somewhere across its
     numeric-ladder rows at least one bonus probability lies strictly inside `(0.001, 0.999)` —
     a grid of nothing but exact 0s and 1s would be blind to any change in a Gaussian fit. If a
     season genuinely cannot satisfy this, add it to an explicit `const STRUCTURALLY_SATURATED`
     list with a one-line reason per season and assert that list is exactly that set. Do NOT
     relax the bound.

**Step 4 — commit.** `git status` must show `analyticPmf.ts` unmodified. Commit the three files
(two new, one extended) together.
  </action>
  <verify>
    <automated>npx vitest run packages/core/rankingPoints/analyticPmfGolden.test.ts 2>&1 | tail -30 && test -z "$(git diff --name-only HEAD -- packages/core/rankingPoints/analyticPmf.ts)" && node -e "const g=require('./packages/core/rankingPoints/analyticPmfGolden.json');const n=Object.keys(g.cells).length;console.log('cells',n);process.exit(n===30?0:1)"</automated>
  </verify>
  <done>`analyticPmfGolden.json` exists with 30 cells and a real `generatedFromCommit` sha; `analyticPmfGolden.test.ts` is green including both non-vacuity assertions; `analyticPmf.ts` is byte-identical to HEAD; all three files are committed in one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive the clause's family from its terms, reuse a single unscaled term's own fit, refuse the rest loudly</name>
  <files>packages/core/rankingPoints/analyticPmf.ts, packages/core/rankingPoints/analyticPmf.test.ts</files>
  <behavior>
    - A clause with one term and no `divisor` (or `divisor === 1`) yields exactly
      `probAtLeast(thatVariablesOwnFittedMarginal, threshold)` — the variable's own fit object,
      not a refit of its moments.
    - The same clause under a `"gte"` direction and under `"lte"` routes to `probAtLeast` /
      `probAtMost` respectively, unchanged.
    - A multi-term clause whose terms all declare `"gaussian"` yields exactly what the current
      hardcode yields: the same left-to-right scaled-moment sum fitted Gaussian.
    - A clause whose terms declare more than one distinct family THROWS, and the message names
      the season, the bonus, the distinct families and the variables.
    - A clause whose single declared family is not closed under scaled addition THROWS, and the
      message names the season, the bonus and that family.
    - `fitMarginal(m.mean, m.variance, "gaussian")` reproduces `m.resolved`, `m.mean` and `m.sd`
      for all three of `fitMarginal`'s ladder rungs — which is why reuse is numerically inert.
    - The resolved-family tally counts are unchanged (pinned by Task 1's golden, which carries
      per-row tally counts).
  </behavior>
  <action>
**Step 1 — add the family-derivation helper to `analyticPmf.ts`, NOT exported.**

Place it immediately above `clauseProbability`. Name it `familyForClauseSum` (any name is fine
except the eight on the dead-symbol list — see `<forbidden>`). Signature:

`function familyForClauseSum(marginals: readonly FittedMarginal[], clause: RpThresholdClause, season: number, bonusName: string): MarginalFamily`

Body:

- Collect `new Set(marginals.map((m) => m.declared))`. If its size is not exactly 1, throw. The
  message names the season, the bonus, the distinct declared families it found, the clause's
  variable names in declared order, and states the violated precondition: a sum of scaled terms
  drawn from different families has no exact closed form, so declare one family across the clause
  or implement that joint explicitly. Word the message so it reads correctly for a zero-term
  clause too (say how many distinct families were found, not "more than one").
- Then `switch` on the single family. `case "gaussian":` returns `"gaussian"` — a comment states
  WHY this is the one that returns rather than throws: the Gaussian family is closed under scaled
  addition, which is exactly what made the previous hardcode numerically correct rather than
  merely convenient.
- `default:` assigns to a `const ...: never` (so a future union member fails to compile here
  before it can fail at runtime) and throws, naming the season, the bonus and the family, and
  stating the violated precondition: that family is not closed under scaled addition, so a sum of
  its scaled terms has no exact closed form in it. This matches the house style of
  `assertIndependencePrecondition`, `assertPairwiseDisjoint` and `groupContribution`'s multi-bonus
  throw — name the season, the subject and the precondition, never fall back silently.

**Step 2 — rewrite `clauseProbability`.**

Preserve, exactly:

- the existing missing-marginal throw, with its current message, raised at the SAME term;
- `resolveRpThreshold(clause.threshold, tier)` AFTER the per-term lookups, so the missing-marginal
  throw still surfaces before any threshold-resolution throw;
- the multi-term accumulation: left-to-right in declared term order, `mean += marginal.mean / divisor`
  and `variance += marginal.variance / (divisor * divisor)`, with `divisor = term.divisor ?? 1`.
  `RpLinearTerm`'s own doc comment says term order and division-not-multiplication are load-bearing
  because a threshold comparison is where that float difference becomes observable. Do not
  restructure the loop into a `reduce`, do not precompute a coefficient, do not reorder.

Change:

- **Single-term fast path.** When `clause.terms.length === 1 && (clause.terms[0].divisor ?? 1) === 1`,
  evaluate `probAtLeast`/`probAtMost` against that variable's own `FittedMarginal` directly. Write
  the reasoning into the comment: in this case the clause's random variable IS the variable's, so
  correctness rests on identity rather than on a closure property, no family derivation applies at
  all, and the variable's `resolved` / `fallbackReason` are preserved instead of re-derived. State
  the structural consequence plainly: `singleThreshold` — the most common predicate shape in the
  registry — would therefore honour a FUTURE family for free, even one not closed under scaled
  addition, which the refit could never have done.
- **Everything else** derives the family via `familyForClauseSum` and passes the derived value as
  `fitMarginal`'s third argument. The call must read `fitMarginal(mean, variance, family)` — the
  third argument is a variable, never a literal.

Rewrite the function's doc comment to describe what it now does, and add a dated line recording
that the hardcoded literal was removed by quick task 260911-w7k on 2026-09-11 — that it moved no
published number (every declaration in the tree is Gaussian, which is closed under scaled
addition, pinned by `analyticPmfGolden.json`), and that its value is reach, not accuracy.

**Step 3 — tests in `analyticPmf.test.ts`, appended as a new `describe` block.**

Write these BEFORE Step 2's edit if working strictly red-first; either way they must be in the
same commit and must have been observed red against the pre-change behaviour where applicable.

  a. **Reuse equivalence, all three ladder rungs.** For each of `fitMarginal`'s three outcomes —
     non-finite input, `variance <= 0`, and a normal `variance > 0` — assert that refitting the
     resulting marginal's own `(mean, variance)` reproduces the same `resolved`, the same `mean`
     and the same `sd`. This is the mechanism-level statement of why the single-term fast path is
     inert; the golden is the system-level one. Note in the test's own name that the ONLY field
     that can differ is `fallbackReason`, and that it is unobservable through `probAtLeast` /
     `probAtMost`, which read `resolved`, `mean` and `sd` and nothing else.
  b. **Mixed-family guard.** Build a rule module (via the existing `buildRuleModuleMoments` plus a
     locally cloned module) whose multi-term clause's variables declare two different families,
     reached with an explicit `as` cast — e.g. `{ ...v, marginalFamily: "negative-binomial" as unknown as MarginalFamily }`.
     Assert the throw and assert the message contains the season, the bonus name and both family
     strings. **Put a comment directly above the cast** saying: the union has one member today, so
     this branch is unreachable from real data and the cast is the only way to reach it; it is
     written this way deliberately rather than leaving the guard untested, and a second family
     would make the branch reachable without any cast at all.
  c. **Not-closed-under-addition guard.** Same shape, but all terms declaring the SAME cast
     non-Gaussian family, so the `default` arm fires. Assert the throw and that the message names
     the family and the violated precondition. Same comment discipline on the cast.
  d. **Single-term reuse is observable.** A `singleThreshold` bonus over a variable whose fit
     resolved DEGENERATE (zero variance) returns exactly `0` or exactly `1` — a structural
     identity, asserted with `toBe`, not `toBeCloseTo`.

**Step 4 — confirm the two pins are untouched.** `analyticPmf.test.ts`'s exported-surface set
equality and its dead-symbol code-line scan must both be green with NO edit to either. If either
goes red, the change is wrong.

**Step 5 — run the golden.** It must be green with zero edits to `analyticPmfGolden.json`. If it
is red, STOP and report: a red oracle here means the change is not numerically inert and the
premise of this whole task has failed. Do not regenerate.
  </action>
  <verify>
    <automated>npx vitest run packages/core/rankingPoints 2>&1 | tail -40 && test -z "$(git diff --name-only -- packages/core/rankingPoints/analyticPmfGolden.json)" && test "$(grep -vE '^\s*(\*|//|/\*)' packages/core/rankingPoints/analyticPmf.ts | grep -c 'fitMarginal(')" = "1" && test "$(grep -vE '^\s*(\*|//|/\*)' packages/core/rankingPoints/analyticPmf.ts | grep -c 'fitMarginal(mean, variance, family)')" = "1" && npx tsc --noEmit</automated>
  </verify>
  <done>`analyticPmf.ts` contains exactly one `fitMarginal(` call on a code line and its third argument is a variable; `analyticPmfGolden.json` is unchanged on disk; the whole `packages/core/rankingPoints` suite is green including the pinned export set, the dead-symbol scan and the four new tests; root `tsc --noEmit` is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Correct every present-tense claim that the hardcode still exists, without overstating the fix</name>
  <files>docs/models/rp-attribution.md, docs/models/rp-layer-config-arms.md, packages/core/rankingPoints/constants.ts</files>
  <action>
Three documents and one source header state, in the PRESENT tense, that `clauseProbability`
refits combined moments as a hardcoded Gaussian. After Task 2 that is false as a statement about
the code and true only as a statement about what was measured. Correct all of them in one commit,
following this repo's established convention (Phase 3.2): annotate with a dated superseded note,
leave the original numbers and the original finding intact.

**The one sentence every edit must respect.** The change does NOT alter what was measured, does
NOT change a published number, and does NOT make the refused negative-binomial result any less
refused. It changes what a FUTURE measurement could reach. Do not write anything stronger than
that in any of these files. A future attempt would still need a fresh reporting slice.

**1. `docs/models/rp-attribution.md` ~line 150** ("The cause is in `analyticPmf.ts`'s
`clauseProbability`. That function takes ... and then **refits the combined variable as a hardcoded
Gaussian** ..."). Keep the paragraph as the measurement-time record — it is what the measurement
found and it must stay readable as such. Add a dated note immediately after it: the hardcode was
removed on 2026-09-11 by quick task 260911-w7k; a clause now derives its family from its terms'
declarations and refuses a combination with no exact closed form; the removal moved no number in
this document, pinned by `packages/core/rankingPoints/analyticPmfGolden.json` captured before the
change and green after it.

**2. `docs/models/rp-attribution.md` ~line 167** ("This is recorded as a finding and **was not
acted on here**."). Still true of that phase and must stay. Append a forward pointer only: it was
acted on separately, as a structural change with no measurement attached, for the reason the
paragraph itself gives — changing `clauseProbability` during the attribution measurement would
have changed what `control` meant mid-flight, and doing it afterwards does not.

**3. `docs/models/rp-attribution.md` ~line 219** ("...once through `clauseProbability` discarding
the family altogether."). Past-tense already in context; verify it reads as a statement about the
measured arm and adjust the tense only if it reads as a claim about today's code.

**4. `docs/models/rp-attribution.md` line 345** — the sentence the handoff names explicitly:
"What was measured is this implementation's reach, which `clauseProbability` limits to one
predicate shape." Rewrite so `limits` becomes past tense scoped to the measurement, and add the
dated removal plus the no-number-moved claim and its pin. Keep the following sentence's point
intact: a future attempt would be measuring a different thing and would need a fresh reporting
slice.

**5. `docs/models/rp-layer-config-arms.md` ~line 272** ("`clauseProbability` refits a clause's
combined moments as a hardcoded Gaussian, so only `nestedSameVariable` bonuses ever honored a
declared family at all"). Same treatment: past-tense the mechanism, keep the 69.62% / 24-of-30
figures and the "counts fits PERFORMED rather than fits USED" point exactly as they are, and add
the dated note. The sentence "it is recorded rather than quietly fixed" must be updated — it was
subsequently fixed, loudly, in its own commit, with the measurement left standing.

**6. `packages/core/rankingPoints/constants.ts` ~lines 64-68** (the `MarginalFamily` header's
"`clauseProbability` refits a clause's combined moments as a hardcoded Gaussian, so only
`nestedSameVariable` bonuses ever honored a declared family at all"). Past-tense it and add one
sentence: the hardcode was removed 2026-09-11, so a future second family added to this union now
reaches every predicate shape whose terms declare it and is refused loudly where no exact closed
form exists — which is the reason the D-02 declaration site was worth keeping. Do NOT change the
union itself and do NOT weaken the "THE FLIP WAS TRIED AND REFUSED" record above it.

**7. `packages/core/rankingPoints/marginals.ts` ~line 192** ("the separate finding that the swap
reached only one predicate shape in the first place") — read it and leave it alone if it reads as
past tense about the measurement, which it does. Confirm rather than assume.

**8. Full verification.** Run the repo-root suite unfiltered and both typechecks. Per project
memory, root `tsc --noEmit` does not cover `apps/web`; run the web tsconfig too even though
nothing here touches it, as cheap insurance. Read the OUTPUT, never an exit code, and never wrap
any of it in `timeout`.

Note: `scripts/measureRpCalibration.test.ts` pins `rp-attribution.md`'s fenced JSON block by
deep-equality and its `## SELECTION SLICE` / `## REPORTING SLICE` headings by name. Prose edits
outside those are safe — but the suite run is what confirms it, not this note.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -30 && npx tsc --noEmit && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -5 && test "$(grep -c '260911-w7k' docs/models/rp-attribution.md)" != "0" && test "$(grep -c '260911-w7k' docs/models/rp-layer-config-arms.md)" != "0" && test "$(grep -c '260911-w7k' packages/core/rankingPoints/constants.ts)" != "0"</automated>
  </verify>
  <done>The repo-root suite is green with the output read, not the exit code; both typechecks are clean; no document or source header states in the present tense that `clauseProbability` hardcodes a Gaussian; every measured figure and every refusal record is intact and unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This change is confined to a pure, browser-safe-adjacent numerical leaf inside `packages/core`. It reads no network, no filesystem at runtime, no user input, and no secret. `analyticPmfGolden.json` is read only by a test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w7k-01 | Tampering | `analyticPmfGolden.json` | medium | mitigate | The oracle's whole value is that it is NOT regenerated to make a red test green. `<forbidden>` states the prohibition, the file's own `note` carries it, and Task 2's verify asserts the file is unmodified on disk after the change. |
| T-w7k-02 | Information disclosure | published RP probabilities | low | accept | No published number changes, proven by the oracle. Nothing new is emitted to any artifact, R2 object or client bundle. |
| T-w7k-03 | Denial of service | `familyForClauseSum`'s two throws | low | accept | Both are unreachable from real data under a one-member union, and both are deliberately loud rather than silently-falling-back — that is the module's house style and the property being added. A future family that trips them SHOULD abort a publish rather than emit a wrong distribution. |
| T-w7k-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package is installed by this plan. No dependency is added. The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>

Run from the repo root, reading OUTPUT rather than exit codes, never under `timeout`:

1. `npx vitest run` — the full node + web suite. Green.
2. `npx tsc --noEmit` and `npx tsc --noEmit -p apps/web/tsconfig.json` — both clean.
3. `git diff --stat` over the three commits — `analyticPmfGolden.json` must appear in the FIRST
   commit only and never again.
4. `git log --oneline -3` — Task 1's commit must precede Task 2's, and Task 1's commit must not
   touch `analyticPmf.ts`.

</verification>

<success_criteria>

- `clauseProbability` names no marginal family as a string literal; the multi-term path passes a
  derived value and the single-term unscaled path reuses the variable's own fit.
- Both new guards throw with season + bonus + violated precondition, and both are reached by a
  test through an explicit, commented cast.
- `analyticPmfGolden.json`, captured before the change, is green after it with exact equality and
  zero edits — the numerical-inertness claim is demonstrated, not assumed.
- The golden is proven non-vacuous and non-saturated by automated assertions.
- `MarginalFamily` is still a one-member union; no resolver symbol, no config object, no
  selectable surface; both pins in `analyticPmf.test.ts` green with no edit.
- Tally counts bit-identical, pinned per-row in the golden.
- Three documents and one source header corrected to past tense with a dated note, with every
  measured figure and every refusal record intact and no overstatement of the fix.
- Full repo-root suite and both typechecks green.
- No measurement, retune, publish, R2/D1 write, BPR/SPR contact or holdout spend.

</success_criteria>

<output>
Create `.planning/quick/260911-w7k-fix-clauseprobability-hardcoded-gaussian/260911-w7k-SUMMARY.md` when done.

The SUMMARY must state, plainly and without softening:

- That NO published number changed, and the specific evidence for it — the golden's commit sha,
  the number of cells and rows it pins, and that it was green after the change with zero edits.
  If the golden ever went red during the task, say so and say why.
- The two design decisions and their reasoning: the single-term `divisor === 1` reuse (why reuse
  rather than refit, and what it preserves), and the family derivation with its two refusals.
- That both guards are unreachable today under a one-member union and are reached in test only
  through an explicit cast — stated as a limitation, not hidden.
- That `clauseProbability`'s combined fit was never fed to the resolved-family tally and still is
  not, so tally counts are unchanged — and that this was confirmed by grep rather than assumed.
- The honest scope of the win: 24 of 30 season/bonus cells become CAPABLE of responding to a
  future family. Nothing was measured, nothing improved, nothing regressed.
- The noticed-and-left follow-up: `ruleModuleWithDeclaredFamily` in `analyticPmf.test.ts` (~line
  54) is dead code whose doc comment describes the deleted `resolveDeclaredFamily` mechanism.
- Any pattern substituted out of the golden grid because it threw, with the season/tier/pattern.

**Write the SUMMARY yourself if you are the main context. A subagent is blocked from writing to
`SUMMARY.md` and must return the text instead of routing around the block via Bash.**
</output>
