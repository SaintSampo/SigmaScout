---
phase: quick-260909-vs5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/harness/swingFactor.ts
  - packages/harness/swingFactor.test.ts
  - packages/harness/sigmaScoutLayer.ts
  - apps/worker/src/scheduled.ts
  - apps/worker/test/scheduled.replay.test.ts
autonomous: true
requirements: [QT-260909-vs5]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A fully-DQ'd alliance whose recorded score is exactly 0 contributes NO deviation to any of its three teams' Swing Factor beliefs, at BOTH fold sites (D1)."
    - "The OPPOSING alliance in that same match still folds normally. A card zeroes one alliance's observation, never the whole match (D2)."
    - "A skipped alliance consumes NO decay step: a team's belief after a history containing a card-zeroed match is EXACTLY equal to its belief after the identical history with that match absent entirely (D3)."
    - "A PARTIAL DQ (some but not all teams on an alliance disqualified) still folds normally -- dq.ts measures that population at 68.4 points with essentially no zeros, and it is genuine evidence."
    - "A whole-alliance DQ with a NON-ZERO recorded score still folds normally -- `isFullyDqZeroScoreAlliance` returns false there by construction, and that is the intended behaviour, not a miss."
    - "The `isFullyDemoAlliance` early return stays FIRST and still drops the WHOLE match, both alliances. The DQ check is a separate per-alliance skip evaluated after it (D5)."
    - "The live Worker and the offline publisher remain bit-identical on the Match Band stream: `apps/worker/test/scheduled.replay.test.ts`'s band digest comparison is green (D4)."
    - "`foldMatch` has exactly ONE signature and THREE callers, all three updated in the same commit. No caller can reach `foldMatch` without supplying DQ keys, because the parameter type makes them required (D4)."
    - "Only `isFullyDqZeroScoreAlliance` is applied. The adjust-based sibling predicate, breakdown parsing, and any Worker breakdown plumbing are absent from this change (D1)."
    - "`npx tsc --noEmit` at the root, against apps/worker/tsconfig.json, and against apps/web/tsconfig.json are all clean, and `npx vitest run` from the REPO ROOT is green."
    - "No republish is attempted. No `pnpm publish:seasons`, no `pnpm publish:artifacts`, no `--dry-run`, no network Bash of any kind."
  artifacts:
    - packages/harness/swingFactor.ts
    - packages/harness/swingFactor.test.ts
  key_links:
    - "`foldMatch`'s signature is the single seam. All THREE callers -- `packages/harness/sigmaScoutLayer.ts:140` (offline publish), `apps/worker/src/scheduled.ts:1003` (live Worker), and `apps/worker/test/scheduled.replay.test.ts:506` (the parity test's own offline mirror) -- must change together. The third one is NOT named in CONTEXT.md and is the one most likely to be missed; it is also the test that would have caught the miss, so missing it disarms the very gate that protects D4."
    - "`MatchResult.redDqs`/`blueDqs` (packages/core/algorithms/types.ts:75-76) are already populated at both production sites -- `scheduled.ts:407-408` maps them from `CorpusMatch` today. No new plumbing is required; the data is in hand."
    - "Swing folds the RAW roster: unlike EPA and Sigma1 it performs no surrogate filter and no demo remap, so the roster `foldMatch` folds IS its rating-eligible team list, and that is the identity compared against the raw DQ key lists per D5's composition contract."
    - "`packages/harness/swingFactor.ts` is imported by apps/web (`SwingPage.tsx`, `SeasonHeader.tsx`, `rowModel.ts`), so anything it imports enters the browser bundle. `packages/core/algorithms/dq.ts` has ZERO imports and is pure arithmetic over arrays -- browser-safe, same as the `demoTeams.js` import already sitting there."
---

<objective>
Wire `isFullyDqZeroScoreAlliance` into `SwingFactorAccumulator.foldMatch` so a red
card's ruling-zero stops being fitted as evidence that three real robots are
inconsistent.

Purpose: `packages/core/algorithms/dq.ts` already exists and is already applied by
EPA and Sigma1. Swing Factor was simply never wired to it -- `foldMatch` checks
`isFullyDemoAlliance` and nothing else. Since quick task 260909-tgf made Swing Factor
a tiered metric, one carded match now tanks both a team's Swing Factor and its rarity
tier. A ruling is not evidence about a robot.

Output: a per-alliance skip in `foldMatch`, all three call sites updated in one
commit, and six tests pinning the behaviour -- including the live/offline bit-equality
gate that must stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260909-vs5-exempt-card-driven-zero-scores-from-swin/260909-vs5-CONTEXT.md
@packages/core/algorithms/dq.ts
@packages/harness/swingFactor.ts
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Teach foldMatch the DQ predicate and update all three callers in one edit</name>
  <files>packages/harness/swingFactor.ts, packages/harness/sigmaScoutLayer.ts, apps/worker/src/scheduled.ts, apps/worker/test/scheduled.replay.test.ts</files>
  <precondition>At HEAD the three typechecks (root, apps/worker/tsconfig.json, apps/web/tsconfig.json) and `npx vitest run --project node apps/worker/test/scheduled.replay.test.ts packages/harness/swingFactor.test.ts` are all green -- verified at plan time 2026-09-09 against `192be647`, 15 tests in ~2s. If any is already red, STOP and report: the parity digest is this task's only guard on D4 and a pre-existing red disarms it.</precondition>

  <read_first>
    Read `packages/core/algorithms/dq.ts` in FULL before writing anything. Its header IS
    the specification for this task: the two encodings, the corpus measurements, the
    drop-only-the-offending-alliance asymmetry, and the composition contract with
    `isFullyDemoAlliance`. Then read `packages/harness/swingFactor.ts` lines 355-410
    (`foldMatch` and `fold`).
  </read_first>

  <behavior>
    After this task, `foldMatch` behaves as follows. Task 2 turns each line into a test.

    - Either alliance fully demo -> return early, NEITHER alliance folds. Unchanged (D5).
    - Red alliance fully DQ'd AND `redScore === 0` -> red does not fold; blue still folds (D1, D2).
    - Blue alliance fully DQ'd AND `blueScore === 0` -> blue does not fold; red still folds (D1, D2).
    - Partial DQ (not every team on the roster is in the DQ list) -> folds normally.
    - Fully DQ'd but score is non-zero -> folds normally.
    - A skipped alliance runs no decay: its teams' beliefs are untouched, exactly as if the match had never been passed in (D3).
  </behavior>

  <action>
    Change `foldMatch` from six positional parameters to TWO object parameters, and
    apply the predicate per alliance.

    STEP 1 -- the parameter types. In `packages/harness/swingFactor.ts`, export two
    small structural interfaces above the class: `SwingFoldMatch` with readonly
    `redTeams: readonly string[]`, `redScore: number`, `redDqs: readonly string[]`,
    `blueTeams: readonly string[]`, `blueScore: number`, `blueDqs: readonly string[]`;
    and `SwingFoldPrediction` with readonly `redScore: number`, `blueScore: number`.

    Structural rather than importing `MatchResult` on purpose, and this is load-bearing
    for two separate reasons. First, `MatchResult` is structurally assignable to
    `SwingFoldMatch` and `Prediction` to `SwingFoldPrediction`, so every production
    caller passes the object it ALREADY holds and no argument can be transposed.
    Second, Task 2's tests can build six-field literals instead of fabricating a
    fifteen-field `MatchResult`, which keeps the tests readable.

    Making both DQ fields REQUIRED (never optional, never defaulted) is the mechanical
    enforcement of D4: a caller that omits them fails typecheck rather than silently
    folding a carded zero while looking healthy. Do NOT add an optional parameter, a
    default of `[]`, or an overload -- that is the exact failure mode D4 names.

    STEP 2 -- the body. `import { isFullyDqZeroScoreAlliance } from "../core/algorithms/dq.js";`
    alongside the existing `demoTeams.js` import. Keep the `isFullyDemoAlliance` early
    return EXACTLY as it is and EXACTLY where it is -- first, against the raw team lists,
    dropping the whole match (D5). Then replace the two unconditional `this.fold(...)`
    calls with two guarded ones: fold red unless
    `isFullyDqZeroScoreAlliance(match.redTeams, match.redDqs, match.redScore)`, and fold
    blue unless the blue equivalent. Two independent guards, never a shared early return
    -- that asymmetry is D2 and it is the whole point: the DQ'd alliance's three robots
    were real and physically on the field, so only THAT alliance's own 0 is meaningless.

    Not calling `fold` is what satisfies D3: decay lives inside `fold`, so a skipped
    alliance ages nothing. Do NOT decay-then-decline.

    STEP 3 -- the doc comment. Extend `foldMatch`'s existing comment to record: (a) why
    the demo rule drops the whole match while the DQ rule drops one alliance, citing
    dq.ts's header rather than restating its measurements; (b) that swing folds the RAW
    roster -- no surrogate filter, no demo remap -- so the roster it folds IS its
    rating-eligible list, which is the team identity D5's composition contract asks be
    compared against the raw DQ key lists; (c) that a PARTIAL DQ deliberately still
    folds, because dq.ts measures that population at 68.4 points with essentially no
    zeros and it is genuine bad-but-real play; (d) that only the DQ-flagged encoding is
    handled here, and the sibling encoding dq.ts's header documents (the one requiring a
    parsed breakdown) is deliberately out of scope per D1, because neither fold site
    parses breakdowns and applying offline a predicate the Worker cannot evaluate under
    its 10 ms budget would break the D4 bit-equality contract that exists to catch
    exactly that.

    STEP 4 -- ALL THREE callers, in this same edit. CONTEXT.md's D4 names two; there are
    three, and the third is a test:
      - `packages/harness/sigmaScoutLayer.ts:140` -> pass `match` and `prediction` directly.
      - `apps/worker/src/scheduled.ts:1003` -> pass `result` and `prediction` directly.
      - `apps/worker/test/scheduled.replay.test.ts:506` -> pass `r.match` and `r.prediction` directly.
    Each becomes a two-argument call passing whole objects. Change nothing else at any
    of the three sites -- the `bandVarianceFor` reads that precede each call are
    predict-before-update and stay exactly where they are.

    The replay test's call is a hand-maintained mirror of the offline publisher, written
    that way because apps/worker cannot import `packages/harness/replay.ts` (better-sqlite3
    pulls in an ambient `URL` collision -- see that file's own header). It must stay a
    faithful mirror or it silently stops testing what it claims to.

    SCOPE FENCE (D1): touch no breakdown parsing, add no breakdown plumbing to the
    Worker, and import exactly ONE symbol from dq.js. Do not touch `packages/core`,
    `publish.ts`, or any apps/web file.
  </action>

  <verify>
    <automated>npx tsc --noEmit; echo "root tsc exit=$?"; npx tsc --noEmit -p apps/worker/tsconfig.json; echo "worker tsc exit=$?"; npx tsc --noEmit -p apps/web/tsconfig.json; echo "web tsc exit=$?"</automated>
    <automated>npx vitest run --project node apps/worker/test/scheduled.replay.test.ts</automated>
    <automated>echo "callers-in-object-form (expect 3): $(grep -rn '\.foldMatch(' --include=*.ts . | grep -v node_modules | grep -cE '\.foldMatch\([A-Za-z0-9_.#$]+, *[A-Za-z0-9_.#$]+\)' || true)"</automated>
    <automated>echo "dq-import-count (expect 1): $(grep -c 'core/algorithms/dq.js' packages/harness/swingFactor.ts || true)"</automated>
    <automated>echo "out-of-scope-symbols-in-code (expect 0): $(grep -vE '^\s*(\*|//|/\*)' packages/harness/swingFactor.ts | grep -cE 'isAdjustZeroed|adjustPoints|tryParseBreakdownPair' || true)"</automated>
  </verify>

  <done>
    All three typechecks print no errors (`tsc` is silent when clean -- judge by the
    absence of error lines, not by exit code alone). `scheduled.replay.test.ts` passes,
    including its "online and offline Match Band streams diverged" digest assertion and
    its non-vacuity guard -- this is the D4 bit-equality proof and it is non-negotiable.
    The caller-form gate prints 3, the dq-import gate prints 1, and the out-of-scope gate
    prints 0 (the grep filters comment lines, so the doc comment STEP 3 asks for may
    freely discuss the deferred encoding without tripping it).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin all six behaviours with non-vacuous tests</name>
  <files>packages/harness/swingFactor.test.ts</files>

  <behavior>
    A new `describe("SwingFactorAccumulator.foldMatch -- card-driven zero scores")`
    block in the existing file, after the existing walk-forward block. Six tests.

    Every test that asserts a SKIP must also prove the skip did something. `fold` is
    public and unchanged, so each skip test builds a second accumulator that folds the
    same history WITH the zero via direct `fold` calls, and asserts the two disagree.
    Without that companion assertion a test that silently folded nothing at all would
    pass while proving nothing -- the same non-vacuity trap `scheduled.replay.test.ts`
    already guards against for its band digest.

    - Test 1 (D1): a fully-DQ'd, zero-score red alliance does not fold. Fold two real
      matches for the red roster plus one card-zeroed match; `swingFor` on a red team
      equals `swingFactorFromDeviations` over the two real deviations only, and does NOT
      equal the value a second accumulator produces when the zero is folded in.
    - Test 2 (D2): the OPPOSING alliance in that same match still folds. Same match
      stream as Test 1; a blue team's swing equals the value it would have had from a
      history where every blue observation folded, including the one opposite the
      carded alliance. Assert the blue value is defined and matches an explicitly
      computed expectation -- not merely "is defined".
    - Test 3: a PARTIAL DQ still folds. Same zero-score red alliance but with only two
      of the three teams in `redDqs`; the red team's swing must equal the folded-in
      value, i.e. the SAME number the Test 1 companion accumulator produced.
    - Test 4: a whole-alliance DQ with a NON-ZERO score still folds. Every red team in
      `redDqs`, `redScore` well away from 0; folds normally.
    - Test 5 (D3): a skipped fold consumes no decay step. Accumulator A folds three real
      matches with a card-zeroed match interleaved between the second and third;
      accumulator B folds the same three real matches with the carded match absent
      entirely. Assert `swingFor` is EXACTLY equal across A and B (compare to at least
      12 decimal places). This is the strongest available form of D3: an implementation
      that decayed and then declined to add would leave A strictly below B and fail here.
    - Test 6 (D5): the demo rule still drops the WHOLE match. A match where the blue
      roster is fully demo leaves BOTH rosters' beliefs untouched, even with empty DQ
      lists and non-zero scores -- proving the DQ change did not disturb the demo early
      return or its ordering.
  </behavior>

  <action>
    Add the six tests described above to `packages/harness/swingFactor.test.ts`. Import
    `SwingFoldMatch` (type-only) alongside the existing imports if a local helper needs
    it; the existing import block already pulls `SwingFactorAccumulator` and
    `swingFactorFromDeviations` from `./swingFactor.js`.

    Follow the file's established style: a small local roster constant like the existing
    `RED`, expectations computed from `swingFactorFromDeviations` over the surviving
    deviations rather than hardcoded magic numbers (the existing "ignores an unplayed or
    malformed row" test at line ~99 is the exact pattern to mirror -- it is already the
    ignore-without-decay contract, one layer down), and `toBeCloseTo` with an explicit
    high precision.

    Use a small factory for the match literal so each test overrides only the fields it
    is about. Six fields, no `MatchResult` fabrication -- that is what Task 1's
    structural parameter type bought.

    Name each test after the behaviour and the reason, in the file's voice -- a title
    like "a card is a ruling, not evidence: a fully-DQ'd zero-score alliance does not
    fold" reads correctly in a failure report. Do not name tests after decision IDs.

    Add nothing else. No changes to the existing five walk-forward tests, no new
    exports, no changes to any other file.
  </action>

  <verify>
    <automated>npx vitest run --project node packages/harness/swingFactor.test.ts</automated>
    <automated>echo "total it() in swingFactor.test.ts -- expect 20, was 14 at HEAD: $(grep -cE '^\s+it\(' packages/harness/swingFactor.test.ts || true)"</automated>
  </verify>

  <done>
    `swingFactor.test.ts` runs green with 6 more tests than the 14 it had at HEAD (20
    total across its four describe blocks), judged by vitest's printed pass count, not
    by exit code. Each of the six behaviours in the block above has its own `it`, and
    every skip test carries its companion did-something assertion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gates, then commit by explicit path</name>
  <files>packages/harness/swingFactor.ts, packages/harness/swingFactor.test.ts, packages/harness/sigmaScoutLayer.ts, apps/worker/src/scheduled.ts, apps/worker/test/scheduled.replay.test.ts</files>

  <action>
    Run the full gates from the REPO ROOT, never from apps/web -- vitest from apps/web
    collects 77 files and from the root 167, and an eight-day red CI once hid in exactly
    that gap. Do NOT wrap any command in `timeout <n> pnpm ...`; on this machine that
    swallows all output and exits 0. Use `npx vitest run` and judge by the printed
    summary line.

    Gate A: `npx vitest run` from the repo root. This runs both vitest projects (node
    and apps/web). Expect 167+ files green. If anything is red, read the failure before
    assuming it is unrelated -- this change alters Swing Factor values and several
    harness tests assert on published metric shapes.

    Gate B: the three typechecks, run separately. The root `tsconfig.json` does NOT cover
    apps/web, so `npx tsc --noEmit -p apps/web/tsconfig.json` is a distinct gate and
    matters here because apps/web imports `swingFactor.ts` directly. Likewise
    `apps/worker/tsconfig.json`, whose Cloudflare-typed program contains two of the three
    callers.

    Then commit. Multiple sessions have been committing to this checkout all day; the
    working tree at plan time carried unrelated foreign modifications to
    `docs/publish-budget.md` and `.planning/todos/pending/00-sigmascout-layer-roadmap.md`,
    and HEAD moved during planning. Per D6: NEVER `git add -A`, `git add .`, `git add -u`,
    `git stash`, or `git reset --hard`. Stage explicit paths only and use
    `git commit -- <paths>` naming the five files in `<files>` above, so a concurrent
    session's staged work cannot ride along. Run `git status --porcelain` and
    `git show --stat HEAD` afterwards and confirm the commit contains those five paths
    and nothing else -- this project has a recorded incident of a commit absorbing
    foreign edits.

    NETWORK FENCE: the executor is network-sandboxed. Attempt NO republish -- no
    `pnpm publish:seasons`, no `pnpm publish:artifacts`, not even `--dry-run`. This
    change alters published Swing Factor values and therefore the rarity tiers added by
    260909-tgf, so a republish IS required for the fix to reach the site, but that is the
    developer's follow-up step and is out of scope here (same rule as 260909-tgf's D4).
    Say so plainly in the SUMMARY rather than silently leaving it implied; the pending
    todo `republish-for-swing-factor-and-band.md` already tracks that debt.
  </action>

  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
    <automated>npx tsc --noEmit; echo "root=$?"; npx tsc --noEmit -p apps/worker/tsconfig.json; echo "worker=$?"; npx tsc --noEmit -p apps/web/tsconfig.json; echo "web=$?"</automated>
    <automated>git show --stat HEAD; echo "--- tree ---"; git status --porcelain</automated>
  </verify>

  <done>
    The root vitest summary line reports all test files passed with zero failures. All
    three typechecks emit no error lines. `git show --stat HEAD` lists exactly the five
    files from `<files>` and no others, and no republish command appears anywhere in the
    session transcript.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline publisher -> live Worker | Both compute the Match Band from the same accumulator; a rule applied in one and not the other silently desynchronizes published data while both sides look healthy. |
| pipeline -> published artifacts | This change alters numbers already on the site; artifacts stay stale until the developer republishes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-vs5-01 | Tampering | `SwingFactorAccumulator.foldMatch` live/offline parity | high | mitigate | Required object parameters make DQ keys un-omittable at typecheck; all three callers change in one commit; `scheduled.replay.test.ts`'s band digest is a blocking gate in Task 1 and Task 3. |
| T-vs5-02 | Information disclosure | published Swing Factor values and rarity tiers | low | accept | Site shows pre-fix values until the developer's republish. Deliberate: the republish is out of scope (D1/network fence) and is tracked by `republish-for-swing-factor-and-band.md`. |
| T-vs5-03 | Tampering | concurrent-session commits to a shared checkout | medium | mitigate | D6: explicit-path staging, `git commit -- <paths>`, and a post-commit `git show --stat` confirming exactly five files. |
</threat_model>

<verification>
1. `packages/harness/swingFactor.ts` imports exactly one symbol from
   `packages/core/algorithms/dq.js` and contains no breakdown-parsing symbols outside
   comments (D1 scope fence).
2. `foldMatch` has one signature, three callers, all passing whole objects; no optional
   or defaulted DQ parameter exists anywhere (D4).
3. `scheduled.replay.test.ts`'s Match Band digest comparison is green, with its
   non-vacuity guard intact (D4 bit-equality).
4. Six new tests cover: skip on fully-DQ'd zero, opponent still folds, partial DQ folds,
   non-zero DQ folds, no decay consumed, demo still drops the whole match.
5. Root `npx vitest run` green; root, apps/worker, and apps/web typechecks all clean.
6. Commit contains exactly the five listed files.
</verification>

<success_criteria>
A card-zeroed alliance's 0 never enters a Swing Factor belief; the opponent's real
observation still does; the skip costs no decay step; live and offline still produce
byte-identical Match Bands; and the whole repo is green under both typecheck and test
from the repo root.
</success_criteria>

<output>
Write `.planning/quick/260909-vs5-exempt-card-driven-zero-scores-from-swin/260909-vs5-SUMMARY.md`
when done. State explicitly that a republish is required and was NOT performed.
</output>
