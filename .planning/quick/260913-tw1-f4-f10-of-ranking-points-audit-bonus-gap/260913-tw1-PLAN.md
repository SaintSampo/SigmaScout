---
phase: 260913-tw1-f4-f10-of-ranking-points-audit-bonus-gap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/260913-tw1/bonusGapAttribution.ts
  - experiments/260913-tw1/results/season-2016.json
  - experiments/260913-tw1/results/season-2017.json
  - experiments/260913-tw1/results/season-2018.json
  - experiments/260913-tw1/results/season-2019.json
  - experiments/260913-tw1/results/season-2020.json
  - experiments/260913-tw1/results/season-2022.json
  - experiments/260913-tw1/results/aggregate.json
  - docs/models/rp-bonus-gap-attribution.md
  - .planning/todos/pending/ranking-points-audit.md
  - .planning/sketches/012-predicted-bonus-dots/
autonomous: false
requirements: [QUICK-260913-tw1]

estimate:
  tokens: 100000
  raw_tokens: 200000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "An offline probe at experiments/260913-tw1/bonusGapAttribution.ts replays seasons 2016-2020 and 2022 walk-forward through SigmaScoutLayer constructed with TWO arguments (ruleModule, 'spr'), exactly as publish.ts does, and refuses any season at or above 2023 before the corpus is opened."
    - "On every scored (match, alliance, bonus) observation, the probe's baseline equals the layer's published redBonusRp/blueBonusRp within 1e-9 (moments read via momentsFor BEFORE foldPlayed), and the probe's own generic predicate evaluator with every suspect switched off equals that baseline within 1e-12. Mismatch count is recorded and must be 0."
    - "Per in-window cell, the baseline's n, observed rate and mean predicted match data/baselines/rp-calibration-2026-09c.json (n exact, observed within 1e-12, mean predicted and Brier within 1e-9), or the mismatch is explained in writing."
    - "Each suspect is removed one at a time against the same baseline: independence (walk-forward league residual correlation), gate/selector oracle (the in-window coopertition analogue), integer shape (discrete lattice marginals, bounded where a robust cap exists), mean deficit (walk-forward running shift, fully-warm rosters only per F9). A combined arm (all four) is reported separately and labelled as not part of the attribution."
    - "Every cell reports n, mean predicted, observed, Brier, gap, share of gap closed and Brier delta per arm; results pool per suspect across the six multi-variable cells and separately across the four single-variable controls; overshoot (share > 1) and 'calibration closed but Brier worse' are flagged explicitly."
    - "docs/models/rp-bonus-gap-attribution.md states the headline first, says plainly that coopertition does not exist in 2016-2022 so it cannot be measured in-window, and gives a reproduce command; the audit's F4 decision subsection body and its STATUS 2026-09-13 F4 row are the only audit lines changed; both paths committed by explicit path."
    - "Nothing under packages/, apps/web Compare files, or scripts/measureRpCalibration.ts is edited; nothing is published; no network access."
  artifacts:
    - path: "experiments/260913-tw1/bonusGapAttribution.ts"
      provides: "walk-forward attribution probe with --selftest, --only <season>, --aggregate modes (gitignored, not committed)"
      contains: "FIRST_HOLDOUT_SEASON = 2023"
    - path: "experiments/260913-tw1/results/aggregate.json"
      provides: "pooled and per-cell results for all six in-window seasons, provenance, -09c comparison (gitignored)"
    - path: "docs/models/rp-bonus-gap-attribution.md"
      provides: "the F4 attribution write-up, committed"
      contains: "## Headline"
    - path: ".planning/todos/pending/ranking-points-audit.md"
      provides: "F4 decision subsection body and F4 STATUS row replaced with the measured result"
      contains: "rp-bonus-gap-attribution.md"
  key_links:
    - from: "experiments/260913-tw1/bonusGapAttribution.ts"
      to: "packages/harness/sigmaScoutLayer.ts"
      via: "new SigmaScoutLayer(ruleModule, algorithm.id) then layer.rpAccumulator.momentsFor BEFORE layer.foldPlayed"
      pattern: "new SigmaScoutLayer\\(ruleModule, "
    - from: "experiments/260913-tw1/bonusGapAttribution.ts"
      to: "packages/core/rankingPoints/analyticPmf.ts"
      via: "allianceBonusRpPmf(moments, ruleModule, eventType).bonusProbabilities as the reproduced baseline"
      pattern: "allianceBonusRpPmf"
    - from: "experiments/260913-tw1/results/aggregate.json"
      to: "data/baselines/rp-calibration-2026-09c.json"
      via: "per-cell baseline comparison, in-window records only"
      pattern: "rp-calibration-2026-09c"
---

<objective>
Find out why multi-variable bonus probabilities under-predict (audit F4), by measuring how much of each
in-window bonus's calibration gap each suspect accounts for when it is removed on its own. Then write the
result up, update only the audit's F4 section, ask Jacob which fix to build, and start F10 (how predicted
bonus dots should render) from that answer.

Purpose: the audit's STATUS 2026-09-13 F4 decision says "measure which cause dominates before fixing
anything". Under SPR (`-09c`) the worst bonuses almost all need several threshold variables at once, but the
same bonuses also carry bounded counts modelled as smooth Gaussians and a mean that runs low. Picking a fix
without this split would be guessing.

Scope limits (from Jacob, verbatim intent): seasons 2016-2020 and 2022 only, never 2023 or later; SPR only,
through SigmaScoutLayer; no edits to packages/, apps/web Compare files or scripts/measureRpCalibration.ts
(quick task 260913-qyn is changing them); no publish; commit by explicit path.

Output: a gitignored probe plus raw JSON under experiments/260913-tw1/, a committed
docs/models/rp-bonus-gap-attribution.md, a committed F4-only audit edit, Jacob's fix decision, and an F10
sketch with Jacob's pick.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/todos/pending/ranking-points-audit.md
@packages/core/rankingPoints/analyticPmf.ts
@packages/core/rankingPoints/marginals.ts
@packages/core/rankingPoints/empiricalMoments.ts
@packages/harness/sigmaScoutLayer.ts
@experiments/probeColdRoster.ts
@data/baselines/rp-calibration-2026-09c.json

Facts the planner verified against the tree (do not re-derive):

- Exported entry points the probe uses, all read-only imports:
  `openCorpusReadOnly` (packages/corpus/db.ts), `buildSeasonStream(db, season, { includeOffseason: true })` and
  `WalkForwardSimulator(stream).runAll([algorithm], teams)` (packages/harness/replay.ts), `SigmaScoutLayer`
  (packages/harness/sigmaScoutLayer.ts), `RP_RULE_MODULES` (packages/core/rankingPoints/rules.ts),
  `actualBonusFlagsForSeason` and `resolvePublishAlgorithms("spr")` (packages/harness/publish.ts),
  `allianceBonusRpPmf` (analyticPmf.ts, exported), `fitMarginal`, `probAtLeast`, `probAtMost`,
  `standardNormalCdf`, `poissonBinomialAtLeast` (marginals.ts), `resolveRpThreshold`, `eventTierFor`,
  `isRpEligibleEventType`, `isBonusRpCompLevel` (constants.ts).
- The scorer the `-09c` baseline came from (scripts/measureRpCalibration.ts lines 915-960): one
  `runAll([spr], teams)` per season with `includeOffseason: true`, `new SigmaScoutLayer(ruleModule, a.id)`,
  `layer.foldPlayed(r.match, r.prediction)` with no third argument, observations only where
  `actualBonusFlagsForSeason` gives a non-null entry and `redBonusRp`/`blueBonusRp` is defined with matching
  length. The probe must reproduce exactly this population.
- `experiments/probeColdRoster.ts` constructs the layer with ONE argument. Since 260913-it4 that yields NO
  ranking points at all (`publishesRankingPoints(undefined)` is false), so that probe silently scores nothing
  today. Copy its imports, not its constructor.
- `RpMomentsAccumulator.momentsFor` is read-only (no insert-on-read). `allianceBonusRpPmf` reads only the
  mean vector and diagonal variance block, never the score mean or variance, so moments read before
  `foldPlayed` with any score arguments reproduce the published bonus probabilities.
- Every in-window threshold variable declares `marginalFamily: "gaussian"`. `probAtLeast` on a Gaussian has
  no continuity correction (deliberate, see its comment).
- In-window cells. MULTI-VARIABLE (six): 2016 `breach` (countOfIndicators, position1..5crossings each gte 2,
  required 4), 2016 `capture` (conjunctionDistinct: attackedTowerEndStrength lte 0 AND
  teleopChallengePoints/5 + teleopScalePoints/15 gte 3), 2017 `kPa` (linearCombination autoFuelPoints +
  teleopFuelPoints gte 40), 2017 `rotor` (linearCombination autoRotorPoints/60 + teleopRotorPoints/40 gte 4),
  2018 `autoQuest` (conjunctionDistinct autoRunPoints gte 15 AND autoSwitchOwnershipSec gte 1, untrackedGate
  signal `autoSwitchAtZero`, errorDirection overstates), 2022 `cargoBonus` (dataDependentMixture: selector
  autoCargoTotal gte 5 picks matchCargoTotal gte 18, else gte 20). SINGLE-VARIABLE CONTROLS (four): 2018
  `faceTheBoss`, 2019 `habDocking`, 2020 `shieldOperational`, 2022 `hangarBonus`. CONSTANT (reference only):
  2019 `completeRocket`.
- `autoSwitchAtZero` IS in the corpus breakdown: `Rp2018Schema` requires it, and 2018 `parse` computes the
  actual flag as `autoRunPoints >= 15 && autoSwitchAtZero`. 2022 `parse` computes quintet as
  `autoCargoTotal >= 5`; the raw breakdown also carries TBA's `quintetAchieved` boolean.
- Corpus shape peek (qm, all 2016-2022 rows, both sides): every in-window variable is integer-valued. Lattice
  steps: teleopChallengePoints 5, teleopScalePoints 15, autoRotorPoints 60, teleopRotorPoints 40,
  autoRunPoints 5, 2018/2020 endgamePoints 5, habClimbPoints 3, everything else 1. RAW maxima are polluted by
  rare anomalies: position crossings reach 3 in 0.01% of rows although 2 is the working ceiling;
  towerEndStrength ranges -14 to 12; autoFuelPoints has a -1. So caps MUST be derived robustly (share-based),
  never from the raw max. Shares at the working ceiling: autoRunPoints 15 at 68.6%, teleopChallengePoints 15
  at 28.2%, 2018 endgamePoints 90 at 9.3%, teleopRotorPoints 160 at 1.6%.
- Timing hazard: `-09c` was measured 2026-09-13T22:52Z with spr 3.0.0; commit b8eb402e (spr@4.0.0 demo
  exclusion, spr.ts only) landed at 23:29Z. The bonus path does not read SPR's ratings, so `-09c` should
  still reproduce. If it does not, that commit is the first suspect.
- Concurrency hazard: at planning time packages/harness/publish.ts, packages/harness/pageArtifacts.ts and
  scripts/measureRpCalibration.ts had UNCOMMITTED edits from another session (260913-qyn), which also plans
  to edit analyticPmf.ts, sigmaScoutLayer.ts and this same audit file.
- House style for docs/models: docs/models/rp-mean-deficit-warm-rosters.md and docs/models/rp-attribution.md
  ("Headline verdict — read this first", "What was measured", slice tables, "What this document does not
  say", "Reproducing this record").
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — the attribution probe end-to-end on 2018 (one season, every layer, every arm)</name>
  <files>experiments/260913-tw1/bonusGapAttribution.ts, experiments/260913-tw1/results/season-2018.json, experiments/260913-tw1/results/aggregate.json</files>
  <read_first>
    - packages/harness/sigmaScoutLayer.ts (foldPlayed ordering, #rpFieldsFor, #foldObservedThresholds)
    - packages/core/rankingPoints/analyticPmf.ts lines 282-400 (clauseProbability, bonusProbability) and 584-630 (allianceBonusRpPmf)
    - packages/core/rankingPoints/marginals.ts lines 160-235 and 300-420
    - packages/core/rankingPoints/2018.ts (predicates, untrackedGate, parse)
    - scripts/measureRpCalibration.ts lines 915-960 ONLY (read, never edit) for the exact scored population
    - data/baselines/rp-calibration-2026-09c.json
  </read_first>
  <action>
Build ONE TypeScript file, experiments/260913-tw1/bonusGapAttribution.ts, run with `npx tsx` from the repo
root. experiments/ is gitignored by convention; do not force-add anything under it. Write it with the Write
tool. This task wires the whole path for season 2018 only (singleThreshold + conjunctionDistinct + the gate),
so the reproduction proof and the -09c match are established before any heavier machinery is added.

MODES (the only CLI surface): `--selftest` (numeric checks, no corpus), `--only <season>` (one season,
writes results/season-<season>.json), `--aggregate` (reads every results/season-*.json, writes
results/aggregate.json, prints tables). No other flags. No `--seasons` flag and no range parsing.

SEASONS GUARD (hard, first thing in main, before openCorpusReadOnly): a literal
`const IN_WINDOW_SEASONS = [2016, 2017, 2018, 2019, 2020, 2022] as const` and
`const FIRST_HOLDOUT_SEASON = 2023`. `assertInWindow(season)` throws an Error whose message begins with
`REFUSED:` if the season is not in the list OR is at or above FIRST_HOLDOUT_SEASON. Call it on the parsed
`--only` value before the corpus opens, and again immediately before the single `buildSeasonStream` call
site. Log `corpus opened` only after openCorpusReadOnly returns. Keep every season literal at or above the
holdout constant out of the file entirely, comments included. In `--aggregate`, filter the -09c records to
IN_WINDOW_SEASONS before reading any figure, and never copy later-season figures into any output.

PROVENANCE (recorded in each season JSON at start and again at finish): `git rev-parse HEAD`,
`git rev-parse HEAD:packages`, sha256 of `git diff HEAD -- packages` (via child_process execSync plus
node:crypto; local git only, no network), node version, the SPR algorithm id and version, startedAt and
finishedAt. If the start and finish values differ, set `provenanceChangedMidRun: true` and exit non-zero.

REPLAY LOOP (mirror measureRpCalibration.ts exactly): open data/corpus.sqlite read-only; stream with
includeOffseason true; teams from the stream; `resolvePublishAlgorithms("spr")[0]`; records from
`runAll([algorithm], teams)`; `actualBonusFlagsForSeason(stream, season)`;
`new SigmaScoutLayer(ruleModule, algorithm.id)` (TWO arguments). For each record in order:
  1. PREDICT. If the match has a non-null actual-flags entry: for each side read
     `moments = layer.rpAccumulator.momentsFor(roster, prediction score for that side, 1)` and
     `fullyWarm = roster.every(t => layer.rpAccumulator.hasHistory(t))`, BEFORE foldPlayed. Compute
     `base = allianceBonusRpPmf(moments, ruleModule, match.eventType).bonusProbabilities`. Compute every
     arm for both sides from the running statistics as they stand now.
  2. FOLD. `enriched = layer.foldPlayed(r.match, r.prediction)` for EVERY record, scored or not.
  3. ASSERT (finding 3). Skip the side exactly as the scorer does when `redBonusRp`/`blueBonusRp` is
     undefined or its length differs from the flags (count skips). Otherwise assert
     |base[i] - enriched bonus[i]| <= 1e-9 for every i; count mismatches and track the max absolute diff. Also
     assert the probe's own generic evaluator with all suspects off equals base[i] within 1e-12 (count
     separately). If either count is non-zero at season end, write the JSON with the counts and exit non-zero.
  4. SCORE. Push (predicted per arm, actual) into per-cell accumulators. Keep raw sums per arm (sum
     predicted, sum squared error, n, sum actual) so pooling across seasons is exact.
  5. UPDATE, only after both sides are predicted. Parse the raw breakdown with `ruleModule.parse(raw, side,
     eventType)` for observed threshold variables (and `JSON.parse(raw)[side]` for the gate boolean); a
     parse throw skips that side's update (count it). Update the running correlation and mean-deficit
     statistics from this match.

GENERIC PREDICATE EVALUATOR. One function evaluates a BonusPredicate from a per-variable "arm marginal
provider" plus flags, mirroring analyticPmf.ts semantics: singleThreshold and single unscaled term reuse the
variable's marginal; multi-term or divisor clauses sum mean/divisor and variance/divisor-squared left to
right; conjunctionDistinct is a product; countOfIndicators is poissonBinomialAtLeast; dataDependentMixture is
sel*T + (1-sel)*F; constant is 0 or 1; thresholds via resolveRpThreshold(threshold, eventTierFor(eventType)).
With all suspects off it must reproduce allianceBonusRpPmf (the 1e-12 assertion). Implement every in-window
kind now, even though this task only exercises 2018's.

SUPPORT DERIVATION (for the shape arm), one pre-pass per season over the scored population's parsed
observations, per variable: lattice step s = gcd of the non-zero values that each hold at least 0.1% of
observations; robustMin = the smallest value v with share(X <= v) >= 1%; robustMax = the largest value v with
share(X >= v) >= 1%; n = (robustMax - robustMin)/s. Treatment: "beta-binomial" when n <= 30, otherwise
"discretized-gaussian". This uses whole-season data. Record that as a disclosed look-ahead in the JSON: these
are rule facts (lattice steps and ceilings), not fitted parameters.

ARMS. Each changes exactly one thing from baseline:
  - BASELINE: base (verified above).
  - INDEPENDENCE: a walk-forward league-pooled Pearson correlation of standardized residuals
    z_v = (observed_v - predicted mean_v) / predicted sd_v, updated from prior scored alliance observations
    of the season only, skipping any observation where either sd is 0 (count per variable). Zero correlation
    until a pair has 500 prior joint observations (CORRELATION_WARMUP_PAIRS = 500). For 2018 autoQuest, the
    probability is the bivariate-normal upper orthant P(X >= a, Y >= b) with that correlation. Implement the
    bivariate normal CDF with Genz's (2004) BVNU algorithm (Drezner-Wesolowsky Gauss-Legendre form); do not
    add a dependency. Clamp |rho| to 0.999 and count clamps. Single-variable cells are unchanged by
    construction; assert equality with baseline.
  - GATE ORACLE (coopertition analogue): 2018 autoQuest becomes P(autoRunPoints >= 15) times the actual
    `autoSwitchAtZero` boolean from the raw breakdown. Label it "gate/selector oracle (coop analogue)". Record
    in the JSON that this gate is one of autoQuest's own two clauses, so this is an upper bound that conditions
    on half the bonus's outcome.
  - SHAPE: every term becomes a discrete pmf on its lattice. For beta-binomial: m = (mean - robustMin)/s,
    v = variance/s^2, p = m/n. A point mass at the nearer end when m <= 0 or m >= n (count "mean-outside-
    support"). Binomial when v <= n p (1-p) (count "binomial-fallback"). Otherwise
    rho_bb = (v/(n p (1-p)) - 1)/(n - 1), clamped to 0.999 (count), alpha = p(1/rho_bb - 1),
    beta = (1-p)(1/rho_bb - 1), pmf via log-gamma (Lanczos). For discretized-gaussian: mass on each lattice
    point k*s is the normal mass on [k*s - s/2, k*s + s/2) over +/- 8 sd, renormalized. A term whose baseline
    marginal is degenerate (variance 0) keeps the baseline point mass at its real mean (count it). A clause is
    the exact tail of the convolution of its term pmfs after dividing each term by its divisor; lte uses the
    lower tail. Also emit a diagnostic arm SHAPE_CC_ONLY that uses discretized-gaussian for every term, so
    continuity correction and bounded support can be told apart.
  - MEAN DEFICIT (control): per variable, the running mean of (observed - predicted mean) over prior scored
    observations whose roster was fully warm; shift predicted means by it ONLY on fully-warm rosters. Partial
    rosters are left unshifted, per the audit's F9 decision (Jacob: cold robots are treated as contributing
    nothing to RP, and known robots still predict it). Zero shift until 200 prior fully-warm observations
    (MEAN_DEFICIT_WARMUP = 200). Variances are unchanged. Record the share of observations shifted.
  - COMBINED (all four together; reported separately and NOT part of the attribution): the shape arm's
    discrete marginals built from mean-shifted moments, joined by a Gaussian copula at the independence arm's
    correlation, with the gate oracle applied as CONDITIONING. For autoQuest: 0 when the gate is false,
    otherwise P(R >= 15, S >= 1)/P(S >= 1), with both tails from the copula (a bivariate orthant at cut points
    Phi^-1 of the discrete lower-tail probabilities).

METRICS per cell and arm: n, mean predicted, observed rate, Brier, gap = observed - mean predicted,
shareClosed = (arm mean - base mean)/(observed - base mean), brierDelta = arm Brier - base Brier. Flags:
`overshoot` when shareClosed > 1; `calibrationClosedBrierWorse` when shareClosed > 0 and brierDelta > 0. Also
record: final correlation matrix per multi-variable footprint with pair counts, final mean deficits, the
support table, and every counter named above. The verify commands read these exact top-level season keys:
`reproductionMismatches`, `reproductionMaxAbsDiff`, `evaluatorMismatches`, `provenanceChangedMidRun`,
`provenance`, `support`, `correlations`, `meanDeficits`, `counters`, `cells`. The aggregate keys are
`missingSeasons`, `baseline09c` (rows with `season`, `bonus`, boolean `match`, and the diffs),
`baseline09cExplanation` (null unless needed), `pooled.multiVariable`, `pooled.controls`, `pooled.gatedCells`,
`referenceRows`, and `provenanceWarnings`.

AGGREGATE (built now so the tracer reaches the far end): read every present results/season-*.json; mark
missing in-window seasons in `missingSeasons` (allowed here, not in Task 2). Compare each present cell's
baseline to -09c (n exact; observed within 1e-12; mean predicted and Brier within 1e-9) and write
`baseline09c` rows with diffs and a `mismatchCount`. Pool raw sums per suspect across multi-variable cells,
and separately across controls, observation-weighted. Also pool the gate arm over "cells where a gate
exists". Report completeRocket as a reference row EXCLUDED from both pools, because its oracle equals the
outcome by construction. Print a compact text table: cell, n, observed, base mean, and shareClosed and
brierDelta per arm. Warn (do not fail) when seasons carry different `HEAD:packages` or diff hashes. The -09c
match is the binding check for baseline drift.

SELFTEST (`--selftest`, exits non-zero on any failure):
  - BVNU(0, 0, rho) equals 1/4 + asin(rho)/(2*pi) within 1e-10 for rho in {-0.9, -0.5, 0, 0.5, 0.9}.
  - BVNU at rho 0 equals the product of the univariate tails within 1e-12 on a small grid.
  - The beta-binomial pmf sums to 1 within 1e-12 and hits the target mean and variance within 1e-9 when
    unclamped.
  - The discretized-gaussian single-term tail equals 1 - Phi((ceil-to-lattice(t) - s/2 - mean)/sd) within 1e-9.
  - The generic evaluator reproduces allianceBonusRpPmf on hand-built diagonal moments for every in-window
    rule module within 1e-12.

Run order: selftest, then the guard refusal check, then `--only 2018` (Bash timeout 600000), then
`--aggregate`. If an import of packages/harness/publish.ts fails because of another session's uncommitted
edit, STOP and report it. Never edit packages/ to work around it.
  </action>
  <verify>
    <automated>cd C:/Users/Jacob/Documents/GitHub/SigmaScout && npx tsx experiments/260913-tw1/bonusGapAttribution.ts --selftest && { out=$(npx tsx experiments/260913-tw1/bonusGapAttribution.ts --only 2023 2>&1); code=$?; [ $code -ne 0 ] && echo "$out" | grep -q "REFUSED" && ! echo "$out" | grep -q "corpus opened" && echo "guard OK"; } && npx tsx experiments/260913-tw1/bonusGapAttribution.ts --only 2018 && npx tsx experiments/260913-tw1/bonusGapAttribution.ts --aggregate && node -e "const s=require('./experiments/260913-tw1/results/season-2018.json');const a=require('./experiments/260913-tw1/results/aggregate.json');if(s.reproductionMismatches!==0||s.evaluatorMismatches!==0)throw new Error('reproduction');if(s.provenanceChangedMidRun)throw new Error('provenance');const m=a.baseline09c.filter(r=>r.season===2018);if(m.length!==2||a.baseline09c.some(r=>r.season===2018&&!r.match))throw new Error('09c 2018');console.log('tracer OK')" && [ "$(grep -vE '^\s*(//|\*|/\*)' experiments/260913-tw1/bonusGapAttribution.ts | grep -cE '\b20(2[4-9])\b')" = "0" ] && grep -q "new SigmaScoutLayer(ruleModule, " experiments/260913-tw1/bonusGapAttribution.ts && git status --porcelain -- packages scripts/measureRpCalibration.ts apps/web/src/components/compare</automated>
  </verify>
  <done>--selftest passes. `--only 2023` exits non-zero with REFUSED and never opens the corpus. season-2018.json shows 0 reproduction mismatches and 0 evaluator mismatches over every scored observation. Both 2018 cells (autoQuest, faceTheBoss) match -09c within the stated tolerances. aggregate.json holds per-cell and pooled shareClosed/brierDelta for all arms. The final git status line shows no change to packages/, scripts/measureRpCalibration.ts or Compare files that was introduced by this task; any pre-existing dirty entries from the other session are listed unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the probe to all six in-window seasons (linear combinations, 5-indicator MVN, mixture) and run it</name>
  <files>experiments/260913-tw1/bonusGapAttribution.ts, experiments/260913-tw1/results/season-2016.json, experiments/260913-tw1/results/season-2017.json, experiments/260913-tw1/results/season-2019.json, experiments/260913-tw1/results/season-2020.json, experiments/260913-tw1/results/season-2022.json, experiments/260913-tw1/results/aggregate.json</files>
  <read_first>
    - packages/core/rankingPoints/2016.ts, 2017.ts, 2019.ts, 2020.ts, 2022.ts (predicates and parse only)
    - experiments/260913-tw1/results/season-2018.json (the tracer's output shape, to keep it identical)
  </read_first>
  <action>
Extend the arms to the remaining predicate shapes. Keep the season JSON shape identical to the tracer's.

INDEPENDENCE ARM, remaining shapes (correlations from the same walk-forward residual statistic, warm-up and
clamp rules as Task 1):
  - linearCombination (2017 kPa, rotor): Gaussian sum with variance sum_i var_i/d_i^2 +
    2 sum_{i<j} rho_ij sd_i sd_j/(d_i d_j); same no-continuity-correction tail as baseline.
  - 2016 capture: clause 1 is the single variable T (attackedTowerEndStrength, lte 0). Clause 2 is the sum S
    = challenge/5 + scale/15, with its variance including rho(challenge, scale). corr(T, S) =
    (rho_TC sd_T sd_C/5 + rho_TS sd_T sd_S/15)/(sd_T sd_S). Probability = P(T <= 0, S >= 3) via BVNU with
    the sign flip for the lte clause.
  - 2022 cargoBonus: P(A >= 5, M >= 18) + P(A < 5, M >= 20) with rho(A, M) via BVNU (A = autoCargoTotal,
    M = matchCargoTotal).
  - 2016 breach (5 indicators, no closed form): seeded Monte Carlo over a 5-dim standard normal with the
    fitted 5x5 correlation R. Indicator i fires when Z_i > Phi^-1(1 - q_i), where q_i is that position's
    baseline Gaussian tail. Count fires >= required. Use common random numbers and a control variate: the
    estimate is the exact Poisson-binomial baseline plus (MC at R minus MC at identity, same draws). Seed per
    observation with fnv1a32 of `${matchKey}:${side}:${bonus}:${arm}` into a small deterministic PRNG
    (mulberry32 or xoshiro128**), with Box-Muller normals. K_DRAWS = 4000. PSD repair: attempt Cholesky; on
    failure shrink off-diagonals by 0.95 until it succeeds; count repairs and record the minimum shrink
    factor. Record the pooled MC standard error of the cell's mean predicted, and the Brier noise floor
    mean(p(1-p))/K. If the pooled MC SE is not below one tenth of |arm mean - base mean| for that cell, rerun
    that season with K_DRAWS = 16000 and record which K was used.

GATE ORACLE, remaining cells: 2022 cargoBonus becomes 1{actual autoCargoTotal >= 5} * P(M >= 18) +
(1 - 1{...}) * P(M >= 20), using the rule module's own quintet definition. Record agreement with TBA's
`quintetAchieved` field as an informational count. 2019 completeRocket becomes its actual flag (reference
row only, excluded from pools, oracle share 1 by construction). Every other cell is unchanged; assert
equality with baseline.

SHAPE ARM, remaining shapes: the Task 1 term-pmf convolution already covers linear combinations (rotor
terms become 0..2 and 0..4 rotors after the divisor, capture terms 0..3 robots each) and the lte clause.
countOfIndicators uses each indicator's discrete tail through poissonBinomialAtLeast. The mixture uses the
discrete tails of A and M. The support table in each season JSON must show, per variable: s, robustMin,
robustMax, n and treatment. Expect position crossings to resolve to [0, 2] (the rare 3 must fall outside
the robust range); if it does not, stop and report rather than hand-setting it.

MEAN DEFICIT ARM: unchanged rule. It applies to every cell, controls included.

COMBINED ARM, remaining multi-variable cells, all through the Gaussian copula with the shape arm's
mean-shifted discrete marginals and the fitted correlation:
  - breach: the same control-variate MC as the independence arm, with the discrete indicator tails as q_i
    (the identity-R comparator is the exact Poisson-binomial of those tails).
  - kPa, rotor, capture: plain seeded MC. Draw correlated normals, map each term through its discrete
    marginal's quantile function, divide, sum and compare. Use a control variate against identity
    correlation, whose exact expectation is the shape arm's independent convolution.
  - cargoBonus: conditioning on the observed selector, so 1{q} P(M >= 18, A >= 5)/P(A >= 5) +
    (1 - 1{q}) P(M >= 20, A < 5)/P(A < 5), as copula orthants.
  - Controls: combined equals shape plus mean shift in closed form.

SELFTEST additions: the MVN count at R = identity equals poissonBinomialAtLeast within 4 SE. The copula MC
for a 2-variable conjunction with Gaussian marginals equals BVNU within 4 SE. The independence linear-sum at
rho 0 equals baseline within 1e-12.

RUN: `--selftest`, then `--only` for each of 2016, 2017, 2019, 2020 and 2022 as separate invocations
(Bash timeout 600000 each). Re-run 2018 only if the evaluator changed in a way that touches 2018's arms.
Then `--aggregate`. Every in-window season must now be present (`missingSeasons` empty; aggregate exits
non-zero otherwise).

-09c DISCREPANCY RULE: if any in-window cell misses tolerance, do not proceed to Task 3 silently.
Investigate in this order: (1) is the provenance different between that season's run and the others (if so,
rerun the season); (2) was `includeOffseason` or the skip condition mirrored exactly; (3) commit b8eb402e.
Record the finding in aggregate.json as `baseline09cExplanation` and carry it into the doc. Scorer
differences have fooled comparisons here before, so name the exact mechanism, not a guess.

Return in the final message a compact per-cell table (shareClosed and brierDelta per arm, including
SHAPE_CC_ONLY and COMBINED) plus the two pooled rows. The orchestrator forms Jacob's question from it without
re-reading the JSON.
  </action>
  <verify>
    <automated>cd C:/Users/Jacob/Documents/GitHub/SigmaScout && npx tsx experiments/260913-tw1/bonusGapAttribution.ts --selftest && npx tsx experiments/260913-tw1/bonusGapAttribution.ts --aggregate && node -e "const a=require('./experiments/260913-tw1/results/aggregate.json');const want=[2016,2017,2018,2019,2020,2022];for(const y of want){const s=require('./experiments/260913-tw1/results/season-'+y+'.json');if(s.reproductionMismatches!==0||s.evaluatorMismatches!==0)throw new Error('reproduction '+y);if(s.provenanceChangedMidRun)throw new Error('provenance '+y)}if(a.missingSeasons.length)throw new Error('missing '+a.missingSeasons);if(a.baseline09c.length!==11)throw new Error('expected 11 in-window cells, got '+a.baseline09c.length);if(a.baseline09c.some(r=>!r.match)&&!a.baseline09cExplanation)throw new Error('unexplained 09c mismatch');if(a.baseline09c.some(r=>r.season>=2023))throw new Error('holdout leak');console.log('aggregate OK',JSON.stringify(a.pooled?.multiVariable?.shareClosed??a.pooled))" && [ "$(grep -vE '^\s*(//|\*|/\*)' experiments/260913-tw1/bonusGapAttribution.ts | grep -cE '\b20(2[4-9])\b')" = "0" ]</automated>
  </verify>
  <done>All six in-window season JSONs exist with 0 reproduction and 0 evaluator mismatches. aggregate.json has 11 in-window cells, an empty missingSeasons, and either every -09c row matching or a named baseline09cExplanation. Pooled rows exist for multi-variable cells and for controls, per suspect, with overshoot and calibration-closed-but-Brier-worse flags. Breach's MC SE rule is satisfied, with K recorded. No later-season figure appears anywhere in the outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/models/rp-bonus-gap-attribution.md, replace only the audit's F4 decision body and F4 STATUS row, commit both by explicit path</name>
  <files>docs/models/rp-bonus-gap-attribution.md, .planning/todos/pending/ranking-points-audit.md</files>
  <read_first>
    - experiments/260913-tw1/results/aggregate.json
    - docs/models/rp-mean-deficit-warm-rosters.md (tone and section order)
    - .planning/todos/pending/ranking-points-audit.md sections "### F4 —", "## STATUS 2026-09-13" to end of file (RE-READ immediately before editing; another session edits this file)
  </read_first>
  <precondition>`git diff --quiet -- .planning/todos/pending/ranking-points-audit.md` succeeds, meaning no uncommitted foreign edits sit in the audit. If it fails, STOP and report. Do not commit another session's edits.</precondition>
  <action>
DOC. Create docs/models/rp-bonus-gap-attribution.md with the Write tool (never a heredoc; long markdown
breaks Git Bash heredocs on this machine). Plain language, short sentences, figures copied from
aggregate.json only. Sections in this order:
  1. Title: "F4 — which cause explains the multi-variable bonus gap (260913-tw1, 2026-09-13)".
  2. "## Headline — read this first": one paragraph naming the suspect that closes the most pooled gap on the
     multi-variable cells, its share and its Brier delta; then the same for the controls; then how much gap
     the combined arm leaves unexplained, labelled as not part of the one-at-a-time attribution. Name any
     overshoot or calibration-closed-but-Brier-worse result here, not only in a table.
  3. "## What was measured": SPR through SigmaScoutLayer (two-argument construction, same scorer as `-09c`);
     2016-2020 and 2022 only; walk-forward, predict before update; the four suspects and exactly how each was
     removed; the warm-up constants, K and seeds; the reproduction proof (0 mismatches over N observations at
     1e-9); the -09c match or its named explanation; provenance (HEAD and packages tree hash).
  4. "## Coopertition cannot be measured in this window": coopertition first appears in 2023. It does not
     exist in 2016-2022, so the coop branch itself was not measured, and nothing from 2023 or later was read
     to get it. What was run instead is the gate/selector oracle (coop analogue): 2022 cargoBonus's quintet
     selector and 2018 autoQuest's autoSwitchAtZero gate. Say that autoQuest's gate is one of its own clauses,
     so its oracle is an upper bound and not comparable with cargoBonus's.
  5. "## Every cell": one table per suspect or one wide table, with n, observed, base mean predicted, and for
     each arm mean predicted, shareClosed and brierDelta. Add SHAPE_CC_ONLY as a diagnostic column. Put
     completeRocket in a separate reference row, marked excluded from pools.
  6. "## Pooled": multi-variable pool and control pool per suspect (observation-weighted), plus the gate arm
     pooled over gated cells only.
  7. "## Supporting measurements": the fitted residual correlation matrices per season (final values, pair
     counts, PSD repairs), the support table (lattice step, robust range, treatment), the final mean deficits
     and share of observations shifted, and the MC SE and Brier noise floor for the MC arms.
  8. "## What this does not show": the coop branch (above); in-window results may not transfer to the later
     coopertition-era bonuses the audit names (coralBonus, autoBonus, ensembleBonus), which have different
     predicate shapes and a relaxed branch; the support derivation uses whole-season rule facts (disclosed
     look-ahead); the mean-deficit arm deliberately leaves partial rosters unshifted (F9); a share is
     calibration-in-the-large only, so a share gain with a worse Brier is not a fix; the combined arm is not
     an attribution. Also state that nothing published changed. Do not describe negative binomial as not
     helping: its retest found a real improvement and was declined on cost.
  9. "## Fix candidates the evidence supports": at most four, each one line on what it would change
     (which file in packages/core/rankingPoints it would touch) and its measured pooled share and Brier delta
     from this probe. Include the constraint that any new knob is inert at its default and must earn
     promotion on the 2016-2022 selection slice. End with "Decision: pending, asked 2026-09-13."
 10. "## Reproducing this record": the exact commands (`npx tsx experiments/260913-tw1/bonusGapAttribution.ts
     --selftest`, one `--only <season>` per in-window season, `--aggregate`). Note that experiments/ is
     gitignored, so the probe lives at that path in the working tree, and give the HEAD it ran against.

AUDIT EDIT (Edit tool, scoped replacements only, after re-reading the file):
  - In the "## STATUS 2026-09-13" table, replace ONLY the row that starts `| **F4**` with:
    `| **F4** dependence between threshold variables | **MEASURED 2026-09-13, fix decision pending.** <one
    sentence: dominant suspect and its pooled share on multi-variable cells>. See
    docs/models/rp-bonus-gap-attribution.md |`. The STATUS 2026-09-12 table's F4 row stays untouched.
  - Under the heading "### F4 decision — measure which cause dominates before fixing anything", keep the
    heading line and replace ONLY its body (up to the next heading, or end of file) with: a two-sentence
    result; a 4-row pooled table (suspect, multi-variable share closed, multi-variable Brier delta, control
    share closed); one line on coop being unmeasurable in-window with the analogue named; one line pointing
    to the doc; and the line "**Fix decision:** pending, asked 2026-09-13."
  - Change nothing else in the file.

COMMIT: `git add docs/models/rp-bonus-gap-attribution.md .planning/todos/pending/ranking-points-audit.md`
(explicit paths only, never -A or .), then check `git diff --cached --name-only` lists exactly those two
paths. Commit with the message `docs(260913-tw1): attribute the multi-variable bonus gap to its causes (F4)`,
ending with the trailer `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`. Then run
`git status` and confirm both paths are clean and no other path was absorbed.

Do NOT write SUMMARY.md (subagent Write block). Return the summary text, the commit hash and the Task 2
compact results table in the final message.
  </action>
  <verify>
    <automated>cd C:/Users/Jacob/Documents/GitHub/SigmaScout && test -f docs/models/rp-bonus-gap-attribution.md && grep -q "## Headline" docs/models/rp-bonus-gap-attribution.md && grep -q "Coopertition cannot be measured" docs/models/rp-bonus-gap-attribution.md && grep -q "bonusGapAttribution.ts --aggregate" docs/models/rp-bonus-gap-attribution.md && C=$(git log -1 --format=%H -- docs/models/rp-bonus-gap-attribution.md) && [ -n "$C" ] && C="$C" node -e "const cp=require('child_process');const c=process.env.C;const p='.planning/todos/pending/ranking-points-audit.md';const strip=s=>{const st=s.indexOf('## STATUS 2026-09-13');const r=s.indexOf('| **F4**',st);if(st<0||r<0)throw new Error('F4 STATUS row not found');const e=s.indexOf('\n',r);const t=s.slice(0,r)+s.slice(e);const i=t.indexOf('### F4 decision');return i<0?t:t.slice(0,i)};const before=cp.execSync('git show '+c+'^:'+p).toString();const after=cp.execSync('git show '+c+':'+p).toString();if(strip(before)!==strip(after))throw new Error('audit changed outside the F4 row and F4 decision body');if(!after.includes('### F4 decision'))throw new Error('F4 heading lost');if(!after.includes('rp-bonus-gap-attribution.md'))throw new Error('no doc pointer');console.log('audit edit scoped to F4')" && [ "$(git show --name-only --format= "$C" | grep -v '^$' | sort | tr '\n' ' ')" = ".planning/todos/pending/ranking-points-audit.md docs/models/rp-bonus-gap-attribution.md " ] && git status --porcelain -- docs/models/rp-bonus-gap-attribution.md .planning/todos/pending/ranking-points-audit.md | wc -l | grep -qx 0 && echo "commit OK"</automated>
  </verify>
  <done>The doc exists with headline-first structure, the coop-unmeasurable section, per-cell and pooled tables, the not-shown section, fix candidates ending "Decision: pending", and reproduce commands. The audit differs from its previous commit only in the STATUS 2026-09-13 F4 row and the F4 decision body. HEAD's commit contains exactly those two paths, and both are clean in git status.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Jacob picks the F4 fix, then the F10 bonus-dot sketch (orchestrator, main context)</name>
  <decision>Stage A: which F4 fix to build, if any. Stage B, after A is answered: how predicted bonus dots should render given that fix.</decision>
  <context>
Stage A. The orchestrator asks with AskUserQuestion, with plain-language background immediately before the
options, and the options ordered by measured pooled share closed on the multi-variable cells (from Task 2's
table). Each option names what it changes, its pooled share and Brier delta, and whether it overshoots or
worsens Brier. Every fix is a new knob that must be inert at its default and earn promotion on the 2016-2022
selection slice, and the later reporting slice stays unspent. Once Jacob answers, the orchestrator records the
answer by replacing "**Fix decision:** pending, asked 2026-09-13." in both the audit's F4 decision body and the
doc's "Decision: pending" line (Edit tool, those two lines only), and commits those two paths explicitly.

Stage B (F10, keep short). Load Skill("sketch-findings-sigmascout") first. Read apps/web/src/lib/bonusRp.ts
(`PREDICTED_BONUS_THRESHOLD = 0.5`, the earned/missed mapping) read-only. Build .planning/sketches/
012-predicted-bonus-dots/ with 2-3 throwaway HTML variants that show predicted bonus dots at the probability
levels the chosen fix is expected to produce (use this probe's arm means for the chosen suspect, not the
published baseline). Candidates to consider: the 0.5 solid/hollow split kept; a graded fill by probability
bucket; a percentage label beside a neutral dot. Screenshot each variant, then ask Jacob with AskUserQuestion.
Do not edit apps/web source, Compare files or packages/. Commit the sketch directory and its MANIFEST.md
entry by explicit path.
  </context>
  <options>
    <option id="fix-top-suspect">
      <name>Build the fix for the suspect with the largest pooled share (named from Task 2's table)</name>
      <pros>Targets the measured dominant cause. Smallest change that moves the most gap.</pros>
      <cons>Leaves the remainder the combined arm shows. May not transfer to coopertition-era bonuses.</cons>
    </option>
    <option id="fix-top-two">
      <name>Build the two largest suspects as separate knobs, each earning promotion on its own</name>
      <pros>Closes more of the gap if both are real and neither overshoots.</pros>
      <cons>Two measurements and two promotions. Only valid if their shares add without overshoot.</cons>
    </option>
    <option id="fix-none">
      <name>Build nothing yet</name>
      <pros>No change if no suspect closes gap with a better Brier.</pros>
      <cons>F10 stays blocked on under-predicted probabilities.</cons>
    </option>
  </options>
  <resume-signal>Stage A: select fix-top-suspect, fix-top-two, fix-none, or name a specific suspect. Stage B: pick a sketch variant or describe changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe to corpus | Read-only SQLite access; the only data risk is reading a sealed season |
| probe to packages/ | Read-only imports of code another session is actively editing |
| executor to git | Shared checkout with concurrent sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-tw1-01 | Information disclosure | seasons guard in bonusGapAttribution.ts | high | mitigate | Literal IN_WINDOW_SEASONS list and FIRST_HOLDOUT_SEASON constant; assertInWindow runs before openCorpusReadOnly and before the single buildSeasonStream call; no range or seasons flag; runtime refusal test plus the filtered grep for later-season literals; -09c records filtered to in-window before use |
| T-tw1-02 | Tampering | baseline drift from concurrent packages/ edits | medium | mitigate | Per-observation 1e-9 reproduction against the layer's own output; HEAD, HEAD:packages and diff hash recorded at start and finish of every season run; -09c per-cell match is the binding cross-season check |
| T-tw1-03 | Tampering | shared audit file and git index | medium | mitigate | Precondition that the audit has no uncommitted foreign edits; re-read before Edit; the scoped-diff node check; explicit-path staging; the commit must contain exactly two paths |
| T-tw1-04 | Information disclosure | .env secrets | low | accept | The probe needs no credentials and runs no network; .env is never read |
</threat_model>

<verification>
- `npx tsx experiments/260913-tw1/bonusGapAttribution.ts --selftest` passes.
- The guard refuses 2023 before the corpus opens; no later-season literal sits in the probe's code lines.
- All six season JSONs: reproductionMismatches 0, evaluatorMismatches 0, provenanceChangedMidRun false.
- aggregate.json: 11 in-window cells, -09c matched or explained by a named mechanism, pooled multi-variable and control rows per suspect, flags present.
- The doc is committed. The audit diff is confined to the F4 STATUS 2026-09-13 row and the F4 decision body. The commit contains exactly two paths.
- `git status --porcelain -- packages scripts/measureRpCalibration.ts apps/web/src/components/compare` shows no entry introduced by this plan.
</verification>

<success_criteria>
Jacob can see, per in-window bonus and pooled, how much of the under-prediction gap each suspect accounts
for when removed alone, with the Brier cost of each. The coop limitation is stated rather than worked around
with sealed data. The audit's F4 section points at the measured result. Jacob has picked which fix to build,
and F10 has a sketch and a pick grounded in the probabilities that fix would produce.
</success_criteria>

<output>
The executor does NOT write SUMMARY.md. It returns the summary text, the Task 3 commit hash, and the compact
per-cell and pooled results table to the orchestrator, which writes
`.planning/quick/260913-tw1-f4-f10-of-ranking-points-audit-bonus-gap/260913-tw1-SUMMARY.md` and appends the
Quick Tasks row with the patched `quick-tasks-append --commit <hash>`.
</output>
