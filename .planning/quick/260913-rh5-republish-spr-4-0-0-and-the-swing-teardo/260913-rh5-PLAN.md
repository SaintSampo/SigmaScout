---
phase: 260913-rh5-republish-spr-4-0-0-and-the-swing-teardo
plan: 01
type: execute
autonomous: false
requirements: [QUICK-260913-rh5]
---

# Quick task 260913-rh5: republish and the owed pre/post steps

Run from the main context. Executor subagents have no network, and every step here needs it.
Jacob chose "republish now" (AskUserQuestion). A concurrent session's 260913-qyn will need its own
republish later.

## What this republish carries

- spr@4.0.0+baseline: the demo and placeholder-key exclusion (todo `republish-spr-4-demo-exclusion`)
- 260913-it4: OPR/EPA stop publishing ranking-point odds (Swing teardown)
- 260913-jkp: the Sigma columns change (its republish was owed)
- 260913-m45: per-match Sigma on SPR metric-history rows
- 260913-nvn: the bounded upload queue at concurrency 48, and `--write-budget`

## Tasks

1. **Pre: ingest.** `pnpm ingest --year 2026`, run detached, so this weekend's offseason results
   (2026mogw, 2026mifli1/2) are in the corpus. Verify it by row counts, not by exit code.
2. **Publish.** Confirm no other publish is running. Then launch `pnpm publish:seasons` detached
   (`Start-Process`) and watch both the log and the PID. It is the first real run at concurrency 48,
   so watch for sustained retries and fall back to `--concurrency 16` if they show up. The run writes
   the budget block itself.
3. **Post.**
   - D1 seed: `seed-opr/epa/spr.sql`, one invocation each, each read back with `GROUP BY
     algorithm_id`. No `algorithm_state` team row may have a placeholder or `frc99[7-9][0-9]` scope
     key, except `demo-pseudo-unregistered`.
   - Worker deploy: `npx wrangler deploy` from `apps/worker`, only on a clean
     `apps/worker packages` tree. Two ticks of tail, stopped by PID.
   - R2 cleanup: `pnpm cleanup:r2-generations --generation spr@3.0.0+baseline`, preview first, then
     `--execute`.
   - Verify by content:
     - the live manifest names spr 4.0.0
     - `verify:subset` passes
     - frc2481/2026 spr history rows carry `sigma`, and opr/epa rows don't
     - OPR/EPA event rows carry no RP pmf
     - no spr frc9970-9999 team rows
   - `payloadBudget.test.ts` green against the rewritten block. Commit the budget doc.
   - Close the todo `republish-spr-4-demo-exclusion`.

## Not in scope

Pushing main: Jacob pushes, and other sessions' commits are pending. Also out: 260913-qyn's changes.
