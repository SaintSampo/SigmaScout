---
phase: quick-260912-1qx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/simulation-architecture.md
  - .planning/todos/pending/stop-baking-preschedule-schedules.md
  - .planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md
  - .planning/quick/260912-1qx-correct-three-stale-or-false-records-abo/260912-1qx-SUMMARY.md
autonomous: true
requirements: [QUICK-260912-1qx]

estimate:
  tokens: 28000
  raw_tokens: 56000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "`docs/simulation-architecture.md` section 4 records the pre-schedule stop as LIVE on all three published algorithms (opr, epa, bpr), not dark. The three live byte counts, the shared `computedAt`, and the `pricedFrom` value are recorded together with the method that produced them."
    - "Section 4 KEEPS both historical reasons — the vpr-keyed orphans and the `--presim-from-season 9999` sentinel committed in `package.json` — as EXPLICITLY LABELLED history. The sentinel paragraph's lesson (a green-looking run that wrote zero sidecars and printed no `presim:` line) survives intact. Nothing is deleted to make the page read cleanly."
    - "Section 3's `Measured size` table is relabelled as a historical vpr measurement from the 2026-09-06 generation, and the live per-algorithm figures sit beside it under their own date."
    - "The false claim in `stop-baking-preschedule-schedules.md` — that nothing on the client needs changing — is GONE and replaced by the enumerated truth: the web app touches exactly two presim fields, `preSchedule?.baked` and `preSchedule?.schedules`, and the second one feeds visitor-facing copy."
    - "That todo now sizes the real work by name and line: the published shape, `PreScheduleArtifactSchema`'s two `schedules` refinements at `packages/harness/pageArtifacts.ts:2010` and `:2015`, a scalar `scheduleCount` replacement, the one line at `SimulationTab.tsx:408`, and the three test files that index into `schedules`."
    - "That todo records the measured live waste — 388,484 B fetched, 12,275 B actually read, 96.8% downloaded and discarded — against the project's stated top UX priority."
    - "Decision item 3 of `preschedule-schedule-count-and-acceptance-bar.md` is CLOSED with the measurement that answers it: pricing dominates drawing roughly 4:1 on both measured events, `drawsPerSchedule` stays at 50, and the closure states explicitly that this does not contradict the earlier 5.7x finding."
    - "That todo's cost basis is corrected to the directly-timed 3.8 ms/schedule (0.0306 ms per schedule per match) with the old differencing method named as ~1.8x too high, and the added publish time stated against the 641 sidecars the 2026-09-12 publish actually wrote."
    - "That todo records the rung-1 branch as CLOSED, cross-referencing quick task `260912-0v3` rather than restating its numbers at length, so the count question reads as LIVE."
    - "EVERY figure that lands in a document carries the method that produced it — an unauthenticated GET whose JSON was parsed, a field enumeration over named source lines, or a direct timing of a named function — so a later reader can re-check it without re-running this session."
    - "NO source file changes. `PRESIM_SCHEDULE_COUNT`, `PRESIM_DRAWS_PER_SCHEDULE`, and everything under `packages/`, `apps/` and `scripts/` are READ-ONLY to this task."
    - "NO measurement, publish, R2 write, D1 write, deploy or network check runs. Every figure here is already verified and is being transcribed, not re-derived."
    - "`.env` is never read, printed, copied or interpolated. Nothing in this task needs a credential."
    - "Every commit stages by explicit path. `git add -A` is never used."
  artifacts:
    - "docs/simulation-architecture.md — section 4 rewritten live-with-history, section 3 size table dated and extended, section 1 and section 5 made internally consistent with it"
    - ".planning/todos/pending/stop-baking-preschedule-schedules.md — false client claim corrected, real work sized, live waste recorded"
    - ".planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md — item 3 closed, cost basis corrected, rung-1 closure cross-referenced"
    - ".planning/quick/260912-1qx-correct-three-stale-or-false-records-abo/260912-1qx-SUMMARY.md"
  key_links:
    - "`SimulationTab.tsx:408` passes `preSchedule?.schedules.length` into `StartMatchPicker.tsx:83`'s `preScheduleScopeText`, which renders to visitors at `StartMatchPicker.tsx:262-264`. Dropping the `schedules` block without replacing that input falls through the `?? 0` and publishes `across 0 randomly generated schedules` with NO test failing. This is the whole reason the todo's old claim was dangerous, and the corrected todo must make it impossible to miss."
    - "Section 4's two historical reasons are the only record of the sentinel incident. Rewriting the section to say LIVE while deleting them would trade one stale record for a lost lesson. Label, do not delete."
    - "Another session may be committing to this same checkout. Re-read the TAIL of each file immediately before editing it, and stage by explicit path — commit `f0c7af48` absorbed a concurrent session's edits through a broad add."
---

<objective>
Correct three planning and documentation records that today's live verification proved stale or
false. All three are records, not code.

Purpose:
1. `docs/simulation-architecture.md` section 4 asserts the pre-schedule stop is dark. Production
   says otherwise on all three published algorithms. A reader following the page today would
   conclude a shipped feature does not exist.
2. `.planning/todos/pending/stop-baking-preschedule-schedules.md` tells a future implementer that
   the client needs no change. Following that instruction ships a silent published-honesty
   regression — visitor-facing copy would read `across 0 randomly generated schedules` with no test
   failing.
3. `.planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md` carries an open
   decision item that has since been measured and answered, and a publish-cost basis inflated ~1.8x
   by the method that produced it.

Output: three document edits, three atomic commits, each figure recorded WITH the method that
verified it.

What this is NOT: no measurement, no publish, no source change, no network call. Every figure below
was verified in the orchestrating session on 2026-09-12 and is supplied in this plan. Transcribe it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

@docs/simulation-architecture.md
@.planning/todos/pending/stop-baking-preschedule-schedules.md
@.planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md
@.planning/quick/260912-0v3-re-run-rung-1-at-n-4000-schedules-agains/260912-0v3-SUMMARY.md
</context>

<constraints>

## Hard boundaries — read before touching anything

**Documentation and planning records ONLY.** Do not change `PRESIM_SCHEDULE_COUNT` or
`PRESIM_DRAWS_PER_SCHEDULE`. Do not change ANY file under `packages/`, `apps/`, or `scripts/` — they
are read-only sources of line numbers and field names for this task. No publish, no R2 write, no D1
write, no manifest bump, no deploy, no retune.

**Do not re-verify anything.** Every figure in this plan was verified today in the orchestrating
session. Do not re-run a measurement, do not re-fetch an artifact, do not open a network connection
to check a byte count. Transcribe the figure AND the method that produced it. If you believe a
figure is wrong, stop and say so — do not go measure it.

**Do not invent a figure.** Only numbers that appear in this plan may land in a document. If a
document needs a number this plan does not supply, either omit that sentence or state the quantity
qualitatively. Arithmetic on supplied numbers is allowed only when the arithmetic is shown inline
(e.g. writing a remainder as `388,484 - 12,275`), so a reader can see it is subtraction and not a
second measurement.

**Stage by explicit path.** Every commit uses `git add -- <path> [<path> ...]`. NEVER `git add -A`,
never `git add .`, never `git commit -a`. Commit `f0c7af48` on this repo absorbed a concurrent
session's edits through a broad add.

**Concurrent sessions.** Another session may be editing this checkout right now. Immediately before
editing each file, re-read its TAIL (`tail -40 <path>`) and confirm the region you are about to
change still reads as this plan describes. After each commit, run `git status --porcelain` and
confirm only the paths you named are involved.

**Secrets.** Never `Read`, `cat`, `head`, `echo` or interpolate `.env`, not even to confirm a key is
set. Nothing in this task needs a credential; the live checks that produced these figures were plain
unauthenticated GETs against the public artifact origin.

**Verify by output, never by exit code.** On this machine `timeout <n> pnpm <cmd>` swallows output
and exits 0. Read what a command prints.

**Commit messages** end with:
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

**Four commits, in order.** One per fix, then the SUMMARY. Do not batch the three fixes into one
commit — each corrects an independent record and should be revertable alone.

</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-schedule stop as live, keeping both historical reasons labelled</name>
  <files>docs/simulation-architecture.md</files>
  <read_first>
    `docs/simulation-architecture.md` lines 1-8 (the audit header), 16-20 (section 1's engine table
    and its fetch-size parenthetical), 149-160 (section 3's size table and the publish-budget
    citation), 163-200 (all of section 4), 268-275 (section 5's two "settle first" items). Read the
    whole file once; do not re-read a range.
  </read_first>
  <action>
    Rewrite section 4 so it records the CURRENT state — the pre-schedule stop is live on every
    published algorithm — and demotes the old state to labelled history. Retitle the section so its
    heading no longer declares the stop unavailable; something in the house voice like
    `## 4. Live status: the pre-schedule stop is live on all three published algorithms`.

    Record these three verified facts as the section's opening, each with its method. All three were
    read on 2026-09-12 by unauthenticated GET against `https://data.sigmascout.org`, with the
    response body PARSED AS JSON and its fields counted — not by reading an HTTP status alone. Say
    that, because status-only checks have misled on this project before and a later reader needs to
    know which kind of check this was.

    | key | bytes | roster | schedules | baked.draws | histograms |
    |---|---|---|---|---|---|
    | `v1/presim/2026mrcmp/opr@4.0.0+baseline.json` | 393,507 | 66 | 20 | 1000 | 66 |
    | `v1/presim/2026mrcmp/epa@10.0.0+baseline.json` | 392,566 | 66 | 20 | 1000 | 66 |
    | `v1/presim/2026mrcmp/bpr@3.0.0+baseline.json` | 388,484 | 66 | 20 | 1000 | 66 |

    All three carry `pricedFrom` = `pre-event-walk-forward` and `computedAt` =
    `2026-09-12T01:06:14.953Z`. The live algorithms manifest resolves `opr 4.0.0+baseline`,
    `epa 10.0.0+baseline`, `bpr 3.0.0+baseline`, so every sidecar is reachable under the client's
    OWN key — state that explicitly, because reachability-under-the-client's-key is the property
    that was broken before, not mere existence in the bucket.

    Note that the epa version the old section named is superseded: the published epa is 10.0.0 now.

    KEEP BOTH HISTORICAL REASONS, under an explicit label such as
    `### How it was dark, and why that is worth keeping`. Neither may be deleted or compressed to a
    single line:

    1. The vpr-keyed orphans — every sidecar in R2 belonged to `vpr`, retired from the published set
       on 2026-09-09, so the objects existed but were unreachable under any key the client would
       request. This is exactly the failure the new reachability sentence above rules out, which is
       why the two belong on the same page.
    2. The `--presim-from-season 9999` sentinel. Preserve the whole lesson verbatim in substance:
       the sentinel was COMMITTED in `package.json`'s `publish:seasons` script rather than passed as
       a per-run CLI override; a cutoff above every season in the corpus made the
       `season >= preScheduleFromSeason` gate false for every season; three republishes wrote zero
       sidecars; `09-RESEARCH.md`'s Pitfall 1 asserted no such literal existed in the tree and that
       re-enabling was a republish rather than a code change; following that reading would have
       spent a full forty-five-minute R2 write pass and printed no `presim:` line while looking
       green. Keep the 09-10 remediation too (argument changed to `2026`, matching
       `DEFAULT_PRESCHEDULE_FROM_SEASON`, plus the drift tripwire in
       `packages/harness/publish.test.ts`). Add one sentence making the transferable lesson explicit:
       a run that exits clean and prints nothing about the artifact class it was supposed to write is
       not evidence it wrote anything.

    Replace section 4's closing paragraph — the one concluding that only the live engine can produce
    anything today — with the true statement: the tab's pre-schedule stop now resolves for the
    selected algorithm on a 2026 event, and the live per-match engine continues to work alongside it.
    KEEP the live-engine spot-check that follows (`2026mrcmp` 132/132 qual rows with pmfs;
    `2024casf` 57/72 with the gaps being exactly `qm1`-`qm15`, the Swing-Factor cold-start chain
    under quick task 260910-kco) and date-label it as the 2026-09-10 audit's own check rather than
    letting it read as re-verified today.

    In section 3, relabel the `Measured size` table as a HISTORICAL vpr measurement from the
    2026-09-06 generation — put the date and the algorithm in the heading itself so the numbers
    cannot be read as current — and add the live per-algorithm figures from the table above beneath
    it, under their own 2026-09-12 heading and method line. Record alongside them the one component
    split measured live today: on `bpr@3.0.0+baseline`, 388,484 B fetched against 12,275 B actually
    read by the client (`roster` + `baked`), leaving `388,484 - 12,275` bytes downloaded and
    discarded, 96.8% of the object. Show that subtraction inline rather than presenting the
    remainder as an independently measured number.

    Also in section 3, the `docs/publish-budget.md` citation carries a clause asserting that the
    2026-09-06 generation is the one still serving. That clause is now false — the live sidecars
    carry `computedAt 2026-09-12T01:06:14.953Z`. Correct the clause only: keep the 216 sidecars /
    median 138,710 B / p95 243,647 B / max 265,617 B figures attributed to the 2026-09-06 generation
    where they came from, and note they are superseded by the 2026-09-12 publish whose budget stats
    are not yet transcribed into that document. Do NOT invent replacement budget statistics.

    Two internal-consistency fixes, both one-liners, both required so the file does not contradict
    itself after this edit:
    - Section 1's engine table prices the pre-schedule fetch at a range taken from the superseded
      generation. Attribute that range to the 2026-09-06 generation and note the directly measured
      live point (~388-394 KB on this 66-team event). Do not state a new median or range across all
      sidecars — none was measured.
    - Section 5's first "settle first" item calls for re-keying or regenerating the sidecars because
      every one belongs to a retired algorithm. Mark that item DONE as of the 2026-09-12 publish and
      point at section 4. Change nothing else in section 5 — Options A/B/C, their blockers, the cost
      estimate and the recommendation all stand untouched.

    Finally, extend the document's audit header (line 3) with a dated re-verification line saying
    sections 1, 3, 4 and 5's first settle-first item were re-verified against
    `https://data.sigmascout.org` on 2026-09-12, and that sections 2, 5 and 6 otherwise still carry
    the 2026-09-10 audit.

    Before editing, `tail -40 docs/simulation-architecture.md` and confirm the file still ends at the
    file map. Commit with `git add -- docs/simulation-architecture.md`.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" ; f=docs/simulation-architecture.md ; echo "stale-heading want 0: $(grep -cF 'is currently dark' $f)" ; echo "opr-bytes want 1+: $(grep -cF '393,507' $f)" ; echo "epa-bytes want 1+: $(grep -cF '392,566' $f)" ; echo "bpr-bytes want 1+: $(grep -cF '388,484' $f)" ; echo "computedAt want 1+: $(grep -cF '2026-09-12T01:06:14.953Z' $f)" ; echo "read-bytes want 1+: $(grep -cF '12,275' $f)" ; echo "history-labels want 2+: $(grep -ciF 'histor' $f)" ; echo "sentinel-kept want 1+: $(grep -cF '9999' $f)" ; echo "pitfall-lesson-kept want 1+: $(grep -cF '09-RESEARCH.md' $f)" ; echo "tripwire-kept want 1+: $(grep -cF 'publish.test.ts' $f)" ; echo "old-max-still-attributed want 1+: $(grep -cF '265,617' $f)" ; git status --porcelain</automated>
    <human-check>The stale-heading count is 0 and every other count is at least 1 (history labels at least 2). `git status --porcelain` lists only `docs/simulation-architecture.md` before the commit and nothing after it. Read the rewritten section 4 end to end: it must be possible for someone who has never seen this page to learn BOTH that the stop is live today AND what the sentinel incident taught, without either reading as the current state by accident.</human-check>
  </verify>
  <done>
    Section 4 records the live state on all three published algorithms with its verification method;
    both historical reasons survive under an explicit history label with the sentinel lesson intact;
    section 3's size table is dated and joined by the live figures and the read-vs-fetched split;
    section 1's fetch-size range and section 5's first settle-first item no longer contradict section
    4; the audit header carries the dated re-verification. One commit, staged by explicit path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the false client claim in the stop-baking todo and size the real work</name>
  <files>.planning/todos/pending/stop-baking-preschedule-schedules.md</files>
  <read_first>
    The whole of `.planning/todos/pending/stop-baking-preschedule-schedules.md` (32 lines). You do
    NOT need to open the source files to do this task — every line number and field name you need is
    supplied below and was verified today. Do not edit any source file.
  </read_first>
  <action>
    The todo's closing sentence tells a future implementer that the client side needs no work because
    the aggregate is all it consumes. That is WRONG, and an implementer who follows it ships a silent
    published-honesty regression. Delete that claim and replace it with the enumerated truth.

    The enumeration method, which must be recorded so the claim is re-checkable: every presim
    artifact field the web app touches was enumerated on 2026-09-12. There are exactly TWO —
    `preSchedule?.baked` and `preSchedule?.schedules`.

    - `apps/web/src/lib/preScheduleResult.ts:41-44` reads only `artifact.roster` and
      `artifact.baked`. THAT HALF OF THE OLD CLAIM IS CORRECT and should be preserved as such — the
      decoder genuinely does not need the priced block.
    - `apps/web/src/components/event/SimulationTab.tsx:408` passes `preSchedule?.schedules.length`
      into `apps/web/src/components/event/StartMatchPicker.tsx:83`'s `preScheduleScopeText`, which
      renders to VISITORS whenever the pre-schedule view is selected
      (`StartMatchPicker.tsx:262-264`). Quote the rendered sentence so the stake is legible:

      "Before the schedule is released - how the field is likely to rank when nobody yet knows who
      plays whom. Computed ahead of time across 20 randomly generated schedules, 1000 draws in
      total."

    State the failure mode in one unmissable sentence: dropping the `schedules` block without
    replacing that input makes the count fall through `StartMatchPicker.tsx:264`'s `?? 0` and
    publishes "across 0 randomly generated schedules" to visitors silently, WITH NO TEST FAILING.
    Give that its own emphasised line — it is the reason this correction exists.

    Then size the real work as a short checklist, so the todo still reads as small-but-real rather
    than as a rewrite:
    - drop the block from the published shape;
    - drop it from `PreScheduleArtifactSchema` INCLUDING its two `schedules` refinements at
      `packages/harness/pageArtifacts.ts:2010` and `:2015`;
    - publish `scheduleCount` as a scalar field in its place;
    - change the one line at `apps/web/src/components/event/SimulationTab.tsx:408` to pass that
      scalar;
    - update the tests that index into `schedules`: `packages/harness/preSchedule.test.ts`,
      `packages/harness/publish.test.ts`, `packages/harness/pageArtifacts.test.ts`.

    Add the live waste measured on the real object today, with its method (unauthenticated GET
    against `https://data.sigmascout.org`, response parsed as JSON, 2026-09-12): on
    `v1/presim/2026mrcmp/bpr@3.0.0+baseline.json`, 388,484 B fetched against 12,275 B actually read
    — 96.8% downloaded and discarded. Tie it to the stated priority: this is the Simulation tab, on a
    project whose top UX priority is page load speed. This figure is a LIVE observation and replaces
    nothing — keep the existing `2025cur` scaling table (20 vs 4,000 schedules, 16,084 B vs 28,200 B
    aggregate against 411 KB vs 79 MB whole artifact) and the 95.8-98.6% share from plan 09-09 exactly
    as they are, and keep the pointer telling this todo to land first or alongside
    `preschedule-schedule-count-and-acceptance-bar`.

    Leave the frontmatter alone except that you may keep `priority: medium` as-is — this correction
    does not re-prioritise the item, it makes its scope honest.

    Before editing, `tail -20` the file and confirm the closing claim is still present and unedited.
    Commit with `git add -- .planning/todos/pending/stop-baking-preschedule-schedules.md`.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" ; f=.planning/todos/pending/stop-baking-preschedule-schedules.md ; echo "false-claim want 0: $(grep -cF 'it already reads only the aggregate' $f)" ; echo "picker-named want 1+: $(grep -cF 'StartMatchPicker' $f)" ; echo "tab-line want 1+: $(grep -cF 'SimulationTab.tsx:408' $f)" ; echo "scalar-field want 1+: $(grep -cF 'scheduleCount' $f)" ; echo "refinement-2010 want 1+: $(grep -cF '2010' $f)" ; echo "refinement-2015 want 1+: $(grep -cF '2015' $f)" ; echo "tests-listed want 3: $(grep -cF 'preSchedule.test.ts' $f) $(grep -cF 'publish.test.ts' $f) $(grep -cF 'pageArtifacts.test.ts' $f)" ; echo "fetched want 1+: $(grep -cF '388,484' $f)" ; echo "read want 1+: $(grep -cF '12,275' $f)" ; echo "waste want 1+: $(grep -cF '96.8' $f)" ; echo "old-table-kept want 1+: $(grep -cF '28,200' $f)" ; git status --porcelain</automated>
    <human-check>The false-claim count is 0, every other count is at least 1, and the three test-file counts are each 1. The rendered visitor sentence and the "across 0 randomly generated schedules" failure mode both appear and are impossible to skim past. `git status --porcelain` lists only this todo before the commit, nothing after.</human-check>
  </verify>
  <done>
    The todo no longer tells an implementer the client is free. It names both touched fields, the
    visitor-facing render path with line numbers, the silent-zero failure mode, the five-item work
    checklist, and the live 96.8% waste with its verification method — while keeping the `2025cur`
    scaling table and the ordering pointer untouched. One commit, staged by explicit path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the answered decision item and correct the inflated cost basis</name>
  <files>.planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md</files>
  <read_first>
    The whole of `.planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md`, and the
    frontmatter plus "The verdict" section of
    `.planning/quick/260912-0v3-re-run-rung-1-at-n-4000-schedules-agains/260912-0v3-SUMMARY.md` —
    enough to cross-reference it accurately without restating it. Do not re-run anything it did.
  </read_first>
  <action>
    Three corrections to this todo. Decision items 1 and 2 stay OPEN and untouched; so does the
    closing caveat about the inverse-square-root scaling with no plateau; so does the "bar measures
    noise" table and its 20/150/1000/2000/4000 rows.

    (a) CLOSE decision item 3. Retitle its bolded lead so it no longer reads as an open ask to
    rebalance the split, and mark it closed with the date 2026-09-12. Record the measurement that
    answers it, with its method: schedule count was held fixed at 200 while `drawsPerSchedule` was
    varied over 1, 5, 25, 50, 100 and 200, and a cost model `cost(d) = pricing + d * simPerDraw` was
    fitted to the result.

    | event | size | pricing | drawing at d=50 | drawing share |
    |---|---|---|---|---|
    | `2026joh` | 75 teams, 125 quals | 3.0775 ms/schedule | 0.9466 ms | 23.5% |
    | `2026txmca` | 18 teams | 0.9164 ms/schedule | 0.2425 ms | 20.9% |

    Pricing dominates drawing roughly 4:1. Cutting `d` from 50 to 10 frees only about 20% of
    per-schedule cost, which funds 1.23x the schedules — worth `sqrt(1.23)` ~ 1.11x on the binding
    floor. And it is not free: total draws would fall from 50,000 to about 12,000, where the
    draw-only floor slips from 100% to about 96%. CONCLUSION: leave `drawsPerSchedule` at 50.

    State EXPLICITLY that this does not contradict the earlier 5.7x finding (one session measured
    20x1000 as 5.7x worse than 1000x20). That finding was about statistical value per draw; this one
    is about wall-clock cost per schedule. They answer different questions and both stand. A reader
    who sees "schedules dominate draws" next to "leave draws at 50" without this sentence will think
    one of them is wrong.

    (b) Correct the cost basis. Record that earlier publish-cost estimates in this todo's lineage
    were derived by DIFFERENCING WHOLE PHASE A RUNS, which include `measureResamplingFloor`'s
    quantile work that a real publish never pays — making them about 1.8x too high. The corrected
    method: time `buildPreScheduleArtifact` directly. That gives 3.8 ms/schedule on `2026joh` at
    d=50, i.e. 0.0306 ms per schedule per match. Against the 641 sidecars the 2026-09-12 publish
    actually wrote, added publish time is about 14 min at 600 schedules, about 23 min at 1,000, and
    about 91 min at 4,000. Add the operational fact that makes those numbers wall-clock rather than
    CPU-seconds: the build runs SYNCHRONOUSLY inside `publish.ts`'s per-event loop at line 3166, so
    none of it parallelizes — `DEFAULT_CONCURRENCY` governs uploads only.

    (c) Record the rung-1 branch as CLOSED, and say what that does to this todo. Quick task
    `260912-0v3` confirmed the NO-SHIP on evidence at n=4,000: candidate 41.4% against a binding
    floor of 98.4%, worst team 3.33 ranks against a floor of 0.71 — failing structurally and scaling
    with roster size. Cross-reference
    `.planning/quick/260912-0v3-re-run-rung-1-at-n-4000-schedules-agains/260912-0v3-SUMMARY.md`
    rather than restating its numbers at length; the four figures above are the whole quotation
    budget. Then draw the consequence for THIS document: the count question is LIVE rather than
    possibly moot, and the rung-2 generator is the path.

    Every figure in this edit is supplied above and was verified in the orchestrating session on
    2026-09-12. Do not re-run a timing, do not re-open the corpus, do not re-run the rung-1 harness.

    Before editing, `tail -20` the file and confirm decision item 3 and the caveat are still present
    and unedited. Commit with
    `git add -- .planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md`.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" ; f=.planning/todos/pending/preschedule-schedule-count-and-acceptance-bar.md ; echo "open-ask-retitled want 0: $(grep -cF '**Rebalance schedules against draws**' $f)" ; echo "closed-label want 1+: $(grep -cF 'CLOSED' $f)" ; echo "joh-pricing want 1+: $(grep -cF '3.0775' $f)" ; echo "txmca-pricing want 1+: $(grep -cF '0.9164' $f)" ; echo "per-sched-per-match want 1+: $(grep -cF '0.0306' $f)" ; echo "direct-timing want 1+: $(grep -cF '3.8 ms' $f)" ; echo "sidecars want 1+: $(grep -cF '641' $f)" ; echo "sync-loop want 1+: $(grep -cF '3166' $f)" ; echo "no-contradiction want 1+: $(grep -cF '5.7' $f)" ; echo "rung1-xref want 1+: $(grep -cF '260912-0v3' $f)" ; echo "binding-floor-quote want 1+: $(grep -cF '98.4' $f)" ; echo "items-1-2-intact want 2+: $(grep -cF 'Set the count' $f) $(grep -cF 'Restate the bar' $f)" ; git status --porcelain</automated>
    <human-check>The open-ask count is 0 and every other count is at least 1, with both "items intact" probes returning 1. Decision items 1 and 2 and the closing inverse-square-root caveat are byte-unchanged (`git diff --word-diff` shows no edit inside them). The 5.7x reconciliation sentence sits close enough to the new drawing-share table that the two cannot be read as contradicting each other.</human-check>
  </verify>
  <done>
    Item 3 is closed with its measurement method and the explicit non-contradiction of the 5.7x
    finding; the cost basis names the old differencing method as ~1.8x high and replaces it with the
    direct timing, the 641-sidecar projections and the synchronous-loop fact; the rung-1 closure is
    recorded by cross-reference with four quoted figures. Items 1, 2 and the caveat are untouched.
    One commit, staged by explicit path.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write and commit the SUMMARY, immediately</name>
  <files>.planning/quick/260912-1qx-correct-three-stale-or-false-records-abo/260912-1qx-SUMMARY.md</files>
  <action>
    Do this BEFORE any self-check, any narration, and any final report. The summary is the record; a
    self-check that runs first and then dies loses the record.

    The `Write` tool is blocked on `SUMMARY.md` for subagents on this project. Create the file with a
    Bash heredoc instead — this is the documented exception, and it applies only to this file.

    Follow `$HOME/.claude/gsd-core/templates/summary.md`. Frontmatter must carry
    `phase: quick-260912-1qx`, `status: complete`, the three edited document paths under
    `key-files.modified`, and `tags` including `documentation`, `pre-schedule`, `phase-9`.

    The body must record, at minimum:
    - the three corrections, one short section each, each stating what the record said BEFORE and
      what it says NOW;
    - for each transcribed figure class, the verification method as it now appears in the document
      (unauthenticated parsed-JSON GET; field enumeration over named source lines; direct timing of
      `buildPreScheduleArtifact`) — so a reader of the SUMMARY alone knows nothing was re-measured
      here;
    - an explicit NOTHING SHIPPED section: no source file under `packages/`, `apps/` or `scripts/`
      changed, `PRESIM_SCHEDULE_COUNT` and `PRESIM_DRAWS_PER_SCHEDULE` unchanged, no publish, no R2
      or D1 write, no deploy, no network call, `.env` never read or interpolated;
    - the staging discipline actually used: every commit staged by explicit path, `git add -A` never
      run;
    - a commit table with the real hashes from `git log --oneline -4`;
    - any deviation from this plan, including any figure this plan supplied that you judged did not
      fit the target document's voice and therefore placed differently than described.

    Commit with
    `git add -- .planning/quick/260912-1qx-correct-three-stale-or-false-records-abo/260912-1qx-SUMMARY.md`
    and a `docs(260912-1qx):` message. Do NOT commit `PLAN.md` or `STATE.md` — the orchestrator owns
    those.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" ; ls -l .planning/quick/260912-1qx-correct-three-stale-or-false-records-abo/260912-1qx-SUMMARY.md ; git log --oneline -4 ; echo "--- src untouched, want empty ---" ; git status --porcelain -- packages apps scripts ; git diff HEAD~4 --stat -- packages apps scripts</automated>
    <human-check>The SUMMARY exists and is committed. `git log --oneline -4` shows four `docs(260912-1qx):` commits in order — simulation-architecture, stop-baking todo, count-and-bar todo, SUMMARY. Both source probes print NOTHING: no working-tree change and no diff under `packages`, `apps` or `scripts` across all four commits.</human-check>
  </verify>
  <done>
    `260912-1qx-SUMMARY.md` exists, records all three corrections with their verification methods and
    the nothing-shipped proof, and is committed by explicit path. Four commits total.
  </done>
</task>

</tasks>

<verification>

Run from the repo root after Task 4, and read the output rather than the exit code:

1. **Nothing shipped.** `git diff HEAD~4 --stat -- packages apps scripts` prints nothing.
   `git diff HEAD~4 -- packages/harness/publish.ts` prints nothing, so `PRESIM_SCHEDULE_COUNT` and
   `PRESIM_DRAWS_PER_SCHEDULE` are provably unchanged.
2. **Only the four intended paths moved.** `git diff HEAD~4 --stat` lists exactly
   `docs/simulation-architecture.md`, the two todo files, and the SUMMARY — nothing else. If a fifth
   path appears, a concurrent session's work was absorbed; stop and report it rather than amending.
3. **No secret in the diff.** `git diff HEAD~4 | grep -ciE 'api[_-]?key|secret|token|access[_-]?key'`
   returns 0. Report the count only, never a matched line.
4. **The three documents agree with each other.** `docs/simulation-architecture.md` says the stop is
   live; neither todo says it is dark; the stop-baking todo's 96.8% waste figure and section 3's
   read-vs-fetched split cite the same two numbers (388,484 and 12,275) and the same date.
5. **Each task's own `<automated>` gate passed** with the counts its `<human-check>` names.

</verification>

<success_criteria>

- [ ] `docs/simulation-architecture.md` section 4 records the pre-schedule stop LIVE on
      `opr@4.0.0+baseline`, `epa@10.0.0+baseline` and `bpr@3.0.0+baseline`, with bytes, shape,
      `pricedFrom`, `computedAt`, the manifest resolution, and the parsed-JSON GET method.
- [ ] Both historical reasons survive under an explicit history label, the sentinel's
      green-run-wrote-nothing lesson intact and generalised in one sentence.
- [ ] Section 3's size table is dated as the historical vpr measurement, joined by the live figures
      and the `388,484 - 12,275` read-vs-fetched split shown as inline arithmetic.
- [ ] Section 1's fetch-size range and section 5's first settle-first item no longer contradict
      section 4; the audit header carries the dated re-verification.
- [ ] `stop-baking-preschedule-schedules.md` no longer claims the client is free; it names both
      touched fields, the visitor render path by line, the silent-zero failure mode, the five-item
      work checklist, and the 96.8% live waste.
- [ ] `preschedule-schedule-count-and-acceptance-bar.md` item 3 is CLOSED at `drawsPerSchedule = 50`
      with its method and the explicit 5.7x non-contradiction; the cost basis is corrected to
      3.8 ms/schedule against 641 sidecars with the synchronous-loop fact; the rung-1 closure is
      cross-referenced to `260912-0v3`.
- [ ] Items 1 and 2 and the closing caveat in that todo are byte-unchanged.
- [ ] Every transcribed figure carries the method that produced it.
- [ ] No file under `packages/`, `apps/` or `scripts/` changed. No publish, R2/D1 write, deploy or
      network call ran. `.env` was never read, printed, copied or interpolated.
- [ ] Four commits, each staged by explicit path, `git add -A` never used, each message ending with
      the required co-author line.

</success_criteria>

<output>
Create `.planning/quick/260912-1qx-correct-three-stale-or-false-records-abo/260912-1qx-SUMMARY.md`
when done — written by Bash heredoc (the `Write` tool is blocked on this path for subagents) and
committed immediately, before any self-check or narration.
</output>