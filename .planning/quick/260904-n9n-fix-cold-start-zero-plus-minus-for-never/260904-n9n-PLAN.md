---
quick_id: 260904-n9n
description: Fix the cold-start 0 ± 0 defect for never-seen teams (todo sigma1-cold-start-zero-plus-minus)
created: 2026-09-04
status: complete
---

# Quick task 260904-n9n — the cold-start `0 ± 0` defect

## Investigation verdict (the todo's own required first step: locate, do not assume)

**The `0 ± 0` defect described in the todo DOES NOT EXIST on the current publish path.**
The todo (filed 2026-09-01) verified the mechanism (never-seen teams are absent from
`teamMetrics`' returned map) but explicitly did NOT locate the downstream site that turns
absence into `0 ± 0` — it instructed the fixer to find it. Located: **there is no such
site.** Absence propagates end to end:

| Layer | Behavior for a never-seen team | Evidence |
|---|---|---|
| `sigma1.teamMetrics` | omitted from result (`if (!teamState) continue`) | `packages/core/algorithms/sigma1/index.ts:1553-1554` |
| `epa.teamMetrics` | omitted (`if (!components) continue`) | `packages/core/algorithms/epa.ts:797` |
| `opr.teamMetrics` | omitted (two `undefined` guards) | `packages/core/algorithms/opr.ts:467-469` |
| publish (teams/event/team-season) | `metrics: {}` — empty object, never zeros | `publish.ts:1284`, `:2031`; percentile pools skip valueless teams (`:1817-1820`, "never counted as a zero") |
| Worker live tick | same `?? {}` defaults | `apps/worker/src/scheduled.ts:487`, `:943`, `:952` |
| Client cell render | blank span for a missing metric; real `0` renders `0.00` — the two are distinguished | `MetricValue.tsx:57-61`; `MetricValue.test.tsx:30`, `:41` |
| Client rank/sort | missing total ranks last with team-number tie-break; missing sort key last both directions; zero-match win rate is `null`, "never a coerced zero" | `rowModel.ts:56`, `:134-135`, `:157-159`; `rowModel.test.ts:39`, `:93`, `:98`, `:176` |

The representation decision the todo asked to be made (option (b): explicit "no data",
never numeric zero) is therefore ALREADY IN FORCE, decided piecewise by:
- the 2026-09-01 blank-cell user request (`MetricValue.tsx`'s own comment),
- quick task 260904-586 ("never counted as a zero" percentile-pool exclusion),
- rowModel's documented missing-sorts-last contract.

A related third case is also already decided and pinned: a team IN state via season carry
with no folded matches publishes a bare value with NO spread (D-Y1,
`sigma1.test.ts:604-623`) — deliberate, with rationale in the test.

## What is actually missing

The one unpinned link in the chain is the algorithm-level omission guarantee itself:
no test asserts that requesting metrics for a team absent from state yields a map with
that team ABSENT — never present-with-zeros. If a future refactor changed `continue` to
`result[team] = { total: { value: 0 } }`, every downstream layer would faithfully publish
and render `0.00`, and today's suite would stay green.

## Task

**Task 1 — pin the omission contract in all three algorithms.**
Add to each of `sigma1.test.ts`, `epa.test.ts`, `opr.test.ts`: a state containing at
least one played team; request metrics for `[playedTeam, neverSeenTeam]`; assert the
played team is present and the never-seen team is ABSENT (`"key" in result === false`),
with a comment naming the todo and the downstream chain the omission protects.
Verify: the three suites pass; repo tsc clean.

## Out of scope

- No production code changes — the investigation found nothing to fix.
- The D-Y1 bare-value-no-spread case — separately decided and already pinned.
