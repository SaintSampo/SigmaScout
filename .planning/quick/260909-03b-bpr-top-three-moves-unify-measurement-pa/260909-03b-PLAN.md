---
task: "BPR top three moves - unify the measurement path, re-derive the selection bar, add a spread-responsive anti-additivity knob"
slug: bpr-top-three-moves-unify-measurement-pa
quick_id: 260909-03b
date: 2026-09-09
mode: quick
type: execute
wave: 1
depends_on:
  - 260908-b4t-fresh-2023-blind-model
  - 260908-vqr-adversarial-review-of-the-bpr-algorithm
autonomous: true
requirements: [P3, P2, P1]
files_modified:
  - packages/bpr/data.ts
  - packages/bpr/evaluate.ts
  - packages/bpr/equivalence.ts
  - packages/bpr/holdout.ts
  - packages/bpr/sealedPaths.ts
  - packages/bpr/slice.ts
  - packages/bpr/ablate.ts
  - packages/bpr/model.ts
  - packages/bpr/tune.ts
  - packages/bpr/model.test.ts
  - packages/bpr/evaluate.test.ts
  - packages/bpr/holdout.test.ts
  - packages/bpr/candidate-spread-params.json
  - packages/core/algorithms/bpr.ts
  - .planning/STATE.md

estimate:
  tokens: 240000
  raw_tokens: 120000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "Every BPR figure produced after this task is computed on the shared-harness population under the D-Q3 accuracy convention - the same rules OPR/EPA/VPR are scored by."
    - "The design-era figure moved DOWN from 73.081 to the honest value, and the pre-change number is recorded next to it rather than replaced silently."
    - "holdout.ts refuses to run unless the parameter file AND model.ts AND the shipped port are clean at HEAD, and prints a blob sha for each (F-15)."
    - "A 2023 run cannot step 2024 or later, because stopAfterYear is bound to max(--years)."
    - "The selection bar is a MEASURED paired event-clustered quantity, not an assumed-independent one."
    - "spreadBeta at its default reproduces today's predictions with zero mismatches across the full corpus, in BOTH the research model and the shipped port."
    - "No hyperparameter, threshold, or accept/reject decision in this task was computed from a season later than 2022."
    - "The 2023 test exists as one documented command with a written acceptance rule, and has NOT been run."
  artifacts:
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BASELINE-PRE-P3.txt
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/POPULATION-RECONCILE.txt
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/GUARDS-PROOF.txt
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BAR.md
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/CHAMPS-DESIGN-ERA.txt
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/PRE-REGISTRATION.md
    - .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/260909-03b-SUMMARY.md
  key_links:
    - "packages/bpr/evaluate.ts -> packages/core/scoring/brier.ts accuracyCall (one accuracy convention)"
    - "packages/bpr/data.ts -> packages/corpus/db.ts selectMatchesChronological (one match population and ordering)"
    - "packages/bpr/holdout.ts -> packages/bpr/sealedPaths.ts -> git blob shas (the seal covers the model, not just the parameters)"
    - "packages/bpr/model.ts spreadBeta <-> packages/core/algorithms/bpr.ts spreadBeta (equivalence replay proves lockstep)"
    - "scripts/paired-bar.ts -> packages/harness/eventBootstrap.ts eventBlockedBootstrap (the paired-difference SE, not a third hand-rolled resampler)"
---

<objective>
Execute the three ranked proposals from the BPR adversarial review (`260908-vqr`),
in dependency order, tuned and selected on **2016-2022 only**.

Purpose: BPR's design-era numbers are currently produced by a measurement path
nobody else uses, its selection bar assumes an independence the data does not
have, and its one measured deficit (championship-level play) sits on the exact
axis the model's novelty lives on. Fix the measuring stick first, calibrate the
decision rule second, and only then touch the model.

Output: an honest design-era figure with its pre-change value recorded beside
it; a measured paired event-clustered selection bar; a verdict on whether
anti-additivity and the champs deficit are real; and - if and only if both
verdicts pass - a frozen candidate parameter set, a written acceptance rule, and
**one command Jacob runs himself on 2023**.

**The executor does NOT run the 2023 test.** Building it and then peeking at it
destroys the only thing this exercise buys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260908-vqr-adversarial-review-of-the-bpr-algorithm/260908-vqr-REVIEW.md
@.planning/quick/260908-b4t-fresh-2023-blind-model/DECISION.md
@packages/bpr/model.ts
@packages/bpr/data.ts
@packages/bpr/evaluate.ts
@packages/bpr/holdout.ts
@packages/core/scoring/brier.ts
@packages/harness/eventBootstrap.ts
@.claude/CLAUDE.md
</context>

<hard_rules>

These bind every task. A task that violates one has failed regardless of its
own `<done>`.

1. **2016-2022 only.** `tune.ts:36` and `score.ts:24` already throw on any year
   past `LAST_DESIGN_YEAR`. Those guards stay armed and are never bypassed,
   commented out, or routed around. No hyperparameter, no threshold, no
   accept/reject decision may be computed from a season after 2022.

2. **Do not run the 2023 test.** The deliverable ends at a frozen candidate, a
   written acceptance rule, and a documented command. `holdout.ts` may be
   invoked ONLY with `--dry-run` (which resolves and prints its plan and exits
   without evaluating) or in a way that throws.

3. **One narrow exception, and only this one.** A *bit-identity* replay may step
   matches from any season, because it compares predictions to predictions and
   never to an outcome. Such a script must contain no accuracy, Brier, or
   log-loss code path at all - it emits `matchKey` and `pRedWin` and a mismatch
   count, nothing else.

4. **Rule A.** A change ships only when accuracy AND Brier both improve.

5. **No parameter bloat, no ensembling.** One new knob, inert at its default,
   promoted only on measured evidence. Do not propose a trained GBDT - it was
   measured, lost, and is shelved.

6. **`frozen-params.json` is sealed.** It records what was measured at freeze
   time. Never edit it. A new candidate goes in a NEW file. At the end of this
   task `git log --oneline -- packages/bpr/frozen-params.json | wc -l` must
   still be exactly `1`.

7. **The port stays in lockstep.** Any model change lands in BOTH
   `packages/bpr/model.ts` and `packages/core/algorithms/bpr.ts`, with an
   equivalence replay proving it.

8. **Nothing touches production.** No R2 write, no republish, no artifact
   regeneration. The `compare` artifact is at 93.2% of its 20,000-byte ceiling
   and this task adds no algorithm - it stays untouched.

9. **Secrets.** Never `Read`/`cat`/`echo` `.env` or interpolate any value from
   it. Nothing in this task needs a credential; every command is local.

10. **Concurrent session.** Another session is editing `apps/web` in this same
    checkout. Stage every commit by EXPLICIT PATH. Never `git add -A`, never
    `git commit -a`. Before each commit, confirm `git status --porcelain` shows
    your paths and treat everything else as someone else's work.

</hard_rules>

<environment>

- **Runtime is fast.** One full design-era BPR evaluation is ~1.2s after a
  ~1.8s corpus load (measured 2026-09-09: `npx tsx packages/bpr/cli.ts` took
  3.0s wall for 82,946 scored matches). A full 3-sweep coordinate descent is
  ~113 evaluations per sweep, so ~6 minutes. Budget accordingly; nothing here
  should take hours.
- **Never `timeout <n> pnpm <cmd>`** - it swallows all output and exits 0, a
  false green. Invoke `npx tsx ...` and `npx vitest run ...` directly and judge
  by OUTPUT, not exit code.
- **Run vitest from the repo root**, never from `apps/web`.
- `pnpm install` exits 1 on this machine (better-sqlite3 node-gyp). `node_modules`
  is fine - verify functionally, never by exit code. Do not run it.
- **A quiet long run is not a dead one.** For anything past ~2 minutes, run it
  with `run_in_background: true` redirecting stdout to a log under
  `reports/260909-03b/`, and monitor by log-file mtime and line count, not by
  silence.
- **Executor subagents' sandbox denies all network Bash.** Everything in this
  plan is local (sqlite corpus, on-disk jsonl, git). If any command is
  nonetheless blocked, surface it in the summary rather than working around it.
- **A shell heredoc long enough to hold a whole document fails on this machine**
  with `ENAMETOOLONG` (hit while writing this plan). Use the `Write` tool for
  new files; reserve heredocs for short content and for `SUMMARY.md`, which
  `Write` is blocked from creating.
- `reports/` and `data/` are gitignored. Large jsonl dumps go to
  `reports/260909-03b/`; committed evidence goes in the task directory.
- **Never a raw `|` in a STATE.md quick-task description** - one pipe in any row
  breaks the append helper for every future row.

</environment>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capture the pre-change baseline, then put BPR on the shared measurement path (P3, part 1)</name>

  <precondition>`data/corpus.sqlite` exists and is ~582MB; `packages/bpr/frozen-params.json` is clean at HEAD with exactly one commit in its history.</precondition>

  <files>
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BASELINE-PRE-P3.txt,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/POPULATION-RECONCILE.txt,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/population-reconcile.ts,
    packages/bpr/data.ts,
    packages/bpr/evaluate.ts,
    packages/bpr/equivalence.ts,
    packages/bpr/evaluate.test.ts
  </files>

  <behavior>
    - A prediction of exactly one half against a decided match is IN the accuracy denominator and is counted a miss, and is separately reported as one abstention.
    - An actual tie is excluded from the accuracy denominator, is included in the Brier population, and is scored against a target of one half.
    - A surrogate-affected match is excluded from scoring entirely, but the model still predicts and updates on it, so the state reaching the next match is unchanged by the exclusion.
    - An offseason match is absent from the loaded stream altogether.
    - Match order is the shared total order (sort time, then event key, then comp-level play order, then set, then match number), so a lexicographic comparison can never place a later-numbered match before an earlier one.
  </behavior>

  <action>
    STEP 1 - BASELINE FIRST, before editing any source file. The point of this
    move is that its effect is decomposed rather than conflated with Task 4's,
    and that requires the pre-change number to exist on disk first.

    Run, and tee into `BASELINE-PRE-P3.txt`:
      `npx tsx packages/bpr/score.ts packages/bpr/frozen-params.json 2016,2017,2018,2019,2020,2022`
    Append `git rev-parse HEAD` and the current commit count for
    `packages/bpr/frozen-params.json`.

    The TOTAL accuracy must read 73.08 - that is `frozen-params.json`'s own
    recorded `_design_accuracy_2016_2022` of 0.7308105. If it does not, STOP and
    report: the measured starting point disagrees with the sealed record, and
    nothing downstream is interpretable until that is explained.

    STEP 2 - `packages/bpr/data.ts`. Replace the local SQL with
    `selectMatchesChronological(db, { excludeOffseason: true })` imported from
    `packages/corpus/db.js`, joined against a `SELECT event_key, year, week FROM
    events` lookup map for the `year` and `week` fields the shared `MatchResult`
    does not carry. Keep the existing `foulOf` extraction, now reading the
    shared row's `scoreBreakdownRaw`. Widen `BprMatch` to carry
    `redSurrogates`, `blueSurrogates`, `redDqs`, `blueDqs`, and `eventType`
    straight through.

    Delete the local event-type filter that drops type 100. Measured 2026-09-09:
    six design-era events and five holdout-era events carry that type while
    `is_offseason = 0`, worth 149 and 151 matches. The shared harness excludes on
    `is_offseason` alone, so those matches ARE part of the common population -
    dropping them is precisely the "~37/season the harness keeps" half of F-12.
    Also drop the null-score filter, matching the shared selector; instead COUNT
    rows whose winner is present while either score is null and print that
    count. Expect zero. A nonzero count is a finding to report, not to silently
    filter away.

    STEP 3 - `packages/bpr/evaluate.ts`. Import `accuracyCall` from
    `packages/core/scoring/brier.js` and drive the numerator and denominator
    from it, deleting the local scoring branch that awards partial credit for an
    unopinionated prediction. Add a `noCall` field to `Stats`, incremented when
    the predicted probability equals one half. Skip SCORING for a
    surrogate-affected match while still calling `predict` and `update` on it -
    that is exactly what the harness replay does, and it is what keeps the state
    stream identical. Brier and log loss are accumulated over the same scored
    set, ties included, unchanged in every other respect.

    STEP 4 - `packages/bpr/equivalence.ts`. Score through `accuracyCall` and the
    same surrogate exclusion instead of its own inline comparison, which today
    credits an unopinionated prediction whenever blue happened to win. Gate the
    holdout half behind an explicit `--include-holdout` flag and make design-era
    the default, so this file can never print a post-2022 figure by accident
    (per hard rule 2, do NOT pass that flag in this task). Re-measure and update
    `KNOWN_DESIGN`, keeping `73.081` in the adjacent comment with a one-line
    note that it was the pre-P3 value under the retired convention. Leave
    `KNOWN_HOLDOUT_ALL` and its comment untouched - it is a historical record.

    STEP 5 - Tests, written before the implementation is considered done:
    `packages/bpr/evaluate.test.ts`, over hand-built synthetic `BprMatch`
    fixtures (never the real corpus), pinning each bullet in `<behavior>` above.
    The surrogate bullet is the one that matters most: assert that a downstream
    prediction is bit-identical between a run whose surrogate match was scored
    and a run whose surrogate match was excluded from scoring, proving the
    exclusion touched the scoreboard and not the state.

    STEP 6 - Reconciliation. Write
    `.planning/.../scripts/population-reconcile.ts`, which (a) counts the loaded
    population per season directly from `loadMatches`, with no model involved,
    and diffs it against the review's recount table hardcoded in the script (per
    season: scored count, accuracy denominator, tie count, from
    `260908-vqr-REVIEW.md`'s Angle A table); and (b) runs `evalDesign` on the
    frozen params and prints the new design-era accuracy, Brier, and log loss.
    Part (b) is design-era only - `runEval` must be called with `stopAfterYear:
    2022`, so later seasons are never stepped. Tee to
    `POPULATION-RECONCILE.txt`.

    Commit by explicit path.
  </action>

  <verify>
    <automated>npx vitest run packages/bpr packages/core/scoring 2>&1 | tail -20</automated>
    <automated>npx tsx .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/population-reconcile.ts 2>&1 | tail -40</automated>
    <automated>npx tsx packages/bpr/score.ts packages/bpr/frozen-params.json 2016,2017,2018,2019,2020,2022 2>&1 | tail -12</automated>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>

  <done>
    `BASELINE-PRE-P3.txt` exists and records 73.08 as the pre-change design-era
    total. The reconciliation reports ZERO per-season mismatches against the
    review's recount table across all ten seasons. The post-change design-era
    total lands inside 72.80-72.95 (the review's shared-harness figure is 72.87;
    the research model sits within ~0.03pp of the port). If it lands outside that
    band, record the number and the discrepancy - do not adjust anything to reach
    it. Tests green; typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Arm the guards - seal the model, bound the holdout, close the accidental-read paths (P3, part 2)</name>

  <files>
    packages/bpr/sealedPaths.ts,
    packages/bpr/holdout.ts,
    packages/bpr/slice.ts,
    packages/bpr/holdout.test.ts,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/GUARDS-PROOF.txt
  </files>

  <behavior>
    - Sealing reports a per-path blob sha, and refuses when ANY sealed path is dirty, naming the offending path.
    - A year outside the holdout set is rejected by the year argument, with a message pointing at score.ts.
    - Naming a single holdout season binds the halt-after year to that season, so no later season is stepped.
    - Absence of the explicit seal-breaking flag still refuses, as it does today.
    - The plan-resolution mode prints its resolved plan and exits without evaluating anything.
  </behavior>

  <action>
    F-15 found that today's `assertCommitted` seals the parameter FILE only,
    while `model.ts` changed twice after the holdout ran, unchecked. The seal is
    read as "the model that produced this number is pinned in git"; it does not
    currently guarantee that.

    Create `packages/bpr/sealedPaths.ts` exporting `SEALED_CODE_PATHS` =
    `packages/bpr/model.ts`, `packages/bpr/evaluate.ts`, `packages/bpr/data.ts`,
    `packages/bpr/cli.ts`, `packages/core/algorithms/bpr.ts`. Those five plus
    whichever parameter files a run names are what "sealed" now means.

    Rewrite `assertCommitted` as `assertSealed(paths, run)` where `run` is an
    injectable command runner defaulting to `execFileSync`, so the refusal logic
    is unit-testable without dirtying the real tree. For every path it must
    require an empty `git status --porcelain -- <path>` and a resolvable
    `HEAD:<path>`, and it must RETURN the HEAD sha plus each path's blob sha
    from `git rev-parse HEAD:<path>`. `holdout.ts` prints all of them in its
    header, so any run's output is self-documenting about exactly which model
    produced it.

    Add three arguments to `holdout.ts`:

      `--years <csv>` - optional, defaults to the whole holdout set. Every
      named year must be a member of `HOLDOUT_YEARS`; anything else throws a
      message naming the year and pointing the caller at `score.ts` for design
      years. Critically, it sets `stopAfterYear` to the MAXIMUM named year, so
      naming one season means the replay halts at that season's end and the
      later ones are never stepped at all. That is a structural guarantee, not a
      promise.

      `--candidate <path>` - optional second parameter file. When present,
      `holdout.ts` runs incumbent and candidate over the same match stream,
      writes one paired per-match record per line to `--out <jsonl>`
      (`matchKey`, `eventKey`, `season`, `compLevel`, `eventType`,
      `actualWinner`, both probabilities), and prints the paired accuracy and
      Brier deltas with an event-blocked interval from `eventBlockedBootstrap`
      in `packages/harness/eventBootstrap.js`.

      `--dry-run` - resolves everything (sealed paths and their blob shas, the
      year set, the resulting halt-after year, both parameter paths, the output
      path), prints it, and exits 0 having evaluated nothing. This is how the
      2023 command gets verified in Task 5 without being spent.

    On the harness import: `eventBootstrap.ts` is a parameterless resampler.
    Reaching for it does not breach the b4t design firewall, which existed to
    keep VPR's *tuned knobs* out of BPR's design - this module carries no
    parameters, no VPR knowledge, and no information about any season. Reusing
    the tested helper is strictly better than adding a fourth hand-rolled
    resampler to a repo that already has three. Say so in a one-line comment at
    the import.

    Also close a live landmine: `slice.ts` defaults its year argument to a
    holdout season and has no guard, unlike `innovations.ts` which already
    requires an explicit override. Give `slice.ts` the same override
    requirement and change its default to 2022.

    Tests in `packages/bpr/holdout.test.ts` cover each `<behavior>` bullet,
    using the injectable runner for the seal cases.

    Then produce `GUARDS-PROOF.txt` by capturing, with their stderr, the four
    refusal-and-resolution demonstrations listed in `<verify>`. Commit by
    explicit path.
  </action>

  <verify>
    <automated>npx vitest run packages/bpr 2>&1 | tail -20</automated>
    <automated>npx tsx packages/bpr/holdout.ts packages/bpr/frozen-params.json 2>&1 | tail -5; echo "---above must be a refusal, seal flag absent---"</automated>
    <automated>npx tsx packages/bpr/holdout.ts packages/bpr/frozen-params.json --years 2022 --break-seal 2>&1 | tail -5; echo "---above must be a refusal, design year named---"</automated>
    <automated>npx tsx packages/bpr/holdout.ts packages/bpr/frozen-params.json --years 2023 --break-seal --dry-run 2>&1 | tail -30</automated>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>

  <done>
    The seal covers six paths and prints a blob sha for each. The two refusals
    fire with their intended messages. The plan-resolution run prints a resolved
    plan whose halt-after year is 2023, evaluates nothing, and exits 0 - and no
    accuracy or Brier figure appears anywhere in its output. `GUARDS-PROOF.txt`
    captures all four. `slice.ts` no longer defaults to a holdout season. Tests
    green; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: The two gates - re-derive the honest bar, and test whether the champs deficit reproduces on 2016-2022 (P2)</name>

  <precondition>`reports/260908-vqr-fourway/predictions-2016.jsonl` through `predictions-2022.jsonl` exist on disk from the prior review task (gitignored, ~280MB across the six design seasons). If any is missing, regenerate with `npx tsx --env-file=.env packages/harness/cli.ts --seasons 2016-2020,2022 --algorithm epa,vpr,bpr --out reports/260909-03b/threeway` and use that directory instead.</precondition>

  <files>
    packages/bpr/ablate.ts,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/paired-bar.ts,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/champs-design-era.ts,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BAR.md,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/CHAMPS-DESIGN-ERA.txt
  </files>

  <action>
    PART A - the bar (F-19/F-21). The review measured a 1.45x design effect
    using MARGINAL standard errors and correctly refused to conclude from it,
    because ablation deltas are PAIRED. `packages/harness/eventBootstrap.ts`'s
    own header spells out why the distinction decides whether a rule is honest:
    a level SE where a paired SE belongs sets the bar far too high; the reverse
    publishes an interval too narrow. Measure the paired quantity. Do not adopt
    the review's rough 0.45pp - it came from the marginal SE and may differ
    materially in EITHER direction.

    Give `ablate.ts` an `--emit <dir>` flag writing one jsonl per variant over
    the design era: `matchKey`, `eventKey`, `season`, `compLevel`, `eventType`,
    `pRed`, `actualWinner`, for every SCORED match. Eleven variants, roughly
    25-40 seconds of replay plus write time.

    `paired-bar.ts` then loads the full-model file plus each variant, pairs on
    `matchKey`, and for each variant calls `eventBlockedBootstrap` over units
    carrying `eventKey` and both per-match hit indicators, with the statistic
    being mean(variant hits) minus mean(full-model hits). Report per variant:
    the delta, the paired clustered SE, twice that SE, and the 95% percentile
    interval. Also report the full model's LEVEL SE and its ratio to the naive
    binomial SE, as a cross-check that the 1.45x design effect reproduces. Emit
    the same paired quantities restricted to the design-era championship slice
    (event type in 2, 3, 4), because Task 4's Gate C needs a champs-slice bar
    and it must be computed BEFORE any candidate exists. Roughly 3-8 minutes for
    2,000 draws across eleven variants over ~82k units - run it in the
    background with stdout to a log and monitor by log growth.

    PART B - does the champs deficit reproduce on the design era? The hypothesis
    behind P1 was found on the HOLDOUT (2023-2026 champs-level events, n=5,820,
    BPR 74.67 vs EPA 76.51). Holdout-suggested is not fatal, but it obliges you
    to verify the effect on 2016-2022 before fitting anything to it.
    Championship events exist in every design year: measured 2026-09-09, the
    design era holds 6,634 matches at event type 3 across 50 events, 140 at type
    4, and 5,015 at type 2 (district championships).

    `champs-design-era.ts` reads the six design-season jsonl files already on
    disk, joins each `matchKey` to its event type from the corpus, applies the
    shared exclusions (surrogate-affected, offseason, actual tie), and reports
    BPR vs EPA vs VPR accuracy and Brier plus a paired event-blocked interval on
    the BPR-minus-EPA difference, on four slices: event type in {3,4}; event
    type in {2,3,4}; the review's own size proxy of events with 121-200 scored
    matches, so the design-era figure is comparable to the holdout's -1.84pp on
    the SAME definition; and everything else as the contrast. Reading ~280MB of
    jsonl takes 1-3 minutes.

    PART C - write `BAR.md`, and write it BEFORE Task 4 runs. It is the
    pre-registration, so it must contain all three gates stated numerically,
    with their verdicts for A and B filled in and Gate C stated but unresolved:

      GATE A (anti-additivity is a real axis). PASS iff the purely-additive
      ablation's paired delta has a 95% interval EXCLUDING zero. If it includes
      zero, anti-additivity is not distinguishable from zero, P1's premise is
      dead, and Task 4 is SKIPPED. Record that as a result, not a failure - it
      is exactly what P2 was cheap enough to be worth running first for.

      GATE B (the champs deficit is real on design-era evidence). PASS iff
      BPR-minus-EPA is negative with a 95% interval excluding zero on at least
      the {2,3,4} definition. If it does not reproduce, the holdout slice was
      noise at n=5,820, and P1 is DROPPED rather than fitted to a number Jacob
      cannot honestly re-test. Task 4 is SKIPPED.

      GATE C (stated now, resolved in Task 4). A candidate is frozen iff ALL of:
      (i) the selected spread coefficient is positive - the direction the champs
      mechanism predicts, and the search grid includes negative values
      specifically so this can be falsified rather than only confirmed;
      (ii) on the FULL design era, accuracy does not fall and Brier does not
      rise - no regression anywhere;
      (iii) on the design-era champs slice, the accuracy gain exceeds the
      champs-slice paired clustered two-sigma bar measured in Part A AND Brier
      falls - Rule A on the slice the mechanism targets.
      Any one of those failing means no candidate is frozen.

    Record the bar's numeric value, both slice bars, and both verdicts, then
    commit by explicit path.

    If Gate A or Gate B says STOP: do not start Task 4. Go directly to Task 5's
    negative branch.
  </action>

  <verify>
    <automated>npx tsx packages/bpr/ablate.ts packages/bpr/frozen-params.json --emit reports/260909-03b/ablate 2>&1 | tail -20 && ls reports/260909-03b/ablate/*.jsonl | wc -l</automated>
    <automated>npx tsx .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/paired-bar.ts 2>&1 | tee .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BAR-OUTPUT.txt | tail -40</automated>
    <automated>npx tsx .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/champs-design-era.ts 2>&1 | tee .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/CHAMPS-DESIGN-ERA.txt | tail -30</automated>
    <automated>grep -n "^GATE [ABC]:" .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BAR.md</automated>
  </verify>

  <done>
    Eleven variant jsonl files exist. `BAR.md` carries the measured paired
    clustered two-sigma bar for the full design era and for the champs slice,
    the anti-additivity verdict against that bar, the design-era champs verdict
    with its interval on all four slice definitions, and three lines beginning
    `GATE A:`, `GATE B:`, `GATE C:` - the first two resolved to PASS or STOP,
    the third stated in full and marked pending. The 1.45x marginal design
    effect is reproduced as a cross-check, and the paired figure is reported
    beside it with the difference explained.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Spread-responsive anti-additivity - inert at default, proven bit-identical, selected on 2016-2022 (P1)</name>

  <precondition>`BAR.md` records `GATE A: PASS` and `GATE B: PASS`. If either says STOP, this task does not run at all.</precondition>

  <reversibility rating="reversible">A knob that defaults to its inert value and is proven bit-identical at that value can be removed by deleting it; nothing downstream depends on it until a parameter file names it.</reversibility>

  <files>
    packages/bpr/model.ts,
    packages/core/algorithms/bpr.ts,
    packages/bpr/tune.ts,
    packages/bpr/model.test.ts,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/inertness-replay.ts,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/INERTNESS.txt,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/GATE-C.txt
  </files>

  <behavior>
    - At the inert default the weight vector handed to the rank assignment is the same array today's code builds, taken from the same branch.
    - The league-mean spread is updated only during the update step, never during prediction, so the value used to predict a match depends only on matches already stepped.
    - Before any alliance has been observed, the responsiveness factor is exactly one.
    - A narrower-than-average alliance spread relaxes rank suppression toward additivity at a positive coefficient, and a wider-than-average spread strengthens it.
    - The responsiveness factor is clamped to a bounded range, so an extreme spread cannot invert or explode a weight.
  </behavior>

  <action>
    STEP 1 - dump the inertness baseline BEFORE editing anything. Write
    `scripts/inertness-replay.ts`, which steps every match in the corpus through
    a named module and emits one line per match: `matchKey` and the predicted
    red-win probability at full precision. It computes NO accuracy, NO Brier, NO
    log loss - it has no such code path, which is what makes it safe to run over
    every season under hard rule 3. Run it twice now, once against
    `packages/bpr/model.ts` at the frozen parameters and once against
    `packages/core/algorithms/bpr.ts`, into
    `reports/260909-03b/inertness-baseline-{research,port}.jsonl`. Roughly 10
    seconds each.

    STEP 2 - the knob, in BOTH modules, with the SAME name and the SAME inert
    default of zero: `spreadBeta`, added to `BprParams` and to `DEFAULTS` in
    `packages/bpr/model.ts`, and to `BprParams` and `BPR_PARAMS` in
    `packages/core/algorithms/bpr.ts`.

    The mechanism is pre-registered here so that no functional form gets
    searched over and cherry-picked. Today's alliance view ranks teams and hands
    out fixed weights of one, w2, w3, renormalized to sum to three. The change
    lets those respond to the alliance's own rating spread:

      s = (max(mus) - min(mus)) / max(mean(mus), 1e-6), over the alliance's
      pre-match slow-plus-fast means, computed inside the alliance view.

      spreadBar is an ONLINE running mean of s, held on the model, updated only
      inside `update`, once per alliance so twice per match, with the same
      learning-rate form the season scale already uses:
      `lr = max(p.scaleMinLr, 1 / (spreadCount + 1))`. It is therefore causal -
      the mean used to predict match k depends only on matches 1 through k-1.

      f = clamp(1 + spreadBeta * (s - spreadBar) / max(spreadBar, 1e-6), 0, 2),
      and f is exactly 1 when no alliance has been observed yet.

      w2' = 1 + (w2 - 1) * f, w3' = 1 + (w3 - 1) * f, then rank-assign and
      renormalize to sum to three exactly as today.

    MANDATORY implementation detail: guard the whole thing behind an explicit
    early branch on the coefficient being zero, which builds the weight base
    array from the untouched parameters. Do NOT rely on the arithmetic
    collapsing to an identity - `1 + (0.7 - 1) * 1` is not guaranteed to return
    the original double bit-for-bit, and inertness that depends on floating-point
    luck is not inertness. The branch makes it structural, and step 4 proves it.

    Sign convention, stated before measurement: a positive coefficient means a
    wider-than-average spread gets MORE rank suppression and a narrower one gets
    LESS. The championship hypothesis - every alliance member is strong, so the
    true spread is narrow and suppressing the second and third over-corrects -
    predicts a POSITIVE coefficient.

    STEP 3 - `tune.ts`: add `spreadBeta` to the grid with values
    `[-0.5, -0.25, 0, 0.25, 0.5, 0.75, 1.0, 1.5]`. The negative values are there
    deliberately: a one-sided grid could only ever confirm the hypothesis.

    STEP 4 - prove inertness. Re-run `inertness-replay.ts` against both modules
    and diff against the two baselines from step 1. The requirement is ZERO
    mismatches across every match in the corpus, in both modules - the same
    standard the phase-components work met at 152,757 matches and 0 mismatches.
    Report the compared count and the mismatch count into `INERTNESS.txt`. Add a
    case to `packages/bpr/model.test.ts` pinning the same property on synthetic
    fixtures, so a future edit cannot break inertness silently.

    STEP 5 - select the coefficient on the design era ONLY, and move nothing
    else. This is a one-knob addition to an already-frozen model, not a fresh
    search: re-tuning everything at once would confound the knob's credit with a
    new optimum, which is exactly F-23's criticism of the existing ablation
    table. Seed from the frozen file and skip every other key:

      `BPR_SEED=packages/bpr/frozen-params.json BPR_SWEEPS=2 \
       BPR_SKIP_KEYS=obsSd,qSlow,rhoFast,qFast,priorVar,fastPriorVar,rookieMean,seasonShrink,seasonVar,tauLr,foulObsSd,foulQ,foulPriorVar,elimWeight,scaleMinLr,w2,w3,defPriorVar,defQ,huberK,biasLr,obsSdSlope \
       npx tsx packages/bpr/tune.ts > reports/260909-03b/tune-spreadbeta.json`

    Eight values over two sweeps at roughly 1.2s per evaluation - well under a
    minute. `tune.ts`'s own guard still refuses any year past 2022, and it is
    not touched.

    STEP 6 - resolve Gate C. Emit per-match predictions at the selected
    coefficient and at zero over the design era (reuse `ablate.ts --emit` with a
    two-variant parameter pair, or a small dedicated dump), pair on `matchKey`,
    and run the same `eventBlockedBootstrap` paired statistic Task 3 used, on
    three populations: the full design era, the champs slice (event type in 2,
    3, 4), and everything else. Report accuracy delta with its interval and
    Brier delta for each. Then write `GATE-C.txt` stating each of the three
    conditions from `BAR.md` and whether it is met, and the overall verdict.

    Commit by explicit path. Do NOT write a candidate parameter file here - that
    is Task 5's job, and only on a passing verdict.
  </action>

  <verify>
    <automated>npx tsx .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/inertness-replay.ts --check 2>&1 | tee .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/INERTNESS.txt | tail -20</automated>
    <automated>npx vitest run packages/bpr packages/core/algorithms/bpr.test.ts packages/harness 2>&1 | tail -20</automated>
    <automated>npx tsx packages/bpr/score.ts packages/bpr/frozen-params.json 2016,2017,2018,2019,2020,2022 2>&1 | tail -10</automated>
    <automated>npx tsc --noEmit 2>&1 | tail -20 && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -20</automated>
    <automated>grep -c . .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/GATE-C.txt</automated>
  </verify>

  <done>
    `spreadBeta` exists in both modules with the same name and the same inert
    default. `INERTNESS.txt` records the compared match count and a mismatch
    count of exactly zero for BOTH modules. The design-era figure with the
    frozen parameters is UNCHANGED from Task 1's post-P3 value, to the digit -
    the knob has not perturbed the incumbent. The coefficient was selected on
    2016-2022 with every other knob pinned. `GATE-C.txt` states all three
    conditions and a single overall verdict. Full test suite for the touched
    packages green; both typechecks clean.
  </done>
</task>

<task type="auto">
  <name>Task 5: Freeze the candidate, pre-register the acceptance rule, hand over one command - and do not run it</name>

  <reversibility rating="costly">This task produces the artifact that authorizes a genuinely one-way action - spending a season of the holdout. The freeze itself is reversible; the run it enables is not, which is exactly why the run belongs to Jacob and not to this task.</reversibility>

  <files>
    packages/bpr/candidate-spread-params.json,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/PRE-REGISTRATION.md,
    .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/260909-03b-SUMMARY.md,
    .planning/STATE.md
  </files>

  <action>
    BRANCH 1 - Gates A, B and C all passed.

    Write `packages/bpr/candidate-spread-params.json`: the frozen parameter set
    verbatim plus the selected `spreadBeta`, with a `_note` recording that it
    was selected on 2016-2022 alone, the design-era and champs-slice deltas it
    achieved, the bar it cleared, and that `frozen-params.json` is untouched. A
    NEW file - never an edit to the sealed one.

    Commit the code and both parameter files FIRST, by explicit path, and note
    the resulting HEAD sha. Then write `PRE-REGISTRATION.md` and commit it
    second. The seal covers the code and parameters, which is what matters; a
    document cannot record the sha of the commit that contains it.

    `PRE-REGISTRATION.md` contains, in this order:

      1. THE COMMAND, on one copy-pasteable line:
         `npx tsx packages/bpr/holdout.ts packages/bpr/frozen-params.json --candidate packages/bpr/candidate-spread-params.json --years 2023 --break-seal --out reports/bpr-2023-test.jsonl`
      2. What it does, and what it will print.
      3. THE ACCEPTANCE RULE, numerically, written before the result exists:
         Rule A on the 2023 combined view - the candidate's accuracy must exceed
         the incumbent's AND its Brier must be lower - AND the accuracy gain must
         exceed the single-season paired clustered two-sigma bar, whose numeric
         value is recorded here and was derived from design-era seasons in
         Task 3. Both models are scored in the SAME run over the SAME matches
         under the SAME post-P3 measurement path, so no previously published
         figure is relied on.
      4. The pre-registered secondary: the 2023 champs-slice delta, reported
         because that slice is what the mechanism targets, and NOT part of the
         accept/reject rule.
      5. "Keep the incumbent is a success." Stated plainly. A negative result
         here is a real measurement and closes the axis honestly.
      6. "Run once. Whatever it prints is the result." No re-tuning, no one more
         variant, no quiet second run.
      7. The HEAD sha and the blob sha of every sealed path, so the run's own
         header can be checked against this document line by line.
      8. What is NOT claimed: the three later holdout seasons remain entirely
         unspent by this test, because the year argument binds the halt-after
         season, so the replay stops at the end of 2023 and never steps a later
         match. Note also that the incumbent's per-season 2023 figure under the
         OLD measurement path is not comparable to what this run prints, and
         that the pairing is what makes the comparison valid.

    Verify the command PARSES by running it with `--dry-run` appended, and paste
    that output into the document. Never run it without.

    BRANCH 2 - any gate said STOP.

    No candidate parameter file. If Gate C failed specifically, the knob exists
    and is proven inert: leave it in `packages/bpr/model.ts`, which by its own
    header retains rejected ideas behind inert defaults for future research, and
    REMOVE it from `packages/core/algorithms/bpr.ts`, whose header states that
    rejected ideas are deliberately absent there rather than present-but-disabled.
    That is the repo's existing, documented convention and Jacob's standing rule
    against parameter bloat; re-run the port inertness replay after removal to
    confirm zero mismatches. Replace `PRE-REGISTRATION.md` with
    `NEGATIVE-RESULT.md` recording what was measured, which gate said stop with
    what interval, why the proposal is not advanced, and the explicit statement
    that NO 2023 test is warranted because there is nothing to test.

    BOTH BRANCHES.

    Write `260909-03b-SUMMARY.md` using a Bash heredoc - the Write tool is
    blocked from that filename, and the heredoc must stay short enough to avoid
    the `ENAMETOOLONG` spawn limit, so build it in two or three appends. It must
    decompose the moves separately, never conflated: the design-era figure
    before P3 and after it, with the delta named as the honest number replacing
    a flattering one rather than a regression; the measured bar next to the
    review's rough estimate, with the direction and size of the difference; and,
    separately, whatever the model change did or did not buy.

    Append the STATE.md quick-task row - no raw pipe character anywhere in the
    description, and verify the appended HEAD sha afterwards, since the helper
    stamps it at append time and another session may have pushed in between.

    Commit by explicit path only.
  </action>

  <verify>
    <automated>test "$(git log --oneline -- packages/bpr/frozen-params.json | wc -l)" = "1" && echo "SEALED FILE UNTOUCHED with one commit" || echo "FAILURE the sealed parameter file history changed"</automated>
    <automated>git status --porcelain -- packages/bpr packages/core/algorithms/bpr.ts .planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa; echo "---above must be empty, everything committed---"</automated>
    <automated>test ! -f reports/bpr-2023-test.jsonl && echo "HOLDOUT TEST NOT RUN, output file absent" || echo "FAILURE the holdout test was run"</automated>
    <automated>npx tsx packages/bpr/holdout.ts packages/bpr/frozen-params.json --candidate packages/bpr/candidate-spread-params.json --years 2023 --break-seal --out reports/bpr-2023-test.jsonl --dry-run 2>&1 | tail -30</automated>
    <automated>npx vitest run packages/bpr packages/core packages/harness 2>&1 | tail -15</automated>
  </verify>

  <done>
    `frozen-params.json` still has exactly one commit in its history and a clean
    working tree. The holdout output file does not exist, proving the test was
    built and not spent. On the passing branch,
    `candidate-spread-params.json` exists as a new file and
    `PRE-REGISTRATION.md` carries all eight numbered sections including a
    plan-resolution transcript showing the halt-after year is 2023. On a
    stopping branch, `NEGATIVE-RESULT.md` names the gate, its interval, and why
    no test is warranted. SUMMARY.md decomposes the three moves separately.
    STATE.md has a new row whose description contains no pipe character, with a
    verified sha. Everything committed by explicit path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| holdout seasons -> design decisions | The 2023-2026 corpus is the only remaining out-of-sample evidence. Any path by which a post-2022 number reaches a threshold, a hyperparameter, or an accept/reject choice is a breach. |
| git -> the seal | `holdout.ts`'s refusal is the mechanism that makes a reported number provably belong to a specific model. If the seal does not cover what a reader believes it covers, the guarantee is fictional. |
| `.env` -> transcript | Live TBA and R2 credentials sit in a gitignored file. Nothing in this task needs them; the exposure path is a careless read, not a commit. |
| concurrent session -> this task's commits | Another session is editing `apps/web` in the same checkout. A broad stage would absorb foreign work into these commits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03b-01 | Information disclosure | `slice.ts`, `innovations.ts`, `equivalence.ts` - scripts that reach post-2022 seasons | high | mitigate | Task 2 gives `slice.ts` the explicit-override guard `innovations.ts` already has and moves its default to a design year; Task 1 makes `equivalence.ts` design-era by default behind an explicit flag. Hard rule 2 forbids passing either flag in this task. |
| T-03b-02 | Tampering | `holdout.ts` seal covering parameters only (F-15) | high | mitigate | Task 2 extends the seal to five code paths plus the parameter files and prints a per-path blob sha, so a post-seal model edit is visible in the run's own output rather than needing manual forensics. |
| T-03b-03 | Elevation of privilege | the year argument and halt-after binding on the 2023 command | critical | mitigate | The year argument rejects any non-holdout year and binds the halt-after season to the maximum named year, so a 2023 run structurally cannot step a later season. Task 2 proves it with a plan-resolution run; Task 5 pastes that transcript into the pre-registration. |
| T-03b-04 | Information disclosure | `.env` secrets reaching a transcript | critical | mitigate | No command in this plan reads `.env`. The one command that would normally take an env-file argument (the harness fallback in Task 3's precondition) passes the path to the tool and never reads the file. Never render it into any output stream. |
| T-03b-05 | Tampering | Foreign edits absorbed by a broad stage | medium | mitigate | Hard rule 10: explicit-path staging only, with a `git status --porcelain` inspection before every commit. |
| T-03b-06 | Repudiation | A measured number that cannot be traced to the code that produced it | medium | mitigate | Every evidence file records the HEAD sha; the bar and both gate verdicts are written into `BAR.md` before the model change runs, so the pre-registration cannot be back-fitted. |
| T-03b-SC | Tampering | package-manager installs | low | accept | This task installs nothing. No npm, pip, or cargo install appears in any task; `pnpm install` is explicitly forbidden by the environment section. No legitimacy audit is required because no package is added. |
</threat_model>

<verification>
Run from the repo root, after Task 5:

```
npx vitest run packages/bpr packages/core packages/harness
npx tsc --noEmit
npx tsc --noEmit -p apps/web/tsconfig.json
git log --oneline -- packages/bpr/frozen-params.json | wc -l      # must be 1
```

The full root suite (`npx vitest run`) will also surface the concurrent
session's in-flight `apps/web` edits. Record its status, but attribute any
`apps/web` failure by checking `git status --porcelain -- apps/web` first -
those files are someone else's work in progress, not this task's regression.
Judge every run by its OUTPUT, never by an exit code.
</verification>

<success_criteria>
- The design-era figure moved down to the honest value, with the pre-change 73.08 recorded beside it and the delta named as decomposition, not regression.
- BPR's loaded population reconciles exactly, per season, against the shared-harness recount.
- The seal covers the model and the shipped port, with per-path blob shas.
- A 2023 run structurally cannot step a later season, demonstrated by a plan-resolution transcript.
- The selection bar is a measured paired event-clustered number, reported next to the review's marginal estimate with the difference explained.
- Gate A and Gate B each have an explicit verdict with an interval behind it.
- Either a candidate exists with a written acceptance rule and one documented command - or a negative result explains why none does.
- The 2023 test has NOT been run: `reports/bpr-2023-test.jsonl` does not exist.
- `frozen-params.json` still has exactly one commit.
</success_criteria>

<output>
Create `.planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/260909-03b-SUMMARY.md` when done (Bash heredoc - the Write tool is blocked from that filename).
</output>
