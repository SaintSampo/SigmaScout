---
phase: quick-260911-3kc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measureEpaDeviations.ts
  - scripts/measureEpaDeviations.test.ts
  - packages/core/algorithms/epaCarryScale.ts
  - packages/core/algorithms/epaCarryScale.test.ts
  - packages/core/algorithms/epa.ts
  - packages/core/algorithms/epa.test.ts
  - packages/core/algorithms/carryover.ts
  - packages/core/algorithms/carryover.test.ts
  - packages/harness/stateSnapshot.ts
  - packages/harness/stateSnapshot.test.ts
  - packages/harness/replay.season.test.ts
  - docs/models/epa-divergences.md
  - reports/epa-deviation-ablation.json
  - data/diagnostics/epa-deviation-ablation.json
  - .planning/quick/260911-3kc-land-the-epa-carryover-scale-anchor-fix-/260911-3kc-SUMMARY.md
autonomous: true
requirements: [QUICK-260911-3kc]

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A team that crosses a season boundary enters the new season expressed in the INCOMING season's point units, not the outgoing season's."
    - "The shipped default reproduces the measured arm: after landing, the harness's unmodified `epa` baseline reports the same per-season and pooled winner accuracy and Brier as the winning carryover arm did before landing, to 6 decimal places."
    - "The deferral threshold was chosen from a candidate set and by a rule that were both written down BEFORE any threshold number existed, and the plain fix wins any tie."
    - "A stale seeded D1 row from before this change fails loudly at load time rather than silently disabling the rescale on live traffic."
    - "carryover.ts's header no longer describes the outgoing-season anchor as an open placeholder, and states the walk-forward constraint that makes our resolution an approximation of Statbotics rather than an equal of it."
    - "docs/models/epa-divergences.md records this as NARROWING a divergence, with the measured effect including where the fix is worse."
    - "Nothing under packages/bpr/ is read, replayed, tuned or edited, and BPR's sealed 2016-2022/2023-2026 holdout is not spent."
    - "No republish runs. No R2 write, no D1 seed, no publish:seasons, no deploy."
  artifacts:
    - packages/core/algorithms/epaCarryScale.ts
    - packages/core/algorithms/epaCarryScale.test.ts
    - data/diagnostics/epa-deviation-ablation.json
    - docs/models/epa-divergences.md
  key_links:
    - "epa.carrySeason -> captures allianceScoreStats.mean BEFORE delegating -> EpaState.carrySeedMean"
    - "epa.predict/epa.update -> epaCarryScale.cleanSeasonMean + carryRescaleRatio -> per-team first-sight materialization"
    - "EpaState.carrySeedMean/carryPending -> stateSnapshot serializeEpaState -> STATE_SNAPSHOT_SHAPE_VERSION 12 -> LeagueRowShapeVersionError on a stale seed row"
    - "landed epa baseline rows -> compared against task-1 artifact's winning arm rows (the reproduction gate)"
---

<objective>
Land the EPA season-boundary carry scale-anchor fix as the shipped default, after first
measuring the one open refinement stage 1 left on the table.

Stage 2 of 4. Stage 1 (quick task 260910-x09, SUMMARY at `3a659864`) already measured the
fix as an arm over nine seasons: pooled winner accuracy **+0.01059** [+0.00846, +0.01285],
Brier **-0.00521** [-0.00638, -0.00412], with the gain concentrated at 2019 (+0.0794 ACC)
— the pre-registered mechanism test passing. Those numbers are the authority and are NOT
re-derived here.

Purpose: `packages/core/algorithms/carryover.ts`'s `epaCarryover` derives one `{mean, sd}`
pair from the OUTGOING season (line 228) and uses it for BOTH `normalizedFromPoints` and
`normalizedToSeasonUnits`, so every team enters a new season carrying last season's point
units. The project's own verbatim reading of Statbotics' `init.py`
(`.planning/phases/02-prediction-models-epa-sigma1/02-CONTEXT.md` D-16, `02-RESEARCH.md`)
specifies conversion into the NEW season's units. This is a port defect against our own
recorded reference, and it would be worth fixing at neutral accuracy.

Output: `epa@8.0.0+baseline` shipping the lazy per-team rescale; a contract test pinning
incoming-season units; a corrected `carryover.ts` header; a new `epa-divergences.md`
section; a regenerated ablation artifact with the deviation marked closed; and a SUMMARY
that names every artifact this change makes stale.

**No tracer task.** The architecture is already proven end-to-end: `scripts/measureEpaDeviations.ts`'s
`carryoverFixArm()` ran this exact mechanism over 147,221 matches across nine seasons and
1,653 event blocks. A thin vertical slice would add no information that run has not already
produced. This plan is expansion from a proven slice, not exploration.

## HARD PROHIBITIONS — every task

1. **Do not touch BPR.** Nothing under `packages/bpr/`, nothing that replays, tunes,
   ablates or scores BPR, and nothing that reads its sealed 2016-2022/2023-2026 holdout.
   BPR is not an arm in this harness and must not become one. (MEMORY: "Never tune BPR".)
2. **Do not run a republish.** No `pnpm publish:seasons`, no `publish.ts --event`, no R2
   write, no `d1 execute`, no `wrangler deploy`. A republish IS owed after this change and
   the SUMMARY must say so — but it is a separate, later job.
3. **Do not touch VPR/sigma1, PCM, or GBR carry paths.** `sigma1/carryover.ts` keeps its
   own copy of the carry math by D-04 design; making it read EPA's new anchor would
   dissolve the frozen-baseline guarantee. Leave it alone.
4. **Do not read or echo `.env`.** This work needs no secrets. `measureEpaDeviations.ts`
   must never be run with `--env-file`. (CLAUDE.md "Secrets handling".)
5. **Do not touch the frozen D-04 constants.** `EPA_MEAN_REVERSION`,
   `EPA_CARRY_LAST_YEAR_WEIGHT`, `EPA_CARRY_PRIOR_YEAR_WEIGHT`, `EPA_NORM_MEAN`,
   `EPA_NORM_SD`, `EPA_INIT_PENALTY` stay at Statbotics' published values. This change
   corrects the SCALE ANCHOR, not the carry parameters.
6. **Do not tune anything.** Task 1 selects one threshold from a three-value pre-declared
   set by a pre-declared rule. That is not a sweep and must not become one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/quick/260910-x09-build-the-epa-deviation-ablation-harness/260910-x09-SUMMARY.md
@scripts/measureEpaDeviations.ts
@packages/core/algorithms/carryover.ts
@packages/core/algorithms/epa.ts
@packages/harness/stateSnapshot.ts
@docs/models/epa-divergences.md
</context>

<environment>
- Corpus `data/corpus.sqlite` is 582 MB and gitignored. **Worktree isolation is DISABLED
  and must stay disabled** — a worktree would not carry the corpus.
- A full nine-season run takes minutes. **Background it and poll**; do not block on it.
- Another Claude session is active in this checkout with its own uncommitted files.
  **Stage only your own files by explicit path.** Never `git add -A`. Commit with
  `git commit -- <pathspec>` and verify with `git show --stat` that nothing foreign landed.
- Run tests with `npx vitest run <paths>`. **Verify by reading output, not exit codes.**
  Never wrap a `pnpm` command in `timeout` — it swallows output and exits 0.
- Root `tsc --noEmit` does NOT cover `apps/web`. If anything under `apps/web` changes, run
  the web tsconfig too.
- A silent `NaN` formats as `-` and reads identically to "no data". Every numeric
  comparison this plan introduces must fail loud on a non-finite value, following
  `finiteOrThrow`'s existing pattern in the harness.
</environment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure the deferral-threshold refinement — pre-declared set, pre-declared rule, plain fix wins ties</name>
  <files>scripts/measureEpaDeviations.ts, scripts/measureEpaDeviations.test.ts, reports/epa-deviation-ablation.json, data/diagnostics/epa-deviation-ablation.json</files>

  <read_first>
`scripts/measureEpaDeviations.ts` — specifically `EPA_CARRY_RESCALE_MIN_OBS` (~line 226),
`cleanSeasonMean` (~line 288), `carryRescaleRatio` (~line 313), `ratioForState` (~line 996),
`carryoverFixArm` (~line 1012), `ARM_IDS` (~line 206), `contrastFor` (~line 1173, note it
already carries a `baselineArmId` field hardcoded to `BASELINE_ARM_ID`), and the per-season
arm list (~line 1365). Also `scripts/measureEpaDeviations.test.ts` for the existing
`cleanSeasonMean` / `carryRescaleRatio` describe blocks.
  </read_first>

  <behavior>
Tests to write BEFORE the arms exist (add to `scripts/measureEpaDeviations.test.ts`):
- `carryoverFixArm(id, minObs)` threads `minObs` all the way to `cleanSeasonMean` — an arm
  built at `minObs = 500` returns `{deferred: true}` from its ratio path at 400 real folds
  and `{deferred: false}` at 600, where a `minObs = 100` arm returns `{deferred: false}` at
  both. Assert against the exported pure helpers, not by replaying a season.
- `contrastFor` records the reference arm it was actually measured against: a contrast
  built with `referenceArmId = CARRYOVER_FIX_ARM_ID` reports that id in `baselineArmId`,
  not `BASELINE_ARM_ID`. A contrast that mislabels its own reference would invert the
  selection decision, which is why this is pinned.
- The selection rule and candidate set are present in the artifact as literal data
  (`notes.thresholdSelection`), and the candidate list has exactly three entries.
- `selectThreshold(...)` (the pure decision function) returns the incumbent `100` when
  handed challenger contrasts whose intervals all span zero, and returns the SMALLER
  passing threshold when handed two passing challengers.
  </behavior>

  <action>
Add the refinement arms to the EXISTING harness. Do not create a second script.

**1. Parameterize the arm.** Give `carryoverFixArm` an explicit id and `minObs`:
`carryoverFixArm(id: string = CARRYOVER_FIX_ARM_ID, minObs: number = EPA_CARRY_RESCALE_MIN_OBS)`.
Thread `minObs` through `ratioForState` into `cleanSeasonMean`'s fourth parameter. The
existing zero-argument call site must behave byte-identically to today.

**2. The candidate set — THREE values, pre-declared, no sweep.**
`EPA_CARRY_RESCALE_MIN_OBS ∈ {100, 250, 500}` alliance scores (≈50, 125, 250 matches).
`100` is the INCUMBENT — the plain fix stage 1 already measured and proved. `250` and `500`
are the two new arms, ids `epa-carryover-fix-min250` and `epa-carryover-fix-min500`. Add
both to `ARM_IDS` and to the per-season `algorithms` array. Three points bracketing "one
event's quals" to "roughly five events" is the whole search. Adding a fourth value, or
scanning a range, is forbidden: a sweep over thresholds scored on nine seasons that include
2019 is selection against the single season carrying the entire effect.

**3. The selection rule — PRINTED AND SERIALIZED BEFORE ANY NUMBER IS COMPUTED.**
Emit it in `main()`'s preamble block (alongside the existing PRE-REGISTERED paragraphs,
before the corpus is opened) and into the artifact as `notes.thresholdSelection`
`{ candidates, rule, tieBreak, default, selected: null }` — `selected` filled only after
the run. The rule, verbatim, in this order:
  - PRIMARY: pooled ONSET Brier, challenger measured against the INCUMBENT arm (not against
    baseline), paired and event-blocked. A challenger passes only if it is BETTER with a 95%
    interval EXCLUDING zero.
  - GUARD 1: pooled SEASON winner accuracy vs the incumbent — reject if WORSE with an
    interval excluding zero.
  - GUARD 2: pooled SEASON Brier vs the incumbent — reject if WORSE with an interval
    excluding zero.
  - TIE-BREAK: if both challengers pass, take the SMALLER threshold.
  - DEFAULT: if no challenger passes all three, the shipped default is the incumbent at
    `minObs = 100`. Print that as the OUTCOME, in those words, not as a failure — an
    unnecessary knob is a permanent cost and the plain fix is already proven.

**4. Challenger-vs-incumbent contrasts.** Add a `referenceArmId: string = BASELINE_ARM_ID`
parameter to `contrastFor` and set the row's existing `baselineArmId` field from it. Emit,
for each of the two challenger arms, contrasts against `CARRYOVER_FIX_ARM_ID` at pooled-season,
pooled-onset, and per-season/per-onset scope, IN ADDITION to the existing vs-baseline
contrasts for every arm (do not remove those). Bump `SCHEMA_VERSION` to `2` and note in its
doc comment that contrasts now carry a meaningful `baselineArmId` that a consumer must read
rather than assume.

**5. Implement `selectThreshold`** as an exported pure function over the contrast rows, so
the decision is unit-tested rather than made by eye in a log. It returns the chosen threshold
plus a prose reason string naming which clause fired. Write its result into
`notes.thresholdSelection.selected` and print it under a `THRESHOLD SELECTION` heading.

**6. Report, but do NOT let the rule read:** per-arm 2019 onset Brier, 2019 season ACC, and
the per-boundary deferral census (`carriedTeams` / `rescaledTeams` / `deferredRescales` /
`neverSeen`) for all three arms. These are named secondaries for the SUMMARY's honesty
ledger, deliberately outside the decision.

**Guard every printed figure with `finiteOrThrow`.** A refinement arm that silently emits
`NaN` would render as `-` and read as "no data" rather than as a broken arm.

**7. Run it.** `pnpm measure:epa-deviations 2>&1 | tee reports/epa-deviation-ablation-run.log`
backgrounded over the full default nine seasons; poll for completion. Then copy the working
artifact to the committed path (`data/diagnostics/epa-deviation-ablation.json`) exactly as
stage 1 did — identical bytes.

**8. Preserve the reference for task 3.** Before anything in task 2 changes `packages/`,
copy the freshly-written `data/diagnostics/epa-deviation-ablation.json` to the scratchpad as
`pre-landing-ablation.json`. Task 3's reproduction gate reads it. Losing it means re-running.

**Still no `packages/` edits in this task.** `git diff -- packages/` must be empty at this
task's commit — the arms remain wrappers and nothing shipped has changed yet.
  </action>

  <verify>
    <automated>npx vitest run scripts/measureEpaDeviations.test.ts</automated>
    <automated>git diff --stat -- packages/ | tee /dev/stderr | grep -c . | grep -qx 0 &amp;&amp; echo "PACKAGES CLEAN"</automated>
    <automated>node -e "const a=require('./data/diagnostics/epa-deviation-ablation.json');const t=a.notes.thresholdSelection;if(!t||t.candidates.length!==3)throw new Error('candidate set missing or not 3');if(t.selected===null)throw new Error('selection not recorded');console.log('SELECTED',t.selected,t.reason)"</automated>
  </verify>

  <done>
`data/diagnostics/epa-deviation-ablation.json` contains three carryover arms, a
`notes.thresholdSelection` block whose candidate set and rule are literal data, a recorded
`selected` threshold with the clause that produced it, and challenger-vs-incumbent contrasts
carrying `baselineArmId: "epa-carryover-fix"`. `git diff -- packages/` is empty. The
pre-landing artifact copy is in the scratchpad.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land the fix as the shipped default — epa@8.0.0, snapshot shape, contract test</name>
  <files>packages/core/algorithms/epaCarryScale.ts, packages/core/algorithms/epaCarryScale.test.ts, packages/core/algorithms/epa.ts, packages/core/algorithms/epa.test.ts, packages/harness/stateSnapshot.ts, packages/harness/stateSnapshot.test.ts, scripts/measureEpaDeviations.ts, scripts/measureEpaDeviations.test.ts</files>

  <precondition>Task 1 committed, and its selected threshold is readable from `data/diagnostics/epa-deviation-ablation.json`'s `notes.thresholdSelection.selected`. Halt if absent — landing an unselected threshold is landing an unmeasured one.</precondition>

  <read_first>
`packages/core/algorithms/epa.ts` lines 300-360 (`EpaState`, `initState`), 500-520
(`standardDeviation` on `allianceScoreStats`), 660-790 (`update`, the `allianceScoreStats`
fold), 881-950 (`carrySeason`), 1060-1111 (the version ledger and module literal).
`packages/harness/stateSnapshot.ts` lines 82-300 (the `STATE_SNAPSHOT_SHAPE_VERSION` bump
ledger and its "load-bearing, not ceremony" reasoning), 576-645 (`serializeEpaState` /
`deserializeEpaState`), and the `MAX_LEAGUE_ROW_BYTES` doc comment.
`scripts/measureEpaDeviations.ts` lines 1012-1095 (`carryoverFixArm`) — **the landed code
must match this mechanism exactly**, including which state each half reads.
  </read_first>

  <behavior>
New contract test (`packages/core/algorithms/epa.test.ts`) — the pin this whole task exists
to establish. Write it RED first:
- **Incoming-season units.** Build an `EpaState` carried out of a high-scoring season
  (`allianceScoreStats.mean = M_out`), cross a boundary, then fold at least `minObs` real
  alliance scores at a materially lower new-season mean `M_in`. A carried team's first-sight
  component total must equal `carriedPoints * (M_in / M_out)` within float tolerance — NOT
  `carriedPoints`. Assert the un-multiplied value explicitly fails, so the test cannot pass
  against the old behaviour.
- **Pinned-zero stays zero.** EPA pins `adjust` at exactly 0 (D-5); after rescale it is
  still exactly 0.
- **No double-apply.** A team materialized once and then seen again in a later match is not
  multiplied a second time.
- **Deferral is a forfeit, not a delay.** A team first seen BEFORE the ratio is readable is
  materialized at ratio 1 and leaves pending permanently — it is never rescaled later, after
  its components have already absorbed this season's observations.
- **predict and update agree.** For the same match against the same pre-update state,
  `predict`'s transient materialization and `update`'s permanent one produce the same
  component values. A divergence here is a live/offline split of exactly the class the
  snapshot ledger's 10 -> 11 bump was written about.
- **Cold start is untouched.** `boundary.isColdStart` leaves pending empty and rescales
  nothing.
- **Stale snapshot fails loud.** `deserializeState` on an EPA league row declaring
  `snapshotShapeVersion: 11` throws `LeagueRowShapeVersionError` (`stateSnapshot.test.ts`).
- **Round trip.** A state with a non-empty pending set and a finite `carrySeedMean`
  serializes and deserializes to an equal state.
  </behavior>

  <action>
**1. Move the pure helpers into `packages/`, do not copy them.** Create
`packages/core/algorithms/epaCarryScale.ts` holding `EPA_CARRY_RESCALE_MIN_OBS` (set to task
1's SELECTED threshold), `cleanSeasonMean`, `carryRescaleRatio`, `rescaleComponents`, and
`materializePendingTeams`, moved verbatim from `scripts/measureEpaDeviations.ts` with their
doc comments intact. Move their existing describe blocks into
`packages/core/algorithms/epaCarryScale.test.ts` — the tests move with the code, they are not
dropped. Re-point `scripts/measureEpaDeviations.ts` to import from the new module. Two copies
of a scale conversion is precisely the drift `REBUILD_SPEC.md`'s failure log warns about, and
`carryover.ts`'s own `populationMeanSd` comment already names it.

If task 1 selected the incumbent `100`, say so in the constant's doc comment along with the
clause that produced that outcome, so a future reader sees a measured decision rather than an
untested default.

**2. Extend `EpaState` with exactly two fields.**
  - `carrySeedMean: number` — the OUTGOING season's alliance-score mean, the units every
    carried component is currently expressed in. **LEAGUE-scoped**: one number.
  - `carryPending: ReadonlySet<string>` — teams carried across the most recent boundary not
    yet materialized. **Serialized PER TEAM, as a flag on that team's row, omitted when
    false.** Do NOT put this set in the league row: D-13 states a league row's bytes must
    never grow with team count, and a few thousand team keys would breach
    `MAX_LEAGUE_ROW_BYTES`. This is the same team-row/league-row split the 10 -> 11 bump made
    for Sigma beliefs versus their population statistics — follow it.
  - `initState` sets `carryPending` empty and `carrySeedMean` to `Number.NaN`.

**3. Implement the mechanism in `epa.ts`, mirroring `carryoverFixArm` exactly.**
  - `carrySeason`: capture `state.allianceScoreStats.mean` into `carrySeedMean` BEFORE
    calling `epaCarryover`/`reseedFromPrior` — after the reseed the value is still recoverable
    but capturing it first is what makes `cleanSeasonMean`'s unwind correct by construction.
    Set `carryPending` to the keys of the freshly built `teamComponents`. Cold start returns
    unchanged.
  - Ratio: `carryRescaleRatio(cleanSeasonMean(state.allianceScoreStats, state.carrySeedMean,
    EPA_SCORE_SD_SEED_COUNT, EPA_CARRY_RESCALE_MIN_OBS), state.carrySeedMean)`. Early-out with
    no work at all when `carryPending` is empty — the common path must stay free.
  - `predict`: materialize TRANSIENTLY (temp components, predict, discard).
  - `update`: materialize PERMANENTLY, then run the ordinary update. Every rating-eligible
    team in the match leaves `carryPending` whether or not the ratio was readable.
  - Use the SAME rating-eligible team filter `epa.ts` already applies (`ratingEligibleTeams`
    over each alliance's teams and surrogates) — not a re-derived one.
  - **`carryover.ts`'s `epaCarryover` is NOT changed in this task.** Its round trip stays
    self-consistent in outgoing units by construction; the correction is composed on top,
    lazily, in `epa.ts`. Only its HEADER changes, and that happens in task 3.

**4. Bump `epa.version` `"7.0.0+baseline"` -> `"8.0.0+baseline"`** with a bump note in the
existing ledger style directly above the module literal (follow the 6.0.0 and 7.0.0 notes'
shape): what changed, why ratings change, and that this is a correctness fix against D-16's
recorded reference rather than a tuning win. Major, because every carried rating changes.

**5. Bump `STATE_SNAPSHOT_SHAPE_VERSION` 11 -> 12** with a ledger entry in the same style,
stating the LOAD-BEARING reason explicitly: `apps/worker/src/stateStore.ts`'s
`readScopedState` filters rows by `algorithm_id` ONLY and never by version, so bumping
`epa.version` alone does NOT make a stale seeded row unreachable. A shape-11 EPA row
deserializes with `carryPending` absent and `carrySeedMean` undefined, which would silently
disable the rescale on live traffic while the offline publisher applied it — a live/offline
divergence that looks healthy. The shape check is the only thing that turns that into a loud
`LeagueRowShapeVersionError` naming the re-seed as the fix. Update `serializeEpaState` and
`deserializeEpaState` accordingly, omitting the per-team flag when false so publish-time byte
budgets are unchanged.

**6. Update every test that pins the OLD behaviour.** Start from
`packages/core/algorithms/carryover.test.ts`, `packages/core/algorithms/epa.test.ts`,
`packages/harness/replay.season.test.ts`, `packages/harness/stateSnapshot.test.ts`,
`packages/harness/manifests.test.ts`, `packages/gbr/features.test.ts`. For each failure,
decide whether the assertion encoded the DEFECT (update it, and say in a comment that it
did) or an unrelated invariant that the change broke (that is a real regression — stop and
reconcile, do not edit the assertion to match). Beware the iteration-list trap: a test that
iterates a season/key list will silently skip rather than fail.

**7. Sweep live references to the old version string.** `grep -rn "7\.0\.0+baseline"
--include='*.ts' --include='*.tsx' --include='*.md' --include='*.json' . | grep -v node_modules`
and update or explicitly justify leaving each hit. `apps/web/src/components/methodology/
epaComparisonContent.ts` and `apps/web/src/lib/api/epaComparison.test.ts` are known hits.
**If anything under `apps/web` changes**, run the web tsconfig (`npx tsc --noEmit -p
apps/web/tsconfig.json`) and `npx vitest run apps/web/src/lib/api/epaComparison.test.ts` —
root `tsc --noEmit` does not cover `apps/web`.

No full harness run in this task; the nine-season reproduction gate is task 3's.
  </action>

  <verify>
    <automated>npx vitest run packages/core/algorithms/epa.test.ts packages/core/algorithms/carryover.test.ts packages/core/algorithms/epaCarryScale.test.ts</automated>
    <automated>npx vitest run packages/harness/ scripts/measureEpaDeviations.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -n 'version: "8.0.0+baseline"' packages/core/algorithms/epa.ts &amp;&amp; grep -n 'STATE_SNAPSHOT_SHAPE_VERSION = 12' packages/harness/stateSnapshot.ts</automated>
    <automated>C=packages/core/algorithms/carryover.ts; grep -q 'export const EPA_MEAN_REVERSION = 0.4;' $C &amp;&amp; grep -q 'export const EPA_CARRY_LAST_YEAR_WEIGHT = 0.7;' $C &amp;&amp; grep -q 'export const EPA_CARRY_PRIOR_YEAR_WEIGHT = 0.3;' $C &amp;&amp; grep -q 'export const EPA_NORM_MEAN = 1500;' $C &amp;&amp; grep -q 'export const EPA_NORM_SD = 250;' $C &amp;&amp; grep -q 'export const EPA_INIT_PENALTY = 0.2;' $C &amp;&amp; echo "FROZEN D-04 CONSTANTS UNCHANGED"</automated>
  </verify>

  <done>
`epa@8.0.0+baseline` ships the lazy per-team rescale at the selected threshold. The contract
test asserting incoming-season units passes and demonstrably fails against the old behaviour.
`STATE_SNAPSHOT_SHAPE_VERSION` is 12 with a load-bearing bump note, and a shape-11 EPA row
throws. The full `packages/` suite and root typecheck are green, read from output. Frozen
D-04 carry constants are untouched. Nothing under `packages/bpr/` was edited.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reproduction gate, harness closeout, docs, and the staleness ledger</name>
  <files>scripts/measureEpaDeviations.ts, packages/core/algorithms/carryover.ts, docs/models/epa-divergences.md, reports/epa-deviation-ablation.json, data/diagnostics/epa-deviation-ablation.json, .planning/quick/260911-3kc-land-the-epa-carryover-scale-anchor-fix-/260911-3kc-SUMMARY.md</files>

  <precondition>The pre-landing artifact copy saved in the scratchpad by task 1 is still present. Without it the reproduction gate cannot run and the landed code's equality with the measured arm is unverifiable.</precondition>

  <action>
**1. Close the harness out. The carryover arms must not survive the landing.** After task 2
the shipped `epa` module already contains the fix, so `carryoverFixArm()` — which WRAPS the
shipped module — would now apply the rescale a second time. Leaving it in place would
silently measure a double-apply and report it as the fix. Therefore:
  - Remove all three carryover arms from `ARM_IDS` and from the per-season `algorithms`
    array. Keep both `epa-winprob-*` arms and everything else untouched.
  - Delete `carryoverFixArm`, `CarryoverFixState`, `ratioForState` and `CARRYOVER_FIX_ARM_ID`
    along with the now-dead threshold arm ids. The pure helpers now live in
    `packages/core/algorithms/epaCarryScale.ts` and stay there.
  - Flip the `carryover-scale-anchor` entry in `deviationRegister()` to `status: "closed"`,
    `armIds: []`, and move its measurement into `priorMeasurement` — source
    (`quick task 260910-x09` + this task), the pooled accuracy and Brier deltas with their
    intervals, the 2019 figures, the 2018 Brier regression, the onset cost, and task 1's
    threshold selection. `elim-weight` and `variance-free` are already closed this way;
    follow that shape. The measurement is preserved for stage 4's UI, not deleted with the
    arm.
  - Update the harness file header's "Arm `epa-carryover-fix` — THE DEFECT" section: the
    defect is CLOSED as of `epa@8.0.0+baseline`, the baseline arm now IS the fix, and the
    section states which quick task landed it.

**2. Run the nine-season reproduction gate.** Background
`pnpm measure:epa-deviations 2>&1 | tee reports/epa-deviation-ablation-run.log` and poll.
Then compare, with a throwaway script in the scratchpad: for every season and for pooled,
the NEW artifact's `epa` baseline `winnerAccuracy` and `brier` must equal the PRE-LANDING
artifact's winning-arm rows for the same season and scope, **to 6 decimal places**. The
script must `throw` on any non-finite value on either side before comparing — a `NaN` on
both sides would otherwise compare as a match after formatting. Print one PASS/FAIL line per
season and a final verdict.

**A mismatch is not a tolerance to widen.** It means the landed code is not the code that was
measured. Reconcile it against `carryoverFixArm`'s mechanism as it stood at task 1's commit
(`git show <task-1-sha>:scripts/measureEpaDeviations.ts`) and fix the landed code, not the
threshold of the comparison.

Copy the regenerated artifact to `data/diagnostics/epa-deviation-ablation.json` as identical
bytes.

**3. Correct `packages/core/algorithms/carryover.ts`'s header** (lines 15-31, the
"Discretionary modelling choice" paragraph). Its closing sentence currently defers the scale
anchor to a future phase as an open question; that question is now answered, so that sentence
goes. Replace the paragraph with the resolution AND its constraint:
  - `epaCarryover` still converts both directions with the OUTGOING season's mean/sd, and
    that is deliberate: it keeps the round trip self-consistent. The correction to the
    INCOMING season's units is applied lazily, per team, on first sight, in `epa.ts` — name
    the functions so a reader can follow the path.
  - State the walk-forward constraint plainly: **Statbotics runs offline and simply KNOWS
    the season's scale. We do not.** We estimate the incoming season's scale from its own
    folded alliance scores once at least `EPA_CARRY_RESCALE_MIN_OBS` of them exist, and a
    team first seen before that threshold forfeits its rescale. This is the closest
    walk-forward-legal approximation and is **never identical** to Statbotics — say that in
    those terms rather than implying parity.
  - Keep the D-04 paragraph and the module-ownership paragraph as they are.

**4. Add `docs/models/epa-divergences.md` §8: "Season-boundary carry scale anchor — NARROWED
as of `epa@8.0.0+baseline`".** Match the existing sections' voice. It must contain:
  - What the divergence was, and that it contradicted D-16's own verbatim reading of
    `init.py` — a port defect against our own recorded reference, not a design choice.
  - What changed, and that this **NARROWS an existing divergence rather than adding one**
    (state it in those words — §7's "NARROWED (not closed)" heading is the precedent).
  - The measured effect: pooled +0.01059 ACC [+0.00846, +0.01285] and -0.00521 Brier
    [-0.00638, -0.00412]; the concentration at 2019 (+0.0794 ACC) as the mechanism test
    passing; and that the pooled figure is 2019 averaged across nine seasons, not a broad
    gain.
  - **Where it is worse, stated plainly**: 2018 Brier +0.00052 [+0.00030, +0.00075], the
    only definitive regression anywhere; and the 2019 onset Brier cost, with whatever task 1
    measured about whether a higher threshold reduced it.
  - The residual divergence that remains: the deferral forfeit, with the measured
    `deferredRescales` / `neverSeen` census.
  - If task 1 adopted a challenger threshold, record that it was selected on the same nine
    seasons it was measured on, with no held-out confirmation — so the marginal gain over
    the plain fix is NOT independently validated, while the plain fix itself is.

**5. Write the SUMMARY with an explicit staleness ledger.** EPA is the FROZEN BASELINE
SigmaScout's "beats EPA" claim is measured against (D-04). Changing EPA means every prior
"beats EPA" number was measured against the OLD EPA. **Do not re-run any of those
comparisons in this task** — record them. A dedicated `## What this change made stale`
section, naming at minimum:
  - `docs/models/epa-vs-statbotics.md` — its measured figures are against `epa@7.0.0`.
  - `scripts/epaVsStatbotics.ts --check` — gated against
    `data/baselines/epa-vs-statbotics-2026-09.json`, whose bands were established under
    7.0.0. It will now fail, and that failure is correct, not a regression. State what
    re-establishes it: a fresh run without `--check` to rebuild the baseline, reviewed
    before it is committed.
  - `data/baselines/epa-vs-statbotics-2026-09.json` itself.
  - Any published "beats EPA" comparison in `apps/web` methodology content that quotes an
    EPA-relative number.
  - **A republish is now OWED** — published EPA ratings change — and it MUST NOT run in this
    task. Say which surfaces are stale until it does.
  Also carry the ordinary SUMMARY content: the threshold decision and the clause that
  produced it, the reproduction-gate result, the shape-12 bump and its re-seed implication,
  and the fact that BPR was never replayed so its sealed holdout is unspent.
  </action>

  <verify>
    <automated>npx vitest run scripts/measureEpaDeviations.test.ts packages/core/algorithms/epaCarryScale.test.ts</automated>
    <automated>node -e "const a=require('./data/diagnostics/epa-deviation-ablation.json');const d=a.deviations.find(x=>x.id==='carryover-scale-anchor');if(d.status!=='closed')throw new Error('deviation not closed: '+d.status);if(d.armIds.length!==0)throw new Error('carryover arms still present');if(!d.priorMeasurement)throw new Error('measurement not preserved');console.log('CLOSED, measurement preserved')"</automated>
    <automated>grep -c 'placeholder Phase 3 may replace' packages/core/algorithms/carryover.ts | grep -qx 0 &amp;&amp; grep -q 'NARROWED as of `epa@8.0.0+baseline`' docs/models/epa-divergences.md &amp;&amp; echo "HEADER CORRECTED AND SECTION 8 PRESENT"</automated>
    <automated>grep -q 'What this change made stale' .planning/quick/260911-3kc-land-the-epa-carryover-scale-anchor-fix-/260911-3kc-SUMMARY.md &amp;&amp; grep -q 'epa-vs-statbotics' .planning/quick/260911-3kc-land-the-epa-carryover-scale-anchor-fix-/260911-3kc-SUMMARY.md &amp;&amp; echo "STALENESS LEDGER PRESENT"</automated>
  </verify>

  <done>
The nine-season run reproduces the pre-landing winning arm's per-season and pooled numbers
to 6 dp with the shipped `epa` baseline alone, printed season by season. The carryover arms
are gone and the deviation is `closed` with its measurement preserved in `priorMeasurement`.
`carryover.ts`'s header describes the resolution and its walk-forward constraint instead of a
placeholder. `epa-divergences.md` §8 records the narrowing including where the fix is worse.
The SUMMARY names every stale artifact and the owed republish, and no republish ran.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> transcript | `.env` holds the live TBA key and R2 token pair; rendering it into any output stream exposes it even though git never sees it |
| corpus -> harness | `data/corpus.sqlite` is third-party-derived match data read by a replay that must not mutate it |
| offline pipeline -> live Worker | a landed state-shape change reaches production through seeded D1 rows that `readScopedState` selects by `algorithm_id` alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3kc-01 | Information disclosure | `.env` secrets in an agent transcript | high | mitigate | This task needs no secrets. `measureEpaDeviations.ts` must never be run with `--env-file`, and `.env` is never `Read`, `cat`-ed, or interpolated. Prohibition 4 in the objective. |
| T-3kc-02 | Tampering | live EPA state vs offline publisher | high | mitigate | `STATE_SNAPSHOT_SHAPE_VERSION` 11 -> 12 in task 2, so a stale seeded row throws `LeagueRowShapeVersionError` instead of silently disabling the rescale on live traffic |
| T-3kc-03 | Repudiation | "beats EPA" claims silently measured against a retired baseline | high | mitigate | Task 3's mandatory `## What this change made stale` ledger names every affected artifact and the owed republish |
| T-3kc-04 | Tampering | the measured arm and the landed code diverging | high | mitigate | Task 3's 6-dp nine-season reproduction gate against the pre-landing artifact; a mismatch is reconciled, never tolerated |
| T-3kc-05 | Tampering | another session's uncommitted files swept into a commit | medium | mitigate | Explicit pathspec staging only, never `git add -A`; `git show --stat` after each commit |
| T-3kc-SC | Tampering | npm/pip/cargo installs | low | accept | This task installs no packages. No install task exists, so the legitimacy gate does not apply. |
</threat_model>

<verification>
1. `npx vitest run packages/ scripts/` — read the output, not the exit code. The repo-root
   run covers ~167 files; a run from `apps/web` covers only 77 and would hide a red suite.
2. `npx tsc --noEmit`, plus `npx tsc --noEmit -p apps/web/tsconfig.json` if anything under
   `apps/web` was touched.
3. The reproduction gate's per-season PASS lines from task 3.
4. `git diff --stat` at each commit shows only this task's files.
</verification>

<success_criteria>
- A carried rating is expressed in the incoming season's point units, pinned by a test that
  fails against the old behaviour.
- The shipped `epa@8.0.0+baseline` reproduces the measured arm to 6 decimal places across
  nine seasons.
- The deferral threshold came from a three-value set and a rule both written down before any
  threshold number existed, with the plain fix winning ties.
- `carryover.ts` and `epa-divergences.md` describe the resolution and its walk-forward
  limits, with the regressions stated rather than glossed.
- The SUMMARY names every stale artifact and the owed republish.
- BPR untouched, sealed holdout unspent, no republish run.
</success_criteria>

<output>
Create `.planning/quick/260911-3kc-land-the-epa-carryover-scale-anchor-fix-/260911-3kc-SUMMARY.md` when done.
</output>
