---
phase: quick-260904-5px
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/algorithms/epa.ts
  - packages/core/algorithms/epa.test.ts
  - packages/core/algorithms/breakdown/groups.ts
  - scripts/epaVsStatbotics.ts
  - data/baselines/epa-vs-statbotics-2026-09.json
  - apps/web/src/components/ribbon/AlgorithmSelect.tsx
  - apps/web/src/components/ribbon/AlgorithmSelect.test.tsx
  - docs/models/epa-divergences.md
  - docs/models/epa-vs-statbotics.md
  - .planning/todos/pending/republish-epa-3-0-no-foul-totals.md
autonomous: true
requirements: [ALGO-02, SC-2]

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "EPA's published `total` metric is the sum of every learned component EXCEPT `foulsCommitted` — the same no-foul quantity Statbotics publishes as `epa.total_points` (D-01). `foulsCommitted` is still learned, still carried in state, and still published as its own per-team metric entry; only its membership in `total` changes."
    - "Every elimination-match observation blends into a team's component means at Statbotics' `ELIM_WEIGHT = 1/3` outer weight, and an elimination match does NOT increment that team's match counter — so `epaPercentFunc`'s decaying learning-rate schedule advances on qualification matches only (D-05). Both halves of `docs/models/epa-divergences.md` §1 are closed together; adopting one half alone would be a third behavior that is neither this project's old one nor Statbotics'."
    - "`predict()`'s own code is untouched, but the prediction STREAM moves and is MEANT to: ratings now evolve differently, so every EPA Brier and winner-accuracy figure changes. The movement is recorded as measured before/after figures in `docs/models/epa-vs-statbotics.md`, never asserted away and never hidden behind a rewritten band (D-05)."
    - "The arithmetic-identity proof survives, staged: with the fouls change alone in place (Task 1, before any elim work), `npx tsx scripts/epaVsStatbotics.ts --check` PASSES while `data/baselines/epa-vs-statbotics-2026-09.json` is byte-identical to what was committed — proof that moving the fouls exclusion from the compare script into the metric is an arithmetic identity and that nothing double-subtracts (D-02 verification signal)."
    - "After the elim discount lands (Task 2), the baseline is RE-MEASURED rather than carried forward: bands recentred on the newly measured values using the SAME published half-widths (slope/Pearson ±0.05, mean absolute difference ±1.0, both SDs ±max(1.5, 10%)), `algorithmVersion` set to `epa@3.0.0+baseline`, and `rationale` recording both the Task 1 identity proof and the elim-weight adoption that made re-measurement necessary."
    - "`epa@2.0.0+baseline` is retired; the module ships exactly ONE new version, `3.0.0+baseline` (D-02), standing for both changes together — nothing is published, fingerprinted or measured-into-a-committed-record between Task 1 and Task 2, so no version string ends up naming two different computations."
    - "`docs/models/epa-divergences.md` §1 is RETIRED with a dated closure note (the divergence is closed as of `epa@3.0.0+baseline`) and §2 is narrowed, both keeping their historical text intact rather than being rewritten."
    - "The algorithm dropdown reads exactly `EPA Statbotics 3.0` whenever the served EPA artifacts are EPA 3.x (D-03), truthfully names the older version while pre-3.0 artifacts are still what R2 serves, and the owed republish plus its artifacts-before-manifest ordering hazard are recorded as a pending todo rather than run from this task."
  artifacts:
    - packages/core/algorithms/epa.ts
    - packages/core/algorithms/epa.test.ts
    - scripts/epaVsStatbotics.ts
    - data/baselines/epa-vs-statbotics-2026-09.json
    - apps/web/src/components/ribbon/AlgorithmSelect.tsx
    - docs/models/epa-divergences.md
    - docs/models/epa-vs-statbotics.md
    - .planning/todos/pending/republish-epa-3-0-no-foul-totals.md
  key_links:
    - "`epa.teamMetrics()` -> `scripts/epaVsStatbotics.ts`'s `ourTeamValuesFromState` — the ONE place the fouls subtraction used to live; the metric absorbing it and the script keeping it would silently double-subtract and move every measured statistic. Task 1's untouched-baseline PASS is what proves this link is clean, and it must be established BEFORE the elim change makes the same statistics move for a different reason."
    - "`result.compLevel` -> `update()` -> `applyComponentUpdate`'s EWMA weight AND its counter increment -> `epaPercentFunc(matchCount)`. Statbotics couples these two halves; they are one decision, not two. Discounting elim observations while still advancing the schedule on them (or the reverse) produces a model that matches neither system."
    - "`epa.version` -> `packages/harness/artifact.ts`'s `{codeVersion}+{paramSetName}` shape check -> published R2 object keys `v1/**/epa@{version}.json` -> the algorithms manifest the browser reads. Code, artifacts and manifest must move in that order."
    - "The re-measured band CENTRES in `data/baselines/epa-vs-statbotics-2026-09.json` -> the slope/Pearson range quoted in `docs/models/epa-divergences.md`'s intro paragraph AND in `docs/models/epa-vs-statbotics.md`'s verdict. Three places quote one measured range; a re-measurement that updates only the JSON leaves two documents asserting numbers the shipped model no longer produces."
    - "`ALGORITHM_DISPLAY_LABELS`/`useAlgorithmOptions` (`AlgorithmSelect.tsx`) -> every model-name render in the app via `algorithmDisplayLabel` — the base label stays `EPA` so table headers stay short; only the dropdown option carries the new full name."
---

<objective>
Make EPA track the real Statbotics more closely on two fronts at once: move the fouls exclusion out
of the comparison script and into EPA's own published metric so `total` means what Statbotics'
`epa.total_points` means (D-01), and adopt Statbotics' elimination-match discount so elim
observations blend at `ELIM_WEIGHT = 1/3` and never advance the learning-rate schedule (D-05).
Retire `epa@2.0.0+baseline` as EPA 3.0 and name it that in the UI (D-02, D-03).

**D-05 (this quick task's fifth decision, locked by the developer 2026-09-04)** — adopt BOTH halves
of the elimination divergence recorded in `docs/models/epa-divergences.md` §1: the `1/3` outer EWMA
weight on every elimination-match observation, and the match counter not incrementing on elims.
Where this plan says D-05 it always means that decision. Phase 02's own D-05 (the breakdown-less
`fallbackObserved` path, cited throughout `epa.ts`) is a DIFFERENT decision, is unrelated to this
work, and is unchanged — in code comments, cite this one as "D-05 (quick task 260904-5px)" so the
two never blur.

Purpose: the FRC-community yardstick is what Statbotics publishes. Yesterday's SC-2 measurement
(`docs/models/epa-vs-statbotics.md`) had to state a "comparability boundary" in prose — a reader
comparing our site's EPA number against Statbotics' site would compare two different quantities —
and §1's elim divergence was the single largest remaining reason our slope rests below 1.0. Both
are closed here.

Output: a no-foul `total` on EPA, Statbotics' elim discount in `update()`, a `3.0.0+baseline`
version, a compare path that reads `total` directly, an unchanged baseline that proves the fouls
half is an arithmetic identity followed by a re-measured baseline that records what the elim half
moved, a recorded before/after Brier and accuracy table, a dropdown reading `EPA Statbotics 3.0`,
and a pending todo for the republish this task deliberately does not run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@packages/core/algorithms/epa.ts
@scripts/epaVsStatbotics.ts
@apps/web/src/components/ribbon/AlgorithmSelect.tsx
@docs/models/epa-divergences.md
@docs/models/epa-vs-statbotics.md
</context>

<scope_fence>
## Do NOT touch

- **`predict()`, `fallbackObserved()`, `foulsCommittedCarryForward()` and `carrySeason()` in
  `epa.ts`.** D-04's cross-attribution (each alliance's own offensive total plus the OPPONENT's
  `foulsCommitted` mean) stays exactly as it is — fouls are real points on a scoreboard, and
  Statbotics folds its own foul rate into match predictions too. `carrySeason()`'s carryover input
  stays the fouls-INCLUSIVE per-team sum. `update()`/`applyComponentUpdate()` DO change in this
  plan, but ONLY to thread the elimination flag into the EWMA weight and the counter increment
  (Task 2) — the attribution arithmetic, the DQ/demo exclusions, the D-05 fallback path and the
  finite-value gates are all untouched.
- **The expanding-window alliance-score SD fold.** `update()` keeps folding BOTH alliance scores
  into `allianceScoreStats` for elimination matches exactly as it does today. Statbotics' elim
  discount is about the rating EWMA and the learning-rate schedule, not about which matches inform
  the win-probability scale, and §1 makes no claim about the SD. Changing it would be an
  undocumented fourth divergence invented mid-task.
- **`packages/core/algorithms/sigma1/**` and `packages/core/algorithms/opr.ts`.** D-01 and D-05 are
  scoped to EPA. VPR's `total` keeps every component including `foulsCommitted` and VPR keeps its
  own elimination policy; OPR keeps its quals-only fit. Those asymmetries are deliberate (EPA
  exists to match Statbotics, the others do not) and get documented, not "fixed".
- **Offseason / replay-population logic anywhere** (`buildSeasonStream`, `includeOffseason`,
  `packages/corpus/**`, `packages/ingest/**`). A concurrent session is editing exactly that ground
  in quick task 260904-586. If a change here appears to need an edit there, stop and report
  instead. Reading through those paths at measurement time is fine; editing them is not.
- **`data/baselines/sc3-rolling-origin-2026-09.json`, `sc3-offseason-inclusive-2026-08.json`, and
  the two `opr-season-pooled-*` files, plus their assertions in
  `packages/harness/baselineFingerprint.test.ts`.** Those are digest-bearing measurement records of
  runs that happened; that file's own doc comments state the do-not-rewrite rule outright ("a
  version string edited to match today's code attaches a real digest to code that never produced
  it"). Their `epa` entry stays at `2.0.0+baseline` forever. Verified while planning: that test
  file only ever reads committed JSON — it never replays EPA — so nothing there goes red when the
  prediction stream moves. Leave it entirely alone.
- **Any publish, manifest, deploy or R2 command** — `pnpm publish:seasons`, `publish:artifacts`,
  `manifest:algorithms`, `worker:deploy`, `cleanup:*`. All of them need credentials and all of them
  collide with the concurrent session. Task 3 files a todo for the republish instead.
- **`.env`, by any means.** Every command in this plan is credential-free by construction; none of
  them takes `--env-file` (the 2022-2026 season replay is a corpus-only run — see
  `docs/models/epa-vs-statbotics.md`'s own note on that command). Never `Read`, `cat`, `echo` or
  interpolate that file (CLAUDE.md, "Secrets handling").
</scope_fence>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: EPA's published total excludes fouls, the compare path stops double-subtracting, and the UNTOUCHED baseline proves it is an arithmetic identity</name>
  <files>packages/core/algorithms/epa.ts, packages/core/algorithms/epa.test.ts, packages/core/algorithms/breakdown/groups.ts, scripts/epaVsStatbotics.ts</files>
  <precondition>`data/corpus.sqlite` exists locally, and either `reports/epa-vs-statbotics/statbotics-team-years-cache.json` is present or `api.statbotics.io` is reachable — `--check` replays five seasons and needs Statbotics' team-year rows. Both are credential-free; if neither the cache nor the endpoint is available, halt and report rather than proceeding, because this task's whole verification signal is that `--check` passes against untouched bands.</precondition>
  <read_first>
    - `packages/core/algorithms/epa.ts` — the file header's deliberate-divergence list, `teamMetrics()` (the one computation this task changes), and the stacked version-bump comment block above `version:` whose format the new entry must match.
    - `packages/core/algorithms/epa.test.ts` around the `epa — contract shape` describe block — the existing `teamMetrics reports one entry per learned component plus TOTAL_METRIC_KEY, with no spread` case, and the fixture shape (`EpaState` literal, `emptyExpandingStats`, `emptyPriorSeasonRatings`) every test in that file builds by hand.
    - `packages/core/algorithms/breakdown/groups.ts` — `UNGROUPED_COMPONENTS` and its doc comment.
    - `scripts/epaVsStatbotics.ts` — the file header's "Our comparable value, per team" paragraph, `ourTeamValuesFromState` (the subtraction site), `runCheck`, and `parseCliOptions` (so the cheaper subset arm below is invoked correctly).
  </read_first>
  <behavior>
    - Given a team whose components are `{ autoLeave: 10, autoAmpNote: 5, foulsCommitted: 7 }`, `teamMetrics()` returns `total` of `15` — the sum of the offensive components alone.
    - The same call still returns a `foulsCommitted` entry of `{ value: 7 }`; the component is published, just not summed.
    - A team with no `foulsCommitted` entry at all is unaffected: `total` is still the plain component sum.
    - `carrySeason()` still carries a fouls-INCLUSIVE per-team total into `epaCarryover` — a season boundary crossed from a state with a nonzero `foulsCommitted` produces a different carried total than the published `total` for that team. This is pinned by a test on purpose so a future reader does not "align" it and silently move every prediction.
    - `ourTeamValuesFromState` returns, per team, exactly the value it returned before this change — because the quantity it used to compute (`total` minus `foulsCommitted`) is now what `total` already is. Demo-key exclusion in that function is unchanged.
    - `--check` reproduces all 25 gated statistics inside their committed bands with every band value, `createdAt` and `sourceReport` untouched.
  </behavior>
  <action>
    Per D-01, change `teamMetrics()` in `packages/core/algorithms/epa.ts` so the accumulator it
    assigns to `TOTAL_METRIC_KEY` skips `FOULS_COMMITTED_COMPONENT` while the per-component
    `perTeam[name] = { value }` assignment still runs for every component including that one —
    mirror the shape `predict()` already uses for its own offensive totals, so both places exclude
    the component the same way. `FOULS_COMMITTED_COMPONENT` is already imported in this file.

    Rewrite that function's doc comment to state the new contract and, in one sentence each, why:
    the published `total` is the no-foul quantity Statbotics publishes as `epa.total_points`; the
    component itself is still published because it is informative and Statbotics publishes foul
    figures separately too; and the fouls a team commits are points it cost the OPPONENT, so they
    were never that team's own scoring output in the first place.

    Add a paragraph to the file header's deliberate-divergence list recording that this NARROWS the
    D-04 divergence rather than reversing it: match prediction keeps the cross-attribution
    (`predict()` is untouched — an alliance's predicted score is still its own offensive total plus
    the opponent's `foulsCommitted` mean), and `carrySeason()`'s carryover input is untouched
    (still the fouls-inclusive per-team sum). State plainly that the published metric and the
    carryover input are now two different quantities, and that this is deliberate.

    Bump `version` to `3.0.0+baseline` per D-02, retiring `2.0.0+baseline`. Append a new bump
    comment above it in the same stacked style the existing `1.0.0 -> 1.1.0` and `1.1.0 -> 2.0.0`
    entries use: MAJOR because `teamMetrics()`'s observable output changed for every team that has
    ever committed a foul, naming the same D-13 invariant those entries name (no version string may
    stand for two structurally different algorithms). Say explicitly that a SECOND change lands
    under this same version string in the next commit — the elimination discount of D-05 (quick
    task 260904-5px) — that this is honest rather than a violation of the invariant because nothing
    is published, fingerprinted, or written into a committed measurement record between the two,
    and that `3.0.0+baseline` therefore names the combined result. Task 2 extends this comment; do
    not leave it claiming the fouls change is all there is.

    In `packages/core/algorithms/breakdown/groups.ts`, correct `UNGROUPED_COMPONENTS`'s doc
    comment, which currently asserts both listed components still contribute to `total`. That is
    now algorithm-dependent: say so, name EPA as the one whose `total` omits
    `FOULS_COMMITTED_COMPONENT` (and why), and confirm VPR's `total` still spans every component.
    Do not change the constant itself — grouping is unrelated to summing.

    In `packages/core/algorithms/epa.test.ts`, extend the existing `teamMetrics reports one entry
    per learned component...` case to cover the first four behaviors above: add a `foulsCommitted`
    value to its fixture, assert the total is the offensive sum, assert the `foulsCommitted` entry
    is still returned with its own value, and keep the existing no-spread assertion. Add one
    further case pinning `carrySeason()`'s fouls-inclusive carryover input — build a two-season
    boundary from a state with a nonzero `foulsCommitted` and assert the carried result reflects
    the larger, fouls-inclusive total rather than the published one. Name that test for what it
    protects, so a future reader sees it is deliberate.

    Then, per D-02's verification signal, change `ourTeamValuesFromState` in
    `scripts/epaVsStatbotics.ts` to push `total` as the comparable value, removing the second
    subtraction that would otherwise apply the exclusion twice now that the metric applies it. Drop
    the now-unused `FOULS_COMMITTED_COMPONENT` local read and its import if nothing else in the
    file uses it — leave `TOTAL_METRIC_KEY` and the demo-key filtering exactly as they are. Rewrite
    that function's doc comment and the file header's "Our comparable value" paragraph: the two
    sides are comparable because EPA's own published `total` is now the no-foul figure, cite the
    same live evidence the header already carries (`frc254`/2024's Statbotics `total_points` 51.71
    reconciling against its auto + teleop + endgame parts), and record that the exclusion moved
    from this script into `epa.teamMetrics()`. Keep the header's statement that this script reads
    the corpus read-only and touches no credential.

    Do NOT edit `data/baselines/epa-vs-statbotics-2026-09.json` in this task — not one field, not
    even `algorithmVersion`. Leaving it byte-identical while `--check` passes IS this task's
    verification signal, and Task 2 owns the single edit that file receives. Run the fast subset arm
    first (`--seasons 2022-2023 --check`, cheap because a season's replayed state depends only on
    the seasons before it and the cold start is positional at the first requested season), then the
    full gate. If any statistic falls outside its band, that is a real double-subtraction or
    regression — stop and report it; never widen a band to make this task green.
  </action>
  <verify>
    <automated>cd "c:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run packages/core/algorithms/epa.test.ts && npx vitest run packages/core/algorithms/breakdown && npx vitest run packages/harness/epaStatboticsCompare.test.ts && grep -n 'version: "3.0.0+baseline"' packages/core/algorithms/epa.ts && npx tsx scripts/epaVsStatbotics.ts --seasons 2022-2023 --check && npx tsx scripts/epaVsStatbotics.ts --check && git diff --exit-code data/baselines/epa-vs-statbotics-2026-09.json</automated>
  </verify>
  <done>
    `epa.test.ts` passes with the extended contract case and the new carryover-pin case; the
    breakdown suite still passes; `epa.ts` ships `version: "3.0.0+baseline"`;
    `npx tsx scripts/epaVsStatbotics.ts --check` prints its PASSED line while
    `git diff --exit-code data/baselines/epa-vs-statbotics-2026-09.json` reports no change at all —
    together the proof that moving the fouls exclusion into the metric is an arithmetic identity on
    the compared quantity. `predict`, `update`, `applyComponentUpdate`, `fallbackObserved` and
    `carrySeason` are unchanged apart from comments at this point.
  </done>
  <reversibility rating="reversible">Both the metric change and the version string are one-line edits with no data migration; artifacts are versioned by key, so nothing already published is overwritten or invalidated.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: adopt Statbotics' elimination discount, then re-measure both baselines and record what moved</name>
  <files>packages/core/algorithms/epa.ts, packages/core/algorithms/epa.test.ts, data/baselines/epa-vs-statbotics-2026-09.json, docs/models/epa-vs-statbotics.md</files>
  <precondition>Task 1's `--check` passed against the untouched baseline (the identity proof this task builds on), and `git status --short` shows no uncommitted changes under `packages/harness/`, `packages/corpus/` or `packages/ingest/` from the concurrent session 260904-586 — the before/after figures below are only apples-to-apples if the replay population is the same one the 2.0.0 figures were measured against. If that ground is dirty, halt and report rather than measuring against a moving target.</precondition>
  <read_first>
    - `docs/models/epa-divergences.md` §1 — the authoritative statement of Statbotics' semantics being adopted: `ELIM_WEIGHT = 1/3` applied to the outer EWMA blend of every elimination-match observation, AND the match counter not incrementing for elim matches so `percent_func`'s decaying schedule never advances on them.
    - `packages/core/algorithms/epa.ts` — `twoStageEwma` (its `weight` parameter, currently always passed `1`), `applyComponentUpdate` (the `twoStageEwma(currentMean, attributed, percent, 1)` call and the `nextCounts.set(team, matchCount + 1)` line), `update()` (which currently never reads `result.compLevel`), `epaPercentFunc`, the `EpaState` doc comment's "increments on EVERY match — D-08" claim, and the file header's D-08 bullet plus the paragraph explaining why the slope rests at 0.84.
    - `packages/core/algorithms/opr.ts`'s `update` — the house test for "is this an elimination match" is `result.compLevel !== "qm"`; reuse that exact form rather than inventing a second one.
    - `packages/core/algorithms/epa.test.ts` lines ~240-280, the `epa.update — D-08 elimination divergence` describe — the one test that directly PINS today's full-weight, counter-incrementing behavior, and therefore the one that must be inverted. Also read the `two-stage EWMA reproduces a hand-computed value` case (~line 95, whose title and inline comment both assert full weight is universal) and the comments at ~lines 181-185 in the error-split describe.
    - `docs/models/epa-vs-statbotics.md` sections "Tolerance and the committed baseline", "The comparability boundary", and "SigmaScout's EPA vs. Statbotics' own win-probability model" (the 2022-2026 accuracy/Brier table and the command that produced it).
    - `data/baselines/epa-vs-statbotics-2026-09.json` — the band half-widths implied by each existing entry, which the re-measured bands must reproduce exactly.
  </read_first>
  <behavior>
    - A team with prior mean `autoLeave: 10` observing an alliance value of 40 as its only rating-eligible member, at match count 0 (percent 1/3): a QUALIFICATION match still lands it at exactly 20, and an ELIMINATION match with the identical observation lands it at exactly 40/3 (the same inner blend of 20, then the outer `1/3 * 20 + 2/3 * 10`).
    - That same elimination match leaves `teamMatchCounts` for the team at 0, while the qualification match leaves it at 1.
    - Two consecutive elimination matches therefore both learn at `epaPercentFunc(0)`, since the schedule never advanced.
    - `ef`, `qf`, `sf` and `f` are all treated as eliminations; only `qm` is a qualification match.
    - Both alliance scores are still folded into `allianceScoreStats` for an elimination match — the expanding-window win-probability scale is unchanged by this task.
    - A fully-demo alliance still skips the whole match at any comp level, and a whole-alliance DQ-zero alliance still gets no update — the elim flag does not reach either exclusion.
  </behavior>
  <action>
    Per D-05 (quick task 260904-5px), adopt both halves of Statbotics' elimination handling in
    `packages/core/algorithms/epa.ts`.

    Add an exported constant for the discount alongside the other Statbotics-parity constants,
    named for what it is and documented as Statbotics' own `ELIM_WEIGHT` from
    `backend/src/models/epa/constants.py`, with the value one third. Its doc comment states the
    adoption plainly: this project used to pass a full weight here as a deliberate divergence, the
    developer has decided EPA should track the real Statbotics, and
    `docs/models/epa-divergences.md` §1 is the record of what was adopted and when.

    In `update()`, derive the elimination flag once from `result.compLevel !== "qm"` — the same test
    `opr.ts` already uses — and thread it into both `applyComponentUpdate` calls. In
    `applyComponentUpdate`, take that flag as a parameter and use it in exactly two places: the
    outer EWMA weight passed to `twoStageEwma` becomes the discount constant for an elimination and
    stays one otherwise, and the per-team counter assignment leaves `matchCount` alone for an
    elimination instead of storing `matchCount + 1`. Nothing else in that function moves — the
    pre-update predicted-total pass, the error split, the cold-start fallback and the returned-map
    discipline are all unchanged. Note in a comment at the counter line that `percent` was already
    computed from the un-incremented count, so an elimination match learns at the schedule position
    it arrived at and leaves the schedule exactly where it found it.

    Correct every comment in `epa.ts` that currently asserts the retired behavior, because a stale
    comment here is precisely this repo's named failure mode (documentation describing a model that
    no longer exists). At minimum: the file header's D-08 bullet becomes a record that the
    divergence is CLOSED as of `3.0.0+baseline` and points at `docs/models/epa-divergences.md` §1;
    the header paragraph explaining why the OLS slope rests at 0.84 must stop naming full-weight
    elims and the incrementing counter as reasons, and should instead say which divergences remain
    (no per-season post-processing, a different component decomposition) and that the measured
    slope moved — quote the re-measured figure once the numbers below exist rather than guessing;
    `twoStageEwma`'s doc comment stops claiming `weight` is always one here; `applyComponentUpdate`'s
    doc comment stops claiming full weight and an unconditional increment; and `EpaState`'s doc
    comment stops describing the counter as incrementing on every match. Leave `update()`'s
    demo-alliance comment alone in substance — EPA does still fold every comp level, just at a
    discount now — but adjust it if its wording implies undiscounted folding. Leave `carrySeason`'s
    per-season counter reset comment alone; it is still true.

    Extend the version-bump comment Task 1 wrote rather than adding a second version: state that
    `3.0.0+baseline` also carries D-05's elimination discount, that this is the change which moves
    the prediction stream (the fouls change did not), and that both landed before anything was
    published under the new string.

    In `packages/core/algorithms/epa.test.ts`, invert the `epa.update — D-08 elimination
    divergence` describe. Rename it for what it now protects (Statbotics' elim discount, adopted),
    and replace its assertions with the first three behaviors above: the elim result equals 40/3
    against the qual result of 20 for the same observation, the elim counter stays at 0 while the
    qual counter reaches 1, and two consecutive elims both learn at the match-count-0 rate. Assert
    the hand-computed constants directly, not merely that elim differs from qual — a test that only
    asserts inequality would pass for any wrong weight. Add a case covering the comp-level coverage
    behavior (at least one non-`sf` elimination level treated identically to `sf`, and `qm` alone
    treated as a qualification) and a case pinning that both alliance scores still fold into
    `allianceScoreStats` for an elimination match. Fix the two stale full-weight claims elsewhere in
    the file: the hand-computed-EWMA case's title and its counter comment should say the match is a
    qualification one, and the error-split describe's counter comments likewise. Do not churn the
    event-boundary-invariance test or the demo-team tests — verified while planning that both stay
    correct as written (the former is all-`qm`; the demo tests either skip the match entirely or
    compare two runs at the same comp level).

    Now re-measure, in this order, and do not edit any recorded figure before its measurement
    exists. First run the per-team comparison without `--check` so it writes a fresh report, then
    read `reports/epa-vs-statbotics/epa-vs-statbotics.json` and take, per season, the
    `minMatchesFiltered` arm's `ordinaryLeastSquaresSlope`, `pearson`, `meanAbsoluteDifference`,
    `ourStandardDeviation` and `theirStandardDeviation` — the printed console row is rounded and is
    not sufficient. Rewrite `data/baselines/epa-vs-statbotics-2026-09.json`'s five season blocks
    with bands recentred on those measured values using the SAME half-widths the file already
    encodes: slope and Pearson at plus/minus five hundredths, mean absolute difference at plus/minus
    one point, and each standard deviation at plus/minus the greater of 1.5 and a tenth of the
    measured value. The half-widths do not change; only the centres do. Set `algorithmVersion` to
    `epa@3.0.0+baseline`, refresh `createdAt`/`sourceReportMeasuredAt` to the new measurement, and
    append a paragraph to `rationale` recording: that the previous bands were measured 2026-09-04
    under the retired `epa@2.0.0+baseline`; that moving the fouls exclusion into the metric was
    verified as an arithmetic identity against those untouched bands first (the previous commit);
    and that re-measurement was required only because adopting Statbotics' elimination discount
    genuinely changes how ratings evolve. Keep the existing rationale text and the 2026-in-progress
    caveat intact — append, do not replace. If a band half-width has to grow beyond the formula for
    `--check` to pass, that is a finding to report, not an edit to make.

    Second, re-run the season replay that produced the accuracy/Brier table, with the identical
    flags the document records (`--seasons 2022-2026 --algorithm epa --include-offseason`, a
    corpus-only run needing no credential) and an `--out` directory of its own so it cannot clobber
    an existing report or collide with the concurrent session. Expect this to run for several
    minutes; judge it by its printed per-season lines and its `Wrote ...` path, never by exit code
    alone, and never wrap it in a `timeout`-plus-package-manager combination (that swallows output
    and exits zero regardless). Read `brierScore` and `winnerAccuracy` for each season's combined
    slice from the artifact it writes.

    Then revise `docs/models/epa-vs-statbotics.md`. In "The comparability boundary", adjustment 1 is
    no longer an adjustment the comparison makes — it is a property of the metric as of
    `epa@3.0.0+baseline`; keep the measured per-team tables, the spot-check tables and the verdict,
    add one sentence recording that the per-team tables were measured on the identical quantity and
    the `Ours` columns formerly labelled as a subtraction are now simply `total`, and record that
    those per-team figures HAVE since been re-measured under the elim discount (pointing at the
    refreshed baseline for the current bands). In "Tolerance and the committed baseline", replace
    the claim that re-running the script unchanged reproduces the old figures exactly with the
    current state: the bands were recentred on a re-measurement under 3.0.0, the half-width formula
    is unchanged, and the determinism claim still holds going forward. In the win-probability
    section, keep the existing 2.0.0 accuracy and Brier columns as the labelled BEFORE and add
    columns for the freshly measured 3.0.0 figures, so both sit side by side per season; update the
    `epa@2.0.0+baseline` citation above the table to name both versions and their commands. Then
    write two or three sentences of honest reading: state which direction each season moved, state
    plainly whether the elim adoption improved or degraded EPA's standing against Statbotics'
    win-probability model, and do not frame a degradation as anything other than what it is. The
    adoption is the developer's locked decision and stands regardless of which way the numbers went
    — the obligation here is to record them, not to defend them.

    Finally, record the directional expectation §1 predicts, cheaply: compare each season's
    re-measured OLS slope against the corresponding centre implied by the OLD bands and note
    whether agreement with Statbotics got TIGHTER (slope nearer 1.0). Report this per season in the
    summary. If most seasons moved AWAY from 1.0, flag it prominently as a surprise worth
    investigating rather than burying it — do not revert the change over it, and do not assert a
    direction the measurement does not support.
  </action>
  <verify>
    <automated>cd "c:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run packages/core/algorithms/epa.test.ts && npx vitest run packages/harness/epaStatboticsCompare.test.ts && grep -n 'compLevel' packages/core/algorithms/epa.ts && npx tsx scripts/epaVsStatbotics.ts --check</automated>
  </verify>
  <done>
    An elimination match moves a team's component mean to exactly 40/3 where the same observation in
    a qualification match moves it to 20, and leaves the team's match counter untouched — both
    asserted against hand-computed constants in `epa.test.ts`, which passes.
    `npx tsx scripts/epaVsStatbotics.ts --check` prints its PASSED line against RE-MEASURED bands
    whose half-widths are identical to the retired ones and whose centres are the newly measured
    values, with `algorithmVersion` reading `epa@3.0.0+baseline` and `rationale` recording both the
    identity proof and the re-measurement. `docs/models/epa-vs-statbotics.md` carries the 2022-2026
    accuracy and Brier figures for 2.0.0 and 3.0.0 side by side with an honest reading of which way
    they moved, and the per-season slope direction is recorded in the summary.
  </done>
  <reversibility rating="reversible">One code change threading a comp-level flag through two call sites plus a re-measured tolerance file; revertable in a single commit, and no artifact is published by this plan, so nothing downstream has consumed the new ratings yet.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: name it EPA Statbotics 3.0 in the ribbon, retire the divergence record, file the republish</name>
  <files>apps/web/src/components/ribbon/AlgorithmSelect.tsx, apps/web/src/components/ribbon/AlgorithmSelect.test.tsx, docs/models/epa-divergences.md, .planning/todos/pending/republish-epa-3-0-no-foul-totals.md</files>
  <read_first>
    - `apps/web/src/components/ribbon/AlgorithmSelect.tsx` — `ALGORITHM_DISPLAY_LABELS` and its "SINGLE place the site's model name is written" doc comment, `useAlgorithmOptions` (which appends the manifest version), and `algorithmDisplayLabel` (read by the Teams rank header, Insights notice, Breakdown caption, Alliances notice, Compare tables and the home podium — all of which keep the short `EPA`).
    - `apps/web/src/components/ribbon/AlgorithmSelect.test.tsx` — the existing `renderHook` merge tests and their manifest fixture shape.
    - `docs/models/epa-divergences.md` — §1 in full (the elimination divergence, now retired), §2 (the fouls divergence and the house style for recording a correction against a section without rewriting its history), and the intro paragraph whose quoted slope range came from the now-superseded measurement.
    - `data/baselines/epa-vs-statbotics-2026-09.json` as re-measured by Task 2 — the slope band centres there are the source for any slope range quoted in prose; take them from the file, never from memory.
    - `.planning/todos/pending/update-project-md-sc2-blocked-claim.md` — the house format for a pending todo.
  </read_first>
  <behavior>
    - `useAlgorithmOptions` yields, for a manifest whose `epa` entry is version `3.0.0+baseline`, an option labelled exactly `EPA Statbotics 3.0` — no version suffix appended after it.
    - For a manifest still serving `epa` at `2.0.0+baseline`, that option reads `EPA 2.0.0+baseline`, unchanged from today.
    - With no manifest entry for `epa` (pending, failed or absent), the option reads `EPA`, unchanged from today.
    - `opr` and `vpr` options are unaffected in all three cases.
    - `algorithmDisplayLabel("epa")` still returns `EPA`, so the Teams rank column header still reads `EPA Rank`.
  </behavior>
  <action>
    Per D-03, make the ribbon dropdown's EPA option read exactly `EPA Statbotics 3.0`. Implement it
    in `useAlgorithmOptions` as a version-gated label rather than a hardcoded string: add a
    documented module constant holding the full name alongside `ALGORITHM_DISPLAY_LABELS`, and in
    the per-id map, when the id is `epa` and the resolved manifest entry's version begins with the
    3.x major, use that full name on its own; otherwise keep today's base-label-plus-version branch
    untouched for every id.

    Explain the gate in that constant's doc comment, because it is the non-obvious part: this
    repo's failure log is a documentation-describes-a-deleted-model story, R2 still serves
    `epa@2.0.0+baseline` artifacts until the republish in the todo below happens, and a hardcoded
    3.0 name would spend that window claiming a number the site is not actually serving. The gate
    is self-correcting — it needs no coordination with the republish and no second edit afterwards.
    Note explicitly that the full name embeds its own version, which is why the manifest suffix is
    not appended a second time on that branch.

    Leave `ALGORITHM_DISPLAY_LABELS` itself and `algorithmDisplayLabel` returning the short `EPA`,
    and add a line to the existing "SINGLE place the site's model name is written" comment
    recording the split: the short label is what table headers, captions and the podium render, the
    full name is the ribbon option only, and D-03 constrains the ribbon.

    In `AlgorithmSelect.test.tsx`, add cases covering all five behaviors above using the file's
    existing `renderHook` pattern and manifest fixture shape.

    In `docs/models/epa-divergences.md`, RETIRE §1 rather than deleting it. Keep the section number
    (other documents cite these by number), keep its existing Statbotics / This project / Why /
    Effect paragraphs as the historical record of what was true until now, retitle it so a reader
    sees at a glance that the divergence is closed, and add a dated closure note in the same style
    §2 uses for its corrections: as of `epa@3.0.0+baseline` (2026-09-04, quick task 260904-5px,
    D-05) this project adopted BOTH halves of Statbotics' behavior — the one-third outer EWMA weight
    on elimination observations and the counter that does not advance on them — because the goal is
    to track the real Statbotics; name the code that now implements it; and record what the
    adoption measured, citing the before/after figures Task 2 wrote into
    `docs/models/epa-vs-statbotics.md` rather than restating them from memory. Say explicitly that
    the "Why" paragraph's original reasoning (that the harness would show whether the discount was
    worth adopting) is now answered by measurement rather than assumption.

    Also rewrite §2 so it records the current state: the per-team cross-attributed
    `foulsCommitted` component survives (D-04 is narrowed, not withdrawn) and still drives match
    prediction, while the PUBLISHED per-team `total` is now the no-foul figure matching Statbotics'
    `epa.total_points`. Record the carryover asymmetry from Task 1, record that VPR's `total` is
    unchanged and why, and note this is the third correction recorded against §2 — keeping the
    existing two correction paragraphs intact rather than rewriting history.

    Update this document's intro paragraph, which currently explains the measured slope as the
    resting point of the divergences below: one of those reasons is now retired, so the sentence
    must name the remaining ones and quote the re-measured slope range. Derive that range from the
    slope band centres in the Task 2 baseline file (each centre is the midpoint of its band), not
    from the retired figures.

    Create `.planning/todos/pending/republish-epa-3-0-no-foul-totals.md` in the house format,
    recording: every published EPA artifact and every EPA rank, percentile and alliance total on
    the site is still the fouls-inclusive, full-weight-elim `epa@2.0.0+baseline` figure until a
    republish runs, and now the PREDICTIONS are stale too, not just the ratings — the elim discount
    moved the prediction stream, so published match predictions and any accuracy surface derived
    from them are also pre-3.0; the republish was deliberately deferred out of this task because a
    concurrent session was working the same repo; and the ordering hazard — artifacts must be
    republished BEFORE the algorithms manifest is, since the browser resolves artifact object keys
    from the manifest's version and a manifest that names 3.0.0 while only 2.0.0 objects exist would
    404 every EPA page. Name the commands (`pnpm publish:seasons`, then `pnpm manifest:algorithms`)
    and note both need credentials this task never touched. Note also that a fresh SC-3 baseline
    fingerprint measured under 3.0.0 is now owed, added ALONGSIDE the committed ones and never over
    them.
  </action>
  <verify>
    <automated>cd "c:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run apps/web/src/components/ribbon/AlgorithmSelect.test.tsx && test -f .planning/todos/pending/republish-epa-3-0-no-foul-totals.md</automated>
  </verify>
  <done>
    The ribbon's EPA option renders `EPA Statbotics 3.0` against a 3.x manifest and the older
    version string against a pre-3.0 one, both under test; `algorithmDisplayLabel("epa")` still
    returns `EPA`; §1 of the divergence record is retired with a dated closure note and its
    historical text intact, §2 records the narrowed fouls divergence, and the intro quotes the
    re-measured slope range; the republish todo exists with its ordering hazard and its
    now-stale-predictions note spelled out.
  </done>
  <reversibility rating="reversible">Label logic, docs and a todo file — all revertable in one commit with no published-state consequence.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api.statbotics.io -> `--check` | Third-party JSON crosses into the comparison. Already Zod-validated at the fetch boundary in `packages/harness/statbotics.ts`, which throws rather than yielding a silent partial series. |
| corpus SQLite -> replay | Read-only (`openCorpusReadOnly`); this task adds no write path. |
| repo -> transcript | `.env` holds the live TBA key and the R2 token pair. No command in this plan reads it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5px-01 | Information disclosure | `.env` secrets reaching a subagent transcript (the Phase-4 04-01 failure, CLAUDE.md "Secrets handling") | high | mitigate | Every command in this plan is credential-free and none takes `--env-file` (the 2022-2026 season replay is corpus-only); the scope fence forbids `Read`/`cat`/`echo` of `.env` and forbids all publish/deploy commands. |
| T-5px-02 | Tampering | A band in `data/baselines/epa-vs-statbotics-2026-09.json` widened beyond its own published formula so that `--check` goes green, converting a real regression into a passing gate — a sharper risk now that the file IS rewritten rather than carried forward | high | mitigate | Task 1 proves the fouls half with the file byte-identical (`git diff --exit-code`), so only the elim half can move a number. Task 2's rewrite recentres bands on measured values while keeping every half-width identical to the retired file, and instructs a stop-and-report if any half-width would have to grow. |
| T-5px-03 | Repudiation | A digest-bearing fingerprint's `epa` version edited to match today's code, attaching a real prediction-stream digest to code that never produced it | high | mitigate | Scope fence names all four fingerprint files and their assertions as untouchable; verified while planning that `baselineFingerprint.test.ts` only reads committed JSON and never replays EPA, so it stays green on its own while the prediction stream moves. Task 3's todo asks for a NEW fingerprint measured under 3.0.0, added alongside. |
| T-5px-04 | Spoofing | The UI claiming `EPA Statbotics 3.0` while R2 still serves `epa@2.0.0+baseline` numbers — now doubly wrong, since both the ratings AND the predictions are stale until the republish | medium | mitigate | Task 3's version-gated label reads the manifest and names the version actually being served; the owed republish, its ordering hazard and its now-stale-predictions scope are filed as a pending todo. |
| T-5px-05 | Tampering | Editing offseason/replay-population code concurrently owned by quick task 260904-586, producing a merge conflict or a lost fix | medium | mitigate | Scope fence forbids touching that ground and requires a stop-and-report if a change appears to need it; `files_modified` shares no file with that task's teams-list scope; Task 2's precondition additionally requires that ground to be clean before measuring, so a concurrent edit cannot silently contaminate the before/after figures. |
| T-5px-06 | Repudiation | Measurement laundering — the elim adoption degrading EPA's accuracy while the re-measured baseline is rewritten to match, leaving no record that anything got worse | high | mitigate | Task 2 requires the 2.0.0 accuracy/Brier figures kept as a labelled BEFORE column beside the 3.0.0 measurement, an explicit written statement of which direction each season moved, and a per-season slope-direction report — with an instruction that a degradation is reported as-is and never reframed. The tolerance baseline alone is never the record of what the change did. |
| T-5px-07 | Repudiation | One version string (`3.0.0+baseline`) standing for two structurally different computations, since the fouls change and the elim change land in separate commits under the same version — exactly the D-13 invariant the bump comments cite | medium | mitigate | Nothing is published, fingerprinted, or written into a committed measurement record between Task 1 and Task 2 — Task 1 explicitly leaves the baseline file byte-identical, so the only committed artifact naming 3.0.0 is written by Task 2 under the final code. Task 1's bump comment states up front that a second change lands under the same string, and Task 2 extends it. |
| T-5px-SC | Tampering | Supply chain | low | accept | No package-manager install in this plan; no dependency added or changed. |
</threat_model>

<verification>
Run from the repo root, not from `apps/web` — a run rooted in the web app sees only its own ~77
test files and would miss the core and harness suites this change actually moves:

```
cd "c:/Users/Jacob/Documents/GitHub/SigmaScout"
npx vitest run
npx tsc --noEmit
```

Do not wrap either command in `timeout ... pnpm ...` — that combination swallows output and exits
0 regardless. Judge both by their printed output, never by exit code alone.

Two load-bearing checks, in this order, and the order is the point:

1. Task 1's `npx tsx scripts/epaVsStatbotics.ts --check` printing PASSED while
   `git diff --exit-code data/baselines/epa-vs-statbotics-2026-09.json` reports no change. That the
   same 25 statistics reproduce after the subtraction moved from the script into the metric is the
   proof the fouls change is an arithmetic identity on the compared quantity and that nothing
   double-subtracts. This proof only exists if it is taken BEFORE the elim change.
2. Task 2's same command passing against RE-MEASURED bands. Here the numbers are expected to have
   moved, because ratings now evolve differently; the check is that they land inside bands built by
   the file's own unchanged half-width formula around freshly measured centres, not that they
   reproduce anything.

Expect the prediction stream to move. Any assertion, comment or document sentence claiming EPA's
Brier or winner-accuracy figures are unchanged by this plan is now false and should be corrected
wherever it is found.
</verification>

<success_criteria>
- `epa.teamMetrics()` publishes a `total` excluding `foulsCommitted`, and still publishes
  `foulsCommitted` as its own entry (D-01).
- Elimination-match observations blend at Statbotics' one-third weight and never advance the
  per-team match counter, so `epaPercentFunc`'s schedule is driven by qualification matches alone
  (D-05) — pinned by hand-computed constants in `epa.test.ts`, not by an inequality.
- `predict()`, `fallbackObserved()` and `carrySeason()` are unchanged apart from comments;
  `update()`/`applyComponentUpdate()` change only in the elim weight and the counter increment; the
  expanding-window score SD still folds both alliance scores for elimination matches.
- `epa.version` is `3.0.0+baseline`; `2.0.0+baseline` appears in the file only in the historical
  bump comments (D-02), and the bump comment states that both changes land under the one string.
- `npx tsx scripts/epaVsStatbotics.ts --check` passed twice: once against the byte-identical
  committed baseline after the fouls change alone, and once against re-measured bands after the
  elim change.
- `docs/models/epa-vs-statbotics.md` carries 2022-2026 accuracy and Brier for both 2.0.0 and 3.0.0
  side by side, with an honest written reading of which direction each season moved.
- `docs/models/epa-divergences.md` §1 is retired with a dated closure note and its historical text
  intact; §2 records the narrowed fouls divergence; the intro quotes the re-measured slope range.
- The ribbon dropdown reads exactly `EPA Statbotics 3.0` against a 3.x manifest (D-03), and names
  the older version while pre-3.0 artifacts are what is served.
- Full-repo `npx vitest run` and `npx tsc --noEmit` are clean.
- The republish todo exists, covering stale ratings, stale predictions, the
  artifacts-before-manifest ordering, and the owed 3.0.0 SC-3 fingerprint.
</success_criteria>

<output>
Return the SUMMARY text to the orchestrator for writing to
`.planning/quick/260904-5px-remove-fouls-from-epa-total-for-statboti/260904-5px-SUMMARY.md`
(subagents are blocked from writing SUMMARY.md directly).

Call out in it: the Task 1 identity proof (PASSED against a byte-identical baseline); the
re-measured per-season slope figures and whether agreement with Statbotics got tighter or looser;
the before/after accuracy and Brier table with a plain statement of which way it went; the
label-gate decision and its rationale; and the owed republish with its artifacts-before-manifest
ordering.
</output>
