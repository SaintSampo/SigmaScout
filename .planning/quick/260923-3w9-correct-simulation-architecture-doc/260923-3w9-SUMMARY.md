---
quick_id: 260923-3w9
status: complete
date: 2026-09-23
---

# Quick task 260923-3w9: correct simulation-architecture.md sidecar sizes

## What changed

`docs/simulation-architecture.md` was two generations stale on the pre-schedule rank simulation
sidecar (`v1/presim/{eventKey}/{alg}@{version}.json`): it still quoted 388 to 394 KB per sidecar
and a `schedules` block at 95.4% of the object, both from before quick task 260912-2ur dropped
the priced `schedules` array from the published body.

- **Header:** records a 2026-09-23 re-verification against HEAD (a code read, not a live R2 fetch).
- **Section 3 (pipeline outputs):** now describes the actual wire shape,
  `PublishedPreScheduleArtifactSchema` (`roster`, `baked` with `draws` and `histograms`, and
  `scheduleCount`), with the priced `schedules` block correctly scoped as builder-only, never
  published. Size tables replaced with `docs/publish-budget.md`'s 2026-09-21 measured figures
  (214 sidecars, median 6,855 B, p95 16,005 B, max 23,831 B); the two historical tables are
  collapsed and labelled superseded. Also corrected a factual error: `baked.draws` is 1,000
  schedules x 50 draws = 50,000, which `publish.ts`'s own comment says is deliberately unlike the
  client engine's 1,000-draw resolution.
- **Section 4:** its per-key byte table is labelled historical, dated 2026-09-12, and pointed at
  section 3. The "how it was dark" narrative is preserved.
- **Section 5:** rewritten as a closed question. The simulation stays client-side on both stops:
  not moved into the sidecar (closed 2026-09-19) and not moved into a server-side HTTP Worker
  (closed 2026-09-23, quick task 260923-1tu findings section 3). Options A/B/C kept as a short
  historical record only.
- **Section 6:** file map re-verified with Glob. `scripts/measureRewindGap.ts` no longer exists
  and its row is dropped; `scripts/measureFieldAveragedRanks.ts` is noted as the current offline
  `simulateRanks` consumer. Stale line references corrected.

## Verification

Read `packages/harness/preSchedule.ts`, both presim schemas in `packages/harness/pageArtifacts.ts`,
`packages/harness/publish.ts` (`PRESIM_SCHEDULE_COUNT`, `PRESIM_DRAWS_PER_SCHEDULE`,
`buildPreScheduleSidecarForEvent`) and `docs/publish-budget.md` before writing. Confirmed that
`FieldAveragedPreScheduleArtifactSchema` exists but is not wired into the publish pipeline.

## Commits

- 82adce2f docs(260923-3w9): correct simulation-architecture.md sidecar sizes and close the browser-move question (worktree branch, fast-forwarded into main by the orchestrator)
