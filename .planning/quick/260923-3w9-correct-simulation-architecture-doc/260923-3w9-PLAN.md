---
quick_id: 260923-3w9
slug: correct-simulation-architecture-doc
date: 2026-09-23
type: docs
---

# Quick task 260923-3w9: correct simulation-architecture.md sidecar sizes

## Task

`docs/simulation-architecture.md` described the pre-schedule rank simulation sidecar
(`v1/presim/{eventKey}/{alg}@{version}.json`) with sizes two generations stale. Sections 3, 4 and
5 quoted 388 to 394 KB per sidecar, a `schedules` block that was 95.4% of the object, and "the 20
priced schedules are downloaded and never read by the client." Since quick task 260912-2ur the
priced `schedules` array is no longer published: the body carries `roster`, `baked` and
`scheduleCount`, and `docs/publish-budget.md`'s machine-written budget block (generation
8caca9d2, measured 2026-09-21) records 214 presim sidecars, median 6,855 B, p95 16,005 B, max
23,831 B. Correct the doc against HEAD and close the browser-move question per Jacob's 2026-09-23
decision (quick task 260923-1tu).

## Steps

1. Read `docs/publish-budget.md`'s presim section, `packages/harness/preSchedule.ts`, and
   `packages/harness/pageArtifacts.ts` (`PreScheduleArtifactSchema`,
   `PublishedPreScheduleArtifactSchema`, `preScheduleKey`) to confirm the current builder shape
   (still carries `schedules` in memory) versus the current wire shape (`schedules` dropped,
   `scheduleCount` added) before writing anything.
2. Read `packages/harness/publish.ts` around `PRESIM_SCHEDULE_COUNT` /
   `PRESIM_DRAWS_PER_SCHEDULE` and `buildPreScheduleSidecarForEvent` to confirm the current
   schedule count (1,000, raised from 20) and that the baked draw total (50,000) is now
   deliberately unlike the client engine's 1,000 `SIMULATION_DRAWS`, correcting the doc's old
   "matching resolution" claim.
3. Read `.planning/quick/260923-1tu-workers-paid-rearchitecture-audit-find-c/260923-1tu-FINDINGS.md`
   section 3 to source the closed browser-move decision, and
   `.planning/todos/completed/price-the-preschedule-simulation-in-the-browser.md` to source the
   earlier, separate "browser-side pricing" closure it is not to be confused with.
4. Rewrite the doc's header to record the 2026-09-23 re-verification as a code read, not a live
   R2 fetch.
5. Rewrite section 3's pipeline-outputs schema description and size tables to the current wire
   shape and the 2026-09-21 budget figures, collapsing the two superseded historical size tables
   with a clear label rather than deleting them.
6. Label section 4's per-key byte table historical (it was a live 2026-09-12 measurement, not
   re-fetched today) and point it at section 3 for current sizes; leave its "how it was dark"
   narrative intact.
7. Rewrite section 5 as a closed question: the simulation stays client-side on both stops, not
   moved into the sidecar (closed 2026-09-19) and not moved into a server-side Worker (closed
   2026-09-23), keeping Options A/B/C only as a short historical record.
8. Re-verify section 6's file map against HEAD with Glob; drop the row for
   `scripts/measureRewindGap.ts` (no longer exists) and correct stale line-number references.
9. Pass over all newly written prose to remove hyphen or dash characters used as punctuation,
   keeping compound-word hyphens and quick-task identifiers.
10. Create this PLAN.md and commit both files together.

## Deliverable

`docs/simulation-architecture.md`, corrected and re-verified against HEAD as of 2026-09-23, plus
this PLAN.md.
