---
quick_id: 260904-nt4
phase: quick-260904-nt4
plan: "01"
type: execute
wave: 1
depends_on: []
subsystem: pipeline+web
tags: [seasons, 2019, 2020, gapped-corpus, publish, republish, docs, cloudflare-pages]

files_modified:
  - package.json
  - packages/harness/publish.ts
  - packages/harness/publish.test.ts
  - packages/harness/seasonBoundary.test.ts
  - scripts/deleteOrphanedDemoTeamObjects.ts
  - apps/web/src/lib/seasons.ts
  - apps/web/src/lib/metricKeys.test.ts
  - apps/web/src/lib/api/compare.ts
  - apps/web/src/lib/api/compare.test.ts
  - apps/web/src/lib/districtNames.ts
  - apps/web/src/lib/districtNames.test.ts
  - .planning/PROJECT.md
  - docs/models/sigma1-rp-verification.md
  - docs/publish-budget.md
  - .planning/STATE.md

autonomous: false
requirements: [T1-parser, T1-script, T2-seasons, T2-compare, T2-districts, T2-docs, T3-republish, T3-budget, T3-verify]

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor picks 2019 or 2020 in the year dropdown and gets a real Teams list, a real team page and a real event page — not a 404 or an empty state"
    - "2021 never appears in the year dropdown, in any URL the router accepts, or in any published artifact — it is a permanent exclusion, and a test proves the exclusion tracks the algorithms' own season registry rather than being an arbitrary hole"
    - "The Compare page still shows exactly 2022-2026 — 2019 has no prior seasons and 2020 has only a thin 2019 prior, so neither can ever be headline-eligible (D-4/D-5)"
    - "A team active in 2019 or 2020 offers those years in its constrained team-page year dropdown, because activeYears was computed from a run that covered all seven seasons"
    - "Every district chip on a 2019 or 2020 event reads a proper district name, not a bare uppercased abbreviation — TBA re-keyed Indiana and Texas between 2020 and 2022 and both spellings resolve"
    - "The artifacts exist in R2 BEFORE the site offers the years — the republish runs first, the push that triggers the Pages deploy runs second"
    - "docs/publish-budget.md's machine-readable block records the seven-season run's real measured numbers, with no budgetMaxBytes ceiling raised to make a test pass"
  artifacts:
    - packages/harness/publish.ts
    - apps/web/src/lib/seasons.ts
    - apps/web/src/lib/districtNames.test.ts
    - docs/publish-budget.md
  key_links:
    - "package.json publish:seasons --seasons string -> publish.ts parseSeasonsRange -> seasonsSorted -> seasonBoundaryFor(seasonsSorted, idx) -> gap-2 carry at 2020->2022, positional cold start at index 0 (2019)"
    - "publish.ts seasonsSorted -> activeYearsByTeam -> team/{teamKey}/{year} activeYears -> YearSelect useConstrainedYears"
    - "seasons.ts SEASONS -> searchParams.ts KNOWN_SEASONS (?year= validation) AND YearSelect options AND compare.ts COMPARE_SEASONS (narrowed at COMPARE_FIRST_SEASON)"
    - "seasons.ts SEASONS -> metricKeys.ts metricKeysFor -> componentMapForSeason (throws for an unregistered season, which is what makes the SEASONS/registry agreement testable)"
    - "corpus events.district_key (2019/2020: in, tx) -> published event artifact districtKey -> districtNames.ts districtDisplayName -> EventFilters/EventsList chip"
    - "R2 artifact existence (task 3 republish) MUST precede git push -> .github/workflows/deploy.yml -> Cloudflare Pages sigmascout-web"
---

<objective>
Ship 2019 and 2020 on the website. The model side of the 2019/2020 backfill landed on 2026-09-03
(ingest, four season modules, gap-aware carry, positional cold start, corpus-relative scoring) and
the re-tune/republish ran on 2026-09-04 — but the SITE side was never done. No 2019/2020 artifacts
exist in R2, `apps/web` is hard-bounded at `FIRST_SEASON = 2022`, and `pnpm publish:seasons` cannot
even express the season set, because its `--seasons` parser accepts only a contiguous range and
`2019-2026` would include 2021 (which has no component map and throws).

Three moves, in this order: teach the publish CLI a gapped season list; widen the web season list
to the gapped seven with 2021 permanently excluded; then republish everything and push.

Purpose: 2019 and 2020 are in the corpus and scored, and the site pretends they do not exist.
Output: 2019 and 2020 selectable and rendering on the live site, docs telling the truth about the
corpus range, and a transcribed budget for the seven-season run.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/todos/completed/extend-corpus-2019-2020.md
</context>

<background>
**2021 is a permanent exclusion, not a deferral.** It was the at-home/remote season with no
conventional 3v3 alliance matches, so there is nothing for a match predictor to ingest or score.
Recorded user decision, 2026-09-03. Never write a plan, comment, test or doc line that frames it as
"not yet ingested".

**The corpus is: 2019, 2020, 2022, 2023, 2024, 2025, 2026.** Seven seasons. 2019 is a FULL-SIZE
season (14,929 official quals with breakdown, inside the 2022-2026 range of 12,064-15,191); only
2020 is small (3,820).

**Verified against the working tree and the live corpus during planning (2026-09-04).** Re-verify
line numbers before editing — they drift.

| Fact | Where | Detail |
|---|---|---|
| Publish script is a contiguous range | `package.json:27` | `--seasons 2022-2026 --include-offseason` |
| Parser rejects a gapped set | `packages/harness/publish.ts` `parseSeasonsRange` ~2184 | single year OR `start-end` only; module-private; `runSeasonsMode` calls it at ~2337 |
| Gap-aware carry already exists | `packages/harness/seasonBoundary.ts` | `seasonBoundaryFor(seasonsSorted, idx)`; publish threads it at ~1592; cold start is POSITIONAL (index 0), `COLD_START_SEASON` deleted by 260904-cs1 |
| `seasonsSorted.length < 5` warning | publish.ts ~1582 | harmless for a 7-season run; it will not fire |
| Web season bound | `apps/web/src/lib/seasons.ts:22` | `FIRST_SEASON = 2022`, contiguous `SEASONS` |
| SEASONS assertions | `apps/web/src/lib/metricKeys.test.ts:58-70` | descending, starts at `CURRENT_SEASON`, ends at `FIRST_SEASON`, consecutive pairs differ by exactly 1, every element resolves through `componentMapForSeason` |
| Compare derives from SEASONS | `apps/web/src/lib/api/compare.ts:49` | `COMPARE_SEASONS = [...SEASONS].sort(asc)`; `compare.test.ts:82` pins `[2022..2026]` |
| Worker has no season floor | `apps/worker/src/` | grep for `2022` returns ZERO hits; versions are read from the algorithms manifest at runtime (D-03), never bundled literals — so **no `pnpm worker:deploy` is expected** |
| Deploy is push-triggered | `.github/workflows/deploy.yml` | push to `main` -> GitHub Actions -> Cloudflare Pages `sigmascout-web` |
| 2019/2020 district keys | `SELECT DISTINCT district_key FROM events WHERE year IN (2019,2020)` | `chs, fim, fma, fnc, in, isr, ne, ont, pch, pnw, tx` — **`in` and `tx` are MISSING** from `districtNames.ts` (which carries `fin`/`fit`, the 2022+ spellings) |
| 2019/2020 weeks | corpus query | 2019 raw weeks 0-6; 2020 raw weeks 0-6 plus one raw week 9. `MAX_SEASON_WEEK = 8` already routes week 9 to the "Other" bucket — **no week work needed** |
| Season scale | corpus query | 2019: 3,876 teams / 266 events with matches. 2020: 2,071 teams / 57 events. |
| bonusRp already covers both | `apps/web/src/lib/bonusRp.ts:30-40` | 2019 two bonuses, 2020 one (energized deliberately absent) — **no change needed** |
| eventKey is year-agnostic | `apps/web/src/lib/eventKey.ts` | validates shape only, pins no year range — **no change needed** |

**Compare artifacts for 2019/2020 WILL be published and that is fine.** `publishSeasons` writes one
`v1/compare/{year}.json` per season in the run. Under the provenance-aware eligibility rule
(260903-n2o) 2019 has 0 priors and 2020 has 1, so both slices carry `headlineEligible: false`. The
client simply never fetches them, because `COMPARE_SEASONS` is floored at 2022. Publishing an
unread, honestly-ineligible slice is not a defect — do not add a filter to suppress it.
</background>

<constraints>
- **SECRETS (`.claude/CLAUDE.md`).** `pnpm publish:seasons` runs `tsx --env-file=.env`. NEVER `Read`,
  `cat`, `head`, `tail`, `less` or `echo` `.env`; never interpolate a value from it into a shell
  command, a log line, a test name, an assertion message, a commit message or a SUMMARY. Let the
  tool read the file itself. To confirm a key is set, test presence and LENGTH only. This rule
  exists because it was broken once and a live R2 token had to be rotated.
- **Foreign uncommitted edit in the tree.** `apps/web/src/components/ribbon/Ribbon.tsx` is modified by
  a concurrent session. **Stage by explicit path only. NEVER `git add -A` or `git add .`** — commit
  f0c7af48 already absorbed a foreign session's edits once.
- **Do NOT push until task 3 says to.** The republish must land in R2 before the Pages deploy offers
  the years. Tasks 1 and 2 commit locally and stop.
- **Long-running publish.** Use `run_in_background: true` from the FIRST invocation. The executor
  Bash timeout does not terminate process trees on Windows/Git-Bash — a timed-out foreground call
  leaves a live writer running (real incident, 07-17). Never a retry while one may still be alive.
- **Never `timeout N pnpm <cmd>`** — it swallows output and exits 0 (false green). Use
  `npx vitest run <path>` and judge by the printed output, not the exit code.
- Test scope: `apps/web` suite runs from `apps/web` (~79 files); harness/pipeline tests from the repo
  root (~174 files). Running from the wrong directory hides failures.
- **STATE.md quick-task descriptions must contain NO pipe characters**, escaped or otherwise — one
  raw `|` in any row breaks `quick-tasks-append` for every row.
- Do NOT gold-plate. The sibling season parsers (`cli.ts` ~415, `baselineFingerprint.ts` ~272,
  `eventScopeDiagnostic.ts` ~309, `identifiability.ts` ~500) are OUT OF SCOPE — do not edit them.
</constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Teach the publish CLI a gapped season list</name>
  <files>packages/harness/publish.ts, packages/harness/publish.test.ts, package.json, packages/harness/seasonBoundary.test.ts, scripts/deleteOrphanedDemoTeamObjects.ts</files>
  <precondition>`packages/harness/seasonBoundary.ts` exports `seasonBoundaryFor(seasonsSorted, seasonIdx, ...)` and `publish.ts` already calls it (landed 260903-3bv). If publish.ts still builds `fromSeason: season - 1` inline anywhere, stop and report — the gap fix has been reverted.</precondition>
  <behavior>
    - `parseSeasonsRange("2026")` -> `[2026]` (unchanged)
    - `parseSeasonsRange("2022-2026")` -> `[2022,2023,2024,2025,2026]` (unchanged)
    - `parseSeasonsRange("2019,2020,2022-2026")` -> `[2019,2020,2022,2023,2024,2025,2026]` — the new form
    - Terms may be given out of order and may repeat; the result is ascending and de-duplicated
    - A malformed term still throws, and the message names BOTH the range form and the list form
    - A term whose end precedes its start still throws
    - The `--seasons` argument in `package.json`'s `publish:seasons` script parses to exactly the seven-season corpus, with 2021 absent
    - Over that parsed list, `seasonBoundaryFor` yields `toSeason - fromSeason === 2` at the 2020->2022 boundary and `1` at every other, and index 0 (2019) is the cold start
  </behavior>
  <action>
Extend `parseSeasonsRange` in `packages/harness/publish.ts` (~2184) to accept a comma-separated list
whose terms are each a single year or a `start-end` range. Split on `,`, trim, drop empties, parse
each term with the two existing regexes, collect into a `Set<number>`, return ascending. Keep both
existing single/range behaviours byte-identical — a `2022-2026` spec must still yield exactly what it
yields today. Reject an empty spec.

**EXPORT the function**, following the exported-for-test precedent `resolvePublishAlgorithms` (~2157)
already sets in this same file.

**Rewrite its doc comment.** It currently claims this is "the same small parser `cli.ts`'s
module-private `parseSeasonsRange` implements". That claim becomes FALSE with the list form, and a
false comment here is how someone later runs `pnpm harness --seasons 2019,2020,2022-2026` and gets a
confusing error. State plainly: the list form is publish-only; the sibling parsers in `cli.ts`,
`baselineFingerprint.ts`, `eventScopeDiagnostic.ts` and `identifiability.ts` accept only the single
and range forms, and were deliberately left alone. Name why the list form is needed at all — the
corpus is gapped because 2021 has no component map, so `2019-2026` throws at
`componentMapForSeason(2021)`.

Update `package.json:27`: `--seasons 2022-2026` -> `--seasons 2019,2020,2022-2026`. Leave
`--include-offseason` and everything else on that line untouched.

Add tests to `packages/harness/publish.test.ts` covering every `<behavior>` bullet. Two of them carry
the real weight and must not be skipped:

1. **The script/parser drift tripwire.** Read `package.json` from disk, pull the `publish:seasons`
   script string, extract its `--seasons` argument, feed it through the exported parser, and assert
   the result equals `[2019,2020,2022,2023,2024,2025,2026]`. This is what stops the shipped command
   and the parser drifting apart — the exact failure class that let `COLD_START_SEASON = 2022` go
   stale.
2. **The gapped-boundary proof.** Import `seasonBoundaryFor` from
   `packages/harness/seasonBoundary.js` and assert, over the parsed seven-season list, that the
   boundary entering 2022 spans two years while every other spans one, and that index 0 is the cold
   start. The gapped list is only useful if it actually threads a gap-2 carry through publish's own
   boundary helper (260903-3bv, 260904-cs1); asserting the array shape alone would not prove that.

Two stale comments this change creates, fix both:
- `packages/harness/seasonBoundary.test.ts:5` describes its range as "the exact range
  `publish:seasons` and `harness --seasons 2022-2026` replay". The equivalence claim over 2022-2026 is
  still valid and the test must not change; reword the description to name 2022-2026 as the
  historical contiguous range rather than as what `publish:seasons` currently covers.
- `scripts/deleteOrphanedDemoTeamObjects.ts:63` calls its array "the five published seasons this
  project's `pnpm publish:seasons` covers (`--seasons 2022-2026`)". Do NOT extend the array — 2019/2020
  were never published before the demo-team exclusion existed, so there are no orphans there. Correct
  the comment to say it targets the PRE-EXCLUSION generation's own range specifically.

Do not run the publish in this task. Do not touch any web file.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run packages/harness/publish.test.ts packages/harness/seasonBoundary.test.ts</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && node -e "const s=require('./package.json').scripts['publish:seasons']; if(!/--seasons 2019,2020,2022-2026\b/.test(s)) { console.error('FAIL: publish:seasons does not carry the gapped set:', s); process.exit(1); } if(/\b2021\b/.test(s)) { console.error('FAIL: 2021 present'); process.exit(1); } console.log('OK', s);"</automated>
  </verify>
  <done>
`pnpm publish:seasons` names the seven-season corpus; the parser accepts it and yields it ascending
with 2021 absent; the gapped list produces a two-year boundary entering 2022 and a positional cold
start at 2019; publish.test.ts and seasonBoundary.test.ts are green from the repo root; `tsc
--noEmit` is clean. Committed by explicit path (`git commit -- <paths>` or `git add <path>` per file,
never `-A`) as `feat(quick-260904-nt4): publish accepts a gapped season list`. NOT pushed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the web season list to the gapped seven, and correct the docs that claim 2022-2026</name>
  <files>apps/web/src/lib/seasons.ts, apps/web/src/lib/metricKeys.test.ts, apps/web/src/lib/api/compare.ts, apps/web/src/lib/api/compare.test.ts, apps/web/src/lib/districtNames.ts, apps/web/src/lib/districtNames.test.ts, .planning/PROJECT.md, docs/models/sigma1-rp-verification.md</files>
  <behavior>
    - `SEASONS` is `[2026,2025,2024,2023,2022,2020,2019]` — descending, 2021 absent
    - `SEASONS[0] === CURRENT_SEASON` (2026) and `SEASONS.at(-1) === FIRST_SEASON` (2019) still hold
    - Every consecutive descending pair differs by 1, OR by more with every skipped year present in `EXCLUDED_SEASONS`
    - Every member of `SEASONS` resolves through `componentMapForSeason` without throwing
    - Every member of `EXCLUDED_SEASONS` THROWS through `componentMapForSeason` — the exclusion tracks the algorithms' own registry, it is not an arbitrary hole
    - `COMPARE_SEASONS` is still exactly `[2022,2023,2024,2025,2026]` ascending, even though `SEASONS` now contains 2019 and 2020
    - `districtDisplayName("in") === districtDisplayName("fin")` and `districtDisplayName("tx") === districtDisplayName("fit")`
    - Every district key the 2019/2020 corpus carries resolves to a proper name, never its uppercased fallback
    - An unknown key still falls back to its uppercased abbreviation
  </behavior>
  <action>
**`apps/web/src/lib/seasons.ts`.** Set `FIRST_SEASON = 2019`. Add an exported
`EXCLUDED_SEASONS: readonly number[] = [2021]`. Build `SEASONS` as the descending
`CURRENT_SEASON..FIRST_SEASON` range with `EXCLUDED_SEASONS` filtered out, so the two exported bounds
stay meaningful and the list stays derived rather than hand-maintained.

Rewrite the file header comment. Both halves of the current one are now stale: it says
`FIRST_SEASON` "happens to be 2022, the same year the algorithms' replay range currently starts", and
it references a `seasons.test.ts` that does not exist (the assertions live in `metricKeys.test.ts`).
The replacement should carry: the seven-season gapped set; that 2021's absence is a PERMANENT
exclusion because the at-home/remote season had no conventional 3v3 alliance matches, so there is
nothing to ingest or score (user decision 2026-09-03, recorded in
`.planning/todos/completed/extend-corpus-2019-2020.md`); that the file still never imports from
`packages/core/` — the registry agreement is enforced by the test, not by an import; and the correct
test filename.

**`apps/web/src/lib/metricKeys.test.ts`** (`describe("SEASONS")`, ~58-70). Keep the first two
assertions as-is. Replace the consecutive-pair contiguity assertion with the gap-aware rule in
`<behavior>`. Add explicit membership assertions (2019 and 2020 present, 2021 absent). Keep the
`componentMapForSeason` resolution loop. **Add the inverse loop**: every `EXCLUDED_SEASONS` member
must THROW. That inverse assertion is the load-bearing one — it means the day someone registers a
2021 component map, this test goes red and forces the exclusion to be revisited deliberately rather
than leaving a silent hole in the dropdown.

**`apps/web/src/lib/api/compare.ts`** (~43-49). Introduce `COMPARE_FIRST_SEASON = 2022` and derive
`COMPARE_SEASONS` as `[...SEASONS].filter((s) => s >= COMPARE_FIRST_SEASON).sort(asc)`. Do NOT let
2019/2020 into it. Rewrite the surrounding comment, which currently says "the five seasons ... derived
from `SEASONS`" and claims exactly one source of "which seasons exist". The new comment must say:
`SEASONS` is still that one source and this is a documented narrowing of it, never a second
hand-typed year array; the floor exists because a Compare row must be able to become
headline-eligible, which under the provenance-aware rule (`packages/harness/score.ts`, quick task
260903-n2o) requires at least two prior corpus seasons AND absence from the algorithm's own
selected-on set — 2019 has no priors and 2020 has only a thin 2019 prior, so both are selection-only
seasons forever (D-4/D-5); and that `v1/compare/2019.json` and `v1/compare/2020.json` ARE published
and simply never fetched, so this floor is what stops the page rendering a permanently-ineligible
empty row.

**`apps/web/src/lib/api/compare.test.ts`** (~80-85). Keep the `[2022..2026]` equality assertion
exactly as it stands — it is the pin. Rename the `it()` so it no longer claims the value is the whole
of `SEASONS` sorted, and add one assertion that `SEASONS` contains 2019 and 2020 while
`COMPARE_SEASONS` contains neither. That pairing is what makes the narrowing visible as a deliberate
decision instead of looking like a bug.

**`apps/web/src/lib/districtNames.ts`.** Add `in: "FIRST IN"` and `tx: "FIRST TX"`, keeping the
existing `fin`/`fit` entries — TBA re-keyed the Indiana and Texas districts between 2020 and 2022, so
both spellings are live in the corpus and both must resolve to the same reader-facing name. Rewrite
the "The key set is closed for 2022-2026" sentence: the set is closed for the seven-season corpus
(2019, 2020, 2022-2026), and name the re-keying explicitly so the apparent duplication reads as
intentional. Verified from the corpus on 2026-09-04: 2019/2020 events carry exactly
`chs, fim, fma, fnc, in, isr, ne, ont, pch, pnw, tx` — every key but `in` and `tx` was already
present.

**New `apps/web/src/lib/districtNames.test.ts`.** There is no test for this map today, which is why
the 2019/2020 keys went unnoticed. Assert: the old and new spellings agree for IN and TX; every key
in the observed 2019/2020 list resolves to something other than `key.toUpperCase()` (hardcode the
list with a comment naming the query and the date — a browser-side test cannot open the corpus); and
the unknown-key fallback still uppercases.

**Docs.** Apply one test to every candidate line: does it state the CURRENT corpus/site range, or is
it a DATED snapshot of what a specific phase or run measured? Update the former; leave the latter
alone. Confirmed candidates in `.planning/PROJECT.md`:
- the Out of Scope row "Seasons before 2022 - modern-era focus ... may extend later" (~line 35).
  Replace it with the 2021 permanent exclusion, framed as an exclusion and not a deferral, and note
  the corpus/site now cover 2019, 2020, 2022-2026.
- the Context bullet "Backtests over 2022-2026 are the proving ground" (~line 47).
- the Key Decisions row "v1 covers seasons 2022-2026" (~line 63) and its Outcome cell — extended
  2026-09-04, all seven seasons carry reconciled component maps and RP rule modules and are scored
  end to end (quick tasks 260903-4fs and this one).
- the Rank-point requirement's "for all covered seasons (2022-2026)" (~line 20) — "all covered
  seasons" is a live claim, and it is now seven.
LEAVE the dated per-phase validation records (the Phase 2 / Phase 3.2 / Phase 6 lines that quote a
specific run's figures) and every `docs/models/*` and `docs/publish-budget.md` line that names a
past run's command or date.

One honest addendum in `docs/models/sigma1-rp-verification.md`: its scope line quotes SC-4's
"verified against the official 2022-2026 game manuals". 2019's and 2020's RP thresholds were DERIVED
FROM CORPUS DATA, not read from a manual. Add a single line saying so and pointing at
`docs/data/tba-rp-thresholds-2019-2020.md` and quick task 260903-4fs. Do not restate or re-scope SC-4
itself.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout/apps/web" && npx vitest run src/lib/metricKeys.test.ts src/lib/api/compare.test.ts src/lib/districtNames.test.ts src/lib/searchParams.test.ts src/components/ribbon/YearSelect.test.tsx</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout/apps/web" && npx vitest run</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout/apps/web" && npx tsc --noEmit -p tsconfig.json</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && grep -rn "2022" .planning/PROJECT.md | grep -v "^.*Phase" | grep -c "Seasons before 2022" | grep -q '^0$' && echo "OK: out-of-scope pre-2022 row is gone"</automated>
  </verify>
  <done>
`SEASONS` is the gapped seven; 2021 is absent and a test proves the exclusion matches the algorithms'
registry; `COMPARE_SEASONS` is still exactly 2022-2026 with `compare.test.ts:82`'s pin untouched;
`in`/`tx` resolve to proper district names under a new test; the full `apps/web` suite and its `tsc`
are green; PROJECT.md and `sigma1-rp-verification.md` state the seven-season range without rewriting
any dated snapshot. Committed by explicit path as
`feat(quick-260904-nt4): web season list covers 2019 and 2020 with the 2021 gap`. **NOT pushed** —
pushing here would offer years whose artifacts do not exist yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tasks 1 and 2 are committed locally and unpushed. The next task runs a ~25 minute full republish of ~75,000 R2 objects and then pushes, which triggers the live Cloudflare Pages deploy.</what-built>
  <how-to-verify>
Confirm before the republish starts:
1. `git log --oneline -3` shows the two new commits, and `git status` still shows only the foreign
   `apps/web/src/components/ribbon/Ribbon.tsx` edit as uncommitted (it must NOT have been swept into
   either commit — check `git show --stat HEAD` and `git show --stat HEAD~1`).
2. No other session is mid-publish. The republish overwrites every artifact; two concurrent writers
   produce a mixed-generation site.
3. You are ready for the live site to change: after the push, the year dropdown gains 2019 and 2020
   on every page.
  </how-to-verify>
  <resume-signal>Type "approved" to start the republish, or name what to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full seven-season republish, budget transcription, deploy and rendered verification</name>
  <files>docs/publish-budget.md, .planning/STATE.md</files>
  <precondition>`git status --porcelain` shows no uncommitted changes other than `apps/web/src/components/ribbon/Ribbon.tsx`, and tasks 1 and 2 are committed but unpushed. `.env` exists and carries the R2 credential pair (confirm by PRESENCE AND LENGTH ONLY — `v=$(grep -E "^KEY=" .env | cut -d= -f2-); [ -n "$v" ] && echo "OK (len=${#v})"` — never print a value).</precondition>
  <action>
**Order is load-bearing: republish FIRST, push SECOND.** The artifacts must exist in R2 before the
Pages deploy offers 2019/2020, or a visitor picking 2019 gets a 404. This is the reverse of quick
task 260904-2i9's ordering, and for the opposite reason — nothing already-deployed depends on the new
artifacts, so the extra artifacts are harmless while they sit unread, whereas the reverse order
breaks the year dropdown.

**Pre-flight.** Record a baseline `generation` from one existing `teams/{year}` key read off the
public origin, so any post-run read that still returns the baseline is detectable as stale. Confirm
zero concurrent `publish.ts` processes with an untruncated, command-line-filtered `Get-CimInstance`
query, excluding the query's own self-match — the procedure `docs/publish-budget.md` already
documents from prior runs.

**Run.** `pnpm publish:seasons` with `run_in_background: true` **on the first invocation**, never a
foreground call and never a retry. Projected: roughly 75,000 page objects (56,774 today plus about
18,800 for the two new seasons — 3 algorithms across 3,876 2019 teams and 2,071 2020 teams, 266 and
57 events, plus the per-year teams/events/compare artifacts), about 2.9 GB, about 25 minutes.
`teams` and `events` object counts go 15 -> 21 each, `compare` 5 -> 7.

Monitor by the process's own log output and by re-reading a live artifact `generation`. A quiet log
and a head-truncated process list are NOT evidence the run died — check for real progress before
concluding anything. Do not start a second run to "make sure".

**SECRETS.** The script is `tsx --env-file=.env`. Never read, print, echo or interpolate that file or
any value in it, anywhere — not in a command, not in a log, not in the summary, not in the commit
message.

**After the run.**
1. `pnpm verify:subset` — expect the same 35/35 the prior runs produced, and exactly ONE distinct
   `generation` across every sampled key (the concurrent-writer detector). It must equal the run's own
   summary line.
2. Read one 2019 key and one 2020 key straight off the public origin (a `teams/{year}` and a
   `team/{teamKey}/{year}` for a team that competed that season) and confirm they exist and carry the
   run's generation.
3. Confirm no algorithm version moved and `apps/worker/src` still carries no hardcoded season floor
   (`grep -rn "2022" apps/worker/src` -> zero hits). If both hold, **no `pnpm worker:deploy` is
   required** — say so explicitly in the summary rather than leaving it ambiguous.

**Transcribe the budget.** The publish prints a summary that is NOT written to disk; it must be
copied into `docs/publish-budget.md` by hand or `payloadBudget.test.ts` fails. Add a new "Latest run"
paragraph in the established voice (date, exact command, generation, object count, total bytes, wall
clock, concurrency evidence, and what changed — the first run to cover 2019 and 2020) and update the
```json budget``` block at the bottom with the run's real `measuredAt`, `run` string, and per-page
`count`/`medianBytes`/`p95Bytes`/`maxBytes`/`largestKey`.

**Do NOT raise any `budgetMaxBytes`.** Current headroom: `team` max 376,373 against a 400,000 ceiling,
`teams` 1,487,189 against 3,500,000. 2019/2020 artifacts are smaller per row (fewer components), so
the ceilings should hold — but if one is exceeded, record the real number, leave the ceiling where it
is, leave `payloadBudget.test.ts` red, and surface it as an open finding for a developer decision.
That is the 260904-586 precedent; silently raising a ceiling to make a test pass is not.

Then `npx vitest run packages/harness/payloadBudget.test.ts` from the repo root and report the result
honestly, pass or fail.

**Commit** `docs/publish-budget.md` by explicit path as
`docs(quick-260904-nt4): record the seven-season republish budget`.

**Then push** all three commits to `main`. This triggers `.github/workflows/deploy.yml` and the
Cloudflare Pages deploy of `sigmascout-web`.

**Rendered verification** (after the deploy lands — or immediately against a local build using the
`VITE_ARTIFACT_ORIGIN` override pattern from quick task 260904-7id if the deploy lags):
- `/teams?year=2019` renders a populated table; `/teams?year=2020` likewise
- one 2019 team page and one 2019 event page render (pick an event present in the corpus)
- one 2020 event page renders, and its week chip is sane (the single raw-week-9 event belongs in
  "Other", not "Week 10")
- a district-event chip on a 2019 event reads a proper name, not `IN` or `TX`
- a team active in 2019 shows 2019 in its team-page year dropdown
- the Compare page still lists exactly 2022, 2023, 2024, 2025, 2026 — no 2019 or 2020 row
- 2021 appears nowhere

Finally append the quick-task row to `.planning/STATE.md` (**no pipe characters anywhere in the
description**) and commit it by explicit path.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run packages/harness/payloadBudget.test.ts</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && pnpm verify:subset</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && grep -c "2019" docs/publish-budget.md | awk '{ if ($1 < 1) { print "FAIL: no 2019 reference in the budget doc"; exit 1 } else print "OK" }'</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && grep -rn "2022" apps/worker/src | wc -l | awk '{ if ($1 != 0) { print "FAIL: worker gained a season literal"; exit 1 } else print "OK: no worker season floor" }'</automated>
    <human-check>Live site: /teams?year=2019 and /teams?year=2020 both render populated tables; a 2019 team page, a 2019 district event page (district chip reads a proper name, not IN or TX) and a 2020 event page all render; a team active in 2019 offers 2019 in its year dropdown; the Compare page shows exactly five rows 2022-2026; 2021 appears nowhere.</human-check>
  </verify>
  <done>
~75,000 objects published under a single generation covering all seven seasons; `verify:subset` green
with one distinct generation; the budget transcribed with real measured numbers and no ceiling raised
to force a pass; `payloadBudget.test.ts` result reported honestly; the worker confirmed not to need a
redeploy; all commits pushed and the Pages deploy landed; 2019 and 2020 render on the live site and
2021 does not appear; STATE.md updated with a pipe-free row.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine -> R2 (write) | `publish:seasons` holds live R2 credentials from `.env` and overwrites every published artifact |
| local machine -> transcript | `.env` contents can escape into a subagent transcript on disk without ever reaching git |
| git push -> Cloudflare Pages | a push to `main` deploys the live site automatically, with no manual gate |
| R2 (read) -> browser | the client fetches whatever artifact keys `SEASONS` lets it construct |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nt4-01 | Information Disclosure | `.env` during the publish step | critical | mitigate | Task 3 forbids Read/cat/echo/interpolation of `.env` outright; presence checks are length-only. This exact exposure happened once in Phase 4 and cost a token rotation |
| T-nt4-02 | Tampering | concurrent `publish.ts` writers | high | mitigate | Pre-flight untruncated `Get-CimInstance` check; `run_in_background: true` on the first invocation so no timed-out foreground call leaves an orphan writer; post-run single-generation assertion via `verify:subset` |
| T-nt4-03 | Denial of Service | live site during deploy | high | mitigate | Strict ordering — republish before push. Deploying first would offer 2019/2020 in the dropdown against artifacts that 404 |
| T-nt4-04 | Tampering | foreign uncommitted `Ribbon.tsx` edit | medium | mitigate | Every commit stages by explicit path; `git add -A` forbidden; the checkpoint verifies `git show --stat` on both commits before the republish |
| T-nt4-05 | Repudiation | budget ceiling raised to force a green test | medium | mitigate | Task 3 forbids moving any `budgetMaxBytes`; an overage is recorded and left red for a developer decision, per the 260904-586 precedent |
| T-nt4-06 | Spoofing | 2019/2020 shown as headline-eligible on Compare | medium | mitigate | `COMPARE_FIRST_SEASON` floor plus a test asserting `SEASONS` contains what `COMPARE_SEASONS` excludes; neither season can be selected into a headline claim it has no prior seasons to support |
| T-nt4-SC | Tampering | npm/pip/cargo installs | high | accept | This task installs nothing — no `package.json` dependency is added or changed; only a script string moves. No legitimacy gate applies |
</threat_model>

<verification>
- `npx vitest run` from the repo root: full suite green (~174 files) after task 1, or every failure
  explained
- `npx vitest run` from `apps/web`: full web suite green (~79 files) after task 2
- `npx tsc --noEmit` from the repo root and `npx tsc --noEmit -p tsconfig.json` from `apps/web`: both clean
- `pnpm verify:subset`: same pass count as the prior run, one distinct generation
- `npx vitest run packages/harness/payloadBudget.test.ts`: result reported honestly whether green or red
- Live site renders 2019 and 2020; 2021 appears nowhere; Compare shows exactly 2022-2026
</verification>

<success_criteria>
1. `pnpm publish:seasons` expresses and publishes 2019, 2020, 2022-2026 — 2021 never enters the set
2. The 2020->2022 boundary carries a two-year gap and 2019 is the positional cold start, proven by test
3. The site's year dropdown offers all seven seasons and never 2021
4. Compare is unchanged at exactly 2022-2026, and a test makes that narrowing deliberate and visible
5. 2019/2020 district chips read proper names under a new test
6. R2 carries the seven-season artifacts BEFORE the deploy that offers them
7. `docs/publish-budget.md` records the run's real numbers with no ceiling raised to force a pass
8. `.env` contents never appear in any transcript, log, command, test name, doc or commit message
9. The foreign `Ribbon.tsx` edit is untouched in every commit
</success_criteria>

<output>
Write `.planning/quick/260904-nt4-ship-2019-and-2020-on-the-website-gapped/260904-nt4-SUMMARY.md`
when done. Record honestly: the real object count and wall clock, any payload budget overage (with
the ceiling left where it stands), the worker-redeploy determination and its evidence, and anything
that rendered wrong on first look.
</output>
</content>
</invoke>
