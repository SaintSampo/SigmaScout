---
quick_id: 260903-tk6
phase: quick-260903-tk6
slug: close-the-four-surviving-review-findings
date: 2026-09-03
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: true
depends_on: []
source: .planning/quick/260903-tk6-close-the-four-surviving-review-findings/260903-tk6-CONTEXT.md
requirements: [F-1, F-2, F-3, F-4]
files_modified:
  - packages/harness/searchWinner.ts
  - packages/harness/selectionProvenance.ts
  - packages/harness/selectionProvenance.test.ts
  - packages/harness/promotedOverrides.test.ts
  - packages/harness/cli.ts
  - packages/harness/publish.ts
  - packages/harness/publish.test.ts
  - docs/models/sigma1-sensitivity-screen.md

estimate:
  tokens: 75000
  raw_tokens: 150000
  tasks: 5
  confidence: high

must_haves:
  truths:
    - "F-1: the `{corpusSeasons, selectedOnSeasons}` pair exists in exactly ONE place in the repo. Neither `cli.ts` nor `publish.ts` passes an eligibility argument to `aggregateScores` at the two flag-bearing call sites any more — there is no argument left at those sites to get wrong."
    - "F-1: that one place is exported and directly tested. A test reddens when its corpus-season source is narrowed to the seasons the run happens to be scoring, AND reddens when its selected-on source is replaced by an all-empty map. Both reverts were APPLIED, observed RED in printed output, and restored."
    - "F-2: `vpr-adapt`'s provenance and `vpr-adapt`'s running parameters are ONE decision, not two agreeing condition lists. The provenance answer is `[]` exactly when the module resolution returns undefined, because both read the same resolver."
    - "F-2: on this checkout — where `reports/tune-joint-on.json` is the stale Aug-16 artifact whose params `Sigma1ParamsSchema` cannot read — `selectedOnSeasonsFor(['vpr-adapt'])` returns `[]`, matching the untuned DEFAULTS module that actually scores. It returned `[2022, 2023, 2024]` before this task."
    - "F-3: a published `compare/{year}.json` artifact's `vpr` eligibility is asserted against the provenance-derived matrix — a season inside `vpr`'s selected-on set is ineligible, a season outside it (with enough priors) is eligible. That test was observed RED under an all-empty selected-on map and restored."
    - "F-4: `docs/models/sigma1-sensitivity-screen.md` asserts no present-tense behavior of `HOLDOUT_SEASONS`, `seasonSplit`, or `seasonLabel`. What was true when the screen ran is stated in the past tense, and the current mechanism is named."
    - "The eligibility matrix does NOT move: `vpr` selected-on `[2022, 2023, 2024]`, `epa`/`opr` `[]`; `vpr` eligible on {2025, 2026}, `epa`/`opr` on {2022-2026}. `vpr-adapt` is the ONLY algorithm whose selected-on set changes, and it changes to match what runs."
    - "`apps/web/src/routes/__fixtures__/compare-*.json` are byte-identical to their committed state. Nothing is tuned, promoted, published, or replayed."
  artifacts:
    - "packages/harness/searchWinner.ts — `resolveOnSearchWinner(path)` and `ON_SEARCH_ARTIFACT_PATH`: the single on-search-winner decision both `cli.ts` and `selectionProvenance.ts` read"
    - "packages/harness/selectionProvenance.ts — `aggregateScoresForRun(db, predictions, algorithmIds)`, the single derivation of the eligibility pair, plus a `vprAdaptSelectedOnSeasons` that delegates rather than mirrors"
    - "packages/harness/selectionProvenance.test.ts — the corpus-backed test of the pair, and the `vpr-adapt` case that has never existed"
    - "packages/harness/publish.test.ts — the first repo-wide assertion of `vpr`'s `headlineEligible` in a published compare artifact"
    - "docs/models/sigma1-sensitivity-screen.md — the holdout-blindness claim rewritten as dated history"
  key_links:
    - "`reports/tune-joint-on.json` -> `resolveOnSearchWinner` -> BOTH `loadSearchWinnerVpr` (what runs) AND `vprAdaptSelectedOnSeasons` (what the flag claims). One resolver is what makes disagreement structurally impossible; two agreeing condition lists is the fallback this plan does NOT take."
    - "`selectCorpusSeasons(db)` + `selectedOnSeasonsFor(ids)` -> `aggregateScoresForRun` -> `ScoreSlice.headlineEligible` -> `report.ts` badges (via `cli.ts`) and `compare/{year}.json` (via `publish.ts`). Two call sites, one derivation."
    - "`selectionProvenance.ts` must not import `cli.ts`: `publish.ts:61` imports `applyPromotedOverrides` from `./cli.js` and `cli.ts:88` imports `selectedOnSeasonsFor` from `./selectionProvenance.js`. Sharing therefore moves DOWN into a leaf module, never sideways."
---

<objective>
Close the four surviving findings from the adversarial review of `260903-n2o`. All four share
one root cause: the eligibility mechanism is correct, but nothing would catch it breaking.

Purpose: `260903-n2o` fixed one of two identical call sites and left the other exposed
(F-1); its own `vpr-adapt` provenance derives from a SECOND condition list that disagrees
with the running parameters right now, on this checkout (F-2); the test it added is
insensitive to the regression it was written for (F-3); and one doc still describes three
deleted mechanisms in the present tense (F-4).

Output: one derivation of the eligibility pair, one resolution of the on-search winner, two
tests each observed failing under the exact regression they guard, and a doc that says what
was true when it was written.

Non-goal, explicitly: this task does NOT run a tuning search, promote, publish, or replay
the harness. No number moves. The only value that changes anywhere is `vpr-adapt`'s
selected-on set, which changes from a claim to a fact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260903-tk6-close-the-four-surviving-review-findings/260903-tk6-CONTEXT.md
@.claude/CLAUDE.md
@packages/harness/selectionProvenance.ts
@packages/harness/score.ts
@packages/harness/cli.ts
@packages/harness/publish.ts
@docs/models/sigma1-sensitivity-screen.md
</context>

<facts_established_during_planning>

Verified against the working tree on 2026-09-03. These are INPUTS to the tasks below — do not
re-derive them, but DO fail loudly if any turns out to be false.

**Working tree at planning time:** `git status --porcelain` showed only untracked `.planning/quick/`
directories. No tracked file was modified. The `cli.ts` edit and the stray
`packages/harness/__probe.test.ts` the review's verification agents left behind have already
been cleaned up. Task 1 re-checks this before touching anything.

**The import graph, which decides where shared code can live:**

| Edge | Where |
|---|---|
| `publish.ts` -> `cli.ts` | `publish.ts:61` imports `applyPromotedOverrides` |
| `cli.ts` -> `selectionProvenance.ts` | `cli.ts:88` imports `selectedOnSeasonsFor` |
| `publish.ts` -> `selectionProvenance.ts` | `publish.ts:103` |
| `selectionProvenance.ts` -> `promote.ts` | `PromotedVersionSchema` |
| `promote.ts` -> core/, corpus/db, replay, score | imports NOTHING from `cli.ts`, `publish.ts`, or `selectionProvenance.ts` |

**F-2, the delegation question, ANSWERED: delegate — do not align two condition lists.**
Direct delegation by importing `loadSearchWinnerVpr` from `cli.ts` is unavailable: it would
close a real cycle (`cli.ts` -> `selectionProvenance.ts` -> `cli.ts`), and there is a genuine
signature mismatch besides — `loadSearchWinnerVpr` returns an `AlgorithmModule`, which carries
no `seasons` field, so even a cycle-free import could not answer the provenance question.
BOTH obstacles dissolve by moving the decision DOWN instead of sideways: a new leaf module
`packages/harness/searchWinner.ts` owns the single five-gate resolution and returns
`{ params, seasons }`; `cli.ts` wraps it with `makeSigma1`, `selectionProvenance.ts` reads
`.seasons` off it. The fallback the CONTEXT permits (two aligned condition lists plus an
agreement test) is therefore NOT taken.

**F-1, the helper-home question, ANSWERED: `packages/harness/selectionProvenance.ts`, as a
WRAPPER around `aggregateScores`, not as an inputs-returning helper.**
- *Home:* it already owns half the pair (`selectedOnSeasonsFor`) and is already imported by
  BOTH call sites. It needs to add `selectCorpusSeasons`/`Corpus` from `../corpus/db.js` and
  `aggregateScores` from `./score.js`; neither of those imports anything from `cli.ts`,
  `publish.ts`, or `selectionProvenance.ts`, so no cycle appears.
- *Wrapper, not inputs:* a helper returning `{corpusSeasons, selectedOnSeasons}` still leaves
  a spreadable options literal at each call site — the exact regression that produced F-1
  stays available. A wrapper leaves no options argument at either site at all. Structural
  prevention over tested agreement, per this task's constraints.
- *Testability given `runSeasonsMode` is not exported:* the wrapper is exported and takes its
  corpus handle as an argument, so a temp corpus plus hand-built `HarnessPredictionInput[]`
  exercises the whole derivation with no network, no module-level `CORPUS_PATH`, and no
  Statbotics call. `runSeasonsMode` itself remains untestable (it opens the real `CORPUS_PATH`
  and awaits `statboticsReference`) — this task does NOT fix that, and must not imply it does.

**F-2 measured on this checkout.** `reports/tune-joint-on.json` exists (gitignored, dated
Aug 16), records `seasons: [2022, 2023, 2024]`, `winnerIndex: 76`, 77 candidates, and its
winner's params fail `Sigma1ParamsSchema.safeParse` under `SIGMA1_CODE_VERSION` 7.0.0. So
today `loadSearchWinnerVpr` returns undefined (with its warning) and `vpr-adapt` scores
untuned DEFAULTS, while `vprAdaptSelectedOnSeasons` returns `[2022, 2023, 2024]`. After
Task 2 both answer from one resolver and the provenance becomes `[]`.

**The existing `loadSearchWinnerVpr` fixtures record no `seasons` field**
(`promotedOverrides.test.ts:270-278`). The shared resolver must therefore treat top-level
`seasons` as OPTIONAL in its schema, or those green tests turn red for the wrong reason.

**`publish.test.ts` already publishes with the real `vpr` module over a synthetic corpus**
(lines 689, 734, 941, using `vpr` imported from `../core/algorithms/sigma1/index.js`). F-3
needs no new fixture machinery and no `resolvePublishAlgorithms` call — `publishSeasons`
reads only `a.id` for eligibility.

**The three deleted mechanisms appear in exactly two places in `docs/models/`:** line 15
(`"2025/2026 are HOLDOUT and structurally unreachable by tune.ts, see below"`) and lines
33-36 (the holdout-blindness bullet), both in `sigma1-sensitivity-screen.md`. Nowhere else
in any `.md` or `.ts` outside `.planning/`. The five other docs mentioning "holdout" as dated
prose are legitimate history and are OUT OF SCOPE.

**The matrix that must not move** (seven-season corpus 2019, 2020, 2022-2026):

| algorithm | selected-on | eligible seasons |
|---|---|---|
| `vpr` | `[2022, 2023, 2024]` | {2025, 2026} |
| `epa` | `[]` | {2022-2026} |
| `opr` | `[]` | {2022-2026} |
| `vpr-adapt` | `[2022, 2023, 2024]` today -> `[]` after Task 2 | the ONLY permitted movement |

**Explicit non-goal inside F-2's scope:** `vpr`'s own `PROMOTED_VPR_VERSION_PATH` is still
MIRRORED between `cli.ts` and `selectionProvenance.ts` rather than shared. That mirror is
already pinned by an existing agreement test (`selectionProvenance.test.ts:43`) and is not
what F-2 reported. Leave it mirrored; just make the header comment describe it accurately
instead of claiming a guard that covers `vpr-adapt` too.

**Known traps** (project memory): never prefix a command with `timeout` — it swallows output
and exits 0. Run `npx vitest run` from the REPO ROOT (167 files; running from `apps/web`
reaches only 77). Verify by printed output, never by exit code alone.

</facts_established_during_planning>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One derivation of the eligibility pair, wired through both call sites (F-1)</name>
  <files>packages/harness/selectionProvenance.ts, packages/harness/selectionProvenance.test.ts, packages/harness/cli.ts, packages/harness/publish.ts</files>
  <precondition>`git status --porcelain` lists no modified tracked file. The review's verification agents left experimental reverts behind once already; if any tracked file is modified before you begin, STOP and report it rather than committing someone else's experiment.</precondition>
  <behavior>
    Against a temp corpus holding 2022, 2023 and 2024 (non-offseason events, mirroring the
    fixtures at `packages/corpus/db.test.ts:782-815`) and hand-built predictions for 2024 ONLY:
    - An `opr` slice for 2024 is headline-eligible. The corpus supplies the two priors even
      though the run scored one season — this is the clause that reddens if the corpus-season
      source is narrowed to what the run happens to be scoring.
    - A `vpr` slice for 2024 is NOT headline-eligible. 2024 is in the committed version file's
      `provenance.tuneSeasons` — this is the clause that reddens if the selected-on source is
      replaced by an all-empty map.
    - Both assertions live in ONE test file so a single command demonstrates both reverts.
  </behavior>
  <action>
    Per F-1, the duplication is the defect: `cli.ts:776` and `publish.ts:1517`/`1998`
    independently build the same pair, which is exactly why fixing one left the other exposed.
    Collapse it to one derivation, and choose the shape that leaves no argument at the call
    sites to get wrong.

    In `packages/harness/selectionProvenance.ts`, add an exported wrapper:

      aggregateScoresForRun(db: Corpus, predictions: readonly HarnessPredictionInput[], algorithmIds: readonly string[]): ScoreSlice[]

    It calls `aggregateScores(predictions, { corpusSeasons: selectCorpusSeasons(db), selectedOnSeasons: selectedOnSeasonsFor(algorithmIds) })` and returns the slices. Import
    `selectCorpusSeasons` and the `Corpus` type from `../corpus/db.js` and `aggregateScores`
    plus the `HarnessPredictionInput`/`ScoreSlice` types from `./score.js`. Verify while
    editing that this adds no import cycle (see the import-graph table above). Do not export
    an inputs-returning variant alongside it — a second export is a second thing to get wrong.

    Carry the two facts the existing call-site comments record into this one function's doc
    comment, since the call sites lose them: the corpus-sourced season set is what the corpus
    HOLDS, never what a given invocation asked to replay (a range-derived value would let a
    single-season republish flip a live key's eligibility); and the selected-on argument comes
    from this module's registry, never a second independently-derived resolution.

    In `cli.ts`, replace the `aggregateScores(...)` call inside `runSeasonsMode` with the
    wrapper, passing the already-open `db` and `algorithms.map((a) => a.id)`. Leave the
    OTHER `aggregateScores` call in that file (the legacy single-event smoke path, which
    passes the not-claimed sentinel) completely alone — it claims no eligibility by design.
    Drop the now-unused `selectCorpusSeasons` import from `cli.ts` only if nothing else there
    uses it.

    In `publish.ts`, replace the compare-artifact `aggregateScores(...)` call with the
    wrapper, passing `db` and `options.algorithms.map((a) => a.id)`, and DELETE the hoisted
    `const corpusSeasons = selectCorpusSeasons(db)` above the season loop along with the
    now-unused import. Record the tradeoff in the new comment: the corpus-season query and the
    small version-file read now happen once per published season instead of once per run —
    negligible against a run that replays every match of every season, and worth the removal
    of a hand-buildable pair.

    Add the tests described in `<behavior>` to `selectionProvenance.test.ts`. Mirror
    `score.test.ts`'s prediction-fixture shape for `HarnessPredictionInput` and
    `packages/corpus/db.test.ts`'s corpus fixtures for the temp corpus; do not invent new
    fixture helpers.

    Then do the revert discipline — this is the point of F-1, not a formality. A test that
    "should" catch a regression but was never observed failing under it is exactly what
    produced this finding.
    1. Inside the wrapper, swap the corpus-season source for the seasons present in
       `predictions`. Run the focused test file. Observe RED. Paste the failing test name and
       assertion into the SUMMARY. Restore.
    2. Inside the wrapper, swap the registry call for a map of every requested id to an empty
       array. Run the focused test file. Observe RED. Paste the failing test name and
       assertion into the SUMMARY. Restore.
    3. Historical control, for honesty: put back the pre-fix shape at the `cli.ts` call site —
       a direct `aggregateScores` call with `corpusSeasons` taken from `runSeasonsMode`'s own
       `seasons` parameter and an all-empty selected-on map. Run the FULL suite. Record the
       OBSERVED result in the SUMMARY exactly as printed. If it stays green, say so plainly:
       call-site coverage does not exist, `runSeasonsMode` is still unreachable from a test
       (module-level corpus path, network call to Statbotics), and the protection this task
       delivers is that there is now exactly ONE derivation site rather than two. Do not claim
       coverage you did not observe. Restore.

    After step 3, confirm `git diff --stat` shows only the intended edits.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/selectionProvenance.test.ts packages/harness/publish.test.ts packages/harness/score.test.ts</automated>
  </verify>
  <done>The eligibility pair is built in exactly one function; neither flag-bearing call site passes an eligibility argument; the new tests pass; both reverts were applied, observed RED in printed output, and restored; the historical call-site control's observed result is recorded honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Make vpr-adapt's provenance and its running params ONE decision (F-2)</name>
  <files>packages/harness/searchWinner.ts, packages/harness/cli.ts, packages/harness/selectionProvenance.ts, packages/harness/selectionProvenance.test.ts, packages/harness/promotedOverrides.test.ts</files>
  <action>
    Per F-2, `vprAdaptSelectedOnSeasons` gates on three conditions while `loadSearchWinnerVpr`
    — which decides what actually runs — gates on five. Make disagreement structurally
    impossible rather than merely tested. Delegation is available; take it (the reasoning and
    the rejected fallback are in the facts section above).

    Create `packages/harness/searchWinner.ts` as a LEAF module — it may import `node:fs`,
    `node:path`, `zod`, `Sigma1ParamsSchema`/`SIGMA1_CODE_VERSION` from
    `../core/algorithms/sigma1/params.js`, and `TuneSearchOutputMinimalSchema` from
    `./promote.js`, and nothing else from the harness. Move `ON_SEARCH_ARTIFACT_PATH` here
    from `cli.ts` (with its doc comment) and export it. Export:

      resolveOnSearchWinner(searchArtifactPath: string): { params: Sigma1Params; seasons: readonly number[] | undefined } | undefined

    It carries the five gates VERBATIM from today's `loadSearchWinnerVpr`, in the same order
    and with the same outcomes: absent file -> undefined; malformed JSON or a minimal-schema
    parse failure -> throws as it does today; `winnerIndex` naming no candidate -> undefined;
    a winner whose params fail `Sigma1ParamsSchema.safeParse` -> undefined plus the existing
    stale-shape warning, word for word. `seasons` is the artifact's own top-level field, read
    through the SAME parse and declared OPTIONAL (see the facts section — the existing
    fixtures omit it and must stay green). Extend `TuneSearchOutputMinimalSchema` if zod makes
    that clean on a passthrough object; otherwise declare an equivalent local schema and say
    in a comment why.

    Rewrite `cli.ts`'s `loadSearchWinnerVpr` as a thin wrapper over it: resolve, return
    undefined if undefined, otherwise `makeSigma1` with the versioned `rpMonteCarloDraws`
    restore exactly as today. Keep its exported signature and behavior identical —
    `promotedOverrides.test.ts` must pass with no edit to its existing cases. Import
    `ON_SEARCH_ARTIFACT_PATH` from the new module rather than declaring it.

    Rewrite `selectionProvenance.ts`'s `vprAdaptSelectedOnSeasons` to delegate: resolve
    through the same function against the same imported constant; `undefined` -> `[]`, which
    is now the same condition rather than a second one. A winner that resolves but whose
    artifact records no `seasons` must THROW, naming the artifact path — a fitted module whose
    provenance silently reads as "never fitted" is the failure this task exists to close, and
    a silent `[]` there would be exactly that. Delete the now-dead local
    `JointArtifactSelectionSchema` and the duplicated path constant.

    Correct the false doc comments. The module header currently asserts that
    `selectionProvenance.test.ts` pins agreement against `resolvePublishAlgorithms(undefined)`
    and that this protects against two independently-derived resolutions — that test covers
    `vpr`'s version identity only and there is no `vpr-adapt` case at all. Rewrite the header
    and `vprAdaptSelectedOnSeasons`' own comment to state what is now true: `vpr-adapt` shares
    ONE resolver with what runs, so the two cannot disagree; `vpr`'s version path remains a
    deliberate mirror (importing it from `cli.ts` would close a cycle) pinned by the existing
    agreement test. Do not describe the `vpr` mirror as shared, and do not silently widen this
    task into sharing it.

    Note and accept one consequence: the stale-shape warning now also prints when provenance
    is resolved, so a harness run may print it twice. It names the file and the reason both
    times; leave it.

    Tests:
    - In `promotedOverrides.test.ts`, beside the existing `loadSearchWinnerVpr` block, add
      direct `resolveOnSearchWinner` cases over temp dirs (reuse that file's own helpers): an
      artifact carrying `seasons` returns them; an artifact omitting `seasons` returns a
      winner with `seasons` undefined; a winner whose params fail the schema returns undefined.
    - In `selectionProvenance.test.ts`, add the `vpr-adapt` case that has never existed, and
      write it so it holds BOTH on a developer machine with a stale artifact and in CI where
      `reports/` is empty: resolve `vpr-adapt` through `applyPromotedOverrides` and assert
      that `selectedOnSeasonsFor(['vpr-adapt'])['vpr-adapt']` is non-empty if and only if the
      resolved module is the search winner (its version carries the search-winner param-set
      suffix) rather than the untuned base. That equivalence IS the structural claim; it must
      not depend on a gitignored file's presence.

    Finally, print the before/after matrix from the repo root and record both in the SUMMARY:
    `npx tsx -e "import('./packages/harness/selectionProvenance.js').then(m => console.log(JSON.stringify(m.selectedOnSeasonsFor(['opr','epa','vpr','vpr-adapt']))))"`.
    `opr`, `epa` and `vpr` must be identical before and after. `vpr-adapt` must go from
    `[2022,2023,2024]` to `[]` on this checkout. If any other id moves, the fix is wrong —
    stop and report.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/promotedOverrides.test.ts packages/harness/selectionProvenance.test.ts packages/harness/baselineFingerprint.test.ts</automated>
  </verify>
  <done>One resolver answers both "what runs" and "what the flag claims" for `vpr-adapt`; `loadSearchWinnerVpr` keeps its signature and its existing tests pass unedited; the header comment no longer claims a guard that does not exist; `vpr-adapt` reads `[]` on this checkout and `opr`/`epa`/`vpr` are unmoved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert vpr's published eligibility, and prove the assertion is sensitive (F-3)</name>
  <files>packages/harness/publish.test.ts</files>
  <behavior>
    - Corpus holds non-offseason events and matches in 2022, 2023, 2024 and 2025; the publish
      run covers 2024 and 2025 only.
    - The `vpr` combined slice for 2024 in `compare/2024.json` is NOT headline-eligible — 2024
      is inside `vpr`'s selected-on set, though it has three corpus priors.
    - The `vpr` combined slice for 2025 in `compare/2025.json` IS headline-eligible.
    - The two seasons differ only in the selected-on clause, so the pair isolates exactly the
      contribution `selectionProvenance.ts` makes.
  </behavior>
  <action>
    Per F-3, the existing block at `publish.test.ts:2154` publishes `algorithms: [opr]`, and
    `opr`'s registry entry is a hardcoded empty array — so the registry's entire contribution
    can be deleted with that test still green. Repo-wide, no test asserts `headlineEligible`
    for `vpr` in a published compare artifact. Add that assertion.

    Extend the existing eligibility describe block (do not start a parallel one) with a case
    publishing `algorithms: [vpr]` — the module already imported in this file from
    `../core/algorithms/sigma1/index.js`, which other cases in this file already publish over
    synthetic corpora. Reuse this file's `seasonEvent`/`seasonMatch`/`findCompareArtifact`
    helpers; add no new fixture machinery. Assert both slices per `<behavior>`.

    State in the test's own comment that its expectation is pinned to the committed version
    file's `provenance.tuneSeasons`, so a future re-tune that promotes a different selected-on
    set is SUPPOSED to redden this test — a headline-eligibility matrix change must be a
    deliberate, visible edit.

    If `vpr` cannot score 2025 over the synthetic fixture for an unrelated season-support
    reason, substitute 2026 for 2025 (2026 is already proven in this file) and keep everything
    else identical. Do not fall back to a non-`vpr` algorithm — an id whose registry entry is
    a hardcoded empty array is precisely what made the old test insensitive.

    Then the mutation discipline, same rule as Task 1: replace the registry call inside
    `aggregateScoresForRun` with a map of every requested id to an empty array, run this test
    file, observe RED (2024 flips to eligible), paste the printed failure into the SUMMARY,
    and restore. Confirm `git diff --stat` afterwards shows `publish.test.ts` only.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/publish.test.ts</automated>
  </verify>
  <done>A published `compare/{year}.json` carries an asserted `vpr` eligibility on both sides of the selected-on boundary; the assertion was observed RED under an all-empty selected-on map and restored; no fixture file changed.</done>
</task>

<task type="auto">
  <name>Task 4: Date the sensitivity screen's holdout claim instead of asserting deleted mechanisms (F-4)</name>
  <files>docs/models/sigma1-sensitivity-screen.md</files>
  <action>
    Per F-4, this doc asserts in the present tense that `tune.ts` refuses holdout seasons via
    `HOLDOUT_SEASONS`, re-checks requested seasons via `seasonSplit`, and re-checks produced
    slices for `seasonLabel`. All three are deleted. This is the project's own named
    historical failure — documentation describing a model that no longer exists.

    Two edits, both in the Method section, and NOTHING else in this file:
    1. The holdout-blindness bullet (lines 33-36). Rewrite it as dated history: state that
       when this screen ran, `tune.ts` enforced a fixed tune/holdout season split structurally
       through the mechanisms named there, and that those mechanisms have since been retired.
       Keep the one claim that is still true and still checkable from the screen's own
       reported seasons — the screen swept 2022 and 2023 only and never touched 2025 or 2026.
       Then point at what governs eligibility now: `score.ts`'s two-clause corpus-relative
       `isHeadlineEligible` (enough distinct prior seasons in the declared corpus AND the
       season absent from the scoring algorithm's own selected-on set) sourced from
       `selectionProvenance.ts`'s registry. Naming the retired identifiers inside a past-tense
       sentence is correct and expected; asserting they still run is the defect.
    2. Line 15's parenthetical, which makes the same present-tense claim and points at that
       bullet. Bring it into agreement with the rewritten bullet.

    Do not touch the Method section's other bullets, the results table, or anything below it —
    those are the screen's measurements and they stand. Do not touch any other file in
    `docs/models/`: five other docs mention "holdout" as dated prose recording measurements
    taken under the old scheme, and that is legitimate history the CONTEXT puts out of scope.
  </action>
  <verify>
    <automated>test "$(git status --porcelain -- docs/ | wc -l)" -eq 1 && git diff --stat -- docs/</automated>
  </verify>
  <done>The screen states what was true when it ran, in the past tense, and names the current mechanism; exactly one file under `docs/` is modified.</done>
</task>

<task type="auto">
  <name>Task 5: Full-suite gate, matrix re-check, and commit</name>
  <files>(no source edits — verification and commit only)</files>
  <action>
    Run every check from the REPO ROOT, never with a `timeout` prefix (project memory: it
    swallows output and exits 0). Read the printed output, not the exit code.

    1. `npx vitest run` — the full suite, 167 files. Every test passes.
    2. `npx tsc --noEmit` at the root (covers `packages/**` and `scripts/**`).
    3. `cd apps/web && npx tsc --noEmit` — the root tsconfig does not reach it.
    4. `git diff --exit-code -- apps/web/src/routes/__fixtures__/` — must exit 0.
    5. `git status --porcelain -- data/ reports/` — nothing may appear; this task tunes
       nothing, promotes nothing, and publishes nothing.
    6. Re-print the matrix:
       `npx tsx -e "import('./packages/harness/selectionProvenance.js').then(m => console.log(JSON.stringify(m.selectedOnSeasonsFor(['opr','epa','vpr','vpr-adapt']))))"`
       — `opr: []`, `epa: []`, `vpr: [2022,2023,2024]`, `vpr-adapt: []`. Any other value on
       the first three means a fix moved the matrix; stop and report rather than commit.
    7. `git status --porcelain` — confirm the modified set is exactly the files this plan
       declares, with no leftover revert and no stray probe file from the revert cycles.

    Then commit. One commit, message body naming F-1 through F-4 and stating that the two
    revert cycles and the one mutation cycle were observed red before restore.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && git diff --exit-code -- apps/web/src/routes/__fixtures__/</automated>
  </verify>
  <done>Full suite green, both typechecks clean, fixtures byte-identical, `data/` and `reports/` untouched, the matrix unmoved except `vpr-adapt`, and one commit landed.</done>
</task>

</tasks>

<verification>
Run from the REPO ROOT, never with a `timeout` prefix:

1. `npx vitest run` — full suite, 167 files.
2. `npx tsc --noEmit` at the root.
3. `cd apps/web && npx tsc --noEmit`.
4. `git diff --exit-code -- apps/web/src/routes/__fixtures__/` — must exit 0.
5. `git status --porcelain -- data/ reports/` — empty.
6. The selected-on matrix probe prints `opr: []`, `epa: []`, `vpr: [2022,2023,2024]`,
   `vpr-adapt: []`.
7. The SUMMARY records, as printed output, the RED observed under: the narrowed corpus-season
   revert, the all-empty selected-on revert, and the `publish.test.ts` mutation — plus the
   honest observed result of the historical `cli.ts` call-site control.
</verification>

<success_criteria>
- F-1: the `{corpusSeasons, selectedOnSeasons}` pair is derived in exactly one exported,
  tested function. Neither flag-bearing call site passes an eligibility argument at all.
  Both reverts were observed red, not assumed.
- F-1, honestly bounded: `runSeasonsMode` remains uncovered, and the SUMMARY says so rather
  than implying the call site is now tested.
- F-2: `vpr-adapt`'s provenance and its running parameters read ONE resolver. The
  two-condition-list fallback was rejected with a stated reason (import cycle plus a return
  type carrying no `seasons`), and delegation was achieved by moving the decision into a leaf
  module instead.
- F-2: `selectedOnSeasonsFor(['vpr-adapt'])` is `[]` on this checkout, matching the untuned
  DEFAULTS module that actually scores; the header comment no longer claims a guard that does
  not exist.
- F-3: a published compare artifact's `vpr` eligibility is asserted on both sides of the
  selected-on boundary, and the assertion was observed red under an all-empty map.
- F-4: the sensitivity screen dates its holdout claim and names the current mechanism; no
  other doc changed.
- The eligibility matrix is unmoved for `vpr`, `epa` and `opr`; `vpr-adapt` is the only
  movement and it moves toward the truth.
- No fixture byte changed; nothing was tuned, promoted, published, or replayed.
</success_criteria>

<output>
Create `.planning/quick/260903-tk6-close-the-four-surviving-review-findings/260903-tk6-SUMMARY.md` when done.

Record in it, explicitly:
- The printed RED for each of the three applied regressions (two reverts in Task 1, one
  mutation in Task 3), by failing test name and assertion.
- The observed result of Task 1's historical `cli.ts` call-site control, stated plainly —
  including "still green, still uncovered" if that is what happened, with the structural
  mitigation named.
- The before/after `selectedOnSeasonsFor(['opr','epa','vpr','vpr-adapt'])` output, showing
  three ids unmoved and `vpr-adapt` correcting from `[2022,2023,2024]` to `[]`.
- That `vpr`'s version-file path remains a deliberate mirror rather than a shared import, why
  (cycle), and which existing test pins it.
</output>
