---
quick_id: 260907-3fq
date: 2026-09-07
description: Re-issue NAV-01 requirement text and correct the stale RP-PMF blurb in CLAUDE.md
source: .planning/v1.0-MILESTONE-AUDIT.md (tech_debt, priority highest, "cross-cutting" bucket)
---

# Quick Task 260907-3fq — Close the two documentation drifts the v1.0 audit found

## Why

The v1.0 milestone audit (2026-09-07, commit `d01f1419`) scored 38/38 requirements with no blockers
and landed at `status: tech_debt`. Its `highest` priority bucket held exactly two items, both the
same defect class: **a document describing a system that no longer exists.** That is the project's
own named historical failure mode ("the README described a model that had been deleted"), so it is
the one bucket worth closing before the milestone is archived.

Everything else the audit found is either a dated signed decision, a bookkeeping lag where the code
is already correct, or a coverage TODO. Those are deliberately out of scope here.

## Task 1 — Re-issue NAV-01's text

**Drift:** `.planning/REQUIREMENTS.md` reads "Top ribbon navigates to Teams, Events, and Compare".
`apps/web/src/components/ribbon/Ribbon.tsx:77-86` links Teams / Events / Districts / Methodology.

**Cause:** quick task `260905-phf` (2026-09-05, commit `a9d971c2`) replaced the ribbon's Compare
slot with Methodology and moved the Compare page intact via `git mv` to `/methodology/compare`.
Deliberate and user-directed — the requirement text simply never followed.

**This is not a scope reduction.** Compare is still reachable, still deep-linkable, and still one
ribbon click away: `apps/web/src/routes/compare.tsx` redirects `/compare` to the new path with
search params preserved (a fixed literal target, no open-redirect), and both
`VprGuide.tsx:49` and `AcknowledgmentsPage.tsx:59` link to it.

**Method — the ALGO-01 pattern, exactly.** Phase 3.2 hit this same situation when OPR went
event-scoped and resolved it by correcting the text in place, preserving the `[x]` and the ID, and
appending a dated "Re-issued" note that states plainly it is a text correction rather than a new
requirement. `.planning/REQUIREMENTS.md` lines 37-41 are the template. Follow it.

**Must be true when done:**

- NAV-01's bullet describes the four destinations the ribbon actually carries, and names where the
  Compare table now lives.
- NAV-01's `[x]` and its ID are byte-identical to before — no new ID, no lost completion mark.
- A dated "Re-issued 2026-09-07" note sits below the NAV block, cites `260905-phf`, and states
  explicitly that this is a text correction and not a scope reduction.
- The traceability table's `NAV-01 | Phase 5 | Complete` row is untouched — the requirement was
  genuinely satisfied by Phase 5 and still is.

## Task 2 — Correct the stale RP-PMF blurb in CLAUDE.md

**Drift:** `.claude/CLAUDE.md`'s sketch-findings skill blurb states "One gap remains and is Phase
8's: played event matches carry no `redRpPmf`/`blueRpPmf`, so the rank simulation cannot rewind
into played matches until that republish lands."

**Evidence it is false:** the republish landed on 2026-09-06 (STATE row 74 — 75,796 objects, 216
presim sidecars live for the first time). The milestone audit fetched
`https://data.sigmascout.org/v1/event/2024casf/vpr@10.0.0+rolling-2026-09d.json` on 2026-09-07 and
measured **72/72 played qualification matches carrying both PMFs**.

This blurb is auto-loaded into context during UI implementation, so a stale claim here actively
misleads future work — it is worse than a stale planning doc.

**Must be true when done:**

- The blurb no longer names an open gap that is closed.
- The correction carries its evidence (the 2026-09-06 republish, the 72/72 live verification and its
  date), not just a deletion — so a future reader can tell the difference between "resolved" and
  "quietly dropped".
- The edit stays inside the `<!-- GSD:skills-... -->` markers and changes nothing else in the block.

## Out of scope

The audit's six `bookkeeping` items (Phase 5's moot NAV-06 override, Phase 6 and 7 stale seal lines,
07-UAT's pending-deploy entries, 07-UI-REVIEW, 03.2-REVIEW, the post-08 UI review) are real but are a
different, larger job — closing them means editing seven sealed phase artifacts. Filed by the audit,
not touched here.

## Verification

Documentation-only change; no code, no tests, no artifacts. Verify by reading:

1. `grep -n "NAV-01" .planning/REQUIREMENTS.md` — bullet text matches the ribbon; `[x]` intact.
2. The ribbon's four `<Link to=...>` targets equal the four the bullet now names.
3. `grep -n "redRpPmf" .claude/CLAUDE.md` — no surviving claim that the gap is open.
