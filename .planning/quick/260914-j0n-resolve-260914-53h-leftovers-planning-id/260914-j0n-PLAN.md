---
phase: 260914-j0n-resolve-260914-53h-leftovers-planning-id
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Wave 1, W1-1 (L3, L4, L2 outside scripts)
  - packages/core/rankingPoints/2018.ts
  - packages/core/rankingPoints/2019.ts
  - packages/core/rankingPoints/2023.ts
  - packages/core/rankingPoints/2024.ts
  - packages/core/rankingPoints/2025.ts
  - packages/core/rankingPoints/analyticPmf.ts
  - packages/core/algorithms/opr.ts
  - packages/corpus/db.ts
  - packages/ingest/rankings.ts
  - packages/spr/sealedPaths.ts
  - packages/harness/publish.ts
  - packages/harness/manifestSchemas.ts
  - packages/harness/pageArtifacts.ts
  - packages/harness/score.ts
  - packages/harness/stateSnapshot.ts
  - apps/worker/src/stateProbe.ts
  - docs/models/sigma1-rp-verification.md   # git mv source
  - docs/models/rp-verification.md          # git mv target
  # Wave 1, W1-2 (L5, L6)
  - packages/harness/publish.test.ts
  - packages/harness/stateSnapshot.test.ts
  # Wave 1, W1-3 (L2 scripts)
  - scripts/measureFieldAveragedRanks.ts
  - docs/models/field-averaged-presim.md
  - scripts/verifySubsetPublish.ts
  - scripts/measureAwardPredictability.ts
  - scripts/measureMatchBandCoverage.ts
  - scripts/measureRpCalibration.ts
  - scripts/replayRig.ts
  # Wave 2 (L1 titles), exact lists in $LISTS/W2-1..W2-5-files.txt (149 test/spec files)
  - packages/harness/*.test.ts        # W2-1 (7 files), W2-2 (20 files)
  - scripts/*.test.ts                 # W2-2 (7 files)
  - packages/core/**/*.test.ts        # W2-3
  - packages/corpus/*.test.ts         # W2-3
  - packages/ingest/*.test.ts         # W2-3
  - packages/spr/*.test.ts            # W2-3 (evaluate.test.ts, holdout.test.ts; not sealed)
  - apps/worker/test/*.test.ts        # W2-3
  - apps/web/src/**/*.test.ts(x)      # W2-4 (21 files), W2-5 (45 files)
  - apps/web/e2e/*.spec.ts            # W2-5 (11 files)
autonomous: true
requirements: [QUICK-260914-j0n]

estimate:
  tokens: 700000
  raw_tokens: 1400000
  tasks: 8
  confidence: high

must_haves:
  truths:
    - "L1: `node $STR scan --files $LISTS/W2-<n>-files.txt` prints zero TITLE rows for each of W2-1..W2-5, except rows the task report names as kept under rule R6 (at planning time the only known one is packages/core/rankingPoints/analyticPmf.test.ts:297, the `(16-12)/4` z-score formula)"
    - "L1 is literal-only: for every task, `node $STR guard --base <task BASE> --files <task list>` ends `STRINGS GUARD OK` and `node $STR dupes` ends `DUPES OK`; `node $STR pairs` shows TITLE changes plus only the non-title literals that task enumerates"
    - "L2: the production strings enumerated in W1-1 and W1-3 carry no planning id (quick-task id, D-/F-/PD-/WR-/A-FA ids, plan/Phase refs, planning-doc names); stateProbe's rp=0 warning still contains `rp=0` and `ABLATED ARM`"
    - "L3+L4: `git grep -n -e rp:conservative-branch -e rpConservativeBranch -e sigma1-rp-verification -- ':!.planning'` prints nothing; docs/models/rp-verification.md exists, the old path does not, and `git log --follow` on the new path reaches the old history"
    - "L5: packages/harness/publish.test.ts contains no `\"vpr\"` literal and no `vpr`-prefixed identifier; its pass count is unchanged"
    - "L6: in packages/harness/stateSnapshot.test.ts the `Missing league row` banner sits directly above `describe(\"deserializeState — missing league row\"`"
    - "renderDoc in scripts/measureFieldAveragedRanks.ts and the committed docs/models/field-averaged-presim.md carry the same replacement text; the doc's json block (lines 11-273) is byte-identical to base"
    - "Nothing else moves: root `npx vitest run` = 237 files passed, 5,304 tests passed, 1 skipped; root, worker and web tsc 0 errors; `npx vite build` succeeds; `npx playwright test --list` still reports `Total: 304 tests in 20 files`"
    - "Sealed files (packages/spr/model.ts, evaluate.ts, data.ts, cli.ts, packages/core/algorithms/spr.ts) unchanged; every commit used explicit paths; no .env content read"
  artifacts:
    - path: "docs/models/rp-verification.md"
      provides: "RP threshold verification record under a live name"
      contains: "## Conservative-Branch Understatement"
    - path: "packages/core/rankingPoints/2023.ts"
      provides: "untrackedGate note citing the renamed doc, no dead command"
      contains: "docs/models/rp-verification.md"
    - path: "packages/harness/publish.test.ts"
      provides: "Fixtures on a live algorithm id"
      contains: "algorithmId: \"spr\""
    - path: "C:/Users/Jacob/AppData/Local/Temp/claude/c--Users-Jacob-Documents-GitHub-SigmaScout/5084c558-8583-40c8-8798-8c91dab02553/scratchpad/j0n-strings/strings.mjs"
      provides: "Literal-only guard, pairs, dupes, scan (scratchpad, not in repo; built and self-tested at planning time)"
      contains: "STRINGS GUARD OK"
  key_links:
    - from: "packages/core/rankingPoints/2023.ts, 2024.ts, 2025.ts untrackedGate.note"
      to: "docs/models/rp-verification.md `## Conservative-Branch Understatement`"
      via: "path string inside the note"
      pattern: "docs/models/rp-verification.md's Conservative-Branch Understatement"
    - from: "scripts/measureFieldAveragedRanks.ts renderDoc"
      to: "docs/models/field-averaged-presim.md"
      via: "writeFileSync(FIELD_AVERAGED_DOC_PATH, renderDoc(...)); the doc is hand-edited to match, never regenerated"
      pattern: "renderDoc"
    - from: "apps/worker/src/stateProbe.ts rp=0 warning"
      to: "apps/worker/test/stateProbe.test.ts:591-592"
      via: "toContain(\"rp=0\") and toContain(\"ABLATED ARM\")"
      pattern: "ABLATED ARM"
    - from: "scripts/measureRpCalibration.ts"
      to: "scripts/measureRpCalibration.test.ts SOURCE assertions"
      via: "raw-text regex counts and negative matches (see 53h-PLAN `## Known source readers`)"
      pattern: "rpMeanShift|latticeRuleModule"
---

<objective>
Resolve every leftover in `260914-53h-SUMMARY.md` `## Left alone on purpose`, repo-wide:
- **L1:** planning ids in test titles.
- **L2:** planning ids in runtime, log and error strings.
- **L3:** the dead `untrackedGate.note` command.
- **L4:** the doc carrying a retired name.
- **L5:** the retired fixture id in publish.test.ts.
- **L6:** the misplaced stateSnapshot.test.ts banner.

No behaviour and no published byte changes. The notes and strings touched are never uploaded, so nothing is published, deployed or pushed by executors.

Dispatch shape (orchestrator):
- **Wave 1**, three tasks, file-disjoint, run in parallel:
  - W1-1: L3, L4 and L2 outside scripts.
  - W1-2: L5 and L6.
  - W1-3: L2 in the measurement scripts and field-averaged-presim.md.
- **Wave 2**, five title batches, file-disjoint, concurrency cap 4. They run after Wave 1, because W2-1 and W2-2 share files with W1-2, and W2-2 runs a test that reads W1-3's script source.
  - Start W2-1, W2-3, W2-4 and W2-5 first.
  - Start W2-2 when a slot frees; it shares the corpus-heavy node suites with W2-1.

**Inventory correction.** An AST scan of every string literal in 255 test/spec files found more than the orchestrator's rough ~505 hits: 702 title literals with a planning token, plus 25 titles whose only token is a date. There were 38 non-title test literals: 31 in scope, and 7 reviewFrontmatterLint fixtures that are domain data. Of the 702 title hits, one is a false positive (`16-12` in a formula).

At ~150 edits per executor that needs five batches (144, 119+2d, 149+1d, 143+15d, 147+7d). Planning-time scans per task sit in `$LISTS/<TASK>-scan-planning.tsv` with line numbers.

**Tracer-first** is realised per executor: step P2 edits, guards, tests and commits one file alone before scaling out.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260914-53h-resolve-todo-finish-nvn-held-back-trims-/260914-53h-SUMMARY.md
@.planning/quick/260914-53h-resolve-todo-finish-nvn-held-back-trims-/260914-53h-PLAN.md
@.claude/CLAUDE.md

## Environment facts (verified 2026-09-14 at HEAD 156a3808, plan on them)

- 53h-PLAN `## Environment facts` still holds: `use_worktrees` is false, everyone works on the main checkout, other sessions may be active, executors RETURN report text and never write SUMMARY.md.
- Clean tree. The 54 unpushed commits are all 53h's.
- No snapshot files and no `-t`/testNamePattern/`--grep` filters anywhere, so renaming a title cannot unhook a test.
- Nothing reads a test file's titles. Checked: comparePalette.test.ts reads its own path only as a path.
- The `untrackedGate` notes and 2019's constant-predicate `reason` are metadata only:
  - `git grep untrackedGate` outside the season modules hits only constants.ts types.
  - measureRpCalibration.ts computes its own `reason`; it never reads the module's.
  - No test asserts note or reason text.
- `docs/models/sigma1-rp-verification.md` is referenced outside `.planning/` only by the 2023/2024/2025 notes. No test, web page or other doc links it. It covers seasons 2022-2026 only, so the 2018 (0.2225%) and 2019 (4.7324%) figures have no doc record to cite.
- Readers of `docs/models/field-averaged-presim.md`: only `renderDoc`, which writes it. No test reads it.
  - Lines to change: 277, 303-308, 325 and 358-360.
  - The json block (lines 11-273) contains no ids; leave it alone.
- Tests that pin L2 text:
  - apps/worker/test/stateProbe.test.ts:591-592 requires the rp=0 warning to contain `rp=0` and `ABLATED ARM`.
  - measureRpCalibration.test.ts reads its script's raw SOURCE: see 53h-PLAN `## Known source readers`. Never add `rpMeanShift`, `latticeRuleModule`, `--allow-reporting` or `--force` to any string there.
  - rpSeed/sigmaSeed read publish.ts's seed block; the two log lines sit outside it.
- Type coverage:
  - `apps/web/e2e/*.spec.ts` is in no tsconfig; verify it with the guard plus `npx playwright test --list` from apps/web (`Total: 304 tests in 20 files` at planning time, no server needed).
  - Root tsc covers packages and scripts. Worker tsc: `-p apps/worker/tsconfig.json`. Web tsc: `-p apps/web/tsconfig.json` (src only).
- Sealed files carry no planning-id string; nothing in them is touched.
  - `packages/spr/sealedPaths.ts` is not sealed: it lists the five sealed paths.
  - `packages/spr/evaluate.test.ts` and `holdout.test.ts` are not sealed.

## Tool: strings.mjs (session scratchpad, NOT in the repo; built and self-tested at planning time)

Prefix every Bash command with:
`SP=C:/Users/Jacob/AppData/Local/Temp/claude/c--Users-Jacob-Documents-GitHub-SigmaScout/5084c558-8583-40c8-8798-8c91dab02553/scratchpad/j0n-strings; STR=$SP/strings.mjs; LISTS=$SP/lists; LOG=$SP/logs; REPO=C:/Users/Jacob/Documents/GitHub/SigmaScout`

Shell state does not persist between calls. If `$STR` is missing, STOP and report; do not reimplement it.

| Subcommand | What it does and what to expect |
|---|---|
| `node "$STR" selftest` | Synthetic guard/classify/dupes/scan cases plus a self-guard of all 540 tracked TS files. Last line must end `failures=0`. |
| `node "$STR" scan --files <list> [--ref <rev>]` | TSV `path line KIND tokens text` for every literal (TS) or line (.md) matching the provenance regex, then a `scan files= hits=` summary. KIND is TITLE, EXPECT_MSG, LOG, ERROR, `ARG:<callee>#i`, `PROP:<name>`, `OTHER:<parent>`, STRICT, JSXTEXT or TEXTLINE. |
| `node "$STR" guard --base <rev> --files <list> [--rename old=new ...]` | Compares base and working ASTs with comments removed and value-position literal text masked. |
| `node "$STR" pairs --base <rev> --files <list> [--rename old=new ...]` | Every changed value literal, old then new, with KIND, then a `pairs` summary. |
| `node "$STR" dupes --base <rev> --files <list>` | Flags titles that newly collide within the same describe chain. Must end `DUPES OK`. |

`guard` details:
- **Strict positions:** a literal here that changes fails the guard. They are import/export/module specifiers, require/import()/vi.mock arguments, property names, element-access keys, case labels, ===/!==/==/!=/in operands, and anything inside a type.
- **Also fails** on:
  - an identifier, number, JSX-text or structure change
  - more parse errors
  - a changed pragma count
  - any sealed-file diff
- `.md` files print SKIP.
- Changed files outside the list print NOTE (usually a peer session).
- Must end `STRINGS GUARD OK`.

Lists (repo-relative, one per line) already exist: `$LISTS/W1-1-files.txt` (16 TS files; the renamed doc is checked separately), `W1-2-files.txt`, `W1-3-files.txt` (includes the .md), `W2-1..W2-5-files.txt`. Write all logs to `$LOG/<TASK>-*.txt`.

## Rewrite rules (every task)

- **R1 Remove.** Delete the planning token and anything it leaves empty or dangling: a wrapper such as `(D-05)`, `(plan 06-02 Task 3)` or `[07-20]`, a leading `08-11: `, and connectives such as `per`, `since`, `as of`, `—`, `/`, `,`.
  - Planning tokens: quick-task ids, `quick task`, `Phase N`, `plan NN-NN`, bare `NN-NN` plan ids, `Task N`, `Step N`.
  - Decision, finding, gap, threat, review and requirement ids: D-, F-, Fn, PD-, SC-, G-, T-, WR-, CR-, IN-, A-FA, ALGO-/COMP-/DATA-/ENH-/EVAL-/EVNT-/NAV-/TEAM-, `decision T1`, and UI-SPEC state ids E1-E5.
  - Planning-doc names: `*-PLAN.md`, `RESEARCH`, `CONTEXT.md`, `07-UAT.md`, `REVIEW.md`, `UI-SPEC`, `Copywriting Contract`, `09-PLAN-OUTLINE.md`, `Pitfall N`.
  - No double spaces, no `()`, no trailing dash.
- **R2 Replace when the sentence needs it.** State the reason the id stood for. Examples:
  - `D-2's two-prior threshold` becomes `the two-prior threshold`.
  - `F10's dot threshold` becomes `the bonus-dot threshold`.
  - `Task 1's inequality` names the inequality the test body asserts.

  Take the meaning only from the test body, the code under test, or an adjacent comment. Never take it from `.planning/` (53h N4, never invent). If it cannot be recovered from code in view, drop the reference.
- **R3 Dates and history.** Delete dates that stamp when something changed or was decided, e.g. `(2026-09-13)`, `(user decision 2026-09-05)`, `removed on 2026-09-07`. Also delete history clauses:
  - `one ranking helper since 260912-tnk`
  - `carried forward from the deleted sigma1/rp/distribution.test.ts`
  - `the retired pre-04-08 shape`, which becomes `the retired shape`
  - `HISTORY: used to expect ...` chains

  KEEP dates that are data or evidence the code uses: fixture dates, `on or after 2024-02-24`, `measured 2026-09-12` where the test asserts that date, and measureRpCalibration.ts:120's reporting-slice refusal dates.
- **R4 Nothing descriptive left?** If only an id and a separator remain (e.g. `G-9 — ` + template), keep the template and name the behaviour from the test body.
- **R5 Uniqueness.** `dupes` must print `DUPES OK`. On a collision, add a distinguishing phrase taken from the test body.
- **R6 Keep:**
  - `Test N`/`Test Nb` enumeration labels (local numbering other titles cross-reference; not an id into a planning doc)
  - `rung N`, `sketch NNN`, `TRACER`/`tracer`, tick `Phase A/B` and step `Phase 1/2` labels
  - `D1` (Cloudflare), seasons, event/team keys, measured figures, generation and commit hashes used as evidence
  - `REGRESSION`/`ADVERSARIAL INPUT` labels, `.each` format specifiers
  - the analyticPmf.test.ts:297 formula
  - reviewFrontmatterLint.test.ts fixtures, which are review-file data that linter parses
- **R7 Retired algorithm names** (vpr, bpr, sigma1). Leave them where the test's behaviour is about them, e.g. `rejects the retired vpr id`. Delete them only inside a history clause removed under R3. Never type one into new text. L5 is the only fixture-id change.
- **R8 Quoted doc headings.** When a string cites a heading that itself carries ids (liveAlgorithmTier.test.ts:412 cites docs/publish-budget.md `## Worker runtime budget (D-21/D-23, plan 04-07)`), cite the id-free prefix: `the "Worker runtime budget" section`. Never edit docs/publish-budget.md; payloadBudget.test.ts reads it.
- **R9 Scope.** Change only title literals plus the non-title literals the task enumerates.
  - Comments are out of scope, except a comment directly adjacent to an edited string that repeats the same id: trim it the same way (53h comment policy).
  - Never change an identifier (L5's listed renames excepted), a number, a test body or an import.
</context>

<shared_procedure>
Reuse 53h-PLAN `## Shared procedure`: O4 ownership check, O6 garble check, O7 commit mechanics, O8 test rules, O10 stop rule; N1 sealed files, N4 never invent, N6 no network or `.env`. These j0n overrides win:

- **P0 Setup.**
  1. `node "$STR" selftest` must end `failures=0`.
  2. Record BASE: `git -C "$REPO" rev-parse HEAD > "$LOG/<TASK>-base.txt"`.
  3. Wave 2 only: first confirm Wave 1 landed. `git -C "$REPO" log --oneline -40 | grep -c "(260914-j0n)"` must print 5 or more, and `git -C "$REPO" status --short -- packages/harness/publish.test.ts packages/harness/stateSnapshot.test.ts` must print nothing. Otherwise STOP.
- **P1 Baselines.**
  - `node "$STR" scan --files "$LISTS/<TASK>-files.txt" > "$LOG/<TASK>-scan-before.txt"`; compare it with `$LISTS/<TASK>-scan-planning.tsv` and re-rank if a peer changed a file.
  - Run the task's test command into `$LOG/<TASK>-tests-before.txt` and read the `Test Files` and `Tests` lines.
  - Run the task's tsc commands and count `error TS`.
- **P2 Tracer.** Take the file with the most hits. Apply O4, edit it, then run guard, dupes, and pairs filtered to that file. Read every `+` line of pairs as a sentence (this replaces O6 for literals). Run the scoped tests on that file alone and commit it. Only then scale out.
- **P3 Batches.** Work file by file in scan order: Read about 10 lines around each hit, then Edit.
  - Commit one file per commit when it has 10 or more hits; otherwise group up to 10 files from one directory.
  - Before each commit: guard OK, dupes OK, pairs reviewed, and the batch's files pass their tests with unchanged counts.
  - Leftover check on the `+` lines: `node "$STR" pairs ... | grep -nE '^  \+ .*(\(\s*\)|\(\s*[,;/—]|[,;/—]\s*\)|  |\s[,;:.)])'`. Read and justify every hit.
- **P4 Commit.** Per O7: `git -C "$REPO" commit -m "<subject>" -m "Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>" -- <explicit paths>`.
  - Retry once on `index.lock`.
  - `git status --short -- <paths>` must then print nothing.
  - Subjects: `test(260914-j0n): drop planning ids from <area> test titles`; `refactor(260914-j0n): ...` for runtime strings and fixtures; `docs(260914-j0n): ...` for the doc move.
- **P5 Final checks.**
  - Guard over the whole list against BASE: `STRINGS GUARD OK`.
  - `dupes`: `DUPES OK`.
  - `pairs > "$LOG/<TASK>-pairs.txt"`: TITLE count plus only the enumerated non-title rows. A message built from concatenated pieces can show one row per changed piece, a line or two from the enumerated line number; that still counts as the enumerated literal.
  - `scan > "$LOG/<TASK>-scan-after.txt"`: every surviving row is justified under R3, R6 or R7.
  - Tests and tsc equal P1.
  - `git -C "$REPO" diff --name-only "$(cat "$LOG/<TASK>-base.txt")" -- packages/spr/model.ts packages/spr/evaluate.ts packages/spr/data.ts packages/spr/cli.ts packages/core/algorithms/spr.ts` prints nothing.
- **P6 Report** (returned as text, never a file):
  - commits (short sha and subject)
  - scan hits before and after, per file and summed
  - pairs summary, with every non-title row listed
  - surviving scan rows and the rule that keeps each
  - baseline and final test and tsc counts
  - titles reworded under R2/R4 whose meaning a reviewer should confirm
  - foreign files, bugs noticed (not fixed), and any stopped-at list (O10)
</shared_procedure>

<tasks>

<task type="tracer">
  <name>W1-1 (wave 1): L3 + L4 + L2 outside scripts — rankingPoints notes, doc rename, runtime error/log strings</name>
  <files>packages/core/rankingPoints/2018.ts, packages/core/rankingPoints/2019.ts, packages/core/rankingPoints/2023.ts, packages/core/rankingPoints/2024.ts, packages/core/rankingPoints/2025.ts, packages/core/rankingPoints/analyticPmf.ts, packages/core/algorithms/opr.ts, packages/corpus/db.ts, packages/ingest/rankings.ts, packages/spr/sealedPaths.ts, packages/harness/publish.ts, packages/harness/manifestSchemas.ts, packages/harness/pageArtifacts.ts, packages/harness/score.ts, packages/harness/stateSnapshot.ts, apps/worker/src/stateProbe.ts, docs/models/sigma1-rp-verification.md -> docs/models/rp-verification.md</files>
  <read_first>
    - `$LISTS/W1-1-scan-planning.tsv`
    - docs/models/sigma1-rp-verification.md lines 1-12, 77-90, 185-195
  </read_first>
  <action>
<!-- planner-discipline-allow: rp:conservative-branch -->
<!-- planner-discipline-allow: rpConservativeBranch -->
<!-- planner-discipline-allow: sigma1-rp-verification -->
P0-P6. The tracer is step 1 below: the rename, committed alone, then proven by `git show --stat -M HEAD` before any note is touched.

**1. L4 decision: rename.** The retired name in the filename is the recorded leftover. Its only live referrers are the three notes. The doc's dated history stays as written; only its identity and two dead pointers change. The 54 `.planning/` mentions are historical records, and `git log --follow` keeps the trail.
- `git -C "$REPO" mv docs/models/sigma1-rp-verification.md docs/models/rp-verification.md` FIRST, then Edit the new path.
- Change the H1 from `# Sigma1 RP verification (SC-4)` to `# RP threshold verification (SC-4)`.
- Lines 9-10: replace the backticked `pnpm rp:conservative-branch` (`packages/harness/rpConservativeBranch.ts`) pointer with "a conservative-branch measurement script (since deleted; the Conservative-Branch Understatement section names the commit to restore it from)". Keep the rest of the sentence.
- Lines 193-195 footer: replace the dead command, report path and "regenerate to reproduce" with the same since-deleted wording.
- Change nothing else in the doc: body history, the banner, SC-4 and plan references stay.
- Then `git -C "$REPO" add docs/models/rp-verification.md`. Commit subject `docs(260914-j0n): rename sigma1-rp-verification.md to rp-verification.md` with both paths. Confirm with `git show --stat -M HEAD` (a rename plus a small change) and `git status --short -- docs/models`.

**2. L3 notes.** In 2023.ts, 2024.ts and 2025.ts, `untrackedGate.note` becomes "... A pooled-season measurement recorded a meanRpUnderstatement of X RP per alliance-match (understatedRate Y%) — see docs/models/rp-verification.md's Conservative-Branch Understatement section." The figures stay exactly as they are.
- 2018.ts:91 becomes "A pooled-season measurement recorded this as an overstatedRate of 0.2225%, the only nonzero overstatedRate anywhere in the RP layer." No doc pointer: no doc records 2018.
- 2019.ts:69 `reason`:
  - Drop the leading `F12: `.
  - The parenthetical becomes "(a pooled-season measurement recorded an understatedRate of 4.7324%)".
  - The final sentence ("Deferred out of Phase 9 deliberately — see 09-CONTEXT.md's deferred list.") becomes "Left constant on purpose."
- One commit: `refactor(260914-j0n): drop the dead command and planning ids from rankingPoints notes`.

**3. L2 strings (enumerated; replace ids per R1/R2):**
- analyticPmf.ts:248: drop ` (F4 boundary)`.
- analyticPmf.ts:453: `F4 (dependence between distinct threshold variables) is deliberately out of scope` becomes `dependence between distinct threshold variables is deliberately out of scope`.
- opr.ts:346: drop ` (01-REVIEW WR-01, D-03)`.
- corpus/db.ts:236: drop ` (D-03, 01-REVIEW WR-06)`.
- ingest/rankings.ts:122: drop ` (D-18.6)`.
- spr/sealedPaths.ts:73: drop ` (260908-vqr F-15)`.
- publish.ts:1293: drop ` (PD-06)`.
- publish.ts:1315: `(PD-04 — the cold-start season's first event)` becomes `(the cold-start season's first event)`.
- manifestSchemas.ts:124 and pageArtifacts.ts:113: `does not carry D-13's` becomes `does not carry the`. Trim `D-13's` the same way in the adjacent JSDoc (manifestSchemas.ts:120, and pageArtifacts.ts's matching class doc if it repeats D-13).
- score.ts:342: drop ` (D-07)`.
- stateSnapshot.ts:99: `(per-team data stored inside the league row, pre plan-04-08)` becomes `(per-team data stored inside the league row)`.
- stateSnapshot.ts:744: drop ` (D-13)`.
- apps/worker/src/stateProbe.ts:654:
  - `plan 09-08's four additions` becomes `the ranking-point additions`.
  - `because they predate Phase 9` becomes `because they are not part of the ranking-point path`.
  - Keep `rp=0` and `ABLATED ARM`.
- Commit subject `refactor(260914-j0n): drop planning ids from runtime error and log strings`.

Guard these 16 TS files with `--files "$LISTS/W1-1-files.txt"`.

Tests (repo root, Bash timeout 600000): `npx vitest run --project node packages/core/rankingPoints packages/core/isomorphic.test.ts packages/core/algorithms/opr.test.ts packages/corpus packages/ingest/rankings.test.ts packages/spr packages/harness/browserSafeSchemas.test.ts packages/harness/manifests.test.ts packages/harness/pageArtifacts.test.ts packages/harness/score.test.ts packages/harness/stateSnapshot.test.ts packages/harness/publish.tracer.test.ts packages/harness/rpSeed.test.ts packages/harness/sigmaSeed.test.ts apps/worker/test/stateProbe.test.ts`. Typechecks: `npx tsc --noEmit` and `npx tsc --noEmit -p apps/worker/tsconfig.json`.
  </action>
  <verify>
    <automated>
From $REPO:
1. `git grep -n -e rp:conservative-branch -e rpConservativeBranch -e sigma1-rp-verification -- ':!.planning'` prints nothing.
2. `test -f docs/models/rp-verification.md && test ! -e docs/models/sigma1-rp-verification.md && echo ok` prints ok.
3. `git log --follow --oneline -- docs/models/rp-verification.md | wc -l` is greater than 1.
4. `git diff --stat "$(cat "$LOG/W1-1-base.txt")" -M -- docs/models` shows one rename with under 15 changed lines.
5. `node "$STR" guard --base "$(cat "$LOG/W1-1-base.txt")" --files "$LISTS/W1-1-files.txt"` ends `STRINGS GUARD OK`.
6. `node "$STR" pairs ...` lists exactly the 5 notes/reason (PROP) plus the 14 enumerated ERROR/LOG/ARG rows, and nothing else.
7. `node "$STR" scan --files "$LISTS/W1-1-files.txt"` ends `hits=0`.
8. The test command and both tsc counts equal P1.
    </automated>
  </verify>
  <done>
    - Three commits (doc rename, notes, runtime strings); no dead command or retired doc name outside `.planning/`
    - Guard OK, scan hits 0 over the 16 TS files, tests and tsc match P1
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W1-2 (wave 1, parallel-safe): L5 publish.test.ts fixtures on spr + L6 stateSnapshot.test.ts banner</name>
  <files>packages/harness/publish.test.ts, packages/harness/stateSnapshot.test.ts</files>
  <read_first>
    - packages/harness/publish.test.ts lines 150-172, 645-680, 720-790, 1700-1720, 1995-2010, 2095-2120, 2160-2175
    - packages/harness/stateSnapshot.test.ts lines 300-375
  </read_first>
  <action>
<!-- planner-discipline-allow: vprArtifact -->
<!-- planner-discipline-allow: vprRow -->
<!-- planner-discipline-allow: "vpr" -->
P0-P6, where step 1 is the tracer.

**1. L5.** Set `algorithmId: "vpr"` to `algorithmId: "spr"` at lines 162 (`eventArtifactParams`), 659, 731, 764, 2004 (`baseParams`), 2111 and 2170 (`historyRow`).
- Verified at planning time: the builders only copy `algorithmId` into the artifact, and no assertion in these tests reads it back or builds a key from it. The oprArtifact-vs-second-artifact bonus-RP equality at 2109-2119 does not depend on the id.
- Rename the locals so no retired id survives:
  - lines 2109-2119: `vprArtifact` becomes `sprArtifact`, `vprRow` becomes `sprRow`
  - lines 1703-1718 (baseParams there is `opr`; the fixture is the variance-carrying prediction): `vprArtifact` becomes `varianceArtifact`, `vprRow` becomes `varianceRow`
- Leave titles alone (W2-1 owns them).
- Guard and pairs flags: `--rename vprArtifact=sprArtifact --rename vprRow=sprRow --rename vprArtifact=varianceArtifact --rename vprRow=varianceRow`.
- Commit `refactor(260914-j0n): use the live spr id in publish.test.ts fixtures`.

**2. L6 (comment-only).**
- The banner at lines 306-308 (`// Missing league row`) sits above the unknown-passenger-key block. Replace its middle line with `// Unknown passenger keys and algorithm ids`; that banner then covers the two blocks at 310 and 345.
- Insert the same three-line banner shape with `// Missing league row` plus one blank line directly above `describe("deserializeState — missing league row"`.
- Match the surrounding banner width exactly.
- Commit `test(260914-j0n): put the missing-league-row banner above its own block`.

Tests (repo root, Bash timeout 600000): `npx vitest run --project node packages/harness/publish.test.ts packages/harness/stateSnapshot.test.ts`. Typecheck: `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>
From $REPO:
1. `grep -c '"vpr"' packages/harness/publish.test.ts` prints 0.
2. `grep -cE '\bvpr[A-Z]' packages/harness/publish.test.ts` prints 0.
3. `node "$STR" guard --base "$(cat "$LOG/W1-2-base.txt")" --files "$LISTS/W1-2-files.txt" --rename vprArtifact=sprArtifact --rename vprRow=sprRow --rename vprArtifact=varianceArtifact --rename vprRow=varianceRow` ends `STRINGS GUARD OK`.
4. `pairs` with the same flags shows exactly 7 `PROP:algorithmId` rows (`"vpr"` to `"spr"`) and nothing for stateSnapshot.test.ts.
5. `grep -n -B3 'describe("deserializeState — missing league row"' packages/harness/stateSnapshot.test.ts` shows the `// Missing league row` banner.
6. Test and tsc counts equal P1.
    </automated>
  </verify>
  <done>
    - Two commits; no retired id or vpr-prefixed identifier left in publish.test.ts; banner above its own block
    - Guard OK with only the listed renames; publish.test.ts and stateSnapshot.test.ts pass counts unchanged
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W1-3 (wave 1, parallel-safe): L2 in the measurement scripts and field-averaged-presim.md</name>
  <files>scripts/measureFieldAveragedRanks.ts, docs/models/field-averaged-presim.md, scripts/verifySubsetPublish.ts, scripts/measureAwardPredictability.ts, scripts/measureMatchBandCoverage.ts, scripts/measureRpCalibration.ts, scripts/replayRig.ts</files>
  <read_first>
    - `$LISTS/W1-3-scan-planning.tsv`
    - scripts/verifySubsetPublish.ts lines 60-75, 150-520, 540-600
    - 53h-PLAN `## Known source readers`, measureRpCalibration.ts bullet
  </read_first>
  <action>
<!-- planner-discipline-allow: A-FA1 -->
<!-- planner-discipline-allow: A-FA3 -->
<!-- planner-discipline-allow: PD-04 -->
<!-- planner-discipline-allow: PD-05 -->
<!-- planner-discipline-allow: D-16 -->
<!-- planner-discipline-allow: D-17 -->
<!-- planner-discipline-allow: PLAN-OUTLINE -->
P0-P6. The tracer is measureFieldAveragedRanks.ts together with its doc.

**1. measureFieldAveragedRanks.ts + docs/models/field-averaged-presim.md** (one commit: `refactor(260914-j0n): drop planning ids from the field-averaged measurement and its doc`).
- Script strings:
  - 632: `assumption A-FA3` becomes `replay-mode assumption`.
  - 686: `(PD-04 — the cold-start season's first event)` becomes `(the cold-start season's first event)`.
  - 973: `A-FA1 residual` becomes `Additivity residual`.
  - 979: `A-FA1:` becomes `Additivity assumption:`.
  - 1043: drop `D-16/D-17 `.
  - 1356: the sentence becomes "Encoded as named constants in the measurement script before the run. No threshold was changed after the run."
  - 1437: becomes `## The additivity assumption — its residual, measured`.
  - 1487: `the one D-16 names and accepts` becomes `the one this pre-schedule design names and accepts`.
  - 1490: `**Assumption A-FA1 (additivity)**` becomes `**The additivity assumption**`.
  - 1493: `**Assumption A-FA3 (replay mode).**` becomes `**The replay-mode assumption.**`.
  - Keep `rung 1` (R6).
- Doc: make the identical replacements on lines 277, 303-308 (table cells), 325 and 358-360, by hand; never regenerate (that needs the corpus and a long run).
- Doc proof:
  - `git diff -U0 BASE -- docs/models/field-averaged-presim.md` touches only those lines.
  - `grep -nE "A-FA|PLAN-OUTLINE|D-16|D-17|PD-04" docs/models/field-averaged-presim.md` prints nothing.
  - Every new script literal's text appears in the doc (`grep -cF`), where renderDoc emits it.

**2. verifySubsetPublish.ts** (commit `refactor(260914-j0n): drop planning ids and history from verify:subset notes`).
- 168: `— PD-05 requires one, since` becomes `— an absence entry needs one, since`.
- Every `note:` in PUBLISHED_SUBSET and PUBLISHED_TEAM_SUBSET is the reason an entry is in the subset. Rewrite each so it says, in present tense, what shape the entry exercises and the corpus or live measurement behind its expectations:
  - Apply R1 to the ids, including UI-SPEC state ids E1-E5 and `RESEARCH.md Question 2`.
  - Apply R3 to `NN-NN:` prefixes, dated `HISTORY:` chains, the commit hashes inside those chains, and `WINDOWS.md ledger #13`.
  - Keep corpus counts, event types, `isRpEligibleEventType` reasoning, and current live measurements with their generation hash (for example `0 of 72 played qm rows live-measured at generation 2dcc057f`).
  - Replace `D-08`, `D-12`, `D-13`, `D-16`, `D-17`, `PD-08` and `D-01/D-02` with the shape they name when the note shows it: no-ranking fallback, summed-RP fallback, quals merge, incomplete-sum rule, empty-alliances case, spread comparison. Otherwise drop them.
- Re-scan with `grep -nE "\bE[1-5]\b|UI-SPEC|HISTORY|RESEARCH" scripts/verifySubsetPublish.ts`; the expected result is empty.
- Types and `expect*` fields are untouched.

**3. The other four scripts** (one commit `refactor(260914-j0n): drop quick-task and finding ids from measurement script output`):
- measureAwardPredictability.ts lines 4073, 4118, 4234, 4283, 4306, 4319: replace each quick-task id with what that run measured, as the surrounding printed text states. Examples: `260912-7bp's artifact` becomes `the near-bottom-ranker artifact`; `unchanged from 260912-5n8` becomes `unchanged`. Line 1965's date and the `BPR` feature labels stay (R3 data, R7).
- measureMatchBandCoverage.ts:257: `the band before 260913-g66` becomes `the band before the roster-size correction`.
- measureRpCalibration.ts:
  - 1086: drop ` (quick task 260912-2uz)`.
  - 1414: `F6 GAP` becomes `PMF-VS-PREDWIN GAP`, matching the comment at 1404.
  - Keep line 120's dates (R3 evidence).
  - Never add the SOURCE-pinned tokens.
- replayRig.ts:73: drop ` (D-20)`. MEASUREMENT_GAP_NOTE's test imports the constant, so it stays consistent.

Guard over `$LISTS/W1-3-files.txt` (the .md prints SKIP).

Tests (repo root, Bash timeout 600000): `npx vitest run --project node scripts/measureFieldAveragedRanks.test.ts scripts/measureRpCalibration.test.ts scripts/measureAwardPredictability.test.ts scripts/measureMatchBandCoverage.test.ts scripts/replayRig.test.ts scripts/pruneR2Generations.test.ts scripts/publishAlgorithmsManifest.test.ts scripts/secrets-boundary.test.ts`. Typecheck: `npx tsc --noEmit`. Never run verify:subset or any script (network).
  </action>
  <verify>
    <automated>
From $REPO:
1. `node "$STR" guard --base "$(cat "$LOG/W1-3-base.txt")" --files "$LISTS/W1-3-files.txt"` ends `STRINGS GUARD OK`.
2. `pairs` shows only PROP:note, ERROR, LOG and `ARG:push` rows in the six scripts, and zero TITLE rows.
3. `node "$STR" scan --files "$LISTS/W1-3-files.txt"` leaves only R3/R6 data rows:
   - measureAwardPredictability.ts:1965's date
   - measureRpCalibration.ts:120's refusal dates
   - replayRig.ts:72's live-event dates
   - the doc's line 5 hash fragment
4. `grep -nE "A-FA|PLAN-OUTLINE|D-1[67]|PD-0" scripts/measureFieldAveragedRanks.ts docs/models/field-averaged-presim.md` prints nothing.
5. `git diff "$(cat "$LOG/W1-3-base.txt")" -- docs/models/field-averaged-presim.md | grep -c '^[-+]```'` prints 0 (json fence untouched).
6. Test and tsc counts equal P1.
    </automated>
  </verify>
  <done>
    - Three commits; renderDoc and the committed doc agree; no planning id in any enumerated script string
    - Guard OK, SOURCE-reading measureRpCalibration test unchanged, tsc matches P1
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W2-1 (wave 2): L1 titles — publish.test.ts and small harness suites (144 titles)</name>
  <files>packages/harness/publish.test.ts (100 titles + 2 expect messages), packages/harness/score.test.ts (12), packages/harness/percentiles.test.ts (9), packages/harness/replay.test.ts (9), packages/harness/preSchedule.test.ts (7), packages/harness/teamRanks.test.ts (4), packages/harness/rounding.test.ts (3)</files>
  <read_first>
    - `$LISTS/W2-1-scan-planning.tsv` (TITLE and EXPECT_MSG rows; PROP/ARG date rows are fixture data)
  </read_first>
  <action>
P0-P6 with R1-R9. The tracer is publish.test.ts.
- Commit publish.test.ts in about three commits of 35 titles each, in file order; commit the other six files together.
- Enumerated non-title literals: publish.test.ts:4408-4409 EXPECT_MSG (`(09-10 Task 1)` and `Claude's Discretion under 09-CONTEXT.md`). Keep its meaning: the explicit value is a recorded decision, so an explicit year is deliberate while a deletion is not.
- Line 1702's title `D-01: rounds a Sigma1-shaped prediction's own-variance fields ...`: drop `D-01: ` and change `Sigma1-shaped` to `variance-carrying` (the fixture at 1703-1714 shows the own-variance fields). This is R7's history-clause case.
- `Test N` labels stay (R6), e.g. `Test 8 (T-07-08-01):` becomes `Test 8:`.
- Tests: from the repo root, Bash timeout 600000, run `npx vitest run --project node $(tr '\n' ' ' < "$LISTS/W2-1-files.txt")`; per commit, run only the committed files. Root tsc at P1 and P5.
  </action>
  <verify>
    <automated>
1. `node "$STR" guard --base "$(cat "$LOG/W2-1-base.txt")" --files "$LISTS/W2-1-files.txt"` ends `STRINGS GUARD OK`.
2. `dupes` ends `DUPES OK`.
3. `pairs` shows only TITLE rows plus publish.test.ts:4408-4409 EXPECT_MSG.
4. `node "$STR" scan --files "$LISTS/W2-1-files.txt" | awk -F'\t' '$3=="TITLE"' | wc -l` prints 0.
5. The vitest `Tests` passed count and root tsc error count equal P1.
    </automated>
  </verify>
  <done>
    - 144 title hits resolved, 0 TITLE rows in scan, guard and dupes OK
    - Test counts unchanged
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W2-2 (wave 2): L1 titles — remaining harness suites and scripts tests (119 titles + 2 date-only)</name>
  <files>packages/harness/pageArtifacts.test.ts (42), packages/harness/stateSnapshot.test.ts (14), packages/harness/browserSafeSchemas.test.ts (7), packages/harness/payloadBudget.test.ts (4 +1 msg), packages/harness/level1Digest.test.ts (3 +1 msg), packages/harness/manifests.test.ts (3 +1 date), packages/harness/sigmaScoutLayer.matchBand.test.ts (3), packages/harness/sigmaScoutLayer.outcomeArms.test.ts (3), packages/harness/sigmaScoutLayer.meanShift.test.ts (2), packages/harness/replay.multiAlgorithm.test.ts (2), packages/harness/rpSeed.test.ts (2), packages/harness/sigmaScoutLayer.bandGuard.test.ts (1), packages/harness/sigmaScoutLayer.lattice.test.ts (1), packages/harness/sigmaScoutLayer.sigmaFor.test.ts (1), packages/harness/sigmaSeed.test.ts (1), packages/harness/sigmaScore.test.ts (1), packages/harness/seasonBoundary.test.ts (1), packages/harness/eventRank.tracer.test.ts (1), packages/harness/publish.tracer.test.ts (1), packages/harness/replay.carryStates.test.ts (1), scripts/measureRpCalibration.test.ts (17), scripts/secrets-boundary.test.ts (2), scripts/replayRig.test.ts (2), scripts/reviewFrontmatterLint.test.ts (2 titles; fixtures stay), scripts/publishAlgorithmsManifest.test.ts (1), scripts/publishDistricts.test.ts (1), scripts/measureAwardPredictability.test.ts (0 +1 date, kept: asserted)</files>
  <read_first>
    - `$LISTS/W2-2-scan-planning.tsv`
  </read_first>
  <action>
P0-P6 with R1-R9. The tracer is pageArtifacts.test.ts.

Enumerated non-title literals:
- level1Digest.test.ts:318 EXPECT_MSG: `frozen by 09-01 at commit 47df877d. Phase 9's RP layer ...` becomes `frozen at commit 47df877d. The RP layer ...`.
- payloadBudget.test.ts:169 EXPECT_MSG: `D-03/D-12's republish breached` becomes `the republish breached`.

Also:
- manifests.test.ts:224 `(2026-08-29 outage, cause A)`: keep the incident identity without the date, e.g. `(the live-windows outage's cause A)`, only if the test body shows it is about live windows; otherwise leave it (R3/R4).
- reviewFrontmatterLint.test.ts fixture strings are review-file data and stay (R6); its two titles lose `Task 2`.
- measureRpCalibration.test.ts reads measureRpCalibration.ts, not its own titles; W1-3 already landed.

Tests (repo root, Bash timeout 600000): `npx vitest run --project node $(tr '\n' ' ' < "$LISTS/W2-2-files.txt")`; per commit, only the committed files. Root tsc at P1 and P5.
  </action>
  <verify>
    <automated>
1. Guard over `$LISTS/W2-2-files.txt` against W2-2's BASE ends `STRINGS GUARD OK`.
2. `dupes` ends `DUPES OK`.
3. `pairs` shows TITLE rows plus only level1Digest.test.ts:318 and payloadBudget.test.ts:169 EXPECT_MSG.
4. `scan | awk -F'\t' '$3=="TITLE"'` shows only rows the report justifies under R3/R6 (expected: none, or manifests.test.ts:224 if left).
5. Test counts and root tsc equal P1.
    </automated>
  </verify>
  <done>
    - 119 title hits resolved plus the two messages; guard and dupes OK
    - Test counts unchanged
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W2-3 (wave 2): L1 titles — core, spr, corpus, ingest and Worker tests (149 titles + 1 date-only)</name>
  <files>packages/core/algorithms/epa.test.ts (18), packages/core/algorithms/opr.test.ts (13), packages/core/rankingPoints/analyticPmf.test.ts (10; line 297 is a formula, keep), packages/core/rankingPoints/marginals.test.ts (7), packages/core/algorithms/breakdown/reconciliation.test.ts (6), packages/core/rankingPoints/rules.test.ts (6), packages/core/algorithms/breakdown/breakdown.test.ts (4), packages/core/rankingPoints/reconciliation.test.ts (3), packages/core/rankingPoints/analyticPmf.universal.test.ts (2), packages/core/scoring/brier.test.ts (2), packages/core/algorithms/carryover.test.ts (1), packages/core/algorithms/epaCarryScale.test.ts (1), packages/core/algorithms/simulation/rankSimulation.test.ts (1), packages/core/rankingPoints/analyticPmf.seasons.test.ts (1), packages/core/rankingPoints/fieldAveraged.test.ts (1), packages/core/scoring/predictionValidity.test.ts (1), packages/core/algorithms/epaWeekOne.test.ts (0 +1 date, kept: fixture data), packages/spr/evaluate.test.ts (2), packages/spr/holdout.test.ts (2), packages/corpus/db.test.ts (12), packages/corpus/integrity.test.ts (6 +1 log), packages/corpus/districts.test.ts (1), packages/ingest/rankings.test.ts (6), packages/ingest/corpusCensus.test.ts (5), packages/ingest/awardsAll.test.ts (4), packages/ingest/normalize.test.ts (3), packages/ingest/rankingsLive.test.ts (3), packages/ingest/alliances.test.ts (2), packages/ingest/tbaClient.test.ts (2), packages/ingest/eventTeams.test.ts (1), apps/worker/test/scheduled.test.ts (9), apps/worker/test/scheduled.rp.test.ts (5 +2 msgs), apps/worker/test/scheduled.officialRecord.test.ts (4), apps/worker/test/liveAlgorithmTier.test.ts (1 +1 msg), apps/worker/test/eventScopedSelections.test.ts (1), apps/worker/test/scheduled.replay.test.ts (1), apps/worker/test/subrequestBudget.test.ts (1), apps/worker/test/tbaPoll.test.ts (1)</files>
  <read_first>
    - `$LISTS/W2-3-scan-planning.tsv`
  </read_first>
  <action>
P0-P6 with R1-R9. The tracer is epa.test.ts.

Enumerated non-title literals:
- corpus/integrity.test.ts:329 LOG: `corpus migration (plan 07-02 Task 3):` becomes `corpus migration:`.
- apps/worker/test/liveAlgorithmTier.test.ts:412 EXPECT_MSG: cite `the "Worker runtime budget" section` of docs/publish-budget.md (R8).
- apps/worker/test/scheduled.rp.test.ts:583 EXPECT_MSG: `09-07's decomposition` becomes `the decomposition`.
- apps/worker/test/scheduled.rp.test.ts:604 EXPECT_MSG: `WIN+TIE shipped 2026-09-13` becomes `WIN+TIE ships`; keep the baselines path (R3).

Title notes:
- liveAlgorithmTier.test.ts titles naming vpr/bpr/sigma1 as rejected ids stay (R7). `(quick task 260908-5wd)` goes.
- analyticPmf.universal.test.ts:106: drop `(D-07), carried forward from the deleted sigma1/rp/distribution.test.ts` entirely, which leaves `pmfMean — hand-computed`.
- `Pitfall N` references: drop them, keeping any behaviour words (`the fallback ladder — ordered, documented, and never NaN`).

Tests (repo root, Bash timeout 600000): `npx vitest run --project node packages/core packages/spr packages/corpus packages/ingest apps/worker`. Typechecks: `npx tsc --noEmit` and `npx tsc --noEmit -p apps/worker/tsconfig.json`.
  </action>
  <verify>
    <automated>
1. Guard over `$LISTS/W2-3-files.txt` ends `STRINGS GUARD OK`.
2. `dupes` ends `DUPES OK`.
3. `pairs` shows TITLE rows plus only the four enumerated non-title rows.
4. `scan | awk -F'\t' '$3=="TITLE"'` leaves only analyticPmf.test.ts:297.
5. The vitest counts, root tsc and worker tsc equal P1.
    </automated>
  </verify>
  <done>
    - 149 title hits resolved plus four messages; guard and dupes OK
    - Suite counts and both tsc counts unchanged
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W2-4 (wave 2): L1 titles — web event components, simulation and event route tests (143 titles + 15 date-only)</name>
  <files>apps/web/src/components/event/AlliancesTab.test.tsx (26 +2d), apps/web/src/components/event/SimulationTab.test.tsx (26 +1d), apps/web/src/components/event/InsightsTab.test.tsx (14 +2d), apps/web/src/components/event/BreakdownTab.test.tsx (12 +2d), apps/web/src/lib/simulationInputs.test.ts (11 +1d), apps/web/src/routes/event.$eventKey.test.tsx (11), apps/web/src/components/event/ElimsTab.test.tsx (6), apps/web/src/workers/simulationProtocol.test.ts (6), apps/web/src/components/event/EventHeader.test.tsx (5), apps/web/src/lib/searchParams.test.ts (5), apps/web/src/components/event/StartMatchPicker.test.tsx (4 +3d), apps/web/src/components/event/EventMatchTable.test.tsx (3 +1d), apps/web/src/lib/metricKeys.test.ts (3 +1d), apps/web/src/components/event/QualsTab.test.tsx (2), apps/web/src/components/event/eventMatchAxis.test.ts (2), apps/web/src/lib/simAxis.test.ts (2 +1d), apps/web/src/test/mockWorker.test.ts (2), apps/web/src/components/event/SimulationTab.failure.test.tsx (1), apps/web/src/components/event/rankRows.test.ts (1), apps/web/src/lib/simQuantile.test.ts (1), apps/web/src/components/event/RankDistributionTable.test.tsx (0 +1d)</files>
  <read_first>
    - `$LISTS/W2-4-scan-planning.tsv`
  </read_first>
  <action>
P0-P6 with R1-R9. The tracer is SimulationTab.test.tsx.
- The 15 date-only titles are change stamps such as `(2026-09-13)`, `(2026-09-01: blank, never an em-dash placeholder)` and `(user decision 2026-09-05)`. Drop the date and keep any behaviour words (R3).
- `(07-UAT.md G-8)` and `T-07-15-01/02` go (R1).
- SimulationTab.test.tsx `08-11: default selection`-style describe titles: if dropping the prefix leaves a vague title, name the layout-stack behaviour the block's tests assert (R4, R5).
- No enumerated non-title literals; pairs must show TITLE rows only.

Tests (from apps/web, Bash timeout 600000): `npx vitest run $(sed -n 's#^apps/web/##p' "$LISTS/W2-4-files.txt" | tr '\n' ' ')`; per commit, only the committed files. Web tsc from the repo root: `npx tsc --noEmit -p apps/web/tsconfig.json`.
  </action>
  <verify>
    <automated>
1. Guard over `$LISTS/W2-4-files.txt` ends `STRINGS GUARD OK`.
2. `dupes` ends `DUPES OK`.
3. The `pairs` summary shows TITLE only.
4. `scan | awk -F'\t' '$3=="TITLE"' | wc -l` prints 0.
5. The web vitest `Tests` count and web tsc error count equal P1.
    </automated>
  </verify>
  <done>
    - 158 title hits resolved; guard and dupes OK
    - Web test and tsc counts unchanged
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W2-5 (wave 2): L1 titles — remaining web tests and e2e specs, plus e2e log/assertion strings (147 titles + 7 date-only + 24 messages)</name>
  <files>apps/web/src/components/team/SeasonHeader.test.tsx (12), apps/web/src/components/team/MatchTable.test.tsx (9 +1d), apps/web/src/components/compare/AccuracyTable.test.tsx (8), apps/web/src/routes/methodology.compare.test.tsx (8 +1 error), apps/web/src/components/compare/MethodologyNote.test.tsx (7), apps/web/src/components/team/EventSection.test.tsx (7 +1d), apps/web/src/components/team/RankCards.test.tsx (7), apps/web/src/components/MetricValue.test.tsx (5 +1d), apps/web/src/components/team/MetricHistoryChart.test.tsx (5), apps/web/src/components/teams-table/columns.test.tsx (5), apps/web/src/routes/team.$teamNumber.test.tsx (4), apps/web/src/components/StateViews.test.tsx (3), apps/web/src/components/compare/DataCoverageTable.test.tsx (3), apps/web/src/components/ribbon/Ribbon.test.tsx (3), apps/web/src/components/team/metricHistorySeries.test.ts (3 +1d), apps/web/src/components/teams-table/groupedView.test.tsx (3), apps/web/src/components/ui/tabs.test.tsx (3), apps/web/src/routes/match.$matchKey.test.tsx (3), apps/web/src/components/districts/DistrictLocksTab.test.tsx (2), apps/web/src/components/ribbon/YearSelect.test.tsx (2), apps/web/src/components/teams-table/rowModel.test.ts (2), apps/web/src/lib/allianceTierApproximation.test.ts (2), apps/web/src/lib/metricGroups.test.ts (2), apps/web/src/routes/__root.test.tsx (2), apps/web/src/routes/teams.test.tsx (2), apps/web/src/components/compare/CompLevelSwitcher.test.tsx (1), apps/web/src/components/compare/comparePalette.test.ts (1), apps/web/src/components/compare/coverageRows.test.ts (1), apps/web/src/components/events-list/EventFilters.test.tsx (1), apps/web/src/components/events-list/filterModel.test.ts (1), apps/web/src/components/ribbon/AlgorithmSelect.test.tsx (1), apps/web/src/components/search/SearchBox.test.tsx (1), apps/web/src/components/team/BonusRpDots.test.tsx (1), apps/web/src/components/team/MetricHistoryTab.test.tsx (1), apps/web/src/components/team/TeamStates.test.tsx (1), apps/web/src/components/team/matchAxis.test.ts (1), apps/web/src/components/teams-table/TeamsBubbleChart.test.tsx (1), apps/web/src/components/teams-table/TeamsTable.test.tsx (1), apps/web/src/components/teams-table/teamsBubbleModel.test.ts (1), apps/web/src/favicon.test.ts (1), apps/web/src/lib/bonusRp.test.ts (1), apps/web/src/lib/resolveSortKey.test.ts (1), apps/web/src/lib/search-index.test.ts (1), apps/web/src/styles/theme.scrollbar.test.ts (1), apps/web/src/lib/api/compare.test.ts (0 +1d), apps/web/e2e/simulation-tab.spec.ts (1 +7), apps/web/e2e/event-live-artifact.spec.ts (2 +3), apps/web/e2e/table-layout-quality.spec.ts (3 +3), apps/web/e2e/zebra-stripe-full-row.spec.ts (3 +1), apps/web/e2e/breakdown-desktop-overflow.spec.ts (1 +2), apps/web/e2e/touch-action-vertical-scroll.spec.ts (2 +1), apps/web/e2e/event-scroll-regions.spec.ts (0 +2d +3), apps/web/e2e/simulation-run.spec.ts (1 +1), apps/web/e2e/tab-strip-alignment.spec.ts (1 +1), apps/web/e2e/tab-strip-trigger-sizing.spec.ts (1 +1), apps/web/e2e/touch-scroll.spec.ts (1)</files>
  <read_first>
    - `$LISTS/W2-5-scan-planning.tsv`
  </read_first>
  <action>
P0-P6 with R1-R9. The tracer is SeasonHeader.test.tsx.

**Enumerated non-title literals** (R1/R2/R3):
- e2e breakdown-desktop-overflow.spec.ts:71 and :76 EXPECT_MSG: `07-UAT.md G-10's measured post-fix bound` becomes `the measured post-fix bound`; `G-7`/`G-10` references go.
- event-live-artifact.spec.ts:
  - :21 ERROR: `07-16/07-17/07-18 have not all landed` becomes `the prerequisite publish has not landed`.
  - :48 and :65 EXPECT_MSG: `the D-08 fallback/control fixture` becomes `the no-ranking fallback/control fixture`.
- event-scroll-regions.spec.ts:
  - :80 PROP:label: drop `07-12's own routed`.
  - :98 and :138 LOG: `[07-20]` becomes `[event-scroll-regions]`.
- simulation-run.spec.ts:102 LOG: `[SC-2 measurement]` becomes `[simulation-run measurement]`.
- simulation-tab.spec.ts:
  - :63, :108, :113, :158, :163 and :255 LOG: `[08-15] S1`/`[08-15] S3` becomes `[simulation-tab]`.
  - :106 EXPECT_MSG: drop `(PD-06)`.
- tab-strip-alignment.spec.ts:36, tab-strip-trigger-sizing.spec.ts:86, table-layout-quality.spec.ts:188, :224 and :368, touch-action-vertical-scroll.spec.ts:56, zebra-stripe-full-row.spec.ts:124, all EXPECT_MSG: drop `07-UAT.md` and `G-N` references and keep the described defect.
- apps/web/src/routes/methodology.compare.test.tsx:150 ERROR: `D-10 parity test` becomes `parity test`.

**Titles:**
- e2e `G-N — ` prefixes go (R4 where only a template remains).
- `(IN-01, 260902-post-phase08-ungoverned-ui/REVIEW.md)` goes.
- `(unchanged by F10)` becomes `(unchanged by the fill-to-odds dots)` only if the test body shows the fill behaviour; otherwise drop the parenthetical.
- VPR-named fixture titles stay (R7).

**Tests:**
- From apps/web (Bash timeout 600000): `npx vitest run $(sed -n 's#^apps/web/##p' "$LISTS/W2-5-files.txt" | grep -v '^e2e/' | tr '\n' ' ')`; per commit, only the committed files.
- Web tsc from the repo root: `npx tsc --noEmit -p apps/web/tsconfig.json`.
- e2e (no server; never run the specs): from apps/web, `npx playwright test --list > "$LOG/W2-5-e2e-before.txt"` at P1 and `-after.txt` at P5. The last line is identical (`Total: 304 tests in 20 files`), and `diff` shows only the renamed titles, once per Playwright project.
  </action>
  <verify>
    <automated>
1. Guard over `$LISTS/W2-5-files.txt` ends `STRINGS GUARD OK`.
2. `dupes` ends `DUPES OK`.
3. `pairs` shows TITLE rows plus only the 24 enumerated non-title rows.
4. `scan | awk -F'\t' '$3=="TITLE"' | wc -l` prints 0.
5. Web vitest counts and web tsc equal P1.
6. `tail -1 "$LOG/W2-5-e2e-after.txt"` equals `tail -1 "$LOG/W2-5-e2e-before.txt"`.
    </automated>
  </verify>
  <done>
    - 154 title hits and 24 e2e/web messages resolved; guard and dupes OK
    - Web test, tsc and Playwright list counts unchanged
    - Report returned per P6
  </done>
</task>

<task type="auto">
  <name>W3-1 (wave 3, after W2-5; added by the orchestrator): retire the vpr id from e2e specs and the web simulation fixture, plus small stragglers</name>
  <wave>3</wave>
  <files>apps/web/e2e/*.spec.ts, apps/web/e2e/support/*.ts, apps/web/src/components/event/simulationTestFixtures.ts, scripts/measureAwardPredictability.ts, scripts/verifySubsetPublish.ts</files>
  <action>
Why: VPR was retired 2026-09-12 (PUBLISHED_ALGORITHM_IDS is opr/epa/spr; no `vpr` alias exists in apps/web/src/lib). The e2e specs run only against the deployed apex (not in CI), so they went stale: a baseline desktop run against https://sigmascout.org on 2026-09-14 had 11 failed / 26 passed, and 10 of the 11 are vpr-caused (event-live-artifact manifest lookup "manifest carries no vpr entry", deep-link `algorithm=vpr` URLs). W2-5's report lists every occurrence (URLs in ~25 specs and support/simulation.ts, `resolveVprVersion`/`a.id === "vpr"`/`vpr@${version}.json` in event-live-artifact.spec.ts, `["opr","epa","vpr"]` in compare-narrow-legibility.spec.ts, two metric-history-axis-legibility titles, `sigma1@`/vpr comments in event-page, no-page-pan, team-page, simulation-run, metric-history-axis-legibility).

1. Replace the retired premier id with `spr` everywhere in apps/web/e2e: URLs, manifest lookup (rename `resolveVprVersion` to `resolveSprVersion`, locals `vpr` to `spr`), artifact keys, id lists, titles naming vpr, and comments naming vpr/sigma1 artifacts (reword to spr or drop). Keep each spec's intent: where a spec relies on a capability only the premier algorithm has (simulation tab, Sigma/bands, RP), spr is that algorithm now. Where an expectation was tied to VPR-specific data (exact values, column counts that included a spread superscript), do NOT guess new numbers: leave the assertion as-is and list it; the orchestrator runs the suite against production and sends failures back.
2. `apps/web/src/components/event/simulationTestFixtures.ts`: `algorithmId: "vpr"` to `"spr"`; run every web test importing it.
3. `scripts/measureAwardPredictability.ts` ~4442: the printed line "In 5n8 both baselines" carries a shortened quick-task id; reword to "In the previous measurement both baselines" (or equivalent meaning-preserving text) and check its test still passes.
4. `scripts/verifySubsetPublish.ts`: comments at about lines 683, 687, 731, 763, 781, 803, 838, 883, 910, 946, 950, 1097, 1141 still carry D-/08-05/date provenance next to the notes W1-3 cleaned; trim them per the nvn-04 comment policy (comment-only).
5. Also drop the `E1 —`..`E6` UI-SPEC state-label prefixes? NO: keep them (enumeration labels cross-referenced by banner comments).

Verify: `git grep -n -i "vpr\|sigma1" -- apps/web/e2e apps/web/src/components/event/simulationTestFixtures.ts` prints nothing; web tsc 0 errors (`npx tsc --noEmit -p apps/web/tsconfig.json`); `cd apps/web && npx playwright test --list` still 304 tests in 20 files; web vitest for simulationTestFixtures importers green; `npx vitest run --project node scripts/measureAwardPredictability.test.ts scripts/verifySubsetPublish*` green if present; root tsc 0. Do NOT run Playwright against any server (no network in executors). Commit per area: `test(260914-j0n): retire the vpr id from e2e specs`, `test(260914-j0n): use spr in the web simulation fixture`, `refactor(260914-j0n): ...` for the script string, `docs(260914-j0n): trim provenance comments in verifySubsetPublish.ts`.
  </action>
  <done>No vpr/sigma1 left in e2e or the web simulation fixture; typechecks and listed tests green; report lists any VPR-data-specific assertions left for the orchestrator's live run.</done>
</task>

</tasks>

<coverage_audit>
| Source item | Covered by |
|---|---|
| L1 titles: publish.test.ts, measureRpCalibration.test.ts, rankingPoints tests, extended repo-wide by the orchestrator | W2-1..W2-5 (702 title hits plus 25 date-only titles, 149 files) |
| L1 companion: planning ids in non-title test strings (assertion messages, logs, errors) | W2-1 (2), W2-2 (2), W2-3 (4), W2-5 (24) |
| L2 publish.ts PD-06/PD-04, stateProbe.ts rp=0 warning, other package error strings found by the repo-wide scan | W1-1 |
| L2 measureFieldAveragedRanks.ts (renderDoc, banner, errors) plus the committed doc, verifySubsetPublish.ts, and quick-task/finding ids in 4 other scripts | W1-3 |
| L2 rankingPoints/2019.ts `F12:` note | W1-1 |
| L2 e2e simulation-tab.spec.ts:106 `(PD-06)` | W2-5 |
| L3 untrackedGate notes 2023/2024/2025, plus 2018 note and 2019 reason | W1-1 |
| L4 retired doc name and its dead references (lines 9 and 193) | W1-1 (decision: rename) |
| L5 publish.test.ts `algorithmId: "vpr"` fixtures | W1-2 |
| L6 stateSnapshot.test.ts banner | W1-2 |

Deliberate exclusions (not planning ids, or would change behaviour). The orchestrator may raise them with Jacob:
- `Test N` enumeration labels (about 190 titles). They are local numbering that other titles cross-reference, e.g. simulationInputs.test.ts `Test 21 already proved`.
- Retired algorithm ids used as live fixture or route ids outside L5:
  - `apps/web/src/components/event/simulationTestFixtures.ts` (`algorithmId: "vpr"`)
  - `apps/web/e2e/support/simulation.ts` (`?algorithm=vpr`)
  - `apps/web/e2e/metric-history-axis-legibility.spec.ts`
  - `event-live-artifact.spec.ts`'s manifest vpr-entry check
  - about 20 web unit-test titles naming VPR fixtures
  - `level1Digest.test.ts` `opr and bpr reproduce the digests`

  The e2e ones likely fail against the live site (not run in CI; unverified).
- zebra-stripe-full-row.spec.ts still asserts sticky-cell behaviour while several web unit tests pin `no sticky columns`. It may be stale; unverified.
- Data dates: statbotics.ts capture dates, fixture dates, measureRpCalibration.ts:120's refusal dates. Also reviewFrontmatterLint.test.ts fixtures and `docs/models/bpr-spr-identity.md` (an identity record by design).
</coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor edit -> committed source | A prose edit to a string could change a literal the program compares, imports or keys on |
| eight executors plus other sessions -> one git index | Concurrent commits on a shared checkout |
| repo -> transcript | `.env` holds live TBA and R2 credentials |
| sealed files -> published holdout claim | Blob shas of five files attest a published number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j0n-01 | Tampering | Every edited TS file | high | mitigate | `strings.mjs guard` masks only value-position literals and keeps module specifiers, property keys, case labels, comparison operands and types strict. `pairs` lists every changed literal with its KIND against the task's enumeration. Scoped tests and tsc are compared with P1 |
| T-j0n-02 | Tampering | Tests that read source or message text | medium | mitigate | stateProbe `rp=0`/`ABLATED ARM` kept (W1-1); measureRpCalibration SOURCE tokens never added (W1-3); rpSeed/sigmaSeed block untouched; a failure means restoring the text, never editing the test |
| T-j0n-03 | Tampering | renderDoc vs committed field-averaged-presim.md | medium | mitigate | Identical replacements in both; grep proof; json fence diff 0; no regeneration |
| T-j0n-04 | Tampering | Doc rename breaking links | low | mitigate | `git grep` for the old name outside `.planning/` is empty; `git mv` before editing, then `git add` the new path, so the edit is not dropped |
| T-j0n-05 | Tampering | Sealed SPR files | high | mitigate | Guard fails on any sealed diff; P5 diff check |
| T-j0n-06 | Repudiation | Shared git index | medium | mitigate | Disjoint file lists per wave, O4 ownership check, explicit-path commits, one retry on index.lock |
| T-j0n-07 | Information disclosure | `.env` | high | mitigate | No task needs it; no network, no script runs, no publish |
| T-j0n-08 | Repudiation | Loss of planning cross-references in titles | low | accept | Git history keeps every removed id; `.planning/` records are untouched |
</threat_model>

<verification>
Orchestrator, after Wave 1 returns:
- `git log --oneline -20 | grep -c "(260914-j0n)"` prints 8 (W1-1 3, W1-2 2, W1-3 3); a different count must be explained by the reports.
- `git status --short` is clean on all Wave 1 files.
- Then dispatch Wave 2: cap 4, with W2-2 last.

After Wave 2:
- For each task, rerun `node "$STR" guard --base <that task's BASE> --files "$LISTS/<TASK>-files.txt"` and expect `STRINGS GUARD OK`.
- `node "$STR" scan --files` over the five W2 lists concatenated, filtered to TITLE, shows only the rows the reports justified.
- Any stopped-at list (O10) gets a follow-up executor with the same task text restricted to the remaining files.
- From the repo root:
  - `npx vitest run` (Bash timeout 600000) gives 237 files passed, 5,304 tests passed, 1 skipped.
  - `npx tsc --noEmit`, `npx tsc --noEmit -p apps/worker/tsconfig.json` and `npx tsc --noEmit -p apps/web/tsconfig.json` give 0 errors each.
- From apps/web: `npx vite build` ends `built in`, and `npx playwright test --list` ends `Total: 304 tests in 20 files`.
- `git diff --name-only 156a3808 -- packages/spr/model.ts packages/spr/evaluate.ts packages/spr/data.ts packages/spr/cli.ts packages/core/algorithms/spr.ts` prints nothing.
</verification>

<success_criteria>
- Every 53h leftover (L1-L6) is resolved. No planning id remains in a test title or in an enumerated runtime, log or error string. No dead command or retired doc name remains outside `.planning/`. publish.test.ts runs on a live id, and the banner is in place.
- Every change is literal-only or comment-only, proven by the guard, except the doc rename and L5's four listed local renames.
- The full suite, three typechecks, vite build and Playwright list match baseline. Nothing is published, deployed or pushed by executors.
</success_criteria>

<output>
Orchestrator close-out checklist (executors return text only):
1. Write `.planning/quick/260914-j0n-resolve-260914-53h-leftovers-planning-id/260914-j0n-SUMMARY.md` with the Write tool (never a heredoc) from the eight reports:
   - commits and per-task hit counts before and after
   - pairs summaries and justified survivors
   - the L4 rename, test/tsc/build/playwright baselines against final
   - titles reworded under R2/R4, and the deliberate exclusions from `<coverage_audit>`
2. Run the full check in `<verification>`.
3. STATE row:
   - Re-read the STATE.md tail first.
   - Confirm `grep -c "LOCAL PATCH (SigmaScout" ~/.claude/gsd-core/bin/gsd-tools.cjs` prints 1.
   - Run `gsd-tools quick-tasks-append --task "<description, no pipe character>" --dir 260914-j0n-resolve-260914-53h-leftovers-planning-id --commit <close-out sha>`.
4. Commit SUMMARY.md, this PLAN.md and STATE.md with explicit paths: `docs(quick-260914-j0n): summary and state row`, Co-Authored-By trailer. Then `git status --short -- .planning` must not list them.
5. Before pushing, run `git fetch` and `git log --oneline origin/main..main`. Confirm every commit is 53h's, j0n's or a known close-out; a push deploys foreign commits too. Name any foreign commit to Jacob before pushing, then push.
</output>
