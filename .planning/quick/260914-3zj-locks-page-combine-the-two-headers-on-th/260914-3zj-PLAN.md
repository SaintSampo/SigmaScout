---
phase: 260914-3zj-locks-page-combine-the-two-headers-on-th
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/districts/DistrictLocksTab.tsx
  - apps/web/src/components/districts/DistrictLocksTab.test.tsx
autonomous: true
requirements: [QUICK-260914-3zj]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "On the District Locks tab the header is ONE data-card: a single wrapping stat row reading District Championship capacity, Lock Line, Pre-DCMP points remaining, District Points (Available/Total), with the event chips row below that stat row inside the same card"
    - "On the Champ Locks tab the header is ONE data-card with a single stat row reading FIRST Championship capacity, Lock Line, Remaining district points"
    - "Every label text, value format and pre-existing data-testid renders exactly as before, so every pre-existing DistrictLocksTab test passes without edits"
    - "The Champ Locks header shows the same three stats it showed before; only the layout changes (content redesign belongs to sketch 013)"
    - "The caveat paragraph, the per-event column toggle and the table render below the header card exactly as before"
  artifacts:
    - path: "apps/web/src/components/districts/DistrictLocksTab.tsx"
      provides: "LocksHeaderCard: one header card per tab (stat row + district-only chips row)"
      contains: "locks-header-stat-row"
    - path: "apps/web/src/components/districts/DistrictLocksTab.test.tsx"
      provides: "Test asserting each tab renders exactly one header card holding capacity, Lock Line and that tab's own stats"
      contains: "locks-header-stat-row"
  key_links:
    - from: "DistrictLocksTab"
      to: "LocksHeaderCard"
      via: "rendered once, in place of the former capacity card plus the DistrictScheduleStrip / ChampRemainingDistrictPoints card"
      pattern: "<LocksHeaderCard"
    - from: "LocksHeaderCard"
      to: "apps/web/src/components/districts/districtLocksHeaderStats.ts"
      via: "computeDistrictLocksHeaderStats (district tab) / computeChampLocksHeaderStats (champ tab), unchanged"
      pattern: "compute(District|Champ)LocksHeaderStats"
---

<objective>
Merge the two stacked header cards on the Locks page into one card, on both the District Locks and
Champ Locks tabs.

Today `DistrictLocksTab` renders a capacity + Lock Line `data-card`, then a second `data-card`
(`DistrictScheduleStrip` on the district tab, `ChampRemainingDistrictPoints` on the champ tab).
After this plan each tab renders exactly one header card: one wrapping stat row (capacity, Lock
Line, then the tab's own stats), and on the district tab the event chips row sits below that stat
row inside the same card.

Purpose: the user sees two headers where one belongs; this matches the layout in the user's
screenshot of the district tab. Layout only: no label, value, format, or content change.
Output: an edited `DistrictLocksTab.tsx`, plus one new test in `DistrictLocksTab.test.tsx`.

Out of scope, do not touch: the champ "Lock Line is low" bug (fixed in b186f81b; the republish runs
from the main context), `districtLocksHeaderStats.ts`, any pipeline or script, and the champ header
content redesign (pending sketch 013).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@apps/web/src/components/districts/DistrictLocksTab.tsx
@apps/web/src/components/districts/DistrictLocksTab.test.tsx

Environment notes (from established findings and project memory):
- Worktrees are disabled. You run on the main checkout, and other sessions share it. Stage by
  explicit path only (the two files above, nothing else). The working tree already holds unrelated
  dirty files under `.planning/sketches/` and `.planning/quick/260913-pnp-*`. Leave them alone.
- Run tests from the repo root with `npx vitest run <path>`. Judge the result by the printed pass
  and fail counts, never by exit code alone. Do not wrap the run in `timeout ... pnpm`, which
  swallows output and exits 0.
- Typecheck with the web tsconfig, `npx tsc --noEmit -p apps/web/tsconfig.json`. Root tsc misses
  apps/web. `apps/web/src/routeTree.gen.ts` exists on this checkout, so route-tree noise is not
  expected.

Current structure (read once, no need to re-read):
- Lines 159-198, `DistrictScheduleStrip`: its own `data-card flex flex-col` with
  `data-testid="district-locks-header-stats"`, holding a stat row (Pre-DCMP points remaining →
  `district-locks-ceiling`; District Points (Available/Total) → `district-locks-points-pool`) and the
  chip row `district-locks-schedule-strip` of `district-locks-schedule-event` spans.
- Lines 200-215, `ChampRemainingDistrictPoints`: its own `data-card` with
  `data-testid="champ-locks-header-stats"`, holding one stat (Remaining district points →
  `champ-locks-remaining-district-points`).
- Lines 287-297, inside `DistrictLocksTab`: the untested capacity + Lock Line `data-card`, then the
  helper chosen by `which`.
- No test or other file references the two `*-locks-header-stats` testids or the two helper
  component names (grep confirmed). They are free to move.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One header card per tab, proven end-to-end by a render test on both tabs</name>
  <files>apps/web/src/components/districts/DistrictLocksTab.tsx, apps/web/src/components/districts/DistrictLocksTab.test.tsx</files>
  <precondition>`DistrictLocksTab.tsx` still renders the capacity/Lock Line card followed by the `DistrictScheduleStrip` / `ChampRemainingDistrictPoints` helper at the top of the tab's return (a concurrent session may have edited it; check `git log -1 --oneline -- apps/web/src/components/districts/DistrictLocksTab.tsx` shows 1d8a1cb8 or read the return block). If the structure differs, halt and report.</precondition>
  <behavior>
    - Test (district): rendering `which="district"` with a fixture team that has one `eventPoints` entry and one `remainingEvents` entry, so the chip row is non-empty. Inside the tab root (`district-district-locks-tab`), exactly ONE `.data-card` element contains no `table`. That card carries `data-testid="district-locks-header-stats"`. Its `district-locks-header-stat-row` contains the text "District Championship capacity", the text "Lock Line", the `district-locks-ceiling` element and the `district-locks-points-pool` element. The card also contains `district-locks-schedule-strip`, and the stat row does NOT contain that strip (the chips sit below the row, not inside it).
    - Test (champ): rendering `which="champ"`. Inside `district-champ-locks-tab`, exactly ONE `.data-card` contains no `table`. That card carries `data-testid="champ-locks-header-stats"`. Its `champ-locks-header-stat-row` contains "FIRST Championship capacity", "Lock Line" and the `champ-locks-remaining-district-points` element. The card contains no `district-locks-schedule-strip`.
    - Both tests FAIL before the JSX change: today each tab has two non-table cards and no stat-row testid.
    - Every pre-existing test in the file passes unchanged. That includes the capacity/Lock Line units test, the schedule strip test, both ceiling tests, both points-pool tests, both champ remaining-points tests, and the per-event column toggle suite.
  </behavior>
  <action>
    RED first. In `DistrictLocksTab.test.tsx`, add one test per tab, as two `it` blocks or one `it.each` over "district" and "champ", titled so it names quick task 260914-3zj. Implement the behavior bullets. Get the tab root via `await screen.findByTestId(...)`, then collect `root.querySelectorAll(".data-card")` and filter to the cards whose `querySelector("table")` is null. Assert that length is 1, then assert on that card. Use `within` from `@testing-library/react` (add it to the existing import) or `querySelector` on `[data-testid="..."]` to reach the stat row and the stat elements. Reuse the existing `team`, `verdict`, `makeArtifact` and `TestHarness` helpers, and follow the file's `findBy*`-first async convention. Run the file and confirm the new tests fail for the expected reason (two non-table cards / missing stat row).

    GREEN. In `DistrictLocksTab.tsx`:
    1. Replace the two helper components `DistrictScheduleStrip` and `ChampRemainingDistrictPoints` with ONE component, `LocksHeaderCard`. Its props are `artifact: DistrictArtifact`, `which: DistrictLockKind`, `slots: number | null` and `cutLine: number | null`.
    2. Outer element: a single `div` with className `data-card flex flex-col gap-[var(--spacing-md)] p-[var(--spacing-md)]` and `data-testid` set from `which` to `${which}-locks-header-stats`. This yields exactly the two existing testids, `district-locks-header-stats` and `champ-locks-header-stats`, now on the merged card.
    3. First child: the stat row, a `div` with className `flex flex-wrap items-center gap-[var(--spacing-lg)]` and the new additive `data-testid` `${which}-locks-header-stat-row`. Its stat items, in order:
       - capacity: label `{LOCK_KIND_LABEL[which]} capacity`, value `Capacity not published` or `${slots} Teams`, moved verbatim from the current tab body.
       - Lock Line: label `Lock Line`, value em-dash or `${formatPoints(cutLine)} Points`, moved verbatim.
       - district tab only: the Pre-DCMP points remaining item (`district-locks-ceiling`) and the District Points (Available/Total) item (`district-locks-points-pool`), moved verbatim with their current value expressions.
       - champ tab only: the Remaining district points item (`champ-locks-remaining-district-points`), moved verbatim with its current value expression.
       Every stat item keeps the existing `text-role-label text-[var(--color-text-muted)]` span plus `text-role-heading` paragraph shape.
    4. Second child, district tab only: the chip row `div` (`district-locks-schedule-strip`, `district-locks-schedule-event` spans, `data-played`, `event-chip event-chip--week`), moved verbatim. The champ tab renders no second child.
    5. Stats computation: call `computeDistrictLocksHeaderStats(artifact.teams, "district", artifact.year)` only on the district branch, and `computeChampLocksHeaderStats(artifact.teams, artifact.year)` only on the champ branch. Both are plain functions, not hooks, so branching is safe. Keep the `availablePoints`/`totalPoints` rounding lines and their "Rounded separately so the two displayed numbers always add up." comment.
    6. In `DistrictLocksTab`, replace the capacity card and the `which`-ternary helper line with a single `<LocksHeaderCard artifact={artifact} which={which} slots={slots} cutLine={cutLine} />`. The `slots`/`cutLine` derivations stay where they are. Leave the caveat paragraph, toggle button, table card, and everything below it untouched.

    Do not change any label text, unit, number format, or champ stat content. The champ content redesign is sketch 013, and this task is layout only. Do not edit `districtLocksHeaderStats.ts`. Do not add new CSS classes or tokens: reuse the classes already on these elements.
  </action>
  <verify>
    <automated>npx vitest run apps/web/src/components/districts/DistrictLocksTab.test.tsx (output must show every test passing, including the new 260914-3zj test(s), zero failures)</automated>
    <automated>grep -c 'className="data-card' apps/web/src/components/districts/DistrictLocksTab.tsx (must print 2: the merged header card and the table card)</automated>
  </verify>
  <done>Both tabs render exactly one non-table `data-card` header. It holds a single stat row with capacity, Lock Line and that tab's own stats, plus the district chip row below it in the same card. The new tests went red, then green. Every pre-existing test in the file passes without edits.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the file header comment, run the districts suite and web typecheck, commit</name>
  <files>apps/web/src/components/districts/DistrictLocksTab.tsx</files>
  <action>
    Rewrite the "A richer header" paragraph (currently lines 25-31) of the top doc comment in `DistrictLocksTab.tsx`. The new wording says that both tabs render ONE header card whose single wrapping stat row reads capacity, Lock Line, then the tab's own stats. On the District Locks tab those are the per-team season ceiling and the district-wide points pool (Available/Total, the available part an estimate), with the played/upcoming event chips below the row inside the same card. On the Champ Locks tab it is the "Remaining district points: X / Y pre-DCMP" stat. Keep the sentence that both are computed client-side by `districtLocksHeaderStats.ts` because the artifact carries no aggregate field. Keep it plain and short, matching the surrounding comment style. Leave the rest of the comment alone.

    Then run the whole districts component directory and the web typecheck. If typecheck reports errors, fix only errors in `DistrictLocksTab.tsx` or `DistrictLocksTab.test.tsx`. Any error elsewhere belongs to someone else: record it in the SUMMARY and leave it.

    Commit with explicit paths only: `git add apps/web/src/components/districts/DistrictLocksTab.tsx apps/web/src/components/districts/DistrictLocksTab.test.tsx`. Never use `git add -A` or `git add .`. Use a message such as "fix(web): merge the Locks page's two header cards into one on both tabs". After committing, run `git status` and confirm both files are clean and no foreign file was absorbed into the commit (`git show --stat HEAD` lists exactly those two files).
  </action>
  <verify>
    <automated>npx vitest run apps/web/src/components/districts (output shows all district component test files passing, zero failures)</automated>
    <automated>npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -c "DistrictLocksTab" (must print 0)</automated>
    <automated>git show --stat HEAD (lists exactly DistrictLocksTab.tsx and DistrictLocksTab.test.tsx)</automated>
  </verify>
  <done>The doc comment describes the single merged header card. The full districts component suite passes. The web typecheck reports no errors in either touched file. One commit contains exactly the two files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R2 artifact → browser render | Published district artifact JSON (already Zod-validated by `DistrictArtifactSchema`) is rendered as text. This plan changes only the JSX nesting of already-rendered values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3zj-01 | Tampering | LocksHeaderCard text rendering | low | accept | Values still render as React text children (auto-escaped), and no raw HTML injection path is introduced; the schema-validated artifact is unchanged |
| T-3zj-02 | Information disclosure | Commit staging on a shared checkout | low | mitigate | Stage the two files by explicit path and confirm `git show --stat HEAD` lists only them, so no concurrent session's dirty files (or any `.env`-adjacent file) enter the commit |
</threat_model>

<verification>
- `npx vitest run apps/web/src/components/districts` passes in full, judged by printed counts.
- `npx tsc --noEmit -p apps/web/tsconfig.json` reports nothing in `DistrictLocksTab.tsx` / `DistrictLocksTab.test.tsx`.
- `grep -c 'className="data-card' apps/web/src/components/districts/DistrictLocksTab.tsx` prints 2.
- Optional visual confirmation, run from the main context rather than the executor, which has no network: start the web dev server with the local visual verification recipe (`VITE_ARTIFACT_ORIGIN=local`, fresh port), open a district's Locks page, and screenshot both tabs. Expect one header card each, with the district chips below the stat row.
</verification>

<success_criteria>
- District Locks tab: one header card, with a single wrapping stat row (District Championship capacity, Lock Line, Pre-DCMP points remaining, District Points (Available/Total)) and event chips below it in the same card.
- Champ Locks tab: one header card, with a single stat row (FIRST Championship capacity, Lock Line, Remaining district points).
- All labels, value formats, and pre-existing testids unchanged. The new test covers both tabs. The districts suite is green. One commit holds exactly the two files.
</success_criteria>

<output>
Create `.planning/quick/260914-3zj-locks-page-combine-the-two-headers-on-th/260914-3zj-SUMMARY.md` when done.
If the Write tool blocks the SUMMARY write from a subagent, return the SUMMARY text to the
orchestrator instead of routing around the block through Bash or a heredoc.
</output>
