---
phase: quick-260906-80e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/theme.css
  - apps/web/src/components/team/MatchTable.tsx
  - apps/web/src/components/team/MatchTable.test.tsx
autonomous: true
requirements: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a team page's match table, the roster line for the alliance THIS team is on renders on a fully-rounded, alliance-tinted pill; the opposing alliance's line renders with no pill at all."
    - "The team's OWN number within that line sits on a deeper tint of the same alliance hue than the rest of the line."
    - "No roster number in that table renders bold, and none renders in a colour other than --color-text-primary."
    - "The three numbers on each roster line are separated by 10px of real layout spacing — there is no literal space character in the rendered text."
    - "The match cell's vertical rhythm is unchanged: the pill adds no vertical padding, no vertical margin and no line-height change, so MATCH_GEOMETRY.Y_RED/Y_BLUE (23/45) still put each band's centre on its roster line's centre."
    - "A page whose teamKey matches NEITHER roster (the published letter-suffixed second-robot shape, WR-02) renders no pill and no own-number tint."
  artifacts:
    - "apps/web/src/styles/theme.css — four new --alliance-*-ground* tokens derived by color-mix, plus the .match-alliance-* rule block"
    - "apps/web/src/components/team/MatchTable.tsx — both roster lines emit the new wrapper/number classes"
    - "apps/web/src/components/team/MatchTable.test.tsx — a theme.css TEXT guard plus DOM class assertions"
  key_links:
    - "The class names the component emits must exactly match theme.css's selectors. jsdom never applies CSS (see theme.scrollbar.test.ts's own note), so only the CSS-text assertion AND the DOM class assertion TOGETHER prove the cue actually renders — either one alone can pass while the feature is invisible."
    - "The tints derive from --alliance-red / --alliance-blue via color-mix, so a future alliance-hue change carries into the ground tint with no second edit."
---

<objective>
Ship sketch 010 variant C on the team page's per-event match table: replace the roster-line bold
with an alliance ground tint (10% behind the team's whole alliance line, 20% behind the team's own
number), and widen the gap between alliance members' numbers from a literal space (~4px) to 10px
of real layout spacing.

Purpose: the shipped bold does two jobs badly — it is a weak 600-against-400 step at 14px, and
because it paints all three numbers it never says *which one is your team*. Ground tint carries
"this is my alliance" peripherally and "this one is me" at a second depth, with the figure left
neutral. Signed off by the user 2026-09-06 at a 10px gap.

Output: four new theme tokens, one rule block, a rewritten roster-line markup, and tests that fail
against today's code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/sketches/010-alliance-membership-emphasis/README.md
@apps/web/src/components/team/MatchTable.tsx
@apps/web/src/components/team/MatchTable.test.tsx
@apps/web/src/styles/theme.css
@apps/web/src/styles/theme.scrollbar.test.ts

**Project skill:** `sketch-findings-sigmascout` auto-applies to this work. Load it before editing.

**The sketch README's "Constraints implementation must honour" section is authoritative and was
signed off. Do not re-litigate the design, the tint percentages, or the 10px gap.**

**Facts already established by the planner — do not re-derive:**

- `theme.css` already derives tokens with `color-mix(in srgb, ..., transparent)` — see
  `--sim-hist-bar` / `--sim-band-overlay` (theme.css ~line 204) and `--sim-picker-selected-bg`
  (~line 217). That is the idiomatic house form for these four tokens.
- The existing `--alliance-*` block is theme.css ~lines 96-113 (`--alliance-red: #DC2626`,
  `--alliance-blue: #2563EB`, the two `-soft` variants, `--loser-ink`). Its comment forbids
  *changing* those values; adding derived tokens beside them is additive and is what constraint 3
  asks for.
- `.alliance-chip` (theme.css ~line 866) is the house precedent for a `border-radius: 999px`
  alliance-token-consuming class; `.match-row-tint` / `.match-row-untinted` (~line 854) is the
  house precedent for a `MatchTable`-specific rule pair. Put the new block after
  `.alliance-chip--blue`.
- The roster lines are `MatchTable.tsx` lines 318-334: two sibling `<span class="numeric-cell
  text-role-body whitespace-nowrap text-[var(--color-text-primary)]">` blocks, each mapping
  `match.redTeams` / `match.blueTeams` to a `<span key={key}>` carrying
  `{index > 0 ? " " : ""}{teamNumberLabel(key)}`.
- `teamIsRed` / `teamIsBlue` already exist (MatchTable.tsx lines 277-278) as
  `match.redTeams.includes(teamKey)`.
- `cn` is `twMerge(clsx(...))` (`apps/web/src/lib/utils.ts`). It passes non-Tailwind class names
  through unchanged, so `cn("a", cond && "b c")` is safe here.
- **jsdom applies no CSS.** vitest's `test.css` is unset, so `theme.css`'s rules never reach the
  test DOM and `getComputedStyle` reflects only the UA stylesheet. `theme.scrollbar.test.ts`
  documents this and establishes the fallback: assert the shipped CSS **text**. Its local
  `findRuleBody(css, selectorPattern)` helper (comment-stripped, first exact-selector match) is
  not exported — reimplement the same few lines locally.
- No `--spacing-*` token equals 10px (they are 4/8/16/24/32/48/64). The 10px is a measured value
  from the sketch's slider, so it is a literal length in the rule with a comment naming its source.
  D-06 governs colour literals in component code, not lengths in theme.css.
- `apps/web/src/components/team/MatchTable.tsx` is clean in the working tree as of planning
  (last touched by commit `7ad5f3e2`, the Video column).

**Concurrent-session hazard — this is not boilerplate.** Another session is working in this same
checkout on TBA video ingest. Stage by explicit path only. `git add -A` or `git add .` will absorb
its in-flight edits (`SimulationTab.tsx`, `publish.ts`, `packages/harness/*`) into this commit.
Re-read `MatchTable.tsx` immediately before editing it in case that session has moved lines.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: theme.css — four ground tokens and the .match-alliance-* rule block, guarded by a CSS-text test</name>
  <files>apps/web/src/styles/theme.css, apps/web/src/components/team/MatchTable.test.tsx</files>
  <read_first>
    - `apps/web/src/styles/theme.css` lines 96-115 (the `--alliance-*` block and its comment) and
      lines 850-880 (`.match-row-tint`, `.alliance-chip`) for house style.
    - `apps/web/src/styles/theme.scrollbar.test.ts` lines 1-60 — copy its `findRuleBody` shape and
      its `readFileSync` + `fileURLToPath` path pattern.
    - `.planning/sketches/010-alliance-membership-emphasis/index.html` lines 101-150 — the
      `.alliance-line` / `.nums` / `.num` / `.v-tint` rules being ported. Reference only: the
      shipped rules must consume tokens, never the raw `rgba()` the sketch uses.
  </read_first>
  <behavior>
    A CSS-text test (new `describe` block in `MatchTable.test.tsx`, since that component is the
    only consumer) that goes RED against today's theme.css and GREEN after this task:
    - `:root` declares `--alliance-red-ground`, `--alliance-blue-ground`,
      `--alliance-red-ground-own`, `--alliance-blue-ground-own`.
    - Each of the four values matches `color-mix(in srgb, var(--alliance-{red|blue}) {10|20}%,
      transparent)` — the two plain tokens at 10%, the two `-own` tokens at 20%, red tokens
      referencing `var(--alliance-red)` and blue tokens `var(--alliance-blue)`.
    - None of the four declarations contains `rgba(` or `#` (constraint 1: tokens, not literals).
    - `.match-alliance-nums` sets `display: inline-flex` and `gap: 10px`.
    - `.match-alliance-nums--mine` sets `padding: 0 5px`, `margin-left: -5px`,
      `border-radius: 999px` — and its body contains NO `padding-top`, `padding-bottom`,
      `padding-block`, `margin-top`, `margin-bottom`, `margin-block` or `line-height`
      (constraint 2: the cell's vertical rhythm may not move).
    - `.match-alliance-nums--red` / `--blue` set `background-color` to the matching plain ground
      token; the two `.match-alliance-nums--{side} .match-alliance-num--own` descendant rules set
      `background-color` to the matching `-own` token.
    - `.match-alliance-nums--mine`'s `border-radius` is `999px` and is NOT `var(--radius)`
      (constraint 4: the pill must not read as a tier box).
  </behavior>
  <action>
Add the four tokens to `:root` immediately after `--loser-ink`, as their own commented sub-block
in the file's house voice. The comment must explain WHY each exists, not just what it is: sketch
010 variant C, selected 2026-09-06; the ground carries alliance membership so the figure can stay
neutral; derived by `color-mix` from `--alliance-red` / `--alliance-blue` rather than restating a
hue, so a future alliance-colour change carries through with no second edit. Record the measured
contrasts from the sketch (slate-900 reads 15.28:1 on the red 10% tint, 15.50:1 on blue, 13.06:1
on the 20% own-number tint) and record the one thing a future reader will otherwise "fix": the own
number's 20% composites ON TOP of the line's 10%, exactly as the approved sketch renders it, so
the effective own-number tint is deeper than 20% by design.

Then add the rule block after `.alliance-chip--blue`, porting the sketch's `.nums` / `.num` /
`.v-tint` rules with tokens substituted for its `rgba()` literals:

- `.match-alliance-nums` — `display: inline-flex; gap: 10px;`. Applied to BOTH roster lines, so
  the 10px separation is uniform across the column. Comment that 10px was measured on the sketch's
  slider against real 4- and 5-digit roster data (`1690` beside `10935`), replacing a literal
  space of about 4px.
- `.match-alliance-num` — `padding: 0 1px; border-radius: 3px;`. Applied to every number on both
  lines so the own number's own ground does not shift its neighbours' spacing relative to the
  unmarked line. Horizontal padding only, same reason as the pill.
- `.match-alliance-nums--mine` — `padding: 0 5px; margin-left: -5px; border-radius: 999px;`.
  Comment both non-obvious values: the negative left margin cancels the pill's own left padding so
  the marked line's first digit stays x-aligned with the unmarked line above it (without it the
  column reads ragged), and `999px` is deliberately NOT `--radius` (6px) because this cell's box
  vocabulary is already spoken for by tier values and the two shapes must not read as the same
  object. State the vertical-padding prohibition inline: `MATCH_GEOMETRY.Y_RED`/`Y_BLUE` (23/45)
  are locked so the bands centre at 27.1/49.1 on the roster line centres; the sketch measured that
  `padding: 1px 5px` grew the line 2px and pushed the blue roster line off its own band. Height
  comes from line-height alone.
- `.match-alliance-nums--red` / `.match-alliance-nums--blue` — `background-color` from the two
  plain ground tokens.
- `.match-alliance-nums--red .match-alliance-num--own` and the blue equivalent — `background-color`
  from the two `-own` tokens. Descendant selectors, mirroring the sketch, so the own-number depth
  is derived from the line's own side rather than needing a third class at the call site.

Write the test block described in `<behavior>` into `MatchTable.test.tsx` in the same task. Resolve
theme.css as `resolve(dirname(fileURLToPath(import.meta.url)), "../../styles/theme.css")`. Anchor
every selector regex exactly (`/^\.match-alliance-nums$/`, not a substring match) or
`.match-alliance-nums` will match `.match-alliance-nums--mine`'s body instead.

Add no new hue, and change no existing token value. Nothing outside the two insertion points in
this file may move.
  </action>
  <verify>
    <automated>cd /c/Users/Jacob/Documents/GitHub/SigmaScout &amp;&amp; npx vitest run apps/web/src/components/team/MatchTable.test.tsx</automated>
    <automated>cd /c/Users/Jacob/Documents/GitHub/SigmaScout &amp;&amp; git diff --unified=0 apps/web/src/styles/theme.css | grep '^-' | grep -v '^---' | grep -c 'alliance-red:\|alliance-blue:\|alliance-red-soft\|alliance-blue-soft\|loser-ink' # expect 0 — no existing alliance token value was removed or rewritten</automated>
  </verify>
  <done>
    All four tokens exist, are `color-mix`-derived from the existing alliance tokens at 10%/20%,
    and carry no `rgba(`/hex literal. The rule block exists with a 999px pill that has zero
    vertical padding, a -5px left margin, and a 10px gap. The new CSS-text `describe` block passes
    and would fail against the pre-change file. No pre-existing token value changed. The whole
    `MatchTable.test.tsx` suite still passes (the markup is untouched this task, so the existing
    weight test is still green).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatchTable.tsx roster lines — emit the tint classes, drop the bold and the literal space</name>
  <files>apps/web/src/components/team/MatchTable.tsx, apps/web/src/components/team/MatchTable.test.tsx</files>
  <read_first>
    - `apps/web/src/components/team/MatchTable.tsx` lines 270-340 (re-read at edit time; the
      concurrent video-ingest session may have shifted them).
    - `apps/web/src/components/team/MatchTable.test.tsx` lines 83-130 (the WR-02 `describe`, whose
      `frc5199B` fixture is reused below) and lines 225-238 (the existing weight test, which this
      task REPLACES rather than adds to — it asserts the exact behaviour being removed and will go
      red by design).
  </read_first>
  <behavior>
    Replacing the existing weight test, three DOM tests on the rendered row:
    - **Red-alliance team** (`teamKey="frc118"`, redTeams `["frc118","frc254","frc971"]`): the
      wrapper returned by `getByText("118").closest(".match-alliance-nums")` carries
      `match-alliance-nums--mine` and `match-alliance-nums--red`, and NOT `--blue`. The `118` span
      carries `match-alliance-num--own`; the `254` span does not, and resolves to that same
      wrapper. The `604` span's wrapper carries neither `--mine` nor `--red` nor `--blue`.
    - **No bold and no coloured text survive:** for every `.match-alliance-nums` in the row and
      every number span inside it, `className` contains neither `font-semibold` nor `font-bold`,
      and matches no `text-[var(--alliance` colour utility.
    - **The gap is layout, not text:** the red wrapper's `textContent` is exactly `"118254971"` —
      no space characters. This is what proves constraint 5 (the `" "` literal is gone) and is the
      assertion that would catch a "kept the space AND added the gap" half-fix.
    - **Blue-alliance team** (`teamKey="frc604"` against the same fixture): the blue wrapper carries
      `--mine --blue`, the `604` span carries `--own`, and the red wrapper carries no `--mine`.
    - **WR-02 letter-suffix negative** (`teamKey="frc5199"`, blueTeams containing `frc5199B`): no
      `.match-alliance-nums` in the row carries `--mine` and no span carries `--own`. This guards
      the strict-equality own-number test against a `startsWith`/`includes` regression.
  </behavior>
  <action>
Restructure both roster lines (MatchTable.tsx lines 318-334) so each line's three numbers sit
inside one wrapper span rather than being three bare siblings of the line span.

For the red line: keep the existing outer
`<span className="numeric-cell text-role-body whitespace-nowrap text-[var(--color-text-primary)]">`
untouched, and nest inside it a single wrapper
`<span className={cn("match-alliance-nums", teamIsRed && "match-alliance-nums--mine match-alliance-nums--red")}>`
containing the `match.redTeams.map(...)`. Each mapped span becomes
`<span key={key} className={cn("match-alliance-num", key === teamKey && "match-alliance-num--own")}>{teamNumberLabel(key)}</span>`.
Blue line identically with `teamIsBlue` and `--blue`.

Three things must go and must not come back in another form:
- The conditional weight utility currently applied to every span on the team's own line. It is
  replaced by ground, not softened — the sketch settles this explicitly.
- The `{index > 0 ? " " : ""}` literal, and with it the now-unused `index` map parameter. The
  separation is `gap` on the wrapper. Do not substitute `&nbsp;`, a margin on the number spans, or
  any other text-node spacing.
- Any temptation to add a colour utility to the numbers. They stay at the outer span's
  `--color-text-primary`; the whole point of variant C is that the figure stays neutral.

Own-number detection is strict `key === teamKey`. Do not loosen it: `frc5199B` on a page rendering
`frc5199` must match neither roster (`teamIsRed`/`teamIsBlue` are already false there, WR-02), so
no pill and no own tint appear — the existing Result-cell gate depends on that same strictness.

Add no comment to `MatchTable.tsx` that names the removed weight utility by its class name — cite
"sketch 010-C ground tint" instead. A verify gate below counts that literal in this file and
expects zero, and a comment mentioning it would fail the gate that proves the change landed.

Then rewrite the existing test at lines 225-238 into the tests described in `<behavior>`, keeping
its existing "both belong to the same physical `<tr>`" assertion (no per-alliance row background
was introduced) and giving each test a name that states the tint behaviour rather than the weight.
  </action>
  <verify>
    <automated>cd /c/Users/Jacob/Documents/GitHub/SigmaScout &amp;&amp; npx vitest run apps/web/src/components/team/MatchTable.test.tsx</automated>
    <automated>cd /c/Users/Jacob/Documents/GitHub/SigmaScout &amp;&amp; grep -v '^\s*\*' apps/web/src/components/team/MatchTable.tsx | grep -c 'font-semibold' # expect 0</automated>
    <automated>cd /c/Users/Jacob/Documents/GitHub/SigmaScout &amp;&amp; npx tsc --noEmit -p apps/web/tsconfig.json</automated>
    <automated>cd /c/Users/Jacob/Documents/GitHub/SigmaScout &amp;&amp; git status --porcelain apps/web/src/components/event/EventMatchTable.tsx | grep -c . # expect 0 — the event table is out of scope</automated>
  </verify>
  <done>
    Both roster lines render one `.match-alliance-nums` wrapper each; only the team's own line
    carries `--mine` plus its side modifier; only the team's own number carries
    `--match-alliance-num--own`. The bold utility appears zero times in `MatchTable.tsx`. The
    rendered roster text carries no space characters. `apps/web` typechecks clean, the full
    `MatchTable.test.tsx` suite passes, and `EventMatchTable.tsx` is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This change is presentation-only: two CSS insertions and a JSX restructure of already-rendered, already-trusted artifact data. No new input is parsed, no network call is added, no storage is touched, and nothing is rendered with `dangerouslySetInnerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80e-01 | Information disclosure | `MatchTable.tsx` roster spans | low | accept | Team numbers rendered are already public TBA data and are already displayed on this exact line today; the change alters only their background and spacing. |
| T-80e-SC | Tampering | package-manager installs | low | accept | No dependency is added, removed, or upgraded by this plan; `package.json` is not in `files_modified`. If any task turns out to need an install, stop and re-plan — that would require a legitimacy audit this plan does not carry. |
</threat_model>

<verification>
Run from the **repo root**, never from `apps/web` (root vitest sees 167 test files, `apps/web`
sees 77 — an 8-day red CI once hid in that gap):

```
npx vitest run apps/web/src/components/team/MatchTable.test.tsx
npx vitest run apps/web/src/components/team          # sibling team-page components unaffected
npx tsc --noEmit -p apps/web/tsconfig.json
```

Judge every one of these by its **printed output**, not by exit code. Do not wrap them in
`timeout ... pnpm ...` — that swallows output and exits 0, producing a false green.

Two pre-existing conditions that are NOT regressions and must not be "fixed" or reported as such:
`pnpm install` exits 1 on this machine (better-sqlite3 node-gyp; `node_modules` is fine), and the
**worker** package carries 4 `tsc` errors from redDqs/blueDqs drift that predate this work.

<human-check>
The one constraint jsdom cannot test is the one the sketch broke first: vertical rhythm. Serve the
site locally (`VITE_ARTIFACT_ORIGIN=local` on a fresh port, per the local visual verification
recipe) and open team 5951 at `2026isrtp` — the sketch's own fixture, chosen because 5951 switches
alliance at Qual 3, back at Qual 9, and again at Qual 12, and its slot within the alliance moves.
Confirm: (a) each red/blue band in the plot column still centres on its own roster line, with no
vertical drift introduced by the pill; (b) the marked line's first digit is x-aligned with the
unmarked line above it; (c) the alliance flips are obvious while scanning the column top-to-bottom
without reading any single row.
</human-check>
</verification>

<success_criteria>
- Four `color-mix`-derived alliance ground tokens in `theme.css`, no `rgba()`/hex literal among
  them, and no existing token value changed.
- The team's alliance line renders on a 999px pill at 10%; the team's own number at 20% on top.
- Zero occurrences of the bold weight utility in `MatchTable.tsx`; zero coloured-text utilities on
  roster numbers.
- 10px gap as `gap`, with no space character in the roster lines' rendered text.
- The pill's rule body contains no vertical padding, vertical margin, or `line-height`.
- `MatchTable.test.tsx` carries both a CSS-text guard and DOM class assertions, and each would fail
  against the pre-change code.
- `EventMatchTable.tsx`, pipeline, harness, worker, and the sketch files are untouched.
- Commit stages exactly three source paths plus this quick task's planning directory, by explicit
  path.
</success_criteria>

<output>
Commit as `feat(quick-260906-80e): alliance ground tint on the team match table's roster lines`,
staging by explicit path only:

```
git add apps/web/src/styles/theme.css \
        apps/web/src/components/team/MatchTable.tsx \
        apps/web/src/components/team/MatchTable.test.tsx \
        .planning/quick/260906-80e-team-page-match-table-replace-the-roster
```

Never `git add -A` or `git add .` — a concurrent session's in-flight edits to `SimulationTab.tsx`,
`packages/harness/publish.ts` and `apps/web/.tanstack/` are live in this same working tree.

Then create
`.planning/quick/260906-80e-team-page-match-table-replace-the-roster/260906-80e-SUMMARY.md`.
</output>
