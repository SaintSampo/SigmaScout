---
quick_id: 260904-7id
phase: quick-260904-7id
plan: "01"
type: execute
wave: 1
depends_on: []
subsystem: pipeline
tags: [epa, phase-groups, percentiles, rarity-tiers, publish, republish, worker-deploy]

files_modified:
  - packages/core/algorithms/epa.ts
  - packages/core/algorithms/epa.test.ts
  - packages/harness/pageArtifacts.ts
  - packages/harness/publish.test.ts
  - apps/web/src/lib/metricKeys.ts
  - apps/web/src/lib/metricKeys.test.ts
  - apps/web/src/lib/metricGroups.ts
  - apps/web/src/lib/metricGroups.test.ts
  - apps/web/src/components/teams-table/columns.test.tsx
  - apps/web/src/components/team/SeasonHeader.tsx
  - apps/web/src/components/team/SeasonHeader.test.tsx
  - apps/web/src/components/event/InsightsTab.test.tsx
  - docs/publish-budget.md
  - .planning/todos/pending/republish-after-adjust-model-change.md
  - .planning/STATE.md

autonomous: false
requirements: [D-1, D-2, D-3, D-4, D-5]

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor on the Teams page in EPA mode sees rarity-tier boxes on the Auto, Teleop and Endgame cells, coloured by the same season-wide percentile every other tier box on the site uses (D-1, D-3, D-5)"
    - "A visitor on a team page in EPA mode sees tier-boxed Auto/Teleop/Endgame tiles; the event Insights tab tier-boxes the same three columns (D-3)"
    - "Every EPA phase-group tier traces to a pipeline-computed percentile over that season's FULL team pool — never an event-local pool, never a client-side ranking (D-1)"
    - "EPA phase groups still carry no spread anywhere — value + percentile/tier only (D-1)"
    - "A published EPA group value and the client's derived fallback value are equal by construction: both sum componentsInGroup over the same present components (D-1, D-3)"
    - "Every committed prediction digest is bitwise unchanged by this change, proving teamMetrics is a read-only display surface that never feeds scoring (D-2)"
    - "The live Worker writes to the same algorithm versions the manifest names, so live event updates still reach the artifacts the client fetches (D-4)"
  artifacts:
    - packages/core/algorithms/epa.ts
    - apps/web/src/lib/metricKeys.ts
    - docs/publish-budget.md
    - "a screenshot under this task's shots/ showing a rendered EPA group-cell tier box"
  key_links:
    - "epa.ts teamMetrics() -> publish.ts withPercentiles(metricsByTeam, teamsThisSeason) -> withPublishedTiers -> teams/{year} positional row -> rowModel buildTeamRows -> withDerivedGroupMetrics published-wins merge -> MetricValue tier box"
    - "epa.ts teamMetrics() -> publish.ts sortedPoolsByMetric -> withHistoryPercentiles (HISTORY_PERCENTILE_METRIC_KEYS already lists the three group keys) -> team/{teamKey}/{year} metricHistory"
    - "epa.ts teamMetrics() -> publish.ts withEventPercentiles -> event/{eventKey} teams -> InsightsTab tierForPercentile"
    - "manifest/algorithms.json versions <-> the deployed Worker's bundled algorithm.version (scheduled.ts writes artifact keys from the MODULE's version, not the manifest's)"
---

<objective>
Make EPA's Auto/Teleop/Endgame carry rarity highlights everywhere they render, by PUBLISHING them
as first-class metrics from the pipeline instead of deriving them tier-lessly in the browser.

This is an explicit user reversal of quick task 260904-5zg's T-5zg-03 decision ("derived groups
carry no tier"). The user's words: "auto teleop and endgame epas should have rarity highlights."

Purpose: the tier must MEAN the same thing on an EPA group cell as it does on every other cell on
the site — a season-wide, pipeline-computed percentile over the full team pool. That is only
honest if the pipeline computes it. A client-side ranking would render in the identical colour
while meaning something else, which a reader cannot detect.

Output: `epa.teamMetrics()` publishes `phaseAuto`/`phaseTeleop`/`phaseEndgame`; the existing
publish pipeline attaches percentile/tier to them with no pipeline change at all; the client's
published-entry-wins seam serves them unchanged; a full 2022-2026 republish plus a Worker redeploy
ships it; one rendered screenshot proves a tier box on a group cell.

## READ THIS BEFORE EXECUTING — two facts verified at plan time, not assumed

**1. Quick task 260904-5px is MID-FLIGHT.** Its plan has three tasks; only Task 1 is committed
(`215b0c34`, EPA's fouls exclusion). Tasks 2 (adopt Statbotics' elimination discount, re-measure
both baselines) and 3 (rename to "EPA Statbotics 3.0", file its own republish) have NOT run, it has
no SUMMARY, and STATE.md has no row for it. Task 3 of THIS plan therefore opens with a
`checkpoint:decision`: publishing now ships a coherent, tested `epa@4.0.0+baseline` but guarantees
a second republish when 5px finishes. See that checkpoint for the two options.

**2. This republish ships far more than this task.** Live is `epa@2.0.0+baseline` /
`vpr@7.0.0+rolling-2026-09` (read from the live manifest at plan time, generation
`4ba99e89-b196-4f88-90c7-3bc1ffae3de9`). Main HEAD is `epa@4.0.0+baseline` /
`vpr@8.0.0+rolling-2026-09`. This republish is the FIRST live ship of 260904-5px Task 1 (EPA's
no-foul total) and all of 260904-6a1 (adjust-zeroed-alliance exclusion, adjust pinned at 0 in both
EPA and Sigma1). It is also Item 3 of `.planning/todos/pending/republish-after-adjust-model-change.md`.
Items 1 (a new baseline fingerprint) and 2 (re-run EPA-vs-Statbotics) of that todo are explicitly
NOT in this task's scope and stay open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@packages/core/algorithms/epa.ts
@packages/core/algorithms/breakdown/groups.ts
@packages/core/algorithms/sigma1/index.ts
@packages/harness/percentiles.ts
@packages/harness/publish.ts
@packages/harness/pageArtifacts.ts
@apps/web/src/lib/metricGroups.ts
@apps/web/src/lib/metricKeys.ts
@.planning/todos/pending/republish-after-adjust-model-change.md
@docs/publish-budget.md

Project skill: load `Skill("sketch-findings-sigmascout")` before touching any component or test
that asserts visual tier behaviour (Task 2, Task 3's render step).
</context>

<facts_established_at_plan_time>
These were read, run or fetched during planning. They are inputs, not things to re-derive.

**The insertion point is `epa.ts`'s `teamMetrics()`, and NOTHING in the harness needs to change.**
Traced end to end in `packages/harness/publish.ts`: the season loop calls
`algorithm.teamMetrics(state, teamsThisSeason)` once (`:1786`), then `withPercentiles(...,
teamsThisSeason)` (`:1795`), `sortedPoolsByMetric(...)` (`:1805`), and
`lastOfficialMetricsByTeam` + `withPercentiles` for the teams row (`:1824-1825`), finally
`withPublishedTiers(...)` (`:1885`). Every one of those is generic over metric NAMES — none
carries an allowlist that would exclude a new key. `HISTORY_PERCENTILE_METRIC_KEYS`
(`percentiles.ts:251`) ALREADY equals the three group keys plus total, so EPA's history rows pick
up percentiles the moment the keys exist. `withEventPercentiles` (`publish.ts:272`) applies no
allowlist at all. Adding three keys in core is the whole pipeline change.

**The client needs no rendering change either.** `columns.tsx:357` renders
`<MetricValue metric={...} tier={info.getValue()?.tier} />`; `SeasonHeader.tsx:189` and
`InsightsTab.tsx:276/293` render `tierForPercentile(entry?.percentile)`. All three read the
published field generically. `withDerivedGroupMetrics` (`metricGroups.ts:129`) returns
`{ ...derived, ...metrics }` — the published entry wins — so a published, tiered `phaseAuto`
simply displaces the derived value-only one.

**The teams artifact's positional wire encoding is already generic.** `encodeTeamMetricEntry`
(`pageArtifacts.ts:822`) emits `[value, spread|null, tier]` for any tiered metric, and the
`metricKeys` preamble is derived from the rows about to be written (`pageArtifacts.ts:866`), not
from a hardcoded per-algorithm list. No wire-schema change is needed. One DOC comment there
(`pageArtifacts.ts:784-787`) states OPR and EPA rows are length-1 for every metric they publish —
that becomes false for EPA's group metrics and must be corrected.

**Version decision: DO NOT bump EPA's version.** Two independent reasons, both checked:
(a) precedent — commit `06f468ad` added `phaseAuto`/`phaseTeleop`/`phaseEndgame` to Sigma1's
`teamMetrics()` and did NOT bump Sigma1's version; its own message records "team metrics do not
feed the prediction-stream digest, so adding these keys leaves every committed digest bitwise
unchanged". (b) `epa@4.0.0+baseline` has never been published to R2 at all — live is
`epa@2.0.0+baseline` — so widening its published metric set before its first publish cannot make
one version string stand for two different published shapes. `git log -S"COMPONENT_GROUP_METRIC_KEYS"
-- packages/core/algorithms/epa.ts` returns nothing: EPA has never had group metrics, so this is a
new capability, not a restored regression. Task 1 must PROVE premise (a) still holds rather than
citing it.

**Payload projection, from live bytes fetched at plan time** (`Accept-Encoding: identity`):

| live object | bytes |
|---|---:|
| `v1/team/frc3538/2024/epa@2.0.0+baseline.json` | 263,568 |
| `v1/team/frc118/2024/epa@2.0.0+baseline.json` | 200,801 |
| `v1/team/frc3538/2024/vpr@7.0.0+rolling-2026-09.json` | **380,020** (the page-kind max) |

`team/{teamKey}/{year}`'s committed `budgetMaxBytes` is **400,000** (raised from 375,000 on
2026-09-04 with explicit user authorisation — see `docs/publish-budget.md`), leaving 19,980 bytes
(5.0%) of headroom, and that headroom is held by a **VPR** object this change does not touch.
frc3538/2024 carries ~234 played matches; three group entries per `metricHistory` row at roughly
45 bytes each projects ~31,600 bytes of growth on EPA's largest team artifact, landing it near
295,000 — under both the budget and VPR's max. The projection is the pre-flight expectation, NOT
the finding; Task 3 reports the run's own measured numbers.

**The deployed Worker writes artifact keys from its OWN bundled `algorithm.version`, not the
manifest's.** `apps/worker/src/scheduled.ts:998/1039` pass `version: algorithm.version` from the
module `buildAlgorithmModules` (`:258`) instantiated. Once this republish repoints the manifest to
`epa@4.0.0+baseline`/`vpr@8.0.0+rolling-2026-09`, a Worker still bundled at the old versions would
keep writing to keys the client no longer fetches — live event updates would silently stop
reaching the site. A redeploy from the same commit that published is required (07-19 set the
precedent of pairing a Worker deploy with a version change).

**Test baselines, run at plan time on current main:** `npx vitest run
packages/harness/baselineFingerprint.test.ts` is **14/14 GREEN**. The two pre-existing failures
this task's brief warned about (from 260904-4aa) are already resolved — treat any red in that file
as NEW and caused by this task.

**Working tree at plan time:** `.planning/STATE.md` and `apps/web/src/styles/theme.css` are
modified but uncommitted, and 5px's/6a1's planning directories are untracked. `theme.css` belongs
to a concurrent session — do not stage it.
</facts_established_at_plan_time>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: EPA publishes its three phase groups, and the whole percentile/tier pipeline picks them up for free (D-1, D-2)</name>
  <files>packages/core/algorithms/epa.ts, packages/core/algorithms/epa.test.ts, packages/harness/pageArtifacts.ts, packages/harness/publish.test.ts</files>
  <precondition>`git status --porcelain` shows no uncommitted changes under `packages/core/` or `packages/harness/` (a concurrent session editing those files means halting and reporting, not merging).</precondition>
  <behavior>
    Write these as failing tests in `packages/core/algorithms/epa.test.ts` BEFORE editing `epa.ts`.

    - For a 2024 EPA state with learned components, `epa.teamMetrics(state, [team])[team]` carries
      `phaseAuto`, `phaseTeleop` and `phaseEndgame`, each exactly equal to the sum of
      `componentsInGroup(2024, group)` entries PRESENT in that team's component record — compared
      against a value computed in the test from `componentsInGroup`, never against a hand-typed
      number.
    - Each group entry carries a `value` and nothing else — no `spread` key at all. EPA carries a
      mean only, everywhere, and that does not change here.
    - A group whose components are all absent from the team's record publishes NO entry — never a
      fabricated zero-valued one. Drive this with a season whose registered grouping names
      components the fixture state does not carry.
    - A state whose `season` is undefined publishes the components and total but no group entries,
      and does not throw.
    - Reconciliation, EPA-specific and worth pinning because it is now TRUE and was not before:
      `phaseAuto + phaseTeleop + phaseEndgame + adjust` equals `total` exactly. Assert it with the
      `adjust` term written out (robust if `adjust`'s pinning is ever revisited), not as a bare
      three-way equality.

    Then, in `packages/harness/publish.test.ts`, extend the synthetic temp-dir `publishSeasons`
    pattern quick task 260904-586 established:

    - An EPA `teams/{year}` row's decoded `metrics.phaseAuto` carries a `tier` when its percentile
      clears the Rare cut, and carries no `percentile` key (the teams row publishes the compact
      tier only).
    - An EPA `team/{teamKey}/{year}` artifact's `seasonStats.metrics.phaseAuto` carries a
      `percentile`, and at least one `metricHistory` row's `metrics.phaseAuto` carries one too.
    - An EPA `event/{eventKey}` artifact's team standing carries `phaseAuto` with a `percentile`.
    - The percentile pool assertion that makes this honest: build the fixture so a team's
      event-local rank and its season-wide rank DIFFER, then assert the published percentile
      matches the season-wide one.
  </behavior>
  <action>
Add a phase-group block to `teamMetrics()` in `packages/core/algorithms/epa.ts`, immediately after
the `TOTAL_METRIC_KEY` assignment, mirroring `packages/core/algorithms/sigma1/index.ts:1587-1614`
in structure and skipping only its spread lookup (D-1).

Import `COMPONENT_GROUP_IDS`, `COMPONENT_GROUP_METRIC_KEYS` and `componentGroupsForSeason` from
`./breakdown/index.js` — the same single grouping source `apps/web/src/lib/metricGroups.ts`
already reads, which is what makes a published value and a client-derived value identical by
construction rather than by two lists being kept in step.

Resolve the season from `state.season`; when it is undefined or `componentGroupsForSeason` returns
undefined, publish no group entries and do not throw. For each group id, sum
`components[name]` for every name in that group that is PRESENT in the team's component record,
tracking presence with a flag rather than inferring it from a zero sum — a group with no present
component publishes nothing, matching Sigma1's own rule and the client's.

Do not touch `update()`, `predict()`, `carrySeason()` or the version string. `teamMetrics` is a
read-only projection of state; nothing reads its output back into scoring. Leave
`version: "4.0.0+baseline"` exactly as it is, and record the reason in the block's doc comment,
citing both the `06f468ad` precedent and the fact that this version has not yet been published.

Extend the existing doc comment above `teamMetrics` so a reader learns that the three published
groups reconcile with `total` once `adjust` is added back — an EPA-specific consequence of 5px's
no-foul total and 6a1's pinned `adjust`, and deliberately NOT a general property (VPR's total
still spans every component including fouls).

In `packages/harness/pageArtifacts.ts`, correct the now-false statement in
`PositionalMetricEntrySchema`'s doc comment that OPR and EPA rows occupy the one-element form for
every metric they publish: EPA's three group metrics now occupy the three-element tiered form,
while its components and total still do not. Change nothing executable in that file.
  </action>
  <verify>
    <automated>npx vitest run packages/core/algorithms/epa.test.ts packages/core/algorithms/breakdown/groups.test.ts packages/core/isomorphic.test.ts packages/harness/publish.test.ts packages/harness/percentiles.test.ts packages/harness/pageArtifacts.test.ts</automated>
    <automated>npx vitest run packages/harness/seasonParamSets.test.ts packages/harness/baselineFingerprint.test.ts packages/harness/replay.test.ts packages/harness/replay.multiAlgorithm.test.ts packages/harness/replay.season.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
EPA publishes `phaseAuto`/`phaseTeleop`/`phaseEndgame` as value-only entries summed from
`componentsInGroup`; the publish path attaches percentile (team + event + history) and tier (teams
row) to them with zero harness edits; every committed prediction digest and every baseline
fingerprint is bitwise unchanged, proving D-2's no-bump decision rests on a premise that still
holds; `npx tsc --noEmit` is clean. Committed as
`feat(quick-260904-7id): publish EPA's phase groups so the percentile pass can tier them (D-1, D-2)`.
  </done>
  <reversibility rating="costly">Publishing three new metric keys is reversible in code but the artifacts carrying them cost a full republish to unwind; nothing breaks and no migration is needed.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the client stops calling EPA's groups tier-less, and proves the published tiers render on all three surfaces (D-3)</name>
  <files>apps/web/src/lib/metricKeys.ts, apps/web/src/lib/metricKeys.test.ts, apps/web/src/lib/metricGroups.ts, apps/web/src/lib/metricGroups.test.ts, apps/web/src/components/teams-table/columns.test.tsx, apps/web/src/components/team/SeasonHeader.tsx, apps/web/src/components/team/SeasonHeader.test.tsx, apps/web/src/components/event/InsightsTab.test.tsx</files>
  <behavior>
    Load `Skill("sketch-findings-sigmascout")` first — every assertion below is about visual tier
    behaviour.

    Write these as failing tests before editing anything.

    NEW coverage, driven by fixtures whose metrics record DOES carry a published group entry:
    - `metricKeysFor`-adjacent: `publishesGroupMetrics("epa")` is true, and `publishesGroupMetrics("opr")`
      stays false.
    - Teams grouped view (`columns.test.tsx`): a row whose `phaseAuto` is a published
      `{ value, tier: "epic" }` renders the metric-tier box. Assert on the rendered tier class /
      `MetricValue`'s `tier` prop, not on the raw fixture.
    - `SeasonHeader.test.tsx`: an EPA season whose `metrics.phaseTeleop` carries a published
      `percentile` renders a tiered Teleop tile.
    - `InsightsTab.test.tsx`: an EPA event artifact whose team standing carries
      `phaseEndgame: { value, percentile }` renders a tiered Endgame cell.
    - `metricGroups.test.ts` parity: for one component record, the value
      `withDerivedGroupMetrics` derives equals the value `epa.teamMetrics` publishes for the same
      season and component set — imported from `packages/core`, computed on both sides, never two
      hand-typed numbers. This is the test that makes "identical by construction" checkable.

    RETAINED coverage — do NOT delete these, retitle them so the intent is explicit:
    - `withDerivedGroupMetrics` still returns a value-only entry (no spread, no percentile, no
      tier) when the input carries components but NO published group entry. That is the
      stale-artifact fallback: a browser holding a cached pre-republish artifact must render an
      honest untiered number rather than nothing, and must never invent a tier for it.
    - A published group entry still wins over a derived one, reference-equality tested, so VPR's
      covariance-derived spread/percentile/tier remain provably untouched.
  </behavior>
  <action>
Flip `publishesGroupMetrics` in `apps/web/src/lib/metricKeys.ts` to answer true for both `vpr` and
`epa`, and rewrite its doc comment to record what changed and why: the pipeline now publishes the
three group metrics for EPA as of quick task 260904-7id, so the narrower published-versus-derived
question this predicate answers has a new answer for EPA. Keep it derived from the algorithm id,
never from inspecting a fetched row.

Update `apps/web/src/lib/metricGroups.ts`'s module header, which currently states as a design rule
that EPA's groups carry no percentile and no tier. Rewrite that passage so it says what is now
true: EPA's groups ARE published with a season-wide percentile and tier; `withDerivedGroupMetrics`
survives as the stale-artifact fallback for a browser holding a pre-republish artifact, and a
derived entry stays value-only precisely because a client has no season-wide pool to rank against
and a fabricated tier would be indistinguishable from a real one. Also correct the header's claim
that the three groups never sum to `total` — that is still true for VPR, but for EPA the groups
plus `adjust` now reconcile exactly, and the surfaces still must not present the sum as a
reconciliation because the property is algorithm-dependent.

Update the inline comment in `apps/web/src/components/team/SeasonHeader.tsx` that explains why a
derived tile yields no tier, so it names the stale-artifact case rather than reading as a
permanent property of EPA.

Change no rendering code. The published-wins merge and the three generic tier call sites already
carry the behaviour; this task's job is to make the tests prove it and the comments stop lying.
  </action>
  <verify>
    <automated>npx vitest run apps/web/src/lib/metricKeys.test.ts apps/web/src/lib/metricGroups.test.ts apps/web/src/components/teams-table apps/web/src/components/team/SeasonHeader.test.tsx apps/web/src/components/event/InsightsTab.test.tsx apps/web/src/routes/event.$eventKey.test.tsx</automated>
    <automated>npx vitest run apps/web</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
`publishesGroupMetrics("epa")` is true; new tests prove a published EPA group entry renders a tier
box on the Teams grouped view, the team-page phase tiles and the event Insights columns; the
tier-less derived fallback stays covered as the stale-artifact path; the published-wins
reference-equality test for VPR still passes; the full `apps/web` suite and `npx tsc --noEmit` are
green. Committed as
`fix(quick-260904-7id): serve EPA's published group tiers and keep the derived path for stale artifacts (D-3)`.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: republish now, or batch with 260904-5px?</name>
  <decision>Republish 2022-2026 now, or hold until quick task 260904-5px finishes?</decision>
  <context>
Nothing EPA's phase groups do is visible to a visitor until the artifacts are rebuilt. Tasks 1 and
2 are committed and green either way; this decides only whether the ~20-minute production
republish happens now.

Background, verified at plan time rather than assumed:

Quick task 260904-5px is unfinished. Its plan has three tasks and only Task 1 is committed
(`215b0c34`, which made EPA's published total exclude fouls). Task 2 would adopt Statbotics'
elimination discount — another change to EPA's numbers — and Task 3 would rename the algorithm to
"EPA Statbotics 3.0" in the ribbon and file its own republish. It has no SUMMARY and no STATE.md
row.

So a republish now ships a coherent, fully-tested `epa@4.0.0+baseline` (5px Task 1's no-foul total
+ all of 260904-6a1's adjust work + this task's phase groups), and 5px's remaining work will need
a second republish later. `.planning/todos/pending/republish-after-adjust-model-change.md` Item 4
records this project's own preference for batching rather than multiplying republish runs, which
is the argument on the other side.

One full publish costs roughly 20 minutes wall clock and ~57,000 R2 Class-A writes — about 5.7% of
one month's free-tier allowance — plus a hand-transcribed entry in `docs/publish-budget.md` and a
set of orphaned objects under the old `epa@2.0.0` / `vpr@7.0.0` prefixes.
  </context>
  <options>
    <option id="publish-now">
      <name>Republish now</name>
      <pros>The user sees the rarity highlights they asked for today. Closes Item 3 of the pending republish todo. Ships 5px Task 1 and all of 6a1 live for the first time — both are committed, tested and currently invisible.</pros>
      <cons>5px's remaining work will need its own republish, so two runs instead of one. A second set of orphaned objects accumulates in R2.</cons>
    </option>
    <option id="hold-for-5px">
      <name>Hold the republish until 5px completes, then batch</name>
      <pros>One republish carries every EPA change of 2026-09-04 at once, matching the batching discipline the pending todo names. One budget entry, one orphan set.</pros>
      <cons>5px is dormant with no owner or date, so "later" is indefinite. The user's requested feature stays invisible until then. Tasks 1 and 2 sit committed but unshipped.</cons>
    </option>
  </options>
  <resume-signal>Select: publish-now, or hold-for-5px</resume-signal>
</task>

<task type="auto">
  <name>Task 3: republish, redeploy the Worker, and render one EPA surface proving a tier box on a group cell (D-4, D-5)</name>
  <files>docs/publish-budget.md, .planning/todos/pending/republish-after-adjust-model-change.md, .planning/STATE.md</files>
  <precondition>The checkpoint above resolved to `publish-now`. If it resolved to `hold-for-5px`, skip this task entirely, record the deferral in the SUMMARY, and append this task's republish to `.planning/todos/pending/republish-after-adjust-model-change.md` Item 3 instead.</precondition>
  <precondition>Zero publish processes are already in flight: an untruncated command-line-filtered `Get-CimInstance Win32_Process` query for `publish.ts`, with a `-notlike '*Get-CimInstance*'` clause so the query does not self-match, returns nothing. Three other sessions ran work today; two interleaved publishes would interleave generations.</precondition>
  <precondition>The live algorithms manifest still reports generation `4ba99e89-b196-4f88-90c7-3bc1ffae3de9` with `epa@2.0.0+baseline` and `vpr@7.0.0+rolling-2026-09` — the state recorded at plan time. A different generation means another publish landed since; stop and report rather than layering on top of an unknown run.</precondition>
  <precondition>`git status --porcelain` shows nothing uncommitted under `packages/`, `apps/worker/` or `scripts/`, and `git rev-parse HEAD` is recorded — the publish and the Worker deploy must come from the same commit.</precondition>
  <action>
**Secrets discipline is load-bearing here (`.claude/CLAUDE.md`).** Credentials reach the publisher
ONLY through `tsx --env-file=.env`. Never call `Read` on `.env`, never `cat`/`head`/`echo` it,
never interpolate a value from it into a command, log line, test name, commit message or SUMMARY.
This rule exists because it was broken once in Phase 4 and cost a token rotation; passing
`scripts/secrets-boundary.test.ts` is not evidence secrets were handled correctly.

Record the pre-run baseline first: the live manifest's `generation` and `computedAt`, and the byte
size of `v1/team/frc3538/2024/epa@2.0.0+baseline.json` (263,568 at plan time) as the before side of
the payload comparison.

Run the republish ONCE, backgrounded from the first invocation, never a foreground call and never
a retry:

`npx tsx --env-file=.env packages/harness/publish.ts --seasons 2022-2026 --include-offseason`

Expect roughly 18-25 minutes and about 56,776 PUTs. If the background task appears to die,
do NOT restart and do NOT patch a subset of seasons — `publishSeasons` bridges season-to-season
algorithm state only within one call, so a partial re-run cold-starts the carried state. Check
whether the underlying process tree is genuinely alive with `Get-CimInstance` and whether the live
`teams/{year}` generations are advancing before concluding anything; quick task 260904-586 hit
exactly this false-death and the process had not died.

Verify the shipped bytes against the credential-free public origin, not the run's own tally:

- `npx tsx scripts/verifySubsetPublish.ts` — expect 0 failing and exactly one distinct generation,
  equal to this run's own summary line.
- Fetch `v1/manifest/algorithms.json` and confirm it now names `epa@4.0.0+baseline` and
  `vpr@8.0.0+rolling-2026-09`.
- Fetch `v1/teams/2026/epa@4.0.0+baseline.json` and confirm its `metricKeys` preamble contains the
  three group keys and that at least one row's slot for one of them is the three-element tiered
  form, with the tier drawn from the season-wide pool.
- Fetch `v1/team/frc3538/2024/epa@4.0.0+baseline.json`, record its byte size against the 263,568
  before-figure, and confirm `seasonStats.metrics.phaseAuto` carries a percentile.

Then redeploy the Worker from the SAME commit, so its bundled `algorithm.version` matches the
versions the manifest now names — `apps/worker/src/scheduled.ts` builds artifact keys from the
module's own version, so a Worker left on the old bundle would keep writing to keys the client no
longer fetches and live event updates would silently stop landing:

`pnpm worker:deploy`

Confirm `git rev-parse HEAD` is unchanged from the value recorded in the precondition, so the
deployed bundle and the published manifest provably came from one commit. If wrangler returns an
auth error, raise it as an authentication gate rather than working around it.

Render the proof (D-5). Reuse quick task 260904-5zg's measurement pattern: a throwaway Playwright
script that intercepts requests to the artifact origin and fulfils them with an added
`Access-Control-Allow-Origin` header, so a local dev server page can render the real published
data despite R2 not allow-listing localhost. Start `pnpm --filter web dev` yourself and confirm the
port actually serves THIS project before measuring — 5zg found a port that answered 200 while
serving a different app. Load the Teams page in EPA mode in its grouped view, assert in the DOM
that an Auto, Teleop or Endgame cell carries a metric-tier class, and save a screenshot to this
task's `shots/` directory. Delete the script before committing; keep the screenshot.

Transcribe the publish summary into `docs/publish-budget.md` by hand — the command prints it but
does not write it, and the budget tests read that file's machine-readable block. Add a new dated
section in the same format every prior entry uses: the exact command, generation, object count,
byte total, wall clock, the concurrent-process check, the per-page-kind table with the change
versus the 2026-09-04 `4ba99e89` run, and a plain statement of what changed (EPA's three published
phase-group metrics, plus the first live ship of 260904-5px Task 1 and 260904-6a1). Update the
machine-readable block to this run's measured numbers.

If any page kind's measured max exceeds its committed `budgetMaxBytes`, report the number and stop
— do NOT raise a budget to make a gate pass. `team/{teamKey}/{year}` is the one to watch: 400,000
committed against a 380,020 max held by a VPR object, only 5% of headroom, and 260904-6a1's model
change moves VPR's values too. Raising a ceiling is a user decision with its own review, exactly as
the 375,000 to 400,000 raise was.

Finally, update `.planning/todos/pending/republish-after-adjust-model-change.md`: mark Item 3 done
naming this run's generation, leave Items 1, 2 and 4 open and untouched, and add a line recording
that 260904-5px's Tasks 2 and 3 remain unshipped and will need their own republish.
  </action>
  <verify>
    <automated>npx tsx scripts/verifySubsetPublish.ts</automated>
    <automated>npx vitest run packages/harness/payloadBudget.test.ts</automated>
    <automated>npx vitest run apps/worker</automated>
    <automated>npx vitest run packages/core packages/harness apps/worker</automated>
    <human-check>Open the Teams page in EPA mode and confirm the Auto, Teleop and Endgame cells wear rarity-tier boxes in the same colours the Total column uses. The screenshot saved under this task's shots/ is the recorded evidence.</human-check>
  </verify>
  <done>
One single internally consistent 2022-2026 publish is live under `epa@4.0.0+baseline` and
`vpr@8.0.0+rolling-2026-09` with one distinct generation across every sampled key; the Worker is
redeployed from the same commit; a fetched EPA teams artifact carries the three group keys in its
preamble with at least one tiered row; a screenshot under `shots/` shows a rendered tier box on an
EPA group cell; `docs/publish-budget.md` carries a hand-transcribed dated entry with this run's
measured numbers and `payloadBudget.test.ts` reflects them honestly; the pending republish todo
records Item 3 as done and 5px's remaining republish as still owed. Committed as
`docs(quick-260904-7id): republish 2022-2026 with EPA phase-group tiers and record the run (D-4, D-5)`.
  </done>
  <reversibility rating="one-way">A production republish plus a Worker deploy changes what every visitor sees and consumes real R2 write quota; the preceding checkpoint is where that is decided.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local pipeline -> R2 / Cloudflare | `.env` credentials cross here; the publisher writes every public artifact |
| pipeline -> browser | published percentile/tier values are rendered as authoritative statements about a team's standing |
| manifest -> deployed Worker | the Worker decides which artifact keys to write from its own bundled version, not from the manifest it reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7id-01 | Information disclosure | `.env` -> agent transcript during Task 3's publish | critical | mitigate | Credentials reach the publisher only via `tsx --env-file=.env`. Task 3's action forbids `Read`/`cat`/`echo`/interpolation of `.env` or any value from it, in commands, logs, test names, commit messages and the SUMMARY. This exact failure occurred in Phase 4 and cost a token rotation. |
| T-7id-02 | Tampering | concurrent publishes interleaving generations in R2 | high | mitigate | Task 3 preconditions require a self-excluding `Get-CimInstance` process check and a live-manifest generation match against the recorded `4ba99e89` baseline before starting; `verifySubsetPublish.ts`'s generation-uniformity check confirms exactly one distinct generation afterwards. |
| T-7id-03 | Spoofing | a tier box coloured from a pool that is not the season-wide field | high | mitigate | Tiers are produced only by `withPercentiles`/`withPublishedTiers` over `teamsThisSeason`. The client never ranks: `withDerivedGroupMetrics` stays value-only and its tier-less behaviour keeps its own retained test. Task 1's publish test asserts a published percentile matches the season-wide rank in a fixture where the event-local rank differs. |
| T-7id-04 | Denial of service | deployed Worker writing to superseded version keys after the manifest repoints | high | mitigate | Task 3 runs `pnpm worker:deploy` after the republish and asserts `git rev-parse HEAD` is unchanged, so the deployed bundle and the published manifest come from one commit. |
| T-7id-05 | Tampering | a payload budget raised to make a failing gate pass | medium | mitigate | Task 3 forbids raising any `budgetMaxBytes`; an overage is reported as a measured finding for a separate user decision, matching the 375,000 to 400,000 precedent. |
| T-7id-06 | Repudiation | `docs/publish-budget.md` not reflecting the run that changed the numbers | medium | mitigate | Task 3 hand-transcribes the printed summary into a new dated section and the machine-readable block; the command does not write the file itself. |
| T-7id-SC | Tampering | supply chain | low | accept | This task installs no packages from npm, pip or cargo — no dependency is added, removed or upgraded — so no legitimacy audit is applicable. |
</threat_model>

<verification>
- Repo-root suite: `npx vitest run packages/core packages/harness apps/worker` — never under `timeout`, verified by reading the output rather than the exit code (`timeout <n> pnpm <cmd>` swallows output and exits 0 on this machine).
- Client suite: `npx vitest run apps/web`. Running vitest from `apps/web` picks up only 77 files while the repo root picks up 167 — always report which scope was run.
- `npx tsc --noEmit` clean.
- `baselineFingerprint.test.ts` was 14/14 at plan time; any red there is new and belongs to this task.
- Every committed prediction digest bitwise unchanged, proving D-2's no-bump reasoning.
- One distinct generation across every key `verifySubsetPublish.ts` samples.
- A screenshot under this task's `shots/` showing a rendered tier box on an EPA Auto/Teleop/Endgame cell.
</verification>

<success_criteria>
- EPA's `teamMetrics()` publishes `phaseAuto`/`phaseTeleop`/`phaseEndgame` summed from `componentsInGroup`, value-only, with no harness change required (D-1).
- EPA's version stays `4.0.0+baseline`, with the reasoning recorded and its premise proven by unchanged digests (D-2).
- `publishesGroupMetrics("epa")` is true; published tiers render on the Teams grouped view, the team-page phase tiles and the event Insights columns; the tier-less derived path stays covered as the stale-artifact fallback (D-3).
- A single consistent 2022-2026 republish is live and the Worker is redeployed from the same commit (D-4).
- One rendered EPA surface proves a tier box on a group cell (D-5).
- `docs/publish-budget.md` carries this run's hand-transcribed numbers; no budget was raised to fit a measurement.
</success_criteria>

<output>
Create `.planning/quick/260904-7id-publish-epa-phase-group-metrics-with-per/260904-7id-SUMMARY.md` when done.

The SUMMARY must record, explicitly: the checkpoint's resolution; the republish generation and
measured per-page-kind numbers; what 260904-5px still owes; and any budget overage as a number
rather than a resolved item.
</output>
