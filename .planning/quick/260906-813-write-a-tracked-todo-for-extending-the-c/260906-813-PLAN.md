---
quick_id: 260906-813
phase: quick-260906-813
slug: write-a-tracked-todo-for-extending-the-c
date: 2026-09-06
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: true
depends_on: []
requirements:
  - F-A
  - F-B
  - F-C
  - F-D
  - F-E
  - F-F
  - F-G
  - F-H
  - F-I
  - F-J
  - R-1
  - R-2
  - R-3
  - R-4
  - R-5
  - R-6
  - R-7
  - R-8
  - R-9
  - TRAP
files_modified:
  - .planning/todos/pending/extend-corpus-2018-2017-2016.md

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A reader opening the new todo learns in its first screen that the scope is 2018 FIRST with 2017 and 2016 named as the follow-on sequence, that 2021 is a permanent exclusion rather than a deferral, and that every number in the file was measured on 2026-09-06 and must not be re-measured."
    - "The anti-additivity problem (F-A/F-B/F-C) is presented as an OPEN MODELLING DECISION with its measured evidence, never as a solved one — a reader cannot mistake the file for having chosen a treatment."
    - "The autoQuest rule (F-E) is presented as two options with their measured agreement/FP/FN numbers and a named deciding step, with neither option selected by this file."
    - "All nine registries (R-1..R-9) appear with their file paths and line anchors, and THE TRAP's three hardcoded test season lists are called out as the single highest-risk item, because a green suite over an unproven component map is the failure mode."
    - "Every measured figure is transcribed with the sample size and provenance date the orchestrator supplied — including finding H's explicitly unmeasured qualification-match count, which is labelled as unmeasured rather than estimated."
    - "No source, data, script, or docs file changes — the diff is exactly one new markdown file under .planning/todos/pending/."
  artifacts:
    - ".planning/todos/pending/extend-corpus-2018-2017-2016.md (new; created in pending/, NOT in completed/)"
  key_links:
    - "The new todo <-> .planning/todos/completed/extend-corpus-2019-2020.md — this file is that job's successor and inherits its structure, its 2021 permanent-exclusion rule, and its recorded hardcoded-test-list trap (which recurs three more times)."
    - "R-9 (packages/core/districts/prequalified.ts) <-> F-G — the file's own comment at :17-22 records that the 2019 prior-year Championship pre-qualifier list is underivable because 2018 is absent from the corpus, so R-9 is a gap this job closes rather than a new burden it creates."
    - "THE TRAP's three test files <-> F-J's first one-time investment — making those lists registry-driven is the same work, so the trap section and the sequencing section must point at each other."
---

<objective>
Create one new tracked todo, `.planning/todos/pending/extend-corpus-2018-2017-2016.md`,
recording what it takes to extend the SigmaScout corpus back to 2018, then 2017, then 2016.

Purpose: `extend-corpus-2019-2020.md` closed 2026-09-04 and the obvious next question — how
much further back can the corpus go, and what does it cost — was investigated on 2026-09-06 by
read-only probes against TBA and `data/corpus.sqlite`. Those measurements currently live
nowhere. The 2019/2020 job's own history shows the cost of that: it recorded a trap (three
hardcoded test season lists that let an unproven component map ship green) precisely because
the trap had already bitten once, and that trap is guaranteed to recur three more times. An
un-filed investigation is an investigation that gets re-run, and re-running it spends TBA rate
limit to rediscover facts that are already known.

Output: one new markdown file. **Nothing is implemented, ingested, probed, or measured.** No
module is authored, no registry is edited, no ingest runs, no network call is made.

**Why there is no tracer task.** Tracer-first decomposition proves an architecture by wiring
one path through every layer. This plan has one layer — a single markdown file whose only
consumer is a human reader — so a tracer slice would be byte-identical to the task it precedes.
The two tasks below split the file by *region*, not by layer, so they cannot collide on the
same lines. This mirrors `260903-230-PLAN.md`, the direct precedent for a document-only quick
task in this repo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/todos/completed/extend-corpus-2019-2020.md
</context>

## READ THIS FIRST — the findings below are FACTS, not hypotheses

Every number in this plan was measured by the orchestrator on **2026-09-06** via read-only
probes against the TBA API and the local `data/corpus.sqlite`. They are to be **transcribed**
into the todo with their stated sample sizes and provenance date.

**Do not re-run any probe.** Do not query TBA. Do not open `data/corpus.sqlite`. Do not run the
harness, the ingest, or any test that touches the corpus. Re-measuring would spend TBA rate
limit to reproduce numbers this plan already carries, and any small discrepancy between a
re-run and the figures here would be noise presented as a finding.

Where a figure's phrasing is ambiguous, **transcribe the phrasing verbatim** rather than
resolving the ambiguity — an invented resolution is worse than a preserved one, and the person
who runs this job can re-derive it cheaply from the corpus once 2018 is ingested.

## Scope guard

The one failure mode with real cost here is scope creep into implementation. Concretely, this
task does **not**: create or edit any file under `packages/`, `apps/`, `scripts/`, `data/`,
`docs/`, or `pipeline/`; author `breakdown/2018.ts` or `sigma1/rp/2018.ts`; edit any registry
or any test; run `pnpm ingest`, `pnpm harness`, `pnpm tune`, or any vitest; decide the
additivity treatment; decide the `autoQuest` rule; or touch
`.planning/todos/completed/extend-corpus-2019-2020.md`. If the work appears to require any of
those, stop — it does not, and the plan is being misread.

## Planner decisions

**P-1 — One file, named for all three seasons, scoped to 2018 first.** The filename
`extend-corpus-2018-2017-2016.md` states the whole remaining sequence, because the job repeats
three times and F-J's two one-time investments only pay off if the reader knows that up front.
The *content* is scoped to 2018, with 2017 and 2016 named as follow-ons — mirroring how the
precedent file scoped 2019+2020 together while explicitly superseding its own earlier "back to
2016" framing.

**P-2 — `priority: high`, and the file records the tension.** ORCHESTRATOR OVERRIDE of the
planner's `medium`: the user stated the intent directly on 2026-09-06 — *"this is important
because after you add 2018 I will ask you to add 2017 and 2016 as well."* Declared user intent
to do the work is what `priority` records, and it outranks the payoff arithmetic. The file must
still carry the honest counterweight in its prose, because it is the more useful half: F-I's
arithmetic shows adding 2018 promotes exactly one new origin, that origin (2020) is a fragment
season, the real gain is a wider selection window for the thinnest origin, and it carries a
genuine transfer risk. State the priority AND the caveat; do not let the caveat silently become
the priority.

**P-3 — This todo decides nothing about `autoQuest`.** Both options go in with their measured
numbers and the deciding step is named. Deciding it here would be deciding it *without* the
threshold sweep that F-E recommends, which is exactly the "threshold taken from memory of the
game manual" mistake the precedent file records as its 2020 near-miss.

**P-4 — This todo decides nothing about the Scale/additivity treatment.** It is the one part of
the 2018 job with no precedent anywhere in the codebase. It is recorded as open, with the
measurement that makes it tractable (F-C's normalization lever) presented as evidence, not as
a chosen answer.

**P-5 — Two tasks, split by region of one file.** Task 1 writes the frontmatter through the
2018 measurements (what 2018 *is*). Task 2 appends the work list (what to *do*). Neither task
edits the other's region.

<tasks>

<task type="auto">
  <name>Task 1: Create the todo through the 2018 measurements — what 2018 is</name>
  <files>.planning/todos/pending/extend-corpus-2018-2017-2016.md</files>
  <read_first>
`.planning/todos/completed/extend-corpus-2019-2020.md` is the direct structural and stylistic
precedent — read it for its frontmatter shape (`id`, `created`, `source`, `resolves_phase`,
`priority`), its habit of stating measured numbers with their `n`, its use of dated
ANSWERED/DECIDED/MEASURED/CORRECTED section markers, and its scope blockquote directly under
the H1. Match that register: declarative, numeric, willing to say what is not known.

Every figure Task 1 needs is in this plan's action below. Do not go to TBA or the corpus for
any of them.
  </read_first>
  <action>
Create `.planning/todos/pending/extend-corpus-2018-2017-2016.md`. Task 1 writes the file from
the top through the RP-rules section; Task 2 appends everything after it. Do not write Task 2's
sections.

**Frontmatter** — the precedent's five keys, no others:
`id: extend-corpus-2018-2017-2016`, `created: 2026-09-06`,
`source: quick task 260906-813 — investigation filed after extend-corpus-2019-2020 closed 2026-09-04`,
an empty `resolves_phase:`, and `priority: high`.

**H1 and scope blockquote.** Title the file for extending the corpus back to 2018, then 2017,
then 2016. Directly under the H1, a blockquote carrying four things:

1. **Scope: 2018 first.** 2017 (Steamworks) and 2016 (Stronghold) follow, in that order, as
   separate passes. 2018 is deliberately first because it is the hard one (see the additivity
   section) — the two easy seasons should not be done while the hard one's shape is unknown.
2. **2021 is never part of the corpus.** It was the at-home/remote season with no conventional
   3v3 alliance matches, so there is nothing for a match predictor to ingest. State in these
   words that this is a `permanent exclusion`, not a deferral. The precedent file sets this
   rule and it must survive into this one intact.
3. **Provenance.** Every number below was measured on `2026-09-06` by read-only probes against
   the TBA API and `data/corpus.sqlite`. Say plainly that they should be read, not re-measured,
   and that re-running the probes spends TBA rate limit for no gain.
4. **Predecessor.** This file succeeds `.planning/todos/completed/extend-corpus-2019-2020.md`,
   which closed 2026-09-04.

**Section: What this buys, and what it does not.** This is F-I and it must lead, because the
honest answer is "less than it looks" and a reader who learns that on the last screen has
already over-invested. Origins require at least 2 distinct prior seasons in the corpus
(`MIN_PRIOR_SEASONS_FOR_HEADLINE`, `packages/harness/score.ts`).

- Today's corpus — 2019, 2020, 2022, 2023, 2024, 2025, 2026 — yields five origins: 2022, 2023,
  2024, 2025, 2026.
- Adding 2018 promotes **exactly one** new origin: **2020**, which is a 3,806-match fragment
  season. 2019 gains only one prior and stays ineligible.
- The real gain is that the **thinnest origin's selection window roughly doubles**: 2022
  currently selects on 2019 + 2020 = 18,749 official quals; with 2018 it selects on ~33,000.
- **State the risk plainly, in its own bolded line.** 2018 is the one season whose structure
  violates the model's core additivity assumption, so hyperparameters selected partly on 2018
  may transfer *worse* to normal seasons. This is testable with the existing rolling-origin
  harness — compare the origin-2022 parameter set selected with and without 2018 — and that
  comparison should **gate promotion**. Do not assume the backfill is a free win.

**Section: The one hard part — 2018 is anti-additive.** F-A, F-B and F-C. Head this section
with the literal marker `OPEN MODELLING DECISION` so no reader can take it as settled, and say
in the first line that this is the only part of the 2018 job with no precedent anywhere in the
codebase — everything else is data entry.

*F-A, the observation.* `corr(red totalPoints, blue totalPoints)` over official qualification
matches. Present as a table; the seven corpus seasons come from `data/corpus.sqlite`, 2018 from
TBA:

| season | corr(red, blue) totalPoints | n |
|---|---|---|
| 2018 | **-0.4567** | 3,248 qual alliance-sides, 18 events (TBA) |
| 2019 | +0.3282 | 14,929 |
| 2020 | -0.0003 | 3,820 |
| 2022 | +0.3785 | 12,064 |
| 2023 | +0.3461 | 13,558 |
| 2024 | +0.2395 | 14,141 |
| 2025 | +0.5192 | 14,821 |
| 2026 | +0.3592 | 15,191 |

Note under the table that a separate 10-event / 858-match sample gave `-0.4239`, and that the
point is not the magnitude but that **the sign flips** relative to every other season.

*F-B, the cause, measured per component.* The shared Scale. From the 858-match sample,
red-vs-blue correlation within the same match:

| breakdown field | corr(red, blue) | reading |
|---|---|---|
| `teleopScaleOwnershipSec` | **-0.9109** | near-perfectly zero-sum |
| `teleopSwitchOwnershipSec` | -0.1362 | each alliance owns its own Switch — normal |
| `vaultPoints` | +0.0528 | normal |
| `endgamePoints` | +0.1895 | normal |

Explain the mechanism in one line: the Scale is a single shared field element, so a second of
red ownership is a second blue cannot have. Then the contest-intensity figures — red + blue
`teleopScaleOwnershipSec` summed: min 0, p10 96, median 124, p90 136, max 143, mean 118.8,
against 135s of teleop. The Scale is contested essentially the whole match.

*F-C, the normalization lever.* Scale-derived points are
`2*autoScaleOwnershipSec + teleopScaleOwnershipSec + teleopScaleBoostSec`. ORCHESTRATOR
CORRECTION — the planner read these two figures as mutually ambiguous; they are not, and the
todo must state them unambiguously as two DISTINCT statistics over the same sample, never as
one derived from the other: (1) scale-derived points averaged **65.5 points per alliance-side**,
and (2) the mean per-side share of that alliance's own total was **18.8%**. Both are means over
alliance-sides, so 18.8% is a mean of ratios and not `65.5 / mean(total)` — they imply a mean
alliance total in the ~350 range but do not pin it, and the todo must not compute one. Then the
decisive pair:

| quantity | corr(red, blue) |
|---|---|
| `totalPoints` | -0.4567 |
| `totalPoints` minus Scale points | **-0.0642** |

Removing the Scale essentially restores additivity. Close the section by naming why this
matters: OPR, EPA and Sigma1 all assume alliance score = sum of independent per-team
contributions, and 2018 violates that directly — a strong opponent literally subtracts from
your score. Frame the `-0.0642` result as *a lever that exists*, explicitly not as a decision
to pull it; a Scale-normalized 2018 is one candidate treatment among several and the choice
has not been made.

**Section: The 2018 component map is clean.** F-D. Lead with the verified identity, which holds
exactly: `autoRunPoints + autoOwnershipPoints + teleopOwnershipPoints + vaultPoints +
endgamePoints + adjustPoints + foulPoints == totalPoints`. 37 distinct `score_breakdown` keys
were observed. Then, as a short list, the same discipline the 2019 job used:

- **Roll-up hazards, never read:** `autoPoints`, `teleopPoints`, `totalPoints`.
- **Per-robot fields, never read** (Assumption A1 / Pitfall Sigma1-2): `autoRobot1/2/3`,
  `endgameRobot1/2/3`.
- **Diagnostic-only counts:** `foulCount`, `techFoulCount`.

Then the two data-quality notes, each with its disposition:

- **Residual.** 33 of 3,108 official alliance-sides mismatch (**1.06%**), all at -1 or -2. This
  becomes a `KNOWN_TOLERANCES` entry, the same treatment 2019's known mismatches got. Flag that
  it is roughly **10x** 2019's rate (2019 was 1 in 18,051) and therefore deserves a proper
  recon sweep before the module is authored — a tolerance that large should be understood
  before it is accepted.
- **Nulls.** 140 alliance-sides carry null `adjustPoints` and null `autoSwitchAtZero` — **all
  140 from `2018cc` (Chezy Champs), an offseason event.** The existing `tryParseBreakdownPair`
  malformed-degradation path already covers exactly this class; the phase-03 security audit
  measured 1,004 of 4,757 2024 offseason matches failing the same way. **Not a new problem.**

Close with the suggested phase grouping for `groups.ts`: auto = `autoRun`, `autoOwnership`;
teleop = `teleopOwnership`, `vault`; endgame = `endgame`. Note that placing Vault in teleop is
a judgement call of the same class `groups.ts`'s own header already documents for 2023 `link`
and 2025 `algae`.

**Section: The 2018 RP rules are easier than 2019's.** F-E. Two bonuses; base rates over
n=3,248 qual alliance-sides: `autoQuest` **61.98%**, `faceTheBoss` **15.33%**.

*`faceTheBoss` — solved.* `endgamePoints >= 90` gives **100.000% agreement, 0 false positives,
0 false negatives**. Exact, numeric-only, therefore fully reachable from `predictThresholds`.
No conservative branch needed.

*`autoQuest` — a real design choice, and this file does not make it.* State that in bold.
Present both options as a table with their measured numbers:

| option | rule | agreement | FP | FN |
|---|---|---|---|---|
| 1 | `autoRunPoints >= 15 && autoSwitchAtZero` | 96.429% | 0 | 116 |
| 2 | `autoRunPoints >= 15 && autoSwitchOwnershipSec > 0` | 99.569% | 13 | 1 |

Then the tradeoff, one bullet each: Option 1 matches the house never-overstate preference, but
`autoSwitchAtZero` is a boolean and is **unreachable from `predictThresholds`** — which is
2019's `completeRocket` asymmetry problem returning. Option 2 is numeric-only and therefore
reachable, with better agreement and no asymmetry, but it introduces false positives, which
cuts against never-overstate.

Then the fact that points at the resolution: **all 116** of Option 1's false negatives are the
`autoSwitchAtZero === false` case specifically — zero of them were "not all robots ran". So a
threshold sweep on `autoSwitchOwnershipSec`, following the
`scripts/recon-rp-thresholds-2019-2020.ts` pattern, would likely find a cut point that is both
exact and reachable. **Recommend that sweep as the deciding step**, and say the decision waits
for it.

Add the supporting histogram: `autoRunPoints` takes only 0 / 5 / 10 / 15 (counts
12 / 107 / 574 / 2,555) — a clean 3-robot count at 5 points per robot.

Stop here. Task 2 owns everything below.
  </action>
  <verify>
    <automated>T=.planning/todos/pending/extend-corpus-2018-2017-2016.md; test -f "$T" && test "$(grep -cE '^id: extend-corpus-2018-2017-2016$' "$T")" -eq 1 && test "$(grep -cE '^created: 2026-09-06$' "$T")" -eq 1 && test "$(grep -cE '^source: ' "$T")" -eq 1 && test "$(grep -cE '^resolves_phase:' "$T")" -eq 1 && test "$(grep -cE '^priority: ' "$T")" -eq 1 && test "$(grep -cF 'permanent exclusion' "$T")" -ge 1 && test "$(grep -cF '2026-09-06' "$T")" -ge 2 && test "$(grep -cF 'OPEN MODELLING DECISION' "$T")" -ge 1 && test "$(grep -cF -- '-0.4567' "$T")" -ge 2 && test "$(grep -cF -- '-0.4239' "$T")" -ge 1 && test "$(grep -cF -- '-0.9109' "$T")" -ge 1 && test "$(grep -cF -- '-0.0642' "$T")" -ge 1 && test "$(grep -cF '18.8%' "$T")" -ge 1 && test "$(grep -cF 'teleopScaleOwnershipSec' "$T")" -ge 3 && test "$(grep -cF '118.8' "$T")" -ge 1 && test "$(grep -cF 'KNOWN_TOLERANCES' "$T")" -ge 1 && test "$(grep -cF '1.06%' "$T")" -ge 1 && test "$(grep -cF '2018cc' "$T")" -ge 1 && test "$(grep -cF '18,051' "$T")" -ge 1 && test "$(grep -cF '100.000%' "$T")" -ge 1 && test "$(grep -cF '96.429%' "$T")" -ge 1 && test "$(grep -cF '99.569%' "$T")" -ge 1 && test "$(grep -cF 'autoSwitchAtZero' "$T")" -ge 3 && test "$(grep -cF 'predictThresholds' "$T")" -ge 2 && test "$(grep -cF '18,749' "$T")" -ge 1 && test -z "$(git status --porcelain -- packages apps pipeline data docs scripts)" && echo PASS</automated>
  </verify>
  <done>
The new file exists at `.planning/todos/pending/extend-corpus-2018-2017-2016.md` with the
precedent's five frontmatter keys; a scope blockquote naming 2018-first, the 2021 permanent
exclusion, the 2026-09-06 provenance and the predecessor file; the F-I payoff-and-risk section;
the anti-additivity section marked `OPEN MODELLING DECISION` carrying the eight-season
correlation table, the four per-component correlations and the with/without-Scale pair; the
component-map section with the identity, the never-read field lists, both data-quality
dispositions and the `groups.ts` proposal; and the RP section with `faceTheBoss` solved and
both `autoQuest` options presented undecided. `git status` shows zero changes under
`packages`, `apps`, `pipeline`, `data`, `docs` or `scripts`. The gate prints PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the work list — the nine registries, the trap, and the 2017/2016 sequence</name>
  <files>.planning/todos/pending/extend-corpus-2018-2017-2016.md</files>
  <read_first>
Task 1's output, so the appended sections continue its register and do not restate its numbers.
Every path, line anchor and figure Task 2 needs is in this plan's action below — do not grep
the codebase to confirm a line anchor, and do not open the corpus.
  </read_first>
  <action>
Append to the file Task 1 created. Do not edit any region Task 1 wrote.

**Section: The nine registries — no branch, just data entry.** Open by recording that none of
these requires a code branch: both dispatch tables document the extension path, and
`packages/core/algorithms/sigma1/rp/rules.ts`'s header literally says extending back to 2016
(D-19, deferred) is data entry. Then the table, all nine rows, paths and anchors intact:

| # | Registry | Where |
|---|---|---|
| 1 | Score components | new `packages/core/algorithms/breakdown/2018.ts` + entry in `breakdown/index.ts:47` (`SEASON_COMPONENT_MAPS`) |
| 2 | Phase grouping | `packages/core/algorithms/breakdown/groups.ts:78` (`GROUPS_BY_SEASON`) |
| 3 | RP rules | new `packages/core/algorithms/sigma1/rp/2018.ts` + entry in `rp/rules.ts:44` (`RP_RULE_MODULES`) |
| 4 | District point ceilings | `packages/core/districts/pointModel.ts:79` — **three** tables: `DISTRICT_BASE_MAXIMA`, the DCMP weight map, the rookie-bonus map |
| 5 | District display names | `apps/web/src/lib/districtNames.ts` |
| 6 | Web bonus-RP copy | `apps/web/src/lib/bonusRp.ts:36` (`BONUS_RP_BY_SEASON`) |
| 7 | Web season bound | `apps/web/src/lib/seasons.ts:33` — `FIRST_SEASON = 2019` becomes 2018 |
| 8 | Statbotics baseline reference | `packages/harness/statbotics.ts:75` — currently throws for any season outside 2022-2026 |
| 9 | Championship pre-qualified lists | `packages/core/districts/prequalified.ts` |

Two notes under the table:

- **Row 4 carries a real hazard.** 2018's district point values must be **verified against the
  corpus after ingest**, never assumed to carry over from 2019. Three tables that look
  copy-pasteable is exactly the shape that invites a wrong constant to ship quietly.
- **Row 9 closes an existing gap rather than adding a burden.** This is F-G — put it in its own
  short subsection, below.

**Section: THE TRAP — three hardcoded test season lists.** Give this its own heading and make
it visually prominent; it is the single highest-value line in the file. State the consequence
first, in bold: **registering a season without editing all three ships an entirely unproven
component map with a fully green test suite.** Only `rules.test.ts` fails loudly, so it is
self-correcting and the other three are not.

| file | what it hardcodes |
|---|---|
| `packages/core/algorithms/breakdown/groups.test.ts:11` | `const SEASONS = [2019, 2020, 2022, ...]` |
| `packages/core/algorithms/breakdown/reconciliation.test.ts:32` | `const REGISTERED_SEASONS = [...]` |
| `packages/core/districts/pointModel.test.ts:37` | asserts `DISTRICT_REGISTERED_SEASONS` equals a literal |

Note that `packages/core/algorithms/sigma1/rp/rules.test.ts` also carries literals, but it
fails loudly and therefore corrects itself. Record that the precedent file already logged this
trap after the 2019/2020 job, and that it will recur on 2018, 2017 and 2016 — which is the
direct argument for the first one-time investment in the sequencing section below. Cross-link
the two sections.

**Section: Already parameterized — these need NO work.** Short, and worth stating explicitly so
nobody re-does burned-down work. The 2019/2020 job eliminated every hardcoded year on the
replay path: `seasonBoundaryFor` (positional cold start), `parseSeasonsRange` (accepts gapped
lists), `isHeadlineEligible` (no year literals), and the gap-aware season carry.

**Section: District key spellings changed — and this has bitten before.** F-F. 2018's districts
are `chs, fim, in, isr, mar, nc, ne, ont, pch, pnw` (10). 2019's, for comparison, are
`chs, fim, fma, fnc, in, isr, ne, ont, pch, pnw, tx` (11). So **`mar` becomes `fma` and `nc`
becomes `fnc`** — two new key spellings the codebase has never seen — and `tx` did not exist in
2018. This is the identical re-keying class already documented at
`apps/web/src/lib/districtNames.ts:13` for `in` and `tx`, so the pattern for handling it exists;
the point is that registry 5 is not a pure append.

**Section: What adding 2018 closes.** F-G. `packages/core/districts/prequalified.ts:17-22`
records that the 2019 prior-year Championship pre-qualifier list could not be derived *because
2018 is not in the corpus*. Ingesting 2018 supplies that award data, which turns registry 9
from new work into the resolution of a gap the codebase already documents against itself.

**Section: Season size — partly unmeasured, and labelled as such.** F-H. 2018 lists **278
events**, of which **178 are official** (`event_type < 99`). The qualification-match count was
**NOT MEASURED** — write that literal phrase rather than an estimate. For scale reference the
precedent file records 2019 at 14,929 official quals across 189 events that played quals, so
2018 is *expected* to be a full-size season — and label that expectation explicitly as
unmeasured. The precedent file's own habit of correcting an over-pessimistic size guess once
the real numbers landed is the reason to leave this blank rather than fill it in.

**Section: Then 2017, then 2016.** F-J. 2017 (Steamworks) and 2016 (Stronghold) are
conventionally additive games, so the hard season is being done first and the two that follow
should be routine. Because the job repeats three times, two one-time investments are worth
making **up front**, on the 2018 pass:

1. **Make the three hardcoded test season lists registry-driven** — see THE TRAP. Paying this
   once removes the same silent failure from all three remaining passes.
2. **Generalize `scripts/recon-*-2019-2020.ts` into season-parameterized scripts** rather than
   copying them a third time. The recon scripts are the tool that decides thresholds from data
   instead of from memory; a copy-paste fork per season is how one of them quietly stops
   matching the others.

**Section: Relationship to other todos.** Short list: the predecessor
`.planning/todos/completed/extend-corpus-2019-2020.md` (closed 2026-09-04) for the structure,
the 2021 rule and the trap's first sighting; and `retune-sigma1-rolling-origin` /
`rolling-origin-hyperparameter-tuning`, whose origin lists and selection windows both change if
this job lands — with F-I's gating comparison named as the thing that must run before any
promotion built on a 2018-inclusive corpus.

Leave the file in `.planning/todos/pending/`. Do not move it to `completed/`, and do not edit
`.planning/todos/completed/extend-corpus-2019-2020.md`.
  </action>
  <verify>
    <automated>T=.planning/todos/pending/extend-corpus-2018-2017-2016.md; for k in SEASON_COMPONENT_MAPS GROUPS_BY_SEASON RP_RULE_MODULES DISTRICT_BASE_MAXIMA districtNames.ts BONUS_RP_BY_SEASON FIRST_SEASON statbotics.ts prequalified.ts groups.test.ts reconciliation.test.ts pointModel.test.ts rules.test.ts seasonBoundaryFor parseSeasonsRange isHeadlineEligible MIN_PRIOR_SEASONS_FOR_HEADLINE recon-rp-thresholds-2019-2020.ts; do grep -qF "$k" "$T" || { echo "MISSING $k"; exit 1; }; done; test "$(grep -cF 'NOT MEASURED' "$T")" -ge 1 && test "$(grep -cF '178' "$T")" -ge 1 && test "$(grep -cF '278' "$T")" -ge 1 && test "$(grep -cF '`fma`' "$T")" -ge 1 && test "$(grep -cF '`fnc`' "$T")" -ge 1 && test "$(grep -cF 'prequalified.ts:17-22' "$T")" -ge 1 && test "$(grep -cF 'Stronghold' "$T")" -ge 1 && test "$(grep -cF 'Steamworks' "$T")" -ge 1 && test "$(grep -cF 'extend-corpus-2019-2020' "$T")" -ge 1 && test "$(ls .planning/todos/pending | wc -l)" -eq 1 && test ! -f .planning/todos/completed/extend-corpus-2018-2017-2016.md && test -z "$(git status --porcelain -- .planning/todos/completed)" && test -z "$(git status --porcelain -- packages apps pipeline data docs scripts)" && echo PASS</automated>
  </verify>
  <done>
The file carries all nine registry rows with their paths and line anchors; THE TRAP section
naming all three silent-failure test files plus the loud one; the already-parameterized list;
the `mar`/`fma` and `nc`/`fnc` re-keying note; the `prequalified.ts:17-22` gap closure; the
event counts with the qualification-match count marked `NOT MEASURED`; the 2017/2016 sequence
with both one-time investments; and the related-todos list. Exactly one file exists in
`.planning/todos/pending/`, nothing was added to `completed/`, the precedent file is unchanged,
and `git status` shows zero changes under `packages`, `apps`, `pipeline`, `data`, `docs` or
`scripts`. The gate prints PASS.
  </done>
</task>

</tasks>

<verification>
Run from the repo root after both tasks.

```bash
T=.planning/todos/pending/extend-corpus-2018-2017-2016.md

# 1. Re-run both tasks' <automated> gate lines together.

# 2. Nothing outside .planning/ was touched by this task's commits.
test -z "$(git show --pretty=format: --name-only HEAD | grep -E '^(packages|apps|pipeline|data|docs|scripts)/')" && echo "scope OK"

# 3. Exactly one new file, in pending, and the precedent is untouched.
#    (Commit count is not assumed — the precedent is checked by path, not by revision range.)
test -f "$T" \
  && test "$(ls .planning/todos/pending | wc -l)" -eq 1 \
  && test -z "$(git log --oneline -3 --name-only --pretty=format: \
       | grep -F 'todos/completed/extend-corpus-2019-2020.md')" \
  && echo "placement OK"

# 4. No probe was run: no new script, report, or corpus artifact appeared.
test -z "$(git status --porcelain -- scripts reports data)" && echo "no probes OK"
```

Note on the grep gates: the usual `grep -v '^#'` comment-stripping hygiene is deliberately
**not** applied. The target is markdown, where `#` lines are headings — they are the content
being gated, not commentary about it. Every gate is a fixed-string or anchored match on a
literal the action specifies the file must contain, so ordinary prose cannot satisfy one by
accident. Every text gate is positive (must-contain); the only negative assertions are
structural (`git status` emptiness, file placement), so no gate can be tripped by the executor
transcribing this plan's own wording.

Final read-through (human, one pass): open the todo top to bottom and confirm two things — that
the additivity treatment and the `autoQuest` rule both read as genuinely open, and that no
number appears without its sample size or its provenance date.
</verification>

<success_criteria>
- One new file exists: `.planning/todos/pending/extend-corpus-2018-2017-2016.md`, with the
  precedent's frontmatter shape (`id`, `created`, `source`, `resolves_phase`, `priority`).
- Findings A through J are all recorded, each with the sample size and the 2026-09-06
  provenance the orchestrator supplied, and none re-measured.
- All nine registries appear with file paths and line anchors, and THE TRAP's three silently
  failing test files are called out prominently as the highest-risk item.
- The anti-additivity treatment and the `autoQuest` rule are both recorded as OPEN, each with
  its evidence and its named deciding step. A reader cannot mistake either for decided.
- 2021 reads as a permanent exclusion. It is never framed as a season awaiting ingest.
- Finding H's qualification-match count reads as unmeasured, not as an estimate.
- Scope is 2018 first, with 2017 and 2016 explicitly named as the follow-on sequence.
- Exactly one file changed. No source, script, data or docs file was modified. No ingest ran,
  no probe ran, no network call was made.
</success_criteria>

<output>
Write `.planning/quick/260906-813-write-a-tracked-todo-for-extending-the-c/260906-813-SUMMARY.md`
when done. Commit with a docs-scoped message, e.g.
`docs(todos): file the 2018/2017/2016 corpus extension investigation`.
</output>
