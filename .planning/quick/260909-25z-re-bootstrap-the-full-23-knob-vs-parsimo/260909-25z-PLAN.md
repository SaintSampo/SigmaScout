---
task: "Re-bootstrap the full 23-knob vs parsimonious BPR gap on the post-P3 unified measurement path, and decide whether the 2023 test is worth firing"
slug: re-bootstrap-the-full-23-knob-vs-parsimo
quick_id: 260909-25z
phase: quick-260909-25z
plan: "01"
date: 2026-09-09
mode: quick
type: execute
wave: 1
depends_on:
  - 260908-b4t-fresh-2023-blind-model
  - 260908-vqr-adversarial-review-of-the-bpr-algorithm
  - 260909-03b-bpr-top-three-moves-unify-measurement-pa
autonomous: true
requirements: [F-20, DECISION-R5, RULE-A]
files_modified:
  - packages/bpr/holdout.ts
  - packages/bpr/holdout.test.ts
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/RULE.md
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/scripts/paired-contrast.ts
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/IDENTIFY.txt
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/GUARDS-PROOF.txt
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-A.txt
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-B.txt
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/RETUNE.txt
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/retune-full.json
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/retune-parsimonious.json
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/VERDICT.md
  - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/260909-25z-SUMMARY.md
  - .planning/STATE.md

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The full-versus-parsimonious gap carries its OWN direct paired event-clustered confidence interval, not a point estimate held up against the generic 0.169pp ablation bar."
    - "Both variants are scored on the post-P3 unified path (shared population, D-Q3 accuracy convention). No pre-P3 figure is mixed into a post-P3 comparison."
    - "The comparison is SYMMETRIC: each arm pairs a full variant against a parsimonious variant tuned under the SAME objective. Any mixed-objective number is labelled a diagnostic, never the answer."
    - "The accept/reject rule is committed to git BEFORE the first contrast number is produced, and the commit order is provable from git log."
    - "Rule A governs: a candidate counts only if accuracy AND Brier both improve. Accuracy alone is not a pass."
    - "F-20 receives an explicit corrected verdict naming the number it now rests on - stands or inverts, said outright."
    - "The reconstructed full variant does NOT search obsSdSlope, so it is genuinely the 23-knob variant and carries no knob motivated by the 2023 holdout glance (F-16)."
    - "No hyperparameter, threshold, or accept/reject decision in this task was computed from a season later than 2022, and no 2023+ accuracy, Brier or log loss was read."
    - "packages/bpr/frozen-params.json is untouched - still exactly one commit."
    - "No model code changed. packages/bpr/model.ts and packages/core/algorithms/bpr.ts are byte-identical to HEAD at task start."
  artifacts:
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/RULE.md
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/IDENTIFY.txt
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/GUARDS-PROOF.txt
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-A.txt
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-B.txt
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/RETUNE.txt
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/VERDICT.md
    - .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/260909-25z-SUMMARY.md
  key_links:
    - "scripts/paired-contrast.ts -> packages/harness/eventBootstrap.ts eventBlockedBootstrap (the one tested resampler; no fourth hand-rolled copy)"
    - "scripts/paired-contrast.ts -> packages/bpr/holdout.ts meanAccuracyDelta/meanBrierDelta (the SAME paired statistics a 2023 runner would use, so a design-era CI and a holdout CI are the same quantity)"
    - "scripts/paired-contrast.ts -> packages/bpr/cli.ts evalDesign plus EvalExtra.onScored (the post-P3 unified path, not a private replay loop)"
    - "RULE.md commit -> reports/260909-25z jsonl mtimes (pre-registration is provable by ordering, not by assertion)"
    - "VERDICT.md -> DECISION.md rule 5 (the reversal condition is checked against its own stated quantity, not against a remembered constant)"
---

<objective>
Re-measure the full 23-knob versus parsimonious BPR gap on the post-P3 unified
measurement path, with a **direct paired event-clustered bootstrap on that
specific contrast**, and issue a corrected verdict on review finding **F-20**.

Purpose: F-20 was marked KILLED against an *estimated* 0.45pp bar derived from a
MARGINAL standard error. Task `260909-03b` then measured the correct quantity —
the paired event-clustered two-sigma bar is **0.169pp**, 2.6x tighter. The
parsimonious model was frozen because it scored 0.238pp below the full variant,
which was inside the pre-registered 0.31pp threshold. Against 0.169pp that gap is
no longer inside anything, so **F-20's verdict plausibly inverts**. Both of those
numbers were also measured on the pre-P3 path and are stale. The correction has
to be measured, not asserted.

Output: a pre-registered decision rule committed before any number exists; two
symmetric paired contrasts with their own 95% intervals on accuracy AND Brier; an
explicit corrected F-20 verdict; and a recommendation on whether to fire the 2023
test — with the firewall-erosion and parameter-bloat tensions stated honestly
rather than treated as settled by "it clears the bar".

**The executor does NOT run the 2023 test.** This task decides whether it is worth
firing. Jacob fires it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/BAR.md
@.planning/quick/260908-b4t-fresh-2023-blind-model/DECISION.md
@packages/bpr/tune.ts
@packages/bpr/cli.ts
@packages/bpr/holdout.ts
@packages/harness/eventBootstrap.ts
@.planning/quick/260909-03b-bpr-top-three-moves-unify-measurement-pa/scripts/paired-bar.ts
</context>

<banked_reconnaissance>

Read this before doing anything. It corrects the task brief on one load-bearing
point and answers three open questions, so none of it needs rediscovering.

## CORRECTION: the full 23-knob variant DOES exist on disk

The brief said the full variant "DOES NOT EXIST ON DISK" and that the task must
reconstruct it from scratch. That claim was scoped to `packages/bpr/*.json`, and
it is wrong one directory over.

**`.planning/quick/260908-b4t-fresh-2023-blind-model/round5-full.json` is the full
23-knob variant.** Evidence, all confirmed at plan time:

- Every component the parsimonious model pins to inert is ACTIVE in it:
  `foulOn: true`, `elimWeight: 0.5`, `defPriorVar: 0.005`, `huberK: 2.5`,
  `biasLr: 0.001`.
- It carries no `obsSdSlope` key. That knob was added post-seal (F-16), so its
  absence dates the file to the 23-knob era exactly.
- `git log --diff-filter=A` puts it in commit **`214f66f5`**, `2026-09-08
  01:18:12`, titled *"test(bpr): model tests, side-bias term, and the
  pre-committed freeze rule"* — the same commit that added `DECISION.md` and
  `parsimonious-seed.json`. `frozen-params.json` was sealed **2m39s later** in
  `a66688af` at `01:20:51`. The full variant, the parsimonious seed and the
  freeze rule were committed together, immediately before the freeze.

`fair-reset-control.json` and `hetero-seed.json` in the same directory carry the
same `_selected` comment string but are NOT the full variant — do not mistake
them for it. Task 1 re-verifies the identification independently before anything
is built on it.

## The parsimonious side has a matching pre-P3 optimum

`packages/bpr/frozen-params.json` IS the re-tuned parsimonious variant
(`DECISION.md` rule 4). `parsimonious-seed.json` is what it was seeded from — the
two differ on `qSlow`, `priorVar`, `rookieMean`, `seasonVar` and `tauLr`, which is
the re-tune having happened. So each side has a recorded pre-P3 optimum:

| side | pre-P3 optimum |
|---|---|
| full 23-knob | `.planning/quick/260908-b4t-fresh-2023-blind-model/round5-full.json` |
| parsimonious | `packages/bpr/frozen-params.json` — SEALED, read only, never edit |

## `tune.ts` is deterministic — confirmed, no fallback branch needed

There is no RNG anywhere in `packages/bpr/tune.ts`. `GRID` is a fixed literal in
fixed order, `better()` is a pure comparison, and `score()` calls `evalDesign`
over a module-cached, deterministically ordered match list. Same seed plus same
code plus same corpus reproduces the same trajectory. The brief's "if it is not
reproducible, that is itself a finding" branch does not fire — but a post-P3 tune
WILL land somewhere different from the pre-P3 one, because P3 changed the
objective (population plus accuracy convention), not the search.

## HAZARD: `obsSdSlope` is now in the tuning grid and must be skipped

`tune.ts:83` added `obsSdSlope` to `GRID` after the seal. That knob was diagnosed
from **2023 weeks 0-1** — F-16, a disclosed holdout glance. A full re-tune that
searched it would (a) stop being the 23-knob variant and (b) import a
holdout-motivated knob into a design-era decision. **Every tune in this task
passes a `BPR_SKIP_KEYS` list that includes `obsSdSlope`.** Not optional.

## The paired statistics already exist, unexported

`packages/bpr/holdout.ts` already has `meanAccuracyDelta` and `meanBrierDelta`
over a `Paired` unit, already wired to `eventBlockedBootstrap`, already reporting
combined / champs / non-champs. They are module-private. `holdout.ts` is NOT in
`SEALED_CODE_PATHS` (that list is `model.ts`, `evaluate.ts`, `data.ts`, `cli.ts`,
`core/algorithms/bpr.ts`), so adding exports touches no sealed path and does not
disturb the seal. `holdout.ts`'s `main()` is entry-guarded, so importing it is
safe — `holdout.test.ts` already imports `resolveYears` from it.

## The emission hook already exists

`EvalExtra.onScored` at `packages/bpr/cli.ts:43` was added by P3 for exactly this:
per-scored-match `(match, pRed)`, called after the prediction and before the
update, unable to influence either. `evalDesign(params, { onScored })` is all the
contrast script needs — no private replay loop.

## Guards, confirmed closed at plan time

`equivalence.ts:49` halts after 2022 without `--include-holdout`;
`slice.ts:95-98` defaults the year argument to 2022 and refuses past it without
`--allow-holdout`; `tune.ts:36` and `score.ts:24` reject holdout years. Task 1
re-proves all four by running them rather than by reading them.

## Cost and environment

- One design-era eval is seconds; a 3-sweep coordinate descent is roughly 340
  evals, so minutes not hours. Two tunes fit comfortably.
- `reports/` is **gitignored**. Machine-readable jsonl goes to
  `reports/260909-25z/`; human-readable evidence is copied into this quick
  directory and committed, exactly as `260909-03b` did with `BAR-OUTPUT.txt`.
- `.planning/quick/**/scripts/*.ts` is NOT in the root `tsconfig.json` include
  list (`packages/**`, `scripts/**`, `vitest.config.ts`). The contrast script is
  validated by running it, not by `tsc`.
- **Another session is active in this checkout** — `git status` at plan time
  showed a dozen modified `apps/web` files belonging to someone else. Stage every
  commit by EXPLICIT PATH. Never `git add -A`, never `git add .`, never
  `git commit -a`.
- Never `timeout N pnpm ...` — it swallows output and exits 0. Use `npx vitest run`
  and `npx tsx`, and judge by OUTPUT, not by exit code.
- A quiet tune log is not a dead tune. Check the log file's mtime and line count
  before concluding anything died.

</banked_reconnaissance>

<tasks>

<task type="tracer">
  <name>Task 1: Pre-register the rule, prove the guards, and measure Arm A end-to-end</name>
  <precondition>`data/corpus.sqlite` exists and is readable (gitignored, roughly 582MB). Every measurement in this plan halts without it.</precondition>
  <files>
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/RULE.md,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/IDENTIFY.txt,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/GUARDS-PROOF.txt,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/scripts/paired-contrast.ts,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-A.txt,
    packages/bpr/holdout.ts,
    packages/bpr/holdout.test.ts
  </files>
  <behavior>
    - `meanAccuracyDelta` and `meanBrierDelta` become importable from `packages/bpr/holdout.ts` with no change to what they compute.
    - A unit where the candidate calls a match right and the incumbent calls it wrong contributes +1 to the accuracy delta numerator.
    - A tied match is excluded from the accuracy denominator entirely but still counted in the Brier mean's denominator.
    - Reversing incumbent and candidate flips the sign of both deltas and nothing else.
    - The contrast script's printed delta is oriented FULL minus PARSIMONIOUS in every arm.
  </behavior>
  <action>
Do these in order. The ORDER IS THE POINT for steps 1 through 3 — a decision rule
written after its own numbers are visible is not a rule.

**Step 1 — write and COMMIT `RULE.md` before any contrast exists.**

Fix the orientation convention first and use it everywhere afterwards:
delta equals FULL minus PARSIMONIOUS. A positive accuracy delta means the full
variant is better; a NEGATIVE Brier delta means the full variant is better.

`RULE.md` states, in this order:

- The question: should the 2023 test fire for the full 23-knob variant?
- The measurement: paired event-clustered bootstrap over whole event blocks,
  paired by `matchKey`, 2000 resamples, `eventBlockedBootstrap`'s default seed, on
  the post-P3 design-era path (2016-2022). The contrast carries its OWN 95%
  percentile interval; `BAR.md`'s generic 0.169pp ablation bar is quoted for
  orientation only and is not the instrument.
- The three arms:
  - **Arm A (as-recorded, symmetric):** `round5-full.json` against
    `frozen-params.json`. Both were tuned under the pre-P3 objective; both are
    scored under the post-P3 one. Neither side was tuned to the path scoring it.
  - **Arm B (re-tuned, symmetric):** post-P3 re-tuned full against post-P3
    re-tuned parsimonious. Both tuned under the same current objective.
  - **Arm C (diagnostic only):** post-P3 re-tuned full against
    `frozen-params.json`. MIXED objective — one side tuned to the scoring path and
    one side not — so it structurally favours the full side, the same inflation
    pattern F-23 names for un-re-tuned ablations. Reported in one line, labelled a
    diagnostic, never quoted as the answer.
- **FIRE the 2023 test only if ALL of:**
  1. *Rule A* — accuracy delta above zero AND Brier delta below zero. Accuracy
     alone is not a pass. If the full variant wins accuracy but loses Brier, that
     is a decisive negative and the test does not fire.
  2. *Signal* — the accuracy delta's 95% percentile interval EXCLUDES ZERO.
  3. *Consistency* — conditions 1 and 2 hold on **both** Arm A and Arm B. If the
     arms disagree in sign, or one excludes zero and the other does not, the
     result is unstable to the objective change and does not warrant spending
     holdout.
- Otherwise: DO NOT FIRE, and F-20's corrected verdict is recorded in whichever
  direction the numbers point.
- **The 2023 candidate, fixed in advance:** if the rule fires, the candidate is
  the post-P3 re-tuned full variant — the one tuned under the same measurement
  path that would score the test. Naming it now removes any post-hoc choice
  between two full variants after the numbers are visible.
- A closing paragraph separating MEASUREMENT from RECOMMENDATION: clearing the
  rule establishes that the gap is real; it does not by itself establish that
  acting on it is wise. Task 3 argues that separately.

Note in `RULE.md` that Rule A is a TIGHTENING relative to `DECISION.md` rule 1,
which was accuracy-only with a calibration carve-out at rule 2. Adding a condition
that makes passing harder is not threshold shopping, and saying so here — before
the numbers — is what makes that claim checkable later.

Commit `RULE.md` by explicit path NOW, before step 5 produces anything.

**Step 2 — verify the guards are still armed.** Run all four and capture the
refusals verbatim into `GUARDS-PROOF.txt`:

- `npx tsx packages/bpr/score.ts packages/bpr/frozen-params.json 2023`
- `BPR_TUNE_YEARS=2023 npx tsx packages/bpr/tune.ts` (throws at module load,
  before any evaluation, so it costs nothing)
- `npx tsx packages/bpr/holdout.ts packages/bpr/frozen-params.json` (no
  `--break-seal`)
- `npx tsx packages/bpr/slice.ts` with a 2023 year argument and no
  `--allow-holdout`

Each must THROW. Record the exact message text. Also record `equivalence.ts`'s
halt-after-2022 default by grepping its flag and its break line. If any guard
fails to refuse, STOP the task and report it — that is a bigger finding than
anything else in this plan.

**Step 3 — verify the full-variant identification independently.** Do not take the
banked reconnaissance on faith. Write `IDENTIFY.txt` recording: the
`git log --diff-filter=A --format='%h %ad %s' --date=iso` line for
`round5-full.json` and for `parsimonious-seed.json`; the `git log` line for
`frozen-params.json`; a `git show --stat 214f66f5` confirming `DECISION.md`
shipped in that same commit; and a printed knob profile of `round5-full.json`
showing every component the parsimonious model pins to inert is active in it, and
that no `obsSdSlope` key is present. State plainly in the file that the
identification rests on that evidence and NOT on a reproduced pre-P3 accuracy
figure — the pre-P3 code path no longer exists at HEAD, so 0.73319 is not
reproducible today and this task does not attempt it.

**Step 4 — export the paired statistics.** In `packages/bpr/holdout.ts`, add
`export` to `meanAccuracyDelta`, `meanBrierDelta`, and the `Paired` interface.
Nothing else in that file changes — no behavior, no signature, no `main()` path.
Reuse rather than copy so that the design-era interval produced here and the
holdout interval a 2023 run would produce are the SAME quantity computed by the
SAME code, which is what makes one a valid pre-registration for the other. Then
extend `packages/bpr/holdout.test.ts` with assertions pinning both statistics on a
small hand-computed fixture, covering the four cases named in this task's
`behavior` block.

**Step 5 — build `scripts/paired-contrast.ts`** in this quick directory (relative
imports go up four levels to the repo root, exactly as
`260909-03b/scripts/paired-bar.ts` does). Signature: an arm label, an INCUMBENT
params path (the parsimonious side), a CANDIDATE params path (the full side). It
calls `evalDesign` twice with `onScored` to collect per-match `pRed` keyed by
`matchKey`, joins them into `Paired` units carrying `eventKey`, `season`,
`compLevel`, `eventType` and `actualWinner`, writes those units as jsonl to
`reports/260909-25z/<label>.jsonl`, and bootstraps with `eventBlockedBootstrap` at
2000 resamples using the imported `meanAccuracyDelta` and `meanBrierDelta`. For
each of combined / champs (eventType 2 through 4) / non-champs it prints: n, the
event-block count, the accuracy delta in pp with its standard error, its 95%
percentile interval and whether that interval excludes zero, and the Brier delta
with its 95% interval. It also prints each side's own level accuracy and Brier via
`formatResult`, so both variants' absolute design-era figures land on the record
next to the difference. Its header comment states the orientation convention and
that the reported delta is FULL minus PARSIMONIOUS.

**Step 6 — run Arm A** and tee the output to `ARM-A.txt`, passing
`packages/bpr/frozen-params.json` as incumbent and
`.planning/quick/260908-b4t-fresh-2023-blind-model/round5-full.json` as candidate.
Do NOT interpret the result here. Task 3 applies the rule.

Absolute prohibitions for this task: do not edit
`packages/bpr/frozen-params.json`; do not touch `packages/bpr/model.ts` or
`packages/core/algorithms/bpr.ts`; do not read or print any 2023 or later
accuracy, Brier or log loss; and never `Read`, `cat` or `echo` `.env` or any value
read from it. Stage every commit by explicit path.
  </action>
  <verify>
    <automated>npx vitest run packages/bpr</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "dAcc" .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-A.txt</automated>
    <automated>git log --oneline -- packages/bpr/frozen-params.json | wc -l</automated>
    <automated>git status --porcelain -- packages/bpr/model.ts packages/core/algorithms/bpr.ts packages/bpr/frozen-params.json</automated>
  </verify>
  <done>
`RULE.md` is committed and its commit timestamp precedes every file in
`reports/260909-25z/`. All four guards threw and their messages are in
`GUARDS-PROOF.txt`. `IDENTIFY.txt` records the commit-ordering and knob-profile
evidence for `round5-full.json`. `npx vitest run packages/bpr` passes with the new
`holdout.test.ts` assertions, and `npx tsc --noEmit` is clean. `ARM-A.txt` holds an
accuracy delta in pp with a 95% interval and a Brier delta with a 95% interval,
for combined, champs and non-champs. `git log` on `frozen-params.json` returns
exactly 1. `git status --porcelain` on `model.ts`, the shipped port and
`frozen-params.json` returns empty output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconstruct both variants by symmetric post-P3 re-tune, then measure Arms B and C</name>
  <files>
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/retune-full.json,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/retune-parsimonious.json,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/RETUNE.txt,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-B.txt
  </files>
  <action>
Re-tune BOTH sides under the post-P3 objective. Re-tuning only the full side and
comparing it against the pre-P3-tuned `frozen-params.json` would be exactly the
asymmetry F-23 names — one side fitted to the scoring path, the other not — and it
inflates the full side's apparent advantage. Symmetry is the requirement, not a
nicety.

**Both tunes seed from their own side's recorded pre-P3 optimum**, which is the
precise mirror of what `DECISION.md` rule 4 did within a single objective:

- **Full 23-knob:** seed `BPR_SEED` from
  `.planning/quick/260908-b4t-fresh-2023-blind-model/round5-full.json`, skip
  `obsSdSlope` only, `BPR_SWEEPS=3`. Output to
  `reports/260909-25z/retune-full.json`, stderr to
  `reports/260909-25z/retune-full.log`.
- **Parsimonious:** seed `BPR_SEED` from `packages/bpr/frozen-params.json` — READ
  it as a seed, never write to it — and skip the whole dropped-component set plus
  `obsSdSlope`: `foulObsSd`, `foulQ`, `foulPriorVar`, `elimWeight`, `defPriorVar`,
  `defQ`, `huberK`, `biasLr`, `obsSdSlope`. `BPR_SWEEPS=3`. Output to
  `reports/260909-25z/retune-parsimonious.json`, stderr to
  `reports/260909-25z/retune-parsimonious.log`.

`foulOn` is a boolean and is not in `GRID`, so it is never searched — it is pinned
by the seed. `frozen-params.json` already carries `foulOn: false`, so the
parsimonious re-tune inherits the correct pinning; assert that rather than assume
it.

Run both with `npx tsx packages/bpr/tune.ts` and shell-level env prefixes. These
are minutes-scale, not hours-scale. If a log goes quiet, check its mtime and line
count before concluding it died — a silent log is not a dead process.

When both finish, copy each tune's stderr trajectory and its final `design` block
into `RETUNE.txt`, alongside: which knobs each run skipped, which seed each used,
and the diff between each output's params and its own seed (so a reader can see
what the objective change actually moved). Assert and record that neither output
file carries a non-zero `obsSdSlope`.

Then run the Task 1 contrast script twice more:

- **Arm B (governing, symmetric):** incumbent `retune-parsimonious.json`,
  candidate `retune-full.json`. Tee to `ARM-B.txt`.
- **Arm C (diagnostic only):** incumbent `packages/bpr/frozen-params.json`,
  candidate `retune-full.json`. Append its combined line to `ARM-B.txt` under a
  heading marking it MIXED-OBJECTIVE and NOT the governing arm.

Copy `retune-full.json` and `retune-parsimonious.json` into this quick directory
and commit them by explicit path — `reports/` is gitignored, so the parameter sets
would otherwise not survive the task. Do not place either file in
`packages/bpr/`; nothing here is being promoted.

Do not interpret any of it. Task 3 applies the rule. Still no 2023 or later
figure is read, still nothing is republished, still no model file is touched.
  </action>
  <verify>
    <automated>node -e "const f=require('./reports/260909-25z/retune-full.json'),p=require('./reports/260909-25z/retune-parsimonious.json');if((f.params.obsSdSlope||0)!==0||(p.params.obsSdSlope||0)!==0)throw new Error('slope was searched');if(p.params.foulOn!==false)throw new Error('parsimonious foul pin lost');console.log('SEEDS-AND-PINS-OK')"</automated>
    <automated>grep -c "dAcc" .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-B.txt</automated>
    <automated>grep -ci "MIXED-OBJECTIVE" .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/ARM-B.txt</automated>
    <automated>git log --oneline -- packages/bpr/frozen-params.json | wc -l</automated>
    <automated>git status --porcelain -- packages/bpr/model.ts packages/core/algorithms/bpr.ts packages/bpr/frozen-params.json</automated>
  </verify>
  <done>
Both re-tunes completed and their trajectories, seeds, skip lists and
seed-versus-output param diffs are recorded in `RETUNE.txt`. Neither output
carries a non-zero `obsSdSlope`, and the parsimonious output still pins
`foulOn: false`. `ARM-B.txt` holds the governing symmetric contrast — accuracy
delta in pp with a 95% interval and a Brier delta with a 95% interval, for
combined, champs and non-champs — plus the Arm C line explicitly labelled
mixed-objective. Both parameter files are committed into the quick directory by
explicit path. `frozen-params.json` still returns exactly 1 from `git log`, and
`git status --porcelain` on the two model paths and the frozen params returns
empty output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply the pre-registered rule, issue the corrected F-20 verdict, and recommend for or against firing the 2023 test</name>
  <files>
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/VERDICT.md,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/TEST-2023.md,
    .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/260909-25z-SUMMARY.md,
    .planning/STATE.md
  </files>
  <action>
Write `VERDICT.md`. Apply `RULE.md` verbatim as committed — do not restate it in
softer or harder terms now that the numbers are visible. Structure it as:

**1. The measurement table.** One row per arm (A, B, C) with n, event-block count,
accuracy delta in pp with its 95% interval and its excludes-zero flag, and Brier
delta with its 95% interval. Both variants' absolute design-era accuracy and Brier
alongside. State the pre-P3 figures (73.081 parsimonious, 73.319 full, 0.238pp
gap) once as history, explicitly marked pre-P3 and NOT comparable to the new
numbers — record them beside the new values rather than letting them vanish, but
never mix the two eras inside one comparison.

**2. Rule application, condition by condition.** Rule A, Signal, Consistency —
each stated pass or fail with the number it turned on. Then the rule's own
verdict: FIRE or DO NOT FIRE. If the full variant wins accuracy but loses Brier,
say outright that this is a decisive negative under Rule A and that accuracy alone
would have been the wrong instrument.

**3. Corrected verdict on F-20, stated outright.** F-20 concluded KILLED — "the
parsimonious was 0.238pp below the full model against a 0.31pp bar; under 0.45pp
it is further inside; decision stands, more comfortably." Say explicitly whether
that verdict **STANDS** or **INVERTS**, and name the number it now rests on. Note
that F-20's reasoning was structurally sound but ran on the LEVEL bar; `BAR.md`
measured the level quantity at 0.445pp, reproducing the review's estimate almost
exactly, so the error was in which quantity was being bounded and not in the
arithmetic. Note also that this task does not compare a point estimate to a
generic bar at all — the contrast carries its own interval, which is the stronger
instrument, and record whether the two approaches agree.

**4. `DECISION.md` rule 5, checked against its own stated quantity.** Rule 5 says
the full model is frozen instead if the parsimonious variant's design accuracy is
"more than 0.31pp below" it. Work through both readings and give both to the
reader:
- Rule 5 as a CONSTANT (0.31pp literally): does the measured gap clear it?
- Rule 5 as its own DEFINITION ("two standard errors", which is what rule 1 calls
  the 0.31pp): under the correct paired standard error the same definition yields
  a different threshold, and correcting a mis-derived constant to the quantity its
  own text names is a correction rather than a rule change.

Then state the counter-argument honestly and without hedging: the correction was
discovered after the fact, and moving a threshold in the direction that changes
the outcome is the textbook forking-paths move regardless of how principled the
derivation is. Do not resolve this by picking the flattering reading — present
both and say which you recommend and why.

**5. Recommendation, with the tensions stated.** Weigh at least these, and do not
treat "it clears the bar" as automatically decisive:
- Adopting the full variant means acting on a changed pre-registered threshold,
  which is precisely the move that erodes a firewall. Against that: rule 5 is
  itself pre-registered, so executing it is not inventing a new rule.
- It cuts against the standing no-parameter-bloat rule — 23 knobs against a lean
  set, where five of the extra components measured under 0.1pp individually and
  the repo has an independent negative result on `elimWeight` (2026-09-05, 6/6
  keep-incumbent).
- F-23: individual ablation deltas were never re-tuned, so each component's credit
  is an upper bound. Whatever the full variant's aggregate advantage turns out to
  be, its per-component story is weaker than the ablation table suggests.
- **Holdout exposure.** 2023 was already read once, in `260908-b4t`'s single
  sealed run covering 2023 through 2026. A candidate-versus-incumbent 2023 run is
  a legitimate new test of a NEW candidate, but 2023 is not virgin data and the
  recommendation must say so rather than imply a clean out-of-sample season is on
  offer. Do not look up or restate any 2023 or later figure while writing this.

**6. If and only if the rule FIRES**, write `TEST-2023.md` as a pre-registration,
BEFORE anything runs and as a separate committed file: the exact candidate file
path; the exact incumbent; the exact command, which is
`packages/bpr/holdout.ts` with `--candidate`, `--years 2023`, `--out` and
`--break-seal`, noting that `--years` binds `stopAfterYear` so 2024 is
structurally unreachable; the sealed paths that must be clean at HEAD first; and
the accept/reject rule in the same Rule-A shape as `RULE.md` — accuracy delta
above zero AND Brier delta below zero, accuracy interval excluding zero, single
shot, whatever it prints is the reported result. **The executor does not run it.**
If the rule does NOT fire, do not write `TEST-2023.md` at all and say in
`VERDICT.md` that no 2023 command is handed over.

Then write `260909-25z-SUMMARY.md`. The Write tool is blocked from creating
SUMMARY.md — produce it with a Bash heredoc, or return the text and have it
written for you. Cover: the corrected F-20 verdict in the first two lines; the
three arms' numbers; the rule application; the recommendation with its tensions;
an explicit "the holdout was not touched" section listing the guard refusals,
`frozen-params.json`'s commit count, and the absence of any 2023 output file; and
a note that the briefed reconnaissance was corrected on the location of the full
23-knob variant.

Finally append one row to `.planning/STATE.md`'s quick-task table. Re-read the
table tail immediately before editing — another session is active and may have
appended since. **No raw pipe characters anywhere in the description text**, not
even escaped: one pipe breaks appends for every future row. Stage by explicit
path.
  </action>
  <verify>
    <automated>grep -ciE "F-20.*(STANDS|INVERTS)|(STANDS|INVERTS).*F-20" .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/VERDICT.md</automated>
    <automated>grep -ciE "FIRE|DO NOT FIRE" .planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/VERDICT.md</automated>
    <automated>test ! -f reports/bpr-2023-test.jsonl && echo "NO-2023-OUTPUT-OK"</automated>
    <automated>git log --oneline -- packages/bpr/frozen-params.json | wc -l</automated>
    <automated>git status --porcelain -- packages/bpr/model.ts packages/core/algorithms/bpr.ts packages/bpr/frozen-params.json</automated>
    <automated>npx vitest run packages/bpr packages/harness</automated>
  </verify>
  <done>
`VERDICT.md` exists with all six sections, states the corrected F-20 verdict as
STANDS or INVERTS naming the number it rests on, applies each of Rule A / Signal /
Consistency with its measured value, works both readings of `DECISION.md` rule 5
including the forking-paths counter-argument, and gives a recommendation that
weighs firewall erosion, parameter bloat, F-23's upper-bound caveat and 2023's
prior exposure. `TEST-2023.md` exists if and only if the rule fired, and was not
executed. `reports/bpr-2023-test.jsonl` does not exist. `frozen-params.json`
returns exactly 1 from `git log`; `git status --porcelain` on the two model paths
and the frozen params returns empty output. `npx vitest run packages/bpr
packages/harness` passes apart from the known `seasonParamSets.test.ts` 5s-default
flake logged as out of scope by `260909-03b`. `260909-25z-SUMMARY.md` and the
`STATE.md` row are written, with no pipe characters in the description.
  </done>
</task>

</tasks>

<verification>

Run after all three tasks:

1. **Holdout untouched.** `git log --oneline -- packages/bpr/frozen-params.json`
   returns exactly 1. `reports/bpr-2023-test.jsonl` does not exist. All four guard
   refusals are transcribed in `GUARDS-PROOF.txt`.
2. **No model change.** `git status --porcelain --` on `packages/bpr/model.ts` and
   `packages/core/algorithms/bpr.ts` returns empty, and neither appears in this
   task's commits. If either changed, the task's own stated expectation broke —
   flag it loudly in the SUMMARY rather than absorbing it.
3. **Pre-registration ordering is provable.** The `RULE.md` commit predates every
   file in `reports/260909-25z/` and every arm output file.
4. **Symmetry.** `RETUNE.txt` shows each side seeded from its own pre-P3 optimum
   and each skip list recorded; `ARM-B.txt` labels Arm C mixed-objective.
5. **No `obsSdSlope` search.** Both re-tune outputs carry slope zero or no key.
6. **Tests and types.** `npx vitest run packages/bpr packages/harness` passes
   apart from the known `seasonParamSets.test.ts` flake; `npx tsc --noEmit` clean.
7. **Secrets.** No `.env` read, no secret value in any transcript, commit message
   or artifact.

</verification>

<success_criteria>

- The full-versus-parsimonious gap has a direct paired event-clustered 95%
  interval on accuracy AND on Brier, measured post-P3, on two symmetric arms.
- F-20 has an explicit corrected verdict naming the number it rests on.
- `DECISION.md` rule 5 is evaluated under both the constant reading and the
  definition reading, with the forking-paths objection stated rather than
  side-stepped.
- A FIRE / DO NOT FIRE recommendation exists with its tensions argued, not
  asserted.
- If FIRE: one pre-registered, unexecuted 2023 command with an accept/reject rule
  written before it runs. If DO NOT FIRE: no 2023 command handed over at all.
- The holdout is provably unspent by this task and no model code changed.

</success_criteria>

<output>
Create `.planning/quick/260909-25z-re-bootstrap-the-full-23-knob-vs-parsimo/260909-25z-SUMMARY.md` when done.
</output>
