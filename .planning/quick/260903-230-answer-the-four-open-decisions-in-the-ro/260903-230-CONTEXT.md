# Quick Task 260903-230: Answer the four open decisions in `rolling-origin-hyperparameter-tuning` - Context

**Gathered:** 2026-09-03
**Status:** Ready for planning

<domain>
## Task Boundary

Answer the four open decisions in the "Decision briefing" section of
`.planning/todos/pending/rolling-origin-hyperparameter-tuning.md` and record the answers in
that same todo. This is a **documentation-only** task — it records decisions, it does not
implement them. No code changes, no searches run, no republish.

</domain>

<decisions>
## Implementation Decisions

All four answered by the user on 2026-09-03. Each took the recommended option.

### D-1 — Go/when: **Sequence onto the re-tune**

This todo's labeling + versioning change lands together with the promotion and republish
produced by `retune-sigma1-rolling-origin`. One republish (~56k PUTs, ~25 min), not two, and
the season labels get written knowing which origins actually cleared the D-T7 acceptance bar.

Rejected: doing the labeling standalone now (costs a second republish, and writes labels
before the acceptance verdicts exist); keeping the whole thing deferred.

### D-2 — Versioning representation: **One version, per-season sets**

`vpr@6.0.0+rolling-YYYY-MM` carrying `paramSetsBySeason` plus one prediction-stream digest
over the full replay. Seasons whose origin returned `keep-incumbent` fall back to the
incumbent set. `v1/manifest/algorithms.json`, the Worker, and every client keep their current
one-version-per-algorithm contract unchanged; only `promote.ts`'s file shape and the digest
definition move.

**Version-number correction:** the todo proposed `vpr@3.0.0+rolling-YYYY-MM`, which was written
when `vpr@2.1.0+tuned-2026-08` was current. The promoted set is now
`data/algorithm-versions/vpr@5.0.0+tuned-2026-08.json`, so 3.0.0 would be a downgrade. The
target is **6.0.0**.

Rejected: three promoted version files (one per origin) — matches `promote.ts`'s current
one-set-per-file shape, but forces the manifest, the Worker, and every client to learn
per-season version resolution.

### D-3 — What the live site runs in 2027: **The 2026-origin set**

Selected on 2022–2025; the most recent set that was also honestly evaluated out-of-sample.
If the 2026 origin returns `keep-incumbent`, the incumbent stands and serves live.

Rejected: an extra search over all 2022–2026 (more data, but never honestly evaluable —
nothing is out-of-sample for it, so it could never carry a headline claim); deferring the
decision until after the re-tune reports.

### D-4 — Is 2023 headline-eligible on a one-season prior: **Not eligible**

Headline seasons are **2024 / 2025 / 2026** — three, which meets acceptance criterion 2's
">= 3" bar. 2022 and 2023 are labeled selection-only/untuned and carry no headline claim.

This ratifies what the shipped tuner already does rather than changing it: `tune.ts`'s origins
are 2024/2025/2026 with 2022–2023 as the earliest selection window, so no 2023 parameter set
exists to score.

Rejected: adding a 2023 origin tuned on 2022 alone — four headline seasons, but two more joint
search runs and it weakens the one-screen leak-free argument the current run shape rests on.

### Claude's Discretion

- Exact placement and heading structure of the recorded answers inside the todo. Preference:
  replace the four "Decision N — ..." subsections' option lists with the decided answer in
  place, so the file cannot be read as still-open, and keep the rejected alternatives as
  recorded rationale rather than deleting them.
- Whether to update the todo's frontmatter `priority` field.

</decisions>

<specifics>
## Specific Ideas

Three factual corrections found while preparing the briefing. All three should be recorded in
the todo alongside the answers, because the todo's own text is now misleading on each:

1. **The rolling-origin machinery already shipped.** Quick task `260901-trz` (D-T5) built
   `--origin` mode into `packages/harness/tune.ts` with four leakage gates, plus
   `packages/harness/acceptance.ts`'s D-T7 bar. The todo is written as though none of it
   exists. What remains in *this* todo is the scoring/labeling/versioning half:
   `packages/harness/score.ts:16-19` still holds `TUNE_SEASONS`/`HOLDOUT_SEASONS`, and
   `SeasonLabel` at `score.ts:14` is still `"tune" | "holdout"`.
2. **Acceptance criterion 1 is already met.** "A test proves a search cannot read a season at
   or after its evaluation target" — `tune.ts`'s gates and `tune.test.ts` cover this. The
   criterion should be marked as satisfied by `260901-trz` rather than left as open work.
3. **The cost figures are stale.** The todo estimates "four hyperparameter searches ...
   ~2–3.5h total". `retune-sigma1-rolling-origin` has measured numbers for the shipped run
   shape: one ~41-min screen at the earliest origin's window, plus six concurrent joint runs
   (3 origins x 2 adaptation arms) at ~70 min wall clock.

</specifics>

<canonical_refs>
## Canonical References

- `.planning/todos/pending/rolling-origin-hyperparameter-tuning.md` — the file being edited
- `.planning/todos/pending/retune-sigma1-rolling-origin.md` — the compute job D-1 sequences
  onto; its "Promotion, afterwards" section already names D-2 as its gating decision
- `.planning/todos/pending/remeasure-baseline-fingerprint-post-trz.md` — runs after that
  republish
- `packages/harness/tune.ts`, `packages/harness/acceptance.ts` — the shipped rolling-origin
  selection machinery
- `packages/harness/score.ts`, `packages/harness/artifact.ts:51`,
  `packages/harness/pageArtifacts.ts:1219` — the `seasonLabel` enum sites this todo still owns

</canonical_refs>
