---
quick_id: 260903-230
phase: quick-260903-230
slug: answer-the-four-open-decisions-in-the-ro
date: 2026-09-03
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: true
depends_on: []
source: .planning/quick/260903-230-answer-the-four-open-decisions-in-the-ro/260903-230-CONTEXT.md
requirements: [D-1, D-2, D-3, D-4]
files_modified:
  - .planning/todos/pending/rolling-origin-hyperparameter-tuning.md

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A reader opening the todo sees in its first ten lines that all four gating decisions are answered, and that the work is sequenced onto retune-sigma1-rolling-origin per D-1 rather than runnable standalone."
    - "Each of D-1, D-2, D-3 and D-4 is recorded as a decided answer, with the alternatives it beat preserved underneath as rationale rather than deleted."
    - "The promoted-version target in the todo reads vpr@6.0.0+rolling-YYYY-MM with paramSetsBySeason (D-2); the superseded 3.0.0 proposal survives only as a recorded correction, never as the target."
    - "The todo credits quick task 260901-trz for the shipped rolling-origin selection machinery and marks acceptance criterion 1 satisfied by it, instead of describing that work as still to be built."
    - "The todo's compute-cost figures describe the shipped run shape (one screen at the earliest origin's window plus six concurrent joint runs) and no longer carry the retired four-sequential-searches estimate."
    - "No source, data, or docs file changes — the diff is one markdown file under .planning/."
  artifacts:
    - ".planning/todos/pending/rolling-origin-hyperparameter-tuning.md (edited in place; stays in pending/, is NOT moved to completed/)"
  key_links:
    - "The todo's D-1 sequencing note <-> retune-sigma1-rolling-origin.md's 'Promotion, afterwards' section, which already names this todo's Decision 2 as its own gating decision. After this edit that pointer resolves to an answer instead of an open question."
    - "The recorded D-2 answer <-> the enum sites the todo still owns: packages/harness/score.ts:14-19 (SeasonLabel, TUNE_SEASONS, HOLDOUT_SEASONS) and seasonLabel at artifact.ts:51 / pageArtifacts.ts:1219."
---

<objective>
Record the four answered decisions, and three factual corrections, inside
`.planning/todos/pending/rolling-origin-hyperparameter-tuning.md`.

Purpose: the todo is the last open item from the 2026-08-31 sweep and it currently reads
as blocked on four undecided questions. All four were answered by the user on 2026-09-03
(see `260903-230-CONTEXT.md`). Until those answers are written into the todo itself, the
next reader — including a future agent picking up `retune-sigma1-rolling-origin`, whose
"Promotion, afterwards" section points *at* this todo's Decision 2 — re-opens settled
ground. Three of the todo's own factual claims are also now wrong, and a stale fact reads
exactly like an open question.

Output: one edited markdown file. **Nothing is implemented.** No searches run, no promotion,
no republish, no schema change, no source edit.

**Why there is no tracer task:** tracer-first decomposition proves an architecture by wiring
one path through every layer. This plan has one layer — a single markdown file with no
consumers other than a human reader — so a tracer slice would be identical to the task it
precedes. The two tasks below split the file by *region*, not by layer, so they cannot
collide on the same lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260903-230-answer-the-four-open-decisions-in-the-ro/260903-230-CONTEXT.md
@.planning/todos/pending/rolling-origin-hyperparameter-tuning.md
@.planning/todos/pending/retune-sigma1-rolling-origin.md
</context>

## Facts verified at planning time (do not re-derive)

These were checked against the working tree on 2026-09-03. Use them; do not go re-grep the
codebase to confirm them.

| Claim | Verified state |
|---|---|
| Current promoted version file | `data/algorithm-versions/vpr@5.0.0+tuned-2026-08.json` (also `vpr@5.0.0+tracer-check.json`). The todo's `vpr@2.1.0+tuned-2026-08.json` reference and its `3.0.0` proposal are both behind it. |
| `packages/harness/score.ts:14` | `export type SeasonLabel = "tune" \| "holdout";` — unchanged, still owned by this todo |
| `packages/harness/score.ts:17-19` | `TUNE_SEASONS = [2022, 2023, 2024]`, `HOLDOUT_SEASONS = [2025, 2026]` — unchanged, still owned by this todo |
| `artifact.ts:51`, `pageArtifacts.ts:1219` | both still `z.enum(["tune", "holdout"])` |
| Shipped run shape | `retune-sigma1-rolling-origin.md` — one screen at the 2022-2023 window (~41 min), then six concurrent joint runs (3 origins x 2 adaptation arms) at ~70 min wall clock, `--evals 40 --batch 4` |
| Todo frontmatter | `priority: medium` |

## Scope guard

The one failure mode with real cost here is scope creep into implementation. Concretely, this
task does **not**: touch `score.ts`, `artifact.ts`, `pageArtifacts.ts`, `promote.ts`, or
`searchSpace.ts`; run `pnpm tune` or `pnpm promote`; regenerate any artifact; move the todo to
`completed/`; or edit `retune-sigma1-rolling-origin.md`. If the work seems to require any of
those, stop — it does not, and the plan is being misread.

<tasks>

<task type="auto">
  <name>Task 1: Convert the decision briefing into four recorded answers</name>
  <files>.planning/todos/pending/rolling-origin-hyperparameter-tuning.md</files>
  <read_first>
    `260903-230-CONTEXT.md`'s `<decisions>` block is the source of truth for all four answers,
    including their rejected alternatives and D-2's version-number correction. Copy the
    substance from there; do not re-derive or re-argue any of it.
  </read_first>
  <action>
Rewrite the file's final section — everything from the `## Decision briefing` heading to the
end of the file — so it reads as a settled record. Leave the rest of the file for Task 2.

Retitle the section so its heading itself states the outcome, e.g.
`## Decisions — ANSWERED 2026-09-03 (all four; none open)`, and replace the heading's stale
parenthetical (which currently promises the section is unstarted) with a lead paragraph
naming quick task `260903-230` as where the answers were given, and stating in bold that no
decision in this todo is open.

Replace the four numbered decision subsections. Each becomes a heading of the exact form
`### D-N — {question}: ANSWERED — {the answer}` for N in 1..4, so an anchored grep finds
exactly four of them. Under each heading write (a) the decided answer in prose, drawn from
CONTEXT, and (b) a `**Rejected:**` line preserving the alternatives it beat, as recorded
rationale — the reasoning is why the answer holds up, so deleting it would cost more than it
saves. Remove the per-option preference italics that made the old list read as a menu; the
answer is now the heading.

The four answers, per CONTEXT:
- **D-1** — sequence this todo's labeling and versioning change onto the promotion and
  republish produced by `retune-sigma1-rolling-origin`. One republish (~56k PUTs, ~25 min),
  not two, and the season labels get written knowing which origins actually cleared D-T7.
- **D-2** — one version carrying per-season sets: `vpr@6.0.0+rolling-YYYY-MM` with
  `paramSetsBySeason` plus a single prediction-stream digest over the full replay; seasons
  whose origin returned `keep-incumbent` fall back to the incumbent set. State explicitly
  that `v1/manifest/algorithms.json`, the Worker and every client keep their current
  one-version-per-algorithm contract unchanged — only `promote.ts`'s file shape and the
  digest definition move. Record the version-number correction as its own visible note: the
  briefing proposed 3.0.0 back when 2.1.0 was current, the promoted set is now
  `vpr@5.0.0+tuned-2026-08.json`, so 3.0.0 would be a downgrade and the target is **6.0.0**.
- **D-3** — the live 2027 site runs the 2026-origin set (selected on 2022-2025), the most
  recent set that was also honestly evaluated out-of-sample; if the 2026 origin returns
  `keep-incumbent`, the incumbent stands and serves live.
- **D-4** — 2023 is not headline-eligible. Headline seasons are 2024 / 2025 / 2026 — three,
  which meets acceptance criterion 2's ">= 3" bar. 2022 and 2023 are labeled
  selection-only/untuned and carry no headline claim. Note that this ratifies what the
  shipped tuner already does: `tune.ts`'s origins are 2024/2025/2026 with 2022-2023 as the
  earliest selection window, so no 2023 parameter set exists to score.

Also inside this section:
- **Correct the cost figures** (CONTEXT correction 3). Keep the cascade paragraph — the full
  harness re-run, the republish, the byte-pinned `compare-*.json` fixtures, the SC-3
  re-measurement, the rewind-gap re-measurement, the `sigma1-tuning-results.md` rewrite are
  all still real and still owed. Replace only the search-cost estimate: the retired range
  assumed four sequential full searches; the shipped shape measured in
  `retune-sigma1-rolling-origin` is one screen at the earliest origin's window (~41 min) plus
  six concurrent joint runs at ~70 min wall clock. Point at that todo as the authority rather
  than restating its whole cost table.
- **Update the closing "Also implied" paragraph.** The `seasonLabel` enum change is still
  owed and still a published-contract change — keep it, and per D-1 note it rides the
  re-tune's republish. But its leakage-test sentence is now historical: that test shipped in
  `260901-trz`. Say so here rather than leaving it as pending work.

Keep the file in `.planning/todos/pending/`. Do not touch anything above the section you are
rewriting; Task 2 owns that region.
  </action>
  <verify>
    <automated>T=.planning/todos/pending/rolling-origin-hyperparameter-tuning.md; test "$(grep -cE '^### D-[1-4] .*ANSWERED' "$T")" -eq 4 && test "$(grep -cE '^### Decision [0-9]' "$T")" -eq 0 && test "$(grep -cF '*Recommended' "$T")" -eq 0 && test "$(grep -cF 'nothing below has been executed' "$T")" -eq 0 && test "$(grep -cF '**Rejected:**' "$T")" -ge 4 && test "$(grep -cF 'vpr@6.0.0+rolling-' "$T")" -ge 1 && test "$(grep -cE '3\.5h' "$T")" -eq 0 && test "$(grep -cF 'paramSetsBySeason' "$T")" -ge 1 && echo PASS</automated>
  </verify>
  <done>
The final section is titled as answered, contains exactly four `### D-N ... ANSWERED`
headings each followed by a `**Rejected:**` rationale line, names `vpr@6.0.0+rolling-` with
`paramSetsBySeason` as the D-2 target, carries no menu-style option preference markers, no
numbered-decision headings in the old form, no unexecuted-section claim in the header, and no
retired multi-hour sequential-search estimate. The gate command prints PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the todo's body and flag its status at the top</name>
  <files>.planning/todos/pending/rolling-origin-hyperparameter-tuning.md</files>
  <read_first>
    `260903-230-CONTEXT.md`'s `<specifics>` block (the three factual corrections) and this
    plan's "Facts verified at planning time" table. Both are authoritative; do not re-grep the
    codebase to confirm them.
  </read_first>
  <action>
Edit the file's frontmatter and everything above the section Task 1 rewrote. The body was
written on 2026-08-30 and now contradicts shipped reality in three places — and a stale fact
sitting in a todo reads exactly like open work, which is the same failure this whole task
exists to fix.

1. **Frontmatter:** change `priority: medium` to `priority: high`. Per D-1 this now rides
   `retune-sigma1-rolling-origin` (itself `priority: high`), and leaving it at medium invites
   a future backlog reviewer to run the re-tune and its republish without the labeling and
   versioning half — the exact ordering D-1 was decided to prevent. Change no other
   frontmatter key; do not add new ones.

2. **A status blockquote immediately under the H1**, so the state is visible before any
   scrolling. It must contain the literal phrase `all four gating decisions ANSWERED`, point
   down to the answered-decisions section, and state that per D-1 this work is sequenced onto
   `retune-sigma1-rolling-origin` and is not to be run standalone.

3. **"The proposal" table** (CONTEXT correction 1 plus D-4). Its `2023 | 2022` row describes a
   rolling origin that the shipped tuner deliberately does not have. Keep the row — the table
   is the original argument and stays readable — but annotate that row with the literal text
   `not an origin (D-4)`. Add a sentence under the table recording that the rolling-origin
   selection machinery this section proposes **already shipped** in quick task `260901-trz`
   (`--origin` mode in `packages/harness/tune.ts` with four leakage gates, plus
   `packages/harness/acceptance.ts`'s D-T7 bar), and that what remains in *this* todo is the
   scoring, labeling and versioning half: `TUNE_SEASONS`/`HOLDOUT_SEASONS` at
   `packages/harness/score.ts:16-19`, `SeasonLabel` at `score.ts:14`, and the `seasonLabel`
   enum at `artifact.ts:51` and `pageArtifacts.ts:1219`.

4. **"Known costs"** (CONTEXT correction 3 and the version fact). The four-sequential-searches
   bullet is superseded — replace its estimate with a pointer to the shipped run shape in
   `retune-sigma1-rolling-origin` (one screen plus six concurrent joint runs), and adjust
   "four parameter sets" to the three origins the shipped tuner actually has. In the
   versioning-collision bullet, the promoted-set filename it cites is two majors behind:
   overwrite it outright with `data/algorithm-versions/vpr@5.0.0+tuned-2026-08.json` — do not
   keep the old filename alongside it as a was/now pair, since the D-2 answer already carries
   that version history — and note the collision is now resolved by D-2 rather than open.
   Leave the "biggest single cost"
   framing intact — it was right, and D-2 is the answer to it, not a refutation.
   In the final bullet, the live-2027 question is no longer undecided: point it at D-3.

5. **"Acceptance criteria"** (CONTEXT correction 2). Criterion 1's leakage-test requirement is
   already met by `tune.ts`'s gates and `tune.test.ts`. Mark that criterion with the literal
   text `SATISFIED (260901-trz)`. Leave criteria 2 through 5 as open work — they are.

Do not edit the section Task 1 rewrote, do not move the file out of `pending/`, and do not
edit `retune-sigma1-rolling-origin.md` — its "Promotion, afterwards" section already points
here and needs no change.
  </action>
  <verify>
    <automated>T=.planning/todos/pending/rolling-origin-hyperparameter-tuning.md; test "$(grep -cE '^priority: high$' "$T")" -eq 1 && test "$(grep -cF 'all four gating decisions ANSWERED' "$T")" -ge 1 && test "$(grep -cF 'not an origin (D-4)' "$T")" -ge 1 && test "$(grep -cF 'SATISFIED (260901-trz)' "$T")" -ge 1 && test "$(grep -cF '260901-trz' "$T")" -ge 3 && test "$(grep -cF 'vpr@5.0.0+tuned-2026-08.json' "$T")" -ge 1 && test "$(grep -cF 'vpr@2.1.0' "$T")" -eq 0 && test -z "$(git status --porcelain -- packages apps pipeline data docs scripts)" && echo PASS</automated>
  </verify>
  <done>
Frontmatter reads `priority: high`; a status blockquote under the H1 carries the
`all four gating decisions ANSWERED` phrase and the D-1 sequencing; the proposal table's 2023
row is annotated `not an origin (D-4)` and is followed by the 260901-trz shipped-machinery
note; Known costs cites `vpr@5.0.0+tuned-2026-08.json` with no reference to the two-majors-old
filename; acceptance criterion 1 is marked `SATISFIED (260901-trz)`; and `git status` shows
zero changes under packages, apps, pipeline, data, docs or scripts. The gate prints PASS.
  </done>
</task>

</tasks>

<verification>
Run from the repo root after both tasks.

```bash
T=.planning/todos/pending/rolling-origin-hyperparameter-tuning.md

# 1. Both task gates still pass together (re-run each task's <automated> line).

# 2. Nothing outside .planning/ was touched by this task's commits.
test -z "$(git show --pretty=format: --name-only HEAD | grep -E '^(packages|apps|pipeline|data|docs|scripts)/')" && echo "scope OK"

# 3. The todo is still pending, not completed.
test -f "$T" && test ! -f .planning/todos/completed/rolling-origin-hyperparameter-tuning.md && echo "still pending"
```

Note on the grep gates: the usual `grep -v '^#'` comment-stripping hygiene is deliberately
**not** applied here. The target is markdown, where `#` lines are headings — they are the
content being gated, not comments about it. Every gate above is an anchored or fixed-string
match on a literal the edit is specified to produce or remove, so prose cannot satisfy one by
accident.

Final read-through (human, one pass): open the todo top to bottom and confirm it cannot be
read as having open decisions. The rejected alternatives should read as *why this answer*,
never as *pick one*.
</verification>

<success_criteria>
- All four decisions from `260903-230-CONTEXT.md` are recorded in the todo as answers, each
  with its rejected alternatives preserved as rationale.
- All three factual corrections from CONTEXT's `<specifics>` are recorded: 260901-trz shipped
  the selection machinery, acceptance criterion 1 is satisfied, the cost figures are the
  measured shipped-run-shape ones.
- The D-2 version target is 6.0.0, and the reason 3.0.0 was wrong is on the page.
- A reader who has never seen `260903-230-CONTEXT.md` cannot mistake any of the four
  decisions for still-open.
- Exactly one file changed. No code, data, or docs file was modified. Nothing was implemented.
</success_criteria>

<output>
Write `.planning/quick/260903-230-answer-the-four-open-decisions-in-the-ro/260903-230-SUMMARY.md`
when done. Commit with a docs-scoped message, e.g.
`docs(todos): answer the four rolling-origin tuning decisions, correct three stale facts`.
</output>
