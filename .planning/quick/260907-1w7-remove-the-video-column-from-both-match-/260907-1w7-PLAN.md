---
phase: quick-260907-1w7
plan: 01
title: Unwire the Video column from both match tables
status: complete
mode: inline
tasks: 1
---

# Quick Task 260907-1w7: Unwire the Video column

## Ask

> "remove the video column from sigma scout. dont tear out any of the backend work, I might come back to this."

## Decision (user, 2026-09-07)

Presented two removal depths. User chose **"Unwire only — keep the components."**

| Layer | Disposition |
|---|---|
| `packages/ingest`, `packages/corpus`, `packages/harness` | **KEEP** — unambiguously backend; not touched |
| `MatchVideoCell.tsx` + test | **KEEP**, unrendered |
| `lib/matchVideo.ts` + test (YouTube-id allowlist) | **KEEP**, unrendered |
| `eventMatchAxis.ts` `video` row field | **KEEP** |
| `.match-video-*` in `theme.css` | **KEEP** — see deviation below |
| Column wiring in the two tables | **REMOVE** |

## Deviation from the presented option

The chosen option's own preview listed `theme.css .match-video-*` under REMOVED. It is kept
instead. The rules are inert while nothing mounts `MatchVideoCell`, so deleting them buys no
visible change, and it would contradict the property the option was chosen for — that re-enabling
is about six lines in two files rather than six lines plus forty of CSS. Flagged to the user in
the same turn.

## Task 1 — remove the wiring

**Files:**
- `apps/web/src/components/event/EventMatchTable.tsx`
- `apps/web/src/components/event/EventMatchTable.test.tsx`
- `apps/web/src/components/team/MatchTable.tsx`
- `apps/web/src/components/team/MatchTable.test.tsx`

**Action:** drop the `MatchVideoCell` import, the `<td data-testid={video-*}>`, and the
`<th>Video</th>` from both tables; drop `"Video"` from `EVENT_MATCH_TABLE_HEADERS` and take
`EVENT_MATCH_TABLE_COLUMN_COUNT` from 7 to 6 so the header and the skeleton cannot disagree;
delete the two `describe("Video column (quick task 260906-7eu)")` blocks.

**Verify:**
- `npx vitest run apps/web/src/components` from the repo root — green, INCLUDING the retained
  `MatchVideoCell.test.tsx`, which proves the component survived the unwire intact.
- `npx tsc --noEmit -p apps/web/tsconfig.json` clean (root `tsc` does not cover `apps/web`).
- `grep -i video` returns 0 in both test files and 0 in `team/MatchTable.tsx`.
- Visual: no Video column on the team page or the event page's Qualifications tab.

**Done:** both tables render one fewer column; nothing under `packages/` changed.
