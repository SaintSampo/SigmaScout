---
phase: quick-260911-j2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/methodology/epaComparisonContent.ts
  - apps/web/src/components/methodology/epaComparisonContent.test.ts
  - scripts/measureEpaDeviations.ts
  - docs/models/epa-divergences.md
  - docs/models/epa-statbotics-gap.md
  - packages/core/algorithms/types.ts
  - packages/corpus/db.ts
  - packages/core/algorithms/epaWeekOne.ts
  - packages/core/algorithms/epaWeekOne.test.ts
  - packages/core/algorithms/epa.ts
  - packages/core/algorithms/epa.test.ts
  - packages/core/algorithms/epaCarryScale.ts
  - packages/harness/stateSnapshot.ts
  - apps/worker/src/scheduled.ts
  - .planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/260911-j2w-SUMMARY.md
autonomous: true
requirements: [QUICK-260911-j2w]

estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "None of the three named surfaces claims Statbotics rates one number per alliance; each instead says both sides rate a per-team list and the difference is which entries the predicted score READS."
    - "The published methodology page's corrected text is pinned by an assertion in epaComparisonContent.test.ts, so the claim cannot silently return."
    - "EPA_DIFFERENCE_IDS is byte-identical to its pre-task value."
    - "Every completed and scheduled match reaching an algorithm carries its event's TBA competition week, or an explicit null, and no construction site supplies a silent default."
    - "An explicit test pins Statbotics' week 1 to this corpus's week === 0, verified against corpus data rather than asserted from memory."
    - "EPA's win-probability denominator and its carry scale anchor both read a FROZEN week-1 aggregate from week 2 onward, and the live expanding estimate only during week 1 itself."
    - "No match played before week 1 ends can read the frozen aggregate, so the replay carries no walk-forward violation of its own."
    - "Winner accuracy and Brier are reported per season and pooled, event-blocked, before versus after, and a regression is printed rather than avoided."
    - "epa.version is bumped and STATE_SNAPSHOT_SHAPE_VERSION is bumped with a doc block naming the silent live/offline divergence a stale league row would cause."
  artifacts:
    - packages/core/algorithms/epaWeekOne.ts
    - packages/core/algorithms/epaWeekOne.test.ts
    - .planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/before-epa-deviation-ablation.json
    - .planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/after-epa-deviation-ablation.json
  key_links:
    - "events.week (corpus) -> selectMatchesChronological / selectScheduledMatches -> UpcomingMatch.week -> epa.update"
    - "tbaEventSchema.week -> apps/worker/src/scheduled.ts toMatchResult/toUpcomingMatch -> the SAME epa.update the publisher calls"
    - "week-1 fold/freeze state -> EpaState league row -> stateSnapshot.ts serialize/deserialize -> STATE_SNAPSHOT_SHAPE_VERSION gate"
    - "frozen week-1 SD -> epa.predict norm-diff denominator (replaces standardDeviation(allianceScoreStats, ...))"
    - "frozen week-1 mean -> carryRescaleRatio numerator (replaces cleanSeasonMean's unwind, from week 2 on)"
---

<objective>
Correct a false claim that is live on a user-facing page and in two internal
surfaces, then retarget EPA's two live-estimated season scalars at the WEEK 1
aggregates Statbotics actually uses.

Purpose: Part A removes a published statement the recovered source disproves.
Part B is the single largest available step toward reproducing Statbotics' EPA,
and it is available precisely because a week-1 aggregate is knowable once week 1
ends — so adopting it from week 2 onward costs no walk-forward legitimacy.

Output: three corrected surfaces (own commit), a `week` field plumbed end to end
with its off-by-one pinned by test (own commit), and a week-1-calibrated
`epa@9.0.0+baseline` with an honestly-reported before/after measurement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/STATE.md

@docs/models/statbotics-breakdown-reference.md
@docs/models/epa-statbotics-gap.md
@docs/models/epa-divergences.md
@packages/core/algorithms/epa.ts
@packages/core/algorithms/epaCarryScale.ts
@packages/core/scoring/expandingStats.ts
@packages/core/algorithms/types.ts
@packages/corpus/db.ts
@scripts/measureEpaDeviations.ts
@apps/web/src/components/methodology/epaComparisonContent.ts
</context>

<standing_prohibitions>
Binding on EVERY task below. A violation is a task failure, not a judgement call.

1. **Do not touch BPR/SPR.** Nothing under `packages/bpr/`, `experiments/phase-bpr/`,
   or any SPR-labelled surface changes behaviour. Its sealed 2016-2022 / 2023-2026
   holdout is NOT spent, NOT read, and NOT run. If `tsc` names a file under
   `packages/bpr/` or `experiments/phase-bpr/` in Task 2, add the one field it asks
   for and change nothing else in that file. `BprMatch` is its own interface and
   already carries `week` (`packages/bpr/data.ts:23-29`), so it should not appear at
   all — if it does, that is a signal to re-read, not to edit BPR logic.
2. **Do not run a republish.** `pnpm publish:seasons`, `publish.ts --event`, any R2
   write, and the `sigmascout-retune-republish` skill are all out of scope. Part B
   changes published ratings and therefore OWES a republish; record that debt in the
   SUMMARY and in STATE.md's activity line, and stop there.
3. **Do not change `EPA_DIFFERENCE_IDS`.** Its three-id set is a locked decision and
   the question of removing it is the developer's, still unanswered. Not a fourth
   entry, not a reorder, not a rename.
4. **Do not tune.** No sweep, no candidate set, no threshold search, no arm selection
   against any season. Reproduction fidelity is the goal. A winner-accuracy or Brier
   REGRESSION is an acceptable, expected and reportable outcome — report it plainly
   and do not reach for a knob to cancel it. If a number looks bad, that is the
   result, not a bug to fix.
5. **Never render `.env` into any output stream** (`.claude/CLAUDE.md`, Secrets
   handling). No task here needs a secret; if one seems to, stop and ask.
6. **Stage by explicit pathspec only.** Another session is active in this checkout
   with untracked work under `.planning/phases/09-*`. Never `git add -A`, never
   `git add .`; always `git commit -- <explicit paths>`. Run `git status` after each
   commit and confirm nothing foreign was absorbed.
7. **Worktrees stay disabled.** `data/corpus.sqlite` is 582 MB and gitignored; a
   worktree cannot see it.
8. **Verify by reading output, not by exit code.** `timeout <n> pnpm <cmd>` swallows
   output and exits 0 on this machine. Use `npx vitest run ...` and `npx tsx ...`
   directly, and read what they print.
</standing_prohibitions>

<tasks>

<task type="tracer">
  <name>Task 1: Correct the one-number-per-alliance claim on all three surfaces (own commit)</name>
  <files>apps/web/src/components/methodology/epaComparisonContent.ts, apps/web/src/components/methodology/epaComparisonContent.test.ts, scripts/measureEpaDeviations.ts, docs/models/epa-divergences.md</files>
  <read_first>
    - `docs/models/epa-statbotics-gap.md` "Stage 1 — Correct the three surfaces still carrying the disproven claim" — it names each surface, its exact lines, and what is wrong with each.
    - `docs/models/statbotics-breakdown-reference.md` §3 (the rated VECTOR), §18 (which vector entries the predicted score reads, per season).
    - The module header of `apps/web/src/components/methodology/epaComparisonContent.ts` — its voice rules are binding on every string you write there (no em dashes, short declarative sentences, no hedging openers, no sentence restating the previous one, neutral between the two ratings, a term explained the first time it appears in the same sentence).
  </read_first>
  <action>
Establish the true statement first, then write it three times for three audiences.

THE TRUE STATEMENT. `main.py`'s `predict_match` computes
`np.array([self.epas[t].mean for t in teams]).sum(axis=0)` — a per-team 18-entry
list over `all_keys[year]`, summed entry by entry across the alliance. Statbotics
rates a per-team list, the same shape SigmaScout uses. What differs is which
entries the predicted score READS: `get_score_from_breakdown` usually reads a
single entry (the no-foul total), but 2018 reads 7 of its own entries plus 3 of
the opponent's, and 2023 reads 7 — and both of those are NON-LINEAR in the rated
entries, via `min()` caps and `zero_sigmoid` terms. So the difference is one of
DEGREE inside a shared structure, not one of kind.

SURFACE 1 — `apps/web/src/components/methodology/epaComparisonContent.ts`, the
`component-maps` entry. This is the published one; do it first and give it the
most care. Two things are false: the paragraph asserting Statbotics rates a
single quantity for an alliance, and the following paragraph's two-sentence
contrast built on it (gap doc Stage 1 names these as lines 105 and 106). Replace
both with student-language prose carrying the true statement. Keep everything the
gap doc marks as surviving: the thin-evidence argument, and the 73.5 / 75.2 / 74.0
percent figures from the 260910-4x0 partition sweep, including the fact that the
74.0 arm is the shape Statbotics' 2024 score formula reads. The `heading`
("How a match score is split into pieces") is deliberately UNCHANGED — the
developer named this exception by that phrase. Also correct the revision note in
the module header (gap doc names lines 25-40) so the header does not still argue
for the claim the body has dropped; add a dated note in the same style as the
existing ones saying what changed and why, citing
`docs/models/statbotics-breakdown-reference.md` §3.

Prohibition restated at the point of temptation: `EPA_DIFFERENCE_IDS` does not
change. This is a reword of an existing entry, never a fourth entry.

SURFACE 2 — `scripts/measureEpaDeviations.ts`, `deviationRegister()`'s
`component-map` entry. The false sentence is in the `summary` field (it says
SigmaScout rates several components and sums them while Statbotics rates a single
quantity per season and predicts from it directly). Rewrite `summary` to the true
statement. If you also touch `reason`, it MUST keep its
`statbotics-breakdown-reference` citation —
`scripts/measureEpaDeviations.test.ts:642` asserts
`expect(entry.reason).toMatch(/statbotics-breakdown-reference/)`. The citation may
point at a different section; it cannot be removed. Leave `status`, `armIds`,
`priorMeasurement` and `ARM_IDS` exactly as they are: registering an arm is a
separate decision and is out of scope here.

SURFACE 3 — `docs/models/epa-divergences.md` §6. The sub-heading stating that the
difference is one of kind rather than of grouping is itself the error, and the
paragraph under it that contrasts an additive partition against a rating menu
repeats it. Rewrite both to the true statement, dated today and attributed to this
quick task in the same style as the existing dated corrections in that file. Keep
every surviving fact: `all_keys[year]` genuinely double-counts (it carries
`no_foul_points` beside the three phase keys it is the sum of, `comp_0..comp_9`
are sub-elements within those phases, `endgame_points` appears twice in 2022 and
2026) — that is TRUE and must survive. Keep the label correction about the 0.7461
figure, keep the 2024 narrowing table, keep the "curve turns over" caution.

SELF-INVALIDATING-GATE WARNING. The verify block below negative-greps each file
for the retracted phrasings, and a `grep` counts comment and prose lines too. So
when you write the dated correction notes, describe the retracted claim by its
LOCATION and its shape ("the paragraph that said Statbotics rates a single total
for an alliance") rather than quoting the old sentence verbatim. Quoting it back
into a retraction note is the single easiest way to fail your own gate while
having done the work correctly.

PIN IT. Add an assertion to
`apps/web/src/components/methodology/epaComparisonContent.test.ts` that the
`component-maps` entry's joined paragraphs do NOT contain the false claim, so a
future editor reintroducing it fails loudly instead of silently. Pin the phrase,
not a paraphrase.

This task changes no model behaviour, no published number, and owes no republish.
Commit it alone, before any of Task 2's plumbing exists.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run apps/web/src/components/methodology/epaComparisonContent.test.ts scripts/measureEpaDeviations.test.ts 2>&1 | tail -30</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && for f in apps/web/src/components/methodology/epaComparisonContent.ts scripts/measureEpaDeviations.ts docs/models/epa-divergences.md; do echo "$f: $(grep -ci 'rates one quantity per alliance\|rates one quantity per season\|difference in KIND' "$f")"; done</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && git diff HEAD -- apps/web/src/components/methodology/epaComparisonContent.ts | grep -c '^[-+].*EPA_DIFFERENCE_IDS'</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
All three files report a count of 0 on the second check. `EPA_DIFFERENCE_IDS` shows
0 changed lines on the third. Both tsconfigs are clean. The new assertion in
`epaComparisonContent.test.ts` fails when the old phrase is pasted back (confirm by
temporarily reintroducing it, watching it go red, then reverting). One commit
containing only these four files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plumb the event's competition week end to end, and pin the off-by-one (own commit)</name>
  <files>packages/core/algorithms/types.ts, packages/corpus/db.ts, packages/core/algorithms/epaWeekOne.ts, packages/core/algorithms/epaWeekOne.test.ts, apps/worker/src/scheduled.ts, packages/harness/preSchedule.ts, packages/harness/scheduleTemplates.ts</files>
  <read_first>
    - `packages/core/algorithms/types.ts` — the `eventType` doc comment on `UpcomingMatch`. It argues for REQUIRED over optional, and for exclusion from `replay.ts`'s `OUTCOME_KEYS`. Both arguments apply verbatim to `week`.
    - `packages/corpus/schema.sql` — `week INTEGER` lives on `events`, not `matches`, so every reader joins it.
    - `packages/corpus/db.ts` `selectMatchesChronological` (~line 533) and `selectScheduledMatches` below it — both already JOIN `events` for `e.event_type`.
    - `apps/worker/src/scheduled.ts` lines ~396-442 (`toMatchResult` / `toUpcomingMatch`) and ~968-977, where `event_type` is pulled off the parsed TBA event detail. `packages/ingest/schemas.ts:47` already declares `week: z.number().nullish()` on `tbaEventSchema`, so the live value is one line away.
    - `packages/core/rankingPoints/reconciliation.test.ts` — the established pattern for a test that reads `data/corpus.sqlite` read-only and skips gracefully when it is absent.
  </read_first>
  <behavior>
    - `STATBOTICS_WEEK_ONE_CORPUS_WEEK` equals 0, and a doc comment states why.
    - `isStatboticsWeekOne(0)` is true; `isStatboticsWeekOne(1)`, `isStatboticsWeekOne(5)` and `isStatboticsWeekOne(null)` are all false.
    - Corpus-backed (skipped when `data/corpus.sqlite` is absent): every 2024 event with `week = 0` has a `start_date` on or after 2024-02-24 and strictly before the earliest `week = 1` start date. This is the assertion that makes the mapping VERIFIED rather than assumed.
    - Corpus-backed: `selectMatchesChronological(db, { year: 2024 })` returns at least one row with `week === 0`, at least one with `week === null`, and no row whose `week` is `undefined`.
    - Corpus-backed: every event with a non-null `week` carries an official `event_type` (nothing offseason or preseason sneaks into the week-1 population).
  </behavior>
  <action>
Write the tests above FIRST and watch them fail, then make them pass.

DECIDED SHAPE, and the reason, so no one re-litigates it mid-task: add
`week: number | null` to `UpcomingMatch` in `packages/core/algorithms/types.ts`,
immediately after `eventType`, REQUIRED and not optional. The reason is the one
`eventType`'s own doc comment already gives — an event's week is fixed when the
event is scheduled, long before any match is played, so it is not outcome-bearing
and must NOT be added to `replay.ts`'s `OUTCOME_KEYS`. Required rather than
optional because `null` is a real, common state (143 of 2024's events carry no
week) and an optional field would collapse "TBA has no week for this event" with
"this construction site forgot to supply one" — precisely the silent-default
failure mode that comment exists to prevent. Do not weaken this to optional to
make the compiler quieter.

Let `tsc` enumerate the blast radius; do not guess it. Most test files build
matches through a local `match()` / `makeMatch()` factory with defaults, so one
added default per factory covers them. Production construction sites are few:
`packages/corpus/db.ts`'s two readers (add `e.week` to both SELECTs and both row
interfaces, mapping `?? null`), `apps/worker/src/scheduled.ts` (thread the value
off the already-parsed TBA event detail exactly the way `eventType` is threaded,
including the `-1`-equivalent unknown case — use `null`, never `0`, because `0`
is a real week), and the harness's pre-schedule builders. Supply the honest value
at every site. Where the week genuinely cannot be known, `null` is correct and a
comment should say so; a fabricated `0` is not.

Add `packages/core/algorithms/epaWeekOne.ts` as the ONE place the off-by-one is
written down:

THE OFF-BY-ONE TRAP, and why it gets its own module. This corpus stores TBA's
week 0-indexed — `packages/corpus/schema.sql` and `packages/bpr/data.ts:29` both
say week 0 is "Week 1", and a live query confirms 2024's `week = 0` events begin
2024-02-24, which is FRC Week 1. Statbotics' filter is `m.week == 1`. So
Statbotics' week 1 is this corpus's `week === 0`. Getting this backwards
calibrates on the wrong week and NOTHING visibly fails: the model still runs, the
numbers still look plausible, and every downstream check still passes. That is why
it is a named constant with a corpus-backed test rather than an inline `=== 0`.

Export from that module: `STATBOTICS_WEEK_ONE_CORPUS_WEEK = 0`, and
`isStatboticsWeekOne(week: number | null): boolean` returning `week === 0`.
Nothing else yet — Task 3 adds the accumulator to this module.

THE NULL-WEEK POLICY, decided here and documented in the module header.
Championship, preseason and offseason events all carry `week = null` in this
corpus (2024: 143 events, 6,255 played matches), and the bucket is heterogeneous —
its start dates span 2024-02-03 to 2024-12-27, so some null-week play happens
BEFORE week 1 and some long after. Policy: a null-week match is never part of the
week-1 population and never triggers the freeze. It is neither week 1 nor
"after week 1", it is unplaced, and treating unplaced play as either would be a
guess. Write that sentence in the header.

No EPA behaviour changes in this task. `epa.ts` does not read `week` yet. The full
suite must be green with no behavioural test edits — if a replay or snapshot test
changes its numbers here, something was wired wrong and you must find it rather
than update the expectation.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run packages/core/algorithms/epaWeekOne.test.ts 2>&1 | tail -25</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit 2>&1 | tail -20 && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -20</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run 2>&1 | tail -25</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && git status --porcelain | grep -c '^.. .planning/phases/09-'</automated>
  </verify>
  <done>
`epaWeekOne.test.ts` passes including its corpus-backed cases (and the run prints
that they RAN, not that they skipped — the corpus is present on this machine).
Both tsconfigs clean. The full suite is green from the repo root, not just from
`apps/web` — run it from the root, since a root-only run and an `apps/web`-only run
cover different file counts. No behavioural expectation was edited to make
something pass. The fourth check confirms foreign phase-09 files are untouched by
this commit's pathspec. One commit containing only this task's files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retarget the win-probability scale and the carry anchor at the frozen week-1 aggregate, then measure and report honestly</name>
  <files>packages/core/algorithms/epaWeekOne.ts, packages/core/algorithms/epaWeekOne.test.ts, packages/core/algorithms/epa.ts, packages/core/algorithms/epa.test.ts, packages/core/algorithms/epaCarryScale.ts, packages/harness/stateSnapshot.ts, docs/models/epa-divergences.md, docs/models/epa-statbotics-gap.md</files>
  <precondition>`data/corpus.sqlite` exists at the repo root and is readable; `data/diagnostics/epa-deviation-ablation.json` exists and its recorded generator/baseline names `epa@8.0.0+baseline`. If the artifact names any other version, it is not a valid BEFORE and must be regenerated at current HEAD before any code change.</precondition>
  <read_first>
    - `docs/models/statbotics-breakdown-reference.md` §20 (`avg.py` verbatim) and §19 (the 7 read points and the 21 `Year` columns).
    - `packages/core/algorithms/epa.ts` around line 585 (the `standardDeviation(state.allianceScoreStats, EPA_FALLBACK_SCORE_SD)` denominator), 891-899 (where alliance scores are folded), 1041-1100 (`carrySeason`), and 1292 (`version`).
    - `packages/core/algorithms/epaCarryScale.ts` in full — `EPA_SCORE_SD_SEED_COUNT`, `EPA_CARRY_RESCALE_MIN_OBS`, `cleanSeasonMean`, `carryRescaleRatio`.
    - `packages/harness/stateSnapshot.ts`'s `STATE_SNAPSHOT_SHAPE_VERSION` doc block, especially the 11 -> 12 entry — it states exactly when a bump is load-bearing and why bumping `epa.version` alone is not enough.
  </read_first>
  <behavior>
    - Folding a week-1 (corpus `week === 0`) alliance score advances BOTH the existing expanding accumulator and the new week-1-only accumulator.
    - Folding a null-week alliance score advances the existing accumulator only, and never freezes.
    - The first fold of a match whose `week` is a number greater than 0 FREEZES the week-1 aggregate; later week-1 rows (an out-of-order arrival) do not reopen it.
    - Before the freeze, `predict`'s denominator is the live expanding SD with `EPA_FALLBACK_SCORE_SD` as its fallback — byte-identical to `epa@8.0.0`'s behaviour.
    - After the freeze, `predict`'s denominator is the frozen week-1 SD for every remaining match of that season.
    - After the freeze, `carryRescaleRatio`'s numerator is the frozen week-1 mean; before it, `cleanSeasonMean`'s unwind is used exactly as `8.0.0` does.
    - A freeze that produced fewer than 2 week-1 observations does not take effect; the live estimate keeps applying and the state records that it did.
    - `carrySeason` resets both the week-1 accumulator and the frozen flag, so a new season starts unfrozen.
    - Deserializing a league row that predates this shape throws, naming the re-seed as the fix.
  </behavior>
  <action>
Write the behaviours above as failing tests first.

STEP 0 — CAPTURE THE BEFORE, BEFORE TOUCHING ANY CODE. Copy
`data/diagnostics/epa-deviation-ablation.json` to
`.planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/before-epa-deviation-ablation.json`.
The measurement script overwrites that path in place, and the change SHIPS INSIDE
the module rather than as an arm, so BEFORE and AFTER are two runs, not two arms
in one run. Confirm the copy's recorded baseline is `epa@8.0.0+baseline` per this
task's precondition; if it is not, re-run
`npx tsx scripts/measureEpaDeviations.ts` at current HEAD first and copy that.

STEP 1 — THE STATE. Add to `epaWeekOne.ts` a small pure accumulator and its fold
rule, then give `EpaState` two league-scoped fields: the week-1
`ExpandingStats`, and the frozen `{ mean, sd } | null`. Both are ONE fact each and
flat in team count, so both belong in the LEAGUE row, not per team — that is the
D-13 rule `stateSnapshot.ts`'s 10 -> 11 and 11 -> 12 entries already applied, not
a preference. Reuse `expandingStats.ts`'s Welford functions; do not write a second
mean/variance implementation.

STEP 2 — THE FREEZE RULE, and why it is live-computable. Matches stream in
chronological order, so the first match carrying a numeric week greater than 0
proves every week-1 match has already passed. That is the freeze trigger. It needs
no lookahead, which is what makes this walk-forward-legal rather than a
season-final read wearing a new name. A null-week match never freezes (Task 2's
policy: unplaced play is neither week 1 nor after it). State plainly in the code
comment that during week 1 itself the frozen value does not exist and the live
expanding estimate is used, so a week-1 prediction can never be informed by a
week-1 match that has not been played.

STEP 3 — THE TWO READ POINTS.
(a) `predict`'s norm-diff denominator: use the frozen week-1 SD when it exists,
otherwise the live `standardDeviation(state.allianceScoreStats,
EPA_FALLBACK_SCORE_SD)` exactly as today. This targets Statbotics' `year.score_sd`,
which `avg.py` computes from the RAW alliance score with fouls INCLUDED — the same
quantity `epa.ts` already folds, so this is an exact target rather than a
neighbour.
(b) The carry anchor: when the frozen week-1 mean exists, it is
`carryRescaleRatio`'s numerator; before then, `cleanSeasonMean`'s unwind applies
unchanged. Do this by passing the chosen numerator INTO the existing
`carryRescaleRatio`, not by rewriting it — `epaCarryScale.ts`'s arithmetic is
proven and its reproduction gate must stay green without edits to its
expectations. If a carry test's number moves, that is a defect in the wiring, not
a reason to update the expectation.

Note why the pre-freeze half must stay: teams first seen during week 1 would ALL
forfeit their rescale if the anchor waited for the freeze, and week 1 is roughly a
sixth of a season's matches. Keeping `8.0.0`'s live estimate during week 1 and
switching at the boundary is what makes this a refinement of 8.0.0 rather than a
reversal of it, which is the framing `epa-statbotics-gap.md`'s correction block
uses.

STEP 4 — A NAMED RESIDUAL GAP, not a silent approximation. Statbotics'
`get_constants` reads week-1 `no_foul_mean` (falling back to `score_mean`), while
this task's frozen mean is over the RAW alliance score. The SD target is exact; the
MEAN target is a named neighbour. Do NOT build a second no-foul accumulator in this
task. Record the gap explicitly in `docs/models/epa-statbotics-gap.md` (extend the
R3 register entry) so it is a known open item rather than an undocumented
divergence. Also record there that `avg.py`'s 2025 processor-algae correction
applies at aggregate time and is NOT adopted here.

STEP 5 — THE TWO VERSION BUMPS.
`epa.version` -> `9.0.0+baseline`, with a dated comment in the existing style
naming this task and what moved. A major bump: ratings change.
`STATE_SNAPSHOT_SHAPE_VERSION` 12 -> 13, and this one IS load-bearing for exactly
the reason the 11 -> 12 block already writes down: `apps/worker/src/stateStore.ts`'s
`readScopedState` filters rows by `algorithm_id` only and never by version, so
bumping `epa.version` alone does not make a stale row unreachable. A shape-12 EPA
league row would deserialize with the week-1 fields absent, leaving the frozen
aggregate permanently unavailable — the Worker would use the live expanding
estimate for the whole season while the offline publisher used the frozen
constant, disagreeing on every prediction from week 2 onward with no error, no NaN
and no malformed row to find. Write that paragraph into the 12 -> 13 block.
Serialize and deserialize both new fields, and add a round-trip test.

STEP 6 — MEASURE, AND REPORT WHAT YOU FIND. Run
`npx tsx scripts/measureEpaDeviations.ts` in the background and poll; it replays
nine seasons off a 582 MB corpus and will take a long time. Verify it finished by
reading its printed output and the artifact's `generatedAt`, never by an exit code.
Copy the result to
`.planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/after-epa-deviation-ablation.json`.
Build one table in the SUMMARY: winner accuracy and Brier, per season and pooled,
BEFORE versus AFTER, with `eventCount` beside each row so the event-blocked
denominator is visible. Both sides come from the same scorer in the same script, so
the two are directly comparable — say so, and say that neither may be differenced
against any figure from a scratch scorer that handles ties differently.

Then state the outcome in one plain sentence at the top of the table, whichever way
it went. The developer has said he is NOT optimizing for accuracy here;
reproduction fidelity is the goal and a regression is an acceptable result. Do not
soften it, do not bury it below the fidelity argument, and do not touch a constant
to improve it.

STEP 7 — THE DEBT. Published EPA ratings change, so a republish is owed and is
FORBIDDEN in this task. Record it in the SUMMARY and in STATE.md's activity line,
alongside the republish debt already outstanding. Note also that
`scripts/epaVsStatbotics.ts --check` was already failing against
`data/baselines/epa-vs-statbotics-2026-09.json` before this task (documented at the
top of `docs/models/epa-divergences.md`) — it will still fail, and that is
pre-existing rather than a new regression. Do not re-baseline it here.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run packages/core/algorithms/epaWeekOne.test.ts packages/core/algorithms/epa.test.ts packages/core/algorithms/epaCarryScale.test.ts packages/harness/stateSnapshot.test.ts 2>&1 | tail -30</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit 2>&1 | tail -15 && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -15</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run 2>&1 | tail -25</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && grep -n 'version: "9\.0\.0+baseline"' packages/core/algorithms/epa.ts && grep -n 'STATE_SNAPSHOT_SHAPE_VERSION = 13' packages/harness/stateSnapshot.ts</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && node -e "const b=require('./.planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/before-epa-deviation-ablation.json'),a=require('./.planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/after-epa-deviation-ablation.json'); const pick=j=>Object.fromEntries(j.rows.filter(r=>r.armId==='epa').map(r=>[r.scope+':'+r.season,[r.winnerAccuracy,r.brier,r.eventCount]])); const B=pick(b),A=pick(a); for(const k of Object.keys(B)) console.log(k, 'acc', (A[k]?A[k][0]-B[k][0]:'MISSING'), 'brier', (A[k]?A[k][1]-B[k][1]:'MISSING'));"</automated>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && git status --porcelain | grep -c '^.. .planning/phases/09-'</automated>
  </verify>
  <done>
Every listed test file passes; the full root suite is green with no behavioural
expectation edited to make it so. Both tsconfigs clean. Both version bumps present
with their doc blocks written. The before/after diff command prints a real delta
for every season and the pooled row, none `MISSING`, and that table is transcribed
into the SUMMARY with its verdict sentence stated plainly whichever direction it
went. `packages/bpr/` and `experiments/phase-bpr/` show zero diff. No R2 write, no
publish command, and no constant changed after seeing a number. The last check
confirms foreign phase-09 files were not staged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBA API -> ingest / Worker | Third-party JSON, already Zod-validated at `packages/ingest/schemas.ts`. `week` is read from the existing `tbaEventSchema.week` (`z.number().nullish()`); no new parse surface is introduced. |
| corpus -> algorithm | `data/corpus.sqlite`, read-only, local. `events.week` is an existing column. |
| algorithm state -> D1 league row -> Worker | Serialized EPA state crosses a version boundary; a stale row read under new code is the real hazard here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j2w-01 | Tampering | `packages/harness/stateSnapshot.ts` league row | high | mitigate | Bump `STATE_SNAPSHOT_SHAPE_VERSION` 12 -> 13 (Task 3 Step 5) so a stale shape-12 row throws `LeagueRowShapeVersionError` instead of silently deserializing with the week-1 fields absent and disabling the freeze on live traffic only. |
| T-j2w-02 | Information disclosure | `.env` / transcripts | high | mitigate | No task reads a secret. `.claude/CLAUDE.md`'s secrets rule is restated in `<standing_prohibitions>` item 5; no `Read`, `cat`, or interpolation of `.env` anywhere in this plan. |
| T-j2w-03 | Tampering | git index (concurrent session) | medium | mitigate | Explicit-pathspec commits only, `git add -A` forbidden, and a `git status --porcelain` check on `.planning/phases/09-` in Tasks 2 and 3's verify blocks. |
| T-j2w-04 | Repudiation | measurement provenance | medium | mitigate | BEFORE artifact copied to the quick-task directory before any code change, AFTER copied after; both committed, and the SUMMARY table is derived from the two files rather than retyped from a console scroll. |
| T-j2w-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan. If one becomes necessary, stop: RESEARCH.md carries no Package Legitimacy Audit for this task, so every package would be `[ASSUMED]` and would need a blocking human checkpoint before install. |
</threat_model>

<verification>
1. Both tsconfigs clean: root `tsc --noEmit` does NOT cover `apps/web`, and Task 1
   touches `apps/web`. Run both, every time.
2. Full suite from the REPO ROOT, not from `apps/web` — the two runs cover
   different file counts and a root-only red has hidden for days before.
3. Three atomic commits, one per task, each staged by explicit pathspec.
4. `git diff HEAD~3 --stat -- packages/bpr experiments/phase-bpr` is empty.
5. No R2 write, no publish, no retune skill invoked.
6. `EPA_DIFFERENCE_IDS` unchanged across all three commits.
</verification>

<success_criteria>
- The published methodology page no longer claims Statbotics rates one number per
  alliance, and a test prevents that claim returning.
- Statbotics' week 1 is mapped to this corpus's `week === 0` by a named constant
  with a corpus-backed test, and the null-week policy is written down where a
  reader of the fold rule will see it.
- EPA reads a frozen week-1 SD and mean from week 2 onward and the live expanding
  estimate during week 1, with no path by which a week-1 match reads an aggregate
  that includes matches not yet played.
- `epa@9.0.0+baseline` and `STATE_SNAPSHOT_SHAPE_VERSION` 13 both land with doc
  blocks explaining why each bump is load-bearing.
- A before/after accuracy and Brier table exists, per season and pooled,
  event-blocked, with its verdict stated plainly — including if it is a regression.
- The republish debt is recorded and unpaid.
</success_criteria>

<output>
Create `.planning/quick/260911-j2w-correct-the-one-number-per-alliance-clai/260911-j2w-SUMMARY.md` when done.

Note: the `Write` tool is blocked for subagents on SUMMARY.md in this project. If a
subagent produced the work, have it RETURN the SUMMARY text and write the file from
the main context rather than letting it route around the block via Bash.
</output>
