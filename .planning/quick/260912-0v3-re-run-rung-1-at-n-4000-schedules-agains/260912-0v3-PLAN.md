---
phase: quick-260912-0v3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measureFieldAveragedRanks.ts
  - scripts/measureFieldAveragedRanks.test.ts
  - docs/models/field-averaged-presim.md
  - .planning/phases/09-analytic-ranking-points-browser-side-simulation/09-CONTEXT.md
  - .planning/todos/pending/rerun-rung1-under-a-valid-bar.md
  - .planning/todos/completed/rerun-rung1-under-a-valid-bar.md
  - .planning/quick/260912-0v3-re-run-rung-1-at-n-4000-schedules-agains/260912-0v3-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260912-0v3]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "`scripts/measureFieldAveragedRanks.ts` accepts a `--schedules` flag. `--draws` stays the TOTAL and `drawsPerSchedule` stays derived as `draws / schedules`, so the two flags keep the same relationship they have today."
    - "WITH NO `--schedules` FLAG THE SCRIPT BEHAVES EXACTLY AS BEFORE — 20 schedules, `drawsPerSchedule = Math.max(1, Math.round(draws / 20))`. This is PROVEN two ways: a unit test over a table of draw values, and a real default run whose printed clause rates and worst team reproduce the committed n=20 record exactly."
    - "The candidate is scored against the BINDING floor — `measureResamplingFloor` (two independent replicates of the same construction) and `measureEdgeNoiseFloor` — both IMPORTED from `scripts/measureGeneratedSchedules.ts`, never re-implemented here. The candidate's rate and the binding floor are printed and written SIDE BY SIDE at the same schedule count so a reader can attribute the gap."
    - "The draw-only `measureSeedNoiseFloor` SURVIVES, still printed, still labelled a diagnostic that does not bind. It is not removed and it is not promoted to the criterion."
    - "`evaluateRungOneCriterion`, `simulateRanks` and `continuousQuantile` are imported and called unchanged. No clause constant moves. No scorer is wrapped, re-implemented or approximated — a scorer mismatch has manufactured a phantom regression on this project before."
    - "Both arms rank over the SAME total draw count. At `--schedules 4000 --draws 200000` the baked artifact's own `draws` is 200,000 and the field-averaged arm is built with `draws: bakedDraws`; the script's existing per-team histogram-sum assertion proves it rather than assuming it."
    - "`docs/models/field-averaged-presim.md` is WRITTEN BY THE SCRIPT via `--write-doc` from the real n=4,000 run. No figure in it is typed by hand. Its title, headline and JSON block carry the schedule count and draws-per-schedule, so the doc is self-labelling and the superseded n=20 headline cannot read as current."
    - "The verdict is whatever the run reports. PASS and FAIL are both acceptable outcomes and neither is presupposed anywhere in the work."
    - "NOTHING SHIPS. `PRESIM_SCHEDULE_COUNT` and `PRESIM_DRAWS_PER_SCHEDULE` in `packages/harness/publish.ts` are read-only to this task, the predictor stays unwired, `data/schedule-templates/` is untouched, and no publish, R2 write, D1 write, manifest bump or deploy runs."
    - "`.env` is never read, printed, copied or interpolated. This script is corpus-only: no network, no credential, no environment-file flag — and it stays that way."
    - "The `### Checkpoint resolution` block in `09-CONTEXT.md` records the re-measurement with a date, keeping the original 2026-09-11 decision text intact as the dated record of what was decided on the evidence then available."
    - "`.planning/todos/pending/rerun-rung1-under-a-valid-bar.md` is closed into `.planning/todos/completed/` carrying the outcome."
  artifacts:
    - "scripts/measureFieldAveragedRanks.ts — `--schedules` flag, `resolveScheduleSplit`, binding-floor wiring, count-aware `renderDoc`"
    - "scripts/measureFieldAveragedRanks.test.ts — default-split regression table and cycle-load assertions"
    - "docs/models/field-averaged-presim.md — rewritten by the script from the n=4,000 run"
    - ".planning/phases/09-analytic-ranking-points-browser-side-simulation/09-CONTEXT.md — dated re-measurement sub-block"
    - ".planning/todos/completed/rerun-rung1-under-a-valid-bar.md"
  key_links:
    - "`measureResamplingFloor` needs TWO artifacts. The second is an independent replicate of the SAME construction, built by appending `REPLICATE_SUFFIX` to `algorithmVersion` and changing NOTHING else. If any other field differs, the floor stops being a same-construction control and the whole comparison is void."
    - "`measureResamplingFloor` computes `draws = count * DRAWS_PER_SCHEDULE` internally from the imported constant 50. If this script's own `drawsPerSchedule` is not 50, the floor is being read at a different draw count than the candidate. That must throw, not slide."
    - "`scripts/measureGeneratedSchedules.ts` already imports from `scripts/measureFieldAveragedRanks.ts`. Importing back creates an ES module cycle — it resolves safely only because neither module touches an imported binding at top level. A test must load the module rather than trust that."
    - "The ~10-minute (possibly much longer) run must be launched with `run_in_background` and polled. A single foreground Bash call risks the 600,000 ms tool ceiling and would lose the whole run."
---

<objective>
Re-run rung 1 — the field-averaged pre-schedule predictor — at a schedule count where the
acceptance bar can actually resolve half a rank, and score it against the BINDING noise floor
rather than the draw-only one. Then confirm or overturn the 09-09 `NO-SHIP` on evidence.

Purpose: plan 09-09 failed rung 1 for a worst team of 7.59 median ranks at n=20. The rung-2
experiment (`docs/models/rung2-generated-schedules.md`, Phase A) subsequently measured the binding
floor at that same count: 27.0% within half a rank, worst team 10.61 ranks, between two runs of the
IDENTICAL construction. Rung 1 was failed for less movement than the measurement's own noise. This
is not a claim that rung 1 passes — it may still lose — but the recorded verdict currently carries
no information.

Output: a `--schedules` flag whose default is byte-identical to today's behaviour; a measurement at
`--schedules 4000 --draws 200000` scored against the binding floor at the same count; a
script-written `docs/models/field-averaged-presim.md`; a dated resolution in `09-CONTEXT.md`; and a
closed todo.

What this decides: if rung 1 passes there is no schedule generator to build and no schedule count to
set — the artifact becomes a roster plus per-team pmf at ~1-5 KB. If it fails on evidence, rung 2's
generator is the path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

@scripts/measureFieldAveragedRanks.ts
@scripts/measureGeneratedSchedules.ts
@docs/models/field-averaged-presim.md
@docs/models/rung2-generated-schedules.md
@.planning/todos/pending/rerun-rung1-under-a-valid-bar.md
@.planning/phases/09-analytic-ranking-points-browser-side-simulation/09-CONTEXT.md
</context>

<constraints>

## Hard boundaries — read before touching anything

**NOTHING SHIPS.** This is a measurement. Do not change `PRESIM_SCHEDULE_COUNT` or
`PRESIM_DRAWS_PER_SCHEDULE` in `packages/harness/publish.ts` — they are read-only here. Do not wire
the field-averaged predictor into the publish path. Do not touch `data/schedule-templates/` or
`packages/harness/scheduleTemplates.ts`. No publish, no R2 write, no D1 write, no manifest bump, no
deploy, no retune.

**One scorer.** `evaluateRungOneCriterion`, `simulateRanks` and `continuousQuantile` stay imported
and called unchanged. Do not re-implement, wrap, inline or approximate any of them, and do not move
a clause constant. A scorer mismatch invented a phantom ~0.003 regression on this project once
already; this task does not reproduce that.

**Secrets.** This script is corpus-only — it opens `data/corpus.sqlite` read-only, makes no network
request, reads no environment variable, and its `package.json` entry deliberately carries no
environment-file flag. Keep every one of those properties. Never `Read`, `cat`, `echo` or
interpolate `.env`, not even to confirm a key is set. See the project instructions' secrets section.

**Verify by output, never by exit code.** On this machine `timeout <n> pnpm <cmd>` swallows all
output and exits 0. Use `npx vitest run <path>` and `npx tsx <script>` directly, and read the
printed result. A quiet log is not a pass.

**Test scope.** `npx vitest run` from the repo root and from `apps/web` see different file sets.
Run the specific test path named in each task from the repo ROOT.

**Do not invent a number.** Every figure that lands in a document either comes from the script's own
`--write-doc` render, or is transcribed verbatim from a line the run actually printed to the log,
with the log line quoted in the SUMMARY. If a number is not in the log, it does not go in a doc.

**The verdict is whatever the run says.** Do not presuppose PASS or FAIL anywhere — not in a task, a
doc edit, a commit message, or the SUMMARY. Write the doc updates so they work either way.

**Commits.** Each task commits atomically. Commit the source change, the script-written doc,
`09-CONTEXT.md` and the todo move. Do NOT commit `SUMMARY.md`, `STATE.md` or this `PLAN.md` — the
orchestrator owns the docs commit.

**Sequential, main checkout.** Worktrees are disabled for this project (`use_worktrees: false`) and
the corpus is gitignored and lives in the main tree. Run every task in the main checkout, in order.

</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --schedules, defaulting to 20, with the default split proven by test</name>
  <files>scripts/measureFieldAveragedRanks.ts, scripts/measureFieldAveragedRanks.test.ts</files>
  <read_first>
    `scripts/measureFieldAveragedRanks.ts` lines 119-135 (constants), 567-575 (`MeasureOptions`),
    620-645 (the hardcoded split and the `buildPreScheduleArtifact` call), 871-905 (`parseArgs` and
    the banner). `scripts/measureGeneratedSchedules.ts` lines 90-101 for the doc-comment style used
    on `DRAWS_PER_SCHEDULE` and `SHIPPED_SCHEDULE_COUNT`.
  </read_first>
  <behavior>
    - With no flag and `draws = 1000`: 20 schedules, 50 draws per schedule — the shipped pairing.
    - With no flag, across a table of draw values (1, 19, 20, 21, 999, 1000, 200000): the result
      equals `Math.max(1, Math.round(draws / 20))` for every entry, including the `max(1,...)` clamp
      at `draws = 1` and the rounding at `draws = 19` and `draws = 21`.
    - With `schedules = 4000` and `draws = 200000`: 4,000 schedules at exactly 50 draws each.
    - A schedule count of 0, a negative count, a fractional count and a non-numeric string each
      THROW, naming the script and the offending value. None of them silently clamps.
  </behavior>
  <action>
    Export a named constant for the measurement's default schedule count, value 20, carrying a
    doc comment that says it mirrors the shipped `PRESIM_SCHEDULE_COUNT` and is READ from it
    conceptually but never writes it — the same read-only framing `SHIPPED_SCHEDULE_COUNT` uses in
    `measureGeneratedSchedules.ts`.

    Export a small PURE helper that takes the total draw count and an optional schedule count and
    returns the pair `{ scheduleCount, drawsPerSchedule }`. When the schedule count is absent it
    uses the default constant, and it derives draws-per-schedule with the SAME
    `Math.max(1, Math.round(total / scheduleCount))` expression that is there today — copy the
    expression, do not rewrite it, so the default path cannot drift. Validate the schedule count: a
    value that is not a finite positive integer throws with the script name and the value in the
    message, in the loud house style the existing corpus re-assertions use.

    Add an optional schedule-count field to `MeasureOptions` beside `draws`. Optional, not required,
    so every existing caller of `measureEvent` keeps today's behaviour structurally rather than by
    remembering to pass a value.

    Replace the two hardcoded lines in `measureEvent` with a single call to the new helper,
    destructuring both values. Everything downstream — the `buildPreScheduleArtifact` call, the
    `bakedDraws` read, the field-averaged build — stays exactly as written.

    Add `schedules` to `parseArgs`'s option map as a string option, parse it to a number in `main`
    when present, leave it undefined when absent, and thread it into the `measureEvent` options
    object. Update the banner line so it prints the resolved schedule count and draws-per-schedule
    alongside the algorithm and total draws, instead of asserting a fixed count in prose.

    Write the tests in `scripts/measureFieldAveragedRanks.test.ts` FIRST, in the existing
    `describe`/`it` style, one `describe` for the split helper. State in an `it` title that the
    default table is a regression pin on unchanged behaviour, not a new specification.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" &amp;&amp; npx vitest run scripts/measureFieldAveragedRanks.test.ts 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
    <human-check>The vitest output names the new split describe block and reports 0 failed. `tsc` prints no error lines. If `tsc` reports an error, triage it — do not batch-dismiss it as pre-existing; a "cosmetic" typecheck red on this repo was once a real live/offline divergence.</human-check>
  </verify>
  <done>
    `--schedules` parses, the split helper is exported and unit-tested, the default path is pinned by
    a regression table, invalid counts throw, and both the test run and the root typecheck are clean.
    Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Score against the binding floor, and make the rendered doc carry its own schedule count</name>
  <files>scripts/measureFieldAveragedRanks.ts, scripts/measureFieldAveragedRanks.test.ts</files>
  <read_first>
    `scripts/measureGeneratedSchedules.ts` lines 280-390 — `aggregateOverPrefix`,
    `measureResamplingFloor` (note it takes TWO artifacts and derives its own draw count from the
    imported `DRAWS_PER_SCHEDULE`), `REPLICATE_SUFFIX` and its doc comment, and
    `measureEdgeNoiseFloor` (ONE artifact, derives draws-per-schedule itself).
    `scripts/measureFieldAveragedRanks.ts` lines 386-405 (`EventMeasurement`), 620-650 (the baked
    build), 735-780 (bytes, diagnostics, the `measureSeedNoiseFloor` call and the returned record),
    840-860 (`printEvent`), 955-975 (the pooled summary and `--write-doc`), 995-1010 (the
    `measureSeedNoiseFloor` doc comment explaining why it is not the criterion), 1063-1240
    (`renderDoc` in full).
  </read_first>
  <action>
    Import `measureResamplingFloor`, `measureEdgeNoiseFloor`, `REPLICATE_SUFFIX` and
    `DRAWS_PER_SCHEDULE` from `./measureGeneratedSchedules.js`, plus their result types. This creates
    an ES module cycle, because that module already imports from this one. It resolves safely only
    because neither module reads an imported binding during top-level evaluation — say so in a short
    comment at the import site so a later edit that adds top-level work knows what it would break.

    In `measureEvent`, build a SECOND baked artifact that is an independent replicate of the same
    construction. It differs from the first in exactly ONE field: `algorithmVersion` gets
    `REPLICATE_SUFFIX` appended. Same roster, same bound `predict` closure, same schedule count, same
    draws-per-schedule, same generation, same timestamp. `buildPreScheduleArtifact` uses that string
    only for seed hashing, which is what makes the replicate a resample of the same construction
    rather than a different arm. Throw if it returns null, in the same style as the existing null
    guards. Write a comment stating that this artifact feeds the floor ONLY and may never reach a
    criterion number.

    Before calling the resampling floor, ASSERT that this run's draws-per-schedule equals the
    imported `DRAWS_PER_SCHEDULE`. That function derives its own draw count from that constant, so a
    mismatch would silently compare the candidate and the floor at different draw counts. Throw
    naming both values and the flag combination that would fix it. Do not clamp and do not proceed.

    Call the resampling floor with (baked artifact, replicate artifact, schedule count), and the edge
    floor with (baked artifact, `bakedDraws`). KEEP the existing `measureSeedNoiseFloor` call and its
    field untouched — it stays printed as the draw-only diagnostic, and its doc comment explaining
    why it is not the criterion stays as written. This task ADDS the binding floors beside it.

    Extend `EventMeasurement` with the resolved schedule count, the resolved draws-per-schedule, the
    resampling floor and the edge floor. Give each new field a doc comment; the binding-floor fields
    should say plainly that this is the floor a two-arm comparison has to clear, and that the
    draw-only one does not bind because it holds the priced schedules fixed.

    Extend `printEvent` and the pooled console summary so the candidate's clause-1 rate and the
    BINDING floor's rate appear SIDE BY SIDE at the same schedule count, roster-weighted the same way
    the existing pooled noise figure is. Do the same for clause 2 against the edge floor. Keep the
    draw-only figure printed, still explicitly labelled as not binding.

    Rewrite `renderDoc` so the record is self-labelling at every count:
    - the title and the headline sentence carry the schedule count and draws-per-schedule instead of
      naming a fixed count in prose;
    - the headline quotes the binding floor at the same count immediately after the candidate's rate,
      so the two are never separated;
    - the JSON block gains the schedule count, the draws-per-schedule, and per-event plus pooled
      binding-floor and edge-floor entries beside the existing draw-only `seedNoiseFloor` entry;
    - the diagnostics table gains binding-floor columns beside the draw-only column, with a sentence
      distinguishing the two and saying which one governs;
    - the artifact-size section gains a caption naming the schedule count those byte figures were
      measured at, because a high-count baked artifact is not the shipped artifact and the table must
      not read as though it were;
    - the caveat that describes the baked arm averaging over a fixed number of shuffles reads the
      count from the measurement rather than naming one.
    `renderDoc` should read the counts from the measurements and throw if the sample disagrees on
    them, rather than taking a new parameter.

    Add a test to `scripts/measureFieldAveragedRanks.test.ts` that imports the two floor functions
    and `DRAWS_PER_SCHEDULE` transitively through this module's own import graph and asserts they are
    defined and that the constant is 50. That is the cycle-load check: it fails loudly if the cycle
    ever stops initialising, instead of failing ten minutes into a run.
  </action>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && npx vitest run scripts/measureFieldAveragedRanks.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -20</automated>
    <human-check>Tests report 0 failed and the cycle-load assertions appear by name. `tsc` prints no error lines. Confirm by reading the source that the draw-only seed-noise function is still called and still printed — this task adds floors, it does not replace one.</human-check>
  </verify>
  <done>
    The binding resampling floor and the edge floor are imported, computed per event from a
    same-construction replicate, carried on the measurement record, printed beside the candidate's
    rate, and rendered into the doc. The draws-per-schedule mismatch guard throws. `renderDoc` emits
    the schedule count in the title, the headline, the JSON block and the artifact-size caption. The
    module cycle is proven to load by test. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the default is unchanged — a real run at defaults, reproducing the committed n=20 record</name>
  <files>(no files changed — this task is a gate)</files>
  <read_first>
    `docs/models/field-averaged-presim.md` at HEAD, specifically the headline sentence, the `Verdict`
    table and the `Per event` table. Those are the committed n=20 record this run must reproduce.
  </read_first>
  <action>
    Run the script with NO `--schedules` flag and NO `--write-doc` flag, on the full default
    six-event panel, and compare its printed output against the committed n=20 record. This is the
    gate: it proves the default path is untouched on the REAL code path, not merely in a unit test,
    before ten minutes of compute is spent at a new count.

    LAUNCH IT IN THE BACKGROUND, NOT IN THE FOREGROUND. Use the Bash tool with
    `run_in_background: true`, redirecting both streams to a log file in the session scratchpad
    directory. Then poll that log with `tail` every 30-60 seconds until the final verdict line
    appears. Do not block on a single foreground call. Echo the absolute log path so it can be cited
    in the SUMMARY.

    The command is `npx tsx scripts/measureFieldAveragedRanks.ts` with no arguments. Do not reach for
    `pnpm` and do not wrap it in `timeout` — that combination swallows all output and exits 0 on this
    machine.

    When it finishes, compare these printed values against the committed document, to the precision
    the document prints:
    - the pooled clause-1 rate, the pooled p10 and p90 rates, and the mean signed median shift;
    - the worst single team's key, its event, and its absolute median difference;
    - all six per-event clause-1 rates;
    - the pooled draw-only seed-noise figure.
    Every one must reproduce. Report them in the SUMMARY as a before/after pair with the log quoted.

    The new binding-floor figures will ALSO print here, at n=20. They are new output and have no
    committed counterpart, so they are not part of the comparison — but record the pooled binding
    clause-1 rate at n=20, because the rung-2 Phase A table carries an independently measured value
    at that same count and agreement between them is a free cross-check that this harness is the same
    harness. Note agreement or disagreement; do not adjust anything to produce agreement.

    STOP AND REPORT if any committed value fails to reproduce. A drift here means the default was not
    preserved, and the n=4,000 run would be measuring something other than what it claims. Do not
    proceed to Task 4.

    Nothing is committed by this task.
  </action>
  <verify>
    <automated>cd "C:/Users/Jacob/Documents/GitHub/SigmaScout" && git status --porcelain -- scripts docs | tail -5</automated>
    <human-check>`git status` shows no uncommitted change under `scripts/` or `docs/` — the run wrote nothing, because `--write-doc` was not passed. The log's pooled clause-1 rate, worst team, worst event, worst difference and all six per-event rates match the committed n=20 document exactly.</human-check>
  </verify>
  <done>
    A real default-flag run reproduced the committed n=20 record on every compared figure, with the
    log path and the matched values recorded. The binding floor at n=20 is noted against the rung-2
    Phase A value. No file changed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run the measurement at n=4,000 and let the script write the record</name>
  <files>docs/models/field-averaged-presim.md</files>
  <action>
    Run:

    `npx tsx scripts/measureFieldAveragedRanks.ts --schedules 4000 --draws 200000 --write-doc`

    That holds draws-per-schedule at the shipped 50 (200,000 / 4,000), which is the constant the
    binding floor derives its own draw count from, and it gives both arms the same 200,000 total
    draws — the script's existing per-team histogram-sum assertion enforces that and will throw if it
    is ever untrue.

    LAUNCH IT IN THE BACKGROUND AND POLL. This is the long one. Use the Bash tool with
    `run_in_background: true` and redirect both streams to a log file in the session scratchpad
    directory. Then poll with `tail` every 60 seconds. A single foreground Bash call risks the
    600,000 ms tool ceiling and would lose the entire run. Echo the absolute log path.

    Budget generously and do not read slowness as failure. The brief prices it at about ten minutes
    on the six-event panel, timed from the builder at roughly 6.8 ms per schedule on a 75-team event
    and 1.6 ms on an 18-team one — but this run builds the replicate artifact too, so it does roughly
    twice that construction work plus the floor simulations. Thirty minutes or more is a plausible
    honest runtime. A quiet log and a stalled-looking terminal are NOT proof it died: check that the
    log's modification time is still advancing before concluding anything.

    If the process dies with a heap out-of-memory error, retry ONCE with
    `NODE_OPTIONS=--max-old-space-size=8192` prefixed. Do NOT lower the schedule count to make it
    fit — 4,000 is the count the acceptance bar becomes usable at, and a smaller run would recreate
    the exact defect this task exists to fix. If it still fails, stop and report the failure.

    When it completes, record from the log, verbatim:
    - the final verdict line;
    - the pooled clause-1 rate and the pooled BINDING clause-1 floor at n=4,000, side by side;
    - the pooled clause-2 p10/p90 rates and the edge floor beside them;
    - clause 3's mean signed median shift;
    - the worst single team, its event, and its absolute median difference;
    - the per-event clause-1 rates and their per-event binding floors;
    - the confirmation line that the document was written.

    Then verify the document ON DISK rather than trusting the print. On this project a sibling
    script prints a summary it does not write, and its tests stay red until a human copies the
    numbers across. Read the written file and confirm its headline and JSON block carry the schedule
    count 4,000, the draws-per-schedule 50, and figures identical to the log's.

    Commit `docs/models/field-averaged-presim.md` alone. Its numbers are the script's, not yours.
    Nothing in the commit message asserts a ship decision.
  </action>
  <verify>
