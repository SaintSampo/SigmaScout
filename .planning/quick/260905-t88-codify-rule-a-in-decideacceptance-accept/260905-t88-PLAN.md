---
quick_id: 260905-t88
phase: quick-260905-t88
slug: codify-rule-a-in-decideacceptance-accept
date: 2026-09-05
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: true
depends_on: []
requirements: [RULE-A, DIAG, PROSE]
files_modified:
  - packages/harness/acceptance.ts
  - packages/harness/acceptance.test.ts
  - packages/harness/tune.ts
  - packages/harness/tune.test.ts
  - packages/harness/objectiveDefinition.ts
  - docs/models/sigma1-tuning-results.md

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "RULE-A: `decideAcceptance` returns `accept` when, and only when, BOTH `accuracyMargin > 0` AND `candidateBrier < incumbentBrier` (and the MAE guardrail does not veto). Both comparisons are STRICT — a zero accuracy margin does not accept, and an unchanged Brier does not accept."
    - "RULE-A: the `sqrt(2 ln N) * SE` noise bar no longer gates anything. A candidate whose accuracy margin is BELOW the bar but positive, with a better Brier, is ACCEPTED — this is the exact case (2025 at ~90% of the bar with better Brier, twice) that the 2026-09-05 policy decision was made to ship, and it is pinned by a named test."
    - "DIAG: the noise bar is still COMPUTED and REPORTED. `threshold` remains an evidence field on every outcome, and a new `clearedNoiseBar` boolean records whether `accuracyMargin > threshold` — diagnostic context ('it cleared the old bar too'), never a gate."
    - "DIAG: `brierVetoBound` remains computed and reported on every outcome, from the unchanged `ACCEPTANCE_BRIER_VETO_*` constants, even though the Brier veto BRANCH is retired — Rule A's strict-improvement requirement strictly subsumes it (nothing that could have tripped the veto can now be accepted), so an active branch for it would be provably unreachable."
    - "RULE-A: the MAE guardrail veto is UNCHANGED in logic, constants and reported reason string (`mae-veto`), and is checked AFTER Rule A's two halves — a candidate that fails Rule A reports the Rule A reason, because the veto was moot for a candidate that was never eligible."
    - "PROSE: `KeepIncumbentReason` is `\"no-accuracy-gain\" | \"brier-regression\" | \"mae-veto\"`. The retired values `below-threshold` and `brier-veto` are REMOVED from the union so the compiler surfaces every reader, and are documented as the vocabulary of the RETIRED bar. Historical acceptance artifacts on disk are NOT rewritten and stay readable as what they were."
    - "PROSE: `buildAcceptanceReport`'s verdict names WHICH RULE decided, on every branch, and reports the noise bar as an explicitly diagnostic clause that states whether it was cleared. No branch depends on a trailing `and was cleared` fragment that Rule A can now make false."
    - "PROSE: the shared verdict prefix stays SIGN-NEUTRAL (260904-4ik's fix, carried forward): it reports the signed accuracy margin and the signed Brier delta and claims no side, so a `keep-incumbent` verdict built from a negative margin contains no sentence asserting the candidate outperformed the incumbent."
    - "PROSE: `keep-incumbent` is still a calmly-reported SUCCESS. `decideAcceptance` never throws for a non-accepting comparison, the origin path still exits 0 for every outcome, and the keep branches still say the search completed rather than failed."
    - "PRE-COMMITMENT: `decideAcceptance`'s own doc comment records that Rule A was chosen while looking at the 36-verdict retroactive analysis and must now run UNCHANGED on future tunes — no margins, no SE minimums, no per-season exceptions added in reaction to any single future result."
    - "NO PREDICTION MOVEMENT: this task touches decision and reporting code only. `digest.test.ts`'s corpus-backed per-version digests are bitwise unchanged. If any digest moves, something is wrong and the change must be re-examined, not re-fingerprinted."
  artifacts:
    - "packages/harness/acceptance.ts — Rule A gate, new reason vocabulary, `clearedNoiseBar` evidence field, rewritten header, pre-commitment note"
    - "packages/harness/acceptance.test.ts — the three named fixtures plus the below-the-bar accept, strict-tie cases, and preserved MAE-guardrail coverage"
    - "packages/harness/tune.ts — verdict prose naming Rule A on all four branches, diagnostic bar clause, corrected budget print, corrected module header"
    - "packages/harness/tune.test.ts — the four outcomes re-pointed to the Rule A vocabulary"
    - "packages/harness/objectiveDefinition.ts — corrected claim about what governs the ship/don't-ship decision"
    - "docs/models/sigma1-tuning-results.md — dated Rule A note, parallel to the existing bar-retirement note"
  key_links:
    - "`decideAcceptance` -> `buildAcceptanceReport` -> the `-acceptance.json` artifact and the printed verdict. The gate and the sentence that reports it must agree; a gate change with unchanged prose is exactly the drift 260904-4ik already had to fix once."
    - "`ACCEPTANCE_BRIER_VETO_*` constants -> `brierVetoBound` evidence field. The constants stay because the field stays; the field stays because the artifact shape is a record other readers depend on."
    - "`accuracyMargin > threshold` -> `clearedNoiseBar`. This is the ONLY remaining consumer of the bar in a decision-shaped expression, and it must feed a reported boolean, never a branch."
---

<objective>
Codify Rule A as the acceptance gate in `packages/harness/acceptance.ts`, so the next tune's
verdicts are decided by the machinery under the policy the operator actually adopted on
2026-09-05 rather than re-read by hand afterwards.

Purpose: `vpr@9.0.0+rolling-2026-09c` was promoted MANUALLY under Rule A while
`decideAcceptance` still implemented only the D-T7 noise bar. Every future tune would print
`keep-incumbent` verdicts that the operator then has to mentally re-decide — drift waiting to
happen, and precisely the "docs describe a rule the code no longer implements" failure this
project's log names.

Output: a gate that accepts when the search winner improves BOTH out-of-sample winner accuracy
AND Brier over the live incumbent; the retired noise bar still computed and reported as
diagnostics; the MAE guardrail untouched; three named fixtures pinning the policy; and prose —
in the code and in the published docs — that says which rule decided.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/codify-rule-a-acceptance.md
@.planning/STATE.md
@packages/harness/acceptance.ts
@packages/harness/acceptance.test.ts
@packages/harness/objectiveDefinition.ts

Read only the ranges named in each task from `tune.ts` (it is ~2200 lines) — do not read it whole.

**Not a tracer-first plan, deliberately.** The wiring
(`decideAcceptance` -> `buildAcceptanceReport` -> artifact + printed verdict) already exists and is
already tested end to end. This is a policy swap inside a proven path, so the tasks are ordered by
blast radius (decision function, then its reporter, then prose), not as a vertical slice.

**Scope fence.** No prediction code, no scoring code, no corpus, no promotion. `acceptanceThreshold`
keeps its formula, its N >= 2 refusal, and its tests. `eventBootstrap.ts`, `promote.ts`,
`promotedVersionPath.ts` and every `data/algorithm-versions/*.json` are untouched.

**Do not commit `.planning/` files.** The todo at
`.planning/todos/pending/codify-rule-a-acceptance.md` is closed by the ORCHESTRATOR after this plan
lands — no task here moves it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rule A becomes the gate in decideAcceptance; the noise bar becomes reported diagnostics</name>
  <files>packages/harness/acceptance.ts, packages/harness/acceptance.test.ts</files>
  <read_first>
    - `packages/harness/acceptance.ts` in full (255 lines) — the module header states the
      three-condition contract that this task rewrites, and the header is where a future edit
      would break the new contract, so it is load-bearing, not decoration.
    - `packages/harness/acceptance.test.ts` in full (184 lines) — every existing test is either
      kept, re-pointed, or inverted by this task; none are deleted wholesale.
    - `.planning/todos/pending/codify-rule-a-acceptance.md` — the decision record. Its
      "IMPORTANT pre-commitment" paragraph must survive into the code.
  </read_first>
  <behavior>
    All cases below are `decideAcceptance` return values. `baseInput` in the test file is the
    "comfortably accepted" fixture and MUST be changed so its Brier IMPROVES
    (`incumbentBrier: 0.16`, `candidateBrier: 0.159`) — under Rule A the current fixture's equal
    Briers no longer accept, and every test that inherits it would silently change meaning.

    - Accuracy improves AND Brier improves, MAE flat -> `accept`. (Todo fixture 1.)
    - Accuracy improves, Brier WORSENS -> `keep-incumbent` / `brier-regression`, at every
      regression size: tiny (0.1605 vs 0.16), large (0.168 vs 0.16), and both of the sizes that
      used to fall inside the retired veto's noise-only and materiality-only halves. The old
      two-half structure no longer decides anything. (Todo fixture 2.)
    - Accuracy WORSENS and Brier worsens -> `keep-incumbent` / `no-accuracy-gain`. (Todo fixture 3.)
    - THE MOTIVATING CASE: accuracy improves by LESS than the noise bar (candidateAccuracy 0.602
      vs 0.6, margin 0.002, bar ~0.003488 at N=60) but Brier improves -> `accept`, with
      `clearedNoiseBar === false`. Name the 2026-09-05 decision in this test's title.
    - Accuracy improves ABOVE the bar with an improving Brier -> `accept`, `clearedNoiseBar === true`.
    - Strict-tie, accuracy: `candidateAccuracy === incumbentAccuracy` with an improving Brier ->
      `keep-incumbent` / `no-accuracy-gain`.
    - Strict-tie, Brier: accuracy improves with `candidateBrier === incumbentBrier` ->
      `keep-incumbent` / `brier-regression`.
    - MAE guardrail preserved: accuracy improves, Brier improves, MAE 20 -> 21.6 with SE 0.05 ->
      `keep-incumbent` / `mae-veto`, with `maeDelta > maeVetoBound`.
    - MAE guardrail's two halves still both load-bearing: +0.3% MAE at SE 0.5, +5% MAE at SE 1.0,
      and an MAE IMPROVEMENT all still `accept` (with the improving-Brier base).
    - Precedence: accuracy does NOT improve AND MAE blows up -> `no-accuracy-gain`, not `mae-veto`.
    - Precedence: accuracy improves, Brier regresses AND MAE blows up -> `brier-regression`, not
      `mae-veto`.
    - Evidence on ALL outcomes: `accuracyMargin`, `threshold`, `clearedNoiseBar`,
      `evaluationCount`, `maeDelta`, `maeVetoBound`, `brierDelta`, `brierVetoBound` are all
      present and correctly typed. `threshold` still equals `acceptanceThreshold(N, accuracySE)`
      and `brierVetoBound` still equals the max of the two `ACCEPTANCE_BRIER_VETO_*` halves.
    - `acceptanceThreshold`'s own three tests are UNCHANGED and still pass.
    - Nothing throws for a non-accepting comparison.
  </behavior>
  <action>
    Change the GATE in `decideAcceptance` to Rule A, keep every number it used to gate on as
    reported evidence, and rewrite the header prose that currently describes the retired rule.

    1. **The gate.** Evaluate in this order and return the first match:
       accuracy half (`accuracyMargin > 0` is false -> `keep-incumbent` / `no-accuracy-gain`);
       Brier half (`input.candidateBrier < input.incumbentBrier` is false -> `keep-incumbent` /
       `brier-regression`); MAE veto (unchanged expression -> `keep-incumbent` / `mae-veto`);
       otherwise `accept`. Both Rule A comparisons are STRICT, and the strictness is the point —
       state in a comment that an exact tie on either quantity is not an improvement.
       Gate-before-veto precedence is preserved from the retired rule for the same stated reason:
       a candidate that never became eligible reports why it was ineligible, not a veto that was
       moot for it.

    2. **Reason vocabulary.** `KeepIncumbentReason` becomes
       `"no-accuracy-gain" | "brier-regression" | "mae-veto"`. REMOVE `"below-threshold"` and
       `"brier-veto"` from the union rather than leaving them as unreachable values — the same
       compiler-surfacing argument this file already records for the `margin` -> `accuracyMargin`
       rename. Document in the header that those two strings are the RETIRED bar's vocabulary,
       that every acceptance artifact recorded before 2026-09-05 carries them, and that those
       artifacts are NOT rewritten: an artifact saying `below-threshold` means the noise bar
       refused it, which was true when it was written.

    3. **The bar, demoted to diagnostics.** Keep computing `threshold` via the unchanged
       `acceptanceThreshold`. Add ONE new field to `AcceptanceEvidence`, carried by BOTH union
       members: `readonly clearedNoiseBar: boolean`, set to `accuracyMargin > threshold`. Its doc
       comment says it is the retired D-T7 bar, reported because "it cleared the old bar too" is
       worth knowing and because its PRESENCE is how a reader tells a Rule-A-era artifact from a
       bar-era one. It must never appear in a branch condition.

    4. **The Brier veto, subsumed.** Delete the `brierVetoed` BRANCH but keep
       `ACCEPTANCE_BRIER_VETO_RELATIVE_TOLERANCE`, `ACCEPTANCE_BRIER_VETO_NOISE_MULTIPLE`, the
       `brierVetoBound` computation and the `brierVetoBound` evidence field. Record why in their
       doc comments: Rule A requires a STRICT Brier improvement, which is strictly stronger than
       "no materially-and-distinguishably-worse Brier", so the veto can never change a decision —
       an active branch for it would be provably unreachable, while the bound remains useful as
       reported context for HOW MUCH Brier moved. Note explicitly that the constants and the field
       keep their `VETO` names because the acceptance artifact's field names are a record other
       readers depend on, and that their names now carry history rather than current behavior.

    5. **The MAE guardrail is untouched** — same two halves, same constants, same `mae-veto`
       string, same named booleans.

    6. **Header rewrite.** The module header currently opens on "D-T7's pre-committed acceptance
       rule" and enumerates three conditions. Rewrite it to state Rule A as the shipping gate, with:
       the 2026-09-05 adoption and its evidence (three accuracy-primary tunes went 30/30
       keep-incumbent under the bar while posting repeated near-accepts — 2025 at ~90% of the bar
       twice, with better Brier both times; the retroactive pass over all 36 recorded
       accuracy-primary verdicts across four runs ships exactly the season-clustered,
       autopsy-predicted improvements, 2025 twice and 2026 once, and rejects every Brier-worse
       positive, all of which pattern as noise); the reason the bar was retired as a gate (its
       winner's-curse correction demands margins around 3 SE that a single season's ~16k matches
       cannot supply for real effects of ~0.2-0.4pt); and why the Brier half addresses
       selection-overfit a different way (an overfit winner buys selection accuracy by paying
       calibration, so requiring both to improve catches it without needing a margin). KEEP the
       existing "`keep-incumbent` is a SUCCESSFUL result" section intact — it is unchanged by this
       task and is the most important contract in the file.

    7. **The pre-commitment note goes in `decideAcceptance`'s OWN doc comment**, not only the
       header: Rule A was chosen while looking at this data, so it must now run UNCHANGED on future
       tunes — no margins, no SE minimums, no per-season exceptions added in reaction to any single
       future result. Say that adding one is the failure mode this note exists to prevent.

    8. **Tests.** Extend `acceptance.test.ts`; do not rewrite the file. Change `baseInput`'s
       `candidateBrier` to 0.159 (its comment must say WHY: under Rule A a fixture with an
       unchanged Brier is not an accepting fixture). Then re-point each existing test per this
       table, and add the missing cases from the behavior block:

    | Existing test | New disposition |
    |---|---|
    | accepts comfortably, Brier and MAE unchanged | still `accept`; retitle to say Brier IMPROVES |
    | accepts despite a slightly WORSE Brier | INVERT: now `keep-incumbent` / `brier-regression` — Rule A's whole point |
    | keep / below-threshold on a sub-bar margin | INVERT: now `accept` with `clearedNoiseBar === false` — the motivating 2026-09-05 case |
    | keep / brier-veto on a large Brier regression | now `keep-incumbent` / `brier-regression`; also assert `brierVetoBound` is still reported |
    | Brier veto noise-half-only / materiality-half-only (two tests) | merge into ONE test proving the halves no longer decide: both inputs now `brier-regression` |
    | never vetoes a Brier improver | unchanged, still `accept` |
    | precedence: fails bar AND regresses Brier | now `brier-regression` (its accuracy margin is positive) |
    | precedence: clears bar, trips BOTH vetoes | now `brier-regression`; ADD a new MAE-precedence test with an improving Brier |
    | mae-veto on an 8% MAE regression | set `candidateBrier` to an improvement so the MAE veto is what is actually under test; still `mae-veto` |
    | the three "does NOT veto MAE" tests | unchanged, still `accept` under the improving-Brier base |
    | below-threshold not mae-veto | re-point to a NEGATIVE accuracy margin (0.599) -> `no-accuracy-gain`, not `mae-veto` |
    | carries the same evidence fields on all outcomes | update the sampled outcomes to the new reasons; add `clearedNoiseBar` and `threshold` to the asserted set |

    Do NOT touch: `acceptanceThreshold` or its tests, the MAE constants, the `AcceptanceInput`
    shape, or any field name in `AcceptanceEvidence`.
  </action>
  <verify>
    <automated>cd "c:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit && npx vitest run packages/harness/acceptance.test.ts</automated>
  </verify>
  <done>
    `decideAcceptance` accepts on Rule A alone; `clearedNoiseBar` and `threshold` are reported on
    every outcome and appear in no branch condition; `KeepIncumbentReason` has exactly the three
    new values; `acceptance.test.ts` covers all three named todo fixtures plus the below-the-bar
    accept and both strict ties, and is green. `tsc --noEmit` may report errors ONLY in `tune.ts`
    / `tune.test.ts` at this point (Task 2 fixes them) — record which, and confirm they are the
    expected reason-vocabulary readers rather than anything unexpected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the verdict says which rule decided, and the bar clause stops claiming to be a gate</name>
  <files>packages/harness/tune.ts, packages/harness/tune.test.ts</files>
  <read_first>
    - `packages/harness/tune.ts` lines 1830-1960 — `OriginAcceptanceReport`, `buildAcceptanceReport`,
      the shared prefix and its four-branch ternary. The comment block above the prefix records
      260904-4ik's sign-neutrality fix and the load-bearing trailing clause; both are directly at
      stake here.
    - `packages/harness/tune.ts` lines 113-141 — the module header's "D-T7's PRE-COMMITTED
      ACCEPTANCE RULE" section.
    - `packages/harness/tune.ts` lines 1444-1450 — the budget-tradeoff print.
    - `packages/harness/tune.test.ts` lines 651-772 — the `buildAcceptanceReport` describe block.
  </read_first>
  <behavior>
    All cases are `buildAcceptanceReport` outputs, using the existing synthetic `units(...)` helper
    (candidateAccuracy, incumbentAccuracy, candidateBrier, incumbentBrier, candidateMae,
    incumbentMae). BASE keeps `evaluationCount: 58`, `accuracyDeltaStandardError: 0.0005` (bar
    ~0.00349).

    - `units(0.61, 0.6, 0.159, 0.16, 20, 20)` -> `accept`; verdict contains `ACCEPTED` and names
      `RULE A`; `report.outcome.clearedNoiseBar === true`; verdict's bar clause reads as CLEARED.
    - `units(0.6002, 0.6, 0.159, 0.16, 20, 20)` -> `accept` with `clearedNoiseBar === false`; the
      verdict says the noise bar was NOT cleared and still says the candidate is ACCEPTED. This is
      the 2026-09-05 case and it must be asserted here as well as in `acceptance.test.ts`, because
      the REPORT is what an operator reads.
    - `units(0.59, 0.6, 0.159, 0.16, 20, 20)` -> `keep-incumbent` / `no-accuracy-gain`; verdict
      contains `INCUMBENT STANDS`, still contains the "completed search, not a failed one" framing,
      and — carrying 260904-4ik's fix forward — does NOT contain a claim that the candidate beat
      the incumbent, even though its `accuracyMargin` is -0.01.
    - `units(0.61, 0.6, 0.168, 0.16, 20, 20)` -> `keep-incumbent` / `brier-regression`; verdict
      names `RULE A` and says the candidate is more accurate but its Brier is not better.
    - `units(0.61, 0.6, 0.159, 0.16, 21.6, 20)` -> `keep-incumbent` / `mae-veto`; verdict contains
      `VETOED` and names the MAE bound.
    - Field recording is unchanged: `evaluationCount` 58, `threshold` equals
      `sqrt(2 ln 58) * 0.0005` and NOT the same expression on either Brier SE, all three SEs are
      recorded under their distinct names, and the verdict contains `N = 58`.
  </behavior>
  <action>
    Bring the reporter and its prose into agreement with Task 1's gate.

    1. **The shared prefix** currently ends `; the bar at N = X was Y`, and both veto branches
       concatenate `${shared} and was cleared`. That trailing fragment is now a HAZARD, not just
       stale: under Rule A a candidate can be accepted, or vetoed on MAE, WITHOUT having cleared
       the bar, so a branch that appends "and was cleared" can now assert something false. Rebuild
       the prefix so it is self-contained and every branch appends only a `, so ...` clause:
       keep the sign-neutral opening (evaluated N candidates on the selection seasons; the
       winner's out-of-sample ACCURACY margin over the incumbent was the signed number; over n
       matches across the event count), ADD the signed Brier delta beside the accuracy margin
       (Rule A has two halves, so a reader needs both numbers), and end with a bar clause that
       states the bar is DIAGNOSTIC ONLY since 2026-09-05, that RULE A is the gate, and whether
       the bar was CLEARED or NOT CLEARED, driven by `outcome.clearedNoiseBar`. No directional
       verb anywhere in the prefix — it is reused by all four branches and one of them has a
       negative margin.

    2. **The four branches**, each naming the rule that decided:
       `accept` — ACCEPTED under RULE A, because it improves BOTH accuracy and Brier over the
       incumbent, with the score-MAE delta and its guardrail bound reported as today.
       `no-accuracy-gain` — INCUMBENT STANDS under RULE A: the candidate is not more accurate
       out-of-sample. Keep the "completed search, not a failed one" sentence.
       `brier-regression` — INCUMBENT STANDS under RULE A: more accurate, but its Brier is not
       better, and a Brier-worse accuracy positive patterns as noise (2026-09-05 policy). Keep the
       "completed search, not a failed one" sentence.
       `mae-veto` — cleared RULE A but VETOED on alliance-score MAE, keeping today's numbers and
       today's closing sentence about why the guardrail exists (the vpr@3.0.0 fix's 16% score-MAE
       regression that Brier and SD(z) both rated equal-or-better).

    3. **Update the comment block above the prefix**: the sign-neutrality requirement stays and
       must be restated; the "trailing clause is load-bearing grammar" warning is REPLACED by a
       note that each branch is now self-contained precisely because the old shared tail could
       assert a bar-clearing that Rule A no longer requires.

    4. **`OriginAcceptanceReport`'s fields are unchanged.** `outcome` gains `clearedNoiseBar`
       transitively from Task 1; nothing else in the artifact shape moves.

    5. **The budget print** (lines ~1444-1450) tells the operator that a smaller `--evals`
       "RELAXES the bar". Under Rule A the bar is not the gate and `--evals` no longer trades
       against shipping probability at all. Rewrite it to say the budget buys search breadth, that
       the `sqrt(2 ln N)` bar still moves with N but is now reported as diagnostics only, and that
       Rule A has no N dependence. Keep the concrete 60 -> 40 / ~0.003488 -> ~0.003310 figures as
       the illustration of what the diagnostic does.

    6. **The module header** section "D-T7's PRE-COMMITTED ACCEPTANCE RULE, and D-T4's two arms"
       (lines ~113-141) must state Rule A as the gate, keep the incumbent-is-what-ships paragraph,
       keep the three-distinct-SE-names paragraph (the accuracy paired SE now feeds the diagnostic
       rather than the gate — say so), and keep "`keep-incumbent` EXITS 0 and reads as a result".
       Leave the exit-0 comment block at lines ~1580-1588 in place; adjust only its "the bar it
       could not clear" phrasing so it does not re-assert the retired gate.

    7. **Tests.** In `tune.test.ts`'s `buildAcceptanceReport` describe block, re-point the five
       outcome tests per the behavior block and retitle the describe to name the Rule A outcomes.
       Both existing `expect(report.verdict).toMatch(/and was cleared/)` assertions must go —
       replace them with assertions on the new diagnostic clause. When asserting the cleared
       versus not-cleared wording, match on the FULL fragment (the not-cleared wording contains
       the cleared word as a substring, so a bare match on the cleared word passes for both and
       proves nothing).
  </action>
  <verify>
    <automated>cd "c:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit && npx vitest run packages/harness/tune.test.ts</automated>
  </verify>
  <done>
    `npx tsc --noEmit` is clean repo-wide. `tune.test.ts` is green and its acceptance block asserts
    all four Rule A outcomes, the below-the-bar accept, and the sign-neutral prefix on a negative
    margin. Read all four assembled verdict sentences end to end and confirm each is grammatical
    English and that none asserts a bar-clearing that did not happen.
  </done>
</task>

<task type="auto">
  <name>Task 3: record Rule A where the objective and the published results are stated</name>
  <files>packages/harness/objectiveDefinition.ts, docs/models/sigma1-tuning-results.md</files>
  <read_first>
    - `packages/harness/objectiveDefinition.ts` in full (46 lines).
    - `docs/models/sigma1-tuning-results.md` lines 1-45 — the existing dated notes, including the
      2026-09-04 "D-T7 acceptance-bar retirement" note this one sits beside.
  </read_first>
  <action>
    1. **`objectiveDefinition.ts`.** `SEARCH_OBJECTIVE_DEFINITION`'s doc comment ends with "The
       same rule, on the same accuracy definition, governs D-T7's ship/don't-ship acceptance bar
       in `acceptance.ts`." That sentence is now FALSE: the SEARCH objective is still
       accuracy-primary with a Brier tiebreak inside the noise band, but the SHIP decision is
       Rule A. Replace that sentence with an accurate one — the search objective still ranks
       candidates, and the ship/don't-ship decision is `acceptance.ts`'s Rule A (both accuracy and
       Brier must improve over the live incumbent), adopted 2026-09-05, which is a DIFFERENT rule
       and deliberately so. Do NOT change either exported constant's STRING VALUE — those strings
       are written into promoted version files' provenance and describe the search, which this
       task does not change. Comment-only edit.

    2. **`docs/models/sigma1-tuning-results.md`.** Add a dated note immediately after the existing
       "D-T7 acceptance-bar retirement (2026-09-04...)" paragraph, in the same voice: Rule A
       adopted 2026-09-05 after three accuracy-primary tunes went 30/30 keep-incumbent under the
       noise bar while posting repeated near-accepts; the retroactive pass over all 36 recorded
       accuracy-primary verdicts (four runs: retune-0905, elim-R v9n, Stage 2 CVF, Stage 3 CER)
       ships exactly the season-clustered, autopsy-predicted improvements (2025 twice, 2026 once)
       and rejects every Brier-worse positive; the first Rule-A promotion
       (`vpr@9.0.0+rolling-2026-09c`, 2025+2026) was applied MANUALLY and this change is what puts
       the rule in the machinery; every verdict recorded before 2026-09-05 was decided under the
       noise bar and is left exactly as measured, with `below-threshold` / `brier-veto` as that
       era's vocabulary; the bar is still computed and reported as diagnostics; and the
       pre-commitment that Rule A now runs unchanged on future tunes. No figure anywhere else in
       the document is edited, and no tuning was re-run.
  </action>
  <verify>
    <automated>cd "c:/Users/Jacob/Documents/GitHub/SigmaScout" && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    `objectiveDefinition.ts` no longer claims the search objective governs the ship decision, and
    both exported string constants are byte-unchanged. `sigma1-tuning-results.md` carries the dated
    Rule A note. The full repo-root suite is green — judged by its printed summary, not by an exit
    code.
  </done>
</task>

</tasks>

<verification>
Run from the repo ROOT, and read the printed output rather than trusting an exit code (a
`timeout N pnpm ...` wrapper swallows output and exits 0 — do not use one):

1. `npx tsc --noEmit` — clean, zero errors.
2. `npx vitest run` — the FULL suite from the repo root, all green. Running from `apps/web`
   collects roughly half the files and has hidden a red suite here before.
3. `npx vitest run packages/harness/digest.test.ts` — every corpus-backed per-version digest case
   green, bitwise unchanged. This task touches decision and reporting code only; if a digest
   moves, a prediction stream moved, which means something in the change is wrong. Investigate the
   change — do NOT re-fingerprint.
4. `git diff --stat` names exactly the six files in `files_modified` and nothing under
   `.planning/` or `data/`.
5. Grep the working tree for the retired reason strings: `below-threshold` and `brier-veto` must
   appear only in historical records (`.planning/`, `docs/`, and the retirement notes inside
   `acceptance.ts`), never as a value the new code can emit.
</verification>

<success_criteria>
- `decideAcceptance` accepts on Rule A alone: accuracy strictly improves AND Brier strictly
  improves, with the MAE veto still able to refuse afterwards.
- A positive-accuracy, better-Brier candidate whose margin is BELOW the retired noise bar is
  ACCEPTED, and a test names the 2026-09-05 decision it pins.
- The noise bar and the Brier veto bound are still computed and reported on every outcome;
  neither appears in any branch condition.
- The reason vocabulary is the three new values; the two retired ones are gone from the code and
  documented as the retired bar's vocabulary, with historical artifacts left untouched.
- Every verdict branch names RULE A, and no branch asserts a bar-clearing that did not happen.
- The pre-commitment against reactive policy-tuning is recorded in `decideAcceptance`'s own doc
  comment.
- `tsc --noEmit` clean, repo-root `npx vitest run` green, digest cases unchanged.
</success_criteria>

<output>
Create `.planning/quick/260905-t88-codify-rule-a-in-decideacceptance-accept/260905-t88-SUMMARY.md`
when done. Return the SUMMARY text to the orchestrator rather than writing it from a subagent if
the Write tool refuses the path.

The orchestrator — not the executor — moves
`.planning/todos/pending/codify-rule-a-acceptance.md` to `completed/` with a dated closure note.
</output>
