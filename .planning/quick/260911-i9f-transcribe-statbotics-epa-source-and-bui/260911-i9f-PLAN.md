---
phase: quick-260911-i9f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/models/statbotics-breakdown-reference.md
  - docs/models/epa-statbotics-gap.md
  - .planning/quick/260911-i9f-transcribe-statbotics-epa-source-and-bui/260911-i9f-SUMMARY.md
autonomous: true
requirements: [QUICK-260911-i9f]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every Statbotics EPA function this project's model depends on is quoted VERBATIM in the repo, byte-identical to the fetched source, and a script proves it rather than a reader trusting it."
    - "The repo no longer claims Statbotics rates one number per alliance. It states what predict_match actually does: sums a per-team VECTOR over all_keys[year], and reads a varying subset of that vector per season."
    - "The secondhand-summary caveat is scoped to the prose it actually applies to, and does not hang over newly transcribed verbatim code."
    - "Every corpus season (2016-2019, 2022-2026) has a per-mechanism verdict of ALREADY MATCHES / GAP / DELIBERATE DIFFERENCE against Statbotics, with no blank cells."
    - "Everything Statbotics does that CANNOT be reproduced under the two locked decisions is named per season with its match-count exposure, not footnoted."
    - "Every season-aggregate quantity Statbotics reads is enumerated exhaustively from the source, so the one documented difference is a closed list rather than an open-ended 'at least three'."
    - "A reader can pick up the next stage of this effort without re-deriving anything: the gap doc ends with an ordered, reasoned stage list, each sized as its own quick task and each naming its republish debt."
    - "No model, package, script, app, artifact or version changed. This task is reference and analysis only, and git proves it."
  artifacts:
    - docs/models/statbotics-breakdown-reference.md
    - docs/models/epa-statbotics-gap.md
  key_links:
    - "scratchpad/statbotics/*.py (already-fetched bytes) -> verbatim ```python fences in statbotics-breakdown-reference.md (substring-identical, script-checked)"
    - "get_score_from_breakdown's per-year branch -> the per-season verdict in reference §8 -> the per-season row in the gap doc's score-formula mechanism"
    - "the RP-dropped locked decision -> get_score_from_breakdown's 2016/2017 elim branches -> the gap doc's cannot-be-reproduced section and its ~4,964-match exposure"
    - "the walk-forward locked decision -> the enumerated season-aggregate list (year.score_sd, get_foul_rate, get_constants, get_mean_components, comp_N_mean) -> the gap doc's one DELIBERATE DIFFERENCE category"
---

<objective>
Transcribe Statbotics' EPA source into this repo verbatim, correct two load-bearing errors the
previous task shipped, and produce the authoritative per-season gap table between Statbotics' EPA
and SigmaScout's EPA.

Purpose: this is the FOUNDATION step of a multi-stage effort to make SigmaScout's EPA reproduce
Statbotics' EPA. The developer's requirement is explicit and locked: *"I NEED to be able to
reproduce statbotics EPA perfectly."* Whether SigmaScout's EPA gets better or worse as a result is
deliberately not a consideration — a faithful reproduction is what makes the project's headline
claim ("measurably better than Statbotics") mean literally what it says. Every prior attempt at
this has stalled on the same thing: a Statbotics detail NAMED in a planning document and never
written into the repo. This task ends that by putting the bytes here.

Output: `docs/models/statbotics-breakdown-reference.md` extended into a complete verbatim
reference and corrected; `docs/models/epa-statbotics-gap.md` created, carrying the per-season
per-mechanism gap table, the cannot-be-reproduced register, and the recommended stage sequence.

**NO MODEL CHANGES. NO CODE CHANGES OF ANY KIND.** See `<hard_prohibitions>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

Already-fetched Statbotics source (the executor has NO network — read these bytes, never re-fetch):
`C:/Users/Jacob/AppData/Local/Temp/claude/c--Users-Jacob-Documents-GitHub-SigmaScout/a6c4c664-07f1-4630-9f25-d553556258fb/scratchpad/statbotics/`
- `models_epa_constants.py` (115 B)   = upstream `backend/src/models/epa/constants.py`
- `models_epa_math.py` (1282 B)       = upstream `backend/src/models/epa/math.py`
- `models_epa_init.py` (1989 B)       = upstream `backend/src/models/epa/init.py`
- `models_epa_main.py` (10201 B)      = upstream `backend/src/models/epa/main.py`
- `models_epa_breakdown.py` (7991 B)  = upstream `backend/src/models/epa/breakdown.py`
- `breakdown.py` (7311 B)             = upstream `backend/src/breakdown.py`

Fetched 2026-09-11 from `raw.githubusercontent.com/avgupta456/statbotics/master/backend/src/`.

Repo-side context:
@docs/models/statbotics-breakdown-reference.md
@docs/models/epa-divergences.md
@packages/core/algorithms/epa.ts
@packages/core/algorithms/breakdown/index.ts
@.planning/quick/260911-gfe-add-the-epa-component-map-seam-and-settl/260911-gfe-SUMMARY.md
</context>

<locked_decisions>
Two decisions are LOCKED by the developer. They are the frame for every verdict in the gap doc.
Do not relitigate either, and do not soften either into a "for now".

**L-01 — SigmaScout's reproduction carries NO walk-forward violations of its own.**
Statbotics reads SEASON-AGGREGATE quantities that are not computable walk-forward (a Week 1
prediction cannot know a season-end number). SigmaScout live-estimates every one of them from the
data available so far, exactly as `epa@8.0.0` already does for the carryover scale anchor.
**That whole category IS the single documented difference** on the methodology page — one
difference, not one per quantity. The gap doc must therefore enumerate that category
EXHAUSTIVELY from the source so it is a closed list.

**L-02 — EPA MUST NEVER PREDICT RANKING POINTS.** Developer, verbatim: *"I do not want EPA
predicting RP at all ever."* `rp_1`/`rp_2`/`rp_3`, `unit_sigmoid`, `inv_unit_sigmoid` and every
RP slot are DROPPED from any SigmaScout adoption. They are still TRANSCRIBED in task 1 (the
reference must be complete), but they are marked NOT ADOPTED in the gap doc, and anything
Statbotics does that depends on them is a cannot-be-reproduced flag rather than a gap to close.

End state, for orientation only (not built in this task): the methodology page carries exactly two
table columns, SigmaScout EPA and Statbotics EPA.
</locked_decisions>

<hard_prohibitions>
Violating any of these fails the task outright.

1. **NO CODE CHANGES.** Do not edit, create or delete anything under `packages/`, `apps/`,
   `scripts/`, `pipeline/`, `experiments/`, `data/`, `reports/`, or any `.ts`/`.tsx`/`.json`
   file anywhere. The ONLY files this task may write are the two markdown files in `docs/models/`
   and its own SUMMARY. Several downstream files carry claims this task proves wrong — record
   them in the gap doc, do NOT fix them here. Fixing them is stage 1 of the sequence you propose.
2. **NO version bumps, NO algorithm-identity changes, NO republish, NO `publish:seasons`, NO
   `publish.ts --event`** (it replays a season cold and produces different numbers — see the
   project memory note).
3. **NO retune, NO tuning run, NO `sigmascout-retune-republish` skill.**
4. **Do not touch BPR/SPR in any way, and do not spend or approach its sealed 2016-2022/2023-2026
   holdout.** BPR tuning is a permanently closed topic by developer decision.
5. **NO NETWORK.** The executor sandbox denies network Bash. Do not `curl`, `WebFetch`, `git
   fetch`, `pnpm add`, or otherwise reach outside this machine. The Statbotics source is already
   on disk at the scratchpad path above.
6. **NEVER GUESS AT SOURCE DETAIL.** If a fact you need is not present in one of the six
   scratchpad files, you do not have it. Write it down as a named residual gap with the upstream
   filename that would answer it, and move on. Do not reconstruct it from memory, from a prior
   planning document's claim, or from what the code "must" do.
7. **Never render `.env` or any secret into output.** See `.claude/CLAUDE.md` "Secrets handling".
   Nothing in this task needs a credential.
8. **Git staging discipline.** Another Claude session is active in this checkout with untracked
   plans under `.planning/phases/09-*`, and `data/diagnostics/epa-deviation-ablation.json` is
   untracked from a prior task. NEVER `git add -A` or `git add .`. Stage only your own files by
   explicit path and commit with an explicit pathspec:
   `git commit -m "..." -- docs/models/statbotics-breakdown-reference.md docs/models/epa-statbotics-gap.md`
   Run `git status --short` after every commit and READ it: your files must be gone from the
   list and every foreign untracked file must still be there, untouched.
9. **Verify by reading output, never by exit code.** `timeout <n> pnpm <cmd>` swallows output and
   exits 0 on this machine. Read the printed lines and judge them.
</hard_prohibitions>

<fidelity_rules>
These govern task 1 and are enforced by an automated gate, so read them before writing.

- A fenced block tagged ` ```python ` is a CONTRACT: its contents must appear as a contiguous,
  byte-identical substring of one of the six scratchpad files (trailing whitespace excepted).
- Therefore: **never** elide with `...` inside a python fence, **never** add your own comment
  inside a python fence, **never** merge two non-adjacent regions into one fence. If you need two
  regions, use two fences.
- Anything reconstructed, summarised, secondhand or illustrative uses ` ```text ` instead, and
  carries a one-line note saying what it is. This applies to the existing §1 and §2 blocks in
  the file, which are python-fenced today and are NOT verbatim quotations of anything in the
  scratchpad — they must be retagged or replaced.
- Two comment lines in the fetched bytes read like fetch-time annotations rather than upstream
  code: `models_epa_breakdown.py`'s `# computed inline (not a stored component)` inside the 2023
  branch, and `breakdown.py`'s `# Derived fields computed from stored EPA components...` header
  above `derived_breakdown`. Quote them exactly as the file has them — the gate compares against
  the file, not against upstream — and add a note OUTSIDE the fence that you cannot distinguish an
  upstream comment from a fetch-time annotation and that neither is load-bearing.
</fidelity_rules>

<tasks>

<task type="tracer">
  <name>Task 1: Transcribe the Statbotics EPA source verbatim, and correct the two errors the previous task shipped</name>
  <files>docs/models/statbotics-breakdown-reference.md</files>
  <precondition>All six `.py` files listed in `&lt;context&gt;` exist in the scratchpad directory with the stated byte sizes. If any is missing, stop and report which one — do not proceed on five files and do not re-fetch.</precondition>
  <read_first>
    All six scratchpad `.py` files in full, then the existing
    `docs/models/statbotics-breakdown-reference.md` in full. Read each file ONCE and extract
    everything you need in that pass.
  </read_first>
  <action>
Restructure `docs/models/statbotics-breakdown-reference.md` into the complete verbatim reference,
obeying `&lt;fidelity_rules&gt;` throughout.

**A. Fix the provenance framing (correction 1 of 2, load-bearing).** The file's current
"Provenance and its limits" block warns that the prose is a secondhand small-model summary and
that any conclusion needing exact key spellings is not established. That caveat is TRUE of the
file's OLD content and must survive, but it must not be left hanging over newly transcribed
verbatim code. Split provenance into two explicitly scoped blocks: one covering the 2026-09-11
verbatim transcription (name all six files, their byte sizes, the upstream paths, the fetch
origin and date, and the fact that the transcribing agent had no network and read already-fetched
bytes), and one covering the residual secondhand prose, naming exactly which sections it still
applies to. Every section heading in the file must be unambiguously under one block or the other.

**B. Transcribe, in this order, each under its own heading.** Minimum coverage:
- `models_epa_constants.py` — the entire file, one fence.
- `models_epa_math.py` — `EPARating` including `update_mean` and `add_obs`; `zero_sigmoid`;
  `unit_sigmoid`; `inv_unit_sigmoid`. Include the module-level comment about the 5-sample
  asymptote and the p=0.8 EWMA decay: it is the only statement of what the update rule's shape
  means and it belongs in the record.
- `models_epa_init.py` — `get_constants` and `get_init_epa`, both entire.
- `models_epa_main.py` — `k_func`, `margin_func`, `percent_func`, `start_season`,
  `predict_match`, and the full update/attribution path (`attribute_match`, `update_team`). Also
  transcribe `pre_record_team`/`post_record_team`/`record_match`, which are what actually defines
  the published field set and its rounding.
- `models_epa_breakdown.py` — `post_process_breakdown`, `get_score_from_breakdown` and
  `post_process_attrib`, ALL THREE IN FULL, every per-year branch included, no exceptions.
- `breakdown.py` — the `key_to_name` table in full for every season present (2016-2026 including
  the empty 2021), the `derived_breakdown` table, and the `all_keys` construction loop. The loop
  is what makes the vector's INDEX ORDER knowable, so it is not optional.

**C. Correct the component-vector conclusion (correction 2 of 2, load-bearing).** The file's §3
and §5 conclude Statbotics has no component vector. That conclusion is WRONG, it was published to
the developer by quick task 260911-gfe, and correcting it precisely is a named deliverable here.
The source says: `predict_match` computes
`np.array([self.epas[t].mean for t in teams]).sum(axis=0)` — a PER-TEAM VECTOR over
`all_keys[year]`, summed component-wise across the alliance, indexed by key NAME via
`keys.index(...)`. Statbotics rates a vector per team, exactly as SigmaScout does.

What is true is narrower and must be stated with that narrowness: `get_score_from_breakdown`
READS only `no_foul_points` out of that vector for most seasons — but not all. Build a table from
the verbatim branches showing, per season, exactly which vector entries the predicted score reads.
Read the branches rather than generalising: 2018 and 2023 each read MANY entries, and the `else`
fallback covers seasons with no branch of their own. Rewrite §3 and §5 around this. Delete or
rewrite any sentence in the file asserting that Statbotics rates a single quantity as a general
claim; keep per-season claims that the branches actually support.

**D. Re-derive §8's per-season verdict table against the now-verbatim branches.** Today §8 says
"not established" for every branch except 2024 and reaches every verdict through a structural
argument about `all_keys` overlapping. Both inputs changed: the branches are now transcribed, and
§3's structural argument was wrong. Re-derive each of the nine corpus seasons' verdict from its
actual `get_score_from_breakdown` branch plus its `post_process_breakdown` branch, and replace
every "not established" evidence cell with a citation to the section of this file that now carries
the code. Where a season's score formula is non-linear in the rated components (caps via `min`,
`zero_sigmoid` terms), say so — that is a different and more precise reason than "the key list
double-counts", and it changes what a faithful reproduction would have to do.

**E. Enumerate the season-aggregate quantities EXHAUSTIVELY (per L-01).** Grep the six files for
every read of a `Year`-object aggregate and list each with its file, its call site, and what it
feeds. The brief named three; the source has more than three — `get_mean_components` and the
2018-only `comp_N_mean` reads in `post_process_attrib` are both in this category and both are
easy to miss. Produce the closed list, and state the count explicitly so a later reader can tell
at a glance whether their list is complete.

**F. Record the residual gaps, do not close them by guessing (per prohibition 6).** At minimum:
`backend/src/tba/breakdown.py` — the module that CLEANS TBA's raw score breakdown into
`no_foul_points`/`auto_points`/`teleop_points`/`endgame_points`/`comp_0..comp_9` — was not
fetched and is not in the scratchpad. Verify this by grepping the scratchpad for the strings that
would have to appear in it. Consequence to record: the per-year CLEANING formulas remain
untranscribed, and §2's `error = no_foul_points - (auto_points + teleop_points + endgame_points)`
fragment does not appear in ANY fetched file, so §2 stays secondhand and unverified — retag its
fence per `&lt;fidelity_rules&gt;` and say plainly that this task could not confirm it. Name the
upstream file that would answer it so the next fetch is one line of work.

Preserve, unchanged in substance: §6 (the mislabelled 0.7461 figure and its relabelling), §7 (the
false "already transcribed" claim), §9 and §10. Update cross-references if you renumber sections.
  </action>
  <verify>
    <automated>
Write a checker to the SESSION SCRATCHPAD (never into the repo) and run it:

mkdir -p "$SCRATCH" &amp;&amp; cat &gt; "$SCRATCH/verify-verbatim.cjs" &lt;&lt;'EOF'
const fs = require('fs'), path = require('path');
const SRC = process.argv[2], DOC = process.argv[3];
const corpus = fs.readdirSync(SRC).filter(f =&gt; f.endsWith('.py'))
  .map(f =&gt; fs.readFileSync(path.join(SRC, f), 'utf8').replace(/\r\n/g, '\n'));
const doc = fs.readFileSync(DOC, 'utf8').replace(/\r\n/g, '\n');
const fence = new RegExp('```python\\n([\\s\\S]*?)```', 'g');
const blocks = [...doc.matchAll(fence)].map(m =&gt; m[1]);
let bad = 0;
blocks.forEach((b, i) =&gt; {
  const t = b.replace(/[ \t]*\n/g, '\n').replace(/\s+$/, '');
  if (!corpus.some(c =&gt; c.replace(/[ \t]*\n/g, '\n').includes(t))) {
    bad++;
    console.log('NOT VERBATIM block #' + (i + 1) + ' :: ' + t.split('\n')[0]);
  }
});
const need = ['k_func','margin_func','percent_func','start_season','predict_match',
  'attribute_match','update_team','post_process_breakdown','get_score_from_breakdown',
  'post_process_attrib','get_constants','get_init_epa','add_obs','zero_sigmoid',
  'unit_sigmoid','inv_unit_sigmoid','key_to_name','all_keys','ELIM_WEIGHT','MEAN_REVERSION',
  'YEAR_ONE_WEIGHT','INIT_PENALTY','NORM_MEAN','NORM_SD'];
const missing = need.filter(n =&gt; !doc.includes(n));
console.log('python fences: ' + blocks.length + ' | non-verbatim: ' + bad +
  ' | missing symbols: ' + (missing.length ? missing.join(',') : 'none'));
EOF

node "$SCRATCH/verify-verbatim.cjs" "$SCRATCH/../statbotics" docs/models/statbotics-breakdown-reference.md

(Substitute the real scratchpad path from `&lt;context&gt;` for both arguments.)

PASS condition, read from the printed summary line: `non-verbatim: 0`, `missing symbols: none`,
and `python fences:` at least 14. Any `NOT VERBATIM` line names a block that must be fixed —
either correct the quotation or retag the fence as ```text per `&lt;fidelity_rules&gt;`.
    </automated>
  </verify>
  <done>
Every function named in the action's section B appears in the file inside a python fence that is
byte-identical to the fetched source; the checker prints `non-verbatim: 0` and
`missing symbols: none`; provenance is split into two explicitly scoped blocks with every section
assigned to one; §3/§5 state that Statbotics sums a per-team vector over `all_keys[year]` and
carry the per-season table of which vector entries the score reads; §8's verdicts are re-derived
from the transcribed branches with no "not established" evidence cell left for a corpus season;
the season-aggregate list is closed and counted; `backend/src/tba/breakdown.py` is recorded by
name as the residual gap and §2 is marked unverified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the per-season gap table between Statbotics EPA and SigmaScout EPA</name>
  <files>docs/models/epa-statbotics-gap.md</files>
  <read_first>
    `packages/core/algorithms/epa.ts` (the whole file — it is 1309 lines and one pass is enough;
    pay attention to `predictCore`, `updateCore`, `applyComponentUpdate`, `twoStageEwma`,
    `epaPercentFunc`, `foulsCommittedCarryForward`, `carrySeason`, `EPA_ELIM_WEIGHT`,
    `EPA_K`, `EPA_FALLBACK_SCORE_SD`), `packages/core/algorithms/carryover.ts`,
    `packages/core/algorithms/breakdown/index.ts`, and two season maps
    (`breakdown/2024.ts` and `breakdown/2018.ts`) as representative examples — do NOT read all
    ten. `docs/models/epa-divergences.md`. Do not re-read the scratchpad files or the reference
    doc; you transcribed them in task 1 and they are in context.
  </read_first>
  <action>
Create `docs/models/epa-statbotics-gap.md` — the authoritative per-season gap record. Its audience
is the agent executing the next stage, so it must be usable without re-deriving anything.

**Structure.** Open with a scannable verdict matrix: mechanisms down, the nine corpus seasons
(2016, 2017, 2018, 2019, 2022, 2023, 2024, 2025, 2026) across, each cell exactly one of
`ALREADY MATCHES` / `GAP` / `DELIBERATE DIFFERENCE`. **No cell may be blank, and no fourth label
may be invented** — a cell that cannot be closed is `GAP` plus a pointer into the
cannot-be-reproduced section. 2020 is registered in `breakdown/index.ts` but is not a corpus
season and is not replayed; give it one line of explanation and leave it out of the matrix.

Then one section per mechanism, each with a season table of: season | what Statbotics does (cite
the transcribed section of `statbotics-breakdown-reference.md`) | what SigmaScout does today
(cite `file.ts:symbol`) | what closing the gap requires | verdict.

**Mechanisms — these eight are mandatory:**
1. The rated component vector — `all_keys[year]` versus `breakdown/{year}.ts`'s
   `OWN_FIELD_COMPONENT_MAP`. Both sides rate a vector (task 1 established this); the gap is which
   entries and how many.
2. `get_score_from_breakdown`'s per-year score formula versus SigmaScout's sum-of-components.
3. `post_process_breakdown`'s per-year adjustments — 2018's switch/scale `zero_sigmoid`
   contest terms, 2023's 9-piece caps and the cube/cone regrading, 2025's processor-algae
   correction, and the `breakdown[0] += total_change` writeback. SigmaScout applies none of these.
4. The RP `unit_sigmoid` path — `post_process_breakdown`'s RP sigmoid, `get_init_epa`'s
   `inv_unit_sigmoid` pre-image, `post_process_attrib`'s elim RP freeze. **NOT ADOPTED per L-02**:
   verdict `DELIBERATE DIFFERENCE` in every season, and say so in one sentence rather than nine.
5. The foul model — Statbotics' season-level `get_foul_rate()` scalar applied as
   `score * (1 + foul_rate)` AFTER the win probability is computed, versus SigmaScout's per-team
   `foulsCommitted` component cross-attributed to the opponent (D-04). Note precisely where in
   `predict_match` the foul multiplier lands relative to `norm_diff`/`win_prob`, because it
   changes whether fouls affect the predicted WINNER at all.
6. Init and carryover — `get_init_epa`, `get_constants`, `NORM_MEAN`/`NORM_SD`/`INIT_PENALTY`/
   `YEAR_ONE_WEIGHT`/`MEAN_REVERSION`, the `max(-year_mean / num_teams / year_sd, ...)`
   non-negativity floor, and the 2026 `district == "isr"` exception — versus
   `carryover.ts` + `epaCarryScale.ts` as shipped at `epa@8.0.0`.
7. Elimination weighting — `ELIM_WEIGHT = 1/3` versus `EPA_ELIM_WEIGHT`. Already adopted;
   confirm it against both sources rather than inheriting the claim, and record the confirmation.
8. The win-probability scale — `norm_diff = (red - blue) / year.score_sd` with
   `win_prob = 1 / (1 + 10 ** (k * norm_diff))` versus SigmaScout's expanding-window SD and
   `EPA_K`. Check the `k_func` sign and base against `EPA_K` explicitly.

**Add any further mechanism the source reveals.** Expected candidates you should evaluate and
include if real: the update rule itself (`add_obs`'s two-stage EWMA, `percent_func`'s
`min(0.5, max(0.3, ...))` schedule and its `2/3` versus `1/2` era factor, `margin_func`'s
opponent-error subtraction) versus `twoStageEwma`/`epaPercentFunc`; `post_process_attrib`'s 2018
total-points overwrite and 2025 processor correction; the published field set and rounding in
`post_record_team`. Do not pad the list — include a mechanism only if the two implementations can
actually disagree about a number.

**The cannot-be-reproduced register.** Its own section, its own table: season | mechanism | which
locked decision it collides with | why no workaround exists | exposure in matches and as a
percentage of that season. Seed it with the known one, already measured by the orchestrator — do
NOT re-measure it: `get_score_from_breakdown`'s 2016 and 2017 branches fold RP predictions into
the score for ELIMINATION matches only (2016 adds `rp_1_pred * 20 + rp_2_pred * 25`; 2017 adds
`rp_1_pred * 100 + rp_2_pred * 20`). Under L-02 those terms do not exist, so those matches cannot
match. Exposure: **2,223 official elim matches in 2016, 16.7% of that season's 13,286; 2,741 in
2017, 17.8% of 15,424** — roughly 5,000 matches, about one in six across those two seasons. State
plainly that this is a real scoping decision for the developer, not a footnote, and lay out what
the options actually are (accept the two seasons never reproduce in elims / reproduce them only
in quals / revisit L-02 — while noting L-02 is locked and this task does not reopen it).

**Then SEARCH for any other collision** and report each the same way. Work from the transcribed
source, not from memory: check every site where an RP slot or a season-aggregate feeds something
that affects a SCORE or a WIN PROBABILITY, and state for each whether it collides or not — a
ruled-OUT collision is a finding worth recording, because it stops the next reader re-checking it.
If you find a new collision whose exposure you cannot state, ONE read-only count against
`data/corpus.sqlite` is permitted; record the exact SQL in the doc. If the corpus cannot answer
it, write the exposure as unmeasured. Never estimate a match count.

Close with the L-01 category: the enumerated season-aggregate list from task 1, presented as the
ONE documented difference, with what SigmaScout live-estimates in place of each.
  </action>
  <verify>
    <automated>
Write a checker to the SESSION SCRATCHPAD (never into the repo) and run it:

cat &gt; "$SCRATCH/verify-gap.cjs" &lt;&lt;'EOF'
const fs = require('fs');
const doc = fs.readFileSync('docs/models/epa-statbotics-gap.md', 'utf8').replace(/\r\n/g, '\n');
const seasons = [2016, 2017, 2018, 2019, 2022, 2023, 2024, 2025, 2026];
const labels = ['ALREADY MATCHES', 'GAP', 'DELIBERATE DIFFERENCE'];
const rows = doc.split('\n').filter(l =&gt; l.trim().startsWith('|'));
const matrix = rows.filter(l =&gt; labels.some(v =&gt; l.includes(v)));
const verdictCells = matrix.flatMap(l =&gt; l.split('|').slice(1, -1).map(c =&gt; c.trim()));
const bogus = verdictCells.filter(c =&gt; /^(ALREADY|GAP|DELIBERATE|MATCH|TODO|TBD|N\/A|-|\?)/i.test(c)
  &amp;&amp; !labels.includes(c) &amp;&amp; !c.startsWith('GAP '));
const missingSeason = seasons.filter(y =&gt; !doc.includes(String(y)));
const has2223 = doc.includes('2,223') || doc.includes('2223');
const has2741 = doc.includes('2,741') || doc.includes('2741');
console.log('verdict rows: ' + matrix.length +
  ' | seasons absent: ' + (missingSeason.length ? missingSeason.join(',') : 'none') +
  ' | malformed verdict cells: ' + (bogus.length ? JSON.stringify(bogus.slice(0, 5)) : '0') +
  ' | 2016+2017 elim exposure present: ' + (has2223 &amp;&amp; has2741));
EOF

node "$SCRATCH/verify-gap.cjs"

PASS condition, read from the printed line: `seasons absent: none`, `malformed verdict cells: 0`,
`2016+2017 elim exposure present: true`, and `verdict rows:` at least 9. Then read the verdict
matrix yourself and confirm by eye that it has no empty cell — the checker catches malformed
labels, you catch omissions.
    </automated>
  </verify>
  <done>
`docs/models/epa-statbotics-gap.md` exists with a complete verdict matrix over the nine corpus
seasons and at least the eight mandatory mechanisms, every cell carrying exactly one of the three
allowed labels; each mechanism has its own section citing both sides by file and symbol; the
cannot-be-reproduced register carries the 2016/2017 elim-RP collision with its 2,223 / 2,741 match
exposure and its percentages, states the scoping options, and reports the outcome of a search for
further collisions including the ones ruled out; the L-01 season-aggregate list appears as the one
documented difference.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the gap doc with the recommended stage sequence, then commit</name>
  <files>docs/models/epa-statbotics-gap.md</files>
  <action>
Append the final section to `docs/models/epa-statbotics-gap.md`: the recommended execution order
for the follow-on stages of this effort.

Rules for the sequence:
- Each stage is sized as **its own quick task** — one coherent change, independently verifiable.
- Order **most foundational or riskiest first**, and state the reason for each position. A stage
  placed early because a later stage's measurement would be meaningless without it must say so.
- Each stage names: what it changes, which gap-table rows it closes, how it would be verified,
  and **its republish debt**. Every stage that changes a published EPA number accrues one.
  Note explicitly that `epa@8.0.0` ALREADY owes a republish, so the debt is cumulative and the
  published methodology numbers are stale until it is paid.
- **Stage 1 must be the correctness cleanup that costs nothing:** three surfaces in this repo
  still carry the "Statbotics rates one number per alliance" claim that task 1 proved wrong, and
  one of them is published to users. Name them with file and location so the follow-on task is
  turnkey: `apps/web/src/components/methodology/epaComparisonContent.ts` (the `component-maps`
  entry's paragraphs and the revision note above them),
  `scripts/measureEpaDeviations.ts` (the `deviationRegister()` `component-map` entry's `reason`
  string), and `docs/models/epa-divergences.md` §6. Record that
  `scripts/measureEpaDeviations.test.ts` asserts that `reason` still cites
  `statbotics-breakdown-reference`, so a reword must keep that citation. This stage changes no
  number and owes no republish, which is exactly why it goes first.
- Where a stage is blocked on something not in this repo — for example anything needing
  `backend/src/tba/breakdown.py`'s cleaning formulas — say what must be fetched and mark the
  stage as blocked rather than ordering it as if it were ready.
- Flag any stage that would force the developer's hand on the 2016/2017 elim-RP collision, since
  that is a decision and not an implementation.

Then commit. Stage ONLY your two docs by explicit path (prohibition 8):

  git add docs/models/statbotics-breakdown-reference.md docs/models/epa-statbotics-gap.md
  git commit -m "docs(quick-260911-i9f): transcribe Statbotics EPA source and build the gap table" -- docs/models/statbotics-breakdown-reference.md docs/models/epa-statbotics-gap.md

Then run `git status --short` and READ it: your two docs must be gone from the list, and
`data/diagnostics/epa-deviation-ablation.json` plus every `.planning/phases/09-*` file must still
be listed as untracked, untouched. If any foreign file was swept in, reset it out before
proceeding — do not amend past it.

Finally, `git show --stat HEAD` and read the output: exactly two files, both under `docs/models/`.
If any `packages/`, `apps/`, `scripts/` or `data/` path appears, the task violated its
prohibitions and must be corrected.
  </action>
  <verify>
    <automated>
git show --stat HEAD -- . | tail -8

PASS condition, read from the output: exactly two changed files, both matching
`docs/models/*.md`, and no path under `packages/`, `apps/`, `scripts/`, `pipeline/`,
`experiments/`, `data/` or `reports/`.

Then: git status --short

PASS condition, read from the output: neither of the two docs appears, and
`data/diagnostics/epa-deviation-ablation.json` still appears as untracked.
    </automated>
  </verify>
  <done>
The gap doc ends with an ordered stage sequence, each stage sized as its own quick task with its
ordering reason, verification approach and republish debt, stage 1 being the zero-cost correction
of the three surfaces still carrying the disproven claim; both docs are committed in a single
commit touching nothing outside `docs/models/`; the working tree still carries every foreign
untracked file it started with.
  </done>
</task>

</tasks>

<verification>
- `node verify-verbatim.cjs` prints `non-verbatim: 0` and `missing symbols: none`.
- `node verify-gap.cjs` prints `seasons absent: none`, `malformed verdict cells: 0`,
  `2016+2017 elim exposure present: true`.
- `git show --stat HEAD` lists exactly two files, both under `docs/models/`.
- `git status --short` shows the foreign untracked files still present and untouched.
- No network call was made, no `.env` was read, no version string changed, no test file changed.
</verification>

<success_criteria>
Someone picking up the next stage of this effort can open `docs/models/epa-statbotics-gap.md`,
see exactly which mechanisms already match and which do not for every corpus season, read the
verbatim Statbotics code for any of them one file away, know which parts can never match under the
two locked decisions and how many matches that costs, and start the next stage without re-fetching
a third-party source or re-deriving a conclusion. The repo no longer asserts that Statbotics rates
one number per alliance.
</success_criteria>

<output>
Create `.planning/quick/260911-i9f-transcribe-statbotics-epa-source-and-bui/260911-i9f-SUMMARY.md` when done.

Note for the executor: the `Write` tool is blocked on `SUMMARY.md` for subagents in this project.
Return the SUMMARY text in your final message and let the orchestrator write it — do not route
around the block with `Bash`.
</output>
