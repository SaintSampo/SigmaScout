---
quick_id: 260903-4fs
phase: quick-260903-4fs
slug: author-the-2019-and-2020-breakdown-and-r
date: 2026-09-03
type: execute
mode: quick
plan: "01"
wave: 1
worktree: false
autonomous: true
depends_on: []
source: .planning/quick/260903-4fs-author-the-2019-and-2020-breakdown-and-r/260903-4fs-CONTEXT.md
requirements: [RP-1, RP-2, RP-3, RP-4, BD-1, BD-2]
files_modified:
  - packages/core/algorithms/breakdown/2019.ts
  - packages/core/algorithms/breakdown/2020.ts
  - packages/core/algorithms/breakdown/index.ts
  - packages/core/algorithms/breakdown/groups.ts
  - packages/core/algorithms/breakdown/groups.test.ts
  - packages/core/algorithms/breakdown/reconciliation.test.ts
  - packages/core/algorithms/sigma1/rp/2019.ts
  - packages/core/algorithms/sigma1/rp/2020.ts
  - packages/core/algorithms/sigma1/rp/rules.ts
  - packages/core/algorithms/sigma1/rp/rules.test.ts
  - packages/core/algorithms/sigma1/rp/reconciliation.test.ts

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "`componentMapForSeason(2019)` and `componentMapForSeason(2020)` return a map, and `rpRuleModuleForSeason(2019)`/`(2020)` return a module — all four, or the season is unusable (CONTEXT: `carrySeason` calls both unconditionally)."
    - "For 2019 and 2020, sum(own offensive components) + foulsCommitted(opponent) === that alliance's own alliance total, at 1e-6, for every row `breakdown/reconciliation.test.ts` samples — zero mismatches, no tolerance mechanism exists in that suite and none is added."
    - "2019 `habDocking` (per D RP-1) reconciles against TBA's recorded flag at EXACTLY 0 mismatches at every event_type present (0, 1, 2, 3, 5, 100). Pre-measured this session: 0/29,858 sides. A single mismatch means the module is wrong."
    - "2020 `shieldOperational` (per D RP-3) reconciles at EXACTLY 0 mismatches at every event_type present (0, 1, 100). Pre-measured: 0/7,640 sides."
    - "2019 `completeRocket` (per D RP-2) under-fires only — every disagreement is a false negative and the false-POSITIVE count is 0 at every event_type. An over-fire is a bug, never a tolerance."
    - "The single `KNOWN_TOLERANCES` entry this task adds (2019 `completeRocket`) carries a rate DERIVED from the test's own printed mismatch report, not from a value chosen to make the suite pass."
    - "`maxRp` is 4 for 2019 and 3 for 2020, arrived at structurally as `winRp + bonusNames.length` — never hand-written. `winRp` is 2 and `tieRp` is 1 for both, confirmed against `red_rp_earned`/`blue_rp_earned` over 37,498 sides with 0 mismatches."
    - "2020's `shieldEnergized` is absent from `bonusNames` and from `thresholdVariables`, and its recorded flag never enters `recordedBonusFlags` (D RP-4) — a recorded flag with no recomputed twin would break the paired shape `reconciliation.test.ts` relies on."
    - "No per-robot field is read by any of the four modules — proven by the existing `no parsed {year} component record has a key ending in Robot1/Robot2/Robot3` assertion, now running for 2019 and 2020."
    - "No roll-up key is read outside a doc comment in either breakdown module — necessary because 2019's period roll-up is NUMERICALLY IDENTICAL to its component, so reconciliation alone cannot catch that substitution."
    - "Every season the two registries name has a declared component grouping, and `groups.test.ts` enforces it for 2019 and 2020 as it already does for 2022-2026."
  artifacts:
    - "packages/core/algorithms/breakdown/2019.ts — 4 offensive components + adjust + foulsCommitted, Zod strip-mode SideSchema, Object.create(null) allowlist loop"
    - "packages/core/algorithms/breakdown/2020.ts — 5 offensive components + adjust + foulsCommitted, same discipline"
    - "packages/core/algorithms/sigma1/rp/2019.ts — bonuses habDocking + completeRocket, one threshold variable, conservative-branch predictThresholds for the rocket"
    - "packages/core/algorithms/sigma1/rp/2020.ts — bonus shieldOperational only, one threshold variable, base-tier-only measurement caveat stated in the header"
    - "Registry entries in breakdown/index.ts and sigma1/rp/rules.ts; grouping entries in breakdown/groups.ts"
    - "One measured KNOWN_TOLERANCES entry in sigma1/rp/reconciliation.test.ts, plus two exact-boundary CHECKS entries"
  key_links:
    - "`SEASON_COMPONENT_MAPS` (breakdown/index.ts) <-> `REGISTERED_SEASONS` (breakdown/reconciliation.test.ts). The breakdown suite does NOT iterate the registry — it carries a hardcoded literal. Registering a season without editing that literal leaves the new map completely unproven while every suite still passes green."
    - "`RP_RULE_MODULES` (rp/rules.ts) -> `RP_REGISTERED_SEASONS` -> five `describe.each`/`it.each` blocks across rp/reconciliation.test.ts and rp/rules.test.ts. This side DOES iterate, so registration alone puts 2019/2020 under corpus-wide test — including a hard-coded `toEqual([2022..2026])` assertion that starts failing the instant the registry grows."
    - "`bonusNames` (each rp module) <-> `KNOWN_TOLERANCES[].bonus` (rp/reconciliation.test.ts). The tolerance is matched by string; a renamed bonus silently detaches its tolerance and the suite fails with an unexplained mismatch rather than a missing-tolerance error."
    - "`GROUPS_BY_SEASON` (breakdown/groups.ts) <-> `componentMapForSeason(season).components`. groups.test.ts asserts every declared component is grouped or explicitly ungrouped — a component name typo'd in one place and not the other fails there, not in reconciliation."
---

<objective>
Author and register the 2019 and 2020 score-component maps and RP rule modules, encoding
thresholds that were already derived and measured, so `.planning/todos/pending/extend-corpus-2019-2020.md`
can replay those two seasons.

Purpose: the corpus already holds both seasons (verified live this session: 2019 = 14,929
official quals with a breakdown, 2020 = 3,820). What blocks a replay is that
`componentMapForSeason` and `rpRuleModuleForSeason` throw for any unregistered season, and
`carrySeason` calls both unconditionally. Four modules and two registry entries unblock it.

Output: `breakdown/{2019,2020}.ts`, `sigma1/rp/{2019,2020}.ts`, their registry and grouping
entries, and the test-literal edits that put both seasons under the existing corpus-backed
proofs.

Explicitly NOT in scope: no tuning search, no publish, no promote, no full harness replay, no
change to `COLD_START_SEASON` (still 2022 — moving it is D-19's own separate decision), no
re-derivation of any threshold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260903-4fs-author-the-2019-and-2020-breakdown-and-r/260903-4fs-CONTEXT.md
@docs/data/tba-field-recon-2019-2020.md
@packages/core/algorithms/breakdown/2022.ts
@packages/core/algorithms/breakdown/constants.ts
@packages/core/algorithms/sigma1/rp/2022.ts
@packages/core/algorithms/sigma1/rp/constants.ts
</context>

<measured_preconditions>
Every number below was measured read-only against `data/corpus.sqlite` during planning, with
`sqlite3 -readonly` and `json_extract`. They are stated so a real failure during execution is
distinguishable from an expected one. Do not re-derive them; do use them as the falsifiable
prediction each task's run must reproduce.

**Population and tier coverage**

| Season | qm w/ breakdown | alliance-sides | elim (all rp_earned = 0) | event_types present (non-offseason) |
|---|---|---|---|---|
| 2019 | 14,929 | 29,858 | 3,122 | 0, 1, 2, 3, 5, 100 (qm); 4 exists but only in elims |
| 2020 | 3,820 | 7,640 | 843 | 0, 1, 100 — **base tier only** |

Consequences already checked, so nothing below is a surprise mid-run:

- Every event_type present maps in `EVENT_TYPE_TIERS`. There is no 6/7 and no unmapped value,
  so `eventTierFor` will not throw partway through a season's reconciliation.
- 2019 covers all three tiers; 2020 covers only `base` — the direct corroboration of D RP-3's
  "the other two tiers carry 65 as an ASSUMPTION, not a measurement" caveat.
- The elimination invariant holds for both seasons (3,122 and 843 played elim matches, every
  `red_rp_earned`/`blue_rp_earned` = 0), so the `elimination RP invariant` describe.each
  passes on registration.
- `breakdown/reconciliation.test.ts` needs >= 2,000 sampled rows per season; the populations
  it queries (all comp levels) are 18,051 for 2019 and 4,663 for 2020.

**Roll-up identity (BD-1's "verify rather than trust")** — own offensive fields + own
foulPoints vs own alliance total, FULL non-offseason breakdown-bearing population:

- 2020: **0 mismatches / 4,663 per side.** Clean.
- 2019: **1 mismatch / 18,051 per side** — two distinct matches, one bad side each, both
  ELIMINATION matches: `2019lake_qf4m1` (red) and `2019nccmp_f1m2` (blue). Both carry a large
  negative scorekeeper adjustment (-74 and -85) that drives the component sum below zero
  (to -10 and -1) while TBA floors the reported alliance total at 0. This is TBA's own
  clamp-at-zero behaviour, not a component-map defect.
  **Both sit outside the suite's sampling window**: it takes the first 2,000 rows ordered by
  match_key ascending, whose last key is `2019casd_qm79`; these two rank 5,620 and 10,729.
  So the suite passes at 0 — for a sampling reason. Record the two keys in the 2019 module
  header so a future sample-size increase meets a documented artifact instead of a mystery.

**RP agreement, per event_type, recomputed vs TBA's recorded flag**

- 2019 `habClimbPoints >= 15` vs `habDockingRankingPoint`: **0 mismatches at every event_type**
  (9,832 / 14,482 / 1,842 / 2,716 / 960 / 26 sides at types 0 / 1 / 2 / 3 / 5 / 100).
- 2020 `endgamePoints >= 65` vs `shieldOperationalRankingPoint`: **0 mismatches at every
  event_type** (2,792 / 4,820 / 28). `shieldEnergizedRankingPoint` fired **0 times / 7,640**.
- 2019 `completedRocketNear || completedRocketFar` vs `completeRocketRankingPoint`:

  | event_type | sides | mismatches | rate | false positives | false negatives |
  |---|---|---|---|---|---|
  | 0 | 9,832 | 136 | 1.3832% | **0** | 136 |
  | 1 | 14,482 | 212 | 1.4639% | **0** | 212 |
  | 2 | 1,842 | 59 | 3.2030% | **0** | 59 |
  | 3 | 2,716 | 104 | 3.8292% | **0** | 104 |
  | 5 | 960 | 29 | 3.0208% | **0** | 29 |
  | 100 | 26 | 0 | 0% | **0** | 0 |

  Overall 540 / 29,858 = 1.809%, matching the CONTEXT's 1.81%. Measured ceiling **3.8292%**
  at event_type 3. Zero false positives at every tier — the never-overstate direction.

**Structural RP facts** — `(win ? 2 : tie ? 1 : 0) + recorded bonus count` vs the corpus's own
`red_rp_earned`/`blue_rp_earned`: **0 nulls, 0 mismatches**, 29,858 sides for 2019 and 7,640
for 2020. Max earned seen: 4 (2019), 3 (2020). So `winRp` is 2 and `tieRp` is 1 for both, and
`maxRp` lands on 4 and 3 through `winRp + bonusNames.length` without any hand-written number.

**Boundary-case availability** (for the exact-boundary checks Task 3 adds): 2019 sides with
`habClimbPoints` exactly 15 — base 2,474, districtChampionship 438, championship 436. 2020
sides with `endgamePoints` exactly 65 — 750, all base. The two nonexistent 2020 tiers will
log "boundary case not observed, not confirmed" — which is exactly the RP-3 caveat becoming
visible in test output rather than living only in a comment.
</measured_preconditions>

<test_blast_radius>
The CONTEXT states both reconciliation suites iterate the registry. **That is true of the RP
suite and false of the breakdown suite.** The breakdown side carries a hardcoded literal.
Getting this wrong is the single highest-value failure mode in this task: it produces two
registered-but-completely-unproven component maps with a fully green test run.

**A. Newly exercised AUTOMATICALLY, by registry iteration (`RP_REGISTERED_SEASONS`)**

`sigma1/rp/reconciliation.test.ts`:
1. `describe.each(RP_REGISTERED_SEASONS)("season %i RP reconciliation (D-12)")` — for 2019 and
   2020 this newly asserts: at least one sampled qm row; **per (event_type x bonus) mismatch
   count is exactly 0 unless a named tolerance covers it**, after printing the full mismatch
   report to the console; and summed-RP `(winRp | tieRp | 0) + totalRp === red/blue_rp_earned`
   per event_type, tolerated only by the sum of that season's bonus tolerances at that tier.
   The `2025/2026 pin win RP at 3` case early-returns for both new seasons.
2. `describe("exact-boundary behaviour")` — its module-scope
   `new Map(RP_REGISTERED_SEASONS.map(...))` newly loads the FULL 2019 and 2020 qm populations
   into memory. Its `CHECKS` array is hardcoded, so **no new boundary assertion runs until
   Task 3 adds one** — registration alone buys the query cost and none of the value.
3. `describe.each(RP_REGISTERED_SEASONS)("season %i elimination RP invariant")` — newly
   asserts `rows.length > 0` and `red_rp_earned === 0 && blue_rp_earned === 0` over the full
   elim population. Pre-verified above: passes.
4. `it.each(RP_REGISTERED_SEASONS)` missing-breakdown report — logs the rate, asserts
   `counts.total > 0`.

`sigma1/rp/rules.test.ts`:
5. `describe.each(RP_REGISTERED_SEASONS)("season %i RP rule module shape")` — newly asserts
   for both seasons: `maxRp === winRp + bonusNames.length`; `tieRp === 1`; threshold-variable
   names unique within the module; every `unit` is `count` or `points`.
6. `it("every season: totalRp equals the count of true bonusFlags")` — newly calls
   `predictThresholds` for 2019 and 2020 with every threshold variable set to 0. Both must
   return `totalRp === 0` and no true flag (2019's rocket is `false` by construction).

**B. Requires a HARDCODED LITERAL EDIT or it silently proves nothing**

- `sigma1/rp/rules.test.ts:25` — `expect(RP_REGISTERED_SEASONS).toEqual([2022, 2023, 2024,
  2025, 2026])`. This **hard-fails** the moment the registry grows. Must become
  `[2019, 2020, 2022, 2023, 2024, 2025, 2026]` (2021 stays absent — there was no standard
  season). Update the describe/it wording too; "the sorted tuple 2022-2026" becomes a lie.
- `sigma1/rp/rules.test.ts:13` — `toThrow(/2022, 2023, 2024, 2025, 2026/)` for season 2021.
  `Object.keys` on an integer-keyed object yields ascending numeric order, so the message
  becomes `2019, 2020, 2022, 2023, 2024, 2025, 2026` and the regex still matches as a
  substring. Expected to keep passing untouched — **confirm from the run, do not assume**.
- `breakdown/reconciliation.test.ts:30` — `REGISTERED_SEASONS`. Not iterated from the registry.
  Editing it is what activates, for 2019 and 2020: the >= 2,000-row sample assertion; the
  red and blue `sum(offensive) + foulsCommitted(opponent) === totalPoints` proof at 1e-6 with
  **no tolerance mechanism at all**; the `Robot1/2/3` per-robot guard (BD-2); and the
  `it.each` prototype-pollution case, which asserts a null-prototype result whose key set
  equals the declared `components` array exactly.
- `breakdown/groups.test.ts:11` — `SEASONS`. Also hardcoded. Editing it is what enforces
  `groups.ts`'s own stated invariant ("a new season cannot be registered without deciding its
  grouping") for the two new seasons.

**C. Unaffected — verified, so no time is spent looking**

- `breakdown/breakdown.test.ts` — entirely 2024-fixture and `distributeResidual`-based, with
  one `tryParseBreakdownPair(1999, ...)` unregistered-season case that stays unregistered.
  Zero new coverage, zero new risk.
- `apps/web/src/lib/api/compare.test.ts`'s `COMPARE_SEASONS`, `packages/harness/*`'s season
  literals, `packages/ingest/*`'s — all keyed to what is PUBLISHED, and this task publishes
  nothing. Out of scope; leave every one of them alone.
</test_blast_radius>

<tasks>

<task type="tracer">
  <name>Task 1: 2020 end-to-end — one season through every layer</name>
  <files>packages/core/algorithms/breakdown/2020.ts, packages/core/algorithms/breakdown/index.ts, packages/core/algorithms/breakdown/groups.ts, packages/core/algorithms/breakdown/groups.test.ts, packages/core/algorithms/breakdown/reconciliation.test.ts, packages/core/algorithms/sigma1/rp/2020.ts, packages/core/algorithms/sigma1/rp/rules.ts, packages/core/algorithms/sigma1/rp/rules.test.ts</files>
  <read_first>packages/core/algorithms/breakdown/2022.ts, packages/core/algorithms/sigma1/rp/2022.ts, packages/core/algorithms/breakdown/constants.ts, packages/core/algorithms/sigma1/rp/constants.ts</read_first>
  <action><!-- planner-discipline-allow: autoPoints, teleopPoints, totalPoints -->
Take 2020 all the way through every layer this task will touch — component map, component
registry, grouping table, RP module, RP registry, and both hardcoded test literals — before
2019 is started at all. 2020 is the thinner season (one bonus, one tier, 34 breakdown keys)
and is pre-measured to reconcile at exactly 0 everywhere, so it proves the shape end to end
without the rocket asymmetry or the tolerance derivation confounding a failure.

This deliberately departs from the "both breakdown maps, then both RP modules" split the task
brief suggested. Reason: the CONTEXT's own finding that a season is either fully registered or
unusable makes the season, not the module kind, the unit that either works or does not — and
slicing by season leaves the repo green after every task instead of half-registered after the
first.

Write `breakdown/2020.ts` on `breakdown/2022.ts`'s structure exactly: a Zod `SideBreakdownSchema`
in default strip mode over only the fields read (never `.passthrough()`/`.loose()`), a
`{red, blue}` wrapper schema, a canonical-name to TBA-key record, `components` derived as
`[...Object.keys(map), FOULS_COMMITTED_COMPONENT]`, and a `parse` that builds its result with
`Object.create(null)` plus a fixed allowlist loop (T-02-04) and never spreads the raw payload.
Read exactly the six own fields D BD-1 names for 2020 — the auto init-line and auto cell point
fields, the teleop cell and control-panel point fields, the endgame point field, and the
adjustment field — plus `FOULS_COMMITTED_COMPONENT` from the OPPONENT's received-fouls field,
via the same D-04 derivation every existing map uses. Per D BD-1, do not read either period
roll-up or the alliance grand total; per D BD-2, read no per-robot field. Name the two roll-ups
in the header comment as the 2022 map does, stating the identity each one is the sum of.
Canonical component names are Claude's discretion — follow 2022/2025 style; note that a bare
endgame component name is already precedented by 2022 and does not collide with the group
metric key, which is prefixed.

Cite `docs/data/tba-field-recon-2019-2020.md` in the header as the field inventory's source,
the way `rp/2022.ts` cites its own verification method.

Write `sigma1/rp/2020.ts` on `rp/2022.ts`'s structure. One bonus only, named for the shield
operational RP (D RP-3): threshold on the endgame point field with an `RpTieredThreshold`
triple of 65/65/65 and `unit: "points"`, `>=` semantics. State two things in the header that
the uniform triple would otherwise hide: the base-tier figure is a MEASUREMENT (0 mismatches
over 7,640 sides), and the other two tiers are an ASSUMPTION because 2020 was cancelled before
any district championship or championship was played, so no data for those tiers exists.
Also record, per D RP-3, that the intuitive control-panel-flag rule scores 85.21% — below the
85.35% of always guessing false — because that near-miss is the reason these rules were
derived from data instead of cited from a manual.

Per D RP-4 the shield energized RP is NOT modelled: keep it out of `bonusNames`, give it no
threshold variable, and do NOT put its recorded flag into `recordedBonusFlags` — a recorded
flag with no recomputed twin breaks the paired shape the reconciliation test compares over.
Put it in `diagnosticKeys` with a comment that it is false in every one of the 7,640 sides in
this corpus, alongside the stage-activation and foul-derived-energized fields at your
discretion. Set `winRp: 2`, `tieRp: 1`, and `maxRp: 2 + BONUS_NAMES.length` as an expression —
never the literal 3. Call `assertFiniteThresholdVariables` before returning, as 2022 does.
`predictThresholds` is fully computable here: the one bonus reads only its tracked threshold
variable, so there is no conservative branch for 2020.

Register both: an import plus a record entry in `breakdown/index.ts`'s `SEASON_COMPONENT_MAPS`
and in `rp/rules.ts`'s `RP_RULE_MODULES` — never a branch in either dispatch function.

Add a 2020 entry to `GROUPS_BY_SEASON` in `breakdown/groups.ts` assigning every declared
component to auto, teleop or endgame (the adjustment and fouls-committed components are
already covered by `UNGROUPED_COMPONENTS`). This is an ADDITION beyond the CONTEXT's four
modules, justified by `groups.ts`'s own header invariant that a new season cannot be
registered without deciding its grouping; it is eight lines of data and fully reversible.

Then edit the three hardcoded literals so the new season is actually proven rather than merely
present: add 2020 to `breakdown/reconciliation.test.ts`'s `REGISTERED_SEASONS`, to
`breakdown/groups.test.ts`'s `SEASONS`, and to `rp/rules.test.ts`'s `RP_REGISTERED_SEASONS`
equality assertion (whose wording also needs updating). Keep them sorted ascending.

Expected outcome, and the falsifiable prediction for this task: **every 2020 assertion passes
at exactly 0 mismatches, with no tolerance entry of any kind.** If the shield operational
bonus shows even one mismatch, the module is wrong — investigate it; do not add a tolerance.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/core/algorithms/breakdown/reconciliation.test.ts packages/core/algorithms/breakdown/groups.test.ts packages/core/algorithms/sigma1/rp/rules.test.ts packages/core/algorithms/sigma1/rp/reconciliation.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('packages/core/algorithms/breakdown/2020.ts','utf8').replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*$/gm,''); const bad=['autoPoints','teleopPoints','totalPoints'].filter(k=>new RegExp('\\\\b'+k+'\\\\b').test(s)); if(bad.length){console.error('roll-up key read outside a comment: '+bad.join(', '));process.exit(1);} console.log('OK: no roll-up key in non-comment source');"</automated>
  </verify>
  <done>2020 appears in both registries and in all three test literals. The RP reconciliation console report shows `shieldOperational` at 0 mismatches for every event_type present, the summed-RP report shows 0 mismatches, the breakdown suite reconciles both alliances at 1e-6 over its 2,000-row sample, and `groups.test.ts` confirms every 2020 component is grouped or explicitly ungrouped. `maxRp` reads 3 only because `bonusNames.length` is 1. No tolerance entry was added.</done>
</task>

<task type="auto">
  <name>Task 2: 2019 through the same path, with the rocket asymmetry</name>
  <files>packages/core/algorithms/breakdown/2019.ts, packages/core/algorithms/breakdown/index.ts, packages/core/algorithms/breakdown/groups.ts, packages/core/algorithms/breakdown/groups.test.ts, packages/core/algorithms/breakdown/reconciliation.test.ts, packages/core/algorithms/sigma1/rp/2019.ts, packages/core/algorithms/sigma1/rp/rules.ts, packages/core/algorithms/sigma1/rp/rules.test.ts</files>
  <read_first>packages/core/algorithms/breakdown/2020.ts, packages/core/algorithms/sigma1/rp/2020.ts</read_first>
  <action><!-- planner-discipline-allow: autoPoints, teleopPoints, totalPoints -->
Repeat Task 1's proven shape for 2019, reusing the two modules just written as the local
template rather than re-deriving the structure from 2022.

`breakdown/2019.ts` reads exactly the five own fields D BD-1 names — the sandstorm bonus point
field, the hatch panel and cargo point fields, the HAB climb point field, and the adjustment
field — plus `FOULS_COMMITTED_COMPONENT` from the opponent's received-fouls field. Two
hazards specific to 2019, both stated in D BD-1:

- The auto-period roll-up is NUMERICALLY IDENTICAL to the sandstorm bonus field in every
  observed row. Reading it instead would be a duplicate, not an independent component, and —
  this is the important part — reconciliation would still PASS, because the two values are
  equal. That is why this task carries a comment-stripped source gate as well as the corpus
  proof: the gate is the only thing that can catch this particular substitution.
- The teleop roll-up is the sum of the hatch panel, cargo and HAB climb fields. State both
  identities in the header.

Record in the header the two clamp artifacts measured during planning: `2019lake_qf4m1` (red)
and `2019nccmp_f1m2` (blue), where a large negative scorekeeper adjustment (-74 and -85) drives
the component sum below zero while TBA floors the reported alliance total at 0. Both are
ELIMINATION matches and both fall outside the current 2,000-row sampling window (they rank
5,620 and 10,729; the window ends at `2019casd_qm79`), so the suite passes at 0 today. Write it
down so a future sample-size increase meets a documented data artifact instead of a mystery —
this is the same honest-reporting discipline `2025.ts` and the RP suite's header already use.

`sigma1/rp/2019.ts` carries TWO bonuses.

The HAB docking bonus (D RP-1): threshold on the HAB climb point field, `RpTieredThreshold`
triple 15/15/15, `unit: "points"`, `>=`. Comment that the flatness across all three tiers was
MEASURED, not assumed — 100.00% agreement at each of base, district championship and
championship (0 mismatches over 29,858 sides), which is why the triple is uniform.

The complete rocket bonus (D RP-2) carries an asymmetry that must be implemented deliberately
and explained where a reader will hit it:

- `parse` recomputes it as the logical OR of the near and far rocket completion booleans,
  which are both present in the raw breakdown and available at parse time.
- `predictThresholds` CANNOT reach them — it receives only numeric threshold-variable values,
  because the Monte Carlo joint draw in `rp/distribution.ts` samples threshold variables and
  never a breakdown. So this bonus takes the CONSERVATIVE BRANCH and is always `false`,
  following 2025's precedent for a bonus with no threshold-variable-only fallback, which
  `RpRuleModule.predictThresholds`'s own doc comment already names as the established pattern.
  Cite the size comparison so the choice reads as precedent-following rather than a shortcut:
  the measured understatement here is about 0.0654 RP per alliance-match, roughly ten times
  SMALLER than the 0.625464 already shipped for 2025's auto bonus.
- The bonus therefore contributes no entry to `thresholdVariables`; the single tracked
  variable for 2019 is the HAB climb point field. Note in the header that the joint
  hatch/cargo threshold variant was evaluated and REJECTED: its best form scored 94.49%,
  worse than the boolean OR, and introduced 0.51% false positives, violating never-overstate.

Both recorded flags go into `recordedBonusFlags`, paired one-to-one with the recomputed
`bonusFlags`. `winRp: 2`, `tieRp: 1`, `maxRp: 2 + BONUS_NAMES.length` as an expression — never
the literal 4. Cite `docs/data/tba-rp-thresholds-2019-2020.md`, `docs/data/tba-rocket-rp-2019.md`
and `docs/data/tba-rp-rates-2019-2020.md` in the header as the verification method.

Register both modules, add the 2019 grouping entry, and add 2019 to the same three test
literals — keeping every list sorted ascending, so both new seasons sort ahead of 2022 and
2021 remains correctly absent.

For the grouping, state the one judgement call rather than leaving it implicit: TBA's own
teleop roll-up for 2019 bundles the HAB climb with hatch panels and cargo, but the climb is
plainly an endgame action and is grouped as endgame here — the identical treatment 2022
already gives its own endgame component, which TBA likewise folds into a teleop roll-up.

Expected outcome, and the falsifiable prediction: the breakdown suite passes at 0 for both
alliances. The HAB docking bonus reconciles at 0 mismatches at every event_type. **The
complete rocket bonus FAILS the bonus-flag assertion, and the summed-RP assertion with it,
because no tolerance names it yet.** That failure is this task's measurement, not a defect —
Task 3 consumes its printed report. Do not add a tolerance in this task. Capture the full
`[RP reconciliation 2019] bonus flag mismatch report` console output before finishing, since
Task 3 needs its per-event_type rates.

If the rocket disagreement shows ANY false positive at any tier, stop: that contradicts the
zero-false-positive measurement at every tier and means the recomputation is wrong, not that
the tolerance needs to be larger.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/core/algorithms/breakdown/reconciliation.test.ts packages/core/algorithms/breakdown/groups.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('packages/core/algorithms/breakdown/2019.ts','utf8').replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/.*$/gm,''); const bad=['autoPoints','teleopPoints','totalPoints'].filter(k=>new RegExp('\\\\b'+k+'\\\\b').test(s)); if(bad.length){console.error('roll-up key read outside a comment: '+bad.join(', '));process.exit(1);} console.log('OK: no roll-up key in non-comment source');"</automated>
    <automated>npx vitest run --project node packages/core/algorithms/sigma1/rp/reconciliation.test.ts</automated>
    <human-check>Read the `[RP reconciliation 2019] bonus flag mismatch report` block in the output above and record every per-event_type rate for `completeRocket` verbatim — Task 3's tolerance is derived from these numbers. This suite is EXPECTED to fail here on `completeRocket` only.</human-check>
  </verify>
  <done>2019 is in both registries, the grouping table and all three test literals. The breakdown suite reconciles 2019 at 1e-6 on both alliances and the per-robot guard passes. The RP report shows `habDocking` at 0 mismatches at every event_type and `completeRocket` failing with false negatives only — zero false positives — and the per-event_type rates are captured for Task 3. The two clamp match keys are named in the 2019 breakdown module header.</done>
</task>

<task type="auto">
  <name>Task 3: Derive the one tolerance from the run, and pin the boundaries</name>
  <files>packages/core/algorithms/sigma1/rp/reconciliation.test.ts, packages/core/algorithms/sigma1/rp/rules.test.ts</files>
  <read_first>packages/core/algorithms/sigma1/rp/reconciliation.test.ts</read_first>
  <action>
Add exactly ONE `KNOWN_TOLERANCES` entry — 2019's complete rocket bonus — with its `rate`
DERIVED from Task 2's printed report.

**The derivation rule, which is the whole point of this task.** Read the per-event_type rates
out of the `[RP reconciliation 2019] bonus flag mismatch report` Task 2 captured. Take the
MAXIMUM rate across event types. Set `rate` to the smallest round value strictly above that
measured ceiling, leaving a small margin, and write the measured ceiling and the margin's
reason into the entry's comment. This follows the two precedents already in the table: the
2024 ensemble bonus (measured maximum 7.825%, tolerance 0.085) and the 2025 coral bonus
(measured maximum 0.336%, tolerance 0.005) — both of which record the exact measured number
they sit above. IN-02 in `03-REVIEW.md` already tightened one of them specifically because a
margin with no stated reason was ~40% wider than its measurement; do not reintroduce that.

**Choosing a number that makes the test pass, rather than reading one out of the run, is
forbidden.** The planner's independent SQL measurement of the same population predicts a
ceiling of 3.8292% at event_type 3 (per-type: 1.3832 / 1.4639 / 3.2030 / 3.8292 / 3.0208 / 0
at types 0 / 1 / 2 / 3 / 5 / 100), which points at a tolerance of 0.04. Use that ONLY as a
cross-check on the run's own report. If the run's rates differ from these by more than
rounding, that is evidence the module is wrong — investigate the module; it is never a reason
to raise the tolerance until the suite goes green.

The entry's comment must record: that every disagreement is a false negative and the false
positive count is 0 at every tier (the never-overstate direction this codebase requires); that
the rate is tier-varying, roughly 1.4% at base against 3.0-3.8% at the higher tiers; and that
it covers a conservative under-firing rule, not a threshold error, so it must never be widened
to cover a rule change. Follow the existing entries' `eventTypes` convention of listing every
type; the assertion is an upper bound, so listing a type measured at 0 costs nothing.

Add NO tolerance for 2019 HAB docking or 2020 shield operational — both are measured at
exactly 0 and any future mismatch there must fail loudly.

The summed-RP assertion needs no separate entry: `summedRpToleranceFor` sums a season's
bonus tolerances at each event_type, and since HAB docking is perfect, every summed-RP
mismatch is exactly one rocket false negative shifting the total by one. Confirm from the
summed-RP report that its rate does not exceed the bonus rate, rather than assuming it.

Then add the two exact-boundary `CHECKS` entries that registration alone does not buy — this
is the block whose module-scope query already loads both new seasons but whose assertion list
is hardcoded. Add 2019's HAB docking bonus on its threshold variable at 15/15/15, and 2020's
shield operational bonus on its threshold variable at 65/65/65. Both are single-condition
tiered scalar comparisons, exactly the curated shape that block documents as eligible; the
2019 rocket bonus is NOT eligible and must not be added, since it is derived from booleans
rather than a threshold-variable comparison.

Boundary rows are pre-confirmed to exist, so neither check is vacuous: 2,474 base / 438
district championship / 436 championship sides sit exactly at 15 for 2019, and 750 base sides
sit exactly at 65 for 2020. The two tiers 2020 never played will log "boundary case not
observed, not confirmed" — leave that as-is. It is the RP-3 tier caveat surfacing in test
output, which is strictly better than the caveat living only in a comment.

Finally, extend `rules.test.ts`'s `winRp per season (Pitfall 2)` block, which does not iterate
the registry, with 2019 and 2020 both asserting 2. This pins the structural counterpart of the
`maxRp` values (4 and 3) that the registry-iterating shape test derives but never anchors,
and it is confirmed by 37,498 sides of `rp_earned` agreement.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/core/algorithms/sigma1/rp/reconciliation.test.ts packages/core/algorithms/sigma1/rp/rules.test.ts</automated>
  </verify>
  <done>Exactly one tolerance entry was added, its rate sits just above the maximum per-event_type rate the run itself printed, and its comment records that measured ceiling, the false-negative-only direction, and the never-widen rule. The RP suite is green for all seven registered seasons. Two exact-boundary checks pass with observed rows at every tier that exists, and log an explicit not-observed line for the two 2020 tiers that do not. `winRp` is pinned at 2 for both new seasons.</done>
</task>

<task type="auto">
  <name>Task 4: Full-repo sweep for blast radius outside the audited files</name>
  <files>(verification only — edits only if the sweep finds a real break)</files>
  <action>
The blast-radius inventory in this plan was built by reading the four test files named in the
brief plus a repo-wide grep for hardcoded season lists. That is evidence, not proof. Run the
whole suite and the typechecker to catch anything the inventory missed.

Run the full test suite from the repo root, across every project — not from `apps/web` and not
scoped to `--project node`. The two scopes differ by roughly 90 files, and a red suite has
hidden in exactly that gap on this repo before. Read the pass/fail counts out of the output;
do not infer success from an exit code, and do not wrap the command in `timeout` with a
package-manager runner, which swallows output and exits 0 regardless.

Then run the typechecker across the repo.

Expected: everything green. The grep found season literals in `apps/web`'s compare list, the
harness, the ingest package and several scripts, but every one of them is keyed to what has
been PUBLISHED, and this task publishes nothing — so none should move. If any of them does
break, that is a genuine finding: report it rather than editing a published-artifact season
list, because widening one of those is a publish-scope change and this task is explicitly
scoped out of publishing.

Do not run a tuning search, a publish, a promote or a harness replay in this task or any
other. Do not delete `data/corpus.sqlite.bak-pre-2019-2020`.
  </action>
  <verify>
    <automated>npx vitest run</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The full suite across every project passes, confirmed by reading the reported file and test counts rather than the exit code, and the typechecker is clean. Any suite outside the eleven files this task touched that changed behaviour is reported, not silently patched.</done>
</task>

</tasks>

<source_audit>
Coverage of every source item, per the multi-source audit requirement.

| ID | Source | Item | Status | Where |
|---|---|---|---|---|
| RP-1 | CONTEXT | 2019 HAB docking, HAB climb points >= 15, untiered, flatness measured | COVERED | Task 2 module; Task 3 boundary check |
| RP-2 | CONTEXT | 2019 complete rocket, boolean OR in parse, conservative false in predictThresholds, rejected joint-threshold variant recorded | COVERED | Task 2 module; Task 3 tolerance |
| RP-3 | CONTEXT | 2020 shield operational, endgame points >= 65, base-tier-only caveat, control-panel near-miss recorded | COVERED | Task 1 module; Task 3 boundary check |
| RP-4 | CONTEXT | 2020 shield energized not modelled; excluded from bonusNames and recordedBonusFlags; diagnostic only; maxRp structural | COVERED | Task 1 module; enforced by the registry-iterated shape test |
| BD-1 | CONTEXT | Both component maps, roll-up avoidance, identities verified against the corpus rather than trusted | COVERED | Tasks 1 and 2; pre-verified in `<measured_preconditions>`, re-proven by the reconciliation suite plus a comment-stripped source gate |
| BD-2 | CONTEXT | No per-robot field read in any of the four modules | COVERED | Tasks 1 and 2; enforced by the existing Robot1/2/3 assertion once the literal is edited |
| — | CONTEXT | Registry entries in both dispatch tables | COVERED | Tasks 1 and 2 |
| — | CONTEXT | Zod strip mode, `Object.create(null)` allowlist, `assertFiniteThresholdVariables`, tiered triples even where untiered | COVERED | Tasks 1 and 2 |
| — | CONTEXT | Doc comments cite the four derivation documents as the verification method | COVERED | Tasks 1 and 2 |
| — | CONTEXT (discretion) | Canonical names, diagnostic key choice, whether tolerances are needed and their values | COVERED | Named as discretion in Tasks 1-2; tolerance derivation constrained in Task 3 |
| — | Brief | Name which existing tests newly exercise 2019/2020 and what they assert | COVERED | `<test_blast_radius>` sections A, B and C |
| — | Brief | Tolerances derived from a real run; guessing called out as forbidden | COVERED | Task 3, stated as a prohibition with the planner's SQL figure demoted to a cross-check |
| — | Brief | No tuning search, publish, promote or full harness replay | COVERED | Excluded in `<objective>`, restated in Task 4 |

**Additions beyond the CONTEXT's four modules, both flagged rather than assumed:**

1. `GROUPS_BY_SEASON` entries plus the `groups.test.ts` literal. Justification: `groups.ts`'s
   own header states that a new season cannot be registered without deciding its grouping, and
   that invariant is enforced by a hardcoded season list. Registering two seasons without it
   leaves the invariant silently unenforced for exactly those seasons. About eight lines of
   data, fully reversible.
2. Two exact-boundary `CHECKS` entries and two `winRp` assertions. Justification: registration
   already pays the query cost of loading both seasons into that block, and buys no assertion
   until a check is added.

**Deliberately NOT done:** `COLD_START_SEASON` stays 2022 (D-19's own separate decision, and
the corpus backfill todo is its owner); no season list keyed to published artifacts is widened;
no threshold is re-derived, re-litigated or "improved".
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| corpus `score_breakdown_raw` -> season module `parse()` | Third-party TBA JSON, stored verbatim, reaching a parser that builds an object consumed by the Kalman/EWMA update path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4fs-01 | Tampering | `breakdown/{2019,2020}.ts` and `rp/{2019,2020}.ts` `parse()` | medium | mitigate | Build every result with `Object.create(null)` plus a fixed allowlist loop, never a spread of the raw payload (T-02-04). The existing prototype-pollution `it.each` in `breakdown/reconciliation.test.ts` covers both new seasons once the literal is edited in Tasks 1-2, asserting a null-prototype result whose key set equals the declared components exactly. |
| T-4fs-02 | Denial of Service | Zod parse boundary in all four modules | low | mitigate | Default strip mode over a closed field list, never `.passthrough()`/`.loose()`; `.finite()`/`.boolean()` on every read field so a malformed or absent field throws rather than coercing to 0, and `assertFiniteThresholdVariables` as the second gate before any value reaches algorithm state. |
| T-4fs-03 | Information Disclosure | test output and commit messages | low | mitigate | No command in this plan needs credentials — every corpus read is local and read-only, so nothing here takes `--env-file=.env`. Per `CLAUDE.md`, never read, echo or interpolate that file in any step of this task. |

No package-manager install occurs in this task, so the package legitimacy gate does not apply
and no `T-{phase}-SC` entry is required.
</threat_model>

<verification>
- Both dispatch functions resolve 2019 and 2020 instead of throwing; 2021 still throws.
- The breakdown reconciliation identity holds at 1e-6 for both new seasons on both alliances.
- 2019 HAB docking and 2020 shield operational reconcile at exactly 0 mismatches at every
  event_type present, with no tolerance entry for either.
- The single 2019 complete rocket tolerance sits just above a ceiling read from the suite's own
  printed report, and its comment records that measurement.
- `maxRp` is 4 for 2019 and 3 for 2020, derived structurally; `winRp` is 2 and `tieRp` is 1.
- The full repo suite and the typechecker are both green.
</verification>

<success_criteria>
The corpus backfill's blocking dependency is cleared: 2019 and 2020 are fully registered on
both the component and RP sides, every existing corpus-backed proof now covers them, and the
one place the data does not reconcile carries a tolerance whose number came out of a real run
and whose comment says exactly what it covers and why it must never be widened.
</success_criteria>

<output>
Create `.planning/quick/260903-4fs-author-the-2019-and-2020-breakdown-and-r/260903-4fs-SUMMARY.md` when done.
</output>
