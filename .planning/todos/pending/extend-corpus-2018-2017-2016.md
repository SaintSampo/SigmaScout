---
id: extend-corpus-2018-2017-2016
created: 2026-09-06
source: quick task 260906-813 — investigation filed after extend-corpus-2019-2020 closed 2026-09-04
resolves_phase:
priority: high
---

# Extend the corpus back to 2018, then 2017, then 2016

> **STATUS 2026-09-07 — all three seasons are ingested and registered; TWO rows remain open.**
> 2018 landed via quick tasks 260907-057 and 260907-12k; 2017 and 2016 via 260907-203. Rows 1-6
> and 8 of "The nine registries" are DONE for all three. **Row 7 (`FIRST_SEASON`) and row 9
> (`prequalified.ts`) are OPEN for all three**, plus the `publish:seasons`/`publish:districts`
> lists — all of which belong to a publish step that has not run, so nothing from 2016, 2017 or
> 2018 is visible on the site. This file stays `pending` for exactly that reason. The full
> measurement record is in the 2026-09-07 update at the end of "Then 2017, then 2016"; the
> sections below are preserved as written, so read them as the plan-of-record they were, not as
> current status.
>
> **Scope: 2018 first.** 2017 (Steamworks) and 2016 (Stronghold) follow, in that order, as
> separate passes. 2018 is deliberately first because it is the hard one (see the additivity
> section below) — the two easy seasons should not be done while the hard one's shape is
> unknown.
>
> **2021 is never part of the corpus.** It was the at-home/remote season with no conventional
> 3v3 alliance matches, so there is nothing for a match predictor to ingest. This is a
> `permanent exclusion`, not a deferral — the same rule the predecessor file set.
>
> **Provenance.** Every number below was measured on 2026-09-06 by read-only probes against the
> TBA API and `data/corpus.sqlite`. Read them, do not re-measure them — re-running the probes
> spends TBA rate limit for no gain.
>
> **Predecessor.** This file succeeds `.planning/todos/completed/extend-corpus-2019-2020.md`,
> which closed 2026-09-04.

**Priority is `high`** because the user stated intent directly on 2026-09-06: after 2018 lands,
2017 and 2016 follow. That declared intent is what `priority` records here — it does **not**
mean the payoff arithmetic below is large. Read "What this buys, and what it does not" for the
honest counterweight before treating `high` as a reason to rush this.

## What this buys, and what it does not

Origins require at least 2 distinct prior seasons in the corpus
(`MIN_PRIOR_SEASONS_FOR_HEADLINE`, `packages/harness/score.ts`).

- Today's corpus — 2019, 2020, 2022, 2023, 2024, 2025, 2026 — yields five origins: 2022, 2023,
  2024, 2025, 2026.
- Adding 2018 promotes **exactly one** new origin: **2020**, which is a 3,806-match fragment
  season. 2019 gains only one prior and stays ineligible.
- The real gain is that the **thinnest origin's selection window roughly doubles**: 2022
  currently selects on 2019 + 2020 = 18,749 official quals; with 2018 it selects on ~33,000.
- **The risk, stated plainly.** 2018 is the one season whose structure violates the model's
  core additivity assumption, so hyperparameters selected partly on 2018 may transfer *worse*
  to normal seasons. This is testable with the existing rolling-origin harness — compare the
  origin-2022 parameter set selected with and without 2018 — and that comparison should **gate
  promotion**. Do not assume the backfill is a free win.

## The one hard part — 2018 is anti-additive

**OPEN MODELLING DECISION.** This is the only part of the 2018 job with no precedent anywhere in
the codebase — everything else in this file is data entry.

### F-A — the observation

`corr(red totalPoints, blue totalPoints)` over official qualification matches. The seven corpus
seasons come from `data/corpus.sqlite`; 2018 is measured directly against TBA:

| season | corr(red, blue) totalPoints | n |
|---|---|---|
| 2018 | **-0.4567** | 3,248 qual alliance-sides, 18 events (TBA) |
| 2019 | +0.3282 | 14,929 |
| 2020 | -0.0003 | 3,820 |
| 2022 | +0.3785 | 12,064 |
| 2023 | +0.3461 | 13,558 |
| 2024 | +0.2395 | 14,141 |
| 2025 | +0.5192 | 14,821 |
| 2026 | +0.3592 | 15,191 |

A separate 10-event / 858-match sample gave **-0.4239**. The point is not the magnitude — it is
that **the sign flips** relative to every other season.

### F-B — the cause, measured per component

The shared Scale. From the 858-match sample, red-vs-blue correlation within the same match:

| breakdown field | corr(red, blue) | reading |
|---|---|---|
| `teleopScaleOwnershipSec` | **-0.9109** | near-perfectly zero-sum |
| `teleopSwitchOwnershipSec` | -0.1362 | each alliance owns its own Switch — normal |
| `vaultPoints` | +0.0528 | normal |
| `endgamePoints` | +0.1895 | normal |

Mechanism, in one line: the Scale is a single shared field element, so a second of red ownership
is a second blue cannot have. Contest-intensity: red + blue `teleopScaleOwnershipSec` summed —
min 0, p10 96, median 124, p90 136, max 143, mean **118.8**, against 135s of teleop. The Scale is
contested essentially the whole match.

### F-C — the normalization lever

Scale-derived points are `2*autoScaleOwnershipSec + teleopScaleOwnershipSec +
teleopScaleBoostSec`. These are two DISTINCT statistics over the same sample — neither is
derived from the other:

1. Scale-derived points averaged **65.5 points per alliance-side**.
2. The mean per-side share of that alliance's own total was **18.8%**.

Both are means over alliance-sides, so 18.8% is a mean of ratios, not `65.5 / mean(total)` —
together they imply a mean alliance total in the ~350 range but do not pin one, and this file
does not compute one.

The decisive pair:

| quantity | corr(red, blue) |
|---|---|
| `totalPoints` | -0.4567 |
| `totalPoints` minus Scale points | **-0.0642** |

Removing the Scale essentially restores additivity. This matters because OPR, EPA and Sigma1 all
assume alliance score = sum of independent per-team contributions, and 2018 violates that
directly — a strong opponent literally subtracts from your score. The `-0.0642` result is **a
lever that exists**, not a decision to pull it; a Scale-normalized 2018 is one candidate
treatment among several and the choice has not been made.

## The 2018 component map is clean

> **Update 2026-09-07 (quick task 260907-057).** The grouping suggestion below (a single fused
> `autoOwnership` component and a single fused `teleopOwnership` component — five own components
> total) is SUPERSEDED by the locked user decision of 2026-09-07 to SPLIT Scale from Switch in
> both auto and teleop: **seven** own components
> (`autoRun`, `autoSwitchOwnership`, `autoScaleOwnership`, `teleopSwitchOwnership`,
> `teleopScaleOwnership`, `vault`, `endgame`), not five. The residual figure below (33/3,108,
> 1.06%, from an 18-event sample) is likewise SUPERSEDED by the corpus-wide measurement: **196 of
> 28,312** official qual alliance-sides (0.6923%), magnitudes 1 (188 sides) and 2 (8 sides) only,
> proven to be TBA's own arithmetic three ways (see `breakdown/2018.ts`'s file header). The "deserves
> a proper recon sweep before the module is authored" instruction immediately below is now
> DISCHARGED — the sweep happened and its result is the named tolerance
> `KNOWN_BREAKDOWN_TOLERANCES` entry in `breakdown/reconciliation.test.ts` (`rate: 0.008,
> maxAbsGap: 2, direction: "positive"`).

The verified identity holds exactly:

`autoRunPoints + autoOwnershipPoints + teleopOwnershipPoints + vaultPoints + endgamePoints +
adjustPoints + foulPoints == totalPoints`

37 distinct `score_breakdown` keys were observed.

- **Roll-up hazards, never read:** `autoPoints`, `teleopPoints`, `totalPoints`.
- **Per-robot fields, never read** (Assumption A1 / Pitfall Sigma1-2): `autoRobot1/2/3`,
  `endgameRobot1/2/3`.
- **Diagnostic-only counts:** `foulCount`, `techFoulCount`.

Two data-quality notes, each with its disposition:

- **Residual.** 33 of 3,108 official alliance-sides mismatch (**1.06%**), all at -1 or -2. This
  becomes a `KNOWN_TOLERANCES` entry, the same treatment 2019's known mismatches got. It is
  roughly **10x** 2019's rate (2019 was 1 in **18,051**) and therefore deserves a proper recon
  sweep before the module is authored — a tolerance that large should be understood before it
  is accepted.
- **Nulls.** 140 alliance-sides carry null `adjustPoints` and null `autoSwitchAtZero` — **all
  140 from `2018cc` (Chezy Champs), an offseason event.** The existing `tryParseBreakdownPair`
  malformed-degradation path already covers exactly this class; the phase-03 security audit
  measured 1,004 of 4,757 2024 offseason matches failing the same way. **Not a new problem.**

Suggested phase grouping for `groups.ts`: auto = `autoRun`, `autoOwnership`; teleop =
`teleopOwnership`, `vault`; endgame = `endgame`. Placing Vault in teleop is a judgement call of
the same class `groups.ts`'s own header already documents for 2023 `link` and 2025 `algae`.

## The 2018 RP rules are easier than 2019's

> **Update 2026-09-07 (quick task 260907-057).** The `autoQuest` design choice this section
> explicitly deferred is now MADE: `parse` takes Option 1, the exact boolean rule
> (`autoRunPoints >= 15 && autoSwitchAtZero`), measured **100.000% agreement at all three
> official event tiers** (0 FP, 0 FN, n=23,042/2,558/2,712) over the full corpus population —
> not the 96.429%/116-FN figure below. That figure is SUPERSEDED: it came from an 18-event
> sample containing `2018cc` (Chezy Champs), an OFFSEASON event, and offseason disagreement for
> the identical rule is 11.19% (370/3,308) against 0.000% on official data — the offseason
> contamination this project excludes from every RP measurement. `predictThresholds` takes the
> measured numeric fallback (`autoRunPoints >= 15 && autoSwitchOwnershipSec >= 1`; the field is
> integral, confirmed against the corpus, so `>= 1` — not `> 0` — preserves the house `>=`
> convention), 0 false negatives at every tier, small honestly-stated over-firing. The
> recommended threshold sweep below is no longer needed. See `rp/2018.ts`'s file header for the
> full account.

Two bonuses; base rates over n=3,248 qual alliance-sides: `autoQuest` **61.98%**, `faceTheBoss`
**15.33%**.

### `faceTheBoss` — solved

`endgamePoints >= 90` gives **100.000%** agreement, 0 false positives, 0 false negatives. Exact,
numeric-only, therefore fully reachable from `predictThresholds`. No conservative branch needed.

### `autoQuest` — a real design choice, and this file does not make it

Both options, with their measured numbers:

| option | rule | agreement | FP | FN |
|---|---|---|---|---|
| 1 | `autoRunPoints >= 15 && autoSwitchAtZero` | **96.429%** | 0 | 116 |
| 2 | `autoRunPoints >= 15 && autoSwitchOwnershipSec > 0` | **99.569%** | 13 | 1 |

- Option 1 matches the house never-overstate preference, but `autoSwitchAtZero` is a boolean and
  is **unreachable from `predictThresholds`** — 2019's `completeRocket` asymmetry problem
  returning.
- Option 2 is numeric-only and therefore reachable, with better agreement and no asymmetry, but
  it introduces false positives, which cuts against never-overstate.

All 116 of Option 1's false negatives are the `autoSwitchAtZero === false` case specifically —
zero of them were "not all robots ran". A threshold sweep on `autoSwitchOwnershipSec`, following
the `scripts/recon-rp-thresholds-2019-2020.ts` pattern, would likely find a cut point that is
both exact and reachable. **Recommend that sweep as the deciding step** — the decision waits for
it.

`autoRunPoints` takes only 0 / 5 / 10 / 15 (counts 12 / 107 / 574 / 2,555) — a clean 3-robot
count at 5 points per robot.

## The nine registries — no branch, just data entry

None of these requires a code branch: both dispatch tables document the extension path, and
`packages/core/algorithms/sigma1/rp/rules.ts`'s header literally says extending back to 2016
(D-19, deferred) is data entry.

> **Update 2026-09-07 (quick task 260907-057).** Rows 1-3 are DONE for 2018 — see
> `breakdown/2018.ts`, `rp/2018.ts`, and the two dispatch-table entries. Rows 4-9 remain open and
> belong to the web/districts follow-up task, the same modules-then-web split the 2019/2020 job
> used (260903-4fs registered modules; 260904-nt4 shipped the web side). **New finding from this
> task's full-suite run, not previously documented here:** with rows 1-3 done and row 6 still
> open, `apps/web/src/lib/bonusRp.test.ts`'s registry-driven pin
> (`BONUS_RP_BY_SEASON` vs `RP_RULE_MODULES`) now fails for 2018 — 2 tests red
> (`covers exactly the seasons core registers`, `2018: bonus keys and order match core exactly`).
> This task's constraints explicitly forbade touching `apps/web/**`, so the failure is left red
> and reported rather than fixed; whoever picks up row 6 should expect the repo-root suite to
> show these 2 as red until that row lands (see `.planning/WINDOWS.md` for the open entry). The
> 2019/2020 precedent (`ad70f6ef`) fixed this pin as a same-task Rule 3 mechanical copy edit
> rather than deferring it — this task deliberately chose the opposite, more conservative split
> per its own explicit scope boundary.

| # | Registry | Where |
|---|---|---|
| 1 | Score components | new `packages/core/algorithms/breakdown/2018.ts` + entry in `breakdown/index.ts:47` (`SEASON_COMPONENT_MAPS`) — **DONE — 2018 (260907-057); 2017 and 2016 (260907-203)** — as of 2026-09-06 (quick task 260906-8kd) this entry automatically extends both `groups.test.ts` and `reconciliation.test.ts` via the exported `BREAKDOWN_REGISTERED_SEASONS`; the `groups.test.ts` pin is the single remaining conscious edit |
| 2 | Phase grouping | `packages/core/algorithms/breakdown/groups.ts:78` (`GROUPS_BY_SEASON`) — **DONE — 2018 (260907-057); 2017 and 2016 (260907-203)** |
| 3 | RP rules | new `packages/core/algorithms/sigma1/rp/2018.ts` + entry in `rp/rules.ts:44` (`RP_RULE_MODULES`) — **DONE — 2018 (260907-057); 2017 and 2016 (260907-203)** |
| 4 | District point ceilings | `packages/core/districts/pointModel.ts:79` — **three** tables: `DISTRICT_BASE_MAXIMA`, the DCMP weight map, the rookie-bonus map — **DONE — 2018 (260907-12k); 2017 and 2016 (260907-203)** |
| 5 | District display names | `apps/web/src/lib/districtNames.ts` — **DONE — 2018 (260907-12k); 2017 and 2016 (260907-203)** |
| 6 | Web bonus-RP copy | `apps/web/src/lib/bonusRp.ts:36` (`BONUS_RP_BY_SEASON`) — **DONE** — the client-side bonus-RP mirror landed in commit `b698e299` ("mirror 2018's bonus RP names into the client, restoring a green main"); the repo-root suite is green. **2017 and 2016 added 260907-203** (commit `97f7c42d`), in the same task that registered their RP modules rather than a task later — the 2019/2020 precedent's ordering, not 260907-057's split. (This row's note previously claimed an ACTIVE red in the repo-root suite — that was stale as of 2026-09-07; the fix landed same-day as the row-1-3 work, just under a different commit than this table had recorded.) |
| 7 | Web season bound | `apps/web/src/lib/seasons.ts:33` — `FIRST_SEASON = 2019` becomes 2016 once all three seasons publish — **OPEN for 2018, 2017 AND 2016, deliberately held.** 260907-12k closed rows 4/5/8 but explicitly declined to touch this row: no 2018 artifacts exist in R2 yet, and the 2019/2020 rollout already produced a year dropdown that shipped ahead of its artifacts once. Publish-then-reveal is the required order — flip `FIRST_SEASON` only after a publish lands for the season being revealed, never before. 260907-203 registered 2017 and 2016 everywhere else and left this row untouched for the same reason, so the year dropdown still starts at 2019 and no 2016/2017 page can 404. |
| 8 | Statbotics baseline reference | `packages/harness/statbotics.ts:75` — currently throws for any season outside the registered set — **DONE — 2018 and 2019 (260907-12k); 2017, 2016 AND 2020 (260907-203, commit `d32f901d`)** — every season the corpus carries now resolves offline and **there is no longer a named gap**. This row previously ended "2020 recorded as a named, unmeasured gap"; 260907-203 filled it (0.7262 / mse 0.1834 over 4,634 matches) and deleted the gap paragraph from `statbotics.ts`'s own header rather than leaving the file claiming a gap it had closed. |
| 9 | Championship pre-qualified lists | `packages/core/districts/prequalified.ts` — **OPEN for 2018, 2017 AND 2016** — untouched by 260907-12k and by 260907-203 alike; an absent list is the documented SAFE direction, so this stays a follow-up rather than a red. |

> **Update 2026-09-07 (quick task 260907-12k).** Rows 4, 5 and 8 closed this session, each
> re-measured directly against `data/corpus.sqlite` (rows 4/5) or fetched live (row 8) on
> 2026-09-07 rather than carried over from 2019's values — see `pointModel.ts`,
> `districtNames.ts` and `statbotics.ts`'s own updated headers for the full record. 2018's
> district point ceilings came back **identical** to every other registered season (22/16/30/15
> district, 66/48/90/30 dcmp observed, rookie bonus 10) — the outcome row 4's own hazard warning
> above was guarding against *assuming*, not an excuse to have skipped the re-measurement. Row 6
> was found already DONE (F-8) and its stale note corrected in the same pass. Rows 7 and 9 stay
> OPEN; row 7 is held deliberately per the publish-then-reveal note above.

- **Row 4 carries a real hazard.** 2018's district point values must be **verified against the
  corpus after ingest**, never assumed to carry over from 2019. Three tables that look
  copy-pasteable is exactly the shape that invites a wrong constant to ship quietly.
- **Row 9 closes an existing gap rather than adding a burden.** See F-G, below.

## THE TRAP — two hardcoded test season lists, both now closed

**CORRECTED 2026-09-06 (quick task 260906-8kd):** this section previously claimed THREE
hardcoded test season lists, naming `packages/core/districts/pointModel.test.ts` as one of
them. That claim was wrong. `pointModel.test.ts` was never a defect — see below. The real trap
was always two files, and quick task 260906-8kd closed both.

**Registering a season without editing an iteration list used to ship an entirely unproven
component map with a fully green test suite.** The distinction that matters is *iteration list*
(fails silently) vs *equality assertion* (fails loudly):

| file | what it hardcoded | status |
|---|---|---|
| `packages/core/algorithms/breakdown/groups.test.ts` | a module-level `const SEASONS = [2019, 2020, 2022, ...]` feeding three `for...of` sites | **CLOSED** — now iterates the exported `BREAKDOWN_REGISTERED_SEASONS` (`breakdown/index.ts`) |
| `packages/core/algorithms/breakdown/reconciliation.test.ts` | a module-level `const REGISTERED_SEASONS = [...]` feeding one `describe.each` and one `it.each` | **CLOSED** — now iterates the same `BREAKDOWN_REGISTERED_SEASONS` |

`BREAKDOWN_REGISTERED_SEASONS` is exported from `breakdown/index.ts`, derived from
`SEASON_COMPONENT_MAPS` the same way `rules.ts` derives `RP_REGISTERED_SEASONS`. Registering a
new season there now automatically extends both breakdown test files with no second edit to
either file's iteration source.

`packages/core/districts/pointModel.test.ts` was never one of the traps. It already iterates
`DISTRICT_REGISTERED_SEASONS` and its equality assertion (line 37) is a deliberate LOUD pin that
fails the instant a season is registered without the pin being updated. It was, and remains, the
exemplar the two breakdown files were just brought up to — not a defect that needed fixing.

The closing claim this section used to make — that only `rules.test.ts` is self-correcting "and
the other three are not" — was wrong for the same reason the file count was wrong. The real
distinction: an *iteration list* fails silently when a season is registered but not added to it;
an *equality assertion* fails loudly the instant the registered set no longer matches the pinned
literal. A registry-driven suite needs exactly one of the latter, not one per file. The
predecessor file already logged this trap after the 2019/2020 job, and it will recur in shape
(a new pin to update, not a new silent gap) on 2018, 2017 and 2016 — see "Then 2017, then 2016"
below.

**What a 2018 executor actually needs: which pins will fail, by design, when 2018 is
registered.** There are three, one per registry family, and each is a one-line literal edit
rather than a bug:

- `packages/core/algorithms/breakdown/groups.test.ts` — the `BREAKDOWN_REGISTERED_SEASONS` pin
  added by quick task 260906-8kd
- `packages/core/algorithms/sigma1/rp/rules.test.ts` — the existing `RP_REGISTERED_SEASONS` pin
- `packages/core/districts/pointModel.test.ts` — the existing `DISTRICT_REGISTERED_SEASONS` pin

The payoff: three loud, self-announcing failures now replace what used to be two silent green
passes.

## Already parameterized — these need NO work

The 2019/2020 job eliminated every hardcoded year on the replay path: `seasonBoundaryFor`
(positional cold start), `parseSeasonsRange` (accepts gapped lists), `isHeadlineEligible` (no
year literals), and the gap-aware season carry.

## District key spellings changed — and this has bitten before

2018's districts are `chs, fim, in, isr, mar, nc, ne, ont, pch, pnw` (10). 2019's, for
comparison, are `chs, fim, fma, fnc, in, isr, ne, ont, pch, pnw, tx` (11). So **`mar` becomes
`fma` and `nc` becomes `fnc`** — two new key spellings the codebase has never seen — and `tx`
did not exist in 2018. This is the identical re-keying class already documented at
`apps/web/src/lib/districtNames.ts:13` for `in` and `tx`, so the pattern for handling it exists;
the point is that registry 5 is not a pure append.

## What adding 2018 closes

`packages/core/districts/prequalified.ts:17-22` records that the 2019 prior-year Championship
pre-qualifier list could not be derived *because 2018 is not in the corpus*. Ingesting 2018
supplies that award data, which turns registry 9 from new work into the resolution of a gap the
codebase already documents against itself.

## Season size — MEASURED 2026-09-06, the ingest has run

**INGEST DONE, 2026-09-06.** `pnpm ingest --years 2018-2018` (297 requests) then
`pnpm ingest:rankings --years 2018-2018` (279 requests), both clean, 576 requests total —
comparable to the 1,038 the two-season 2019/2020 backfill took. What landed:

| season | matches | official quals w/ breakdown | events | ranking rows |
|---|---|---|---|---|
| **2018** | **20,606** | **14,156** | **278** | **9,533** |
| 2019 | 21,899 | 14,929 | 303 | 10,001 |
| 2020 | 4,763 | 3,820 | 196 | 2,101 |
| 2022 | 18,215 | 12,064 | 288 | 7,890 |
| 2026 | 20,305 | 15,191 | 310 | 9,121 |

**2018 is a full-size season** — its 14,156 official breakdown-bearing quals sit inside the
2019–2026 range of 12,064–15,191. The earlier "NOT MEASURED" placeholder this section carried is
now resolved; the expectation it declined to guess was correct.

Consequence for tuning, replacing the estimate in "What this buys": the 2022 origin's selection
window becomes 2018 + 2019 + 2020 = **32,905** official quals, up from 18,749 — a 1.75x increase
in the thinnest origin's evidence base. That number is now measured rather than approximated.

**Two ingest details worth not re-investigating:**

- *43 ranking rows skipped for an unregistered team key* is **normal, not a 2018 defect.** The
  same message appears for every season, at 21–87 rows (2022: 48, 2023: 73, 2024: 87, 2025: 67,
  2026: 21). 2018's 43 is mid-range.
- *41 events with empty rankings, 237 populated of 278.* In range for a season with offseason
  and never-played events.

**District keys confirmed against the ingested data, not just the API probe:** 2018 carries
`chs, fim, in, isr, mar, nc, ne, ont, pch, pnw`; 2019 carries `chs, fim, fma, fnc, in, isr, ne,
ont, pch, pnw, tx`. The `mar`→`fma` / `nc`→`fnc` rename and the absence of `tx` in 2018 are now
established from the corpus itself — see "District key spellings changed" above.

**Full test suite after the ingest: 204 files, 3,752 passed, 0 failures.** No test broke on the
presence of an ingested-but-unregistered season, because every suite scopes by season explicitly.
A pre-existing backup, `data/corpus.sqlite.bak-pre-2018`, was moved OUT of the repo to
`C:/Users/Jacob/Documents/SigmaScout-corpus-backups/` — see [[algorithm-identity-sweep-reads-all-of-data]]
for why a large file under `data/` breaks a test.

**Still not done (as of the ingest, 2026-09-06):** 2018 is ingested but NOT registered.
`breakdown/2018.ts` and `rp/2018.ts` do not exist, so `componentMapForSeason(2018)` and
`rpRuleModuleForSeason(2018)` still throw. The data is present and queryable; nothing reads it
yet.

> **Update 2026-09-07 (quick task 260907-057).** This line is now DONE for the algorithm layer:
> `componentMapForSeason(2018)` and `rpRuleModuleForSeason(2018)` both resolve. See "The nine
> registries" table above for the row-by-row status — rows 1-3 done, rows 4-9 (web/districts)
> remain open.

## Then 2017, then 2016

2017 (Steamworks) and 2016 (Stronghold) are conventionally additive games, so the hard season is
being done first and the two that follow should be routine. Because the job repeats three times,
two one-time investments are worth making **up front**, on the 2018 pass:

1. **DONE, 2026-09-06 (quick task 260906-8kd).** Make the two hardcoded test season lists
   registry-driven — see THE TRAP, above (corrected count: two files, not three). Paying this
   once removes the same silent failure from all three remaining passes.
2. **DONE, 2026-09-06 (quick task 260906-w38).** Generalize `scripts/recon-*-2019-2020.ts` into
   season-parameterized scripts rather than copying them a third time. The three probes now sit
   on a shared spine (`scripts/recon-shared.ts`) and take `--seasons`:
   `recon-tba-fields-2019-2020.ts` → `recon-breakdown-fields.ts`,
   `recon-rp-rates-2019-2020.ts` → `recon-rp-rates.ts`,
   `recon-rp-thresholds-2019-2020.ts` → `recon-rp-thresholds.ts`. **2018, 2017 and 2016 need no
   new script files.** (`recon-tba-fields.ts` and `recon-rocket-rp-2019.ts` were deliberately left
   alone — the first is a one-shot that also probed Statbotics, the second is genuinely
   season-specific.)

   **The substantive change, not just a rename: RP flags are now found by enumerating EVERY
   boolean field, never by matching names.** The old per-season `RP_FLAGS`/`SUPPORT_FLAGS`/
   `CANDIDATES`/`BOOLEAN_CANDIDATES` records are gone. Measured 2026-09-06, a name filter fails in
   both directions — it finds **zero** RPs for 2016, whose Breach and Capture RPs are named
   `teleopDefensesBreached`/`teleopTowerCaptured` with no RP-shaped name anywhere, and it falsely
   admits `quintetAchieved` (2022), `coopertitionCriteriaMet` (2023/2025) and 2025's `g###Penalty`
   fields. Since 2016 is in this job's own scope, that was not a hypothetical.

   Equivalence proven before the old scripts were retired, by re-running the new ones and
   comparing mechanically: the 2019 (51-row) and 2020 (34-row) field tables came back
   byte-identical; the full 2020 rates walk reproduced 186/52/3,806/7,612 and every flag rate and
   `rp`-distribution row; the full 2020 thresholds walk reproduced 7,640 alliance-sides and all
   five committed rows byte-identically. Enumeration is a strict superset — the rates re-run adds
   one row (`tba_shieldEnergizedRankingPointFromFoul`, 0 of 7,612) that the hardcoded list never
   tracked. Nothing previously recorded changed.

   **Do not "clean up" the two event filters.** `recon-rp-rates.ts` filters by
   `IN_SEASON_EVENT_TYPES` (types 0-6); `recon-rp-thresholds.ts` filters by `EVENT_TYPE_TIERS`,
   which also admits preseason. That is why the same 2020 season is 7,612 alliance-sides in one
   document and 7,640 in the other. Both are correct. Unifying them would silently move every
   number in `docs/data/tba-rp-thresholds-2019-2020.md`.

> **Update 2026-09-07 (quick task 260907-203) — BOTH SEASONS ARE IN.** 2017 and 2016 were
> ingested and then registered in every registry this todo lists except rows 7 and 9. The
> nine-registry table above was written 2018-scoped; rather than rewrite it, each row's status
> marker now names all three seasons and their commits, so the table stays the historical record
> of how the work was sequenced while still reporting today's truth.
>
> **The ingest.** Both seasons landed clean on 2026-09-07: 492 match + 460 ranking + 373 district
> requests, every one exit 0. Both are queryable in `data/corpus.sqlite`.
>
> **Additivity — the headline result, and it is a negative one for 2018.** corr(red, blue) on
> `totalPoints` is **+0.3416 for 2016** and **+0.3293 for 2017**, both comfortably inside the
> normal +0.24..+0.52 band every other registered season sits in. Neither needs a normalization
> treatment. **2018 (−0.4567) therefore remains the ONLY anti-additive season in the corpus** —
> extending backward past it did not find a second example, which makes 2018's open modelling
> question a genuine one-off rather than the leading edge of a pre-2019 pattern. That was the
> single most useful thing this backward extension could have told us, and it told us by not
> reproducing.
>
> **Breakdown reconciliation.** 2017: `sum(own components) + adjust + foulPoints == totalPoints`
> holds with **0 mismatches over 30,880** official non-offseason alliance-sides, all comp levels —
> no tolerance of any kind. 2016: **3 / 26,604** (0.01128%), and all three are ELIMINATION
> matches — `2016capl_f1m1` (+25), `2016milsu_qf4m1` (+25), `2016mndu2_f1m2` (−25). Proven to be
> TBA's own arithmetic three ways (our auto half reconciles against TBA's `autoPoints`, our teleop
> half against `teleopPoints`, and all 3 of 3 also fail TBA's own
> `totalPoints == autoPoints + teleopPoints + foulPoints + adjustPoints` with the exact
> opposite-signed gap). Exactly one falls in the suite's 2,000-row window, so 2016 ships a named
> `{ season: 2016, rate: 0.001, maxAbsGap: 25 }` tolerance with **`direction` deliberately
> OMITTED** — the three signs are mixed, and only the positive one is in today's window, so
> setting a direction would encode a sampling artifact as a fact.
>
> **A finding worth carrying forward: `breakdown/reconciliation.test.ts` is NOT qual-scoped.**
> Its sample has no `comp_level` filter and `match_key` sorts `f` < `qf` < `qm` < `sf`, so the
> window is playoff-inclusive. That is what makes 2016's `breachPoints`/`capturePoints` and
> 2017's `kPaBonusPoints`/`rotorBonusPoints` — always 0 in quals, real points in playoffs, counted
> by TBA inside `teleopPoints` — provably load-bearing rather than merely morally important.
> Omitting them costs 3,974/4,446 mismatching sides in 2016 and 1,195/5,500 in 2017. The RP
> suite, by contrast, genuinely is qual-scoped.
>
> **RP rules.** 2017 `kPa` (`autoFuelPoints + teleopFuelPoints >= 40`) and `rotor`
> (`autoRotorPoints/60 + teleopRotorPoints/40 >= 4`): **100.000%, 0 FP, 0 FN** at base
> (n=20,240), districtChampionship (n=2,446) and championship (n=2,700), thresholds flat across
> tiers as a measured fact, no tolerance. The obvious `autoRotorPoints + teleopRotorPoints >= 160`
> rule is WRONG (14 false positives) — auto and teleop rotors are worth different amounts, so a
> point SUM cannot recover a rotor COUNT. 2016 `breach` is **FOUR** of five defenses damaged, not
> five (the intuitive five-of-five reading costs 12,248 false negatives): 0 FP / 0 FN at all three
> tiers. 2016 `capture` reads the OPPONENT side's `towerEndStrength` (a side's tower is the one
> its opponent attacks) and gives 0 FN everywhere with exactly **one** false positive in 22,158
> sides — `2016melew_qm24`, 1/9,590 at `event_type` 1 — carried by a single named tolerance scoped
> to that event type alone.
>
> **Districts.** Re-measured from the corpus, not copied forward: 2016 = 1,170 `district_rankings`
> team rows / 2,770 `event_points_raw` entries; 2017 = 1,438 / 3,486. Both give district tier
> **22/16/30/15**, dcmp tier **66/48/90/30** observed, rookie bonus **10**, DCMP weight **3**
> confirmed by the exact 3x ratio. The measurement trap is recorded in `pointModel.ts`'s header:
> an entry's tier must be read from its OWN `district_cmp` boolean, never inferred from
> `events.event_type == 2`, which misclassifies and yields a bogus district-tier qual max of 66.
>
> **District display names — no map change needed.** 2016 carries 8 distinct keys
> (`chs, fim, in, mar, nc, ne, pch, pnw`), 2017 carries 10 (adds `isr`, `ont`). Every one already
> resolved; `mar`/`nc` had landed with 2018. **The backward extension reached 2016 without
> introducing a single new key** — 2018's two pre-rename spellings were the whole of it. Neither
> season has `tx`, which is therefore a 2019/2020-only key. Both key sets are now pinned by their
> own tests.
>
> **Statbotics.** 2016 acc 0.7312 / mse 0.1799 (13,286 matches), 2017 acc 0.6694 / mse 0.2023
> (15,429), and **2020** acc 0.7262 / mse 0.1834 (4,634) — the last closing row 8's named gap.
> **2017's 0.6694 is the lowest Statbotics accuracy of any season in the table**, below even
> 2018's 0.7435, and its 0.2023 is the worst `mse`. That is third-party evidence that 2017, not
> only 2018, is genuinely hard to predict — and a reason not to read a weak SigmaScout 2017 result
> as a SigmaScout defect.
>
> **Corpus census moved, and the bound with it.** Adding two seasons took "corpus events with no
> `event_rankings` row" from 490 to **570**, past `corpusCensus.test.ts`'s 550 upper bound. The 80
> new rows (2016: 37, 2017: 43) are legitimately rankings-less and NOT for 2020's
> cancelled-season reason — classified exhaustively as 66 offseason events TBA never posted a
> rankings table for, 9 preseason shells with zero matches, 4 playoff-only events with zero `qm`,
> and `2017micmp`'s finals-only district-championship shell. The bound was raised 550 -> **634**
> (= 570 + 64, one season of headroom at the worst observed non-cancelled rate) with the per-year
> table and that classification recorded in the comment. It affords exactly ONE more season by
> design: an eleventh must re-measure.
>
> **What stays OPEN, for all three seasons.** `FIRST_SEASON` (row 7) is still 2019;
> `prequalified.ts` (row 9) is still absent; and the `publish:seasons`/`publish:districts` season
> lists in `package.json` are untouched. Nothing from 2016, 2017 or 2018 is visible on the site,
> and nothing should be until a publish for those seasons lands in R2. Publish, then reveal.
>
> **Reported, not extended:** `rp/reconciliation.test.ts`'s `CHECKS` boundary list is an
> iteration list, and neither 2016 nor 2017 can take an entry — its shape requires a single
> `variable` compared against a tiered scalar, and every bonus in both seasons is multi-variable.
> That is the same exclusion the block's own header states for compound bonuses, and the same
> reason 2018 lists only `faceTheBoss`.

## Relationship to other todos

- `.planning/todos/completed/extend-corpus-2019-2020.md` (closed 2026-09-04) — the structure,
  the 2021 rule, and the trap's first sighting.
- `retune-sigma1-rolling-origin` / `rolling-origin-hyperparameter-tuning` — both origin lists and
  selection windows change if this job lands, with F-I's gating comparison named as the thing
  that must run before any promotion built on a 2018-inclusive corpus.
</content>
