---
phase: 06-team-pages
plan: 06
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - docs/publish-budget.md
  - packages/harness/payloadBudget.test.ts
autonomous: true
requirements: [TEAM-02, TEAM-03, TEAM-04, TEAM-05]

estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every published team artifact for 2022-2026 across all three algorithms carries the five D-01..D-05 additions, the real event names, and the scheduled-match rows — one republish, not five (TEAM-02, TEAM-03, TEAM-04, TEAM-05)."
    - "A live team page fetched from data.sigmascout.org parses and carries robotImageUrl for a team known to have a photo, activeYears, a per-metric percentile, per-alliance own variance on a Sigma1 match, and actual RP on a played match."
    - "docs/publish-budget.md records the new measured distribution for every page kind from THIS run, naming the exact command, the date and the artifact count — a figure here can be checked against a real run, not merely believed."
    - "packages/harness/payloadBudget.test.ts passes against the re-measured numbers, and the team-page maximum stays under its 375,000-byte budget."
    - "If a page kind's measured maximum exceeds its budget, the run is reported with the overage rather than the budget being raised to fit — the budget is a decision, not a description."
    - "The republish's real cost is recorded: wall clock, object count, total bytes, and the resulting share of one month's free-tier Class-A allowance."
  artifacts:
    - path: "docs/publish-budget.md"
      provides: "The re-measured payload table and machine-readable budget block for the post-D-01..D-05 artifacts"
      contains: "budgetMaxBytes"
  key_links:
    - from: "docs/publish-budget.md"
      to: "packages/harness/payloadBudget.test.ts"
      via: "the machine-readable block is the single source the test reads — never two files that could drift"
      pattern: "budgetMaxBytes"
---

<objective>
Spend the one republish this phase is allowed, then re-measure the budget it moved.

Purpose: all five published-data additions were grouped into this phase precisely so they ride a
single full publish rather than five. The prior run is measured at approximately 25 minutes of wall
clock, 54,673 PUTs, and 5.47% of one month's free-tier Class-A allowance. This plan is the gate
between "the publisher can produce the fields" and "the client can read them", and it is the only
plan permitted to run a full publish.

Output: live artifacts carrying every field the client plans read, plus a re-measured
`docs/publish-budget.md` whose machine-readable block keeps the automated budget gate honest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-team-pages/06-CONTEXT.md
@docs/publish-budget.md
@packages/harness/publish.ts
</context>

## Artifacts this phase produces

This plan creates no new code symbols. It produces:

- Updated live R2 objects under `v1/team/{teamKey}/{year}/{algorithmId}@{version}.json`, `v1/teams/{year}/...`, `v1/events/{year}/...`, `v1/event/{eventKey}/...`, `v1/compare/{year}.json`
- A new dated measurement section in `docs/publish-budget.md`
- Updated `maxBytes` / `medianBytes` / `p95Bytes` values in that file's machine-readable block

<tasks>

<task type="auto">
  <name>Task 1: Run the single full republish</name>
  <precondition>data/corpus.sqlite holds 2022-2026 matches and the team_media rows plan 06-03 filled, and the R2 credential pair is present in the untracked .env; confirm both by row counts and by presence-and-length only, never by printing a value.</precondition>
  <reversibility rating="costly">This overwrites every published team, teams, events, event and compare object with a new schema shape. Undoing it means another full republish at the same cost. The decision to spend it was made in 06-CONTEXT.md; this task executes it, it does not re-open it.</reversibility>
  <read_first>
    - docs/publish-budget.md (the prior run's metadata section: the exact command, the date/time window, the object count, and the per-page-kind table this run replaces)
    - packages/harness/publish.ts (the summary the run prints at the end: objects, totalBytes, per-page distributions, seed files, manifest keys)
    - package.json scripts (publish:seasons and its tsx --env-file=.env invocation)
    - .planning/phases/06-team-pages/06-04-SUMMARY.md (the dry-run projection this run must land close to)
    - .claude/CLAUDE.md section "Secrets handling"
  </read_first>
  <files>docs/publish-budget.md</files>
  <action>
    Run `pnpm publish:seasons` — the full 2022-2026, all-three-algorithms publish. Use the
    established script so the credential reaches the process through `--env-file`; never read, print
    or interpolate the file's contents.

    Expect roughly 25 minutes of wall clock and roughly 54,673 PUTs. Do not run it twice. Do not run
    a narrower scope as a rehearsal: a single-season run starts every algorithm cold rather than
    carrying state across the season boundary, so it would overwrite live artifacts with
    cold-start numbers that disagree with the harness. If the run fails partway, diagnose and
    resume rather than re-running from the top, and record what happened.

    Capture the run's own printed summary verbatim: object count, total bytes, and the per-page-kind
    median/p95/max distribution with each page kind's largest key. Compare the object count against
    the prior run's 54,671 page objects plus 2 manifests and explain any difference — the
    scheduled-match rows added by plan 06-04 change bytes, not object count, so a materially
    different count means an artifact was dropped or duplicated and must be investigated before this
    task is marked done.

    Record in the SUMMARY: the command, the date and UTC time window, the wall clock, the object
    count, the total bytes, and the PUT count expressed as a percentage of one month's free-tier
    Class-A allowance.

    Then confirm the live result over the network, not just from the run's own log: fetch one real
    team artifact from `https://data.sigmascout.org` for a team known from `team_media` to have a
    photo, and confirm the response body carries `robotImageUrl`, `activeYears`, a `percentile` on a
    metric, `redScoreVarianceOwn` on a Sigma1 match row, `actualRedRp` on a played row, and a real
    event name rather than an event key. Use a plain unauthenticated GET — these objects are public
    and no credential is involved.
  </action>
  <verify>
    <automated>node -e "(async()=>{const r=await fetch('https://data.sigmascout.org/v1/team/frc118/2024/sigma1@2.0.0+tuned-2026-08.json');if(!r.ok)throw new Error('HTTP '+r.status);const a=await r.json();const ev=a.events?.[0];const m=ev?.matches?.[0];const miss=[];if(!Array.isArray(a.activeYears))miss.push('activeYears');if(ev&&/^\d{4}[a-z]/.test(ev.eventName))miss.push('eventName still a key: '+ev.eventName);if(m&&m.redScoreVarianceOwn===undefined)miss.push('redScoreVarianceOwn');if(m&&m.actualWinner!==undefined&&m.actualRedRp===undefined)miss.push('actualRedRp');const anyPct=Object.values(a.seasonStats?.metrics??{}).some(v=>typeof v.percentile==='number');if(!anyPct)miss.push('percentile');if(miss.length)throw new Error('live artifact missing: '+miss.join(', '));console.log('live artifact carries every phase-6 field')})()"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm publish:seasons` exits 0 and its summary reports an object count within 1% of 54,673 total PUTs.
    - The live fetch gate above passes against `data.sigmascout.org`, proving the fields reached R2 and not merely the local assembly.
    - The fetched artifact's first event section carries a name that does not match the event-key pattern of four digits followed by a letter.
    - The SUMMARY records the command, date, UTC window, wall clock, object count, total bytes, and the Class-A percentage.
    - Exactly one full publish was run; if a resume was needed, the SUMMARY says so and explains why.
    - `git status --porcelain .env` reports nothing and no credential value appears anywhere in the task's output.
  </acceptance_criteria>
  <done>Every published artifact carries this phase's additions, confirmed by a live unauthenticated fetch, at a recorded cost.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure the payload budget and keep the automated gate honest</name>
  <read_first>
    - docs/publish-budget.md (full file — the payload table, the two named at-risk artifacts and why, the compression finding, and the machine-readable block at the bottom)
    - packages/harness/payloadBudget.test.ts (exactly what the gate asserts and how it parses the block)
    - .planning/phases/06-team-pages/06-04-SUMMARY.md (the dry-run projection, for comparison against the real figures)
  </read_first>
  <files>docs/publish-budget.md, packages/harness/payloadBudget.test.ts</files>
  <action>
    Update `docs/publish-budget.md` from Task 1's real run: replace the payload table's
    median/p95/max/largest-key values for every page kind, and update the run-metadata paragraph with
    this run's command, date, time window, object count and total bytes. Follow the file's own
    established pattern of naming the run that produced each figure — that is what makes a number
    here checkable rather than believed.

    Add a short subsection recording the delta on the two named at-risk artifacts: the year-wide
    teams table and the 292-match team page. State the team-page maximum's movement against the
    recorded 287,264-byte baseline in both bytes and percent, and the remaining headroom against the
    375,000-byte budget.

    Update the machine-readable block's measured values so `payloadBudget.test.ts` reads the new
    numbers. Do NOT raise any `budgetMaxBytes` value to accommodate a measurement: those are
    decisions, and a budget edited to fit the measurement stops being a gate. If a measured maximum
    exceeds its budget, leave the budget alone, let the test fail, and report the overage with the
    page kind, the key and the amount — a locked field is not trimmed to make a gate pass.

    Note in the file whether the compression finding still holds — the prior measurement found the
    plain public URL returned no `Content-Encoding` at all. If the custom domain now compresses,
    record the measured compressed size alongside the raw one; if it does not, say so, since a fresh
    check is cheap and the earlier finding is explicitly framed as a finding rather than an omission.
  </action>
  <verify>
    <automated>pnpm test -- payloadBudget</automated>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/publish-budget.md','utf8');if(d.includes('287,264')&&!d.includes('baseline'))throw new Error('stale max byte figure left in place with no baseline framing');if(!/2026-08-2[5-9]|2026-09/.test(d))throw new Error('no run date from this phase recorded');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- payloadBudget` passes, or fails with a reported overage that names the page kind, key and amount — and in the failing case no `budgetMaxBytes` value was changed (`git diff docs/publish-budget.md | grep -c '^[+-].*budgetMaxBytes'` returns 0).
    - The payload table's `team/{teamKey}/{year}` row carries a max-bytes value produced by this phase's run, and the surrounding prose names the run's date and command.
    - A delta subsection states the team-page maximum's movement in bytes and percent against 287,264, and the remaining headroom against 375,000 as a percentage.
    - The compression check is re-run and its result recorded, whichever way it comes out.
    - `docs/publish-budget.md`'s machine-readable block still parses — `pnpm test -- payloadBudget` reaching its assertions at all proves this.
  </acceptance_criteria>
  <done>The payload budget records this phase's real numbers and the automated gate reads them, with no budget value edited to make a measurement fit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` -> publisher process | The R2 credential pair crosses into a process whose output is logged |
| Publisher -> public R2 bucket | This run overwrites every object every visitor reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | `.env` in a transcript | high | mitigate | The run uses the established `tsx --env-file=.env` script; the task's precondition is a presence-and-length check, never a read. This is the exact failure that occurred in Phase 4 and forced a token rotation |
| T-06-14 | Tampering | overwriting every live object | medium | mitigate | Plan 06-04's dry-run already validated every artifact through its Zod schema at full corpus scale, and Task 1 confirms the result over the network rather than trusting the run's own log. Every field added this phase is optional, so a client reading a mixed-generation bucket during the run cannot fail to parse |
| T-06-06 | Denial of Service | payload growth | medium | mitigate | Task 2 re-measures against the recorded budget and refuses to raise a budget value to accommodate a measurement; an overage is reported, not absorbed |
| T-06-15 | Denial of Service | free-tier Class-A exhaustion | low | accept | One full run costs roughly 5.47% of a month's allowance and this phase runs exactly one. Recorded as a cost in the SUMMARY so the month's remaining headroom is visible rather than assumed |
</threat_model>

<verification>
- `pnpm publish:seasons` completed once, with its cost recorded.
- The live-artifact field gate passes against `https://data.sigmascout.org`.
- `pnpm test -- payloadBudget` passes against re-measured numbers, with no budget value edited.
- `docs/publish-budget.md` names the run that produced every figure it now carries.
</verification>

<success_criteria>
- Every field D-01..D-05 added is live and fetchable, proven over the network.
- Event section headings will show real names.
- The payload budget is re-measured from the real run, with the at-risk artifacts' deltas and headroom stated.
- Exactly one full republish was spent.
</success_criteria>

<output>
Create `.planning/phases/06-team-pages/06-06-SUMMARY.md` when done
</output>
