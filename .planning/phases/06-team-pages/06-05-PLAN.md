---
phase: 06-team-pages
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/web/package.json
  - apps/web/src/components/team/MetricHistoryChart.tsx
  - apps/web/src/components/team/MetricHistoryChart.test.tsx
  - apps/web/src/components/team/metricHistorySeries.ts
  - apps/web/src/components/team/metricHistorySeries.test.ts
  - apps/web/src/components/team/MetricHistoryTab.tsx
  - apps/web/src/routes/team.$teamNumber.tsx
  - apps/web/src/components/search/SearchBox.tsx
  - apps/web/src/components/search/SearchBox.test.tsx
  - apps/web/src/components/teams-table/columns.tsx
  - docs/first-paint-measurement.md
autonomous: true
requirements: [TEAM-06]

estimate:
  tokens: 48000
  raw_tokens: 96000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The Metric History tab plots the team's Total metric across the season with the team's OWN match sequence on the x-axis — array position 1..n over its metricHistory rows, never the season-wide matchIndex field, which would leave large gaps (D-12, TEAM-06, E9 populated)."
    - "A variance band is drawn around the line only where the metric row carries a spread; on OPR and EPA the band silently does not render, with no explanatory copy (D-13, TEAM-06, E9 partial)."
    - "Only the Total metric is plotted; per-component trajectories are deferred and must not appear (D-11)."
    - "Event boundaries are marked by alternating tinted vertical bands with a truncating event-name label, derived by detecting eventKey changes across consecutive metricHistory rows — no second lookup into events[] is needed for grouping (D-12)."
    - "Recharts loads only when the Metric History tab opens, via a dynamic import, and the deferral is confirmed by a real bundle measurement rather than assumed (D-14)."
    - "A team-year with zero played matches renders an empty-chart state — a plain labelled axis with no line — distinct from the load-failure state (E9 empty)."
    - "The chart tab's loading state is a chart-shaped skeleton with no text, covering both the dynamic-import wait and any brief render delay (E9 loading)."
    - "A dynamic-import failure renders 'Chart failed to load' with a Retry that re-attempts the import, not the data fetch — the metric-history array already arrived with the page artifact (E9 error)."
    - "A single played match renders as one point with no line to draw between; many render the ordinary line-plus-band (E9 zero-one-many)."
    - "A team's search-box hit navigates to /team/{teamNumber}, carrying the selected year and algorithm as search params (E10 populated, D-15/D-16)."
    - "The Teams table's team-number and nickname cells link to /team/{teamNumber}, carrying the currently-selected year and algorithm, and keep their existing truncate-plus-title treatment (E11 populated)."
    - statement: "A high-match-count team fixture renders the chart with legible point and axis-label density (E9 overflow) — verified by a real render, since Recharts' default tick behavior at that volume has not been observed in this repo."
      verification: backstop
    - statement: "An event-name label on a boundary band truncates with an accessible full-text affordance (E9 long-text), verified against a real long-event-name fixture inside the chart specifically, since Recharts label rendering is a different code path from the plain-HTML event section header."
      verification: backstop
  prohibitions:
    - "The chart's band must never be drawn from a match-level predictive variance, and the match table's bands must never be drawn from the team's consistency spread. These are two different quantities answering two different questions; a sketch that drew one from the other put real results 7-10 sigma outside the drawn band while the model itself was fine. Whichever is drawn must be the one the axis label names."
  artifacts:
    - path: "apps/web/src/components/team/metricHistorySeries.ts"
      provides: "buildMetricSeries and detectEventBands — the pure derivation the chart renders"
      exports: ["buildMetricSeries", "detectEventBands"]
    - path: "apps/web/src/components/team/MetricHistoryChart.tsx"
      provides: "The Recharts ComposedChart: Area band plus Line, event bands, empty state"
      contains: "ComposedChart"
    - path: "apps/web/src/components/team/MetricHistoryTab.tsx"
      provides: "The lazy boundary, skeleton, and import-failure retry"
      contains: "lazy"
  key_links:
    - from: "apps/web/src/components/team/MetricHistoryTab.tsx"
      to: "apps/web/src/components/team/MetricHistoryChart.tsx"
      via: "React.lazy(() => import('./MetricHistoryChart.js')) inside a Suspense boundary"
      pattern: "lazy\\("
    - from: "apps/web/src/components/search/SearchBox.tsx"
      to: "apps/web/src/routes/team.$teamNumber.tsx"
      via: "navigate({ to: '/team/$teamNumber', params: { teamNumber } })"
      pattern: "/team/\\$teamNumber"
---

<objective>
Build the second tab — the metric-history plot — and point the two Phase 5 surfaces that were built
to lead here at the route that now exists.

Purpose: TEAM-06 is the only phase requirement that reads nothing the republish adds. Its data,
`metricHistory[]`, is already published and already browser-safe, so this plan can run in parallel
with the publisher wave instead of queuing behind it. The two integration points (search box, Teams
table) are in the same plan because they are small, they share no files with the publisher work, and
Phase 5 left explicit notes at both call sites saying Phase 6 changes exactly one destination each.

Output: a working Metric History tab behind a measured dynamic import, and a Teams page whose rows
and search results finally go somewhere.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-team-pages/06-CONTEXT.md
@.planning/phases/06-team-pages/06-UI-SPEC.md
@.planning/phases/06-team-pages/06-PATTERNS.md
@.claude/skills/sketch-findings-sigmascout/references/chart-craft.md
@.claude/skills/sketch-findings-sigmascout/references/uncertainty-display.md
@packages/harness/metricHistorySchema.ts
@apps/web/src/lib/metricKeys.ts
@docs/first-paint-measurement.md
</context>

## Artifacts this phase produces

Symbols and paths created by THIS plan (new — exclude from drift verification):

- `apps/web/src/components/team/metricHistorySeries.ts` — `buildMetricSeries`, `detectEventBands`, types `MetricSeriesPoint`, `EventBand`
- `apps/web/src/components/team/MetricHistoryChart.tsx` — `MetricHistoryChart` (default export, for the dynamic import)
- `apps/web/src/components/team/MetricHistoryTab.tsx` — `MetricHistoryTab`
- `apps/web/package.json` — dependency `recharts`
- `docs/first-paint-measurement.md` — a new dated entry, "Fifth measurement — the chart tab's dynamic import"

<tasks>

<task type="auto">
  <name>Task 1: The series derivation and the Recharts chart</name>
  <read_first>
    - packages/harness/metricHistorySchema.ts (full file — MetricHistoryRowSchema's fields, and matchIndex's own doc comment stating it is the season-wide chronological position, not a per-team counter)
    - packages/harness/pageArtifacts.ts TeamSeasonArtifactSchema.metricHistory and TeamMetricSchema
    - apps/web/src/lib/metricKeys.ts (TOTAL_KEY, metricKeysFor — the chart keys off this, never off an inspected row)
    - .claude/skills/sketch-findings-sigmascout/references/chart-craft.md (derive coupled geometry; render it and look; one axis; text wears text tokens not the series colour)
    - .claude/skills/sketch-findings-sigmascout/references/uncertainty-display.md (the two-± rule — the band here is the D-09 consistency spread, the TeamMetric.spread field)
    - .planning/phases/06-team-pages/06-UI-SPEC.md "UI Considerations" E9 and the Color section
    - apps/web/src/styles/theme.css (the --color-* tokens; no hex literal may appear in a component)
  </read_first>
  <files>apps/web/package.json, apps/web/src/components/team/metricHistorySeries.ts, apps/web/src/components/team/metricHistorySeries.test.ts, apps/web/src/components/team/MetricHistoryChart.tsx, apps/web/src/components/team/MetricHistoryChart.test.tsx</files>
  <action>
    Install Recharts into the web app with `pnpm --filter web add recharts` at version 3.10.x, the
    version CLAUDE.md fixes and `06-RESEARCH.md`'s legitimacy audit cleared.

    Create `metricHistorySeries.ts` as a pure module with no React import, so it is testable without
    a DOM:

    `buildMetricSeries(rows, metricKey)` returns `MetricSeriesPoint[]` where each point carries
    `x` = the row's **array position plus one** in the team's own `metricHistory` array, `value` =
    `row.metrics[metricKey]?.value`, `spread` = `row.metrics[metricKey]?.spread`, `matchKey`, and
    `eventKey`. Use array position, never `row.matchIndex` — that field is the season-wide
    chronological position and plotting it directly leaves large gaps, which D-12 rules out by name.
    Do not re-sort the array: `TeamSeasonArtifactSchema.metricHistory` is already this team's own
    rows in this team's own chronological order. A row missing the metric produces a point with an
    undefined `value`, which Recharts renders as a gap rather than a zero.

    `detectEventBands(points)` returns `EventBand[]` of `{ eventKey, startX, endX, index }` by
    walking consecutive points and opening a new band wherever `eventKey` changes. Each row already
    carries `eventKey`, so no second lookup into `events[]` is needed for grouping — only for a
    display name, which the caller supplies.

    Create `MetricHistoryChart.tsx` with a **default export** (the dynamic import in Task 2 needs
    one). Props: `{ rows, algorithmId, season, eventNameByKey }`. It plots `TOTAL_KEY` only — D-11
    ships Total only, and ten component lines would be spaghetti. Compose a Recharts
    `ComposedChart` containing, in order: `ReferenceArea` bands for alternating event blocks, an
    `Area` for the variance band, and a `Line` for the value.

    Draw the band from `value - spread` to `value + spread` using an `Area` with a two-element
    `dataKey` range. When no point in the series carries a `spread` — OPR publishes Total only with
    no spread and EPA publishes no spread on any metric — do not render the `Area` at all and do not
    render any explanatory text. D-13 locks this as silent.

    Event bands alternate a tint of `var(--color-bg-surface)` at reduced opacity, with the event's
    display name labelled at the top of each band, truncated with an accessible full-text
    affordance. Every colour is a CSS custom property; no hex literal may appear in this component.
    The value line uses a text/ink token, never an alliance colour — red and blue mean alliances in
    FRC and cannot be reused here.

    Label the y-axis with the metric name and the x-axis as the team's match sequence. The axis is
    zoomed to the data, never anchored at zero, which is exactly why it must be labelled.

    Handle the two degenerate series shapes explicitly: zero points renders a plain labelled axis and
    no line (E9 empty); exactly one point renders a single dot with no line segment (E9
    zero-one-many).

    Add tests for both modules. The series tests are pure. The chart tests render into jsdom and
    assert on the produced SVG structure — element counts and `dataKey` wiring — rather than on pixel
    positions, which jsdom does not compute.
  </action>
  <verify>
    <automated>pnpm --filter web test -- metricHistorySeries</automated>
    <automated>pnpm --filter web test -- MetricHistoryChart</automated>
  </verify>
  <acceptance_criteria>
    - `buildMetricSeries` over a fixture whose rows carry `matchIndex` values 4, 17 and 92 yields `x` values 1, 2, 3 — asserted numerically.
    - `grep -c 'matchIndex' apps/web/src/components/team/metricHistorySeries.ts` returns at most 1, and if present it is only in the returned point's passthrough, never in an `x` computation.
<!-- planner-region-allow: matchIndex -->
    - `detectEventBands` over a fixture with rows `[A, A, B, B, B, C]` yields 3 bands with `startX`/`endX` of 1-2, 3-5 and 6-6.
    - A chart test with rows carrying `spread` finds an `Area` element in the rendered SVG; the same test with `spread` absent on every row finds zero `Area` elements and no text node containing the words `variance` or `spread`.
    - A chart test with a single-row fixture renders without throwing and produces no line path with more than one coordinate pair.
    - A chart test with a zero-row fixture renders axis elements and zero line elements.
    - `grep -Ec '#[0-9A-Fa-f]{6}' apps/web/src/components/team/MetricHistoryChart.tsx` returns 0 — every colour is a token.
    - Only `TOTAL_KEY` is plotted: `grep -c 'metricKeysFor' apps/web/src/components/team/MetricHistoryChart.tsx` returns 0, and the component references `TOTAL_KEY`.
    - `apps/web/package.json` lists `recharts` at a `3.10.` version.
  </acceptance_criteria>
  <done>The chart plots the team's own match sequence with a band where a spread exists, silent where it does not, with event blocks tinted and labelled.</done>
</task>

<task type="auto">
  <name>Task 2: The lazy boundary, its states, and a real measurement of the deferral</name>
  <read_first>
    - apps/web/src/routes/team.$teamNumber.tsx (the tab shell plan 06-01 built, and the metric-history panel placeholder test id)
    - apps/web/src/components/Skeletons.tsx (the skeleton family the chart placeholder joins)
    - .planning/phases/06-team-pages/06-UI-SPEC.md "Copywriting Contract" rows for the chart tab's loading and load-failure states
    - docs/first-paint-measurement.md — the FOURTH entry's Method section (both builds, real CDP throttling via Network.emulateNetworkConditions plus Emulation.setCPUThrottlingRate at 4x, LCP from a PerformanceObserver registered before navigation, median of three) and its Decision section explaining why route-level code splitting was reverted
    - .planning/phases/05-site-shell-navigation-browsing/05-CONTEXT.md D-19 (the reverted split)
  </read_first>
  <files>apps/web/src/components/team/MetricHistoryTab.tsx, apps/web/src/routes/team.$teamNumber.tsx, docs/first-paint-measurement.md</files>
  <action>
    Create `MetricHistoryTab.tsx` holding the lazy boundary: `React.lazy(() =>
    import("./MetricHistoryChart.js"))` inside a `Suspense` whose fallback is a chart-shaped
    `Skeleton` with no text, matching the skeleton-not-text pattern used elsewhere in the app.

    Wrap the boundary in an error boundary whose fallback renders the copy `Chart failed to load` and
    `Check your connection and try again.` with a **Retry** button. Retry must re-attempt the
    dynamic import, not the data fetch — the metric-history array already arrived with the page
    artifact, so a data refetch would fix nothing. Implement retry by remounting the lazy boundary
    with a changed key.

    Mount `MetricHistoryTab` in the route's Metric History panel, replacing plan 06-01's
    placeholder. Keep the placeholder's test id on the panel so 06-01's tab test keeps passing.
    Build `eventNameByKey` from `artifact.events` and pass it down.

    **Then measure the deferral, do not assume it.** D-14's own text requires this, and D-19's
    reverted route split is the reason: that split deferred only ~11 KB because the weight sat in the
    shared vendor chunk, and it cost a serialized round trip, measuring slower on every profile.
    Recharts is used by exactly one tab and nothing else, so the deferral should be real — but the
    claim is the thing under test.

    Reuse `docs/first-paint-measurement.md`'s fourth entry's method verbatim: build both variants
    (chart statically imported versus lazily imported) from the same worktree, serve them from
    identical local static servers on adjacent ports, drive Chromium via Playwright with real CDP
    throttling on all three network profiles at 4x CPU, read LCP from a `PerformanceObserver`
    registered before navigation, and take the median of three runs per cell. Measure the Overview
    tab's LCP (the tab that should be unaffected) and record the eager-bundle size for both variants.

    **Do not re-attempt route-level code splitting** as part of this or any measurement in this
    phase — it was tried, measured slower on every profile, and reverted.

    Append a fifth dated entry to `docs/first-paint-measurement.md` following the existing entries'
    structure — Why this entry exists, Method, Results table, size table, Decision — and state the
    outcome plainly whether or not it favours the lazy import. If the lazy import does not measurably
    help, say so and keep it anyway only if it is at worst neutral; if it measures worse, revert to a
    static import and record that.
  </action>
  <verify>
    <automated>pnpm --filter web test -- MetricHistoryTab</automated>
    <automated>pnpm --filter web build</automated>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/first-paint-measurement.md','utf8');const m=d.match(/^## (Fifth|Fourth) measurement/gm)||[];if(!m.some(s=>s.startsWith('## Fifth')))throw new Error('no fifth dated entry recorded');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - A component test renders `MetricHistoryTab` with a rejecting import stub and finds the text `Chart failed to load` and a Retry button; clicking Retry triggers a second import attempt (asserted by a call count on the stub), and no data refetch occurs (asserted by a query-function call count of zero).
    - A component test in the suspended state finds a skeleton element and zero text nodes inside the panel.
    - `pnpm --filter web build` output shows `recharts` in a chunk that is not the eager entry chunk — assert by checking that the chunk containing `recharts` is not referenced by a `<script>` tag in `dist/index.html`.
    - `docs/first-paint-measurement.md` gains a `## Fifth measurement` section containing a three-row network-profile results table, an eager-JS size table for both variants, and an explicit Decision line.
    - The recorded decision matches the shipped code: if the entry's decision is to revert to a static import, `MetricHistoryTab.tsx` contains no `lazy(` call.
    - The measurement section contains no claim that route-level code splitting was used.
  </acceptance_criteria>
  <done>The chart tab loads its library on demand behind a skeleton, fails gracefully with an import-scoped retry, and the deferral is a recorded measurement rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Point the search box and the Teams table at the team route</name>
  <read_first>
    - apps/web/src/components/search/SearchBox.tsx (the SearchNavigate union type, handleSelectTeam and its Phase 5 note naming this exact change, handleSelectEvent as the shape to leave alone)
    - apps/web/src/components/teams-table/columns.tsx (the teamNumber column with no custom cell, and the nickname cell's existing truncate-plus-title treatment)
    - apps/web/src/components/teams-table/TeamsTable.tsx (how a cell renderer sits inside the virtualized, single-scroll-element table — a link must not break either)
    - apps/web/src/lib/searchParams.ts (TeamSearchSchema, added by plan 06-01)
    - apps/web/src/routes/team.$teamNumber.tsx (the route's param and search shape)
  </read_first>
  <files>apps/web/src/components/search/SearchBox.tsx, apps/web/src/components/search/SearchBox.test.tsx, apps/web/src/components/teams-table/columns.tsx</files>
  <action>
    In `SearchBox.tsx`, widen the local `SearchNavigate` union's `to` field to include the team
    route's id alongside the two existing entries, and add a `params` field to the option object so
    the team destination can carry its path param. Change `handleSelectTeam` to navigate to the team
    route with `params: { teamNumber: String(team.teamNumber) }`, preserving `year` and `algorithm`
    from the current search exactly as the event handler does. The route takes the plain team number,
    not the internal key — that is D-15's whole point, so pass `team.teamNumber`, not `team.teamKey`.
    Rename the parameter from its underscore-prefixed unused form and delete the interim-target
    comment, replacing it with a one-line note that the destination is now the real team page. Leave
    `handleSelectEvent` untouched: the event detail page is Phase 7.

    In `teams-table/columns.tsx`, give the `teamNumber` column a `cell` renderer wrapping the value
    in a router `Link` to the team route with the row's team number as the path param and the current
    `year`/`algorithm` as search params, and wrap the existing nickname cell's `span` in the same
    `Link` while keeping its `title` attribute and its `block max-w-full` truncation classes exactly
    as they are. Do not replace the truncation treatment — it is what keeps a multi-byte character
    from being cut mid-codepoint, and it is the Phase 5 precedent this phase inherits.

    Read the current year and algorithm inside the cell renderers through the same loose
    cross-route search read the ribbon components already use, or thread them through
    `buildColumns(algorithmId, season)`'s existing arguments — prefer threading, since both values
    are already parameters of that function.

    Add `SearchBox.test.tsx` cases asserting the team-hit navigation target and its params/search.
  </action>
  <verify>
    <automated>pnpm --filter web test -- SearchBox</automated>
    <automated>pnpm --filter web typecheck</automated>
    <automated>pnpm --filter web test:e2e -- no-page-pan</automated>
  </verify>
  <acceptance_criteria>
    - A `SearchBox.test.tsx` case selects a team result and asserts `navigate` was called with the team route id, `params.teamNumber` equal to the selected team's number as a string, and a search updater preserving the current `year` and `algorithm`.
    - Region-scoped so the widened `SearchNavigate` union's own `"/teams"` member is not counted: `awk '/function handleSelectTeam/,/^  }/' apps/web/src/components/search/SearchBox.tsx | grep -c '/teams'` returns 0 — the interim destination is gone from the handler.
<!-- planner-region-allow: /teams -->
    - A `columns.tsx` render test finds an anchor in both the team-number and nickname cells whose href contains `/team/` and the row's team number.
    - The nickname cell's anchor still carries the full nickname in a `title` attribute and the `max-w-full` truncation class.
    - `pnpm --filter web test:e2e -- no-page-pan` still passes for the existing Teams routes — adding links must not change the table's measured width.
  </acceptance_criteria>
  <done>A team hit in the search box and a click on a Teams table row both land on that team's page for the current year and algorithm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published artifact -> chart render | `metricHistory[]` rows of arbitrary length and sparsity cross into a rendering library new to this repo |
| Dynamic import -> runtime | A chunk fetched after first paint can fail independently of the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | `MetricHistoryChart` | low | mitigate | `metricHistory` rows already passed `TeamSeasonArtifactSchema.parse` at fetch time; the series builder additionally tolerates a missing metric per row by producing an undefined value rather than a coerced zero |
| T-06-13 | Denial of Service | dynamic-import failure | low | mitigate | An error boundary around the lazy chunk with an import-scoped Retry; a failed chunk degrades one tab, never the page |
| T-06-01 | Tampering | search-box and table link targets | low | mitigate | Both build the destination from an integer team number already validated by the source artifact's schema, and the destination route re-validates every search param through `TeamSearchSchema` |
| T-06-SC | Tampering | supply chain (`recharts`) | medium | mitigate | `recharts` is a CLAUDE.md-fixed dependency, not a search-discovered one. `06-RESEARCH.md`'s Package Legitimacy Audit records an OK verdict against the npm registry with no `postinstall` script; the version installed must match the audited `3.10.x` line, asserted in Task 1's acceptance criteria |
</threat_model>

<verification>
- `pnpm --filter web typecheck` clean.
- `pnpm --filter web test` green including the four new/updated test files.
- `pnpm --filter web build` succeeds and the recharts chunk is not in the eager entry.
- `docs/first-paint-measurement.md` carries a fifth dated entry whose recorded decision matches the shipped code.
- `pnpm --filter web test:e2e -- no-page-pan` still green for the Teams routes.
</verification>

<success_criteria>
- The Metric History tab plots the team's own match sequence with a variance band only where a spread exists.
- Recharts is deferred behind the tab, and the deferral is a measurement in the project's own dated methodology, not a claim.
- The search box and the Teams table both navigate to the real team page.
</success_criteria>

<output>
Create `.planning/phases/06-team-pages/06-05-SUMMARY.md` when done
</output>
