---
phase: 06-team-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/teamKey.ts
  - apps/web/src/lib/teamKey.test.ts
  - apps/web/src/lib/api/team.ts
  - apps/web/src/lib/searchParams.ts
  - apps/web/src/lib/searchParams.test.ts
  - apps/web/src/routes/team.$teamNumber.tsx
  - apps/web/src/routeTree.gen.ts
  - apps/web/src/components/ui/tabs.tsx
  - apps/web/src/styles/theme.css
  - apps/web/src/components/team/OverviewTab.tsx
  - apps/web/src/components/team/SeasonHeader.tsx
  - apps/web/src/components/team/EventSectionList.tsx
  - apps/web/src/components/team/TeamStates.tsx
  - apps/web/src/components/team/TeamStates.test.tsx
  - apps/web/e2e/team-page.spec.ts
autonomous: true
requirements: [TEAM-02, TEAM-03, TEAM-04]

estimate:
  tokens: 48000
  raw_tokens: 96000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Navigating to /team/1114?year=2024&algorithm=sigma1 renders that team's nickname, team number and season record read from the live published team artifact — the route exists and resolves for a real team (TEAM-02, TEAM-03)."
    - "The URL path carries the plain team number (/team/1114), never the internal corpus key (/team/frc1114) — the frc prefix is added by one named helper before artifactKey is called (D-15)."
    - "The active tab is a URL search param (?tab=overview|history), typed through TanStack Router; browser back/forward moves between tabs and a shared link opens on the named tab (D-16)."
    - "A hand-edited or malformed ?tab=, ?year= or ?algorithm= value resolves to a declared valid state before any component reads it, via TeamSearchSchema's .catch() fallbacks (T-06-01)."
    - "A team-year the team did not compete in keeps the page, shows the team's identity, states that the team did not compete that year, and offers its active years as one-click links; it never silently redirects to another year (D-19)."
    - "A valid team-year whose artifact carries zero events shows its own distinct copy — a data-gap message, not the D-19 'didn't compete' claim — below a normally-rendered season header (E5 empty)."
    - "A long team nickname is truncated by CSS ellipsis and exposed in full via the title attribute — never by slicing the string, so no multi-byte character can be cut mid-codepoint (TEAM-02 encoding edge)."
    - "A team with a null or absent nickname renders 'Team {teamNumber}' in the nickname slot; the team number is always present so the header can never lose its identity anchor (E1 partial)."
    - "The page's loading state is a shaped skeleton — header block plus two-to-three skeleton event-section cards — not a single full-page spinner (E1 loading, E5 loading)."
    - "A fetch failure renders the canonical error copy with 'team {teamNumber}' substituted plus a Retry button; the whole page surfaces the failure because there is exactly one artifact fetch (E1 error, E5 error, E2 error, E7 error)."
    - "Both tabs are present and clickable from first render — they gate content, not their own existence — and Overview is the default when ?tab= is absent (E8 loading)."
    - "The D-19 empty state renders its heading and body even when activeYears is absent or empty, omitting the chip row entirely rather than leaving a dangling 'This team's active seasons:' label (E4 partial)."
    - "The D-19 active-year chip row reads correctly for one active year and for many — one chip per entry, no special singular phrasing (E4 zero-one-many)."
    - "The D-19 empty state renders the header identity chrome (name, image slot, TBA link — none of which are year-scoped) above the empty body (E4 empty, E4 populated)."
    - "A malformed or partial published artifact routes to the error state via ArtifactValidationError; it is never rendered as if complete (T-06-02)."
    - "The FRC alliance colour tokens and the loser-ink token are defined once in the theme at their recorded, colour-vision-validated values, so plan 06-08's match table can consume them without a hex literal ever appearing in a component."
  artifacts:
    - path: "apps/web/src/routes/team.$teamNumber.tsx"
      provides: "The /team/$teamNumber route: validateSearch, single artifact query, tab shell, page states"
      contains: "createFileRoute"
    - path: "apps/web/src/lib/api/team.ts"
      provides: "fetchTeamArtifact + teamQueryOptions, mirroring lib/api/teams.ts exactly"
      exports: ["fetchTeamArtifact", "teamQueryOptions"]
    - path: "apps/web/src/lib/teamKey.ts"
      provides: "toTeamKey/teamNumberFromKey — the single home for the frc-prefix convention"
      exports: ["toTeamKey", "teamNumberFromKey"]
    - path: "apps/web/src/components/team/OverviewTab.tsx"
      provides: "The Overview panel's composition seam — mounts SeasonHeader and EventSectionList with their final prop contracts"
    - path: "apps/web/src/components/team/TeamStates.tsx"
      provides: "D-19 year-mismatch empty state and the E5 zero-events data-gap state"
    - path: "apps/web/e2e/team-page.spec.ts"
      provides: "End-to-end proof the route reads a real published artifact from the R2 origin"
  key_links:
    - from: "apps/web/src/routes/team.$teamNumber.tsx"
      to: "apps/web/src/lib/api/team.ts"
      via: "useQuery(teamQueryOptions({ teamKey: toTeamKey(teamNumber), ... }))"
      pattern: "teamQueryOptions"
    - from: "apps/web/src/lib/api/team.ts"
      to: "packages/harness/pageArtifacts.ts"
      via: "artifactKey({ page: 'team', ... }) and TeamSeasonArtifactSchema.parse"
      pattern: "TeamSeasonArtifactSchema"
    - from: "apps/web/src/components/team/OverviewTab.tsx"
      to: "apps/web/src/components/team/SeasonHeader.tsx"
      via: "props { artifact, algorithmId, season } — the contract plan 06-07 fills in"
      pattern: "SeasonHeader"
---

<objective>
Prove the team-page vertical end to end, then build the page shell every later plan fills in.

This plan LEADS with the phase's tracer: one real, already-published field flowing from the R2
bucket through a new route into a rendered element on `/team/{number}`. It then adds the tab shell
(D-16) and the page's four non-populated states.

Purpose: the publish -> R2 -> browser path is already proven by Phases 4 and 5. What is unproven is
the *team-artifact* read path — `teamKey` derivation (Pitfall 4), the `page: "team"` key shape, a
287 KB artifact parse, a new file-route with a third search param. The tracer proves exactly that,
at zero republish cost, so every D-01..D-05 field added later rides a path that already works.

Output: `/team/{number}` with two tabs, four page states, and named composition seams
(`SeasonHeader`, `EventSectionList`) that plans 06-07 and 06-08 fill without touching each other's
files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-team-pages/06-CONTEXT.md
@.planning/phases/06-team-pages/06-PATTERNS.md
@.planning/phases/06-team-pages/06-UI-SPEC.md
@apps/web/src/lib/api/teams.ts
@apps/web/src/routes/teams.tsx
@apps/web/src/lib/searchParams.ts
@apps/web/src/components/StateViews.tsx
@apps/web/src/components/Skeletons.tsx
</context>

## Artifacts this phase produces

Symbols and paths created by THIS plan (new — exclude from drift verification):

- `apps/web/src/lib/teamKey.ts` — `toTeamKey(teamNumber: number): string`, `teamNumberFromKey(teamKey: string): number`, `TEAM_KEY_PREFIX`
- `apps/web/src/lib/api/team.ts` — `FetchTeamArtifactParams`, `fetchTeamArtifact`, `teamQueryOptions`
- `apps/web/src/lib/searchParams.ts` — new exports `TeamSearchSchema`, `TeamSearch`, `TEAM_TABS`
- `apps/web/src/routes/team.$teamNumber.tsx` — route id `/team/$teamNumber`, component `TeamPage`
- `apps/web/src/components/ui/tabs.tsx` — shadcn `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`
- `apps/web/src/styles/theme.css` — `--alliance-red`, `--alliance-red-soft`, `--alliance-blue`, `--alliance-blue-soft`, `--loser-ink`
- `apps/web/src/components/team/OverviewTab.tsx` — `OverviewTab`
- `apps/web/src/components/team/SeasonHeader.tsx` — `SeasonHeader`, `SeasonHeaderProps`
- `apps/web/src/components/team/EventSectionList.tsx` — `EventSectionList`, `EventSectionListProps`
- `apps/web/src/components/team/TeamStates.tsx` — `YearMismatchEmptyState`, `NoEventDataState`
- `apps/web/e2e/team-page.spec.ts` — e2e spec (no exported symbols)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "/team/1114 shows a real team" — one path only</name>
  <read_first>
    - apps/web/src/lib/api/teams.ts (the exact fetch + Zod + query-options shape to mirror)
    - apps/web/src/routes/teams.tsx (validateSearch + useQuery + useAlgorithmVersion + keepPreviousData wiring)
    - apps/web/src/lib/artifactOrigin.ts (artifactUrl — the data.sigmascout.org resolver)
    - apps/web/src/lib/api/errors.ts (ArtifactFetchError, ArtifactValidationError)
    - apps/web/src/lib/perfMarks.ts (markArtifactParsed)
    - packages/harness/pageArtifacts.ts lines 100-130 and 300-335 (artifactKey's team branch, TeamSeasonArtifactSchema)
    - apps/web/src/components/ribbon/AlgorithmSelect.tsx (useAlgorithmVersion)
    - apps/web/e2e/touch-scroll.spec.ts lines 1-35 (why e2e specs run against the deployed origin, not localhost — R2 CORS does not allow-list localhost)
  </read_first>
  <files>apps/web/src/lib/teamKey.ts, apps/web/src/lib/teamKey.test.ts, apps/web/src/lib/api/team.ts, apps/web/src/routes/team.$teamNumber.tsx, apps/web/src/routeTree.gen.ts, apps/web/e2e/team-page.spec.ts</files>
  <action>
    Wire ONE path from the live R2 bucket to a rendered element on a new route. No tabs yet, no
    event sections, no chart — one path, real error handling on it.

    Create `apps/web/src/lib/teamKey.ts` exporting `TEAM_KEY_PREFIX = "frc"`, `toTeamKey(teamNumber:
    number): string` returning `` `frc${teamNumber}` ``, and `teamNumberFromKey(teamKey: string):
    number` parsing the digits after the prefix and throwing a named error on a key that does not
    match `/^frc\d+$/`. This is the ONLY place in `apps/web` that knows the prefix convention;
    Pitfall 4 in `06-RESEARCH.md` names the repeated inline template literal as the failure mode.

    Create `apps/web/src/lib/api/team.ts` mirroring `teams.ts` verbatim in shape: interface
    `FetchTeamArtifactParams { teamKey: string; year: number; algorithmId: string; version: string }`,
    `async function fetchTeamArtifact(params)` that calls `artifactKey({ page: "team", teamKey,
    year, algorithmId, version })`, `fetch(artifactUrl(key))`, throws `ArtifactFetchError("team",
    year, res.status)` on a non-ok response, parses through `TeamSeasonArtifactSchema.parse`, calls
    `markArtifactParsed()` on success, and rethrows a parse failure as `ArtifactValidationError("team",
    year, err)`. Export `teamQueryOptions(params)` with `queryKey: ["team", teamKey, year,
    algorithmId, version] as const`. Import `pageArtifacts.js` by deep relative path with an explicit
    `.js` extension at the same depth `teams.ts` uses — there is no workspace alias in this repo.

    Create `apps/web/src/routes/team.$teamNumber.tsx` with `createFileRoute("/team/$teamNumber")`.
    For this task only, use `validateSearch: RootSearchSchema` (year + algorithm); Task 2 replaces it
    with `TeamSearchSchema`. Read `Route.useParams().teamNumber`, coerce with `Number.parseInt(_, 10)`,
    and when it is not a positive integer render the D-19-shaped "not a team number" message rather
    than firing a fetch. Resolve the algorithm version with `useAlgorithmVersion(algorithm)` and keep
    the query `enabled: version !== undefined`, exactly as `teams.tsx` does. Use
    `placeholderData: keepPreviousData`. Render, at this stage, only: the nickname (falling back to
    `Team {teamNumber}` when empty), the team number, and the season record formatted
    `{wins}-{losses}-{ties}`, plus the pending and error branches reusing `Skeletons`/`StateViews`.

    Let the router plugin regenerate `apps/web/src/routeTree.gen.ts` from the build; commit the
    regenerated file rather than hand-editing it.

    Create `apps/web/e2e/team-page.spec.ts` following `touch-scroll.spec.ts`'s own header rule:
    it targets the deployed `baseURL` from `playwright.config.ts`, because the R2 CORS policy does
    not allow-list localhost and there is nothing to render without the real artifact. Assert that
    `/team/1114?year=2024&algorithm=sigma1` renders a non-empty nickname and a record matching
    `/^\d+-\d+-\d+$/`.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck &amp;&amp; pnpm --filter web test -- teamKey</automated>
    <automated>pnpm --filter web test:e2e -- team-page</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/teamKey.ts` exports `toTeamKey` and `teamNumberFromKey`; `teamKey.test.ts` asserts `toTeamKey(1114) === "frc1114"`, `teamNumberFromKey("frc1114") === 1114`, and that `teamNumberFromKey("1114")` throws.
    - The prefix literal is interpolated in exactly one module: `grep -rl 'frc\${' apps/web/src | grep -v 'lib/teamKey.ts' | wc -l` equals 0.
<!-- planner-discipline-allow: frc${ -->
    - `apps/web/src/lib/api/team.ts` exports `fetchTeamArtifact` and `teamQueryOptions`, and `grep -q 'TeamSeasonArtifactSchema.parse' apps/web/src/lib/api/team.ts` succeeds.
    - `apps/web/src/routeTree.gen.ts` contains the string `/team/$teamNumber`.
    - `pnpm --filter web test:e2e -- team-page` passes against the deployed origin, proving a real published `v1/team/frc1114/2024/sigma1@{version}.json` was fetched, parsed and rendered.
    - Visiting `/team/notateam` renders a message and fires no artifact fetch (asserted by the absence of a `team` query key in the rendered error branch's test).
  </acceptance_criteria>
  <done>`/team/1114?year=2024&amp;algorithm=sigma1` renders a real team's nickname, number and record from the live bucket, and the e2e spec proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Tab shell as a typed URL search param, with the composition seams later plans fill</name>
  <read_first>
    - apps/web/src/lib/searchParams.ts (RootSearchSchema, TeamsSearchSchema, the .catch() convention and the "do not add a redundant view field" note)
    - apps/web/src/routes/team.$teamNumber.tsx (as written by Task 1)
    - apps/web/components.json (the shadcn install this repo already has — do not re-init)
    - .planning/phases/06-team-pages/06-UI-SPEC.md sections "Design System" and "UI Considerations" E8
  </read_first>
  <files>apps/web/src/lib/searchParams.ts, apps/web/src/lib/searchParams.test.ts, apps/web/src/components/ui/tabs.tsx, apps/web/src/styles/theme.css, apps/web/src/routes/team.$teamNumber.tsx, apps/web/src/components/team/OverviewTab.tsx, apps/web/src/components/team/SeasonHeader.tsx, apps/web/src/components/team/EventSectionList.tsx</files>
  <action>
    Add `tabs` to the existing shadcn install with `npx shadcn@latest add tabs` run from `apps/web`.
    Do not re-run `shadcn init` — `apps/web/components.json` already exists with the locked preset.

    Define the alliance colour tokens in `apps/web/src/styles/theme.css` now, in the same block style
    the existing colour tokens use, with the exact values the sketch findings record: alliance red
    `#DC2626` and its 30%-alpha soft companion, alliance blue `#2563EB` and its 30%-alpha soft
    companion, and a loser-ink value of `#94A3B8`. Add a comment recording that this pair is already
    validated for colour-vision deficiency at delta-E 29.9 protan and is safe as-is — it is FRC
    domain vocabulary, not a palette choice, and must not be adjusted to match the site's accent.
    They are defined here, in this plan, because plan 06-08 consumes them in the same wave as plan
    06-07's own edit to this file; defining both blocks in wave 4 would put two plans in one file.
    Nothing in this plan renders them yet.

    In `apps/web/src/lib/searchParams.ts`, add `export const TEAM_TABS = ["overview", "history"] as
    const;` and `export const TeamSearchSchema = RootSearchSchema.extend({ tab:
    z.enum(TEAM_TABS).catch("overview") });` plus `export type TeamSearch = z.infer<typeof
    TeamSearchSchema>;`. Per D-16 the delta from `RootSearchSchema` is exactly one `tab` field — do
    not extend `sort`/`sortDir` in, the team page has no sortable table.

    Point the route's `validateSearch` at `TeamSearchSchema`. Render a shadcn `Tabs` whose `value`
    is the `tab` search param and whose `onValueChange` calls `Route.useNavigate()` with the updater
    form so `year` and `algorithm` survive. Tab labels are exactly `Overview` and `Metric History`.
    Both triggers render from first paint regardless of query state (E8) and both meet the inherited
    44x44px minimum tap target. The active tab indicator uses `var(--color-accent)`.

    Create `apps/web/src/components/team/OverviewTab.tsx` exporting `OverviewTab({ artifact,
    algorithmId, season, teamNumber })` where `artifact: TeamSeasonArtifact`. It renders
    `<SeasonHeader artifact={artifact} algorithmId={algorithmId} season={season} teamNumber={teamNumber} />`
    followed by `<EventSectionList artifact={artifact} algorithmId={algorithmId} season={season} teamNumber={teamNumber} />`.
    Export both prop interfaces from their own modules. **These prop contracts are frozen by this
    task** — plans 06-07 and 06-08 fill the two child components without editing `OverviewTab.tsx`,
    which is what keeps them in the same wave.

    Create `SeasonHeader.tsx` and `EventSectionList.tsx` as production-quality minimum
    implementations against those contracts: `SeasonHeader` renders the team number at Display
    28/600, the nickname (falling back to `Team {teamNumber}`) truncated by CSS with a `title`
    attribute, and the record/win-rate strings using local `formatRecord`/`formatWinRate` helpers
    copied from `teams-table/columns.tsx` (copy the two pure functions; do not import across the
    teams-table module boundary). `EventSectionList` renders one heading per entry in
    `artifact.events`, ordered by `startDate` ascending, showing `eventName` and `startDate`,
    truncated with a `title` attribute.

    Render the Metric History panel as a placeholder element with `data-testid="metric-history-panel"`;
    plan 06-05 replaces its body.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck &amp;&amp; pnpm --filter web test -- searchParams</automated>
  </verify>
  <acceptance_criteria>
    - `searchParams.test.ts` asserts `TeamSearchSchema.parse({ tab: "history" }).tab === "history"`, `TeamSearchSchema.parse({ tab: "bogus" }).tab === "overview"`, and `TeamSearchSchema.parse({}).tab === "overview"`.
    - `TeamSearchSchema.parse({ year: "1899", algorithm: "nope" })` yields the `CURRENT_SEASON` year and the `sigma1` algorithm — the inherited `RootSearchSchema` fallbacks still apply.
    - `apps/web/src/components/ui/tabs.tsx` exists and exports `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`.
    - `OverviewTab.tsx` references both `SeasonHeader` and `EventSectionList`, and neither `SeasonHeader.tsx` nor `EventSectionList.tsx` imports the other.
    - A component test renders the route at `?tab=history` and finds `[data-testid="metric-history-panel"]`; at `?tab=` absent it finds the Overview panel.
    - `apps/web/components.json` is unchanged by this task (`git diff --exit-code apps/web/components.json` succeeds).
    - `theme.css` defines exactly five alliance/loser tokens: `grep -c '\-\-alliance-red:\|--alliance-red-soft:\|--alliance-blue:\|--alliance-blue-soft:\|--loser-ink:' apps/web/src/styles/theme.css` returns 5.
    - `grep -c '#DC2626' apps/web/src/styles/theme.css` is at least 1 and `grep -c '#2563EB' apps/web/src/styles/theme.css` is at least 1 — the recorded, CVD-validated FRC values, unadjusted.
  </acceptance_criteria>
  <done>Both tabs render from first paint, the active tab is carried in `?tab=`, back/forward moves between them, and the two composition seams exist with frozen prop contracts.</done>
</task>

<task type="auto">
  <name>Task 3: The page's four non-populated states</name>
  <read_first>
    - apps/web/src/components/StateViews.tsx (EmptyState, ErrorState — the canonical copy and Retry pattern)
    - apps/web/src/components/Skeletons.tsx (SkeletonRows — the family the new shapes join)
    - .planning/phases/06-team-pages/06-UI-SPEC.md "Copywriting Contract" and "UI Considerations" E1/E4/E5
    - apps/web/src/routes/team.$teamNumber.tsx (as written by Task 2)
  </read_first>
  <files>apps/web/src/components/team/TeamStates.tsx, apps/web/src/components/team/TeamStates.test.tsx, apps/web/src/routes/team.$teamNumber.tsx</files>
  <action>
    Create `apps/web/src/components/team/TeamStates.tsx` with two exported components, both taking
    `{ teamNumber, nickname, year, activeYears }`:

    `YearMismatchEmptyState` (D-19) — heading exactly `{Nickname} didn't compete in {year}`, body
    exactly `This team's active seasons: ` followed by one accent-styled link chip per entry in
    `activeYears`, each a router `Link` to `/team/{teamNumber}` preserving the current `algorithm`
    and `tab` search params and setting `year`. When `activeYears` is `undefined` or empty, render
    the heading and omit the body label and the chip row **entirely** — never a dangling label with
    nothing after it (E4 partial). The chip row wraps; the copy is identical for one active year and
    for many (E4 zero-one-many). Nickname falls back to `Team {teamNumber}`.

    `NoEventDataState` (E5 empty) — heading exactly `No event data for {Nickname} in {year} yet.`,
    body exactly `This usually means results haven't published yet. Check back shortly.` No Retry
    button: the fetch succeeded, so retrying returns the same empty artifact. This copy is
    deliberately distinct from D-19's, which would assert the team did not compete when the truth is
    that the data is missing.

    In the route, decide between them explicitly: a 404 from the artifact fetch (or a successful
    fetch whose artifact carries zero events AND whose `year` is not in `artifact.activeYears`)
    routes to `YearMismatchEmptyState`; a successful fetch whose artifact carries zero events but
    whose year IS in `activeYears`, or whose `activeYears` is absent, routes to `NoEventDataState`.
    In both cases the season header still renders above the empty body — the identity chrome is not
    year-scoped.

    Add two skeleton shapes to `Skeletons.tsx`'s family — a header skeleton (name/number placeholder
    plus a square block for the image) and an event-section-card skeleton (header bar plus a few
    skeleton rows) — and render two to three event-section skeletons during the pending state rather
    than one full-page spinner.

    Route the error branch through `StateViews`' `ErrorState` with the resource substituted as
    `team {teamNumber}` and a Retry button calling `refetch()`.
  </action>
  <verify>
    <automated>pnpm --filter web test -- TeamStates</automated>
  </verify>
  <acceptance_criteria>
    - `TeamStates.test.tsx` renders `YearMismatchEmptyState` with `activeYears: [2023, 2024]` and finds exactly two year link chips whose `href` values contain `/team/1114` and the respective years.
    - The same component with `activeYears: undefined` renders the heading and the rendered text does not contain the substring `active seasons`.
    - The same component with `activeYears: [2024]` renders one chip and the same body wording as the many-year case (asserted by comparing the rendered body text against the two-year case with the chip row removed).
    - `NoEventDataState` renders a heading containing `No event data for` and a body containing `haven't published yet`, and the rendered output contains no button element.
    - A route-level test with a mocked 500 response finds the `ErrorState` heading containing `team 1114` and a Retry button.
    - A route-level test in the pending state finds at least two elements with the event-section skeleton test id and zero elements with `role="progressbar"`.
  </acceptance_criteria>
  <done>Loading, error, year-mismatch and zero-events states all render with their specified copy and never with the wrong one of the two empty states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL -> route component | Hand-editable path param (`$teamNumber`) and search params (`tab`, `year`, `algorithm`) cross into render logic |
| R2 origin -> browser | A published JSON artifact of untrusted shape (a bad publish run, a truncated response) crosses into render logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `routes/team.$teamNumber.tsx` search + path params | medium | mitigate | `TeamSearchSchema` gives every field a `.catch()` fallback (Task 2); `$teamNumber` is `Number.parseInt`-coerced and a non-positive-integer result short-circuits to a message before any fetch fires (Task 1) |
| T-06-02 | Tampering / Information Disclosure | `lib/api/team.ts` | medium | mitigate | `TeamSeasonArtifactSchema.parse()` on every fetch; a parse failure raises `ArtifactValidationError` and routes to the error state, never a partial silent render (Task 1) |
| T-06-03 | Information Disclosure | `apps/web` bundle | high | mitigate | No TBA credential or authenticated TBA request path exists in `apps/web` — D-03 resolves the robot photo offline in the pipeline. Enforced by the repo-wide gate in plan 06-03's Task 2 acceptance criteria |
| T-06-07 | Tampering | router `Link` targets in `TeamStates.tsx` | low | accept | Year chip targets are built from `activeYears` integers already validated by the artifact schema and re-validated by the destination route's own `validateSearch` — a bad value can only resolve to a declared valid state |
</threat_model>

<verification>
- `pnpm --filter web typecheck` clean.
- `pnpm --filter web test` green, including the three new test files.
- `pnpm --filter web test:e2e -- team-page` green against the deployed origin.
- `pnpm --filter web build` produces a bundle in which `routeTree.gen.ts` registers `/team/$teamNumber`.
</verification>

<success_criteria>
- A real published team artifact is fetched, parsed and rendered on `/team/{number}` for a real team, proven end-to-end by a Playwright spec.
- `?tab=` is a typed, shareable, back/forward-navigable search param defaulting to Overview.
- All four non-populated page states (loading, error, year-mismatch, zero-events) render with their exact specified copy.
- `SeasonHeader` and `EventSectionList` exist with frozen prop contracts so plans 06-07 and 06-08 can run in the same wave without touching one another's files.
</success_criteria>

<output>
Create `.planning/phases/06-team-pages/06-01-SUMMARY.md` when done
</output>
