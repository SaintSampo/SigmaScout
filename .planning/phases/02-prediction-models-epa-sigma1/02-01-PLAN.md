---
phase: 02-prediction-models-epa-sigma1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/algorithms/types.ts
  - packages/core/algorithms/opr.ts
  - packages/core/algorithms/epa.ts
  - packages/core/algorithms/breakdown/2024.ts
  - packages/core/algorithms/breakdown/index.ts
  - packages/core/scoring/expandingStats.ts
  - packages/corpus/db.ts
  - packages/harness/replay.ts
  - packages/harness/score.ts
  - packages/harness/artifact.ts
  - packages/harness/report.ts
  - packages/harness/cli.ts
  - packages/core/algorithms/epa.test.ts
  - packages/core/scoring/expandingStats.test.ts
  - packages/harness/replay.multiAlgorithm.test.ts
  - packages/harness/replay.test.ts
  - packages/harness/artifact.test.ts
  - packages/harness/score.test.ts
  - packages/harness/report.test.ts
autonomous: false
requirements: [ALGO-02, ALGO-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "score_breakdown_raw travels from the corpus row to an algorithm's update() as MatchResult.scoreBreakdownRaw, and reading it inside predict() throws through the existing leak-proof Proxy (D-22)."
    - "One WalkForwardSimulator.run call drives OPR and EPA over one shared chronological match stream, and both algorithms observe the identical match sequence (D-22)."
    - "artifact.json declares schemaVersion 2, carries an algorithms[] array with one entry per algorithm run, and every slice is tagged with an algorithmId (D-20, D-21)."
    - "Every AlgorithmModule exposes teamMetrics(state, teams?) returning plain data keyed team -> component -> {value, spread?}, with no Node-only import anywhere under packages/core (D-27)."
    - "EPA's win-probability scale comes from an expanding-window standard deviation folded match-by-match, so no statistic computed from a later match can change an earlier match's prediction (D-13, Pitfall EPA-1)."
    - "At an exactly-tied predicted margin (redScore - blueScore == 0) every algorithm emits pRedWin == 0.5 and winner == \"red\", identically and by an asserted rule rather than by accident."
    - statement: "Two matches with identical sort_time are processed in a stable total order (sort_time, event_key, comp-level play order, set_number, match_number) inherited from Phase 1's selectMatchesChronological, which Phase 2 does not redefine."
      verification: backstop
  artifacts:
    - path: packages/core/algorithms/breakdown/index.ts
      provides: "Season -> component-map dispatch table plus the cold-start season parameter (D-02, D-19)"
    - path: packages/core/algorithms/breakdown/2024.ts
      provides: "The 2024 per-season component map, Zod-validated at the parse boundary"
    - path: packages/core/algorithms/epa.ts
      provides: "export const epa: AlgorithmModule<EpaState> (ALGO-02)"
    - path: packages/core/scoring/expandingStats.ts
      provides: "Welford expanding-window mean/variance used as the walk-forward-safe score-SD source"
    - path: packages/harness/replay.multiAlgorithm.test.ts
      provides: "D-22's shared-stream guarantee under test"
  key_links:
    - "packages/corpus/db.ts selectMatchesChronological SELECT -> MatchResult.scoreBreakdownRaw -> breakdown/index.ts parse -> epa.update()"
    - "packages/harness/replay.ts OUTCOME_KEYS -> toLeakProofUpcoming Proxy -> predict() cannot read scoreBreakdownRaw"
    - "packages/harness/score.ts HarnessPredictionInput.algorithmId -> aggregateScores -> ScoreSlice.algorithmId -> artifact.slices[]"
  flagged_assumptions:
    - statement: "$COVERAGE row ALGO-02 is category `unclassified` with no probe question attached, so no edge predicate could be derived for it mechanically. It is carried into this phase unresolved and must be reviewed by a human during phase verification; it is NOT treated as covered by the ALGO-02 truths above."
      status: unresolved
      source: "specless edge probe, ALGO-02 / unclassified"
  prohibitions:
    - statement: "No code, constant, tuned value, or model structure originating from this project's pre-v3 implementations (git tag v2-poc) may be consulted or reintroduced; the only inheritance permitted is REBUILD_SPEC.md's failure log."
      status: unverified
      verification: none
---

<objective>
Prove the entire Phase 2 pipeline end-to-end on one thin path: a 2024 match's `score_breakdown` reaches a real EPA implementation, two algorithms replay one shared stream, and a v2 artifact comes out the far end carrying both algorithms' scores side by side.

Purpose: RESEARCH.md found two structural gaps (score_breakdown is not threaded to algorithms at all; a season-final score SD would be walk-forward leakage) that block BOTH new algorithms equally. Fixing them inside a working vertical slice means the architecture is proven before Sigma1, the four per-season component maps, or cross-season carryover are written on top of it.
Output: The extended `AlgorithmModule` contract (D-27), the corpus-to-algorithm `score_breakdown` plumbing, the 2024 component map, a walk-forward EPA, the multi-algorithm replay loop (D-22), and artifact schema v2 (D-20/D-21).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prediction-models-epa-sigma1/02-CONTEXT.md
@.planning/phases/02-prediction-models-epa-sigma1/02-RESEARCH.md
@.planning/phases/02-prediction-models-epa-sigma1/02-PATTERNS.md
@.planning/phases/02-prediction-models-epa-sigma1/02-VALIDATION.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is **created by Phase 2** and does not exist in the codebase before this phase. Source-grounding / drift checks must treat these as new, not as unresolved references.

**New types and contract members (`packages/core/algorithms/types.ts`)**
`ComponentPrediction`, `TeamMetric`, `TeamMetrics`, `TOTAL_METRIC_KEY`, `SeasonBoundary`, `MatchResult.scoreBreakdownRaw`, `Prediction.redComponents`, `Prediction.blueComponents`, `AlgorithmModule.teamMetrics`, `AlgorithmModule.carrySeason`

**New files (this plan)**
`packages/core/algorithms/epa.ts`, `packages/core/algorithms/breakdown/2024.ts`, `packages/core/algorithms/breakdown/index.ts`, `packages/core/scoring/expandingStats.ts`, `packages/core/algorithms/epa.test.ts`, `packages/core/scoring/expandingStats.test.ts`, `packages/harness/replay.multiAlgorithm.test.ts`

**New symbols (this plan)**
`epa`, `EpaState`, `EPA_NORM_MEAN`, `EPA_NORM_SD`, `EPA_INIT_PENALTY`, `EPA_YEAR_ONE_WEIGHT`, `EPA_MEAN_REVERSION`, `EPA_K`, `epaPercentFunc`, `SeasonComponentMap`, `ParsedComponents`, `parseBreakdown`, `componentMapForSeason`, `COLD_START_SEASON`, `breakdown2024`, `ExpandingStats`, `emptyExpandingStats`, `foldObservation`, `standardDeviation`, `MultiAlgorithmRun`, `MultiAlgorithmPredictionRecord`, `ARTIFACT_SCHEMA_VERSION` (value 2), `ScoreSlice.algorithmId`, `HarnessPredictionInput.algorithmId`, `HarnessPredictionInput.predictedRedScore`, `HarnessPredictionInput.predictedBlueScore`, `HarnessArtifact.algorithms`

**Created by later plans in this phase** (listed here so the whole phase's new surface is in one place)
`breakdown2022`, `breakdown2023`, `breakdown2025`, `breakdown2026`, `FOULS_COMMITTED_COMPONENT`, `distributeResidual`, `FALLBACK_NOISE_MULTIPLIER`, `epaCarryover`, `carryEpaState`, `sigma1`, `makeSigma1`, `Sigma1State`, `TeamComponentBelief`, `applyProcessNoise`, `updateAllianceSum`, `teamTotalVariance`, `allianceTotalPredictiveVariance`, `ewmaCovariance`, `shrinkConsistency`, `WinProbMode`, `winProbability`, `erf`, `normalCdf`, `SIGMA1_*` hyperparameter constants, `PredictionRecordSchema`, `writePredictionsJsonl`, `MetricHistoryRow`, `writeMetricHistoryJsonl`, `identifiability.ts`, CLI flags `--algorithm` (comma list), `--predictions-out`, `--metric-history`, `--cold-start-season`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — one 2024 match's score breakdown reaches EPA, and OPR + EPA emerge in one v2 artifact</name>

  <files>
packages/core/algorithms/types.ts
packages/core/algorithms/opr.ts
packages/core/algorithms/epa.ts
packages/core/algorithms/breakdown/2024.ts
packages/core/algorithms/breakdown/index.ts
packages/core/scoring/expandingStats.ts
packages/corpus/db.ts
packages/harness/replay.ts
packages/harness/score.ts
packages/harness/artifact.ts
packages/harness/report.ts
packages/harness/cli.ts
  </files>

  <read_first>
packages/core/algorithms/types.ts
packages/core/algorithms/opr.ts
packages/corpus/db.ts
packages/harness/replay.ts
packages/harness/score.ts
packages/harness/artifact.ts
packages/harness/report.ts
packages/harness/cli.ts
packages/core/isomorphic.test.ts
packages/core/scoring/brier.ts
.planning/phases/02-prediction-models-epa-sigma1/02-PATTERNS.md
.planning/phases/02-prediction-models-epa-sigma1/02-RESEARCH.md
  </read_first>

  <precondition>`data/corpus.sqlite` exists and contains 2024 matches with `has_score_breakdown = 1` (Phase 1 output; a full-corpus ingest is a prior-phase artifact this task assumes rather than creates).</precondition>

  <reversibility rating="costly">D-20's artifact restructure is the Phase 8 Compare-page contract. Breaking it now costs a re-run; breaking it after Phase 4 publishes costs a migration. Deliberately done now, at its cheapest.</reversibility>

  <action>
This is one vertical slice touching one file per layer. The file count is inherent to a tracer; each individual edit below is small and fully specified. Do them in this order so the tree typechecks at the end, not after every file.

**1. `packages/core/algorithms/types.ts` — contract extension (D-27, D-24).** Preserve the existing file-header comment verbatim; it is the load-bearing "must stay Worker-importable, no Node-only APIs" statement. Add, additively:
- `MatchResult.scoreBreakdownRaw: string | null` — verbatim TBA JSON, or null when TBA omitted it.
- `export interface ComponentPrediction { mean: number; variance?: number }`.
- `Prediction.redComponents?: Record<string, ComponentPrediction>` and `Prediction.blueComponents?: Record<string, ComponentPrediction>` — present only for algorithms that decompose scores.
- `export interface TeamMetric { value: number; spread?: number }` and `export type TeamMetrics = Record<string, Record<string, TeamMetric>>` — outer key is the team key, inner key is the component name. Plain data only.
- `export const TOTAL_METRIC_KEY = "total"` — the one component name every algorithm must include, so a renderer has a headline number regardless of which algorithm is selected.
- `AlgorithmModule.teamMetrics(state: S, teams?: readonly string[]): TeamMetrics` — required, pure, read-only. The optional `teams` filter exists because D-28 snapshots only the 6 teams in a match after every match; a full-state snapshot per match would be O(all teams) and is not acceptable.
- `export interface SeasonBoundary { fromSeason: number; toSeason: number; isColdStart: boolean }` and `AlgorithmModule.carrySeason?(state: S, boundary: SeasonBoundary): S` — declared optional now, implemented by EPA and Sigma1 in later plans (D-16, D-17, D-19). Declaring it here is what lets the season loop be written once.

**2. `packages/core/scoring/expandingStats.ts` — NEW (Pitfall EPA-1).** Implement exactly the module RESEARCH.md § Code Examples specifies: `ExpandingStats { count, mean, m2 }`, `emptyExpandingStats()`, `foldObservation(stats, x)` (Welford), `standardDeviation(stats, fallback)` returning `fallback` when `count < 2`. Match `brier.ts`'s file-header convention: a bulleted list of the boundary contracts the module makes explicit, including why `count < 2` takes a caller-supplied fallback rather than 0 or NaN. Pure functions, no classes, no imports outside the module.

**3. `packages/core/algorithms/breakdown/2024.ts` — NEW (D-02).** Export `breakdown2024: SeasonComponentMap`. The 2024 field inventory was verified directly against `data/corpus.sqlite` during planning; use these exact source keys, all of them alliance-level (never per-robot — see the guard in Task 3):

| canonical component | TBA `score_breakdown` key |
|---|---|
| `autoLeave` | `autoLeavePoints` |
| `autoAmpNote` | `autoAmpNotePoints` |
| `autoSpeakerNote` | `autoSpeakerNotePoints` |
| `teleopAmpNote` | `teleopAmpNotePoints` |
| `teleopSpeakerNote` | `teleopSpeakerNotePoints` |
| `teleopSpeakerNoteAmplified` | `teleopSpeakerNoteAmplifiedPoints` |
| `endGameOnStage` | `endGameOnStagePoints` |
| `endGamePark` | `endGameParkPoints` |
| `endGameHarmony` | `endGameHarmonyPoints` |
| `endGameNoteInTrap` | `endGameNoteInTrapPoints` |
| `endGameSpotLightBonus` | `endGameSpotLightBonusPoints` |
| `adjust` | `adjustPoints` |

Plus the canonical component `foulsCommitted`, which is NOT read from the alliance's own fields: an alliance's own `foulPoints` are the points it *received* from the opponent's fouls, so the quantity D-04 wants — points this alliance handed the opponent — is the **opposing** alliance's `foulPoints` for the same match. Deriving it that way makes it season-agnostic and sidesteps every per-season foul point-value table; record that reasoning inline as a documented divergence from RESEARCH.md's field-aliasing sketch, in `opr.ts`'s deliberate-divergence comment style. The per-season foul *count* fields are still aliased in later plans for the identifiability report only.

Validate at the parse boundary with a Zod schema for the subset of keys this map reads, following `HarnessArtifactSchema`'s validate-then-map pattern. Reject non-finite values explicitly. Never substitute `0` for an absent or malformed field — mirror `opr.ts`'s `?? leagueMeanPerTeamShare` discipline of a documented fallback over a silent zero.

**4. `packages/core/algorithms/breakdown/index.ts` — NEW (D-02, D-19).** Export:
- `SeasonComponentMap` — the interface a per-season module implements: the canonical component names it emits, and a pure `parse(rawBreakdownJson: unknown, side: "red" | "blue"): ParsedComponents` function.
- `ParsedComponents` — `Record<string, number>`, every value finite.
- `parseBreakdown(season: number, scoreBreakdownRaw: string | null, side): ParsedComponents | null` — returns `null` (not an empty object, not zeros) when `scoreBreakdownRaw` is null, so the D-05 fallback path in a later plan is reachable and distinguishable.
- `componentMapForSeason(season: number): SeasonComponentMap` — reads a `Record<number, SeasonComponentMap>` lookup table, and throws for an unmapped season rather than defaulting, in `score.ts`'s `seasonSplit()` style. Adding 2016-2021 later must be a new entry in that record and nothing else (D-19).
- `export const COLD_START_SEASON = 2022` with a doc comment stating it is a parameter that becomes 2016 when the corpus is extended (D-19) — no other module may hardcode 2022.
- Build every `ParsedComponents` object with `Object.create(null)` or an explicit per-map allowlist so a `__proto__` key arriving in third-party TBA JSON cannot reach `Object.prototype` (threat T-02-04).

Register only 2024 in the lookup table for now; the other four seasons land in plan 02-02.

**5. `packages/corpus/db.ts` — surface the raw breakdown.** Add `m.score_breakdown_raw` to `selectMatchesChronological`'s SELECT list and `scoreBreakdownRaw: row.score_breakdown_raw` to its row mapping. The sibling read near line 157 already maps this column, confirming its `string | null` typing. Add `score_breakdown_raw` to the `MatchRow` row type if it is not already present.

**6. `packages/harness/replay.ts` — leak-proof the new field, then drive many algorithms (D-22).** Add `"scoreBreakdownRaw"` to `OUTCOME_KEYS`; it is match-outcome data and without this the Proxy's guarantee has a hole. Then add a multi-algorithm entry point alongside the existing single-algorithm `run<S>` (keep `run` — `replay.season.test.ts` and the single-event CLI path use it):
- `export interface MultiAlgorithmPredictionRecord { match: MatchResult; algorithmId: string; prediction: Prediction }`
- `runAll(algorithms: readonly AlgorithmModule<any>[], teams: readonly string[]): MultiAlgorithmPredictionRecord[]` — `initState` once per algorithm, then a single outer loop over `this.#matches`; for each match, an inner loop over algorithms calling `predict(state, toLeakProofUpcoming(result))`, pushing the record, then `update(state, result)`. Every algorithm must be called through the same `toLeakProofUpcoming` wrapper. One `toLeakProofUpcoming(result)` value per match, shared across the inner loop, so all algorithms provably receive the identical object.
- Optional `onMatchComplete?: (match, algorithmId, state) => void` hook parameter, invoked immediately after each `update` — this is the seam plan 02-05 uses for D-28's metric snapshots. Leave it unused here.

**7. `packages/harness/score.ts` — tag predictions by algorithm (D-21, D-24).** Add to `HarnessPredictionInput`: `algorithmId: string`, `predictedRedScore: number`, `predictedBlueScore: number`. Add `algorithmId: string` to `ScoreSlice`. Change `aggregateScores` to group by `(algorithmId, season, compLevelView)` instead of `(season, compLevelView)`, emitting one slice per combination and preserving the existing exclusion-accounting behavior exactly. Keep the doc-comment style at the top of `HarnessPredictionInput`, describing the new fields' semantics the way `actualWinner: null` is already explained.

**8. `packages/harness/artifact.ts` — schema v2 (D-20, D-21).** Set `ARTIFACT_SCHEMA_VERSION = 2` and update the "Bumped whenever..." comment. Remove `algorithmId`/`algorithmVersion` from `ProvenanceSchema` and add a top-level `algorithms: z.array(z.object({ id: z.string().min(1), version: z.string().min(1) })).min(1)`. Add `algorithmId: z.string().min(1)` to `ScoreSliceSchema`. Change `BuildArtifactParams` to take `algorithms: readonly { id: string; version: string }[]` in place of the two scalar fields. Store raw numbers only — no deltas, no significance tests, no "algorithm X beats Y" field anywhere in the schema (D-21). Update the file-header comment to cite D-20/D-21 alongside the existing D-02/D-03. Keep `writeArtifact`'s validate-then-persist and `secretToScrub` behavior byte-for-byte unchanged.

**9. `packages/harness/report.ts` — keep it compiling and grouped.** It currently reads `artifact.provenance.algorithmId`. Change it to read `artifact.algorithms` and render one score-table section per algorithm id, each headed by that algorithm's id and version, with slices filtered by `slice.algorithmId`. Minimum viable grouping only — the loud D-15 caveat and the head-to-head comparison layout land in plan 02-03.

**10. `packages/core/algorithms/opr.ts` — implement the new contract member (D-27).** Add `teamMetrics(state, teams?)` returning, for each requested team (or every team in `state.ratings` when `teams` is omitted), `{ [TOTAL_METRIC_KEY]: { value: rating } }` with no `spread` — OPR is explicitly the no-variance baseline. Do not touch anything else in this file.

**11. `packages/core/algorithms/epa.ts` — NEW (ALGO-02, D-13, D-08).** Open with a header comment in `opr.ts`'s density: which parts of Statbotics' EPA are ported faithfully and which are deliberate divergences. Export named constants, each with its own reasoning doc comment in `OPR_RIDGE_LAMBDA`'s style: `EPA_NORM_MEAN = 1500`, `EPA_NORM_SD = 250`, `EPA_INIT_PENALTY = 0.2`, `EPA_YEAR_ONE_WEIGHT = 0.7`, `EPA_MEAN_REVERSION = 0.4`, `EPA_K = -5 / 8`. Export `epaPercentFunc(matchCount: number): number` implementing `(2/3) * clamp(0.5 - (0.2/6) * (matchCount - 6), 0.3, 0.5)` — the decaying learning rate, `1/3` at count 0 falling to `0.2` at count 12 and above.

`EpaState` holds: a per-team `Map<string, Record<string, number>>` of component means, a per-team non-elim... no: a per-team match counter that increments on **every** match, an `ExpandingStats` for alliance scores this season, and the season number. Implement:
- `initState(teams)` — every team starts at the season's component-share baseline derived from `EPA_NORM_MEAN - EPA_INIT_PENALTY * EPA_NORM_SD` expressed in this season's point units; before any observation exists there is no season scale yet, so seed from a documented constant and let the first observations correct it.
- `predict(state, match)` — sum each alliance's rating-eligible teams' component means via the **existing exported** `ratingEligibleTeams` from `opr.ts` (import it; do not re-derive surrogate math). Populate `redComponents`/`blueComponents` with `{ mean }` per component. `pRedWin = 1 / (1 + Math.exp(-(redScore - blueScore) / scale))` where `scale = standardDeviation(state.allianceScoreStats, fallback) / (-EPA_K * Math.LN10)` — the natural-exp form algebraically identical to Statbotics' base-10 `10 ** (k * normDiff)`, reusing `logisticWinProbability`'s shape rather than introducing a second logistic base. Tie rule, asserted in Task 3: when `redScore - blueScore` is exactly 0, `pRedWin` is exactly 0.5 and `winner` is `"red"` — matching `opr.ts`'s existing `pRedWin >= 0.5 ? "red" : "blue"` so all algorithms agree at the boundary.
- `update(state, result)` — parse components via `parseBreakdown(state.season, result.scoreBreakdownRaw, side)`. When it returns null, this plan's tracer may skip the state update for that match and must log nothing silently: record the count on the returned state as `fallbackSkipped` so plan 02-02's D-05 proportional-residual fallback has a counter to zero out and a test to flip. Attribute the alliance's observed component vector across its rating-eligible teams, then apply the two-stage EWMA from Statbotics' `add_obs`: `newMean = (1 - percent) * mean + percent * observation`, then `mean = weight * newMean + (1 - weight) * mean`. **Deliberate divergence (D-08):** `weight` is 1 for every match including eliminations, and the match counter increments on every match — Statbotics applies `ELIM_WEIGHT = 1/3` and does not count elims. Document that inline with what Statbotics does, what this project does, and why, in `opr.ts`'s disqualification-comment shape. Also fold both alliances' observed totals into the `ExpandingStats` so the next match's win-probability scale reflects only matches already replayed. Skip Statbotics' per-season post-processing entirely (no 2018 switch/scale sigmoid, no per-year clamps) and say so in the header comment (D-13).
- `teamMetrics(state, teams?)` — per team, one entry per component plus `TOTAL_METRIC_KEY` equal to the component sum. No `spread` — EPA carries a mean only, exactly as Statbotics' `EPARating` does.
- `carrySeason` is deliberately NOT implemented in this plan; plan 02-03 adds it (D-16).

**12. `packages/harness/cli.ts` — run several algorithms in one pass (D-22).** Change `ALGORITHMS` to `{ opr, epa }`. Replace `resolveAlgorithm` with `resolveAlgorithms(spec: string | undefined): AlgorithmModule<any>[]` splitting on `","`, trimming, rejecting duplicates, and reusing the existing `Unknown algorithm: ${name} (known: ${...})` message shape verbatim. Change `runSeason`/`runSeasons`/`runSeasonsMode`/`runEventMode` to take the array and call `simulator.runAll(...)`, emitting one `HarnessPredictionInput` per (match, algorithm) with `algorithmId`, `predictedRedScore`, `predictedBlueScore` populated. Pass `algorithms: algorithms.map(a => ({ id: a.id, version: a.version }))` to `buildArtifact`. Keep the per-season progress line and extend it to report counts per algorithm. Do not add cross-season state threading here — plan 02-03 owns that structural change.
  </action>

  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm vitest run packages/core/isomorphic.test.ts</automated>
  </verify>

  <acceptance_criteria>
- `pnpm typecheck` exits 0.
- `pnpm vitest run packages/core/isomorphic.test.ts` exits 0 — proving `epa.ts`, `breakdown/*`, and `expandingStats.ts` introduced no Node-only or better-sqlite3 import under `packages/core`.
- `grep -n 'scoreBreakdownRaw' packages/harness/replay.ts` shows the identifier inside the `OUTCOME_KEYS` set literal.
- `grep -c 'score_breakdown_raw' packages/corpus/db.ts` returns at least 2 (the pre-existing mapping plus `selectMatchesChronological`'s new one).
- `packages/core/algorithms/epa.ts` contains `export const epa: AlgorithmModule<EpaState>` with `id: "epa"`.
- `packages/harness/artifact.ts` contains `export const ARTIFACT_SCHEMA_VERSION = 2` and a top-level `algorithms: z.array(` entry in `HarnessArtifactSchema`.
- `packages/core/algorithms/types.ts` declares `teamMetrics` as a non-optional member of `AlgorithmModule` (no `?` before its parameter list), and `packages/core/algorithms/opr.ts` implements it.
- `packages/core/algorithms/breakdown/index.ts` exports `COLD_START_SEASON`, and `grep -rn '2022' packages/core/algorithms/ --include=*.ts | grep -v breakdown/index.ts` returns no line where 2022 is used as a cold-start sentinel.
  </acceptance_criteria>

  <done>A 2024 match's `score_breakdown_raw` reaches `epa.update()` through the corpus and the leak-proof replay loop, and `runAll` drives OPR and EPA over one shared stream into a schema-v2 artifact whose slices are tagged by algorithm. The tree typechecks and `packages/core` is still Worker-importable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Inspect the vertical slice and the v2 artifact shape before anything is built on top of them</name>

  <what-built>
Task 1 wired one 2024 match's `score_breakdown` through twelve files — the `AlgorithmModule` contract (D-27), the corpus SELECT, the 2024 component map, a walk-forward EPA, the shared-stream `runAll` loop (D-22), artifact schema v2 (D-20/D-21), and the report/CLI edits that keep the tree compiling. Nothing downstream has been written against it yet.

This gate exists because that slice is the widest single reversal point in Phase 2: schema v2 is the Phase 8 Compare-page contract that Phase 4 will publish against, and a half-applied version of it is expensive to discover later. Inspecting the proven slice before expansion is the *point* of tracer-first, not overhead on it — this task makes that inspection explicit rather than implicit.
  </what-built>

  <how-to-verify>
Automated state to confirm first (the executor should paste the output of each):

1. `pnpm typecheck` exits 0.
2. `pnpm test` exits 0 with no fewer tests than Phase 1's baseline of 116 — no pre-existing test was deleted or skipped to make the refactor pass.
3. `git status --porcelain` shows every file in the plan's `files_modified` Task 1 set touched, and nothing outside it. A file listed in the tracer that shows no diff means a layer was skipped, not that it needed no change.
4. A single-event smoke run through the multi-algorithm path, using the single-event mode `cli.ts` already supports (`runEventMode` / the `--event` flag) against any 2024 event key present in `data/corpus.sqlite`, with `--algorithm opr,epa`. The executor states the exact invocation and event key it used. This is minutes, not the multi-minute full-season run Task 4 performs.

Then read, with the question "is this the contract Phase 4 and Phase 8 should be handed?":

- `packages/harness/artifact.ts` — `ARTIFACT_SCHEMA_VERSION` is 2, `algorithms[]` is top-level with `{id, version}` per entry, `algorithmId` is on every slice, and `algorithmId`/`algorithmVersion` are gone from `provenance`. Confirm this matches D-20's shape as written, since changing it after Phase 4 publishes costs a migration rather than a re-run.
- The smoke run's `artifact.json` — both `opr` and `epa` appear in `algorithms[]` and in slice `algorithmId`s, and EPA's component values are not all sitting at their cold-start constant (RESEARCH.md Pitfall Harness-1's named warning sign; a wiring gap looks exactly like a working run otherwise).
- `packages/harness/replay.ts` — `runAll` builds one `toLeakProofUpcoming(result)` per match and shares it across the inner algorithm loop, and `"scoreBreakdownRaw"` is in `OUTCOME_KEYS`. These are D-22's identical-inputs guarantee and T-02-02's mitigation; Task 3 tests both, so confirm the shape is right before tests are written to lock it in.
- `packages/core/algorithms/epa.ts` — the D-08 elim divergence and the D-13 skipped-post-processing exclusion are both documented inline with what Statbotics does and why this project differs.

If anything is half-applied, say so rather than approving: this plan's remaining tasks write seven test files and a multi-minute corpus run against whatever shape is approved here.
  </how-to-verify>

  <resume-signal>Type "approved" to proceed to Task 3, or describe what is half-applied, mis-shaped, or missing.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression tests for the slice — leakage, shared-stream order, EPA math, Welford, artifact v2</name>

  <files>
packages/harness/replay.test.ts
packages/harness/replay.multiAlgorithm.test.ts
packages/harness/artifact.test.ts
packages/harness/score.test.ts
packages/harness/report.test.ts
packages/core/algorithms/epa.test.ts
packages/core/scoring/expandingStats.test.ts
  </files>

  <read_first>
packages/harness/replay.test.ts
packages/harness/artifact.test.ts
packages/harness/score.test.ts
packages/harness/report.test.ts
packages/core/algorithms/opr.test.ts
packages/core/algorithms/epa.ts
packages/core/scoring/expandingStats.ts
packages/harness/replay.ts
.planning/phases/02-prediction-models-epa-sigma1/02-VALIDATION.md
  </read_first>

  <behavior>
- Reading `scoreBreakdownRaw` on the object handed to `predict()` throws, with the same message shape as the existing outcome fields.
- `runAll([a, b])` over N matches produces an interleaved call log of the form match1/a, match1/b, match2/a, match2/b — one visit per match, both algorithms seeing the identical `MatchResult` sequence.
- `epaPercentFunc(0)` is 1/3, `epaPercentFunc(6)` is 1/3, `epaPercentFunc(12)` is 0.2, `epaPercentFunc(20)` is 0.2 — a decaying, clamped learning rate.
- EPA's two-stage EWMA reproduces a hand-computed value: starting mean 10, observation 40, percent 1/3, weight 1 gives 20.
- EPA's elim divergence (D-08): an elimination match moves the mean by the same amount as a qualification match with the same observation, and increments the match counter.
- At an exactly-tied predicted margin, both `opr.predict` and `epa.predict` return `pRedWin === 0.5` and `winner === "red"`.
- `foldObservation` reproduces textbook Welford values on a known sequence, and `standardDeviation` on a prefix is unchanged by folding later observations — the leakage regression.
- `standardDeviation` with `count < 2` returns the caller's fallback, not 0 and not NaN.
- `buildArtifact` with two algorithms produces `schemaVersion: 2`, `algorithms.length === 2`, and every slice carrying a non-empty `algorithmId`.
- A season map's parse output never contains a key matching `/Robot[123]$/` — the guard for RESEARCH.md assumption A1.
  </behavior>

  <action>
Extend `replay.test.ts` by adding `scoreBreakdownRaw` to its existing `it.each([...])` outcome-key table, so the new field is proven leak-guarded by exactly the mechanism the other five already are.

Create `replay.multiAlgorithm.test.ts` reusing `replay.test.ts`'s `makeInstrumentedAlgorithm` pattern (a module pushing labelled strings into one shared `log: string[]`). Assert one interleaved log across algorithms, not per-algorithm logs — that is the observable form of D-22's byte-identical-order guarantee. Also assert that the object passed to each algorithm's `predict` for a given match is reference-identical across algorithms.

Create `epa.test.ts` following `opr.test.ts`'s synthetic-fixture convention: build a small deterministic fixture, drive `epa` through it, and assert against hand-computed values. Cover every bullet in the behavior block above. Include an explicit test naming D-08 in its title so the divergence from Statbotics is visible in test output rather than only in a comment. Also assert that the win-probability scale is derived from `standardDeviation(...) / (-EPA_K * Math.LN10)` by checking a known margin maps to a known probability.

Create `expandingStats.test.ts` per the behavior block, including the leakage regression: capture `standardDeviation` after k observations, fold 10 more, and assert the captured value is still what a fresh fold of only the first k produces.

Extend `artifact.test.ts` for schema v2, `score.test.ts` for per-algorithm slice grouping (assert N algorithms x M seasons x 3 views slices), and `report.test.ts` for the per-algorithm sections. Add the `/Robot[123]$/` guard assertion to a new test in `epa.test.ts` or a small `breakdown.test.ts` — a component map that consumes per-robot fields is forbidden this phase because their positional correspondence to `red_teams`/`blue_teams` order is unverified (RESEARCH.md A1).
  </action>

  <verify>
    <automated>pnpm vitest run packages/harness/replay.test.ts packages/harness/replay.multiAlgorithm.test.ts packages/harness/artifact.test.ts packages/harness/score.test.ts packages/harness/report.test.ts packages/core/algorithms/epa.test.ts packages/core/scoring/expandingStats.test.ts</automated>
  </verify>

  <acceptance_criteria>
- The command in `<verify>` exits 0.
- `pnpm test` (full suite) exits 0, with a test count strictly greater than the Phase 1 baseline of 116.
- `packages/harness/replay.test.ts` lists `scoreBreakdownRaw` among its parameterized outcome-key cases.
- `packages/harness/replay.multiAlgorithm.test.ts` exists and contains an assertion on a single shared log array covering both algorithms.
- A test title in `packages/core/algorithms/epa.test.ts` contains the string `D-08`.
- A test asserts a parsed component map contains no key ending in `Robot1`, `Robot2`, or `Robot3`.
  </acceptance_criteria>

  <done>Every structural guarantee the tracer introduced is under an automated test that fails if the guarantee is removed: leak-proofing, shared-stream ordering, EPA's documented math and its D-08 divergence, Welford's no-lookahead property, and artifact v2's shape.</done>
</task>

<task type="auto">
  <name>Task 4: Real single-season head-to-head run proves the pipe on corpus data</name>

  <files>
reports/tracer/artifact.json
reports/tracer/report.html
  </files>

  <read_first>
packages/harness/cli.ts
packages/harness/artifact.ts
.planning/phases/02-prediction-models-epa-sigma1/02-CONTEXT.md
  </read_first>

  <precondition>`data/corpus.sqlite` contains 2024 matches; `reports/` is gitignored (D-26), so these outputs stay out of git.</precondition>

  <action>
Run the harness against the real corpus for one season with both algorithms:

`pnpm harness --season 2024 --algorithm opr,epa --out reports/tracer`

Expect this to take several minutes — OPR's incremental solve was measured at roughly 15-30ms per match at season scale and 2024 has about 22,000 played matches. It is the honest end-to-end proof that the plumbing reaches real data; unit tests against synthetic fixtures cannot detect a wiring gap where every EPA component stays at its cold-start value (RESEARCH.md Pitfall Harness-1's named warning sign).

Then assert the output, with a short throwaway node one-liner reading `reports/tracer/artifact.json`:
- `schemaVersion` is 2
- `algorithms` has exactly 2 entries, ids `opr` and `epa`
- every entry in `slices` has a non-empty `algorithmId`, and both ids appear
- OPR's 2024 combined-view Brier and winner accuracy are within 0.005 of the Phase 1 measured baseline (Brier 0.1687, winner accuracy 0.7501) — this is the regression check proving the multi-algorithm refactor did not change OPR's behavior
- EPA's 2024 combined-view `scoredCount` equals OPR's, and its Brier is a finite number in (0, 0.5)

Do NOT add an acceptance criterion requiring EPA to beat OPR. The phase goal is comparison; improving on the baseline is Phase 3's tuning work, and writing a "must beat" gate here would create pressure to tune against a number this phase is not allowed to tune against.

Record the observed EPA and OPR figures in the plan SUMMARY so plan 02-06's full-range run has a single-season reference point to sanity-check against.
  </action>

  <verify>
    <automated>pnpm harness --season 2024 --algorithm opr,epa --out reports/tracer &amp;&amp; node -e "const a=require('./reports/tracer/artifact.json');const ids=new Set(a.slices.map(s=>s.algorithmId));if(a.schemaVersion!==2)throw new Error('schemaVersion '+a.schemaVersion);if(a.algorithms.length!==2)throw new Error('algorithms '+a.algorithms.length);if(!ids.has('opr')||!ids.has('epa'))throw new Error('slice ids '+[...ids]);console.log('ok',[...ids])"</automated>
  </verify>

  <acceptance_criteria>
- The command in `<verify>` exits 0 and prints `ok`.
- `reports/tracer/artifact.json` exists with `schemaVersion: 2` and `algorithms.length === 2`.
- OPR's 2024 combined slice reports a Brier within 0.005 of 0.1687 and a winner accuracy within 0.005 of 0.7501.
- EPA's 2024 combined slice reports a finite Brier strictly between 0 and 0.5 and a `scoredCount` equal to OPR's for the same slice.
- `reports/tracer/report.html` renders one score-table section per algorithm.
- `git status --porcelain reports/` prints nothing (D-26: these outputs are gitignored).
  </acceptance_criteria>

  <done>A real 2024 replay produced one artifact holding two algorithms' scores side by side, OPR's numbers are unchanged from Phase 1's measured baseline, and EPA's numbers are real rather than cold-start constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBA JSON -> component parser | `score_breakdown_raw` is third-party JSON stored verbatim in Phase 1. This phase's per-season parsers are the first code to read structured fields out of it. |
| `MatchResult` -> `predict()` | The leak-proof Proxy boundary. Outcome-bearing data must not cross it. |
| in-memory artifact -> disk | `writeArtifact` is the only sanctioned persistence path and already carries a secret-scrub check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `breakdown/2024.ts` parse boundary | high | mitigate | Zod-validate the read subset of `score_breakdown`; assert every emitted component value is finite before it can enter EPA's EWMA state; throw rather than coerce absent/malformed fields to 0 (ASVS V5) |
| T-02-02 | Tampering | `replay.ts` `OUTCOME_KEYS` | high | mitigate | Add `scoreBreakdownRaw` to `OUTCOME_KEYS` in the same commit that adds it to `MatchResult`, with a parameterized test case in `replay.test.ts` proving the Proxy throws for it |
| T-02-04 | Tampering | `breakdown/index.ts` `ParsedComponents` construction | medium | mitigate | Build every component record with `Object.create(null)` or a fixed per-season allowlist so a `__proto__` key in third-party TBA JSON cannot reach `Object.prototype` |
| T-02-05 | Information Disclosure | `writeArtifact` / `report.ts` | medium | mitigate | Leave `writeArtifact`'s `secretToScrub` check and `runEventMode`'s HTML secret check untouched; the v2 schema restructure must not bypass either |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages are introduced this phase (RESEARCH.md § Package Legitimacy Audit: not applicable). Every dependency used — `ml-matrix`, `zod`, `better-sqlite3`, `vitest` — was audited in Phase 1. Accepted because the attack surface is unchanged from an already-audited state; the disposition flips back to `mitigate` the moment a task adds a package |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm test` exits 0 with more than 116 tests.
- `pnpm harness --season 2024 --algorithm opr,epa --out reports/tracer` completes and produces a schema-v2 artifact containing both algorithms.
- `packages/core` remains free of Node-only imports (`isomorphic.test.ts` green).
- OPR's 2024 numbers match Phase 1's measured baseline within 0.005, proving the refactor was behavior-preserving for the baseline.
</verification>

<success_criteria>
The `score_breakdown` plumbing gap and the walk-forward score-SD gap RESEARCH.md identified are both closed, the `AlgorithmModule` contract carries `teamMetrics` and component predictions, the harness runs many algorithms over one shared stream into a v2 artifact, and a real 2024 replay proves all of it on corpus data.
</success_criteria>

<output>
Create `.planning/phases/02-prediction-models-epa-sigma1/02-01-SUMMARY.md` when done, recording the observed OPR and EPA 2024 figures and any place the verified 2024 field list did not reconcile.
</output>
