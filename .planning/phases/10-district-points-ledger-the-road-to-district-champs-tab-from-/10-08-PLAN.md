---
phase: 10-district-points-ledger-the-road-to-district-champs-tab-from-
plan: 08
type: execute
wave: 4
depends_on: ["10-02", "10-05", "10-06", "10-07"]
files_modified:
  - apps/web/src/components/methodology/districtLedgerContent.ts
  - apps/web/src/components/methodology/districtLedgerContent.test.ts
  - apps/web/src/components/methodology/DistrictPointsPage.tsx
  - apps/web/src/components/methodology/methodologyCardData.ts
  - apps/web/src/components/methodology/methodologyCardData.test.ts
  - apps/web/src/components/methodology/MethodologyCards.tsx
  - apps/web/src/components/methodology/awardsContent.ts
  - apps/web/src/components/methodology/awardsContent.test.ts
  - apps/web/src/routes/methodology.district-points.tsx
  - apps/web/src/routes/methodology.district-points.test.tsx
  - apps/web/src/routes/methodology.awards.test.tsx
  - docs/worker-operations.md
  - docs/publish-budget.md
  - docs/simulation-architecture.md
  - apps/web/e2e/districts-ledger.spec.ts
  - apps/web/playwright.config.ts
autonomous: true
requirements: [SC-6, SC-7]

estimate:
  tokens: 75000
  raw_tokens: 150000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "SC-6's last clause is discharged: the selection model's measured pick-order agreement and the award base-rate tables are STATED ON A PUBLISHED METHODOLOGY PAGE in Jacob's voice, not left in a SUMMARY. `/methodology/district-points` states how each of the four district point categories is predicted, the alliance selection model with the progressive captain rule and the serpentine draft order, the measured pick-order agreement against its own no-information floor, the measured gap between the browser's alliance pricer and the site's own `pRedWin`, and what the model does not cover. The award base-rate table itself lands on `/methodology/awards`, which is where 10-CONTEXT.md's locked awards decision says it goes."
    - "EVERY number on both pages traces to committed code, and the plan names the five sources: `pnpm measure:alliance-win-probability`, `pnpm measure:district-award-base-rates` and `pnpm measure:selection-agreement` (10-02, whose recorded constants are exported beside each script), plus `npx vitest run packages/core/districts/pointFormulas.reconciliation.test.ts` (10-01) and `npx vitest run packages/core/districts/selectionModel.reconciliation.test.ts` (10-04). A figure that appears in none of those five is not written. Numbers are FILLED FROM the sibling SUMMARYs and the committed constants at execution time, never invented, never rounded into a nicer shape, and never softened in direction."
    - "The voice gate runs at RUNTIME over the exported string VALUES, exactly as `awardsContent.test.ts` does and for the same stated reason (this module's own doc comments legitimately use dashes): zero hyphen minus, zero en dash, zero em dash, zero U+00B1, no retired vocabulary, no singular first person, at most three sentences per paragraph. Structure is pinned BY EQUALITY against a hand typed id array, never by iterating the exported constant (project memory `project_iteration_list_trap`)."
    - "`AWARDS_LEAD`'s claim that the site shows no award predictions is RETIRED, and so is the test assertion that pinned it. The district ledger prints a chance of award points and a typical amount per team per event, so that sentence becomes false the moment 10-07 ships. A published page asserting something the site contradicts is this project's own named failure mode."
    - "The three runbooks stop describing a system that no longer exists. `docs/worker-operations.md` gains the district refresh pass, its four `TickResult` fields in the one structured tick line, a symptom row, and the recorded freshness limit (awards that post after every member event's live window has closed wait for the next offline republish). `docs/simulation-architecture.md`'s section 1 stops describing a single simulating tab and an engine count this phase made wrong (the exact retired phrases are literals in Task 3's acceptance criteria, deliberately not repeated elsewhere). `docs/publish-budget.md`'s live-writer table gains the district object. Before this plan, the three docs contain the word district zero times between them."
    - "A live e2e spec covering the Road to District Champs tab at 1440x900 and at 390px EXISTS, is matched by the two DEPLOYED-origin Playwright projects, and is NOT RUN by this plan. Executor subagents have no network (project memory `project_subagent_network_block`); 10-09 runs it from the main context after the deploy and the republish. Every selector in it is read out of 10-07's shipped source and paired with a grep, never invented (the `e2e/support/simulation.ts` rule)."
  artifacts:
    - "apps/web/src/components/methodology/districtLedgerContent.ts (the six sections, content-as-data, the single source of every string the page renders)"
    - "apps/web/src/components/methodology/districtLedgerContent.test.ts (structure by equality, runtime voice gate, required-figure pins)"
    - "apps/web/src/components/methodology/DistrictPointsPage.tsx (the renderer, mirroring AwardsPage.tsx)"
    - "apps/web/src/routes/methodology.district-points.tsx and its route test (the rendered-DOM dash gate lives here)"
    - "apps/web/src/components/methodology/methodologyCardData.ts + MethodologyCards.tsx + methodologyCardData.test.ts (the sixth hub card)"
    - "apps/web/src/components/methodology/awardsContent.ts + awardsContent.test.ts (the district award base-rate section, and the retired lead)"
    - "docs/worker-operations.md, docs/simulation-architecture.md, docs/publish-budget.md (the three runbook updates)"
    - "apps/web/e2e/districts-ledger.spec.ts + playwright.config.ts registration (written here, run by 10-09)"
  key_links:
    - "`METHODOLOGY_CARDS` -> `MethodologyCards.tsx`'s POSITIONAL destructure -> `/methodology/district-points`. The destructure is load bearing: a sixth descriptor with no sixth `<Link>` renders nothing and every existing test stays green. `methodologyCardData.test.ts` pins the order by equality and `methodology.index.test.tsx` counts the rendered links against `METHODOLOGY_CARDS.length`."
    - "`districtLedgerContent.ts`'s figures -> 10-02's exported recorded constants and 10-01's / 10-04's reconciliation-test assertions. This is the link the project's failure log exists about: the README that described a deleted model. The figure pin list is what turns a drifted number into a red test instead of a stale true-looking sentence."
    - "`districtLedgerContent.ts`'s limits section -> 10-04's and 10-06's recorded module headers. Seven limitations are recorded in code across two plans; the page is where a reader meets them."
    - "`playwright.config.ts`'s `desktop` and `phone-390` `testMatch` -> `e2e/districts-ledger.spec.ts`. A spec file that no project matches is collected by nothing and runs never, which is the silent-failure shape project memory `project_e2e_live_only_drift` records (40 specs failing unnoticed)."
    - "`docs/worker-operations.md`'s tick line -> 10-05's four `TickResult` fields (`districtsConsidered`, `districtsRefreshed`, `districtsUnchanged`, `districtsFailed`). An operator reads the tail against the doc; a field the doc does not name is a field nobody watches."
---

<objective>
The words and the proof. Phase 10 measured four things and shipped a tab; this plan is where the
measurements become published prose, the runbooks stop describing the old system, and the new tab
gets a deployed-origin e2e spec.

Purpose (SC-6, SC-7): SC-6's final clause is "measured walk-forward on the corpus, pinned by tests,
and stated on the methodology page in its voice" — the first two halves are 10-02's and 10-04's, the
third is this plan's and only this plan's. SC-7 requires that "the live e2e spec covers the new tab";
this plan writes it, 10-09 runs it.

Output: the artifacts in `must_haves.artifacts`. Nothing here deploys, publishes, pushes or fetches.
The e2e spec is written and registered, never executed (executor subagents have no network).

**The numbers are FILLED, not authored.** Every figure on both methodology pages is copied at
execution time out of a committed constant or a sibling SUMMARY section named in Task 1's and Task
2's `<read_first>`. If a figure the copy needs is absent from all five named sources, the executor
STOPS and says which figure and which source it expected, rather than writing a plausible number.
A number that lives only in a transcript does not exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md

@.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-CONTEXT.md
@.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-PATTERNS.md
@.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-VALIDATION.md
@.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-PLAN-OUTLINE.md
</context>

<sibling_summaries_are_the_source_of_every_number>
This plan runs in wave 4. Four SUMMARY files exist by then and they are the authority, alongside the
committed constants the same plans wrote. Read the NAMED SECTIONS only; none of these files needs a
full read.

| Figure on the page | Source of record | Cross-check |
|---|---|---|
| mean / median / p90 absolute gap against `pRedWin`, both Brier figures, the `coin` and `sign-only` floors, the winner-disagreement rate | `10-02-SUMMARY.md` section `## The win-probability gap` | the recorded constants exported from `scripts/measureAllianceWinProbability.ts`, beside `MEASURED_COMMAND` and `MEASURED_WINDOW` |
| award base rates per decoration bucket crossed with rookie status, the registered season set, the per-cell `n`, every CANNOT BE SCORED cell | `10-02-SUMMARY.md` section `## Award base-rate tables` | the committed table literals in `packages/core/districts/awardBaseRates.ts` and `DISTRICT_AWARD_BASE_RATE_SEASONS` |
| pick-order exact agreement per round and pooled, top-3 agreement, the model rank of the real pick, the pooled coin floor | `10-02-SUMMARY.md` section `## Selection agreement` | the recorded constants exported from `scripts/measureSelectionAgreement.ts` |
| every constant's exact generating command, its flags and its date | `10-02-SUMMARY.md` section `## Recorded constants and their commands` | the same constants' own `MEASURED_COMMAND` strings |
| the progressive captain rule's slot and event counts, the single named miss, the naive top-eight rule's score, the second-pick rank gradient | `10-04-SUMMARY.md` and the assertions in `packages/core/districts/selectionModel.reconciliation.test.ts` | — |
| the qualification-points formula's exact-match count and the playoff placement table | `10-01-SUMMARY.md` and the assertions in `packages/core/districts/pointFormulas.reconciliation.test.ts` | — |
| the freshness limit's wording and the four `TickResult` field names | `10-05-SUMMARY.md` and `apps/worker/src/districtRefresh.ts`'s module header | — |
| the three bake limitations and the `awardsPosted` residual gap | `10-06-SUMMARY.md` and `scripts/publishDistricts.ts`'s comments | — |

**Do not run the `measure:*` scripts.** They need `data/corpus.sqlite`, which is gitignored and
absent from worktrees, and re-running a measurement to re-derive a number a sibling already committed
is how two slightly different versions of the same figure end up in the repo. Read the constants.

**Halt rule.** If a named SUMMARY section is missing, or a constant the copy needs is absent from the
module that should export it, or the SUMMARY and the constant disagree, stop and report which one.
Do not reconcile them by picking the nicer number.
</sibling_summaries_are_the_source_of_every_number>

<voice_rules_binding_on_every_exported_string>
From `feedback_methodology_copy_voice` (Jacob, sketch 014, reconfirmed by sketches 015 and 016) and
from `awardsContent.ts`'s own header, which is the shipped statement of the same rules:

1. **No dash characters at all.** Not the hyphen minus, not the en dash, not the em dash. Compounds
   go open. Ranges go in words.
2. **Flat, factual, third person, short declarative sentences.** One plain sentence per fact. Drop
   any sentence that explains mechanism or justifies a choice unless a reader would miss the fact
   without it. No singular first person anywhere on a public page.
3. **Numbers and comparisons go in small TABLES**, not in prose and not in cards.
4. **Results before explanation**, and no verdict sentence about which thing is better. The table
   carries the comparison; Jacob deleted the verdict sentence himself last time.
5. **Never a U+00B1 and never a partial variance**, on this page as on the tab.

The gate runs at RUNTIME over the exported string VALUES, never as a grep over the module's source,
because the module's own doc comments legitimately use normal punctuation. This is `awardsContent.ts`
line 23's rule verbatim and the reason its test collects `{ where, text }` records.

Jacob may want to edit this copy himself. The SUMMARY must name the content file by path so he can
open it directly, exactly as `feedback_methodology_copy_voice` asks.
</voice_rules_binding_on_every_exported_string>

<tasks>

<task type="tracer">
  <name>Task 1: End to end "a reader opens the hub, clicks through, and reads one section whose every number traces to committed code" — one section only</name>
  <files>apps/web/src/components/methodology/districtLedgerContent.ts, apps/web/src/components/methodology/districtLedgerContent.test.ts, apps/web/src/components/methodology/DistrictPointsPage.tsx, apps/web/src/routes/methodology.district-points.tsx, apps/web/src/routes/methodology.district-points.test.tsx, apps/web/src/components/methodology/methodologyCardData.ts, apps/web/src/components/methodology/methodologyCardData.test.ts, apps/web/src/components/methodology/MethodologyCards.tsx</files>
  <read_first>
    - `apps/web/src/components/methodology/awardsContent.ts` **lines 1 to 70** — the content-as-data header, the two binding voice rules, the EVERY NUMBER HERE TRACES TO COMMITTED CODE paragraph naming its generating commands, and the `AwardsTable` / `AwardsSection` interfaces. This is the exact analog; the new module is its shape with a different subject.
    - `apps/web/src/components/methodology/awardsContent.test.ts` **in full** — the three describes and their stated reasons: structure by equality against a hand typed literal, voice at runtime over exported VALUES, figures pinned as substrings. Also read its final `it` asserting `AWARDS_LEAD` contains the no-award-predictions claim, because Task 2 retires exactly that assertion and this task must not pre-empt it.
    - `apps/web/src/components/methodology/AwardsPage.tsx` **in full** — the renderer to mirror: `PARAGRAPH_CLASS`, the `AwardsTableBlock` wrapper with its `min-w-0 touch-pan-xy overflow-x-auto overscroll-x-contain` scroll region, the `numeric-cell` versus `whitespace-normal` cell rule, and the `id` per section that makes a section linkable.
    - `apps/web/src/routes/methodology.awards.tsx` **in full** and `apps/web/src/routes/methodology.awards.test.tsx` **in full** — the route shape (`createFileRoute`, the `mx-auto w-full max-w-[1200px]` container, the `h1` from the content module's title constant) and the self contained `createRootRoute` + `RootSearchSchema` + `createMemoryHistory` harness, including its rendered-DOM dash gate over `document.body.textContent`.
    - `apps/web/src/components/methodology/methodologyCardData.ts` **in full** — the `to` union type that must gain the new route path, the ORDER IS LOAD BEARING paragraph, and the regular-expression-metacharacter warning on titles.
    - `apps/web/src/components/methodology/methodologyCardData.test.ts` **in full** — `EXPECTED_CARD_ORDER`, the positional assertions, the unique-`testId` check and the per-card dash gates.
    - `apps/web/src/components/methodology/MethodologyCards.tsx` **in full** — the positional destructure, the `undefined` guard, the `preserveSearch` escape hatch and the grid ladder. Note its header already says SIX cards while destructuring five; the count becomes true again here, and that is worth one line in the header rather than a silent fix.
    - `apps/web/src/routes/methodology.index.test.tsx` **lines 20 to 60** — `escapeRegExp` over card titles and the assertion that the rendered link count equals `METHODOLOGY_CARDS.length`.
    - `10-01-SUMMARY.md` and `packages/core/districts/pointFormulas.reconciliation.test.ts` — the qualification-points exact-match count, the selection point values, and `PLAYOFF_PLACEMENT_POINTS`. Read the test's asserted floors, not its prose.
    - `10-CONTEXT.md` sections "Alliance selection and captains" and "Corrections from planning 10-01" — the corpus-verified point values and the placement-table correction that supersedes the research's per-round annotation.
    - `Skill("sketch-findings-sigmascout")` — the uncertainty-display reference, for the rule that an interval is stated as a range and never as a U+00B1.
    - `C:/Users/Jacob/.claude/projects/c--Users-Jacob-Documents-GitHub-SigmaScout/memory/feedback_methodology_copy_voice.md` **in full** — the voice, the tables-not-prose preference, and the request to name the content file in the hand off.
  </read_first>
  <behavior>
    Tests in `districtLedgerContent.test.ts` (pure, no corpus, no network) and in
    `methodology.district-points.test.tsx` (the route harness). Written before the copy, because a
    voice gate written after the prose gets shaped to fit the prose.

    Structure, in `districtLedgerContent.test.ts`:
    - The exported section id array equals a hand typed literal, by equality, and the rendered
      section array's ids equal the same literal in the same order. Two assertions, not one, exactly
      as `awardsContent.test.ts` splits them.
    - Every section has a non blank heading and at least one paragraph.
    - Every table is rectangular: each row as wide as its head, no blank cell.
    - The page title carries no regular expression metacharacter, because the hub test builds a
      `RegExp` from card titles.

    Voice, at runtime over the collected `{ where, text }` records:
    - No hyphen minus, no en dash, no em dash in any exported string. Three separate assertions so a
      failure names which character.
    - No U+00B1 in any exported string.
    - No retired vocabulary: the deleted internal rating names and the deleted Swing vocabulary are
      each asserted absent as whole words. The exact token list is in this task's
      `<acceptance_criteria>`.
    - No singular first person as a whole word.
    - At most three sentences per paragraph, counted by sentence-terminating periods. This is the
      mechanical proxy for one plain sentence per fact; a four sentence paragraph is the shape Jacob
      cut last time.

    Figures:
    - A hand typed `REQUIRED_FIGURES` list, every entry a substring of the joined page text. For this
      task the list holds only the first section's figures.

    Route level, in `methodology.district-points.test.tsx`:
    - An `h1` carrying the page title renders.
    - The lead paragraph renders.
    - Every section renders a level 2 heading, an anchor `id`, and every one of its paragraphs.
    - Every table renders its caption, its column headers and every cell; the rendered table count
      equals the content module's table count.
    - The rendered DOM carries none of the three dash characters and no U+00B1. This is the gate that
      still holds if someone later adds a label or a caption in JSX rather than in the content module.

    Hub, in `methodologyCardData.test.ts`:
    - `EXPECTED_CARD_ORDER` grows to six entries and is asserted by equality; Acknowledgments stays
      last; the new card's title and blurb carry none of the three dash characters.
  </behavior>
  <action>
Build the thinnest complete path and nothing else: ONE content module with ONE section, ONE renderer,
ONE route, ONE hub card, and the two gates that make the section's numbers and voice provable. The
remaining five sections are Task 2. The slice is production quality, not a stub: real interfaces, the
real voice gate, the real figure pins, the real route.

**1. Write `districtLedgerContent.ts`'s header first, before any prose.** State, in
`awardsContent.ts`'s own header style: that this module is the single source of every string
`/methodology/district-points` renders, table cells included, so the test can pin structure by
equality and gate voice over the values without a second hand typed copy; the two binding voice rules
restated verbatim from the analog; that the gate runs at RUNTIME over exported string VALUES and never
as a source grep, and that this file's own doc comments therefore use normal punctuation; and an EVERY
NUMBER HERE TRACES TO COMMITTED CODE paragraph naming all five generating sources by their exact
commands, with one line per source saying which figures came from it. Record why the module is named
for the ledger while the route is named for the points: `10-PATTERNS.md` and 10-07's own hand off both
name this file, and the route slug names what a reader searches for.

**2. Export the shape.** A page title constant, a lead constant, a `readonly` section id tuple with
`as const` and its derived id type, a table interface with optional caption plus `head` and `rows`,
and a section interface with `id`, `heading`, `paragraphs` and an optional `table`. Deliberately NO
subsection level: the awards page needed one because its results split four ways, and this page's six
sections are each one idea. Say so at the interface rather than leaving a reader to wonder. Declare
all six section ids now, so Task 2 adds bodies rather than reopening the structure, and have the
structure test assert the rendered array equals the declared tuple; a declared id with no section is
a red test, which is the intended pressure on Task 2.

**3. Write the first section only: how the four district point categories work.** Results before
explanation. A lead of one or two sentences saying what the page covers. Then one small table whose
rows are the four categories plus the district championship weight, with columns for the category,
the points, and where the number comes from. The qualification row states the game manual's inverse
error function formula on rank and field size and its exact-match count against the corpus, filled
from 10-01's reconciliation test's asserted floor. The alliance selection row states seventeen minus
the alliance number for a captain and a first pick, the alliance number itself for a second pick, and
zero for a fourth robot, spelled as plain words and figures with no dash character. The playoffs row
states the final placement values from `PLAYOFF_PLACEMENT_POINTS`. The championship row states the
weight `districtTierWeight` applies. Every figure in this table appears in `REQUIRED_FIGURES`.

**4. Write `DistrictPointsPage.tsx`** as `AwardsPage.tsx`'s renderer with the subsection branch
removed: the lead paragraph, then each section as a `<section>` carrying its `id`, an `h2` with the
heading, the paragraphs, and the table block. Lift `AwardsPage.tsx`'s table wrapper and its cell rule
unchanged so the two methodology pages read as one system. Where a text role class sits beside a
colour custom property, write the class list as a plain string rather than passing it through `cn()`
(project memory `project_cn_drops_text_role_classes`). Reuse the existing tokens; add no colour, so
the dataviz palette validator has nothing new to check and the SUMMARY says so rather than running it
against an unchanged palette.

**5. Write the route** `apps/web/src/routes/methodology.district-points.tsx`, mirroring
`methodology.awards.tsx` exactly: `createFileRoute("/methodology/district-points")`, the shared page
container, the `h1` from the title constant, the page component. Its header records that the page
fetches nothing and that its numbers move only when a measurement is rerun and the copy updated with
it. No layout registration is needed; the router plugin derives the tree from the file name and
`methodology.tsx` already nests its children.

**6. Add the sixth hub card.** Widen `methodologyCardData.ts`'s `to` union with the new path, add the
descriptor, and place it directly after the awards card so the two prediction-methodology pages sit
together and Acknowledgments stays last. Title and blurb in the voice, dash free, no regular
expression metacharacter. Then add the sixth `<Link>` to `MethodologyCards.tsx`'s positional
destructure and its `undefined` guard, and correct the header's card count note to match reality.
Leave the grid ladder alone: the header already reasons it out for six cards.

**7. Run the route tree generator before trusting a typecheck.** `apps/web`'s `tsc` reports dozens of
unrelated errors until `routeTree.gen.ts` includes the new route (project memory
`project_pnpm_install_exit1_worktrees`). Build `apps/web` first, then typecheck, and never read the
pre-build error list as a finding.

Commit once at the end of the task.
  </action>
  <acceptance_criteria>
    - `npx vitest run apps/web/src/components/methodology apps/web/src/routes/methodology.district-points.test.tsx apps/web/src/routes/methodology.index.test.tsx` passes from the REPO ROOT, never from `apps/web`.
    - `npm --prefix apps/web run build` succeeds and `npx tsc --noEmit -p apps/web/tsconfig.json` reports no error AFTER that build. The pre-build error list is not reported as a finding.
    - The declared section id tuple holds exactly these six ids, in this order, asserted by equality in the test against a hand typed literal: `how-district-points-work`, `how-open-categories-are-predicted`, `the-alliance-selection-model`, `award-base-rates`, `how-well-the-bracket-pricer-works`, `what-this-does-not-model`.
    - Exactly one of those six sections carries a body in this task (`how-district-points-work`); the other five are declared and bodied in Task 2. The structure test's equality assertion against the rendered array is therefore RED until Task 2 lands, and that is recorded in the commit message rather than worked around by a placeholder paragraph.
    - The runtime voice gate asserts, over every exported string value, the absence of each of: U+002D, U+2013, U+2014, U+00B1, and the whole words `BPR`, `VPR`, `Sigma1`, `Swing`, `spread`, `I`, `my`. <!-- planner-discipline-allow: BPR, VPR, Sigma1, Swing, spread -->
    - The runtime voice gate asserts no paragraph carries more than three sentence-terminating periods.
    - `REQUIRED_FIGURES` is a hand typed array, and every entry is a substring of the joined page text. Every entry traces to `packages/core/districts/pointFormulas.reconciliation.test.ts`'s asserted values or to `10-01-SUMMARY.md`; the test's own comment names that source.
    - The route test's rendered-DOM gate finds none of the three dash characters and no U+00B1 in `document.body.textContent`.
    - `METHODOLOGY_CARDS` has six entries, `EXPECTED_CARD_ORDER` in `methodologyCardData.test.ts` is updated to six by equality, `methodology.index.test.tsx`'s rendered-link count assertion passes unmodified, and the new card sits directly after `/methodology/awards` with `/methodology/acknowledgments` still last.
    - `MethodologyCards.tsx` destructures six descriptors and renders six `<Link>` elements; a grep of that file shows six `data-testid={` occurrences.
    - No file under `apps/web/src/components/districts/` is touched, and `apps/web/src/styles/theme.css` is unchanged: this plan adds no colour.
    - No `.env` was read, printed, copied, hashed or interpolated, and no network request was made.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/web/src/components/methodology apps/web/src/routes/methodology.district-points.test.tsx apps/web/src/routes/methodology.index.test.tsx && npm --prefix apps/web run build && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>A reader on `/methodology` sees a sixth card, clicks it, and lands on a real page that states how the four district point categories are scored, every figure on it traced to a committed reconciliation test, with the voice gated at runtime over the exported values and again over the rendered DOM. The structure test declares all six sections, so the remaining five cannot be quietly dropped.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining five sections, the award base-rate table on the awards page, and the retirement of the awards page's no-predictions claim</name>
  <files>apps/web/src/components/methodology/districtLedgerContent.ts, apps/web/src/components/methodology/districtLedgerContent.test.ts, apps/web/src/components/methodology/awardsContent.ts, apps/web/src/components/methodology/awardsContent.test.ts, apps/web/src/routes/methodology.awards.test.tsx</files>
  <read_first>
    - `10-02-SUMMARY.md` sections `## The win-probability gap`, `## Award base-rate tables`, `## Selection agreement` and `## Recorded constants and their commands` — **these four sections and no others**. 10-02's plan states that this plan may quote only numbers appearing in them.
    - `scripts/measureAllianceWinProbability.ts`, `scripts/measureSelectionAgreement.ts` and `scripts/measureDistrictAwardBaseRates.ts` — the RECORDED CONSTANTS blocks only, plus each `MEASURED_COMMAND` and `MEASURED_WINDOW`. These are the authority; the SUMMARY is the cross-check.
    - `packages/core/districts/awardBaseRates.ts` — `DISTRICT_AWARD_BASE_RATE_SEASONS`, the point support and its stated "fifteen or more" top bucket, the decoration bucket type, the rookie state type including `unknown`, and the three `source` values of the lookup. The published table's shape has to match what the module actually exports.
    - `10-04-SUMMARY.md` and `packages/core/districts/selectionModel.reconciliation.test.ts` — the progressive captain rule's slot and event counts, the single named miss, the naive top-eight rule's score, and the second-pick rank gradient's asserted direction and margin.
    - `10-04-PLAN.md`'s `<corpus_facts_verified_during_planning>` Facts 1 and 2, and its hand off naming the three limitations it records in code.
    - `10-06-PLAN.md`'s hand off naming its three limitations, and `scripts/publishDistricts.ts`'s `awardsPosted` comment stating the residual gap.
    - `10-05-PLAN.md`'s objective paragraph headed "Known freshness limit to record, not to fix", and `apps/worker/src/districtRefresh.ts`'s module header where the same limit is recorded.
    - `10-CONTEXT.md` section "Awards (Jacob): base rates by decoration bucket" — the locked decision that the award base-rate table is stated on the methodology AWARDS page, and that no award prediction ever feeds `locks.ts`.
    - `apps/web/src/components/methodology/awardsContent.ts` **lines 30 to 45 and the `our-results` section** — `AWARDS_LEAD`, the section id tuple, and the shape a new section must match.
    - `apps/web/src/components/methodology/awardsContent.test.ts` **lines 30 to 55 and 180 to 195** — `EXPECTED_SECTION_IDS`, `REQUIRED_FIGURES`, and the final `it` pinning the lead's no-predictions claim.
    - `apps/web/src/components/districts/districtLedgerCopy.ts` — the five status labels, the likely-range wording and the tilde-prefixed conditional wording, so the methodology page describes the vocabulary the tab actually prints rather than a synonym for it.
  </read_first>
  <behavior>
    Assertions added to the two existing content tests; the awards route test adapts by iteration
    except for the one assertion this task replaces.

    In `districtLedgerContent.test.ts`:
    - The structure equality assertion from Task 1 now passes: six declared ids, six bodied sections,
      same order.
    - `REQUIRED_FIGURES` grows to cover every figure the five new sections state. Each entry carries a
      comment naming its generating command. A prose rewrite that drops a sample size turns this red,
      which is the correct outcome.
    - The limits section carries at least seven paragraphs or table rows, one per recorded limitation,
      and a test asserts that count by equality against a hand typed literal so a limitation cannot be
      silently dropped in a later edit.
    - The voice gate and the three-sentence ceiling from Task 1 hold over every new string.

    In `awardsContent.test.ts`:
    - `EXPECTED_SECTION_IDS` gains the new district award base-rate section id, by equality.
    - `REQUIRED_FIGURES` gains every figure the new section states.
    - The assertion pinning the lead's no-predictions claim is REPLACED, not deleted: the new
      assertion pins the rewritten lead's own claim about where award predictions now appear. A
      deleted assertion leaves the sentence unguarded; a replaced one keeps the page honest in the
      other direction.

    In `methodology.awards.test.tsx`:
    - Every existing assertion iterates `AWARDS_SECTIONS` and must pass unmodified with the new
      section present. The rendered table count assertion must still equal the content module's table
      count. If any assertion needed a hand edit, the SUMMARY names it and why.
  </behavior>
  <action>
Fill the five declared sections and the awards-page section. Copy the numbers; author only the
sentences around them. Commit once at the end of the task.

**1. `how-open-categories-are-predicted`.** One table whose rows are the four categories and whose
columns are the category and how it is predicted, plus one or two sentences before it. State that one
run produces a ranking, then the captains and the picks, then the bracket, so a team's four numbers in
a run belong to the same imagined weekend; that each histogram is a marginal of the same runs; that
the event total is the per-run sum and the grand total the exact convolution of the two event totals
plus the rookie bonus and adjustments; and that a finished category shows the points already earned
rather than a prediction. State that an event nobody has played yet is priced by the pipeline before
the season and the browser computes nothing for it. Name the interval convention once: a blue cell
shows a median with a likely range, or a chance with a typical amount, using the wording
`districtLedgerCopy.ts` prints. Do not state a U+00B1 form anywhere, and do not name a spread.

**2. `the-alliance-selection-model`.** Results before explanation. First a table of the measured
agreement: the captain rule's correct slots against its total, the naive top-eight rule's score on the
same events, the pooled and per-round exact pick-order agreement, the top-3 agreement, and the pooled
no-information floor. Then two or three sentences: that the captain at each alliance's turn is the
highest ranked team not yet on an alliance, that the draft runs alliance one through eight and then
eight back down to one, and that picks are taken by SPR order among the teams still available. State
the single missed captain slot and name its event, because 10-04 records it and a stated miss is worth
more than a rounded percentage. If second-pick agreement came out barely above the floor, that
sentence goes on the page unsoftened; 10-02's SUMMARY carries it unsoftened and so does this.

**3. `award-base-rates`.** Short, and pointing outward. State that the award cell is priced from a
table of how often teams in the same position have earned district award points, that the table for
each season is built only from earlier seasons, and that the full table is on the Predicting awards
page. Name the page in words rather than adding a link primitive to a content-as-data module. State
that no award prediction moves a team's status, because a locked status is a guarantee; that sentence
is 10-CONTEXT.md's locked decision made visible to a reader.

**4. `how-well-the-bracket-pricer-works`.** One table: the mean, median and ninetieth percentile
absolute gap against the site's own published win probability, the Brier figure for each arm, the coin
and sign-only floors, and the share of matches where the two disagree about the winner. Then the
honest sentences: that the browser prices an alliance from the two numbers a published event file
already carries per team, that this is an approximation of the number the site publishes rather than
the same calculation, and how large the gap measured. No verdict sentence. The table carries it.

**5. `what-this-does-not-model`.** One row or one sentence per recorded limitation, seven in total,
each traced to the sibling plan that recorded it: alliance declines are not modelled, with the one
missed captain slot as the measured size; the award draw does not depend on how the team did on the
field, because the table covers judged awards only; the district level award table is applied at the
district championship too, and that has not been tested; an unplayed event is priced from the team's
current rating and from generated schedules rather than the real one; every baked number's resolution
is set by its draw count; an event whose awards are posted can still read as open when the award rows
are missing and no team earned award points, so its award cell stays blue; and awards posted after
every event in the district has finished do not appear until the next offline republish. Each stated
plainly, in one sentence, with no justification clause.

**6. The awards page gains the district award base-rate section**, placed after `our-results` so the
page still reads goal, model, results, then the one table the ledger uses. The table's rows are the
decoration buckets crossed with rookie status; its columns are the chance of any award points and the
distribution over point values, with the top bucket stated as fifteen or more. Carry every CANNOT BE
SCORED cell into the page as its own stated absence rather than omitting the row, and state the
registered season set and the per-cell sample sizes. If the rookie crossing left most cells thin, that
finding goes on the page in one sentence; 10-02's plan calls it a finding for this plan, not a reason
to lower a bar.

**7. Retire `AWARDS_LEAD`'s claim and its pin.** The lead currently tells a reader the site shows no
award predictions anywhere. The district ledger prints a chance of award points and a typical amount
per team per event, so that sentence is false the moment 10-07 ships. Rewrite the lead to say where
award predictions now appear and what this page is about, in the same flat voice and the same two
sentence length. Then replace the test assertion that pinned the old claim with one that pins the new
claim's own load-bearing substring. Note in the content module's header that the sentence was retired
by this phase and why, so a reader of the diff does not restore it.

**8. Do not touch `locks.ts`, the districts components, or any published artifact.** This task writes
prose and tests only.
  </action>
  <acceptance_criteria>
    - `npx vitest run apps/web/src/components/methodology apps/web/src/routes/methodology.district-points.test.tsx apps/web/src/routes/methodology.awards.test.tsx` passes from the repo root.
    - `npx tsc --noEmit -p apps/web/tsconfig.json` reports no error.
    - All six declared section ids carry a bodied section; the structure equality assertion added in Task 1 is green.
    - Every figure on both pages appears in exactly one of: `10-02-SUMMARY.md`'s four named sections, the recorded constants of the three `measure:*` scripts, `pointFormulas.reconciliation.test.ts`'s assertions, or `selectionModel.reconciliation.test.ts`'s assertions. The SUMMARY carries a figure-to-source table with one row per figure.
    - `REQUIRED_FIGURES` in `districtLedgerContent.test.ts` and in `awardsContent.test.ts` both grew, each new entry carrying a comment naming its generating command.
    - The limits section carries exactly seven recorded limitations, asserted by equality against a hand typed literal in the test.
    - `EXPECTED_SECTION_IDS` in `awardsContent.test.ts` is updated by equality and the awards route test passes with no hand edit, or the SUMMARY names every hand edit and its reason.
    - `AWARDS_LEAD` no longer carries the substring `does not show award predictions`, and no file under `apps/web/src/` carries that substring either. The replacing assertion pins the new lead's own substring.
    - The runtime voice gate and the three-sentence ceiling pass over every string added by this task, and the awards page's own dash gate still passes.
    - `packages/core/districts/locks.ts`, every file under `apps/web/src/components/districts/`, and every published schema are unchanged, confirmed by `git diff --stat` for this task's commit.
    - No `.env` was read, printed, copied, hashed or interpolated, and no network request or `measure:*` run was made.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/web/src/components/methodology apps/web/src/routes/methodology.district-points.test.tsx apps/web/src/routes/methodology.awards.test.tsx && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Both methodology pages state every measured result this phase produced, in Jacob's voice, with each figure pinned by a substring test and traced in the SUMMARY to the one committed command that printed it. The seven limitations are on the page. The awards page no longer claims the site shows no award predictions, and the assertion that pinned that claim was replaced rather than deleted.</done>
</task>

<task type="auto">
  <name>Task 3: The three runbooks stop describing the old system — the district refresh pass, the third browser engine, the fourth live writer, and the recorded freshness limit</name>
  <files>docs/worker-operations.md, docs/simulation-architecture.md, docs/publish-budget.md</files>
  <read_first>
    - `docs/worker-operations.md` **lines 1 to 20, 353 to 420 and 884 to 945** — the opening claim about what the browser reads, the "Live folding tier" section's own convention of dating every change and keeping the superseded paragraph as history, the "Watching it" section's single structured tick line and its explicit note that a field was deleted rather than silently dropped, and the "When something is wrong" symptom table's column shape.
    - `docs/simulation-architecture.md` **lines 1 to 50 and 265 to 347** — the header's stack of dated re-verification notes (the convention: add a dated note, correct the section, label the stale part historical, never overwrite), section 1's engine table, section 5's closure of the browser-versus-server question, and section 6's file map with its closing note that every row was checked against HEAD.
    - `docs/publish-budget.md` **lines 1 to 20 and 74 to 128** — "What this document is" including the rule that the fenced machine-readable block is machine-written and never transcribed by hand, and the live-writer section's per-writer table and its stated basis (driven by matches folded, not by ticks).
    - `10-05-SUMMARY.md` and `apps/worker/src/districtRefresh.ts`'s module header — the pass's position in the tick, the four `TickResult` field names, the per-district try/catch isolation, the two reserved `event_cursor` key shapes, the per-tick cost per live district, and the recorded freshness limit with `LIVE_WINDOW_PAD_MS` as its cause.
    - `10-06-SUMMARY.md` — the district section it filled in `docs/publish-budget.md`, so this task adds the live-writer row without disturbing what 10-06 already measured there.
    - `10-07-SUMMARY.md` — the district simulation Web Worker triad's file names and the ledger component's, for section 6's file map.
    - `10-02-SUMMARY.md` section `## Alliance win-probability API surface` — the new browser-side pricer's module path and signature, for section 6's file map and section 1's engine description.
    - `apps/web/src/components/methodology/districtLedgerContent.ts` as just written — the limits wording, so the runbook and the public page state the freshness limit in compatible words rather than two subtly different ones.
  </read_first>
  <behavior>
    Documentation has no test suite, so the gates are greps and a scoped diff, and they are named in
    `<acceptance_criteria>` rather than left to judgement.

    Positive presence: each of the three docs must mention the district work at all. Before this task
    the three files contain the word district ZERO times between them, which is the measurement that
    makes these greps meaningful rather than decorative.

    Named absence: two sentences that this phase makes false must be gone from
    `docs/simulation-architecture.md`, including from any dated historical note added by this task.
    The retired phrases live in a markdown heading and in body prose, so the usual
    strip-the-comment-lines grep hygiene must NOT be applied here: filtering heading lines would
    defeat the gate. The dated historical note refers to the retired section by its NUMBER, never by
    re-quoting its old heading, so the note cannot re-introduce the phrase it retires.

    Untouched: `docs/publish-budget.md`'s fenced machine-readable `json budget` block is byte
    identical to HEAD, confirmed by a diff scoped to that block. That block is written by
    `pnpm publish:seasons --write-budget` and hand editing it is the one thing that document forbids.
  </behavior>
  <action>
Three files, one commit. Each edit follows the host document's own established convention rather than
a new one.

**1. `docs/worker-operations.md`.** Add a dated section for the district refresh pass, placed with the
other tick-behaviour sections and headed with its phase and date the way every other section there is.
Describe, in an operator's terms and in this order: that a district counts as live when any of its
member events has a live window; that the pass makes one conditional rankings request per live
district per tick and reads the published artifact back from R2 only when something moved; that it
applies the shared merge and recomputes the lock verdicts; that it issues at most one conditional
awards request per live member event once that event's playoffs are done; that every step for one
district sits inside that district's own error isolation so one bad district cannot cost the tick its
other districts or its rotation offset; and that it never simulates. Name the two reserved cursor key
shapes it writes so an operator reading `event_cursor` recognises them.

Then extend "Watching it". Add the four district counts to the structured tick line's sample JSON and
say in one sentence what each counts, following that section's own habit of explaining which field
tells you what. Follow its precedent for a changed line shape: the section already records that a
field was deleted on a date so a reader of an old tail is not confused, so state the date these four
appeared.

Then add one row to the "When something is wrong" table: earned district points not moving during a
live district weekend, its likely causes, and the first thing to check, pointing at the four counts.

Then record the freshness limit in its own short paragraph, in the words the methodology page uses:
awards that post after every member event's live window has closed do not appear until the next
offline republish, because the window is padded a bounded interval past the last observed match. State
it as a known consequence with a named cause, not as a defect.

**2. `docs/simulation-architecture.md`.** Follow the file's own convention: add a dated
re-verification note to the header stack saying what changed and that this is a code read against
HEAD, not a live fetch. Then correct section 1 so its heading and its opening sentence describe the
system as it is: two tabs simulate, three engines exist, and the district ledger's engine is the third.
Extend its table with the district ledger's row: what decides which engine runs, where the draws
happen, and what it costs the visitor, with the zero-compute finished and unstarted cases named. Keep
every historical figure in that section exactly as it is, labelled as the file already labels stale
figures.

Add a short subsection describing the district engine: that it calls the same Monte Carlo core through
the per-draw hook, so a ranking, a draft and a bracket in one draw belong to the same run; and that it
prices an alliance with the new browser-side win-probability function, which is NEW rather than a
revival of the state-block pricer deleted on 2026-09-23, with its measured gap named and the
methodology page named as where that gap is published. That sentence is the one that stops a reader
concluding the browser prices nothing.

Then update section 5's closure with one sentence: a third engine arriving IN THE BROWSER is
consistent with the decision that simulation stays client side, not a reopening of it. Then add the
district rows to section 6's file map and re-state its closing note with this task's date.

**3. `docs/publish-budget.md`.** In the live-writer section, list the district object among the
writers alongside the per-team artifact, the event artifact and the year rebuild, and update that section's
opening sentence so its list of what the tick writes is complete. Add a row to the per-writer table
with its own basis, stated the way the other rows are: driven by what moved, not by ticks, so a
district with nothing new costs nothing. Give the peak-month figure as the plan's own arithmetic from
10-05's recorded per-tick cost, labelled as an estimate on that basis rather than as a measurement,
and say plainly whether it changes the section's total conclusion. Do not touch the fenced
machine-readable block and do not restate 10-06's measured district payload numbers, which already
live in that document's district section.
  </action>
  <acceptance_criteria>
    - `grep -ci district docs/worker-operations.md`, `grep -ci district docs/simulation-architecture.md` and `grep -ci district docs/publish-budget.md` each return a count greater than zero. Recorded in the SUMMARY beside the pre-task count of zero for all three.
    - `grep -c districtsRefreshed docs/worker-operations.md` is greater than zero, and all four of `districtsConsidered`, `districtsRefreshed`, `districtsUnchanged` and `districtsFailed` appear in the structured tick line's sample and in the prose that explains it.
    - `grep -q "two engines" docs/simulation-architecture.md` finds NOTHING, including inside any dated historical note this task adds.
    - `grep -q "one tab and two engines" docs/simulation-architecture.md` finds NOTHING.
    - The dated historical note added to `docs/simulation-architecture.md` refers to the corrected section by its number and does not re-quote its retired heading.
    - `docs/worker-operations.md` carries the freshness limit in one paragraph whose wording is compatible with the same limitation's sentence in `districtLedgerContent.ts`; the SUMMARY quotes both so a reader can compare them.
    - `docs/worker-operations.md`'s "When something is wrong" table gained exactly one row, and that row's first-thing-to-check names the four district counts.
    - `docs/publish-budget.md`'s fenced machine-readable `json budget` block is byte identical to HEAD, confirmed by a diff scoped to that block and quoted as empty in the SUMMARY.
    - `docs/publish-budget.md`'s district payload section, written by 10-06, is unchanged by this task.
    - No source file, test, schema or published artifact is touched: `git diff --name-only` for this task's commit lists only the three paths under `docs/`.
    - No `.env` was read, printed, copied, hashed or interpolated; no credential, token or key value appears in any added sentence; no network request was made.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -ci district docs/worker-operations.md) -gt 0 && test $(grep -ci district docs/simulation-architecture.md) -gt 0 && test $(grep -ci district docs/publish-budget.md) -gt 0 && test $(grep -c districtsRefreshed docs/worker-operations.md) -gt 0 && test $(grep -c districtsConsidered docs/worker-operations.md) -gt 0 && test $(grep -c districtsUnchanged docs/worker-operations.md) -gt 0 && test $(grep -c districtsFailed docs/worker-operations.md) -gt 0 && ! grep -q "two engines" docs/simulation-architecture.md</automated>
  </verify>
  <done>An operator reading any of the three runbooks meets the district refresh pass, its four log fields, its symptom row and its freshness limit; a reader of the simulation audit meets three engines rather than two and learns that the browser prices alliances again with a measured gap; and the write-volume table counts the fourth live writer. The machine-written budget block and 10-06's measured district numbers are untouched.</done>
</task>

<task type="auto">
  <name>Task 4: The live e2e spec for the Road to District Champs tab at 1440x900 and 390px — written and registered here, run by 10-09</name>
  <files>apps/web/e2e/districts-ledger.spec.ts, apps/web/playwright.config.ts</files>
  <read_first>
    - `apps/web/playwright.config.ts` **in full** — the TWO ORIGINS, TWO FAMILIES OF PROOF header, the CORS constraint that keeps local projects on the preview proxy, the six projects and their `testMatch` regexes, and the `webServer` note that any run also builds the local preview. The new spec belongs to the DEPLOYED family (`desktop` and `phone-390`), because 10-09 runs it after the deploy and the republish against real published bytes.
    - `apps/web/e2e/simulation-tab.spec.ts` **in full** — the closest analog: a per-width pair of tests, constants transcribed by hand with a comment naming the shipped source of each, measured geometry rather than screenshots alone, the U+00B1 absence check over a scroll region's `innerText`, `console.log` lines marked for the SUMMARY's measured-figure obligation, and `testInfo.outputPath` screenshots with their paths printed.
    - `apps/web/e2e/support/simulation.ts` **in full** — the rule this task inherits: every literal is read out of shipped source and paired with a grep proving it appears verbatim in the component that renders it, an e2e spec never imports app source, and a support module carries no `.spec.` segment so no project collects it.
    - `apps/web/e2e/event-scroll-regions.spec.ts` and `apps/web/e2e/support/scrollRegions.ts` — the established way this repo asserts that a region is the only horizontal scroller and that the document never pans. The ledger's sticky first column and in-card horizontal scroll is exactly that shape.
    - `10-07-SUMMARY.md` **in full** — the rendered surface: the renamed tab id and its panel test id, every test id `DistrictLedger.tsx` renders, the status chip labels and their counts, the Rewind to slider's test id and search param, the drawer's trigger, and the named 390px backstop this spec is the real check for.
    - `apps/web/src/routes/districts.tsx` and `apps/web/src/components/districts/DistrictLedger.tsx` as 10-07 left them — the source of every selector. Grep each literal out of these files; do not transcribe one from the SUMMARY without the grep.
    - `apps/web/src/lib/searchParams.ts` — the district tab enum after 10-07's rename, the `catch` fallback that makes a pre-rename `?tab=` land on the same panel, and the rewind position param's name.
    - `data/fixtures/phase10/district-2026pnw.json` and `data/fixtures/phase10/districts-index-2026.json` — the published artifact and index this spec's pinned counts come from. Derive the counts from these files with a small throwaway node one liner; do not take them from this plan's prose.
    - `C:/Users/Jacob/.claude/projects/c--Users-Jacob-Documents-GitHub-SigmaScout/memory/project_e2e_live_only_drift.md` **in full** — why these specs drift, why they must be rerun after UI changes from the main context, and the 40-silent-failure incident that makes project registration load bearing.
  </read_first>
  <behavior>
    The spec is not run by this task. Its gate is collection: Playwright lists it, under both deployed
    projects, with the test titles this plan names. Collection proves the file compiles, that the two
    `testMatch` regexes match it, and that the titles are what 10-09 will see.

    The spec's own tests, all against the deployed origin at `2026pnw`, a FINISHED district whose
    numbers cannot move:

    - **1440x900.** The Road to District Champs panel is the default panel with no `?tab=`. The ledger
      table renders. The five status chips render, each with a count. The counts sum to the district's
      roster. Prequalified plus Locked equals the district championship slot count, Locked out equals
      the artifact's own eliminated count, and In range and Out of range are both zero, because a
      finished district has no open category and every team's status is decided. No element inside the
      table's scroll region prints a U+00B1. A full page screenshot is written and its path printed.
    - **390x844.** The same panel renders. The table's own scroll region is the only horizontal
      scroller: the document's `scrollWidth` does not exceed its `clientWidth`, while the table
      region's `scrollWidth` may exceed its own `clientWidth`, and a region that does not overflow
      proves nothing, so the premise is asserted before the conclusion. The first column stays at the
      same viewport x after the region is scrolled right. A screenshot is written and its path printed.
    - **The slider.** The Rewind to control renders, its maximum spans the district's interleaved
      timeline, moving it writes a shareable search param, and reloading that URL lands on the same
      position.
    - **The rename's durability.** A URL carrying the PRE-RENAME first tab id lands on the Road to
      District Champs panel, because the tab schema falls an unrecognised id back to the default and
      the renamed tab is the default.
    - **The scoped-removal control.** The Champ Locks panel still renders the shipped champ table with
      its own header stats card and its own per-event columns toggle, asserted positively by their own
      test ids, so this phase's district-tier removal is provably scoped.

    Every pinned count carries a comment naming the artifact field it was derived from, plus the
    sentence that a failure on it means the verdicts moved and is a finding, not spec drift to soften.
  </behavior>
  <action>
Write one spec and register it. Commit once.

**1. Write the spec's header first.** State that it runs against the DEPLOYED origin only and is
therefore run by the phase's operator plan after the deploy and the republish, never by an executor
(no network in an executor sandbox); that every selector in it was grepped out of shipped source and
the grep output is in the plan's SUMMARY; that it imports no app source, following this suite's own
precedent; and that the district it targets is finished, so its pinned counts are stable facts rather
than a moment in a live weekend.

**2. Pin the constants at the top**, each with a comment naming where it came from: the district key,
the roster size, the district championship slot count, the locked total and the locked-out total, each
derived from the published artifact and index in `data/fixtures/phase10/`. Add the reasoning that on a
finished district the locked total equals the slot count, so the two pins check each other. Add the
tab id and every test id as a frozen record of literals, in `e2e/support/simulation.ts`'s style, each
with a comment naming the exported constant in shipped source it mirrors.

**3. Write the five tests** exactly as `<behavior>` describes them, at the two widths, with the
premise-before-conclusion discipline `no-page-pan.spec.ts` established: a scroll region that does not
overflow is not evidence, so assert the overflow premise unconditionally before asserting the
arbitration. Print each measured figure with a `console.log` marked for the SUMMARY, and write a full
page screenshot per width with `testInfo.outputPath`, printing the path so a human can look at it.
Give the desktop test a generous timeout: the ledger's rows are many and the page polls.

**4. Register the spec on both deployed projects.** Extend the `desktop` project's `testMatch` and the
`phone-390` project's `testMatch` with the new file, and add one line to each project's comment saying
why the spec belongs at that width. Do NOT add it to any `local-*` project: the local preview reaches
artifacts through the preview proxy, and the point of this spec is the deployed page reading the
republished district artifact. A spec no project matches runs never, which is the failure this repo has
already had once at scale.

**5. Do not run it, and do not run the full Playwright suite.** The only gate here is collection.
State in the SUMMARY that the spec's first real execution is 10-09's, and name it as an unproven gate
rather than a passing one.
  </action>
  <acceptance_criteria>
    - `npm --prefix apps/web exec -- playwright test --list e2e/districts-ledger.spec.ts --project=desktop --project=phone-390` lists the spec under BOTH projects and prints exactly the five test titles this plan's `<behavior>` describes. The full `--list` output is in the SUMMARY.
    - If `--list` cannot run in the executor's environment, the SUMMARY says so explicitly, names the error, and records the collection gate as UNVERIFIED with 10-09's run as its first proof. It is never reported as passing.
    - Every selector literal in the spec is paired with a grep against the file that renders it, and every grep's output line is in the SUMMARY. A selector with no grep is not shipped.
    - Every pinned count is derived from `data/fixtures/phase10/district-2026pnw.json` or `districts-index-2026.json` by a command whose output is in the SUMMARY, and each carries a comment naming its source field.
    - The spec imports nothing from `apps/web/src`.
    - `apps/web/playwright.config.ts`'s `desktop` and `phone-390` `testMatch` both match the new file, and no `local-*` project does. Confirmed by `--list` under one local project returning no test from this file.
    - The spec asserts the U+00B1 absence over the ledger region's text, and asserts the document's horizontal non-overflow at 390px only after asserting the region's own overflow premise.
    - `npx vitest run` and both tsconfigs are untouched by this task's change, and `git diff --name-only` for this commit lists only the two paths under `apps/web/`.
    - No `.env` was read, printed, copied, hashed or interpolated, and no network request was made: the spec was written, not executed.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix apps/web exec -- playwright test --list e2e/districts-ledger.spec.ts --project=desktop --project=phone-390</automated>
  </verify>
  <done>A deployed-origin spec covering the Road to District Champs tab exists at both of this site's named widths, every selector in it grepped out of 10-07's shipped source and every pinned count derived from the published artifact, matched by the two deployed Playwright projects and by no local one, collected without being run. SC-7's e2e clause has its artifact; 10-09 has its command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured result -> published prose | The only boundary this plan crosses. A number leaves a script's output or a test's assertion and becomes a sentence on a public page. Nothing validates prose at runtime, so the boundary is guarded by the figure pins and by the rule that a figure with no named source is not written. |
| a runbook -> an operator's action during a live weekend | An operator reads the doc and acts. A doc that describes a system that no longer exists is worse than no doc, because it is trusted. |
| a spec file -> the Playwright project that collects it | A spec no project matches runs never and reports nothing. The registration is the boundary and it is silent when it fails. |

No untrusted input crosses any boundary in this plan: nothing here parses a payload, reads a
credential, accepts a query parameter or writes an artifact.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08-01 | Repudiation | a stated figure on either methodology page | high | mitigate | This project's failure log is a log of a README describing a deleted model. Every figure is filled from one of five named committed sources, cross-checked against the exported constant beside the script that printed it, pinned as a substring in `REQUIRED_FIGURES` with a comment naming its generating command, and traced row by row in the SUMMARY. A rerun measurement that moves a number turns the pin red, which is the correct outcome. A figure absent from all five sources triggers the halt rule instead of an invented number. |
| T-10-08-02 | Tampering | the voice gate | medium | mitigate | The gate runs at RUNTIME over exported string VALUES, never as a grep over the module's source, so the module's own doc comments cannot satisfy it and cannot break it. Structure is pinned by equality against a hand typed literal, never by iterating the exported constant, because this repo has a recorded iteration-list trap where a test that iterates silently absorbs a new entry. The route test adds a second, independent gate over the rendered DOM, which still holds if a later edit puts a string in JSX instead of the content module. |
| T-10-08-03 | Repudiation | a doc sentence this phase made false | high | mitigate | Two sentences are named as literals in Task 3's acceptance criteria and gated by negative greps; the dated historical note is required to reference the corrected section by number rather than re-quote the retired heading, so the note cannot re-introduce what it retires. The awards page's no-predictions claim is gated by a replaced test assertion rather than a deleted one, so the page stays pinned in the other direction. Positive presence greps prove each of the three docs mentions the district work at all, against a recorded pre-task count of zero. |
| T-10-08-04 | Information Disclosure | a secret reaching a doc or a transcript | high | mitigate | No task reads `.env`, and no task runs a credentialed command: the three `measure:*` scripts are deliberately not run, the publish is 10-09's, and the e2e spec is not executed. Task 3 edits an operations runbook, which is exactly where a credential is most tempting to paste, so every task carries an explicit no-secret-read, no-secret-printed acceptance line and the doc edits name procedures rather than values. This is the boundary CLAUDE.md's secrets section exists about, and the reason it exists is that git-side protections all passed once while a secret escaped into a transcript anyway. |
| T-10-08-05 | Denial of Service (self) | the new e2e spec running where it cannot pass | medium | mitigate | The spec is registered on the two DEPLOYED-origin projects only and never on a `local-*` one, so it cannot be collected by `test:e2e:local`. It is not run by this plan at all (executor subagents have no network), and the collection gate is `--list`, which does not drive a browser. Playwright is not part of CI in this repo, so the spec cannot turn CI red before its first intended run. |
| T-10-08-06 | Spoofing | a spec selector that matches nothing | medium | mitigate | Every selector literal is grepped out of the file that renders it and the grep output goes in the SUMMARY; a selector with no grep is not shipped. This is `e2e/support/simulation.ts`'s own stated rule, and the 40-silent-failure incident in project memory is what it was written for. The scoped-removal control asserts the champ panel's test ids POSITIVELY, so a spec that silently matches nothing on the district tier cannot pass by absence alone. |
| T-10-08-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan. Every task writes prose, tests or a spec file; `files_modified` adds no dependency and touches no manifest's dependency block. 10-RESEARCH.md's Package Legitimacy Audit applies to 10-02's measurement work, not here. The disposition is accept because there is no install to gate. |

Every threat carries a severity and a disposition. `security_enforcement` is on for this phase and
`REVERSIBILITY_GATES` is off; every change here is prose or a new test file behind a `git revert`, so
no task's reversibility rating is worse than reversible and no checkpoint is warranted.
</threat_model>

<verification>
Run from the REPO ROOT, never from `apps/web` (77 files there against 167 at root; an eight day red CI
hid in that gap). Never wrap a run in `timeout <n> pnpm ...`: it swallows output and exits 0.

1. `npx vitest run apps/web/src/components/methodology apps/web/src/routes/methodology.district-points.test.tsx apps/web/src/routes/methodology.awards.test.tsx apps/web/src/routes/methodology.index.test.tsx`
2. `npm --prefix apps/web run build` then `npx tsc --noEmit -p apps/web/tsconfig.json` (the build regenerates the route tree; the pre-build error list is not a finding)
3. `npx tsc --noEmit` at the repo root
4. `npx vitest run` from the repo root, and the collected file count recorded in the SUMMARY beside the baseline
5. Task 3's doc greps, with the pre-task count of zero recorded beside each
6. `npm --prefix apps/web exec -- playwright test --list e2e/districts-ledger.spec.ts --project=desktop --project=phone-390`

Not this plan's: the deploy, the republish, the push, `gh run list`, and the actual Playwright run.
All five are 10-09's checkpoint tasks and every one of them needs network an executor does not have.
</verification>

<success_criteria>
1. `/methodology/district-points` exists, is reachable from a sixth hub card, and carries six sections: how the four categories are scored, how the open ones are predicted, the alliance selection model with its measured agreement, where the award base rates live, how well the bracket pricer works, and what the model does not cover.
2. The award base-rate table is on `/methodology/awards`, which is where 10-CONTEXT.md's locked awards decision puts it, with its registered season set, its per-cell sample sizes and every CANNOT BE SCORED cell stated rather than omitted.
3. Every figure on both pages traces to one of five named committed sources, is pinned as a substring by a test carrying its generating command in a comment, and appears in the SUMMARY's figure-to-source table. No figure was authored.
4. The voice gate runs at runtime over exported string values and finds zero dash characters of any kind, zero U+00B1, no retired vocabulary, no singular first person and no paragraph over three sentences; the route test finds the same over the rendered DOM.
5. Seven recorded limitations are stated on the page, one sentence each, pinned by an equality assertion on their count.
6. `AWARDS_LEAD` no longer claims the site shows no award predictions, and the assertion that pinned that claim was replaced rather than deleted.
7. All three runbooks mention the district work, against a recorded pre-task count of zero; the two named false sentences are gone from the simulation audit including from its own historical note; the machine-written budget block and 10-06's measured district numbers are byte identical.
8. `apps/web/e2e/districts-ledger.spec.ts` exists, is matched by the `desktop` and `phone-390` projects and by no `local-*` project, carries five titled tests across 1440x900 and 390px, has every selector paired with a grep and every count derived from the published artifact, and was collected without being run.
9. The full suite from the repo root shows zero new failures, both tsconfigs are clean, and nothing published, no schema and no district component changed.
</success_criteria>

<artifacts_this_plan_produces>
## New files

| Path | Role |
|---|---|
| `apps/web/src/components/methodology/districtLedgerContent.ts` | content-as-data, six sections, the single source of every string the page renders |
| `apps/web/src/components/methodology/districtLedgerContent.test.ts` | structure by equality, runtime voice gate, figure pins, limitation count |
| `apps/web/src/components/methodology/DistrictPointsPage.tsx` | the renderer, `AwardsPage.tsx`'s shape without the subsection branch |
| `apps/web/src/routes/methodology.district-points.tsx` | the route, `methodology.awards.tsx`'s shape |
| `apps/web/src/routes/methodology.district-points.test.tsx` | the route harness and the rendered-DOM dash and U+00B1 gate |
| `apps/web/e2e/districts-ledger.spec.ts` | the deployed-origin spec, five tests across two widths, written not run |

## Modified files

| Path | Change |
|---|---|
| `methodologyCardData.ts` / `MethodologyCards.tsx` / `methodologyCardData.test.ts` | the sixth card: widened `to` union, a descriptor after the awards card, a sixth positional `<Link>`, `EXPECTED_CARD_ORDER` by equality |
| `awardsContent.ts` / `awardsContent.test.ts` | the district award base-rate section, the retired lead, the replaced lead assertion, grown `EXPECTED_SECTION_IDS` and `REQUIRED_FIGURES` |
| `routes/methodology.awards.test.tsx` | adapts by iteration; any hand edit is named in the SUMMARY |
| `docs/worker-operations.md` | the district refresh pass, the four tick fields in the structured line, one symptom row, the recorded freshness limit |
| `docs/simulation-architecture.md` | a dated re-verification note, section 1 corrected to three engines, a district engine subsection naming the new browser pricer and its measured gap, section 5's one-sentence consistency note, section 6's file map rows |
| `docs/publish-budget.md` | the district artifact added to the live-writer list and table, with its own basis and an estimate labelled as one |
| `apps/web/playwright.config.ts` | the new spec registered on `desktop` and `phone-390`, with a reason line per project |

## Nothing this plan touches

No published artifact, no R2 key, no schema, no `PAGE_ARTIFACT_SCHEMA_VERSION`, no algorithm version,
no `locks.ts`, no file under `apps/web/src/components/districts/`, no file under `apps/worker/`, no
`theme.css` entry, no new colour, no new dependency, no `package.json` script, and no checkpoint task.

## Handoffs this plan creates

- **10-09 (operator):** the exact command `npx playwright test e2e/districts-ledger.spec.ts` run from
  `apps/web` after the deploy and the republish, its five expected test titles, and the note that this
  is the spec's FIRST execution, so a failure is as likely to be spec drift as a site defect and both
  must be triaged rather than one assumed.
- **Jacob:** the content file path `apps/web/src/components/methodology/districtLedgerContent.ts` and
  `awardsContent.ts`, named in the SUMMARY so he can edit the copy himself, per
  `feedback_methodology_copy_voice`.
- **A named follow-up, not this phase's:** `apps/web/e2e/**` is in neither tsconfig's `include`, so no
  e2e spec in this repo is typechecked by anything except Playwright's own transpile at run time. This
  plan does not close that gap (adding the directory would surface unknown errors across nineteen
  existing specs and balloon a documentation plan), but it records it as a real gap rather than leaving
  it to be rediscovered.
</artifacts_this_plan_produces>

<source_coverage_audit>
Scoped to this plan. Phase-wide coverage is the outline's (`10-PLAN-OUTLINE.md`); items below that
belong to a sibling plan are marked with its ID so nothing reads as an unplanned gap.

| Source | Item | Status |
|---|---|---|
| GOAL | "awards are priced from walk forward base rates by decoration bucket" — the published statement of it | COVERED — Task 2, section `award-base-rates` plus the awards page table |
| REQ | SC-6, "the selection model's pick-order agreement with SPR and the award base-rate tables are measured walk-forward on the corpus, pinned by tests, and stated on the methodology page in its voice" | COVERED — measured and pinned by 10-02 and 10-04; STATED here in Tasks 1 and 2 |
| REQ | SC-7, "the live e2e spec covers the new tab" | COVERED — Task 4 writes and registers it; 10-09 runs it |
| REQ | SC-7, "repo-root vitest is green, both tsconfigs are clean" | COVERED — every task's `<verify>` and the `<verification>` block |
| REQ | SC-7, CI green after the push, the republish, the deploy before the republish | 10-09 |
| REQ | SC-1 to SC-5 | 10-03, 10-04, 10-05, 10-06, 10-07 |
| RESEARCH | Sources and confidence breakdown, "every stated number must trace to a committed script" | COVERED — the five named sources, the figure pins, the SUMMARY's figure-to-source table, the halt rule |
| RESEARCH | Validation Architecture, SC-6 award leak test and SC-7 row | COVERED — the leak test is 10-02's; the SC-7 gates are this plan's `<verification>` block |
| RESEARCH | Security Domain, the `.env`-into-a-transcript row | COVERED — T-10-08-04 and a per-task acceptance line |
| PATTERNS | `districtLedgerContent.ts` row: content-as-data with the runtime voice gate over exported string VALUES | COVERED — Tasks 1 and 2 |
| PATTERNS | Shared Pattern "Token-only colour, dataviz palette validator" | COVERED — no colour is added, stated rather than validated against an unchanged palette |
| CONTEXT | "Awards (Jacob)": the table is "stated on the methodology awards page in its voice" | COVERED — Task 2, item 6, on `/methodology/awards` specifically |
| CONTEXT | "Alliance selection and captains": "the selection model's agreement with actual pick order is measured on `event_alliances` and stated on the methodology page" | COVERED — measured by 10-02; stated in Task 2, section `the-alliance-selection-model` |
| CONTEXT | "Corrections from research": the browser pricer's gap "state the gap on the methodology page" | COVERED — Task 2, section `how-well-the-bracket-pricer-works` |
| CONTEXT | "No award prediction ever feeds `locks.ts`. A Locked verdict stays a guarantee." | COVERED — stated on the page in Task 2, item 3; `locks.ts` is untouched |
| CONTEXT | "Never print a U+00B1 anywhere on this tab, and never render a partial variance" | COVERED — the runtime gate, the rendered-DOM gate, and the e2e region check |
| CONTEXT | "Process rules that apply": repo-root vitest, both tsconfigs, no `.env` read, publishes and pushes from the main context | COVERED — every `<verify>`, every acceptance block, and the explicit not-this-plan's list |
| CONTEXT | Claude's Discretion, "copy for the drawer and the definitions row, within the methodology voice rules" | 10-07 (`districtLedgerCopy.ts`); this plan owns methodology prose only, and reads that module so the two agree |
| CONTEXT | Deferred: the Impact and Rookie All Star ordering tables, an advancement chance number, the Champ Locks tab | OUT OF SCOPE — not planned, not mentioned on the page as coming |

No item is MISSING. The three locked CONTEXT clauses that name the methodology page by name are each
mapped to a specific section of a specific page, and the one that names the AWARDS page specifically
is honored on that page rather than relocated for convenience.
</source_coverage_audit>

<output>
Create `.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-08-SUMMARY.md` when done.

It must carry, beyond the standard template:

- **`## Figure to source`** — one row per figure appearing on either methodology page: the figure as
  it reads on the page, the committed source it came from, and the exact command or test that produced
  it. A figure with no row is a defect. **10-09 and any future editor read this section.**
- **`## The retired sentences`** — the old text and the new text, side by side, for `AWARDS_LEAD` and
  for each sentence removed from the three docs, plus the negative-grep output proving each is gone.
- **`## Doc mention counts, before and after`** — `grep -ci district` on each of the three docs, the
  pre-task zero beside the post-task count, and the scoped diff of `docs/publish-budget.md`'s fenced
  machine-readable block quoted as empty.
- **`## The e2e spec, as collected`** — the full `--list` output under both deployed projects, the five
  test titles, one grep line per selector literal proving it appears verbatim in the component that
  renders it, and the command and output that derived each pinned count from the published artifact.
  If `--list` could not run, this section says so and records the gate as UNVERIFIED.
- **`## Content files for Jacob`** — the two content module paths, named plainly, because
  `feedback_methodology_copy_voice` records that he prefers to edit the copy himself.
- **`## Baseline vs post-plan failing test sets`** — the two sets side by side, plus the collected file
  count of the root-run full suite.
- **`## What is NOT proven here`** — the e2e spec has never been executed; the 390px and reduced-motion
  backstops remain 10-09's and the UAT's; `apps/web/e2e/**` is typechecked by neither tsconfig. Each
  named as unproven rather than claimed.
- One explicit line confirming no secret was read, printed, copied, hashed or interpolated, that no
  `measure:*` script was run, and that no network request was made.
</output>
