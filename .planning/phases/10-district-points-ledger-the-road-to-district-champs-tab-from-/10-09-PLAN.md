---
phase: 10
plan: 09
type: execute
wave: 5
depends_on: ["10-08"]
files_modified:
  - docs/publish-budget.md
  - .planning/STATE.md
autonomous: false
requirements: [SC-7]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 8
  confidence: high

user_setup:
  - service: cloudflare
    why: "The Worker deploy, the R2 publishes and the `wrangler tail` window all run against Jacob's Cloudflare account from the MAIN context; auto mode denies some of them and Jacob has to grant each in-message."
    env_vars:
      - name: TBA_API_KEY
        source: ".env (already present; never read, printed or interpolated — `tsx --env-file=.env` and `wrangler --env-file .env` read it themselves)"
      - name: R2 access key pair
        source: ".env (already present; same rule)"
    dashboard_config:
      - task: "Read this month's R2 Class A (write) operation count BEFORE choosing option-a in Task 5. STATE.md's own standing note says `pnpm rebaseline` is deliberately not scheduled because September's ~17 republishes have probably passed the 1,000,000/month free allowance, and one full republish is ~109,000 more."
        location: "Cloudflare Dashboard -> R2 -> sigmascout-artifacts -> Metrics"
      - task: "Grant `npx wrangler deploy` (Task 2) and `npx wrangler tail` (Task 3) in-message when the permission prompt appears, or run them yourself and paste the output."
        location: "this session"

must_haves:
  truths:
    - "SC-7 is proved by six recorded observables, not by assertion: a repo-root `npx vitest run` pass count with zero failures, three silent `tsc --noEmit` runs (root, apps/web, apps/worker), a new Worker Version ID from `npx wrangler deploy`, a live `v1/district/{key}.json` whose `generation` equals the `publish:districts` run's own stamp, two green `gh run list` rows for the pushed head SHA, and a non-zero Playwright test count for 10-08's district spec against `https://sigmascout.org`."
    - "The Worker is deployed BEFORE the district republish, in that order, as two separate gates (SC-7 names the ordering explicitly). Deploying AFTER the republish would put a Worker that cannot merge the new shape in front of an artifact that already carries it; the reverse order is safe only because every field 10-03 added is optional and `MANIFEST_SCHEMA_VERSION`/`PAGE_ARTIFACT_SCHEMA_VERSION` did not move — both directions are stated in Task 2 so an operator reading only one line still gets the order right."
    - "The production status of the live district refresh (SC-1's half) is RECORDED HONESTLY rather than implied: the pass is inert until `v1/manifest/live-windows.json` carries `districtKey`, nothing but `packages/harness/publish.ts` writes that manifest, and whether it gets rewritten in this phase is Jacob's explicit option-a/option-b choice in Task 5 with the R2 write cost on the table. In neither branch has a real district fold been observed in production, and the record says so."
    - "Every command in this plan runs from the MAIN context. Executor subagents have no network (`project_subagent_network_block`): no publish, no `wrangler deploy`, no `wrangler tail`, no `git push`, no `gh run list`, no Playwright run against the deployed origin."
    - "This plan changes no source code. Its only tracked-file edits are `docs/publish-budget.md`'s district numbers and `.planning/STATE.md`'s operator record. A failing gate is reported and fixed in its owning plan or a quick task — never patched inside a checkpoint, because a fix applied here would ship untested behind a gate that already reported green."
    - "`.env` is never read, `cat`ed, echoed or interpolated by any task (`.claude/CLAUDE.md` § Conventions — the rule exists because it was broken once and a live R2 key had to be rotated). Presence is confirmed by length only; every credentialed command gets the file by path through `tsx --env-file=.env` or `wrangler --env-file .env`."
  artifacts:
    - "docs/publish-budget.md (the district section's REAL production numbers from this plan's `pnpm publish:districts` run, beside 10-06's locally-measured ones; plus the machine-readable `json budget` block if option-a rewrote it)"
    - ".planning/STATE.md (the operator record: Worker Version ID, district `generation`, CI run IDs, e2e counts, the Task 5 decision and anything it leaves owed)"
  key_links:
    - "Task 2's `npx wrangler deploy` -> Task 4's `pnpm publish:districts`. The ordering SC-7 names. Proved by two recorded timestamps, not by intent."
    - "`packages/harness/publish.ts` -> `v1/manifest/live-windows.json` -> the Worker's district liveness. The ONLY writer of that manifest (verified this session: `publish:districts`, `publish:stubs` and `manifest:algorithms` all leave it untouched), which is why `districtKey` cannot reach production without a full season republish and why Task 5 is a decision rather than a step."
    - "`git push` -> both GitHub Actions workflows -> the Pages deploy -> the live e2e run. `deploy.yml` is what deploys the site (there is no manual Pages step), so the e2e run is only meaningful after that workflow concludes green and its own `pnpm check:deployed-assets` has passed."
    - "`npx playwright test <spec> --list` -> a non-zero test count. A spec absent from every project's `testMatch` in `apps/web/playwright.config.ts` runs ZERO tests and exits 0, which is the exact shape of a false green."
---

<objective>
The operator gates for Phase 10, in the one order that is safe. Eight blocking checkpoints, all
`autonomous: false`, every one run by the orchestrator from the MAIN context: local proof (repo-root
vitest plus three tsconfigs) and a tree inventory, the Worker deploy at the verified SHA, a
post-deploy tick health check, the district republish verified by content on the real origin, Jacob's
decision on the live-windows `districtKey` regeneration and its follow-through, the push and both CI
workflows, the live e2e run against the deployed site, and the record.

Purpose: SC-7 is the only success criterion nothing in Waves 1 to 4 can touch, because every part of
it needs the network and executor subagents have none. It is also where this project's recorded
failure modes cluster: a Windows-only green that hid a Linux-only red for two days, a deploy race
that cached `index.html` under the asset URLs and blanked the site while `curl` reported it healthy,
a `pnpm --filter worker deploy` that deploys nothing, forty silently-failing e2e specs, and a root
`tsc --noEmit` that returns clean over real `apps/web` errors. Each gate below is shaped by one of
those.

Output: no new source artifact. Deployed and published state, plus two records —
`docs/publish-budget.md`'s measured district numbers and `.planning/STATE.md`'s operator line.

WHY EIGHT TASKS AND NOT THREE. The usual 2-to-3-task ceiling exists to keep an executor's context
inside its budget. It does not apply here: a checkpoint task consumes essentially no executor
context (the orchestrator runs the command and reads the output), and 10-PLAN-OUTLINE.md's
"Sequencing hazards for 10-09" requires these gates to be SEPARATE ordered checkpoints precisely so
that one cannot be collapsed into another and quietly reordered. Splitting them further would be
noise; merging them would destroy the ordering guarantee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-CONTEXT.md
@.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-VALIDATION.md
@.claude/CLAUDE.md
@docs/worker-operations.md
@docs/publish-budget.md
@package.json
@apps/web/playwright.config.ts
</context>

<facts_verified_this_session>
Read these before the first command. Each was checked directly against HEAD on 2026-09-25 and each
one changes what a gate below means.

1. **`v1/manifest/live-windows.json` has exactly ONE writer: `packages/harness/publish.ts:2441-2455`,
   inside its `if (!options.skipState)` block.** `scripts/publishDistricts.ts` does not touch it.
   `scripts/publishProbeStubs.ts` does not touch it (its own header says it exists to avoid the
   ~109,000-write full republish). `scripts/publishAlgorithmsManifest.ts` writes
   `v1/manifest/algorithms.json` only. `scripts/replayRig.ts` patches it temporarily and restores it.
   Therefore 10-03's `LiveWindowEntrySchema.districtKey` reaches production only through
   `pnpm publish:seasons` — a full republish. That is Task 5's entire subject.
2. **A full republish costs about 109,000 R2 Class A writes and ran 1h42m last time** (quick task
   260923-3w8's SUMMARY; the 108,979-object figure in STATE.md's `stopped_at`), peaks around 7.2 GB
   while two generations coexist, emits four seed files that MUST be applied with `seed-cursors.sql`
   LAST, and leaves a prune owed for six hours. STATE.md's Session Continuity block carries a
   standing note that this is deliberately NOT scheduled and that Jacob should check R2 usage in the
   dashboard first.
3. **`pnpm publish:districts` is a different, small run**: `tsx --env-file=.env
   scripts/publishDistricts.ts --years 2016-2020,2022-2026`. Its `generation` is its own
   `new Date().toISOString()` (`scripts/publishDistricts.ts:544-545`), independent of the algorithm
   generation, and district artifacts are deliberately NOT algorithm-scoped
   (`packages/harness/pageArtifacts.ts:2032`). It writes on the order of a hundred objects, not
   100,000.
4. **CI is two workflows, both on push.** `.github/workflows/test.yml` runs `pnpm typecheck` then
   `pnpm test` on ubuntu — and root `pnpm typecheck` is `tsc --noEmit` at the ROOT ONLY, which
   `project_root_tsc_misses_web` records as returning clean over real `apps/web` errors. CI therefore
   does NOT typecheck `apps/web` or `apps/worker`; Task 1 is the only gate that does.
   `.github/workflows/deploy.yml` builds `apps/web`, deploys Pages with `--branch=main`, sleeps 90 s
   and runs `pnpm check:deployed-assets`. **The Pages deploy is CI's job — there is no manual
   `pnpm --filter web run deploy` step in this plan.**
5. **`npx wrangler deploy` from `apps/worker` is the deploy.** `pnpm --filter worker deploy` hits
   pnpm's own built-in and deploys nothing (`project_worker_deploy_and_tail`). `pnpm worker:deploy`
   is `pnpm --filter worker run deploy` and is equivalent, but `docs/worker-operations.md`'s
   canonical form is the explicit one. A healthy deploy prints `schedule: * * * * *` and exactly TWO
   bindings, `DB` and `ARTIFACTS`; a `MANIFEST` line means a pre-260923-3w4 `wrangler.toml`, not a
   healthy deploy.
6. **The tick log gains four district fields from 10-05**: `districtsConsidered`,
   `districtsRefreshed`, `districtsUnchanged`, `districtsFailed`, all typed `TickResult` members, so
   every return site carries them and the one JSON line per invocation reports them. An idle tick
   reports all four as 0.
7. **Every Playwright run starts the local preview `webServer`** (top-level config, no per-project
   form) with `reuseExistingServer: true` — so a stale `:4173` listener is asserted against as-is.
   And a spec that appears in no project's `testMatch` runs zero tests and exits 0.
8. **The R2 edge Cache Rule is 60 s on `data.sigmascout.org`** (`project_r2_edge_cache_dynamic`).
   Verify published content with GET (not HEAD) and an `Origin` header; an old `generation` inside
   the first 60 s is a cache hit, not a failed publish.
</facts_verified_this_session>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Local proof and tree inventory — the full suite, all THREE tsconfigs, and every unpushed commit named</name>
  <files>(no file is modified; this task only reads and reports)</files>
  <precondition>Running in the MAIN working tree, not a worktree: `git rev-parse --git-dir` prints `.git`. Plans 10-01 through 10-08 are committed (`git log --oneline -20` shows their commits and `.planning/phases/10-*/10-0{1..8}-SUMMARY.md` all exist). `data/corpus.sqlite` exists, so the corpus-backed reconciliation tests run for real rather than skipping. If any check fails, HALT and report which.</precondition>
  <read_first>
    - `.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-VALIDATION.md` "Test Infrastructure" and "Sampling Rate" — the repo-root rule and the ~90 s runtime.
    - Project memory `project_test_scope_trap` (vitest from `apps/web` sees 77 files, repo root 167; an eight-day red CI hid in that gap), `project_root_tsc_misses_web` (root `tsc --noEmit` returns clean over real web errors), `project_timeout_pnpm_false_green` (`timeout <n> pnpm <cmd>` swallows all output and exits 0), `project_concurrent_session_hazards` (one checkout, many sessions: a push deploys other sessions' commits too), `project_pnpm_install_exit1_worktrees` (`apps/web` shows ~45 phantom route-type errors until a `vite build` generates `routeTree.gen.ts`).
    - The last recorded baseline to compare against: 250 test files, 5,550 tests passed, 1 skipped (quick task 260923-3w8's SUMMARY). Phase 10 adds files, so the count must be ABOVE that, never below.
  </read_first>
  <what-built>Waves 1 to 4 committed every source change this phase makes. Nothing here is built; this gate proves what was built compiles and passes on this machine, across all three tsconfigs CI does not cover, and establishes the exact SHA the later gates deploy and push.</what-built>
  <how-to-verify>Run the `<commands>` list below, in order, from the repo root. Read the `<acceptance_criteria>` against the recorded output — counts and silence, never exit codes.</how-to-verify>
  <action>
    Run the gates in the order below from the repo root and record every output verbatim in the
    SUMMARY. Never wrap a run in `timeout <n> pnpm ...`. Never run vitest from `apps/web`.

    The tree inventory is not a formality. `git log origin/main..main` is the exact set of commits
    Task 6's push will publish and `deploy.yml` will deploy — including any a concurrent session left
    sitting there. Name every one of them and attribute it. If any commit is not Phase 10's, stop and
    ask before going further; Jacob, not this plan, decides whether a foreign commit ships.

    If `npx tsc --noEmit -p apps/web/tsconfig.json` reports roughly 45 errors all about missing route
    types, that is the known `routeTree.gen.ts` artifact: run `npx vite build` once inside `apps/web`,
    then re-run the typecheck. Real errors survive that; phantom ones do not.
  </action>
  <commands>
    git rev-parse --git-dir
    git status --short
    git fetch origin
    git log --oneline main..origin/main
    git log --oneline origin/main..main
    git rev-parse HEAD
    npx vitest run
    npx tsc --noEmit
    npx tsc --noEmit -p apps/web/tsconfig.json
    npx tsc --noEmit -p apps/worker/tsconfig.json
  </commands>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npx tsc --noEmit -p apps/web/tsconfig.json && npx tsc --noEmit -p apps/worker/tsconfig.json</automated>
    <human-check>Jacob confirms the `origin/main..main` list contains only commits he is willing to publish.</human-check>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --git-dir` printed `.git` (main checkout, so the corpus-backed tests are real and CRLF-only failures from `project_crlf_worktree_harness_tests` cannot appear).
    - `npx vitest run` from the REPO ROOT reported zero failures, and its test-file and test counts are recorded and are at or above the 250-file / 5,550-test baseline. A skip count is recorded with each skipped suite named (a corpus-gated skip in the main checkout is a problem, not a healthy no-op).
    - All three `tsc --noEmit` invocations printed NOTHING. If `apps/web` needed a `vite build` first, the SUMMARY says so and records the post-build re-run.
    - `git log --oneline main..origin/main` is EMPTY (local `main` is not behind) or the divergence is reported and resolved before any later task runs.
    - Every commit in `git log --oneline origin/main..main` is listed in the SUMMARY with its subject and an attribution (Phase 10 plan NN, or another session). Nothing is summarized as "and N others".
    - `git status --short` is reported in full. The pre-existing untracked `.planning/sketches/020-district-locks-page-reimagined/` and the modified `.planning/sketches/MANIFEST.md` are expected; anything else is named and explained.
    - The HEAD SHA is recorded verbatim. Task 2 deploys THAT SHA and Task 6 pushes it.
  </acceptance_criteria>
  <resume-signal>Jacob types "gates green" to proceed, or names the failing gate. If `origin/main..main` holds a commit that is not Phase 10's, Jacob must say explicitly whether pushing it is acceptable — a push deploys every commit in that range, not just this phase's.</resume-signal>
  <done>The full suite is green from the repo root, all three tsconfigs are silent, local `main` is not behind `origin/main`, every unpushed commit is named and attributed, and the SHA that will be deployed and pushed is written down.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy the Worker at the verified SHA — BEFORE any republish</name>
  <files>(no file is modified; `apps/worker/wrangler.toml` is read, never edited)</files>
  <precondition>Task 1 is green and its HEAD SHA is recorded. `git status --porcelain` is EMPTY — a dirty tree must not be deployed, because the bundle would carry uncommitted code that no test run and no CI run ever saw. If the tree is dirty and cannot be cleaned, deploy from a clean detached worktree created OUTSIDE the repo at the recorded SHA (`project_worker_deploy_and_tail`) and record that you did. Jacob must grant `npx wrangler deploy` in-message; auto mode may deny it. HALT if the tree is dirty and no clean worktree is available.</precondition>
  <read_first>
    - `docs/worker-operations.md` § "Deploying" in full — the manual-by-design rule (D-27, there is deliberately no deploy-on-push for the Worker), the two canonical command forms, the `npx wrangler deployments list` confirmation, the "must print `schedule: * * * * *` and TWO bindings" check, and the `MANIFEST` binding's meaning (a pre-260923-3w4 config).
    - `apps/worker/wrangler.toml` in full — `main = "src/scheduled.ts"`, `LIVE_ALGORITHM_IDS = "opr,epa,spr"`, the `[triggers] crons` line, the `[[d1_databases]]` and `[[r2_buckets]]` blocks, and the recorded absence of `[[kv_namespaces]]`.
    - Project memory `project_worker_deploy_and_tail` — `pnpm --filter worker deploy` hits pnpm's built-in and deploys nothing; deploy on a clean tree; stop `wrangler tail` by PID; auto mode may deny both.
    - `10-CONTEXT.md` § "Process rules that apply" — "Deploy the Worker BEFORE the republish."
  </read_first>
  <what-built>10-05 built the Worker's district refresh pass and its tests; it has never run in production. This gate puts that code on the live cron Worker, at the SHA Task 1 verified, before any artifact carrying the new shape exists.</what-built>
  <how-to-verify>Run the `<commands>` list. Read the deploy output for the Version ID, the two bindings and the cron line; then confirm independently with `npx wrangler deployments list` rather than trusting the deploy's exit code.</how-to-verify>
  <action>
    Deploy from `apps/worker` with the explicit wrangler command, then confirm the deployment
    independently rather than trusting the deploy's own exit code.

    WHY THIS ORDER, BOTH DIRECTIONS, so an operator reading one line still gets it right. Deploying
    AFTER the republish would leave the old Worker reading a district artifact whose shape it was
    never built to merge — it would read back an artifact carrying 10-03's new fields, merge with a
    producer that does not know them, and write the result. Deploying BEFORE the republish is safe
    only because every field 10-03 added is OPTIONAL and neither `PAGE_ARTIFACT_SCHEMA_VERSION` nor
    `MANIFEST_SCHEMA_VERSION` moved: the new Worker parses the OLD artifact and the OLD manifest
    without complaint, and (with no `districtKey` in the manifest yet) does zero district work at all.
    So: deploy first, always.

    Record the Version ID verbatim. It is the observable SC-7's "the Worker is deployed before the
    republish" clause rests on, and Task 8 writes it into STATE.md.
  </action>
  <commands>
    git status --porcelain
    git rev-parse HEAD
    cd apps/worker && npx wrangler deploy
    cd apps/worker && npx wrangler deployments list
  </commands>
  <verify>
    <automated>cd apps/worker && npx wrangler deployments list</automated>
    <human-check>Jacob grants the deploy in-message (or runs it himself and pastes the output).</human-check>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` was empty at deploy time, or the deploy ran from a clean detached worktree at the recorded SHA and the SUMMARY says so and names the path.
    - The deploy output printed a new **Version ID**, recorded verbatim, together with the upload size and the worker name `sigmascout-worker`.
    - The deploy output lists exactly TWO bindings: `DB` (D1 `sigmascout-state`) and `ARTIFACTS` (R2 `sigmascout-artifacts`). A `MANIFEST` line means a stale `wrangler.toml` — STOP and report rather than proceeding.
    - The deploy output prints `schedule: * * * * *` and the vars `TBA_BASE_URL` and `LIVE_ALGORITHM_IDS ("opr,epa,spr")`.
    - `npx wrangler deployments list` shows that Version ID as the current deployment at 100%.
    - The SUMMARY records the wall-clock time of the deploy. Task 4's publish timestamp must be LATER — that pair of timestamps is the proof of SC-7's ordering clause.
    - No secret appears anywhere in the recorded output. `TBA_API_KEY` is a Worker secret set once; this task neither reads nor sets it.
  </acceptance_criteria>
  <resume-signal>Jacob types "deployed" with the Version ID, or "denied" if the permission prompt was refused and the deploy must be handed over.</resume-signal>
  <done>A new Worker version carrying 10-05's district refresh pass is live at 100%, its Version ID and deploy time are written down, its bindings and cron schedule are confirmed, and the deploy provably precedes every republish in this plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-deploy tick health — two consecutive healthy ticks, and the district fields recorded as inert</name>
  <files>(no file is modified)</files>
  <precondition>Task 2 recorded a new Version ID and `wrangler deployments list` shows it at 100%. At least two minutes have passed since the deploy, so two ticks have fired (the cron is `* * * * *`, year-round). Jacob must grant `npx wrangler tail` in-message; auto mode may deny it, in which case read the same lines from Workers Logs in the dashboard (observability is on at `head_sampling_rate = 1.0`, so nothing is sampled away).</precondition>
  <read_first>
    - `docs/worker-operations.md` § "Watching it" — the exact tail command and the healthy-idle-tick JSON line, including the note that `eventsDeferred` no longer exists.
    - `docs/worker-operations.md` § "Troubleshooting" rows for `state-generation-mismatch` (D1 has not been seeded for the live generation) and "No logs at all in `wrangler tail`".
    - `10-05-PLAN.md`'s `TickResult` additions — `districtsConsidered`, `districtsRefreshed`, `districtsUnchanged`, `districtsFailed` — and its must-have that a pre-phase manifest (no `districtKey` on any entry) makes the tick do ZERO district work: zero district TBA requests, zero district R2 reads, zero district writes, no throw.
    - Project memory `project_worker_cputime_not_reproducible` — there is ~5 ms run-to-run spread on unchanged code, so read NO conclusion from a single `durationMs` or `cpuTime` figure and pre-register nothing as an absolute bar.
  </read_first>
  <what-built>Task 2 deployed a Worker whose tick now calls a new pass. This gate proves the tick still completes — a Worker that throws on every invocation passes `wrangler deploy` and `tsc` alike and warns nobody.</what-built>
  <how-to-verify>Tail the Worker for two or three minutes and read the one structured JSON line per invocation against the `<acceptance_criteria>`. If the tail permission is denied, read the same lines from Workers Logs in the dashboard — sampling is at 1.0, so nothing is dropped.</how-to-verify>
  <action>
    Watch two or three consecutive ticks and read the one structured JSON line each emits. Stop the
    tail by PID when done (it does not self-terminate).

    This gate exists because a broken tick is silent: `wrangler deploy` and `tsc` both pass against a
    Worker that throws on every invocation, and nothing upstream warns. Two consecutive `ok: true`
    lines are the cheapest possible proof that 10-05's new pass did not break the fold path it was
    inserted into.

    What a healthy line looks like right now, in late September with no district live: `ok: true`, the
    four district fields all `0`, and `districtsConsidered: 0`. Record that as INERT, not as evidence
    the district pass works — it proves only that the pass costs nothing when there is nothing to do.
    If the tail is denied, hand Jacob the command and ask for two pasted lines.
  </action>
  <commands>
    cd apps/worker && npx wrangler tail sigmascout-worker --format json
    (stop it by PID once two or three tick lines have arrived)
  </commands>
  <verify>
    <automated>cd apps/worker && npx wrangler deployments list</automated>
    <human-check>Two consecutive tick lines, roughly 60 s apart, both `"ok":true`, pasted into the SUMMARY verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    - At least two consecutive tick lines are recorded verbatim, arriving roughly 60 s apart, both with `"ok":true`.
    - Both lines carry all four district fields — `districtsConsidered`, `districtsRefreshed`, `districtsUnchanged`, `districtsFailed` — which is itself the proof that 10-05's typed `TickResult` change reached production. A line MISSING those fields means an older version is serving: stop and re-check `wrangler deployments list`.
    - Neither line reports `state-generation-mismatch`, and no `LeagueRowShapeVersionError` naming `opr` or `epa` appears (that shape would mean a partial D1 seed, which would matter only if Task 5 takes option-a).
    - `districtsFailed` is 0 on every observed tick. Any non-zero value is reported with its accompanying warn line and blocks the republish until explained.
    - The SUMMARY states plainly: the district refresh pass is INERT in production because `v1/manifest/live-windows.json` carries no `districtKey` yet, and no real district fold has ever been observed. `durationMs` is recorded as an observation with no bar attached to it.
  </acceptance_criteria>
  <resume-signal>Jacob types "tick healthy" with the two lines pasted, or "hand it over" if the tail permission was denied.</resume-signal>
  <done>The newly deployed Worker is provably still folding: two consecutive healthy ticks, the four new district counters present and zero, no generation mismatch, and the pass's inert-in-production status recorded as a fact rather than assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Corpus freshness and the district republish, verified by CONTENT on the real origin</name>
  <files>(no tracked file is modified; `data/corpus.sqlite` is refreshed and `reports/` output is read)</files>
  <precondition>Tasks 1 to 3 are green and the Worker deploy's wall-clock time is recorded. 10-06's SUMMARY is on disk and read: it names which branch 10-03 landed (inline `baked` on the district artifact, or the `v1/district-presim/{districtKey}/{eventKey}.json` sidecar) and whether it leaves any owed version bump. `.env` holds the TBA key and the R2 pair; confirm presence by LENGTH ONLY (`v=$(grep -E "^TBA_API_KEY=" .env | cut -d= -f2-); [ -n "$v" ] && echo "OK (len=${#v})"`) and never render a value. HALT if 10-06's SUMMARY names an owed version bump — that must ship first, per `feedback_version_bump_on_output_change`.</precondition>
  <read_first>
    - `10-06-PLAN.md`'s Task 3 acceptance criteria and its `<success_criteria>` command list — the production entry is the committed `pnpm publish:districts` (`--years 2016-2020,2022-2026`); the `--as-of` / `--local-out` / `--no-bake` flags exist for verification runs and measured, on the real corpus, that a production run today has ZERO bake-eligible events because no district event in the corpus starts in the future. Expect an events-baked count of 0 and record it as the production cost of the day, not as a failure.
    - `10-06`'s budget wiring: `assertWithinDistrictBudget` runs on every detail object and every sidecar BEFORE anything is written, in `--dry-run` and real runs alike, against `DISTRICT_DETAIL_MAX_BYTES_PER_TEAM` / `DISTRICT_DETAIL_MAX_BYTES` (and `DISTRICT_PRESIM_MAX_BYTES` on the sidecar branch). Byte counting is UTF-8 (`Buffer.byteLength`), which matters for the 30 district teams with a non-ASCII nickname.
    - `10-03-PLAN.md`'s `<artifacts_this_plan_produces>` field list — `state.{qualMatchesPlayed,qualMatchesTotal,alliancesPicked,playoffsDone,awardsPosted}` on the per-team-per-event rows, `awardProfile` on the team row, top-level `awardBaseRates`, and the inline-`baked`-versus-`bakedEvents`+sidecar branch.
    - `docs/worker-operations.md`'s "Check what the Worker currently believes is live" snippet (the `curl | node -e` one-liner over `live-windows.json`) — reuse its shape for the content checks below.
    - Project memory `project_r2_edge_cache_dynamic` (60 s edge TTL on `data.sigmascout.org`; test with GET, not HEAD), `project_pages_cache_poisoning` (always send a browser-shaped `Origin` header), `.claude/CLAUDE.md` § Conventions (the `.env` rule in full).
  </read_first>
  <what-built>10-06 built the publisher that emits the four state facts, the award tables and the baked pmfs, verified so far only against `--local-out` files. This gate freshens the corpus and runs it for real against R2, then proves the published bytes by reading them back.</what-built>
  <how-to-verify>Run the `<commands>` list in order: the length-only key probe, the four 2026 ingest passes, the `--dry-run` publish, the real publish, then the three content fetches. Judge the result by the parsed field counts and the `generation` string, never by an exit code.</how-to-verify>
  <action>
    Three moves: freshen the corpus, publish, then prove the published bytes by reading them back from
    the real origin.

    **Freshen the corpus first.** The four state facts and the award base-rate tables are derived from
    the corpus, so a stale corpus publishes stale truths. Run the 2026 passes that feed them: the
    plain pass (matches), then districts, alliances and awards. Each is a `tsx --env-file=.env`
    invocation that reads `.env` itself; nothing here reads or echoes a credential. Record each pass's
    row counts.

    **Publish dry first, then for real.** The dry run composes, validates against
    `DistrictsIndexArtifactSchema`/`DistrictArtifactSchema` and prints per-object bytes through the
    same budget gate, without calling `putObject` — so a budget violation or a schema break surfaces
    before a single write. Only then run the committed `pnpm publish:districts`.

    **Verify by CONTENT, never by exit code.** Fetch the published index and at least two district
    detail objects from `https://data.sigmascout.org` with a browser-shaped `Origin` header, parse the
    JSON, and count the new fields. If a body still shows the previous `generation`, that is the 60 s
    edge cache: wait out the window and re-fetch. Do not treat a cache hit as a failed publish, and do
    not use HEAD.

    Record the publish's wall-clock time. It must be LATER than Task 2's deploy time.
  </action>
  <commands>
    v=$(grep -E "^TBA_API_KEY=" .env | cut -d= -f2-); [ -n "$v" ] && echo "TBA key present (len=${#v})"; unset v
    npx tsx --env-file=.env packages/ingest/cli.ts --year 2026
    npx tsx --env-file=.env packages/ingest/cli.ts --districts-only --years 2026-2026
    npx tsx --env-file=.env packages/ingest/cli.ts --alliances-only --years 2026-2026
    npx tsx --env-file=.env packages/ingest/cli.ts --awards-only --years 2026-2026
    npx tsx --env-file=.env scripts/publishDistricts.ts --years 2016-2020,2022-2026 --dry-run
    pnpm publish:districts
    curl -s -H "Origin: https://sigmascout.org" -D - -o /dev/null "https://data.sigmascout.org/v1/districts/2026.json"
    curl -s -H "Origin: https://sigmascout.org" "https://data.sigmascout.org/v1/districts/2026.json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);console.log('generation',j.generation,'computedAt',j.computedAt,'districts',(j.districts??[]).length);})"
    curl -s -H "Origin: https://sigmascout.org" "https://data.sigmascout.org/v1/district/2026pnw.json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const teams=j.teams??[];const withState=teams.filter(t=>(t.eventPoints??[]).some(e=>e.state!==undefined)).length;const withProfile=teams.filter(t=>t.awardProfile!==undefined).length;console.log('generation',j.generation,'teams',teams.length,'teamsWithEventState',withState,'teamsWithAwardProfile',withProfile,'awardBaseRates',j.awardBaseRates===undefined?'absent':j.awardBaseRates.rows.length+' rows measuredThrough '+j.awardBaseRates.measuredThroughSeason,'bytes',Buffer.byteLength(s));})"
  </commands>
  <verify>
    <automated>npx tsx --env-file=.env scripts/publishDistricts.ts --years 2016-2020,2022-2026 --dry-run</automated>
    <human-check>The live `v1/district/2026pnw.json` body, fetched with an Origin header, carries the run's own `generation` and a non-zero count of teams with per-event `state`.</human-check>
  </verify>
  <acceptance_criteria>
    - Each ingest pass's output is recorded with its row counts. A pass that fetched nothing is reported as such rather than glossed over.
    - The `--dry-run` output is recorded in full: objects composed, per-object bytes, the largest object's bytes and bytes-per-team, and every budget-gate line. No `DistrictBudgetExceededError`. Events baked is expected to be 0 and is recorded explicitly with 10-06's measured reason.
    - `pnpm publish:districts` exited 0 AND its full stdout is recorded: object count, total bytes, the `generation` stamp, and the write ordering (every `v1/district/{key}.json` before `v1/districts/{year}.json`).
    - The publish's wall-clock time is LATER than Task 2's recorded deploy time. Both timestamps are in the SUMMARY, side by side. This pair is SC-7's "the Worker is deployed before the republish" evidence.
    - `v1/districts/2026.json` fetched from the live origin reports the publish run's own `generation` and a non-zero district count. The response headers include an `access-control-allow-origin` value, and the body is JSON — not an HTML SPA fallback.
    - At least TWO district detail objects were fetched and parsed. For each: `generation` equals the run's, `teamsWithEventState` is non-zero, `awardBaseRates` is present for a registered season (or its absence is explained by 10-06's stated rule that a season with an empty prior award corpus publishes no table rather than one fitted on nothing), and the observed UTF-8 byte size and bytes-per-team are recorded.
    - On the SIDECAR branch only: at least one `v1/district-presim/{districtKey}/{eventKey}.json` was fetched and parsed, or its absence is explained by the zero-baked-events fact. On the INLINE branch, this criterion is recorded as not applicable with the branch named.
    - No `.env` content, and no value read from it, appears anywhere in the recorded output — only the length probe's `len=` line.
  </acceptance_criteria>
  <resume-signal>Jacob types "republished" with the district `generation` string, or names what the content check found missing.</resume-signal>
  <done>The corpus is fresh for 2026, every district artifact for every published season is republished under one recorded generation with the budget gate satisfied, and the new state, award-profile and award-base-rate fields are proved present by reading the real bytes back off `data.sigmascout.org` with a browser-shaped request — after the Worker deploy, not before.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: Decision — regenerate the live-windows manifest with `districtKey` now, or defer it and record what that leaves inert</name>
  <files>docs/publish-budget.md (option-a only: `--write-budget` rewrites the machine-readable block)</files>
  <precondition>Task 4 is green and the district artifacts are republished (this ordering is deliberate: if the manifest gains `districtKey` while the district artifacts are still pre-phase, a live district's tick would merge rankings into an artifact carrying none of 10-03's state fields). Jacob has read this month's R2 Class A operation count in the Cloudflare dashboard. The live-windows check below has been run so both branches are being chosen with the current live-window count in hand.</precondition>
  <read_first>
    - This plan's `<facts_verified_this_session>` items 1 and 2 in full — the single-writer fact and the ~109,000-write cost.
    - `.planning/STATE.md`'s Session Continuity block — its standing note that `pnpm rebaseline` is deliberately NOT scheduled, that September's ~17 republishes have probably passed the 1,000,000/month free Class A allowance, and that Jacob should check R2 usage in the dashboard.
    - `scripts/rebaseline.ts`'s header and `main()` — the six steps (`ingest`, `deploy`, `publish`, `seed`, `verify`, `prune`), the `--from` / `--skip-*` semantics, why the deploy precedes the publish, why `seed-cursors.sql` is applied LAST (it carries the markers that switch folding back on), and the six-hour prune guard that makes a "NOT pruned" line expected rather than a failure.
    - `docs/worker-operations.md` § "Re-baselining", and project memory `project_d1_seed_import_auth` (`--file` needs the `.env` token, `--command` needs its ABSENCE) and `project_rebaseline_one_command`.
    - `10-05-PLAN.md`'s must-have on the pre-phase manifest: no `districtKey` on any entry means zero district TBA requests, zero district R2 reads, zero district writes and no throw — which is what makes option-b safe rather than merely tolerable.
  </read_first>
  <decision>Does `v1/manifest/live-windows.json` get rewritten in this phase so it carries 10-03's `districtKey`, switching the Worker's district refresh pass from inert to live-capable?</decision>
  <context>
    10-03 added `districtKey` to `LiveWindowEntrySchema` and `buildLiveWindowsManifest` populates it
    from the corpus `districts` join. But that manifest has exactly ONE writer in this repository —
    `packages/harness/publish.ts`, inside `publishSeasons`. `publish:districts` does not write it.
    `publish:stubs` does not write it (its own header exists to explain that it is the cheap
    alternative to the full run). `manifest:algorithms` writes a different file. So there is no small
    command that puts `districtKey` in front of the Worker: the only path is a full
    `pnpm publish:seasons`, which last measured ~109,000 R2 Class A writes over 1h42m, bumps the
    generation, emits four D1 seed files that must all be applied with `seed-cursors.sql` last, and
    leaves a prune owed for six hours.

    Until that happens the Worker's district pass is provably inert — 10-05 has a committed test for
    exactly this state. Nothing breaks, nothing is stale, nothing is at risk; SC-1's live half simply
    has not run in production. It also has not run in production in either branch: no district event
    is live in late September, so even option-a would only make the pass ELIGIBLE, not observed. The
    first real district weekend is months away, and at least one ordinary rebaseline will happen
    before it.

    SC-7 does not name the manifest. It names the district republish (done in Task 4), the deploy
    ordering (done in Task 2), the local gates, CI and the e2e spec. So this decision is about when
    SC-1's live half becomes reachable, and about ~109,000 R2 writes against an allowance STATE.md
    already flags as probably spent.
  </context>
  <options>
    <option id="option-a">
      <name>Regenerate now — the full republish chain, deploy step skipped</name>
      <pros>The Worker's district pass becomes live-capable immediately, so no future operator has to remember an owed step before the first district weekend. Every 2026 artifact also picks up Task 4's fresh ingest. The chain is one command and it self-verifies.</pros>
      <cons>~109,000 R2 Class A writes against an allowance STATE.md says is probably already spent this month, ~1h42m of wall clock, a generation bump that makes the four-file D1 seed MANDATORY (miss it and every tick refuses to fold with `state-generation-mismatch`), R2 storage peaking near 7.2 GB while two generations coexist, and a prune that cannot run for six hours. It also rewrites `docs/publish-budget.md`'s machine block, which must then be committed.</cons>
      <commands>
        pnpm rebaseline --year 2026 --skip-ingest --skip-deploy
        (the chain runs: publish -> seed (four files, seed-cursors.sql LAST) -> verify -> prune)
        curl -s -H "Origin: https://sigmascout.org" "https://data.sigmascout.org/v1/manifest/live-windows.json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const n=Date.now();const live=j.windows.filter(w=>w.startMs<=n&&n<w.endMs);const withKey=j.windows.filter(w=>w.districtKey!==undefined&&w.districtKey!==null);console.log('generation',j.generation,'windows',j.windows.length,'liveNow',live.length,'probeOnly',live.filter(w=>w.inferred).length,'withDistrictKey',withKey.length);})"
        cd apps/worker && npx wrangler tail sigmascout-worker --format json
        git add -- docs/publish-budget.md && git commit -m "docs(10): measured publish budget after the phase 10 republish"
      </commands>
    </option>
    <option id="option-b">
      <name>Defer — record it as owed, and say plainly what stays inert</name>
      <pros>Spends nothing. The Worker's district pass is provably inert with the current manifest (10-05's own committed test), so production behaviour is unchanged and no failure mode is opened. Respects STATE.md's standing "rebaseline is deliberately not scheduled" note and lets Jacob check the R2 dashboard before spending ~11% of a monthly allowance.</pros>
      <cons>SC-1's live half stays unreachable until the next full republish, so it will not have run in production when the phase is verified. The owed step must be recorded somewhere an operator will actually read it before the first district weekend, or it will be forgotten.</cons>
      <commands>
        curl -s -H "Origin: https://sigmascout.org" "https://data.sigmascout.org/v1/manifest/live-windows.json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const n=Date.now();const live=j.windows.filter(w=>w.startMs<=n&&n<w.endMs);const withKey=j.windows.filter(w=>w.districtKey!==undefined&&w.districtKey!==null);console.log('generation',j.generation,'windows',j.windows.length,'liveNow',live.length,'withDistrictKey',withKey.length);})"
        (then Task 8 records the owed regeneration in STATE.md and in docs/publish-budget.md's re-baseline cadence section)
      </commands>
    </option>
  </options>
  <verify>
    <automated>curl -s -H "Origin: https://sigmascout.org" "https://data.sigmascout.org/v1/manifest/live-windows.json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const withKey=j.windows.filter(w=>w.districtKey!==undefined&&w.districtKey!==null);console.log('generation',j.generation,'windows',j.windows.length,'withDistrictKey',withKey.length);})"</automated>
    <human-check>Jacob has seen this month's R2 Class A count in the dashboard before selecting.</human-check>
  </verify>
  <acceptance_criteria>
    - The live-windows check was run BEFORE the decision and its output is recorded: total windows, how many are open right now, how many of those are probe-only, and how many carry a `districtKey`. A count of 0 open windows is normal out of season.
    - The selected option is named in the SUMMARY with Jacob's words, and the R2 Class A figure he read is recorded (or explicitly recorded as not checked, if he chose option-b without needing it).
    - **option-a only:** the full `pnpm rebaseline` output is recorded — the new `generation`, the object count, the four seed files applied in order with `seed-cursors.sql` LAST, the verify step's printed `id@version` list, and whether the prune reported NOT pruned inside the six-hour window (expected; a `--from prune` rerun is then owed and recorded).
    - **option-a only:** the re-fetched `live-windows.json` reports the NEW generation and a non-zero `withDistrictKey` count, and a subsequent tick line shows `ok: true` with no `state-generation-mismatch` — the proof the seed landed. `docs/publish-budget.md`'s machine block was rewritten by `--write-budget` and is committed.
    - **option-b only:** the SUMMARY states, in one sentence an operator will read later, that `districtKey` is absent from the live manifest, that the Worker's district pass is therefore inert, and that the next full republish (any `pnpm rebaseline` / `pnpm publish:seasons`) switches it on with no further code change. Task 8 carries this into STATE.md.
    - In BOTH branches the SUMMARY records that no real district fold has been observed in production, because no district event is live.
  </acceptance_criteria>
  <resume-signal>Select: option-a or option-b.</resume-signal>
  <done>Whether the live-windows manifest carries `districtKey` in production is Jacob's recorded decision rather than an accident of which command someone happened to run, the cost he weighed it against is written down, and the branch he chose is either executed and verified or recorded as owed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Push, then hold until BOTH CI workflows are green for the pushed SHA</name>
  <files>(no file is modified)</files>
  <precondition>Tasks 1 to 5 are green. `git log --oneline origin/main..main` is RE-RUN here, not reused from Task 1 — a concurrent session may have committed in between, and a push publishes whatever is in that range (`project_concurrent_session_hazards`). Jacob has already approved every commit in the range (Task 1's resume signal) and re-approves any that appeared since. `gh` is authenticated. HALT if the range grew and the new commits are not Phase 10's.</precondition>
  <read_first>
    - `.github/workflows/test.yml` in full — `on: push` for ANY branch plus every PR, `pnpm typecheck` and `pnpm test` as SEPARATE steps on ubuntu, and the RUN/SKIP inventory at the bottom (`level1Digest.test.ts` and the presim/publishSeasons suites MUST run there; a skip is a regression, not a healthy no-op).
    - `.github/workflows/deploy.yml` in full — it is what deploys the site: `pnpm --filter web build`, `pages deploy dist --project-name sigmascout-web --branch=main`, a 90 s propagation sleep, then `pnpm check:deployed-assets`, whose red is a LIVE OUTAGE until proven otherwise.
    - Project memory `project_ci_red_after_local_green` (a Windows-only local pass kept Linux CI red for two days; run `gh run list` after EVERY push) and `project_pages_cache_poisoning` (a deploy race once cached `index.html` under the asset URLs and blanked the site while plain `curl` reported it healthy).
  </read_first>
  <what-built>Every source change of Phase 10 is committed locally and proved on Windows only. This gate publishes it and holds until Linux CI and the Pages deploy both agree.</what-built>
  <how-to-verify>Re-run the unpushed-commit inventory, push, then `gh run list --limit 6` and `gh run watch` each of the two workflow runs for the pushed head SHA. Both must conclude `success`, including the deploy run's `check:deployed-assets` step.</how-to-verify>
  <action>
    Re-inventory, push, then watch both workflows to completion. Do not start Task 7 until both are
    green: the e2e specs assert against the DEPLOYED origin, so running them before `deploy.yml`
    finishes tests the previous build and proves nothing about this one.

    Root `pnpm typecheck` is the root tsconfig ONLY — `apps/web` and `apps/worker` were typechecked in
    Task 1 and nowhere else, which is why Task 1 is not redundant with this gate. Conversely this gate
    is not redundant with Task 1: it is the only place the suite runs on Linux, and a Windows-only
    green has hidden a Linux-only red in this repo before.
  </action>
  <commands>
    git fetch origin
    git log --oneline origin/main..main
    git push origin main
    git rev-parse HEAD
    gh run list --limit 6
    gh run watch <test-run-id>
    gh run watch <deploy-run-id>
    gh run view <run-id> --log-failed   (only on a red run)
  </commands>
  <verify>
    <automated>gh run list --limit 6</automated>
    <human-check>Both workflow rows for the pushed head SHA read success.</human-check>
  </verify>
  <acceptance_criteria>
    - The re-run `git log --oneline origin/main..main` output is recorded; if it grew since Task 1, every new commit is named and re-approved before the push.
    - `git push origin main` succeeded and the pushed head SHA is recorded. It matches Task 2's deployed SHA, or the divergence is explained (for example the option-a budget-doc commit) and the Worker's deployed code is confirmed unaffected by the difference.
    - `gh run list` shows BOTH workflows for that head SHA — "Test" and "Build & deploy to Cloudflare Pages" — and both concluded `success`. Both run IDs are recorded. A green Test with a red deploy is NOT a pass.
    - The Test run's logs confirm `pnpm typecheck` and `pnpm test` each ran as their own step and that the presim/publishSeasons and `level1Digest` suites RAN rather than skipped (their skipping is a recorded regression signal).
    - The deploy run's `Verify the live assets the way a browser requests them` step (`pnpm check:deployed-assets`) passed. A failure there is treated as a live outage: stop, report, and follow the remedy the script prints (Purge Everything; a single-file purge may not clear a header variant).
    - No red run is left unexplained. If either workflow failed, `gh run view --log-failed` output is recorded and the failure is triaged to its owning plan — never patched from inside this checkpoint.
  </acceptance_criteria>
  <resume-signal>Jacob types "CI green" with both run IDs, or names the failing workflow and step.</resume-signal>
  <done>The phase is on `origin/main`, the suite and the root typecheck have passed on Linux rather than only on this machine, the site has been rebuilt and deployed by `deploy.yml`, and the deployed assets have been verified the way a browser actually requests them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: The live e2e run — 10-08's district spec against the deployed site, with a non-zero test count</name>
  <files>(no file is modified)</files>
  <precondition>Task 6 is green: BOTH workflows concluded success for the pushed SHA, so the Pages deploy has landed and its assets were verified. No stale listener is on port 4173 (any Playwright run starts the top-level `webServer` and `reuseExistingServer: true` will reuse a STALE build as-is; kill it first per `project_local_visual_verification_recipe`). 10-08's district e2e spec exists in `apps/web/e2e/` and its filename is read from 10-08's SUMMARY rather than guessed.</precondition>
  <read_first>
    - `apps/web/playwright.config.ts` in full — `use.baseURL = "https://sigmascout.org"`, the two-origin split and its CORS reason, the top-level `webServer` with `reuseExistingServer: true` and its stated staleness foot-gun, and every project's `testMatch` regex. 10-08 had to ADD its new spec to a desktop project and a 390px project; this gate is where that is confirmed to have actually happened.
    - 10-08's SUMMARY — the spec's exact filename and which projects it was assigned to.
    - Project memory `project_e2e_live_only_drift` (these specs hit `sigmascout.org` only, never CI; 40 silent failures were fixed 2026-09-14 to reach 170/170; rerun the whole family after UI changes, from the main context, after the deploy) and `project_pages_cache_poisoning` (curl hides a poisoned asset cache — look at the page in a real browser too).
  </read_first>
  <what-built>10-08 wrote the district e2e spec and assigned it to Playwright projects; 10-07 replaced the whole District Locks tab. This gate runs that spec, and the entire deployed-origin family, against the site `deploy.yml` just shipped.</what-built>
  <how-to-verify>Kill any stale `:4173` listener, then run `--list` and require a non-zero collected count on a desktop and a 390px project; then the spec; then the full family; then open `https://sigmascout.org/districts` in a real browser at desktop width and at 390 px.</how-to-verify>
  <action>
    First prove the spec RUNS, then run it, then run the whole family.

    `npx playwright test <spec> --list` is the gate that matters most here. A spec matched by no
    project's `testMatch` collects zero tests and exits 0 — indistinguishable from a pass in every
    output except the count. Read the count, and read which projects it lists.

    Then run the full suite, not just the new spec. The whole deployed-origin family drifts silently
    when the UI changes (40 specs once failed quietly), and Phase 10 replaced a whole tab.

    Finally, look at `https://sigmascout.org/districts` in a real browser: the Road to District Champs
    tab, a blue cell, the drawer histogram, the Rewind slider, and the same at 390 px. A poisoned
    asset cache renders a blank page that `curl` and Playwright's own request context can both miss.
  </action>
  <commands>
    (kill any process listening on 4173 before the first run)
    cd apps/web && npx playwright test e2e/<the 10-08 district spec> --list
    cd apps/web && npx playwright test e2e/<the 10-08 district spec>
    cd apps/web && npx playwright test
    curl -s -H "Origin: https://sigmascout.org" "https://sigmascout.org/districts" -o /dev/null -D -
  </commands>
  <verify>
    <automated>cd apps/web && npx playwright test</automated>
    <human-check>Jacob (or the orchestrator) opens https://sigmascout.org/districts in a real browser at desktop width and at 390 px and confirms the ledger renders with live data.</human-check>
  </verify>
  <acceptance_criteria>
    - `--list` reported a NON-ZERO test count for the district spec, and the project names it listed include one desktop-width project and one 390px project. A zero count means the spec is in no `testMatch` — report it as a 10-08 gap and stop; do not edit the config from inside this checkpoint.
    - The district spec's own run passed with zero failures, and its test count and project breakdown are recorded.
    - The FULL `npx playwright test` run is recorded: total tests, passes, failures, skips. The total is at or above the 170-test baseline plus the new spec's tests. Zero failures. Any failure is recorded verbatim with its project name and triaged to its owning plan.
    - The run started AFTER `deploy.yml` concluded green for the pushed SHA — both timestamps recorded.
    - `https://sigmascout.org/districts` returned a 200 with an HTML body (not an asset-cache variant) and the browser look confirms: the tab is labelled "Road to District Champs", two rows per team, at least one grey earned cell and one blue predicted cell, a drawer histogram on click, and the "Rewind to" slider present and moving. Confirmed at desktop width and at 390 px.
    - No `±` appears anywhere on the tab (10-CONTEXT's binding rule), confirmed by eye at both widths.
  </acceptance_criteria>
  <resume-signal>Jacob types "e2e green" with the counts, or names the failing spec and project.</resume-signal>
  <done>10-08's district spec provably RAN (non-zero collected tests on a desktop and a 390px project) and passed against the deployed site, the whole live e2e family is still green after a tab replacement, and a human has seen the real page render at both widths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: The record — measured district budget numbers, the operator line in STATE.md, and one scoped commit</name>
  <files>docs/publish-budget.md, .planning/STATE.md</files>
  <precondition>Tasks 1 to 7 are green and their observables are all recorded: suite counts, three silent typechecks, the Worker Version ID and deploy time, the district `generation` and publish time, the Task 5 decision and anything it left owed, both CI run IDs, and the e2e counts. `git status --short` is inspected before staging. HALT if the working tree holds edits this plan did not make — stage by explicit path only, never `git add -A` (commit `f0c7af48` absorbed a concurrent session's edits exactly that way).</precondition>
  <read_first>
    - `docs/publish-budget.md` § "What this document is", its "Changing a ceiling" / "never widen" rules, the district section 10-03 created and 10-06 filled with locally-measured numbers, § "Re-baseline cadence", and § "The machine-readable block" with its `json budget` fence (machine-written by `pnpm publish:seasons --write-budget`; district rows deliberately stay OUT of it so `--write-budget` cannot erase them).
    - `.planning/STATE.md`'s Session Continuity block — the shape of the existing entry, and project memory `project_state_md_seal_lines_go_stale` and `project_quick_tasks_table_pipes` (a `|` anywhere in a quick-task description breaks the append helper for every row; keep pipes out of anything written here).
    - Project memory `project_git_mv_drops_unstaged_edits` — always `git status` after committing.
  </read_first>
  <what-built>Tasks 1 to 7 produced a set of observables that exist only in this session's scrollback. This gate writes them into the two files the next session will read, and commits by explicit path.</what-built>
  <how-to-verify>Edit the two files, then run the `<commands>` list: `git status --short` before and after staging, `git show --stat HEAD` to prove only the named paths are in the commit, the push, and `gh run list --limit 4` for the re-run workflows.</how-to-verify>
  <action>
    Write down what happened, in the two places an operator will actually look, then commit only those
    paths.

    **`docs/publish-budget.md`.** Add this plan's REAL production numbers to the district section
    beside 10-06's locally-measured ones, each labelled with which run produced it: the object count,
    the largest district object's UTF-8 bytes and bytes-per-team, the events-baked count (0, with
    10-06's measured reason), and the `generation`. Keep district rows out of the machine-readable
    `json budget` block. If Task 5 took option-a, that block WAS rewritten by `--write-budget` and
    must be committed as-is — do not hand-edit it. If Task 5 took option-b, add the owed
    manifest regeneration to § "Re-baseline cadence" in one sentence: the next full republish is what
    puts `districtKey` in `live-windows.json` and switches the Worker's district pass on.

    **`.planning/STATE.md`.** Update the Session Continuity block with one operator line carrying
    every observable: Worker Version ID, district `generation`, the Task 5 branch and what it left
    owed, both CI run IDs, the e2e counts, and the honest status of the live district refresh (inert
    or live-capable, and unobserved either way). No pipe characters.

    Then stage those two paths explicitly and commit. The push that follows re-runs both workflows,
    including a Pages deploy of an unchanged bundle — that is expected and cheap, and
    `check:deployed-assets` re-verifies it.
  </action>
  <commands>
    git status --short
    git add -- docs/publish-budget.md .planning/STATE.md
    git status --short
    git commit -m "docs(10): phase 10 operator record and measured district budget"
    git show --stat HEAD
    git push origin main
    gh run list --limit 4
  </commands>
  <verify>
    <automated>git show --stat HEAD && gh run list --limit 4</automated>
    <human-check>Jacob reads the STATE.md operator line and confirms it says what actually happened, including anything owed.</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/publish-budget.md`'s district section carries BOTH 10-06's locally-measured numbers and this plan's production numbers, each labelled with its run. Every number traces to a run whose stdout is in a SUMMARY.
    - The machine-readable `json budget` block is byte-identical to its pre-task state UNLESS Task 5 took option-a, in which case it is the block `--write-budget` wrote, unedited. Confirmed by a diff scoped to that block.
    - Under option-b, `docs/publish-budget.md` § "Re-baseline cadence" names the owed manifest regeneration and what it switches on.
    - `.planning/STATE.md`'s Session Continuity block records, in prose with no pipe characters: the Worker Version ID and deploy time, the district `generation` and publish time (later than the deploy), the Task 5 branch plus anything owed (an option-a `--from prune` rerun, or the option-b manifest regeneration), both CI run IDs, the full and district e2e counts, and the plain statement that no real district fold has been observed in production.
    - `git show --stat HEAD` lists ONLY `docs/publish-budget.md` and `.planning/STATE.md` (plus a plan SUMMARY if the execution workflow adds one). A third unexpected path means a concurrent session's edit was absorbed: report it rather than pushing.
    - `git status --short` after the commit is clean of this plan's paths.
    - The push succeeded and `gh run list` shows both workflows green again for the new head SHA, including `check:deployed-assets`.
  </acceptance_criteria>
  <resume-signal>Jacob types "recorded" or names what the record gets wrong.</resume-signal>
  <done>Phase 10's operator outcome is written where the next session will find it — measured district bytes beside the synthesized ceilings, and one STATE.md line carrying every version id, generation, run id and count, plus the honest inert-or-live status of the district refresh and anything still owed — committed by explicit path and green in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` on disk -> any command's stdout or a transcript | Live credentials (the TBA key, the R2 access key pair). Every task in this plan runs commands that need them; none may render them. The git-side protections all passed the day this was broken, so passing `secrets-boundary.test.ts` is not evidence. |
| this working tree -> `origin/main` | A push publishes every commit in `origin/main..main`, including a concurrent session's, and `deploy.yml` deploys whatever lands. |
| this working tree -> the Cloudflare account | `wrangler deploy` (Worker code), `publishDistricts` (R2 objects), `rebaseline` (R2 objects, D1 rows). Irreversible against production data. |
| `data.sigmascout.org` / `sigmascout.org` -> a verification command | The edge cache and the Pages asset cache both sit in this path, so a response can be true of a previous deploy rather than this one. |
| GitHub Actions ubuntu runner -> this Windows machine's local result | Two different platforms, two different test outcomes; a local green has hidden a Linux red here for two days. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09-01 | Information Disclosure | `.env` rendered into a transcript by an operator command | critical | mitigate | No task calls `Read`, `cat`, `head`, `echo` or `less` on `.env`, and no task interpolates a value from it into a command, a log line or a commit message. Every credentialed command receives the file BY PATH (`tsx --env-file=.env`, `wrangler --env-file .env`) and reads it itself. Task 4's only probe of the key is a length report (`len=N`). The failure this guards against is recorded in `.claude/CLAUDE.md`: a Phase 4 executor read `.env` to copy it into a worktree, the live R2 pair landed in a transcript on disk, and the token had to be rotated — while every git-side check passed. |
| T-10-09-02 | Tampering | `git push` publishing and deploying another session's commits | high | mitigate | `git log --oneline origin/main..main` is enumerated in Task 1, RE-ENUMERATED in Task 6 immediately before the push, and every commit is named and attributed with no "and N others". Jacob explicitly approves any commit that is not Phase 10's, because `deploy.yml` will deploy the whole range. |
| T-10-09-03 | Repudiation | a local green standing in for a CI green | high | mitigate | Task 6 gates on `gh run list` showing BOTH workflows `success` for the PUSHED head SHA, not on the local suite. Root `pnpm typecheck` covers only the root tsconfig, so Task 1 typechecks `apps/web` and `apps/worker` separately — the gap `project_root_tsc_misses_web` records. `project_ci_red_after_local_green` is the incident: a Windows-only pass kept Linux CI red for two days. |
| T-10-09-04 | Denial of Service (self) | the R2 Class A monthly write allowance | high | mitigate | The district republish is O(100) objects. The ~109,000-write full republish is NOT run by default: it is Task 5's explicit option-a, gated on Jacob reading this month's Class A count in the Cloudflare dashboard first, with STATE.md's standing "deliberately not scheduled" note quoted in the decision body. Option-b spends nothing and leaves the Worker's district pass provably inert rather than broken. |
| T-10-09-05 | Tampering | the Pages asset cache serving `index.html` under an asset URL | high | mitigate | Task 6 requires `deploy.yml`'s own `check:deployed-assets` step green — it requests every referenced asset twice, bare and with a browser-shaped `Origin`, and fails on an HTML body, a non-200, a media-type disagreement or differing strong ETags. Task 7 adds a real-browser look, because plain `curl` reported the 2026-09-17 outage healthy. |
| T-10-09-06 | Spoofing | a false-green e2e run | high | mitigate | Task 7 runs `--list` FIRST and requires a non-zero collected count plus the expected project names: a spec in no `testMatch` collects zero tests and exits 0. Any stale `:4173` listener is killed first, because `reuseExistingServer: true` asserts against whatever build is already serving. The whole deployed family is rerun, not just the new spec (`project_e2e_live_only_drift`: 40 specs once failed silently). |
| T-10-09-07 | Tampering | the Worker deployed from a dirty tree | high | mitigate | Task 2's precondition requires an empty `git status --porcelain`; a dirty tree deploys from a clean detached worktree at the SHA Task 1 recorded and verified. The deployed SHA is recorded and compared against the pushed SHA in Task 6, so the bundle in production is always one that a recorded test run covered. |
| T-10-09-08 | Denial of Service (self) | a partial or missing D1 seed after a generation bump | medium | mitigate | Reachable only under Task 5 option-a. `scripts/rebaseline.ts` applies all four files in the order `publish:seasons` wrote them with `seed-cursors.sql` LAST (that file carries the markers that switch folding back on) and retries the known two-step `--file` upload failure. Task 5's acceptance requires a post-seed tick line with no `state-generation-mismatch` and no `LeagueRowShapeVersionError` naming `opr` or `epa`. |
| T-10-09-09 | Tampering | an unrelated concurrent edit absorbed into this plan's commit | medium | mitigate | Task 8 stages by explicit path (`git add -- docs/publish-budget.md .planning/STATE.md`), never `git add -A`, and asserts `git show --stat HEAD` lists only those paths. Incident: `f0c7af48` absorbed a foreign edit exactly this way. |
| T-10-09-10 | Repudiation | an unobserved capability recorded as a working one | medium | mitigate | Tasks 3, 5 and 8 each require the SUMMARY to state plainly that no real district fold has been observed in production and whether the pass is inert or live-capable. An inert pass is never recorded as evidence that the pass works — the strongest available claim is 10-05's committed test driving `runTick` itself. |
| T-10-09-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs NOTHING: no `pnpm add`, no lockfile change, no new dependency. CI installs with `pnpm install --frozen-lockfile`, so a drifted lockfile fails there rather than resolving silently. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
Run in this order; each line is a gate, not a suggestion.

1. `npx vitest run` from the REPO ROOT — zero failures, counts at or above 250 files / 5,550 tests.
2. `npx tsc --noEmit`, `npx tsc --noEmit -p apps/web/tsconfig.json`, `npx tsc --noEmit -p apps/worker/tsconfig.json` — all three silent.
3. `git log --oneline origin/main..main` — every commit named and attributed, before the deploy and again before the push.
4. `cd apps/worker && npx wrangler deploy` then `npx wrangler deployments list` — a new Version ID at 100%, two bindings, `schedule: * * * * *`.
5. Two consecutive tick lines, `ok: true`, carrying the four district counters.
6. `npx tsx --env-file=.env scripts/publishDistricts.ts --years 2016-2020,2022-2026 --dry-run` then `pnpm publish:districts` — publish time LATER than the deploy time.
7. The live `v1/districts/2026.json` and at least two `v1/district/{key}.json` bodies, fetched with an `Origin` header via GET, reporting the run's own `generation` and a non-zero count of teams with per-event `state`.
8. Task 5's decision recorded, and (option-a) the new `live-windows.json` generation with a non-zero `withDistrictKey` count plus a clean post-seed tick.
9. `git push origin main` then `gh run list --limit 6` — BOTH workflows `success` for the pushed head SHA, including `check:deployed-assets`.
10. `cd apps/web && npx playwright test e2e/<district spec> --list` — non-zero collected tests on a desktop and a 390px project; then that spec green; then the full `npx playwright test` family green.
11. A real browser at `https://sigmascout.org/districts`, desktop and 390 px — the ledger renders, the slider moves, no `±` anywhere.
12. `git show --stat HEAD` after Task 8 — only `docs/publish-budget.md` and `.planning/STATE.md`.
</verification>

<success_criteria>
- [ ] SC-7's six clauses each have ONE recorded observable: repo-root vitest counts; three silent typechecks; a Worker Version ID with a deploy timestamp earlier than the publish timestamp; a live district `generation` read back off `data.sigmascout.org`; two green CI run IDs for the pushed head SHA; a non-zero Playwright test count for the district spec against the deployed origin.
- [ ] The Worker was deployed BEFORE the district republish, proved by two recorded timestamps, and both directions of that ordering rule are stated in the plan.
- [ ] The district artifacts for every published season carry 10-03's state, award-profile and award-base-rate fields in production, verified by parsing the real bytes with a browser-shaped request rather than by trusting an exit code.
- [ ] Jacob's decision on the live-windows `districtKey` regeneration is recorded with the R2 write cost he weighed, and whichever branch he chose is executed and verified or recorded as owed in both `docs/publish-budget.md` and `.planning/STATE.md`.
- [ ] The production status of the live district refresh is recorded honestly — inert or live-capable, and unobserved either way — in every place this plan writes.
- [ ] No source file was modified by this plan. The only tracked edits are `docs/publish-budget.md` and `.planning/STATE.md`.
- [ ] No secret was rendered into any output stream, and no value read from `.env` appears in a command, a log line, a commit message or a SUMMARY.
</success_criteria>

<artifacts_this_phase_produces>
## No new source artifact

This plan writes no code, no schema, no test and no component. Everything it produces is either
DEPLOYED or PUBLISHED state plus two records.

**Deployed / published state (not files in the repo):**
- A new `sigmascout-worker` version carrying 10-05's district refresh pass, recorded by Version ID.
- Republished `v1/district/{districtKey}.json` for every published season and `v1/districts/{year}.json`, under one recorded `generation`, carrying 10-03's `state` / `awardProfile` / `awardBaseRates` fields and (branch-dependent) the inline `baked` pmfs or the `v1/district-presim/{districtKey}/{eventKey}.json` sidecars.
- The Pages deployment of the Road to District Champs tab, performed by `.github/workflows/deploy.yml` on push — there is no manual Pages step in this plan.
- Option-a only: a rewritten `v1/manifest/live-windows.json` carrying `districtKey`, a new algorithm generation, and a re-seeded D1.

**Files modified:**
- `docs/publish-budget.md` — the district section's real production numbers beside 10-06's locally-measured ones; under option-b, the owed manifest regeneration in the re-baseline cadence section; under option-a, the `--write-budget`-rewritten machine block.
- `.planning/STATE.md` — one operator line: Worker Version ID, district generation, the Task 5 branch and what it owes, both CI run IDs, the e2e counts, and the honest unobserved status of the live district refresh.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-district-points-ledger-the-road-to-district-champs-tab-from-/10-09-SUMMARY.md` when done.

It must carry, verbatim and unsummarized: the vitest counts; all three typecheck results; the full
`origin/main..main` inventory with attributions; the Worker Version ID, its bindings line and its
deploy timestamp; the two tick lines; every ingest pass's counts; the district dry-run and real-run
stdout with the publish timestamp; the parsed live-artifact field counts; the live-windows check
before the Task 5 decision and Jacob's selected option with its outcome; both CI run IDs; the
`--list` count and the full e2e counts; the committed paths; and one paragraph headed "What is owed"
naming every item left open (an option-a `--from prune` rerun, or the option-b manifest
regeneration) plus the standing fact that no real district fold has been observed in production.
</output>
