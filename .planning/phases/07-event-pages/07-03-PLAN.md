---
phase: 07-event-pages
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - packages/ingest/schemas.ts
  - packages/ingest/alliances.ts
  - packages/ingest/alliances.test.ts
  - packages/ingest/tbaClient.ts
  - packages/ingest/tbaClient.test.ts
  - packages/ingest/cli.ts
  - package.json
  - .planning/phases/07-event-pages/COVERAGE.md
autonomous: true
requirements: [EVNT-05]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`tbaAllianceResponseSchema` parses all three live-observed TBA body shapes without throwing — a populated array (38 of 40 sampled events), an empty array (`2025bc`, `2026wvrox`), and a bare `null` body (`2022ispr`) — because the top-level `.nullable()` is load-bearing exactly as it is on `tbaEventRankingsResponseSchema`, and an ingest run that aborts on TBA's honest 'no alliance selection' answer would be the same defect 06.1-RESEARCH.md Pitfall 2 already cost this project once."
    - "An alliance object whose `name` key is entirely ABSENT parses and normalizes to `name: null` — the live-observed `2024wvrox` case, where the alliance object's only keys are `declines`, `picks` and `status`. Absence, an explicit `null`, and an empty string are all the same fact ('TBA has no name for this alliance') and all three normalize to the single representation `packages/corpus/db.ts`'s `event_alliances.name` admits, which 07-02 fixed as SQL NULL and never an empty string. The `Alliance {n}` display fallback is 07-14's decision to make from an honest NULL; this layer never fabricates it."
    - "A 4-team alliance round-trips with `picks.length === 4` and the 4th team key at `picks[3]`. No schema field, no normalized field, no interface property and no executable identifier anywhere in `packages/ingest/alliances.ts`, `schemas.ts` or `cli.ts` names a fourth-pick concept TBA's raw response does not have — RESEARCH.md Q2 confirmed live that the response carries no separately-named field for it (D-16)."
    - "`ingestSeasonAlliancesOnly` reports five separate per-season counts — `populatedCount`, `nullBodyCount`, `emptyAlliancesCount`, `cacheHitCount`, `notFoundCount` — and they sum exactly to the season's corpus event count, so the tri-state accounting is a closed, checkable invariant rather than a log line nobody can falsify. (EVNT-05 `empty` probe, INGEST half — 07-02 owns the storage half, 07-14 owns the rendered half; see this plan's probe-edge accounting section.)"
    - "`allianceNumber` is assigned as the 1-based index of the alliance object in TBA's own response array, at normalize time, and is never parsed out of `name` (absent at `2024wvrox`) and never renumbered. (EVNT-05 `ordering` probe, INGEST half — 07-02's explicit `ORDER BY` is the storage half, 07-14's TBA-seed-order rendering is the rendered half.)"
    - "Re-running the alliances ingest over an event already ingested writes the same row set: `upsertEventAlliance`'s `(event_key, alliance_number)` conflict target merges rather than duplicates, so the season's `event_alliances` row count is unchanged by a second `--force` pass. (EVNT-05 `adjacency` probe, INGEST half — 07-02 owns the storage half, 07-14 the rendered half.)"
    - "A drifted TBA payload throws at the fetch boundary rather than being coerced: `picks` retyped from an array of strings to a single string, `picks` present but empty, or `declines` absent entirely each fail `tbaAllianceResponseSchema.parse`. This is `packages/ingest/schemas.ts`'s own stated header policy — a loud failure on TBA drift is the point — and it is what makes 07-02's 'never a row with an empty `picks` array' contract structurally true rather than dependent on a downstream skip nobody can see."
    - "`status` is stored verbatim as serialized JSON and is never modelled field-by-field, because RESEARCH.md Q2 observed its shape varying by `playoff_type` across values 0, 4, 8 and 10 — the same verbatim-source-JSON treatment `tbaMatchSchema.score_breakdown` already gets under D-05."
    - "`packages/ingest/tbaClient.ts`'s module header, its inline capability-section comment, and `tbaClient.test.ts`'s capability-surface case all state the same, currently-true count of exposed TBA capabilities — eleven after this plan adds `fetchEventAlliances`. All three were stale by two before this plan (team-media from Phase 6 and event-rankings from Phase 6.1 were both added without updating the count), which is a live instance of this project's own named failure mode: a document describing a shape the code no longer has."
    - "`packages/ingest/alliances.ts` is a pure transform with no I/O and no corpus import — it imports a type from `./schemas.js` and nothing else, mirroring `rankings.ts`'s own header claim ('this module has no I/O and no corpus import, mirroring media.ts's shape'), so the normalize rule stays testable without a database or a network."
    - "`.planning/phases/07-event-pages/COVERAGE.md` enumerates TBA v3's full event-scoped capability surface with `INTEGRATE` as the default, and every `OPT-OUT` row carries a non-empty one-line reason — an unreasoned opt-out is an undecided hole, and a malformed or absent matrix blocks the phase seal at `verify:pre`."
    - "No task in this plan reads, prints, copies or interpolates the contents of `.env`. The live TBA calls run through `tsx --env-file=.env`, which is the mechanism `package.json`'s existing `ingest:rankings` and `ingest:media` scripts already use; `TBA_API_KEY` is read only by `tbaApiKey()` via `process.env` and reaches only `tbaFetch`'s `X-TBA-Auth-Key` header, never a log line, a test name, an assertion message or a commit message."
    - statement: "The live two-season alliances ingest is provable only on a machine holding both the gitignored `data/corpus.sqlite` and a populated `.env`; CI has neither, so no CI run can reproduce it and no CI run may be read as having confirmed it. The reproducible half in CI is the fixture-based `alliances.test.ts`, which pins every shape the live run exercises."
      verification: backstop
    - statement: "Whether TBA's alliance response shape holds across all ~1,581 corpus events — rather than across RESEARCH.md's 40-event sample plus the two seasons ingested here — is proven only by 07-05's full 2022-2026 pass. RESEARCH.md Assumption A3 states this caveat explicitly; this plan narrows it from 40 events to two full seasons and hands the remainder to 07-05 rather than claiming it closed."
      verification: backstop
  artifacts:
    - packages/ingest/schemas.ts
    - packages/ingest/alliances.ts
    - packages/ingest/alliances.test.ts
    - packages/ingest/tbaClient.ts
    - packages/ingest/tbaClient.test.ts
    - packages/ingest/cli.ts
    - package.json
    - .planning/phases/07-event-pages/COVERAGE.md
  key_links:
    - "`tbaAllianceResponseSchema` (this plan) -> `normalizeEventAlliances` (this plan) -> `upsertEventAlliance` (07-02) -> `selectEventAlliancesForSeason` (07-02) -> `EventAllianceSchema` (07-07) -> `AlliancesTab.tsx`'s Captain / Pick 2 / Pick 3 / 4th-pick columns and D-15's three-pick sigma arithmetic (07-14). One ordered `picks` array carries the whole chain; a reorder or a renumber at this layer silently reassigns every captain on the site."
    - "`normalizeEventAlliances`'s 1-based response-array index <-> `event_alliances.alliance_number` <-> 07-02's explicit `ORDER BY ea.alliance_number` <-> 07-14's TBA-seed-order rendering — four layers agreeing that TBA's array position is the seed, with `name` never consulted because it is sometimes absent."
    - "`ingestSeasonAlliancesOnly`'s five counters <-> `ingestSeasonRankingsOnly`'s existing tally shape — one tri-state model for both endpoints (PD-02), so a future reader does not have to learn two absent-data vocabularies for the same three TBA answers."
    - "`packages/ingest/tbaClient.ts`'s stated capability count <-> `tbaClient.test.ts`'s capability-surface case <-> `COVERAGE.md`'s INTEGRATE rows — three places that must name the same set, and the only reason the stale count was findable at all is that all three exist."
    - "`COVERAGE.md`'s measured-cost section <-> 07-05's per-season tri-state verification — 07-05 sees 2022 and 2024 as ETag cache hits because this plan fetched them fresh, so this plan's recorded figures ARE 07-05's evidence for those two seasons. Recorded deliberately, applying 06.1-04's note [3] lesson rather than repeating it."
  prohibitions:
    - statement: "MUST NOT invent an alliance identity the source did not provide. No synthesized `Alliance {n}` name written at ingest, no absent `name` coerced to an empty string so a type gets simpler, no fourth-pick field TBA's response does not carry, and no placeholder row for an event that never held an alliance selection. Every one of these makes an absence look like a measured fact to every downstream reader, and D-17's disabled tab, D-16's three-pick arithmetic and 07-14's fallback label all depend on the absence being real."
      status: unverified
      verification: test
    - statement: "MUST NOT absorb a failure or a distinction to keep a long run quiet. A drifted TBA payload throws rather than being coerced past the parse boundary; a null body and an empty array stay separately tallied rather than collapsed into one 'no data' count. This project's failure log names 'no evaluation harness' as a founding failure, and a silent bad-parse inside a 1,581-event run is that failure wearing an ingest costume."
      status: unverified
      verification: test
    - statement: "MUST NOT leave this repo's own description of its TBA capability surface disagreeing with the surface it actually exposes. The module header, the capability-surface test and COVERAGE.md are the three places that state it, and all three were already stale by two endpoints before this plan touched them. 'The README described a model that had been deleted' is a verbatim entry in this project's inherited failure log; letting the same drift widen by a third endpoint while adding that endpoint is choosing to repeat it."
      status: unverified
      verification: test
---

<objective>
New `/event/{key}/alliances` TBA ingest (D-18.7): `tbaAllianceResponseSchema` in
`packages/ingest/schemas.ts` (distinct from the existing per-match `tbaAllianceSchema`),
`packages/ingest/alliances.ts` normalize mirroring `rankings.ts`, `alliances.test.ts` mirroring
`rankings.test.ts`, and `ingestSeasonAlliancesOnly` in `cli.ts` reusing
`ingestSeasonRankingsOnly`'s exact `populatedCount`/`nullBodyCount`/`emptyAlliancesCount`/
`cacheHitCount` tri-state tally. Tests encode the two live-probe findings: `name` is sometimes
**absent** (not `""`), and a 4th pick is `picks[3]` with no separate field for it. **Also authors
`COVERAGE.md`** — the TBA event-scoped API coverage matrix.

Purpose: this is the only plan in Phase 7 that opens a genuinely new third-party boundary. TBA's
`/event/{key}/alliances` has never been called by this repo — there is no fetch helper, no Zod
schema, no normalize rule and no CLI mode for it. 07-02 built the table that receives the rows;
07-05 spends the full-corpus live pass that fills it for 2022-2026; 07-08 reads it into the
published event artifact; 07-14 renders it. Everything downstream of here inherits whatever shape
this plan decides, and 07-05's pass is expensive enough that a shape error found afterwards costs
that pass again.

The risk this plan actually carries is a silent one. Third-party JSON crossing a new parse boundary
either becomes a durable local fact or becomes a loud failure, and there is no third option that is
honest. RESEARCH.md Q2 measured the three answers TBA really gives across 40 live events — 37
populated, 2 empty arrays, 1 null body — plus two shape findings that a naive schema gets wrong
(`name` absent entirely rather than empty, and a 4th pick living at `picks[3]` with no field of its
own). Task 1 encodes all five of those facts as tests before any of them can become an assumption.

**Execution constraint:** this plan must run in the **main working tree, not a git worktree.**
Task 2 makes live TBA calls (needs `.env`) and writes to `data/corpus.sqlite` (gitignored). Neither
file is copied into a worktree, and rows written inside one do not merge back.

Output: a parsed, normalized, tallied `/event/{key}/alliances` ingest proven against two full live
seasons, and a committed capability matrix recording every event-scoped TBA capability this project
takes and every one it declines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-pages/07-CONTEXT.md
@.planning/phases/07-event-pages/07-RESEARCH.md
@.planning/phases/07-event-pages/07-PATTERNS.md
@.planning/phases/07-event-pages/07-VALIDATION.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Newly-created symbols and paths, listed so a source-grounding pass can exclude them from drift
verification. **This plan creates:**

| Kind | Name | Location |
|---|---|---|
| schema | `tbaAllianceEntrySchema` | `packages/ingest/schemas.ts` |
| type | `TbaAllianceEntry` | `packages/ingest/schemas.ts` |
| schema | `tbaAllianceResponseSchema` | `packages/ingest/schemas.ts` |
| type | `TbaAllianceResponse` | `packages/ingest/schemas.ts` |
| function | `fetchEventAlliances` | `packages/ingest/tbaClient.ts` |
| file | `packages/ingest/alliances.ts` | new |
| interface | `NormalizedEventAlliance` | `packages/ingest/alliances.ts` |
| function | `normalizeEventAlliances` | `packages/ingest/alliances.ts` |
| file | `packages/ingest/alliances.test.ts` | new |
| function | `ingestSeasonAlliancesOnly` | `packages/ingest/cli.ts` |
| CLI flag | `--alliances-only` | `packages/ingest/cli.ts` |
| interface field | `CliOptions.alliancesOnly` | `packages/ingest/cli.ts` |
| npm script | `ingest:alliances` | `package.json` |
| file | `COVERAGE.md` | `.planning/phases/07-event-pages/COVERAGE.md` |

Sibling plans in this phase additionally create: `event_alliances` and its `CorpusEventAlliance`/
`EventAllianceSelection`/`upsertEventAlliance`/`selectEventAlliancesForSeason` accessors plus the
four `event_rankings` record/ranking-score columns (07-02, `packages/corpus/`); `EventAllianceSchema`
and the `rank`/`record`/`rp` fields on `EventTeamSchema` (07-07, `packages/harness/pageArtifacts.ts`);
the `--include-offseason` CLI flag (07-09, `packages/harness/publish.ts`); and `AlliancesTab.tsx`
(07-14, `apps/web/src/components/event/`).

## Scope boundaries (what this plan deliberately does NOT do)

- **No corpus schema change.** `event_alliances`, `CorpusEventAlliance`, `EventAllianceSelection`,
  `upsertEventAlliance` and `selectEventAlliancesForSeason` are 07-02's and already exist when this
  plan runs. Use those exact names; do not redefine, widen or re-derive any of them. `git diff` on
  `packages/corpus/` must be empty for this whole plan.
- **No rankings-ingest extension.** Widening `ingestSeasonRankingsOnly` to persist TBA's `record`
  and `sort_orders[0]`, and the `sort_order_info[0].name === "Ranking Score"` assertion that guards
  it, are 07-04's. This plan touches `ingestSeasonRankingsOnly` for exactly nothing.
- **No full-corpus pass.** 07-05 owns the real 2022-2026 run for both endpoints. This plan runs two
  seasons, deliberately chosen (see Task 2), as a live tracer — the same 06.1-01 -> 06.1-04
  relationship the rankings ingest already had.
- **No published-artifact change and no rendering.** `EventAllianceSchema` is 07-07's,
  `buildEventArtifact`'s alliance read is 07-08's, `AlliancesTab.tsx` and D-17's disabled tab are
  07-14's.
- **`COVERAGE.md` is authored at plan time, not at execution time.** The api-coverage gate requires
  the matrix before the plan is finalized, so both capability tables and all seven bracketed notes
  are already written and committed alongside this PLAN.md. Task 3 does not author them — it
  reconciles them against the code Task 1 ships and fills the measured-cost section, which the
  planner could not know. This is 06.1-04's two-step (matrix first, real figures after the run)
  with the first step moved earlier to satisfy the gate.
- **No `checkpoint:decision`.** Per the outline's Notes, this phase's two one-way doors are gated at
  07-17 (the republish write pass) and 07-19 (the destructive delete pass). Every decision in this
  plan is rated below and none is one-way.
- **No UI-SPEC `## UI Considerations` rows.** All 55 rows are scoped to the seven rendered surfaces
  E1-E7; this plan renders nothing. Recorded explicitly so the row accounting shows a reasoned zero
  rather than a silent drop.

## Probe-edge accounting for this plan

The phase's 17 probe edges are assigned by requirement to the plan owning the surface each
constrains (outline assumption 14): EVNT-05's three — `empty`, `ordering`, `adjacency` — belong to
**07-14**, which owns the rendered contract. Each of those three also has a **storage** half already
authored into 07-02's `must_haves.truths`, and an **ingest** half that is decided here and nowhere
else. This plan authors those three ingest halves as `must_haves.truths` above, each carrying an
explicit cross-reference naming which plan owns the storage half and which owns the rendered half:

| EVNT-05 probe | ingest half (this plan) | storage half | rendered half |
|---|---|---|---|
| `empty` | five counters summing to the season event count; zero upserts for null body and for empty array | 07-02 (zero rows, no placeholder) | 07-14 (D-17 disabled tab) |
| `ordering` | `allianceNumber` = 1-based index in TBA's response array, never parsed from `name` | 07-02 (explicit `ORDER BY`) | 07-14 (TBA seed order) |
| `adjacency` | a re-run merges rather than duplicates; two events keep separate rows | 07-02 (`ON CONFLICT` merge) | 07-14 (rendered adjacency) |

Nothing here is double-counted against 07-14's budget — 07-14 still owns all three of EVNT-05's rows
in the 17-edge total — and nothing is dropped, because the ingest-layer answer to each probe is
recorded rather than assumed to be somebody else's.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The alliances parse boundary — schema, fetch helper, normalize rule, and the Wave 0 test file</name>
  <files>packages/ingest/schemas.ts, packages/ingest/alliances.ts, packages/ingest/alliances.test.ts, packages/ingest/tbaClient.ts, packages/ingest/tbaClient.test.ts</files>
  <read_first>
    - `packages/ingest/schemas.ts` — the file being modified. Read it in full. Two blocks matter most: the module header's stated policy (a parse failure throws; a loud failure on TBA drift is the point, never a silently coerced default), and `tbaEventRankingSchema`/`tbaEventRankingsResponseSchema` at the end of the file — the doc-comment convention to copy and the load-bearing top-level `.nullable()` to mirror. Also read the module-private `tbaAllianceSchema` near line 57: it is the per-MATCH red/blue roster (`team_keys`/`surrogate_team_keys`/`dq_team_keys`/`score`), an entirely different thing from the endpoint this task models, and `tbaMatchSchema.score_breakdown: z.unknown()` immediately below it is the verbatim-source-JSON precedent (D-05).
    - `packages/ingest/rankings.ts` — read the whole 36-line file. Its header states the exact contract this task's sibling must mirror: both absent cases are real distinct answers rather than parse failures, neither is coerced into the other, the function never throws, distinguishing them for logging is `cli.ts`'s job, and the module has no I/O and no corpus import.
    - `packages/ingest/rankings.test.ts` — read the whole 133-line file. The `rankingEntry(overrides)`/`rankingsResponse(overrides)` fixture-factory pair at lines 12-40, then the two describe blocks: schema-parse cases (null body yields null without throwing; several real season shapes parse; three separate drift cases throw) and `normalizeEventRankings` cases (populated to N records, null to `[]`, empty array to `[]` as a SEPARATE case, single-entry smallest non-empty pool). This is the structure to mirror case for case.
    - `packages/ingest/tbaClient.ts` — the file being modified. Read the module header (lines 1-15), the inline capability-section comment above `fetchStatus` (~line 117), and `fetchEventRankings` (~lines 188-201) whose shape `fetchEventAlliances` copies exactly. Count the exported fetch helpers as you read: the header's stated capability count is already wrong before this task touches it.
    - `packages/ingest/tbaClient.test.ts` — the file being modified. Read the `capability surface` describe block (lines 114-174): the `fetchMock`/`ctx` harness, the case asserting the exposed capability set, and `fetchTeamMedia`'s own path-plus-conditional-header case, which is the shape the new `fetchEventAlliances` case copies.
    - `packages/corpus/db.ts` — read `CorpusEventAlliance` and `EventAllianceSelection` as 07-02 shipped them (immediately after the `event_rankings` accessors). The normalized shape this task produces must supply every non-key field `upsertEventAlliance` takes, under those exact property names. Do not modify this file.
    - `.planning/phases/07-event-pages/07-RESEARCH.md` § Code Examples, "Live TBA `/event/{key}/alliances` response shape (verified 2026-08-27)", and § Open Questions, Question 2 in full — the real field set, the absent-`name` finding at `2024wvrox`, the `picks[3]` finding, the `status`-varies-by-`playoff_type` finding, and the 37/2/1 three-state split across the 40-event sample.
    - `.planning/phases/07-event-pages/07-CONTEXT.md` § Alliances tab (D-15, D-16, D-17) and § Published-data additions item 7 — what the parsed shape has to be able to feed, and the explicit warning that the existing `tbaAllianceSchema` is a different thing.
    - `.planning/phases/07-event-pages/07-02-PLAN.md` — its `must_haves.truths` and its Task 1 action, for the storage contract this task's output has to satisfy: NULL never an empty string, zero rows never a placeholder, `picks` verbatim and ordered, no team-key foreign key.
  </read_first>
  <behavior>
    - A bare `null` response body parses to `null` without throwing, and `normalizeEventAlliances(null)` returns `[]`.
    - An empty array response parses, and normalizes to `[]` — asserted as a SEPARATE case from the null-body case, never folded into it.
    - A populated response of N alliance objects normalizes to N records whose `allianceNumber` values are `1..N` in the response array's own order.
    - An alliance object with no `name` key at all parses, and normalizes to `name: null`. A second case asserts the normalized value is not an empty string.
    - An alliance object whose `name` is an explicit `null` normalizes to `name: null`; an alliance object whose `name` is `""` also normalizes to `name: null`; an alliance object with a real name string preserves it verbatim.
    - A 4-team alliance normalizes with `picks.length === 4` and `picks[3]` equal to the 4th team key; a 3-team alliance normalizes with `picks.length === 3`. Element order is preserved exactly as sent.
    - `status` normalizes to a string that `JSON.parse` round-trips back to the original object; an alliance object with no `status` key normalizes to `statusRaw: null`.
    - `declines` normalizes to the array as sent, including the empty array observed in all 40 sampled events.
    - Drift throws, as three separate cases: `picks` retyped from an array of strings to a single string; `picks` present but empty; `declines` absent entirely.
    - Normalizing a response whose alliance objects appear in a given order never reorders them and never renumbers them.
    - `fetchEventAlliances` issues `/event/{key}/alliances` and forwards a supplied cached ETag as an `If-None-Match` request header.
  </behavior>
  <action>
Write the tests first, in `packages/ingest/alliances.test.ts`, and confirm each fails for the right
reason before writing any implementation. Mirror `rankings.test.ts` case for case: a module header
comment naming this plan and the RESEARCH.md live probe it draws from, one fixture factory per shape
(`allianceEntry(overrides)` and `alliancesResponse(overrides)`, matching `rankingEntry`/
`rankingsResponse`'s pattern), then a `describe` for the schema and a `describe` for
`normalizeEventAlliances`. Seed the default fixture from the real `2022roe` response RESEARCH.md
recorded, so the fixture is a transcription of something TBA actually sent rather than an invention.

**In `packages/ingest/schemas.ts`**, appended after the rankings block so related shapes stay
contiguous, add:

`export const tbaAllianceEntrySchema` — a `z.object` with exactly four keys.
`declines: z.array(z.string())` required. `name: z.string().nullish()`.
`picks: z.array(z.string()).min(1)`. `status: z.unknown()`.

Each of those four choices needs its reason in the doc comment, because a later reader has no other
source for them:

- `name` is `.nullish()` — not required, not `.optional()` alone, and never given a `.default()`.
  RESEARCH.md Q2 observed the key ABSENT entirely at `2024wvrox`, where the alliance object's only
  keys are `declines`, `picks` and `status`. `.nullish()` additionally tolerates an explicit `null`
  without aborting a 1,581-event run over a purely cosmetic label. A `.default()` of any kind is
  forbidden here: it would make an absence indistinguishable from a value at every layer downstream.
- `picks` carries `.min(1)`. An alliance object with zero picks is not an alliance, was never
  observed across the 38 populated events sampled, and could not be stored anyway — 07-02's
  `event_alliances` contract forbids a row with an empty picks array. Rejecting it at the parse
  boundary is what makes that contract structurally true instead of dependent on a downstream skip.
  Do NOT constrain the maximum: 3 and 4 were both observed, and a length ceiling would turn a future
  format change into a parse failure over something this pipeline does not care about.
- `status` is `z.unknown()`, the same treatment `tbaMatchSchema.score_breakdown` already gets under
  D-05. RESEARCH.md Q2 observed its shape varying with `playoff_type` across values 0, 4, 8 and 10,
  with only `status.status`, `status.record`, `status.current_level_record` and `status.level`
  reliably present. Modelling it field-by-field would make a future playoff format a parse failure;
  storing it whole keeps the provenance without the brittleness.
- `declines` is required. It was present, as an empty array, in all 40 sampled events, and
  `event_alliances.declines` is `NOT NULL` — a missing key is genuine drift that a NOT NULL column
  cannot honestly absorb, and this file's header policy says drift throws.

`export type TbaAllianceEntry = z.infer<typeof tbaAllianceEntrySchema>;`

`export const tbaAllianceResponseSchema = z.array(tbaAllianceEntrySchema).nullable();` with a doc
comment stating that the top-level `.nullable()` is load-bearing and non-negotiable for the identical
reason `tbaEventRankingsResponseSchema`'s is: TBA returns HTTP 200 with a bare `null` body for an
event with no alliance structure at all, observed live at `2022ispr`, and a schema that throws on it
converts TBA's honest answer into an aborted ingest run.

`export type TbaAllianceResponse = z.infer<typeof tbaAllianceResponseSchema>;`

Name-collision note to put in the doc comment: this is deliberately NOT the module-private
`tbaAllianceSchema` earlier in this same file, which models a single match's red or blue roster
(`team_keys`/`surrogate_team_keys`/`dq_team_keys`/`score`). Two different TBA concepts share the word
"alliance"; the response schema must never be substituted for the per-match one or vice versa.

**In `packages/ingest/tbaClient.ts`**, add `export function fetchEventAlliances(ctx: TbaClientContext,
eventKey: string, cachedEtag?: string): Promise<TbaFetchResult>` immediately after
`fetchEventRankings`, delegating to `tbaFetch` with the path `/event/${eventKey}/alliances` and the
same `ctx.apiKey`, `cachedEtag`, `ctx.counter`, `ctx.baseUrl` arguments. Its doc comment names
D-18.7 and plan 07-03 and states that it is event-scoped — one request per event covers every
alliance at it — mirroring `fetchEventRankings` rather than `fetchTeamMedia`'s per-entity loop.

Then correct this file's own stated capability inventory, which is already wrong. The module header
(lines 8-14) and the inline section comment above `fetchStatus` both claim the client exposes the
**eight capabilities** COVERAGE.md marks INTEGRATE and enumerate them; that enumeration predates
`fetchTeamMedia` (Phase 6) and `fetchEventRankings` (Phase 6.1), both of which were added without
updating it. <!-- planner-discipline-allow: eight capabilities --> Rewrite both to state **eleven**
and to enumerate all eleven: status, teams-list (paginated), team-detail, events-list, event-detail,
event-teams, event-matches, match-detail, team-media, event-rankings, event-alliances. Add a
parenthetical naming which phase added each of the last three, so the next addition has an obvious
place to append rather than an obvious place to forget.

**In `packages/ingest/alliances.ts`** (new), mirroring `rankings.ts`'s shape and length:

A module header stating the contract in `rankings.ts`'s own voice — the whole response can be a bare
`null` body and separately the array can be genuinely empty; both are real distinct answers rather
than parse failures; this function returns `[]` for either, never coercing one into the other and
never throwing; distinguishing them for logging is `cli.ts`'s job; and this module has no I/O and no
corpus import. Cite D-18.7, EVNT-05 and plan 07-03, and name the two live-probe findings the
normalize rule exists to respect.

`export interface NormalizedEventAlliance` with `allianceNumber: number`, `name: string | null`,
`picks: string[]`, `declines: string[]`, `statusRaw: string | null` — every non-key field
`upsertEventAlliance` takes, under 07-02's exact property names. `eventKey` and `fetchedAt` are
deliberately absent: the caller supplies both, exactly as `NormalizedEventRanking` omits them.

`export function normalizeEventAlliances(response: TbaAllianceResponse | null): NormalizedEventAlliance[]`
— returns `[]` when the response is `null` or has length 0, and otherwise maps each entry with its
zero-based array index `i` to `allianceNumber: i + 1`. Its doc comment must state the four rules a
reader cannot recover from the code alone:

- `allianceNumber` is TBA's own seed order, taken from the response array position. It is never
  parsed out of `name`, because `name` is absent entirely at some events.
- `name` collapses `undefined`, `null` and `""` to a single `null`. All three are the same fact and
  07-02's storage contract admits exactly one representation of it. Any other string passes through
  verbatim. Fabricating an `Alliance {n}` label is 07-14's decision to make from an honest NULL, and
  is forbidden here.
- `picks` and `declines` pass through as the arrays TBA sent, order intact, with no filtering,
  de-duplication or length branching. `picks[0]` is the captain; a 4th team, where one exists, is
  `picks[3]`. TBA's response has no separate field for it (D-16), so neither does this interface.
  <!-- planner-discipline-allow: backup -->
- `statusRaw` is `JSON.stringify` of `status` when present and `null` when absent — the verbatim
  provenance 07-02's `status_raw` column stores. Nothing in Phase 7 reads it; it is kept so a later
  consumer of alliance status does not need another full-corpus live pass to get it.

**In `packages/ingest/tbaClient.test.ts`**, add `fetchEventAlliances` to the capability-surface
case's call list and to its expected-path array (`/api/v3/event/2024casj/alliances`), update that
case's name to state eleven, and add a sibling case in `fetchTeamMedia`'s shape asserting that a
supplied cached ETag is forwarded as `If-None-Match`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ingest/alliances.test.ts packages/ingest/tbaClient.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/ingest/alliances.test.ts packages/ingest/tbaClient.test.ts` passes with 0 skipped, and `alliances.test.ts` contains at least 16 cases split across a schema describe block and a `normalizeEventAlliances` describe block.
    - `pnpm typecheck` exits 0.
    - `grep -c 'export const tbaAllianceResponseSchema' packages/ingest/schemas.ts` prints `1`, and `grep -c 'export const tbaAllianceEntrySchema' packages/ingest/schemas.ts` prints `1`.
    - The pre-existing per-match schema is untouched and still module-private: `grep -c 'const tbaAllianceSchema = ' packages/ingest/schemas.ts` prints `1` and `grep -c 'export const tbaAllianceSchema' packages/ingest/schemas.ts` prints `0`. The new code never references it: `grep -vE '^\s*(\*|//|/\*)' packages/ingest/alliances.ts packages/ingest/cli.ts | grep -cE 'tbaAllianceSchema\b'` prints `0` \u2014 comment lines are filtered because the new response schema's own doc comment is required to name it, since warning a reader off the collision is that comment's whole purpose. <!-- planner-discipline-allow: tbaAllianceSchema -->
    - No implementation file names a fourth-pick concept TBA does not have: `grep -vE '^\s*(\*|//|/\*)' packages/ingest/alliances.ts packages/ingest/schemas.ts packages/ingest/cli.ts | grep -icE '\bbackup\b'` prints `0`. `packages/ingest/alliances.test.ts` is deliberately excluded from this grep — a case name must be able to say what it is ruling out. <!-- planner-discipline-allow: backup -->
    - A case parses an alliance object whose keys are exactly `declines`, `picks`, `status` (no `name` key) without throwing, and a paired case asserts the normalized `name` is `null` via `toBeNull()`; a third case asserts it is not `""` via an explicit `not.toBe("")`.
    - Separate cases assert that `name: null` and `name: ""` each normalize to `null`, and that a real name string round-trips verbatim.
    - A case asserts a 4-pick alliance normalizes with `picks` of length 4 and `picks[3]` equal to the 4th team key written, and a separate case asserts a 3-pick alliance normalizes with `picks` of length 3.
    - `normalizeEventAlliances(null)` and `normalizeEventAlliances([])` are asserted in two SEPARATE cases, each returning `[]`, neither folded into the other.
    - A case builds a 5-alliance response and asserts the normalized `allianceNumber` array is exactly `[1, 2, 3, 4, 5]` in the response's own order, with the input alliance objects carrying names that do NOT match their positions — so the assertion proves the index is the source, not the name.
    - Three drift cases each assert `tbaAllianceResponseSchema.parse` throws: `picks` retyped to a single string, `picks` present but empty, and `declines` omitted.
    - A case asserts `JSON.parse(normalized.statusRaw)` deep-equals the original `status` object, and a separate case asserts an entry with no `status` key normalizes to `statusRaw: null`.
    - `packages/ingest/alliances.ts` imports nothing but a type from `./schemas.js`: `grep -cE '^import' packages/ingest/alliances.ts` prints `1`, and `grep -vE '^\s*(\*|//|/\*)' packages/ingest/alliances.ts | grep -cE 'corpus|node:|better-sqlite3'` prints `0` \u2014 comment lines are filtered because the module header is required to state in prose that it has no corpus import, mirroring `rankings.ts`'s own header.
    - The client's stated capability inventory is current: `grep -c 'eight capabilities' packages/ingest/tbaClient.ts` prints `0`, `grep -ci 'eleven' packages/ingest/tbaClient.ts` prints at least `2` (module header and inline section comment), and `grep -c 'event-alliances' packages/ingest/tbaClient.ts` prints at least `1`. <!-- planner-discipline-allow: eight capabilities -->
    - `tbaClient.test.ts`'s capability-surface case includes `/api/v3/event/2024casj/alliances` in its expected-path array and its case name states eleven rather than the prior count.
    - `git diff --stat packages/corpus/` is empty — this task redefines nothing 07-02 shipped.
  </acceptance_criteria>
  <reversibility rating="costly">Reversible as source at this commit — deleting the schema, helper and normalize rule restores the tree exactly. It becomes costly at 07-05, whose full-corpus live TBA pass over ~1,581 events materializes this shape into ~11,000 durable rows; a field-set change after that point costs that pass again. Not one-way: nothing published, no R2 key and no D1 row depends on it yet. Rated on the cost it actually carries rather than the cost at the moment of writing.</reversibility>
  <done>`tbaAllianceEntrySchema`/`tbaAllianceResponseSchema` are exported from `schemas.ts` and distinct from the per-match schema, `fetchEventAlliances` is exported from `tbaClient.ts` with the client's capability inventory corrected to eleven, `normalizeEventAlliances` is a pure transform in a new `alliances.ts`, and every behavior above is pinned by a passing case in `alliances.test.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: `ingestSeasonAlliancesOnly`, its closed five-count tally, and a live two-season proof</name>
  <files>packages/ingest/cli.ts, package.json</files>
  <precondition>`.env` exists in the repo root with a populated `TBA_API_KEY`, `data/corpus.sqlite` exists locally carrying 07-02's `event_alliances` table and the 2022 and 2024 rows of the `events` table, and no other ingest, publish or harness process holds `data/corpus.sqlite.lock`. This task must run in the main working tree — both `.env` and the corpus are gitignored, are not copied into a git worktree, and rows written inside one do not merge back.</precondition>
  <read_first>
    - `packages/ingest/cli.ts` — the file being modified. Read four regions in full. The module header (lines 1-29) whose usage block documents `--events-only`, `--media-only` and `--rankings-only` and is the convention a new mode's usage line follows. `CliOptions` and `parseCliOptions` (lines 80-150) — the three existing `*-only` flags, their `?? false` defaults, and the three return branches each mode must be threaded through. `ingestSeasonMediaOnly`'s 404 handling and its `notFoundCount` (roughly lines 260-300). And `ingestSeasonRankingsOnly` in full (roughly lines 318-417) — its `SELECT event_key FROM events WHERE year = ?` iteration, its 404-skip, its 304 `cacheHitCount` early-continue, its parse-then-branch tri-state, its `writeEtag` placement, and its single closing `console.log`. Finally `main()`'s dispatch chain (roughly lines 460-535), where each `*-only` branch calls its season loop and re-records the ingest run.
    - `packages/corpus/db.ts` — `upsertEventAlliance` and `CorpusEventAlliance` as 07-02 shipped them, to confirm the exact call-site property names and that `eventKey` and `fetchedAt` are the caller's to supply. Do not modify this file.
    - `packages/ingest/alliances.ts` and `packages/ingest/schemas.ts` — this task's own Task 1 output, read so the call site is written against the real signatures rather than remembered ones.
    - `package.json` — the file being modified. Read the `scripts` block: `ingest`, `ingest:media` and `ingest:rankings` are the three-line pattern the new script copies, including the `tsx --env-file=.env` prefix that is how every credential in this repo reaches a process.
    - `.claude/CLAUDE.md` § Conventions, "Secrets handling" in full — the binding rule for this task, and the incident that produced it.
    - `.planning/phases/07-event-pages/07-RESEARCH.md` § Open Questions, Question 2 — the three-state coverage counts and the four named events this run is chosen to exercise.
    - `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` § "The recurring cost this integration commits to" and its note [3] — the measured per-request cost this run is budgeted against, and the specific blind spot a cached season creates for a later full pass, which this task is structured to avoid repeating.
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-01-SUMMARY.md` — the single-season live tracer (2024 rankings, 280/324 events) whose relationship to 06.1-04's full pass this task's relationship to 07-05 copies.
  </read_first>
  <action>
**In `packages/ingest/cli.ts`**, add `--alliances-only` alongside the three existing single-purpose
modes, threading it identically: an `alliancesOnly: boolean` field on `CliOptions` with a doc comment
naming EVNT-05, D-18.7 and plan 07-03; `"alliances-only": { type: "boolean", default: false }` in
`parseCliOptions`'s options object; a local `const alliancesOnly = values["alliances-only"] ?? false;`
alongside the other three; and the field added to all three of `parseCliOptions`'s return branches
(the `--event` branch, the `--years` branch and the `--year` branch). Add a usage line to the module
header in the existing block's voice, naming `pnpm ingest:alliances --years 2022-2026` and stating
that it resolves each event's playoff alliance selection via `/event/{key}/alliances`, one request
per event, and stores the result in `event_alliances`.

Add `async function ingestSeasonAlliancesOnly(db: Corpus, ctx: TbaClientContext, year: number, force:
boolean): Promise<void>` placed immediately after `ingestSeasonRankingsOnly`, structurally identical
to it:

- Iterate the corpus's OWN events for the season via `SELECT event_key FROM events WHERE year = ?`,
  the same iteration shape, and deliberately do not filter offseason events — 06.1's PD-01 decided
  that question for this pipeline and `COVERAGE.md` records it as an explicit row; RESEARCH.md Q2's
  live probe found offseason events are exactly where the two empty-array cases live, so excluding
  them would hide the absent-data case D-17 is designed around.
- Build the URL as `/event/${eventKey}/alliances`, call `fetchEventAlliances(ctx, eventKey,
  cachedEtagFor(db, alliancesUrl, force))` inside a `try`, and on an error whose message matches
  `/HTTP 404/` log the skip and `continue` — the same placeholder/unregistered-event-key handling
  `ingestSeasonRankingsOnly` and `ingestSeasonMediaOnly` both already use. Rethrow anything else.
- On `result.status === 304`, increment `cacheHitCount` and `continue`.
- Otherwise `tbaAllianceResponseSchema.parse(result.body)` and branch the tri-state exactly as the
  rankings function does: `parsed === null` increments `nullBodyCount`; `parsed.length === 0`
  increments `emptyAlliancesCount`; anything else increments `populatedCount`.
- Call `normalizeEventAlliances(parsed)`, capture one `fetchedAt` per event as
  `new Date().toISOString()`, and `upsertEventAlliance(db, { eventKey, ...alliance, fetchedAt })` for
  each normalized record. Then `writeEtag(db, alliancesUrl, result.etag)` when an ETag came back —
  after the upserts, never before, so an interrupted event is re-fetched rather than skipped by a
  cached ETag that outran its own rows.

Two deliberate divergences from `ingestSeasonRankingsOnly`, each of which must be stated in the
function's doc comment because a reader will otherwise assume they were forgotten:

1. **There is no unknown-team guard and no `unknownTeamCount`.** `ingestSeasonRankingsOnly` needs one
   because `event_rankings.team_key REFERENCES teams(team_key)`, and TBA reports rankings for
   synthetic second-robot keys such as `frc1165B` at `2024azrl1`..`5` that TBA's own `/team/{key}`
   404s on. `event_alliances` stores `picks` as a JSON array with no team-key foreign key — 07-02's
   explicit decision, taken because of that very incident — so a synthetic key inside `picks` is
   harmless here and must not be filtered out. Filtering it would silently drop a real team from a
   real alliance.
2. **There IS a `notFoundCount`**, mirroring `ingestSeasonMediaOnly`'s rather than
   `ingestSeasonRankingsOnly`'s bare log line. With it, the five counters — `populatedCount`,
   `nullBodyCount`, `emptyAlliancesCount`, `cacheHitCount`, `notFoundCount` — sum exactly to the
   season's event count, which turns the tally from a log line into a closed invariant a reader can
   falsify. Every event takes exactly one of the five paths.

Close with one `console.log` in the rankings function's format, printing the season, the event count,
and all five counters by name, so the sum is checkable by eye from the run's own output.

Add the `else if (options.alliancesOnly)` branch to `main()`'s dispatch chain, placed after the
`rankingsOnly` branch and before the final `else`, copying that branch's per-season loop and its
`recordIngestRun` call verbatim with only the function name changed.

**In `package.json`**, add `"ingest:alliances": "tsx --env-file=.env packages/ingest/cli.ts
--alliances-only"` immediately after `ingest:rankings`, copying its form exactly.

**Then run it live, against real TBA, for two deliberately chosen seasons:**
`pnpm ingest:alliances --year 2022` followed by `pnpm ingest:alliances --year 2024`.

The seasons are not arbitrary. 2022 contains `2022roe`, whose alliance 1 RESEARCH.md recorded live
as the 4-pick set `frc3310`, `frc67`, `frc4451`, `frc3539`, and `2022ispr`, the one bare-`null` body
in the 40-event sample. 2024 contains `2024wvrox`, the one event whose alliance objects omit `name`
entirely. Between them the two seasons exercise every shape Task 1 encoded, against the live API
rather than against a fixture — which is the whole point of running anything live here. Roughly 612
requests total across the two seasons; at the 160ms/request that 06.1-04 measured on this network
that is two to three minutes, against 07-05's eventual full pass.

This is the same relationship 06.1-01's single-season live tracer had to 06.1-04's full pass, and it
carries the same known cost, which must be handled rather than rediscovered: **07-05 will see 2022
and 2024 as ETag cache hits**, so its own per-season tally for those two seasons will read as zeroes.
Record this run's five counters per season in the task commit message, and carry them into
`COVERAGE.md`'s measured-cost section in Task 3, so those two seasons' split is preserved at the run
that actually fetched them fresh. Note in the commit message that 07-05 may either cite these figures
or re-run those two seasons with `--force`. This is 06.1-04's note [3] applied in advance instead of
written up afterwards.

**Secrets discipline, binding on every command in this task.** The API key reaches the process only
through `tsx --env-file=.env`, which is why the `package.json` script carries that flag. Do not call
`Read` on `.env`. Do not `cat`, `head`, `tail` or `echo` it or any variable read from it. Do not
interpolate a key into a shell command, a log line, a test name, an assertion message, a SUMMARY or a
commit message. To confirm the key is set without revealing it, test presence and length only, in the
form `.claude/CLAUDE.md` § Conventions gives. `tbaApiKey()` already reads `process.env` and
`tbaClient.ts`'s header already states the module never logs the key and never embeds it in a
returned value; nothing in this task changes either.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ingest/alliances.test.ts packages/ingest/tbaClient.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0 and `pnpm test` passes across both vitest projects with 0 unexpected skips.
    - `grep -c '"ingest:alliances"' package.json` prints `1`, and the script's value contains `--env-file=.env` and `--alliances-only`.
    - `grep -c 'alliances-only' packages/ingest/cli.ts` prints at least `2` (the `parseArgs` option key and the module-header usage line), and `grep -c 'async function ingestSeasonAlliancesOnly' packages/ingest/cli.ts` prints `1`.
    - All five counter identifiers appear in `packages/ingest/cli.ts`: `populatedCount`, `nullBodyCount`, `emptyAlliancesCount`, `cacheHitCount` and `notFoundCount` each return at least `1` from `grep -c`.
    - No unknown-team filtering leaked into the new function: `git diff -U0 packages/ingest/cli.ts | grep -E '^\+' | grep -vE '^\+\s*(\*|//|/\*)' | grep -cE 'knownTeamKeys|unknownTeamCount'` prints `0`. Added comment lines are filtered because the new function's doc comment is required to explain why neither exists here; an executable line naming either is the failure this checks for. <!-- planner-discipline-allow: unknownTeamCount --> <!-- planner-discipline-allow: knownTeamKeys -->
    - Both live runs completed. For each of 2022 and 2024, the console line reports all five counters and they sum exactly to that season's `SELECT COUNT(*) FROM events WHERE year = ?`; both sums are recorded in the task commit message as explicit numbers alongside the five per-season counters.
    - `populatedCount` is non-zero for both seasons, so the run is not vacuously green.
    - `SELECT COUNT(*) FROM event_alliances WHERE event_key = '2022roe'` returns at least `1`, and that event's `alliance_number = 1` row has a `picks` array of length `4` whose elements are exactly `frc3310`, `frc67`, `frc4451`, `frc3539` in that order — the live values RESEARCH.md recorded, asserted against what actually landed.
    - `SELECT COUNT(*) FROM event_alliances WHERE event_key = '2022ispr'` returns `0` — the bare-`null` body case stores zero rows, no placeholder.
    - `SELECT COUNT(*) FROM event_alliances WHERE event_key = '2024wvrox' AND name IS NULL` returns at least `1` and `SELECT COUNT(*) FROM event_alliances WHERE event_key = '2024wvrox' AND name = ''` returns `0` — the absent-`name` case stored as SQL NULL, never an empty string.
    - `SELECT COUNT(*) FROM event_alliances WHERE picks = '[]' OR picks IS NULL` returns `0` across the whole table.
    - Idempotence: re-running `pnpm ingest:alliances --year 2022 --force` leaves `SELECT COUNT(*) FROM event_alliances` unchanged, recorded as the same number before and after in the commit message.
    - `ls data/corpus.sqlite.lock` reports no such file after the runs complete.
    - No command in this task read, printed or copied `.env`, and no output, log line, test name, assertion message, SUMMARY line or commit message contains any value read from it — confirmed by reading the task's own diff and commit message before committing.
    - `git diff --stat packages/corpus/` is empty and `git diff --stat packages/ingest/rankings.ts packages/ingest/schemas.ts` shows no change to `ingestSeasonRankingsOnly`'s behavior — 07-04 owns that extension, not this plan.
  </acceptance_criteria>
  <reversibility rating="costly">The CLI and script changes are ordinary source edits `git revert` undoes completely. The two live seasons' rows land in the gitignored `data/corpus.sqlite`, which no git operation restores — undoing means `DELETE FROM event_alliances` by hand or re-running the ingest. Not one-way: the table is this phase's own, nothing published depends on it yet, and the ingest is idempotent so re-running restores correctness at any point. The ETag cache entries for 2022 and 2024 are the one durable side effect worth naming, and `--force` clears past them.</reversibility>
  <done>`--alliances-only` and `pnpm ingest:alliances` exist and mirror the rankings mode, `ingestSeasonAlliancesOnly` reports five counters that sum to the season's event count, and two full live seasons are ingested with the four named live-observed events asserted against what actually landed in the corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the TBA event-scoped capability matrix against the shipped code, and fill its measured cost</name>
  <files>.planning/phases/07-event-pages/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/07-event-pages/COVERAGE.md` — **the file being modified, which already exists.** It was authored at plan time (the api-coverage gate requires the matrix before the plan is finalized) with both capability tables complete, all seven bracketed notes written, and the measured-cost section left as an explicit placeholder. Read it in full before touching it. This task does not rewrite it; it reconciles the tables against the code Task 1 actually shipped and replaces the placeholder with real figures.
    - `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` — the whole file. Its closing measured-cost section is the exact structure to fill in: exact command, date, total requests, cache hits versus fresh, elapsed wall clock read from `ingest_runs` rather than from console timing, observed per-request time against the throttle floor, and a per-season table.
    - `.planning/phases/01-data-foundation-evaluation-harness/COVERAGE.md` — the original TBA matrix. Two rows matter directly: `event-rankings | OPT-OUT | not needed yet — actual rank is only compared against in EVNT-07 rank simulation, Phase 8` and `event-alliances | OPT-OUT | not needed yet — alliance selection is EVNT-05, Phase 7`. The second one names this phase by requirement; this file is the phase it was pointing at. Read the whole surface enumeration for the list of event-scoped paths already decided once.
    - `packages/ingest/tbaClient.ts` — this plan's own Task 1 output. The eleven exposed capabilities are the INTEGRATE set this matrix must agree with exactly; a capability marked INTEGRATE here with no helper there, or a helper there with no row here, is the drift this file exists to prevent.
    - `packages/ingest/schemas.ts` — `tbaAllianceEntrySchema` and `tbaAllianceResponseSchema` as Task 1 wrote them, so every modelled field gets a row and no field is decided twice.
    - `.planning/phases/07-event-pages/07-RESEARCH.md` § Open Questions, Question 2 in full, and § Code Examples' live alliances response — the field set, the `status` sub-fields observed across `playoff_type` 0, 4, 8 and 10, and the three-state coverage counts. Also § Assumptions Log A3, the sampling caveat this file must record rather than paper over.
    - `.planning/phases/07-event-pages/07-CONTEXT.md` § Alliances tab (D-15, D-16, D-17) and § Published-data additions item 7 — which alliance facts Phase 7 actually consumes, which is what separates an INTEGRATE row from a stored-for-provenance row.
    - The task 2 commit message from this same plan — the five per-season counters and request counts the measured-cost section is written from. Use the recorded figures; do not re-run the ingest to produce them and do not estimate them.
  </read_first>
  <action>
`.planning/phases/07-event-pages/COVERAGE.md` already exists, authored at plan time with both
capability tables complete and all seven bracketed notes written. This task does two things to it:
reconcile it against the code that actually shipped, and replace its measured-cost placeholder with
real figures. Do not rewrite the tables wholesale, and do not re-decide a row that Task 1's code
already implements — a divergence between the two is a finding to report, not a cell to quietly edit
into agreement.

**Reconciliation pass — both directions.** List the exported `fetch*` helpers in
`packages/ingest/tbaClient.ts` beside the request-level table's INTEGRATE rows and check the
correspondence in both directions: every event-scoped path marked INTEGRATE must have a helper, and
every exported event-scoped helper must have an INTEGRATE row. Then list `tbaAllianceEntrySchema`'s
modelled keys beside table two and confirm every one has a row and no field is decided twice. Record
both side-by-side lists in the task commit message — that record is the only evidence the check was
performed rather than assumed. **If either direction disagrees, stop and report it** with the
specific row and the specific symbol: the tables were written from RESEARCH.md's live probe before
Task 1's code existed, so a disagreement means either the schema drifted from the researched shape or
the matrix mis-recorded it, and both are worth surfacing rather than smoothing over.

**Fill the measured-cost section**, replacing the placeholder blockquote and the "structure to fill"
list with the real thing, following the structure 06.1's COVERAGE.md uses:

- The two exact `pnpm ingest:alliances` commands Task 2 ran, and the run date.
- Total requests, and the split between cache hits (304) and fresh (200), read from `ingest_runs`
  rather than from an in-memory tally.
- Elapsed wall clock computed from `ingest_runs.started_at` to `finished_at` — **not** from the
  ingest's own console timing. 06.1's file makes this an explicit discipline (trust stored state over
  an in-memory tally); follow it.
- Observed per-request time against the 100 ms `THROTTLE_INTERVAL_MS` floor, compared against the
  ≈160 ms/request 06.1-04 measured on this network for the rankings pass — so a materially different
  figure is visible as a change in conditions rather than lost.
- A per-season table with a row for 2022 and a row for 2024, columns: events, `populatedCount`,
  `nullBodyCount`, `emptyAlliancesCount`, `cacheHitCount`, `notFoundCount`. State in prose that the
  five counters sum exactly to the season's event count, and give both sums.
- A budget projection for the remaining three seasons (2023, 2025, 2026), which 07-05 spends.

Take every figure from Task 2's recorded output and commit message. Do not re-run the ingest to
produce them and do not estimate any of them.

**Keep the handoff paragraph** that already closes the section — 2022 and 2024 will read as ETag
cache hits during 07-05's pass, so this file is the authoritative record of those two seasons'
three-state split, and 07-05 may either cite these figures or re-fetch with `--force`. Update it only
to point at the real numbers now sitting above it.

This task makes no network call and needs no credential.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-event-pages/COVERAGE.md &amp;&amp; awk '/^\|/ &amp;&amp; /OPT-OUT/ {n=split($0,c,"|"); r=c[n-1]; gsub(/^[ \t]+|[ \t]+$/,"",r); if (r=="") {print "UNDECIDED OPT-OUT ROW: " $0; bad=1}} END {exit bad?1:0}' .planning/phases/07-event-pages/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-event-pages/COVERAGE.md` exists and contains two capability tables, each with the three columns `capability`, `decision`, `reason`.
    - The two capability tables and the seven bracketed notes are byte-identical to their plan-time form unless the reconciliation pass found a real divergence: `git diff .planning/phases/07-event-pages/COVERAGE.md` shows changes confined to the measured-cost section, OR the diff includes a table row change that the task commit message names, explains and attributes to a specific code symbol.
    - The reconciliation pass is evidenced, not asserted: the task commit message carries both side-by-side lists — exported event-scoped `fetch*` helpers against INTEGRATE rows, and `tbaAllianceEntrySchema`'s modelled keys against table two's rows — with the correspondence stated as holding in both directions.
    - The verify command exits 0 — no `OPT-OUT` row has an empty reason cell. This is the gate the whole file exists for: an unreasoned opt-out is an undecided hole, and a malformed matrix blocks the phase seal at `verify:pre`.
    - Every decision cell is exactly one of `INTEGRATE` or `OPT-OUT`: `grep -cE '^\|.*\|\s*(INTEGRATE|OPT-OUT)\s*\|' .planning/phases/07-event-pages/COVERAGE.md` equals the total number of capability rows in the two tables, counted by reading them.
    - The request-level table has at least 20 rows and includes a row for each of these literal paths: `/event/{key}/alliances`, `/event/{key}/rankings`, `/event/{key}/awards`, `/event/{key}/district_points`, `/event/{key}/insights`, `/event/{key}/oprs`, `/event/{key}/coprs`, `/event/{key}/predictions`, `/event/{key}/matches`, `/event/{key}/teams`, `/event/{key}/simple`, `/event/{key}/teams/keys`, `/event/{key}/matches/timeseries`, `/event/{key}/teams/statuses`.
    - The response-field table has at least 16 rows and covers every key `tbaAllianceEntrySchema` models — cross-checked by reading that schema and confirming no modelled field lacks a row — plus separate rows for the absent-`name` case, the top-level `null` body and the empty-array response.
    - `grep -c 'OPT-OUT' .planning/phases/07-event-pages/COVERAGE.md` is at least `12`, confirming the matrix is a real subtraction record rather than a blanket approval.
    - The file's INTEGRATE set for request-level TBA capabilities agrees with `packages/ingest/tbaClient.ts`'s exposed helpers: every event-scoped path this file marks INTEGRATE has a corresponding exported `fetch*` helper, and no exported event-scoped helper lacks an INTEGRATE row. Verified by listing both sides side by side in the task commit message.
    - The supersession section names Phase 1's and Phase 6.1's `event-alliances` OPT-OUT rows explicitly, states that D-18 item 7 is the locked decision they were waiting for, and states that neither prior file is edited. `git diff --stat .planning/phases/01-data-foundation-evaluation-harness/ .planning/phases/06.1-match-and-event-data-enrichment/` is empty.
    - The measured-cost section names the two exact commands run in Task 2, a date, a total request count, and a per-season table whose five counters per season match the figures recorded in Task 2's commit message exactly — not re-estimated, not re-run.
    - The measured-cost section states in prose that 2022 and 2024 will read as ETag cache hits during 07-05's pass and that this file is the authoritative record of those seasons' split.
    - A bracketed note records RESEARCH.md Assumption A3 as an open scope caveat owned by 07-05, rather than presenting the shape as proven across the full corpus.
    - No value read from `.env` appears anywhere in the file — this task makes no network call and needs no credential.
  </acceptance_criteria>
  <reversibility rating="reversible">A planning-artifact edit with no code, no schema and no published consequence. Every decision the file records is drawn from CONTEXT.md and RESEARCH.md rather than minted here, and the only thing this task adds that cannot be re-derived from those sources is Task 2's measured figures — which are also in that task's commit message.</reversibility>
  <done>The matrix is reconciled against the shipped code in both directions with the check evidenced in the commit message, and the measured-cost placeholder is replaced by 2022's and 2024's real per-season counters, request counts and wall clock read from `ingest_runs`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBA HTTPS response body -> `tbaAllianceResponseSchema.parse` | **The new boundary this plan opens.** Third-party JSON from a volunteer-run public API, never previously parsed by this repo, crossing into typed local state. Everything downstream — the corpus, the published artifact, the rendered Alliances tab — treats whatever gets past this line as fact. |
| `normalizeEventAlliances` output -> `upsertEventAlliance` -> `data/corpus.sqlite` | Parsed values become durable local records in a populated, gitignored ~359 MB database that no git operation restores. |
| `.env` -> `tsx --env-file=.env` -> `process.env.TBA_API_KEY` -> `tbaFetch`'s `X-TBA-Auth-Key` header | A live credential reaches a process. The transcript, the logs, the test names and the commit messages are all output streams this key must never enter. |
| `event_alliances` -> 07-08's `buildEventArtifact` -> R2 -> browser | An alliance `name` is human-entered third-party text that becomes rendered page content two plans downstream. |
| `ingestSeasonAlliancesOnly` -> TBA request volume | Roughly 1,581 outbound requests per full pass against a free, volunteer-run service with published rate expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | `tbaAllianceResponseSchema` at the `/event/{key}/alliances` fetch boundary | high | mitigate | Zod `.parse()` at the boundary, per `packages/ingest/schemas.ts`'s stated header policy — a drifted payload throws rather than being coerced into a plausible-looking record. Task 1 pins three explicit drift cases (`picks` retyped to a string, `picks` empty, `declines` absent) as throwing tests. `picks.min(1)` is what makes 07-02's "never a row with an empty picks array" contract structurally true at the parse boundary rather than dependent on a downstream skip. `status` is `z.unknown()` and never trusted as a shape — it is serialized whole and read by nothing in this phase. |
| T-07-03-02 | Spoofing | An absent `name` coerced to a fabricated or empty value | high | mitigate | Absence, explicit `null` and `""` all normalize to a single `null`, which is the only representation 07-02's `event_alliances.name` admits for the fact "TBA has no name for this alliance." No `.default()` appears anywhere in the schema, and Task 1 asserts the `2024wvrox`-shaped object (keys exactly `declines`/`picks`/`status`) normalizes to `toBeNull()` and explicitly `not.toBe("")`. Task 2 asserts the same against the real row that landed in the corpus. A synthesized `Alliance {n}` label at ingest would make an absence indistinguishable from a TBA-supplied name for every downstream reader; it is 07-14's display decision to make from an honest NULL. Carried as this plan's first `must_haves.prohibitions` entry. |
| T-07-03-03 | Spoofing | A fourth-pick concept TBA's response does not carry | high | mitigate | RESEARCH.md Q2 confirmed live that the raw response has no separately-named field for a 4th team — it is simply `picks[3]` (D-16). The schema, the normalized interface and the CLI therefore name no such field, and Task 1's acceptance criteria grep all three implementation files' non-comment lines for that concept and require a count of `0`, with the test file deliberately excluded so a case name can still say what it rules out. Inventing the field would attribute a model to the source that the source does not have, and would then be indistinguishable from a real TBA field to 07-07, 07-08 and 07-14. |
| T-07-03-04 | Repudiation | The null-body / empty-array / populated distinction collapsing into one count | high | mitigate | Three separate counters, branched from the parse result exactly as `ingestSeasonRankingsOnly` does (PD-02), plus `cacheHitCount` and a new `notFoundCount` so all five sum to the season's event count — a closed invariant rather than an unfalsifiable log line. Task 2 asserts that sum for both live seasons and records both in the commit message. This distinction is load-bearing here in a way it was not for rankings: D-17's disabled tab renders exactly this absence, and 06.1-04 measured the two states occurring at genuinely different rates. Carried as this plan's second `must_haves.prohibitions` entry. |
| T-07-03-05 | Information Disclosure | `TBA_API_KEY` reaching a transcript, log, test name or commit message | high | mitigate | The key reaches the process only via `tsx --env-file=.env`, the mechanism every existing `package.json` ingest script already uses; `tbaApiKey()` reads `process.env` and the value flows only into `tbaFetch`'s `X-TBA-Auth-Key` header. `tbaClient.ts`'s own module header already states the module never logs it and never embeds it in a returned value, and this plan does not change that. Task 2's action forbids `Read`/`cat`/`echo` of `.env` and forbids interpolating a key into any command, log line, test name, assertion message or commit message, and its acceptance criteria require confirming the diff and commit message before committing. This control exists because it was broken in Phase 4 and a live R2 token had to be rotated — see `.claude/CLAUDE.md` § Conventions. |
| T-07-03-06 | Denial of Service | Outbound request volume against a free volunteer-run API | medium | mitigate | `THROTTLE_INTERVAL_MS = 100` is enforced inside `tbaFetch` itself, so no call site can bypass it, and `TbaRequestCounter` tallies 200s and 304s into `ingest_runs` so volume is measured rather than assumed. This plan's live scope is deliberately bounded to two seasons (~612 requests) rather than the full corpus; 07-05 owns the full pass. ETag conditional requests mean a repeat run costs the same request count but almost no bandwidth. |
| T-07-03-07 | Tampering | `data/corpus.sqlite` write lock contention during Task 2's live runs | medium | mitigate | Task 2's precondition requires no concurrent ingest, publish or harness process. `openCorpus`'s existing `close` override unlinks the lock, and `main()`'s `finally { db.close() }` runs it on both the success and the failure path. If the lock is genuinely held, `openCorpus` throws its existing clear already-open message and the run fails loudly rather than writing partially. Task 2 asserts no lock file remains after the runs. |
| T-07-03-08 | Tampering | `writeEtag` recorded before the event's rows are upserted | medium | mitigate | The `writeEtag` call is placed after the upsert loop, deliberately and stated in the action, so an interrupted event has no cached ETag and is re-fetched on the next run rather than skipped as a 304 whose rows never landed. This mirrors `ingestSeasonRankingsOnly`'s own ordering. Better-sqlite3 commits each statement immediately, so partial progress within an event is durable and the re-fetch merges idempotently on `(event_key, alliance_number)`. |
| T-07-03-09 | Repudiation | This repo's stated TBA capability inventory disagreeing with the surface it exposes | medium | mitigate | `tbaClient.ts`'s module header, its inline section comment and `tbaClient.test.ts`'s capability-surface case were all already stale by two endpoints before this plan — a live instance of this project's own inherited failure-log entry about documentation describing a shape the code no longer has. Task 1 corrects all three to eleven and enumerates them with the adding phase named, Task 3's matrix is cross-checked against the exposed helpers in both directions, and Task 1's acceptance criteria grep for the stale phrasing and require `0`. Carried as this plan's third `must_haves.prohibitions` entry. |
| T-07-03-10 | Tampering | Alliance `name` rendered as page content two plans downstream | low | accept | Canon stored-XSS concern, filed as a breadcrumb rather than a new control. React's default JSX text-node escaping neutralizes it, `07-RESEARCH.md` § Security Domain confirms no `dangerouslySetInnerHTML` pattern exists anywhere in the reused component set, and the rendering decision belongs to 07-14 rather than to this ingest layer. This plan's contribution is that the value is stored verbatim rather than interpolated into any markup or query — every write goes through `upsertEventAlliance`'s named `db.prepare` parameters. |
| T-07-03-11 | Elevation of Privilege | `eventKey` interpolated into the TBA request path | low | accept | Canon injection concern, filed as a breadcrumb. Event keys come from this corpus's own `events` table (`SELECT event_key FROM events WHERE year = ?`), never from user, file or network input, and reach `tbaFetch` as a template-literal path segment against a fixed base URL — the identical construction `fetchEventRankings` and `fetchEventMatches` have used since Phase 1. |
| T-07-03-12 | Denial of Service | An alliance removed upstream leaving a stale corpus row | low | accept | The ingest is upsert-only with no delete pass, exactly as `ingestSeasonRankingsOnly` is. TBA does not retract a completed event's alliance selection, and a stale row is a bounded, visible inconsistency rather than a fabricated fact. Accepted with the premise re-derived against the current code rather than assumed (the 06.1-06 lesson): `upsertEventAlliance` overwrites every non-key column on conflict, so a *changed* alliance is corrected by any re-run; only a *removed* one would persist. |
| T-07-03-SC | Tampering | npm/pip/cargo installs | high | accept | Not applicable to this plan or this phase — `07-RESEARCH.md` § Package Legitimacy Audit records "this phase installs no new external packages" and § Standard Stack confirms no `npm install` is needed. This plan adds one `package.json` **script**, not a dependency; `git diff` on the `dependencies` and `devDependencies` blocks must be empty. Retained as a register row so a later task adding a dependency is a visible deviation rather than a silent one. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm test` passes across both vitest projects with 0 unexpected skips.
- `pnpm vitest run packages/ingest/alliances.test.ts packages/ingest/tbaClient.test.ts` passes — the Wave 0 test file `07-VALIDATION.md` names for D-18.7 exists and is green.
- `git diff --stat` for the whole plan touches exactly eight files: `packages/ingest/schemas.ts`, `packages/ingest/alliances.ts`, `packages/ingest/alliances.test.ts`, `packages/ingest/tbaClient.ts`, `packages/ingest/tbaClient.test.ts`, `packages/ingest/cli.ts`, `package.json`, and `.planning/phases/07-event-pages/COVERAGE.md`. Any other file in the diff is a scope deviation to report.
- `git diff --stat packages/corpus/ packages/harness/ apps/` is empty — this plan redefines nothing 07-02 shipped and touches no publisher or client surface.
- `git diff package.json` shows a change to the `scripts` block only; the `dependencies` and `devDependencies` blocks are untouched.
- `git log --oneline` shows one commit per task, each scoped `07-03`.
- `ls data/corpus.sqlite.lock` reports no such file.
- No commit message, log line, test name or assertion message in this plan's diff contains a value read from `.env`.
</verification>

<success_criteria>
- `tbaAllianceEntrySchema` and `tbaAllianceResponseSchema` are exported from `packages/ingest/schemas.ts`, are distinct from the module-private per-match `tbaAllianceSchema`, and carry the top-level `.nullable()` that keeps TBA's bare-`null` body a real answer rather than an aborted run.
- `normalizeEventAlliances` in a new `packages/ingest/alliances.ts` is a pure transform with no I/O and no corpus import, assigning `allianceNumber` from TBA's own response-array position, collapsing absent/`null`/`""` names to a single `null`, and passing `picks` through verbatim with the 4th team at `picks[3]` and no field invented for it.
- `packages/ingest/alliances.test.ts` exists as the Wave 0 file `07-VALIDATION.md` names for D-18.7, mirrors `rankings.test.ts` case for case, and encodes both live-probe findings plus all three response states and three drift cases.
- `fetchEventAlliances` is exported from `packages/ingest/tbaClient.ts`, and that file's module header, inline section comment and capability-surface test all state the same currently-true count of eleven exposed capabilities.
- `ingestSeasonAlliancesOnly`, `--alliances-only` and `pnpm ingest:alliances` mirror the rankings mode exactly, and the function's five counters sum to the season's corpus event count for both live seasons run.
- 2022 and 2024 are ingested live against real TBA, with `2022roe`'s 4-pick alliance, `2022ispr`'s zero rows and `2024wvrox`'s SQL-NULL name each asserted against what actually landed in the corpus, and the run is proven idempotent on a `--force` re-run.
- `.planning/phases/07-event-pages/COVERAGE.md` decides TBA's full event-scoped capability surface with `INTEGRATE` as the default, carries a one-line reason on every `OPT-OUT`, records the alliances response field by field, records the two seasons' measured cost from the real run, and states the A3 sampling caveat as an open item owned by 07-05.
- Every ingest-level probe edge this plan owns (EVNT-05 `empty`, `ordering`, `adjacency`) is asserted by a passing test or a corpus query, and this plan's two backstop markers are the only truths not machine-checked.
- No credential reached any output stream, and `packages/corpus/` is untouched.
</success_criteria>

<output>
Create `.planning/phases/07-event-pages/07-03-SUMMARY.md` when done.
</output>
