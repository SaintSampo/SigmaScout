---
phase: 07-event-pages
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - packages/ingest/corpusCensus.test.ts
  - .planning/phases/07-event-pages/COVERAGE.md
autonomous: true
requirements: [EVNT-02, EVNT-05]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every one of the five rankings season invocations carries `--force`, and a season that aborts partway is resumed with `--force` again — never without it. `cachedEtagFor` returns a cached ETag unless forced, and the 304 branch does `cacheHitCount++; continue;` BEFORE any upsert, so an unforced pass writes nothing and leaves all four of 07-02's new columns NULL. The resume case is the sharper trap: an aborted forced season has already written FRESH ETags for the events it reached, so a resume without `--force` 304-skips both the events it finished AND — on their older 06.1-04 ETags — exactly the events it never backfilled. The silent partial backfill would then be indistinguishable from success. 06.1-04 measured all 324 of 2024's requests returning 304 on precisely such a re-run."
    - "After the forced five-season pass, zero rows in `event_rankings` carry a NULL `record_wins`, `record_losses` or `record_ties`. If any do, each is traced to a named event key that returned a null body, an empty `rankings` array, or a 404 in this run's own captured logs — a stale row from 06.1-04 that this pass had no fresh body to refresh — and recorded as such. It is never resolved by deleting rows and never left untraced."
    - "The events with no official ranking land as zero `event_rankings` rows and zero errors: all five forced invocations exit 0, and a fresh read-only query counts the corpus events with no ranking row at all as a non-zero number in the 150–400 band around 06.1-04's measured 259. Absence is TBA's honest answer for a scheduled-but-unplayed or playoff-only event, not a failure — this is the corpus-wide premise D-08's Insights fallback is built on. (EVNT-02 `empty` probe, RUN-LEVEL half only — 07-02 owns the storage half, 07-04 the per-response ingest half, 07-11 the rendered half; see this plan's probe-edge accounting table.)"
    - "For each of the five seasons, the rankings run's EVENT-level paths sum exactly to that season's `SELECT COUNT(*) FROM events WHERE year = ?`: populated + null-body + empty-rankings + cache-hits + 404-skips. `cacheHitCount` is 0 in every season because every invocation is forced, and the 404-skips are countable only from the captured log's `404 Not Found, skipping` lines because `ingestSeasonRankingsOnly`'s 404 branch increments no counter. The two ROW-level counters — `unknownTeamCount` and 07-04's `nullRankingScoreCount` — are deliberately excluded from that sum; adding them produces a phantom mismatch."
    - "For each of the five seasons, the alliances run's five counters — populated, null-body, empty-alliances, cache-hits, not-found — sum exactly to that season's corpus event count, the closed invariant 07-03 built `notFoundCount` to make checkable. 2022 and 2024 read as all-cache-hits (288 and 324) with zero populated, because 07-03 already fetched them fresh; their real three-state split lives in `COVERAGE.md`'s measured-cost section, recorded by the run that actually fetched them."
    - "The forced re-ingest refreshes rows in place and never duplicates or deletes: `upsertEventRanking`'s `(event_key, team_key)` conflict target merges, and nothing on this path issues a DELETE, so `SELECT COUNT(*) FROM event_rankings` after the pass is greater than or equal to 06.1-04's measured 47,695. A forced overwrite of an already-populated 47,695-row table is where a duplicate-or-wipe defect would actually show up, and it is checked at that scale rather than inferred from a two-row fixture. (EVNT-02 `adjacency` probe, RUN-LEVEL half — 07-04 owns the per-response half, 07-11 the rendered half.)"
    - "The five season invocations are mutually independent and order-free: `ingestSeasonRankingsOnly` and `ingestSeasonAlliancesOnly` each read only `SELECT event_key FROM events WHERE year = ?` and write only rows keyed by that season's event keys, so no season's result depends on which seasons ran before it. This is what makes a per-season invocation strategy safe at all, and what makes a single aborted season resumable without re-running the others. (EVNT-02 `ordering` probe, RUN-LEVEL half — 07-04 owns TBA's-array-order-preserved, 07-11 the rendered order.)"
    - "`2025bc`, `2026wvrox` and `2022ispr` each exist as rows in the corpus `events` table and each carry exactly zero `event_alliances` rows after the full pass. These are RESEARCH.md Q2's three live-observed absent shapes — two valid empty arrays and one bare null body — and the whole point is that they are stored as an honest zero rather than raised as an error, which is the state D-17's disabled tab renders. (EVNT-05 `empty` probe, RUN-LEVEL half — 07-02 owns the storage half, 07-03 the per-response ingest half, 07-14 the rendered half.)"
    - "Every event with any `event_alliances` row has a contiguous `alliance_number` sequence: `MIN(alliance_number) = 1` and `MAX(alliance_number) = COUNT(*)` for that event. TBA's seed order is what 07-14 renders top-to-bottom, so a gap or an off-by-one origin would silently mislabel which alliance is which across the whole corpus. (EVNT-05 `ordering` probe, RUN-LEVEL half — 07-14 owns the rendered order.)"
    - "The 2022 and 2024 alliance rows 07-03 wrote survive this pass untouched: the maximum `fetched_at` across all 2022 and 2024 `event_alliances` rows is strictly less than the minimum `fetched_at` across all 2023, 2025 and 2026 rows. A 304 short-circuit must leave existing rows exactly as they were — neither refreshed nor blanked — and this is the assertion that proves it at corpus scale rather than on one re-run event. (EVNT-05 `adjacency` probe, RUN-LEVEL half — 07-03 owns the per-event idempotence half, 07-14 the rendered half.)"
    - "Every claim this plan makes about the corpus is checked by a fresh read-only query executed after the writing process exits, never by the run's own in-memory tally — 06.1-04's repudiation-resistance discipline, adopted there after an interrupted-then-resumed season made a console tally under-report. The console counters are recorded as evidence of what the RUN saw; `packages/ingest/corpusCensus.test.ts` is the evidence of what the CORPUS holds, and the two are reconciled rather than conflated."
    - "No task in this plan reads, prints, copies or interpolates the contents of `.env`. Every live invocation goes through the existing `pnpm ingest:rankings` / `pnpm ingest:alliances` scripts, which are `tsx --env-file=.env packages/ingest/cli.ts …` — the tool reads the file, no agent does. `TBA_API_KEY` reaches only `tbaApiKey()` via `process.env` and only `X-TBA-Auth-Key`, never a log line, a test name, an assertion message, a captured log file or a commit message."
    - "A quiet log and a truncated process list are never accepted as proof that a run died. Before any intervention, all three of these must agree that nothing is progressing: the mtime of `data/corpus.sqlite-wal` has stopped advancing across two checks at least 60 seconds apart; the season's row count has stopped advancing across the same interval; and an UNTRUNCATED process query filtered on the command line finds no matching node process. This project already paid for this lesson once — on 2026-08-25 a live media ingest was declared dead from exactly the two lying signals, redone, and had in fact been alive and durably committed."
    - statement: "2024's rankings null-body / empty-rankings split has never been measured — 06.1-04 deliberately declined to force-refetch 324 events purely to recover a classification, and note [3] of its COVERAGE.md records that gap. This plan's forced pass measures it for the first time, as a free by-product of a force that is required anyway for the four new columns. There is no prior figure to compare it against, so the value is observed and recorded, never predicted."
      verification: backstop
    - statement: "The corpus-wide count of events carrying `event_alliances` rows has never been measured — RESEARCH.md Q2's sample was 40 events. The census floor of 100 distinct events per season is a deliberately low non-vacuity bound, not a prediction, chosen specifically to avoid repeating 06.1-04's 250-events-per-season over-estimate that three of five seasons then honestly undershot. The real per-season figure is observed and recorded in COVERAGE.md."
      verification: backstop
    - statement: "Whether any alliance in the corpus carries fewer than three picks is unmeasured — RESEARCH.md's 40-event live sample saw only lengths 3 and 4. The census asserts the safe 1–4 bound and records the observed length histogram; any event with a sub-three-pick alliance is reported to 07-14 as a real input to D-16's first-three-picks sum rather than assumed away."
      verification: backstop
    - statement: "Every assertion in this plan is provable only on the machine holding the gitignored `data/corpus.sqlite`; `corpusCensus.test.ts` skips with an explicit message naming that path everywhere else, so CI never proves it and never silently passes it either."
      verification: backstop
  artifacts:
    - packages/ingest/corpusCensus.test.ts
    - .planning/phases/07-event-pages/COVERAGE.md
  key_links:
    - "`--force` on every rankings invocation <-> 07-04's `cachedEtagFor` / 304-early-continue pair <-> 07-02's four new nullable columns. This is the single link that decides whether Phase 7 ships real record and RP values or four silently-empty columns, and it has no automated guard anywhere in the pipeline — only this plan's discipline and `corpusCensus.test.ts`'s NULL assertion after the fact."
    - "This plan's populated `event_rankings` rows -> 07-08's `buildEventArtifact` read -> 07-07's `EventTeamSchema.rank`/`.record`/`.rp` -> 07-11's Rank / Record / RP columns on the Insights tab. 07-08 can be unit-tested against a seeded in-memory corpus without this plan, but 07-10's real subset publish cannot — which is why 07-10 depends on 07-05 and 07-08 does not (outline assumption 11)."
    - "This plan's `event_alliances` rows -> 07-08's `selectEventAlliancesForSeason` read -> 07-07's `EventAllianceSchema` -> 07-14's Alliances tab and D-17's disabled-tab condition. The three zero-row events verified here are the corpus-side proof that D-17's absent case is real and reachable, not a defensive branch nothing exercises."
    - "`corpusCensus.test.ts` <-> 07-04's `rankingsLive.test.ts` corpus-backed block. 07-04 proved the widened write path on one season; this file generalizes the same evidence to all five and adds the alliances half, and both stay green afterwards as standing regression guards for a write path `cli.ts`'s top-level `main()` call makes untestable by import."
    - "`COVERAGE.md`'s measured-cost section <-> 07-03's recorded 2022/2024 alliance figures. 07-03 wrote those figures in advance precisely because this pass would see those two seasons as ETag cache hits; Task 3 realizes that handoff instead of rediscovering it, applying 06.1-04's note [3] lesson rather than repeating it."
    - "The sub-three-pick histogram this plan records -> 07-14's D-16 combined-value rule (sum the first three picks only). If any alliance has fewer than three picks, D-16's rule needs a stated behavior for it, and this is the only plan in the phase that looks at all 1,581 events."
  prohibitions:
    - statement: "MUST NOT spend a request against TBA to re-measure something already measured. Concretely: no `--force` on the alliances pass, because 07-03 already fetched 2022 and 2024 fresh and recorded their split, and re-fetching 612 events to recover a number that is already written down is measurement for its own sake — the exact move 06.1-04 declined for 2024 and was right to decline. TBA is a free, volunteer-run service that donates the data this entire project is built on; every avoidable request is a real cost imposed on someone else."
      status: unverified
      verification: manual
    - statement: "MUST NOT kill and restart a live ingest on inconclusive liveness evidence. A restart re-spends an entire season of requests against that donated service and risks discarding work that was already durably committed. All three liveness signals must agree before intervening, and the executor — which holds the local evidence — checks before anyone asserts the run is dead. This is a values commitment about someone else's infrastructure, not a convenience rule, and it is written here because it was violated once already in this repo."
      status: unverified
      verification: manual
    - statement: "MUST NOT fabricate, pad, or re-run-until-the-number-matches. If a measured figure falls short of 06.1-04's baseline, it is reported as measured with its cause traced to named event keys and real TBA responses — never resolved by deleting rows, re-running with different flags until the count agrees, or quietly widening a floor after seeing the result. 06.1-04 undershot its own 250-per-season estimate in three of five seasons and recording that honestly was the correct outcome; a pre-run figure in a plan is an expectation, never ground truth, and a plan that can only report agreement is reporting nothing."
      status: unverified
      verification: manual
---

<objective>
Run the real full-corpus ingest for 2022–2026 — alliances (new, D-18.7) and rankings (a forced re-run that finally writes TBA's authoritative record and ranking score into 07-02's four new columns, D-18.6) — against live TBA via the existing `tsx --env-file=.env` scripts, and prove the result by fresh read-only corpus queries rather than by the runs' own console tallies.

Purpose: every pipeline plan downstream of this one reads what lands here. 07-08 threads `event_rankings` and `event_alliances` into `buildEventArtifact`; 07-10 publishes a real subset the tab plans build against; 07-11 renders rank/record/RP; 07-14 renders alliances and disables its own tab on the absent case. Until this plan runs, those columns and that table hold nothing for four of five seasons, and the phase's most likely silent failure — four new columns shipping entirely NULL because a re-run without `--force` 304-skipped every event — happens right here or nowhere.

Output: `event_rankings` refreshed across all five seasons with `record_wins`/`record_losses`/`record_ties`/`ranking_score` populated; `event_alliances` populated for all five seasons; `packages/ingest/corpusCensus.test.ts` as a standing corpus-wide census gate covering both tables; and `COVERAGE.md`'s measured-cost section carrying the real full-pass figures for both endpoints, including 2024's rankings three-state split which 06.1-04's note [3] left unmeasured.

**Execution constraint — this plan must run in the main working tree, never a git worktree.** `data/corpus.sqlite` is gitignored and is not copied into a worktree, `.env` is gitignored and is needed by every invocation here, and rows written inside a worktree do not merge back when its commits do — the tracked code would land on `main` while `main`'s corpus stayed empty, and `corpusCensus.test.ts`'s `existsSync` guard would then skip and report the suite green over the exact invariant meant to catch it. Phase 5 plan 05-02 and Phase 06.1 both hit this; the standing mitigation is `workflow.use_worktrees=false` for any run containing this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-pages/07-CONTEXT.md
@.planning/phases/07-event-pages/07-RESEARCH.md
@.planning/phases/07-event-pages/07-PATTERNS.md
@.planning/phases/07-event-pages/07-VALIDATION.md
@.planning/phases/07-event-pages/07-03-PLAN.md
@.planning/phases/07-event-pages/07-04-PLAN.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Newly-created symbols and paths, listed so a source-grounding pass can exclude them from
drift verification. **This plan creates:**

| Kind | Name | Location |
|---|---|---|
| file | `packages/ingest/corpusCensus.test.ts` | new test file |
| constant | `CORPUS_PATH` | `packages/ingest/corpusCensus.test.ts` |
| constant | `CORPUS_AVAILABLE` | `packages/ingest/corpusCensus.test.ts` |
| constant | `SEASONS` | `packages/ingest/corpusCensus.test.ts` |
| constant | `POPULATED_EVENT_FLOORS` | `packages/ingest/corpusCensus.test.ts` |
| constant | `ALLIANCE_EVENT_FLOOR` | `packages/ingest/corpusCensus.test.ts` |
| constant | `ABSENT_ALLIANCE_EVENT_KEYS` | `packages/ingest/corpusCensus.test.ts` |
| describe block | `"event_rankings — full-corpus census after the forced 2022-2026 pass (plan 07-05)"` | `packages/ingest/corpusCensus.test.ts` |
| describe block | `"event_alliances — full-corpus census after the 2022-2026 pass (plan 07-05)"` | `packages/ingest/corpusCensus.test.ts` |
| doc section | `## Measured cost — the real full-corpus 2022-2026 pass (plan 07-05)` | `.planning/phases/07-event-pages/COVERAGE.md` |
| directory | `data/ingest-logs/` (gitignored by `data/*`) | run-evidence logs, machine-local |

Sibling plans in this phase additionally create: `event_alliances` and the four `event_rankings`
columns `record_wins`/`record_losses`/`record_ties`/`ranking_score` plus
`EVENT_RANKING_RECORD_COLUMNS`, `hasEventRankingRecordColumns`, `CorpusEventAlliance`,
`EventAllianceSelection`, `upsertEventAlliance`, `selectEventAlliancesForSeason` (07-02,
`packages/corpus/`); `tbaAllianceResponseSchema`, `normalizeEventAlliances`,
`ingestSeasonAlliancesOnly`, the `--alliances-only` flag, `pnpm ingest:alliances` and
`COVERAGE.md` itself (07-03, `packages/ingest/` + `package.json`);
`RANKING_SCORE_SORT_ORDER_NAME`, `RANKING_SCORE_SORT_ORDER_INDEX`,
`RankingScoreSortOrderError`, `nullRankingScoreCount` and `packages/ingest/rankingsLive.test.ts`
(07-04); `EventAllianceSchema` and the `rank`/`record`/`rp` fields on `EventTeamSchema` (07-07,
`packages/harness/pageArtifacts.ts`); and the `--include-offseason` CLI flag (07-09,
`packages/harness/publish.ts`).

## Scope boundaries (what this plan deliberately does NOT do)

- **No source change to `packages/ingest/`.** `cli.ts`, `rankings.ts`, `alliances.ts`,
  `schemas.ts`, `tbaClient.ts` and `package.json` are all as 07-02/07-03/07-04 left them. This
  plan runs what they built and measures the result. `git diff --stat packages/ingest/` shows
  exactly one file: the new `corpusCensus.test.ts`.
- **No corpus schema change.** 07-02 owns `schema.sql` and `db.ts`. If a migration turns out not
  to have been applied to the real corpus, that is a halted precondition to report, never a
  schema edit made here.
- **No `--force` on the alliances pass.** See this plan's first `must_haves.prohibitions` entry
  and Task 2's action for the full reasoning; the asymmetry with rankings is deliberate and load
  bearing, not an inconsistency.
- **No published-artifact change and no R2 or D1 write.** This plan touches the local corpus and
  nothing else. `buildEventArtifact` is 07-08's; the subset publish is 07-10's; the full
  republish is 07-17's.
- **No edit to Phase 06.1's `COVERAGE.md`.** That phase is sealed. 2024's newly-measured rankings
  split is recorded in *this* phase's `COVERAGE.md` with an explicit cross-reference closing
  06.1-04's note [3], rather than by editing a completed phase's artifact.
- **No `checkpoint:decision`.** Per the outline's Notes, this phase's two one-way doors are gated
  at 07-17 (the republish write pass) and 07-19 (the destructive delete pass). All three tasks
  below are rated honestly and none is one-way.
- **No UI-SPEC `## UI Considerations` rows.** Every row in that section is scoped to the seven
  rendered surfaces E1–E7 (event header, tab strip, Insights, Breakdown, Quals, Elims,
  Alliances); this plan renders nothing. Recorded explicitly so the row accounting shows a
  reasoned zero rather than a silent drop.
- **No edit to `07-VALIDATION.md`.** Its Per-Task Verification Map states in its own header that
  `/gsd-validate-phase 7` fills the Task ID / Plan / Wave / Threat Ref columns after planning.

## Probe-edge accounting for this plan

This plan is the primary owner of **none** of the phase's 17 probe edges. The rendered halves of
EVNT-02's three (`empty`, `ordering`, `adjacency`) belong to **07-11** and EVNT-05's three to
**07-14**, per outline assumption 14. Below those, each edge has already been split by layer:
07-02 owns the *storage* half, 07-03 and 07-04 own the *per-response ingest logic* half.

What is genuinely left, and decided here and nowhere else, is the **run level** — the claim that
holds across all 1,581 events at once rather than per response. Six such halves are authored into
`must_haves.truths` above, each carrying an explicit cross-reference to its sibling owners:

| Requirement | Category | RUN-LEVEL half authored here | Storage half | Ingest-logic half | Rendered half |
|---|---|---|---|---|---|
| EVNT-02 | `empty` | Corpus-wide census of events with zero ranking rows is non-zero and lands in the 150–400 band; all five invocations exit 0 | 07-02 | 07-04 | 07-11 |
| EVNT-02 | `ordering` | Season invocations are mutually independent and order-free | — | 07-04 | 07-11 |
| EVNT-02 | `adjacency` | Forced overwrite of an already-populated 47,695-row table merges; row count is monotone | 07-02 | 07-04 | 07-11 |
| EVNT-05 | `empty` | `2025bc` / `2026wvrox` / `2022ispr` each hold exactly zero alliance rows, as data not as errors | 07-02 | 07-03 | 07-14 |
| EVNT-05 | `ordering` | `alliance_number` is contiguous 1..N for every event corpus-wide | 07-02 | — | 07-14 |
| EVNT-05 | `adjacency` | 07-03's 2022/2024 rows survive the pass with strictly-earlier `fetched_at` | 07-02 | 07-03 | 07-14 |

Nothing is double-counted against 07-11's or 07-14's budget, and nothing is dropped.

## Recalled prohibitions — canon-referral breadcrumbs (dropped, not minted)

Three Stage-1 candidates survived Stage 2 as genuine values commitments and are authored into
`must_haves.prohibitions` above. The rest were dropped, recorded here as one-line breadcrumbs so
each drop is visible:

- Disclosure of `TBA_API_KEY` — canon, and already binding as a project convention
  (`.claude/CLAUDE.md` § Secrets handling) plus an enforced gate (`scripts/secrets-boundary.test.ts`).
- SQL injection through event keys — canon; every event key on this path comes from the corpus's
  own `events` table and every write uses named `db.prepare` parameters.
- Prototype pollution / unsafe deserialization of third-party JSON — canon; every response is
  Zod-parsed at the fetch boundary before any property is read.
- Catching the sort-order drift guard to let a season finish, and defaulting a NULL ranking score
  to `0` — already minted as 07-04's own prohibitions; not re-minted here, so they are counted
  once.
- Silently narrowing the season range, and deleting rows to "clean up" before a re-ingest —
  routine engineering, fully covered by this plan's acceptance criteria.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The forced five-season rankings re-ingest, and the corpus-wide rankings census that proves it</name>
  <files>packages/ingest/corpusCensus.test.ts</files>
  <precondition>Running in the main working tree (not a git worktree); `.env` exists at the repo root with a populated `TBA_API_KEY`; `data/corpus.sqlite` exists locally with 07-02's migration applied so `PRAGMA table_info(event_rankings)` lists nine columns including `record_wins`, `record_losses`, `record_ties` and `ranking_score`; `packages/ingest/cli.ts` is as 07-04 left it, writing all four of those values through `upsertEventRanking`; and no other ingest, publish or harness process holds `data/corpus.sqlite.lock`.</precondition>
  <read_first>
    - `packages/ingest/cli.ts` — read four regions, not skimmed. `cachedEtagFor` (roughly lines 145–150) — one line, and the whole `--force` argument rests on it. `ingestSeasonRankingsOnly` in full including its doc comment as 07-04 extended it (roughly lines 318–425) — specifically the 404 branch that logs `404 Not Found, skipping` and `continue`s WITHOUT touching any counter, the `result.status === 304` branch that increments `cacheHitCount` and `continue`s BEFORE any upsert, the `knownTeamKeys` guard and `unknownTeamCount`, 07-04's `nullRankingScoreCount`, and the closing `console.log`'s exact figure order. `main()`'s `rankingsOnly` dispatch branch and its per-season `recordIngestRun` call (roughly lines 500–520) — this is why a per-season invocation yields a per-season `ingest_runs` row and a `--years` invocation does not. And the module header's usage block, which 07-04 extended with the forced form.
    - `packages/corpus/db.ts` — `openCorpusReadOnly` (roughly lines 211–230) and its doc comment on multiple readers being safe under WAL; `openCorpus` (roughly lines 139–150) for `journal_mode = WAL`, which is why `data/corpus.sqlite-wal` exists and why its mtime is a liveness signal; and `upsertEventRanking` as 07-02 left it, for the `ON CONFLICT (event_key, team_key) DO UPDATE` merge semantics.
    - `packages/ingest/rankingsLive.test.ts` — as 07-04 left it. Read its `CORPUS_AVAILABLE` gate, its `existsSync` guard, its named skip message and its `openCorpusReadOnly` + `JOIN events` query shape. The new file copies all four conventions; do not invent a second style.
    - `packages/harness/digest.test.ts` — the module header and its `CORPUS_AVAILABLE` constant, the repo's canonical corpus-gated-test precedent (06.1-LEARNINGS § Patterns, "Corpus-gated end-to-end tracer test").
    - `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` — the per-season measured table (2022: 288 events / 236 populated / 0 null-body / 52 empty / 48 unregistered-team skips; 2023: 309 / 249 / 2 / 58 / 73; 2024: 324 / all cache hits, split unknown per note [3]; 2025: 350 / 311 / 8 / 31 / 67; 2026: 310 / 246 / 40 / 24 / 21; totals 1,581 / 1,322 / 47,695 rows) and its "recurring cost" section (1,582 requests, 253.8 s wall clock read from `ingest_runs`, ≈160 ms/request). Every floor in this task is drawn from that table.
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-04-SUMMARY.md` § "Documented (not auto-fixed) discrepancy" — three of five seasons honestly undershot the plan's own 250-per-season estimate. This is why every floor below sits deliberately under the measured value and why a shortfall is traced, never padded.
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-LEARNINGS.md` § Surprises, "TBA reports rankings for synthetic second-robot team keys" — why `unknownTeamCount` is a row-level counter that must stay out of the event-level closed sum.
    - `.claude/CLAUDE.md` § Conventions, "Secrets handling" — binding on every command in this task.
  </read_first>
  <behavior>
    - Per-season distinct events with at least one `event_rankings` row meet these floors: 2022 ≥ 210, 2023 ≥ 220, 2024 ≥ 250, 2025 ≥ 280, 2026 ≥ 215 — each roughly 10% below 06.1-04's measured value, because real TBA coverage legitimately moves and a pre-run figure is an expectation rather than ground truth.
    - `SELECT COUNT(*) FROM event_rankings` is at least 47,695 — the forced pass refreshes in place and nothing on this path deletes, so the count is monotone against 06.1-04's measurement.
    - Zero rows carry a NULL `record_wins`, `record_losses` or `record_ties`.
    - Zero rows carry a negative value in any of the three record columns.
    - At least 20,000 rows carry `record_wins` greater than zero — a non-vacuity floor, so the NULL and negativity checks cannot pass against an all-zero table.
    - Every season 2022–2026 has at least one row whose `ranking_score` differs from its own integer truncation, proving 07-02's `REAL` column type is load-bearing in every season rather than in one.
    - Zero rows violate `rank >= 1 AND total_teams >= 1` — 06.1-04's own invariant, re-asserted at the new row count.
    - The count of corpus `events` rows with no `event_rankings` row at all is greater than zero and lies between 150 and 400, bracketing 06.1-04's measured 259.
    - The whole block skips with a message naming `data/corpus.sqlite` when the corpus is absent, and makes no network call.
  </behavior>
  <action>
Write the census file first, run the ingest second. The file is this task's specification and its
evidence; writing it after the run would turn it into a description of whatever happened.

**Create `packages/ingest/corpusCensus.test.ts`.** Give it a module header stating what it is: a
corpus-wide census gate, asserting facts about the whole `data/corpus.sqlite` rather than about one
response or one event, written for plan 07-05's full 2022–2026 pass and left standing afterwards as
a regression guard for a write path `packages/ingest/cli.ts` makes untestable by import, because
that module calls `main()` at top level. State that it opens the corpus read-only, acquires no write
lock, writes no row and issues no network request of any kind — it is gated on corpus availability
alone and must never acquire a live-API gate.

Copy `rankingsLive.test.ts`'s environment-gate conventions exactly rather than inventing a second
style: `const CORPUS_PATH = "data/corpus.sqlite"`, `const CORPUS_AVAILABLE = existsSync(CORPUS_PATH)`,
and a `describe.skipIf`/`it.skip` form whose skip message names `CORPUS_PATH` literally so an absent
corpus is loud and never a silent pass. Declare `const SEASONS = [2022, 2023, 2024, 2025, 2026]` as
a module-level readonly, a `POPULATED_EVENT_FLOORS` record mapping each season to the floor listed
in `<behavior>`, and — in a comment beside it — the 06.1-04 measured value each floor derives from
and the reason the floor sits below it.

Add the describe block `"event_rankings — full-corpus census after the forced 2022-2026 pass (plan
07-05)"`. Open the corpus with `openCorpusReadOnly` once in a `beforeAll`. Scope every per-season
query through the same `JOIN events e ON e.event_key = …` + `WHERE e.year = ?` shape
`selectEventRankingsForSeason` uses, so the census and the production read agree on what "a season"
means. Write one case per `<behavior>` bullet. For the non-populated-event census use a `NOT EXISTS`
correlated subquery over `events` rather than a `LEFT JOIN … IS NULL`, so the intent — "this event
has no ranking row at all" — is readable in the SQL itself. Assert the 150–400 band with two
separate expectations rather than one compound, so a failure says which side it fell off.

**Then run the forced ingest — five separate per-season invocations, each with `--force`:**
`pnpm ingest:rankings --year 2022 --force`, then `--year 2023 --force`, `--year 2024 --force`,
`--year 2025 --force`, `--year 2026 --force`. Tee each one's combined output to
`data/ingest-logs/rankings-{year}.log` (create the directory first; `data/*` is gitignored, so these
logs stay machine-local evidence and never enter a commit).

Five per-season invocations rather than one `--years 2022-2026`, for three concrete reasons, all of
which should be stated in the commit message so the choice is not re-litigated later. First, 07-04's
`RankingScoreSortOrderError` propagates uncaught and aborts the run — scoping each invocation to one
season bounds that blast radius to one season instead of the whole pass. Second, `main()` calls
`recordIngestRun` once per season against a single `runId` whose `seasonStart`/`seasonEnd` span the
requested range, so only separate invocations yield five separate `ingest_runs` rows and therefore a
per-season wall clock read from durable state rather than from console timing. Third, a season
finishing is an observable progress event roughly every 45–60 seconds, which is the difference
between a run you can watch and a five-minute silence you have to guess about.

**`--force` is mandatory on every one of the five, and on any resume.** `cachedEtagFor` returns a
cached ETag unless forced, and `ingestSeasonRankingsOnly`'s 304 branch increments `cacheHitCount` and
`continue`s before reaching any upsert — so an unforced pass writes nothing at all and leaves 07-02's
four columns NULL across the entire corpus. 06.1-04 measured exactly this: all 324 of 2024's requests
returned 304 on a plain re-run. The resume case is sharper and must be handled deliberately: an
aborted forced season has already written FRESH ETags for the events it reached, so resuming that
season WITHOUT `--force` would 304-skip both the events it finished and — on their older 06.1-04
ETags — precisely the events it never backfilled, producing a partial backfill indistinguishable from
success. If a season aborts, re-run that one season with `--force`; the upsert merges, so the
already-done half costs about 45 seconds of re-fetch and nothing else.

Expect roughly 288 / 309 / 324 / 350 / 310 requests per season at ≈160 ms each on this network —
about 45–60 seconds per season, four to six minutes total, matching 06.1-04's measured 253.8 s for
the same 1,581-event surface. With `--force` in play every request is a fresh 200, so every season's
`cacheHitCount` should read 0; a non-zero cache-hit count in any season means the flag did not reach
`parseCliOptions` and the run must be stopped and investigated rather than accepted.

**Record all six figures from each season's summary line** — populated, null-body, empty-rankings,
cache hits, unregistered-team skips, null-ranking-score — plus the season's 404-skip count obtained
by `grep -c '404 Not Found, skipping' data/ingest-logs/rankings-{year}.log`. Then check the
EVENT-level closed sum per season: populated + null-body + empty-rankings + cache-hits + 404-skips
must equal `SELECT COUNT(*) FROM events WHERE year = ?`. `unknownTeamCount` and
`nullRankingScoreCount` are ROW-level counters and must be left out of that sum — including them
manufactures a mismatch that does not exist. Note that this closed sum is weaker than the alliances
one 07-03 built, because the rankings 404 branch increments no counter; the log grep is what closes
it, and that difference is worth one sentence in the commit message.

**2024 is the interesting season.** 06.1-04's note [3] records that all 324 of its requests returned
304 that run, so its null-body / empty-rankings split has never been measured — that plan declined,
correctly, to spend 324 requests purely to recover a classification. This pass forces 2024 anyway,
because the four new columns require it, so the split comes free. Record 2024's three-state figures
explicitly and note that they close 06.1-04's note [3]; Task 3 carries them into `COVERAGE.md`.

**If a run appears to stall, do not restart it.** A quiet log and a process list that came back empty
both lie, and this project has already paid for believing them: on 2026-08-25 a live media ingest was
declared dead from exactly those two signals, redone, and had in fact been alive and had already
durably committed two seasons. Before intervening, get all three of these to agree that nothing is
progressing: the mtime of `data/corpus.sqlite-wal` unchanged across two checks at least 60 seconds
apart (a live writer keeps touching it); the season's `event_rankings` row count unchanged across the
same interval, read with a fresh read-only connection; and an UNTRUNCATED process query filtered on
the command line — a PowerShell `Get-CimInstance Win32_Process` filtered with `Where-Object` on
`CommandLine` matching `packages/ingest/cli.ts`, never piped through `head` or `Select-Object -First`,
because this machine carries many long-lived stray node processes that push the ingest below any
truncation cutoff. Only when all three agree is intervention warranted, and better-sqlite3 commits
each statement immediately, so nothing already written is lost either way.

**Secrets discipline, binding on every command in this task.** The key reaches the process only
through `tsx --env-file=.env`, which is what the `pnpm ingest:rankings` script already is. Do not call
`Read` on `.env`. Do not `cat`, `head`, `tail` or `echo` it or any variable read from it. Do not
interpolate a key into a shell command, a log line, a test name, an assertion message, a tee'd log
path, a SUMMARY or a commit message. To confirm the key is set without revealing it, test presence
and length only, in the form `.claude/CLAUDE.md` § Conventions gives. Before committing, load the
values into shell variables, `grep -qF` for them across the staged diff and the commit message, and
report only a match count — never the matched text.

If any census case fails after the run, trace it to named event keys and real responses in the tee'd
logs and report what you found. Do not delete rows, do not lower a floor after seeing the result, and
do not re-run with different flags until the number agrees.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ingest/corpusCensus.test.ts -t "event_rankings"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/ingest/corpusCensus.test.ts -t "event_rankings"` passes with every case reported as RUN, not skipped, and completes in under 30 seconds. A skip here means the corpus precondition failed and must be reported, not accepted.
    - `pnpm typecheck` exits 0 and `pnpm test` passes across both vitest projects with no unexpected skips.
    - All five commands `pnpm ingest:rankings --year {2022..2026} --force` exited 0, each wrote its tee'd log under `data/ingest-logs/`, and each season's summary line is quoted verbatim in the commit message with all six of its figures.
    - For each of the five seasons, populated + null-body + empty-rankings + cache-hits + (the count of `404 Not Found, skipping` lines in that season's tee'd log) equals `SELECT COUNT(*) FROM events WHERE year = ?`, with both sides recorded as explicit numbers in the commit message.
    - Every season's reported `cacheHitCount` is `0`, confirming `--force` reached `parseCliOptions` on all five invocations.
    - A fresh read-only query executed after the last process exited — never the runs' own tallies — reports `0` rows in `event_rankings` with a NULL `record_wins`, `record_losses` or `record_ties`, and `0` rows with a negative value in any of the three.
    - A fresh read-only query reports at least `47695` total `event_rankings` rows and at least `20000` rows with `record_wins` greater than zero.
    - A fresh read-only query reports at least one row per season 2022–2026 whose `ranking_score` differs from its own integer truncation.
    - A fresh read-only query reports the count of `events` rows with no `event_rankings` row at all, and that count is greater than `150` and less than `400`; the exact number is recorded in the commit message beside 06.1-04's measured 259.
    - 2024's null-body and empty-rankings counts are recorded as explicit numbers in the commit message, with one sentence noting they close 06.1-04 COVERAGE.md note [3]'s unmeasured split.
    - `grep -c 'CORPUS_AVAILABLE' packages/ingest/corpusCensus.test.ts` prints at least `2`, and `grep -c 'data/corpus.sqlite' packages/ingest/corpusCensus.test.ts` prints at least `2` — the gate exists and its skip message names the missing path.
    - `grep -vE '^\s*(\*|//|/\*)' packages/ingest/corpusCensus.test.ts | grep -c 'tbaFetch\|fetchEvent\|tbaClient'` prints `0` — the census file is corpus-only and issues no network request. <!-- planner-discipline-allow: tbaFetch -->
    - `git diff --stat packages/ingest/` lists exactly one file, `packages/ingest/corpusCensus.test.ts`; `git diff --stat packages/corpus/` and `git diff package.json` are both empty.
    - `git status --porcelain` shows `.env` untracked and unstaged, `data/ingest-logs/` does not appear as untracked output, and `pnpm vitest run scripts/secrets-boundary.test.ts` passes.
    - No task output, log line, test name, assertion message, tee'd log path or commit message contains any value read from `.env`; the pre-commit `grep -qF` sweep reported a match count of zero.
    - `ls data/corpus.sqlite.lock` reports no such file after the last ingest process exits.
  </acceptance_criteria>
  <reversibility rating="costly">The new test file is source-only and `git revert`s cleanly. The forced pass, however, rewrites `event_rankings` across all 1,581 events in `data/corpus.sqlite` — a gitignored artifact that no git operation restores. Every row's prior `fetched_at`, and every ETag cache entry for `/event/{key}/rankings`, is overwritten with no recoverable prior value. The practical undo is one re-run of the same five commands against live TBA, roughly five minutes plus 1,581 requests against a donated service, which is the real cost worth naming. Not one-way: nothing outside this machine's corpus depends on it yet — no published artifact, no R2 key and no D1 row reads these columns until 07-08 and 07-10 — and the ingest is idempotent, so a re-run restores correctness at any point. Rated `costly` rather than `reversible` because the artifact it mutates is unrecoverable from version control, which is the property that matters here even though the data itself is re-fetchable.</reversibility>
  <done>`packages/ingest/corpusCensus.test.ts` exists with a corpus-gated `event_rankings` census block covering all five seasons, all five forced season invocations completed and are reconciled by a closed event-level sum, and every census claim is proven by a fresh read-only query executed after the writing processes exited — including 2024's three-state split, measured for the first time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The full-corpus alliances ingest, and the corpus-wide alliances census that proves it</name>
  <files>packages/ingest/corpusCensus.test.ts</files>
  <precondition>Running in the main working tree (not a git worktree); `.env` exists at the repo root with a populated `TBA_API_KEY`; `data/corpus.sqlite` exists locally with 07-02's `event_alliances` table present, `packages/ingest/cli.ts` carries 07-03's `ingestSeasonAlliancesOnly` and `--alliances-only`, `package.json` carries the `ingest:alliances` script, and 07-03's 2022 and 2024 alliance rows are already in the corpus; and no other ingest, publish or harness process holds `data/corpus.sqlite.lock`.</precondition>
  <read_first>
    - `packages/ingest/cli.ts` — `ingestSeasonAlliancesOnly` in full including its doc comment as 07-03 wrote it. Read four things rather than assume them: the 404 branch and its `notFoundCount`, the 304 branch's `cacheHitCount` early-continue, the tri-state branch on the parsed body (`null` -> `nullBodyCount`, `length === 0` -> `emptyAlliancesCount`, else `populatedCount`), and the deliberate ABSENCE of any unknown-team guard — `event_alliances.picks` is a JSON array with no team-key foreign key, so a synthetic key like `frc1165B` inside `picks` is stored rather than filtered.
    - `packages/corpus/db.ts` — `upsertEventAlliance` and `CorpusEventAlliance` as 07-02 left them: the `(event_key, alliance_number)` conflict target, and `picks` / `declines` / `status_raw` stored as JSON text with `name` admitting SQL NULL and never an empty string.
    - `packages/ingest/alliances.ts` — `normalizeEventAlliances`, for how `alliance_number` is derived from array position and how an absent `name` key becomes `null`.
    - `packages/ingest/corpusCensus.test.ts` — the file being modified, as Task 1 left it. The new block extends its existing `CORPUS_PATH` / `CORPUS_AVAILABLE` / `SEASONS` constants and `beforeAll` connection; do not open a second connection or declare a second gate.
    - `.planning/phases/07-event-pages/07-03-PLAN.md` — its Task 2 action (the two live seasons and why they were chosen), its Task 3 `COVERAGE.md` handoff paragraph stating that this plan will see 2022 and 2024 as ETag cache hits, and its `must_haves.truths` entry on `upsertEventAlliance`'s merge-not-duplicate conflict target.
    - `.planning/phases/07-event-pages/07-RESEARCH.md` § Open Questions, Question 2 — the three live-observed absent shapes (`2025bc` and `2026wvrox` returning a valid empty array despite both having real rankings; `2022ispr` returning a bare null body), the `picks` length of 3 or 4 with a 4th pick being simply `picks[3]`, and the `2024wvrox` absent-`name` case.
    - `.planning/phases/07-event-pages/07-CONTEXT.md` § D-16 and D-17 — why the pick count and the absent case matter downstream: D-16 sums the first three picks only, and D-17 disables the tab when alliance data is absent OR empty.
    - `.claude/CLAUDE.md` § Conventions, "Secrets handling" — binding on every command in this task.
  </read_first>
  <behavior>
    - Every season 2022–2026 has at least 100 distinct events carrying `event_alliances` rows — a deliberately low non-vacuity bound, not a prediction, since this quantity has never been measured corpus-wide.
    - `2025bc`, `2026wvrox` and `2022ispr` each exist as rows in `events` and each carry exactly zero `event_alliances` rows.
    - Every event with at least one `event_alliances` row has `MIN(alliance_number) = 1` and `MAX(alliance_number) = COUNT(*)` for that event — a contiguous seed sequence with no gap and no zero origin.
    - Every `event_alliances` row's `picks` parses as a JSON array whose length is between 1 and 4 inclusive.
    - The maximum `fetched_at` across all 2022 and 2024 `event_alliances` rows is strictly less than the minimum `fetched_at` across all 2023, 2025 and 2026 rows — 07-03's rows survived this pass untouched, neither refreshed nor blanked by the 304 short-circuit.
    - At least one `event_alliances` row corpus-wide carries a SQL NULL `name`, and zero rows carry an empty-string `name` — absence and null collapse to one honest representation, and the `Alliance {n}` display fallback stays 07-14's decision to make.
    - The block skips with a message naming `data/corpus.sqlite` when the corpus is absent, and makes no network call.
  </behavior>
  <action>
Same order as Task 1: extend the census file first, run the ingest second.

**Add the describe block `"event_alliances — full-corpus census after the 2022-2026 pass (plan
07-05)"`** to `packages/ingest/corpusCensus.test.ts`, reusing the existing `CORPUS_AVAILABLE` gate,
`SEASONS` constant and `beforeAll` connection. Declare two new module-level readonly constants beside
the existing ones: `ALLIANCE_EVENT_FLOOR = 100` and `ABSENT_ALLIANCE_EVENT_KEYS = ["2025bc",
"2026wvrox", "2022ispr"]`.

Put a comment on `ALLIANCE_EVENT_FLOOR` stating plainly why it is 100 and not a realistic estimate:
the corpus-wide count of events with alliance rows has never been measured — RESEARCH.md Q2's sample
was 40 events — so this bound exists to catch a season that silently ingested nothing, not to predict
the answer. Cite 06.1-04's own 250-events-per-season estimate, which three of five seasons then
honestly undershot, as the reason for erring low. The real per-season figures go into `COVERAGE.md` in
Task 3, which is where a measurement belongs.

Put a comment on `ABSENT_ALLIANCE_EVENT_KEYS` naming what each one is: `2025bc` and `2026wvrox` both
returned a valid 200 with an empty array despite carrying real rankings — events that ran
qualification matches but never held an alliance selection — and `2022ispr` returned a bare null body.
Assert both halves for each key: the event EXISTS in `events`, and it has zero `event_alliances`
rows. Asserting only the second half would pass vacuously against a typo'd key, which is exactly how
an absent-data test becomes a test of nothing.

For the contiguity case, use one grouped query — per `event_key`, select `MIN(alliance_number)`,
`MAX(alliance_number)` and `COUNT(*)`, then assert the number of groups where `MIN <> 1 OR MAX <>
COUNT` is zero. Report the offending event keys in the failure message rather than only a count, so a
failure is actionable without re-querying.

For the `picks` length case, prefer SQLite's `json_array_length(picks)` in the query. If that function
is unavailable in this better-sqlite3 build, fall back to selecting `picks` and parsing with
`JSON.parse` in the test body — the assertion is the same either way, only the layer moves. Alongside
the 1–4 bound, build the observed length histogram (how many rows at each length) and assert nothing
about its shape; it is recorded, not gated.

**Then run the ingest — five per-season invocations, no `--force`:** `pnpm ingest:alliances --year
2022`, then `--year 2023`, `--year 2024`, `--year 2025`, `--year 2026`. Tee each to
`data/ingest-logs/alliances-{year}.log`.

**The absence of `--force` here is deliberate and is the opposite call from Task 1.** Rankings had to
be forced because the four new columns cannot be backfilled from a bodyless 304. Alliances is a brand
new table: 2023, 2025 and 2026 have no ETag cached for `/event/{key}/alliances` at all, so they fetch
fresh without any flag, and 2022 and 2024 were already fetched fresh by 07-03 and their rows are
already correct. Forcing would spend 612 requests against a free, volunteer-run service to re-derive a
three-state split that 07-03 already wrote into `COVERAGE.md` — the same measurement-for-its-own-sake
move 06.1-04 declined for 2024 and was right to decline. Expect 2022 and 2024 to report 288 and 324
cache hits with zero populated; that is the ETag path working, not a shortfall, and it is why the
corpus row counts rather than this run's tallies are the evidence for those two seasons.

Per-season invocations rather than one `--years 2022-2026` for the same reasons as Task 1, plus one
specific to this endpoint: `tbaAllianceResponseSchema.parse` throws on an unmodelled shape, and 969
events across 2023/2025/2026 are being fetched fresh against a schema validated on a 40-event sample.
A per-season scope bounds a Zod throw to one season. Resumption here is cheaper than in Task 1 and
needs no flag: because the pass is unforced, a re-run of an aborted season 304-skips every event it
already completed and picks up where it stopped.

Expect roughly 1,581 requests total at ≈160 ms each — about four to six minutes across the five
seasons, with 2022 and 2024 being pure 304 traffic that still costs the unconditional 100 ms throttle
per request.

**Record all five counters from each season's summary line** and check the closed invariant 07-03
built them for: populated + null-body + empty-alliances + cache-hits + not-found must equal
`SELECT COUNT(*) FROM events WHERE year = ?` **exactly**, for every season. Unlike the rankings sum in
Task 1, this one needs no log grep, because `ingestSeasonAlliancesOnly` counts its 404s. If any season
fails to close, stop and report the discrepancy rather than reconciling it by hand — a closed
invariant that has to be talked into closing is not one.

**Record the observed `picks` length histogram across all five seasons**, and if any alliance carries
fewer than three picks, record the event keys and flag them to 07-14. D-16 sums the first three picks
only; an alliance with two picks is a real input to that rule, and this plan is the only one in the
phase that looks at all 1,581 events. Do not assume the 3-or-4 shape holds corpus-wide just because it
held in a 40-event sample.

**Liveness, if a run appears to stall:** the same three-signal rule as Task 1, and the same
prohibition on restarting without it — `data/corpus.sqlite-wal` mtime across two checks at least 60
seconds apart, the season's `event_alliances` row count across the same interval, and an UNTRUNCATED
command-line-filtered process query never piped through `head` or `Select-Object -First`. All three
must agree before intervening.

**Secrets discipline, binding on every command in this task,** identical to Task 1: the key reaches
the process only through the `pnpm ingest:alliances` script's `tsx --env-file=.env`; no `Read`, `cat`,
`head`, `tail` or `echo` of `.env` or anything read from it; no interpolation into a command, log
line, test name, assertion message, tee'd log path, SUMMARY or commit message; presence-and-length
only to confirm it is set; and a pre-commit `grep -qF` sweep over the staged diff and commit message
reporting a match count and never the matched text.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ingest/corpusCensus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/ingest/corpusCensus.test.ts` passes with BOTH describe blocks reported as RUN, not skipped, and completes in under 30 seconds.
    - `pnpm typecheck` exits 0 and `pnpm test` passes across both vitest projects with no unexpected skips.
    - All five commands `pnpm ingest:alliances --year {2022..2026}` exited 0, and each season's summary line is quoted verbatim in the commit message with all five of its counters.
    - For each of the five seasons, populated + null-body + empty-alliances + cache-hits + not-found equals `SELECT COUNT(*) FROM events WHERE year = ?` exactly, with both sides recorded as explicit numbers in the commit message.
    - 2022 reports `288` cache hits and `0` populated, and 2024 reports `324` cache hits and `0` populated — the realized form of 07-03's recorded handoff, noted as such in the commit message rather than reported as a shortfall.
    - A fresh read-only query executed after the last process exited reports at least `100` distinct events with `event_alliances` rows in every one of the five seasons, and the exact per-season figures are recorded in the commit message.
    - A fresh read-only query reports that `2025bc`, `2026wvrox` and `2022ispr` each exist in `events` and each have exactly `0` rows in `event_alliances`.
    - A fresh read-only query reports `0` events whose `alliance_number` sequence is non-contiguous, i.e. zero groups where `MIN(alliance_number) <> 1 OR MAX(alliance_number) <> COUNT(*)`.
    - A fresh read-only query reports `0` rows whose `picks` array length falls outside 1–4, and the observed length histogram across all five seasons is recorded in the commit message.
    - Any alliance with fewer than three picks is enumerated by event key in the commit message and flagged as an input to 07-14's D-16 rule; if there are none, the commit message says so explicitly rather than omitting the point.
    - A fresh read-only query reports `MAX(fetched_at)` over 2022 and 2024 rows strictly less than `MIN(fetched_at)` over 2023, 2025 and 2026 rows, with both timestamps recorded.
    - A fresh read-only query reports at least `1` row with a SQL NULL `name` and exactly `0` rows whose `name` is an empty string.
    - `grep -c 'ABSENT_ALLIANCE_EVENT_KEYS' packages/ingest/corpusCensus.test.ts` prints at least `2` (declaration and use), and `grep -c 'ALLIANCE_EVENT_FLOOR' packages/ingest/corpusCensus.test.ts` prints at least `2`.
    - `git diff --stat packages/ingest/` lists exactly one file, `packages/ingest/corpusCensus.test.ts`; `git diff --stat packages/corpus/` and `git diff package.json` are both empty.
    - `git status --porcelain` shows `.env` untracked and unstaged, and `pnpm vitest run scripts/secrets-boundary.test.ts` passes.
    - No task output, log line, test name, assertion message, tee'd log path or commit message contains any value read from `.env`; the pre-commit `grep -qF` sweep reported a match count of zero.
    - `ls data/corpus.sqlite.lock` reports no such file after the last ingest process exits.
  </acceptance_criteria>
  <reversibility rating="costly">The census block is source-only and `git revert`s cleanly. The pass writes `event_alliances` rows for 2023, 2025 and 2026 into the gitignored `data/corpus.sqlite`, which no git operation restores, and writes ETag cache entries for 969 `/event/{key}/alliances` URLs that had none. The practical undo is `DELETE FROM event_alliances` for those three seasons by hand, or a re-run — roughly four minutes and about 969 fresh requests against a donated service. Not one-way: the table is this phase's own, nothing published reads it until 07-08 and 07-10, and `upsertEventAlliance`'s `(event_key, alliance_number)` conflict target makes the ingest idempotent, so a re-run restores correctness at any point. Lower blast radius than Task 1, which overwrote 47,695 pre-existing rows; this one is additive for three seasons and a no-op for two. Rated `costly` for the same reason as Task 1 — the artifact it mutates is unrecoverable from version control.</reversibility>
  <done>`event_alliances` is populated for all five seasons, every season's five-counter tally closes exactly against its corpus event count, the three live-observed absent events hold zero rows rather than having raised errors, 07-03's 2022 and 2024 rows are proven untouched, and the whole census file passes green against the real corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the measured cost into COVERAGE.md — both endpoints, five seasons, against 06.1-04's baseline</name>
  <files>.planning/phases/07-event-pages/COVERAGE.md</files>
  <precondition>Tasks 1 and 2 are committed, `data/ingest-logs/rankings-{2022..2026}.log` and `data/ingest-logs/alliances-{2022..2026}.log` all exist locally, and `data/corpus.sqlite` carries both endpoints' full 2022–2026 data so the `ingest_runs` timings and corpus counts can be read fresh.</precondition>
  <read_first>
    - `.planning/phases/07-event-pages/COVERAGE.md` — the file being modified, read in full as 07-03 left it. Two parts matter: the capability matrix itself (every row's `INTEGRATE`/`OPT-OUT` decision and reason, which this task does not touch), and the measured-cost section carrying 07-03's 2022 and 2024 alliance figures plus its closing handoff paragraph predicting that this plan would see those two seasons as ETag cache hits. That paragraph is what Task 3 realizes.
    - `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` § "The recurring cost this integration commits to" — the format to follow exactly: command, date, total requests, cache hits, fresh, elapsed wall clock read from `ingest_runs.started_at`/`finished_at` rather than console timing, observed per-request time, budget guidance, then the per-season table. Also read note [3], the unmeasured 2024 split this plan closes.
    - `packages/corpus/schema.sql` — the `ingest_runs` table (`run_id`, `started_at`, `finished_at`, `season_start`, `season_end`, `request_count`, `cache_hit_count`, `completed`), which is where the durable per-season wall clock lives.
    - `data/ingest-logs/rankings-{2022..2026}.log` and `data/ingest-logs/alliances-{2022..2026}.log` — this plan's own run evidence, for the per-season summary lines and the rankings 404-skip counts.
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-LEARNINGS.md` § Lessons, "When a plan's descriptive prose and its own reference implementation disagree, the acceptance criteria is the authoritative contract" and the project failure-log entry about documentation describing a model that had been deleted — why a measured-cost section that goes stale is a real failure mode here and not bookkeeping.
  </read_first>
  <action>
Add one section to `.planning/phases/07-event-pages/COVERAGE.md`, headed `## Measured cost — the
real full-corpus 2022-2026 pass (plan 07-05)`, placed after 07-03's existing measured-cost section
rather than replacing it: 07-03's figures are the authoritative record of 2022's and 2024's fresh
alliance fetch and must stay exactly where they are.

Follow 06.1-04's `COVERAGE.md` format exactly, so the two phases' cost records are readable side by
side. Cover both endpoints, each with its own subsection.

**Rankings subsection.** State the five commands actually run (`pnpm ingest:rankings --year {year}
--force`, one per season) and the run date. State plainly, in prose, that `--force` was mandatory
rather than optional — that `cachedEtagFor` returns a cached ETag unless forced and the 304 branch
continues before any upsert, so an unforced pass would have written nothing and shipped all four of
07-02's new columns NULL, and that 06.1-04 measured all 324 of 2024's requests returning 304 on
exactly such a re-run. Then the per-season table with one row per season and columns: events,
populated, null-body, empty-rankings, cache hits, 404 skips, unregistered-team skips,
null-ranking-score rows, requests, wall clock. Read each season's wall clock from that season's own
`ingest_runs` row (`finished_at` minus `started_at`), never from console timing — 06.1-04's stated
discipline of trusting stored state over an in-memory tally. Add a totals row.

Immediately under that table, add a short paragraph headed **2024's three-state split, measured for
the first time**, giving 2024's null-body and empty-rankings figures and stating in one sentence that
06.1-04's COVERAGE.md note [3] recorded this split as unknown because that plan correctly declined to
spend 324 requests purely to recover a classification — and that this pass forced 2024 for the
independent reason that the four new columns require it, so the measurement came free. Do not edit
06.1's COVERAGE.md; that phase is sealed and this cross-reference is the whole record.

**Alliances subsection.** State the five commands (`pnpm ingest:alliances --year {year}`, no
`--force`) and the run date, and state the reasoning for the absent flag in one or two sentences: 2023,
2025 and 2026 had no cached ETag for this brand-new endpoint and fetched fresh without it, while 2022
and 2024 were already fetched fresh by 07-03, so forcing them would have spent 612 requests to
re-derive a split already written down in this same file. Then the per-season table: events,
populated, null-body, empty-alliances, cache hits, not-found, alliance rows written, distinct events
with rows, requests, wall clock — with a totals row, and 2022's and 2024's rows showing their
all-cache-hit tally beside a pointer to 07-03's section for their real three-state split. Note that
this endpoint's five counters close exactly against each season's event count, whereas the rankings
tally needs the log's 404-skip lines to close, because `ingestSeasonRankingsOnly`'s 404 branch
increments no counter.

**Corpus-state subsection**, common to both endpoints, sourced from fresh read-only queries rather
than from either run's tally. Give: total `event_rankings` rows and distinct populated events per
season, against 06.1-04's 47,695 and 1,322; the count of corpus events with no ranking row at all,
beside 06.1-04's measured 259; total `event_alliances` rows and distinct events with rows per season,
labelled as the first corpus-wide measurement of that quantity; the `picks` length histogram; and the
three named zero-row events. Where a figure undershoots 06.1-04's baseline, record it as measured with
its traced cause and say so plainly — a pre-run figure is an expectation, not ground truth, and
06.1-04's own three-of-five-seasons undershoot is the precedent for reporting rather than padding.

**Budget guidance**, one short paragraph: what a future full re-ingest of each endpoint costs in wall
clock and requests, and the standing warning that any future backfill of a column added after a prior
ingest requires `--force`, including on a resume — because an aborted forced season has already
written fresh ETags for the events it reached, so an unforced resume would 304-skip both the finished
events and, on their older ETags, precisely the events it never backfilled.

Finally, update 07-03's closing handoff paragraph in place: it predicted this pass would see 2022 and
2024 as cache hits and offered the choice of citing its figures or re-fetching with `--force`. Replace
the prediction with the realized outcome — which option was taken and why — and leave a forward
pointer to this new section. Change nothing else in that section, and change nothing in the capability
matrix; 07-03 owns those decisions and this task is a cost record, not a re-decision.

Verify the file mechanically before committing: every `OPT-OUT` row in the capability matrix still
carries a non-empty reason, since a malformed matrix blocks the phase seal at `verify:pre` and an edit
elsewhere in the file must not disturb it.

**Secrets discipline:** no value from `.env` reaches this document, any table cell, or the commit
message. The tee'd logs are read for counters only; nothing is pasted from them without being read
first.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ingest/corpusCensus.test.ts && awk -F'|' '/OPT-OUT/ { if (length($4) < 10) { print "unreasoned OPT-OUT: " $0; exit 1 } }' .planning/phases/07-event-pages/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Measured cost — the real full-corpus 2022-2026 pass (plan 07-05)' .planning/phases/07-event-pages/COVERAGE.md` prints `1`.
    - The rankings per-season table has one row per season 2022–2026 plus a totals row, and every cell is a real number read from that season's tee'd log or its own `ingest_runs` row — no cell reads `TBD`, `~`, `n/a` or an em dash placeholder.
    - The alliances per-season table has one row per season 2022–2026 plus a totals row, with 2022 and 2024 showing `288` and `324` cache hits and pointing to 07-03's section for their three-state split.
    - Every wall-clock figure in both tables is stated as read from `ingest_runs.finished_at` minus `started_at`, and the file says so in prose.
    - The 2024 three-state paragraph exists, gives both figures as numbers, and names 06.1-04's COVERAGE.md note [3] as the gap it closes.
    - The corpus-state subsection reports total `event_rankings` rows against 06.1-04's 47,695, distinct populated events against its 1,322, the no-ranking-event count against its 259, and the first corpus-wide `event_alliances` figures — each as a number, each labelled as read from a fresh read-only query.
    - The budget-guidance paragraph states the `--force`-required-on-resume rule explicitly, including the reason an unforced resume silently skips exactly the un-backfilled events.
    - 07-03's closing handoff paragraph now records the realized outcome rather than a prediction, and points forward to the new section.
    - `awk -F'|' '/OPT-OUT/ { if (length($4) < 10) exit 1 }' .planning/phases/07-event-pages/COVERAGE.md` exits `0` — every `OPT-OUT` row still carries a non-empty reason.
    - `git diff --stat` for this task lists exactly one file, `.planning/phases/07-event-pages/COVERAGE.md`.
    - `pnpm vitest run packages/ingest/corpusCensus.test.ts` still passes with both blocks RUN — the documentation edit changed no behavior.
    - No value read from `.env` appears in the document or the commit message.
  </acceptance_criteria>
  <reversibility rating="reversible">A single documentation file edit, fully undone by `git revert`. No corpus write, no network request, no published artifact. The only durable effect is that a measurement which would otherwise live only in a transcript becomes part of the repository.</reversibility>
  <done>`COVERAGE.md` carries the real measured cost of both endpoints' full 2022–2026 pass — per-season tables sourced from `ingest_runs` and the tee'd logs, a corpus-state subsection sourced from fresh read-only queries and reconciled against 06.1-04's baseline, 2024's first-ever three-state split, and the standing `--force`-on-resume warning — with the capability matrix untouched and still well-formed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBA JSON -> `tbaEventRankingsResponseSchema` / `tbaAllianceResponseSchema` -> `event_rankings` / `event_alliances` | ~3,163 third-party, volunteer-maintained responses cross into durable local state in this one plan, including all three absent shapes. 07-08 later treats every stored row as fact and 07-11/07-14 render it as an authoritative standing. |
| `cachedEtagFor` -> the 304 early-continue -> 07-02's four nullable columns | A *silence* boundary, not a data one: without `--force`, every request succeeds, every season reports a healthy tally, the run exits 0, and nothing at all is written. This is the boundary where Phase 7's most likely failure lives, and no automated gate in the pipeline defends it — only this plan's flag discipline and `corpusCensus.test.ts`'s after-the-fact NULL assertion. |
| `.env` `TBA_API_KEY` -> `process.env` -> `tbaFetch`'s `X-TBA-Auth-Key` header | A live credential crosses into ten separate live invocations, and — in this repo's own history — into a subagent transcript. Phase 4 plan 04-01 lost an R2 token exactly this way. |
| This repo -> TBA's free, volunteer-run public API | A resource boundary crossed ~3,163 times in this plan alone. Every request is a real cost borne by a donated service the entire project depends on, and a needless restart doubles a whole season of it. |
| Ingest process -> `data/corpus.sqlite` + `.lock` + `-wal` | Ten sequential write sessions against a 359 MB gitignored artifact that no git operation can restore, contended by any concurrent ingest, publish or harness run. |
| A run's in-memory console tally -> a claim about the corpus | The tally is what the process believed; the corpus is what exists. An interrupted-then-resumed season already made these disagree once (06.1-04), and every claim in this plan is deliberately routed around the tally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | `--force` omitted from a rankings invocation or from a resume, silently shipping four NULL columns | high | mitigate | Every one of the five rankings invocations carries `--force`, stated in Task 1's action, in its acceptance criteria, in `must_haves.truths`, and permanently in `COVERAGE.md`'s budget guidance. Two independent detectors, neither relying on the operator remembering: every season's reported `cacheHitCount` must be `0` (a non-zero value means the flag never reached `parseCliOptions`), and `corpusCensus.test.ts` asserts zero NULL `record_wins`/`record_losses`/`record_ties` across the whole table after the fact. The resume case is called out separately because it is strictly worse — a resumed unforced season 304-skips both the events it finished and, on their older 06.1-04 ETags, exactly the events it never backfilled. 06.1-04 measured all 324 of 2024's requests returning 304 on precisely such a re-run, so this is a measured failure mode, not a hypothetical. |
| T-07-05-02 | Information Disclosure | `TBA_API_KEY` reaching a transcript, log file, test name or commit message across ten live invocations | high | mitigate | The key reaches each process only through `tsx --env-file=.env`, which is what the existing `pnpm ingest:rankings` and `pnpm ingest:alliances` scripts already are — the tool reads the file, no agent does. `tbaApiKey()` reads `process.env` and the value flows only into `tbaFetch`'s `X-TBA-Auth-Key` header; `tbaClient.ts`'s module header already states the module never logs it and never embeds it in a returned value, and this plan changes no source on that path. Both run tasks forbid `Read`/`cat`/`head`/`tail`/`echo` of `.env` and forbid interpolating a key into a command, log line, test name, assertion message, tee'd log path, SUMMARY or commit message; presence-and-length is the only permitted confirmation. Tee'd logs land under `data/ingest-logs/`, matched by `.gitignore`'s `data/*`, so a captured log cannot reach a commit even by accident. A pre-commit `grep -qF` sweep over the staged diff and commit message reports a match count and never the matched text, and `scripts/secrets-boundary.test.ts` stays green. This is a control against a failure that has already happened in this repo, not a hypothetical. |
| T-07-05-03 | Repudiation | A run's console tally standing in for corpus truth | high | mitigate | Every claim this plan makes about the corpus is asserted by `packages/ingest/corpusCensus.test.ts` against a fresh read-only connection opened after the writing process exited, never by an in-memory counter — 06.1-04's repudiation-resistance discipline, adopted there after an interrupted-then-resumed season made a tally under-report. The console counters are recorded separately, as evidence of what the RUN saw, and the two are reconciled by the closed per-season sums rather than conflated. `COVERAGE.md`'s wall-clock figures come from `ingest_runs.started_at`/`finished_at` — durable state — rather than from console timing, for the same reason. |
| T-07-05-04 | Tampering | A partial pass that later reads as complete — a Zod throw or 07-04's `RankingScoreSortOrderError` aborting mid-season | high | mitigate | Both endpoints run as five per-season invocations rather than one ranged pass, bounding any abort's blast radius to one season. `better-sqlite3` commits each statement immediately, so a killed season loses nothing already written. Detection does not depend on noticing the abort: the closed per-season sums must equal `SELECT COUNT(*) FROM events WHERE year = ?` for all five seasons on both endpoints, and the census floors are per-season rather than aggregate, so a silently-missing season cannot hide inside a healthy total. The two resume protocols differ and are both stated explicitly — forced for rankings, unforced for alliances — because using the wrong one is itself the T-07-05-01 failure. |
| T-07-05-05 | Denial of Service | TBA's free, volunteer-run public API — ~3,163 requests in this plan | medium | mitigate | `tbaFetch`'s 100 ms `THROTTLE_INTERVAL_MS` is enforced inside the client so no call site can bypass it, and is unchanged. The alliances pass deliberately omits `--force`, saving 612 requests that would only re-derive a figure 07-03 already recorded — carried as this plan's first `must_haves.prohibitions` entry. The rankings force is required by T-07-05-01 and is not discretionary. The total is the same 1,581-event surface 06.1-04 already ran once, twice, not an expansion of scope. |
| T-07-05-06 | Repudiation | Declaring a live run dead from a quiet log or a truncated process list, then restarting it | medium | mitigate | Both run tasks carry the three-signal liveness rule and the explicit prohibition on restarting before all three agree: `data/corpus.sqlite-wal` mtime unchanged across two checks at least 60 seconds apart, the season's row count unchanged across the same interval, and an UNTRUNCATED command-line-filtered process query never piped through `head` or `Select-Object -First`. This is a control against a documented incident — 2026-08-25, Phase 6 plan 06-03, where a live media ingest was declared dead from exactly the two lying signals, redone, and had in fact been alive with two seasons durably committed. Carried as this plan's second `must_haves.prohibitions` entry, because the cost of getting it wrong is borne by TBA. |
| T-07-05-07 | Tampering | A measured shortfall against 06.1-04's baseline resolved by padding, deleting or re-running until the number agrees | medium | mitigate | Every floor in the census sits deliberately below 06.1-04's measured value, and each carries a comment naming the measurement it derives from and why it errs low. Both run tasks forbid deleting rows, lowering a floor after seeing the result, and re-running with different flags until the count agrees; a shortfall is traced to named event keys and real responses in the tee'd logs and reported as measured. The precedent is 06.1-04's own three-of-five-seasons undershoot of its 250-per-season estimate, which its SUMMARY correctly recorded rather than fixed. Carried as this plan's third `must_haves.prohibitions` entry. |
| T-07-05-08 | Denial of Service | `data/corpus.sqlite.lock` contention across ten sequential write sessions | low | mitigate | Both tasks' preconditions require that no ingest, publish or harness process holds the lock. `cli.ts`'s `main()` closes the corpus in a `finally` and `openCorpus`'s `close` override unlinks the lock; if the lock is genuinely held, `openCorpus` throws its existing already-open message and the run fails loudly rather than corrupting anything. `corpusCensus.test.ts` opens read-only via `openCorpusReadOnly` and never acquires the write lock, so the census can safely run against a corpus another process is reading. |
| T-07-05-09 | Tampering | Running this plan inside a git worktree, stranding every ingested row | high | mitigate | `data/corpus.sqlite` is gitignored and is not copied into a worktree, so a pass run inside one writes rows that never merge back while the tracked `corpusCensus.test.ts` does — and on `main` that file's `existsSync` guard would then skip and report the suite green over the exact invariant meant to catch it. Both run tasks state the main-working-tree requirement as an explicit `<precondition>`, and the objective names `workflow.use_worktrees=false` as the standing mitigation for any run containing this plan. Phase 5 plan 05-02 and Phase 06.1 both hit this failure mode; it is documented, not speculative. |
| T-07-05-10 | Spoofing | A value stored under `ranking_score` that came from an unasserted `sort_orders` position | high | transfer | Transferred wholly to 07-04, which owns the guard: `normalizeEventRankings` asserts `sort_order_info` at the named index equals `RANKING_SCORE_SORT_ORDER_NAME` before reading any value there, and throws `RankingScoreSortOrderError` naming the event and the observed name otherwise, with the guard and the read structurally inseparable in one function. This plan's contribution is exposure at scale rather than a new control: it is the first time that guard meets all 1,581 events across five seasons instead of 07-04's single forced season and seven-event live sample, so an abort here is the guard working. Recorded as `transfer` rather than `accept` so the dependency is visible. <!-- planner-discipline-allow: RankingScoreSortOrderError --> |
| T-07-05-11 | Elevation of Privilege | Event keys interpolated into `/event/{key}/rankings` and `/event/{key}/alliances` request paths | low | accept | Canon injection concern, filed as a breadcrumb rather than a new control: every event key on both paths comes from the corpus's own `events` table, populated by a prior Zod-parsed ingest, never from user input; `tbaFetch`'s template-literal path join is pre-existing and unchanged by this plan; and every corpus write uses named `db.prepare` parameters. |
| T-07-05-SC | Tampering | npm/pip/cargo installs | high | accept | Not applicable to this plan or this phase — `07-RESEARCH.md` § Package Legitimacy Audit records "this phase installs no new external packages," and no task here runs a package manager or adds a dependency. `git diff package.json` must be empty for both run tasks. Retained as a register row so a later task adding one is a visible deviation rather than a silent one. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm test` passes across both vitest projects with no unexpected skips.
- `pnpm vitest run packages/ingest/corpusCensus.test.ts` passes with BOTH describe blocks reported as RUN, not skipped — a skip means the corpus precondition failed and must be reported, not accepted.
- `pnpm vitest run packages/ingest/rankings.test.ts packages/ingest/rankingsLive.test.ts packages/ingest/alliances.test.ts` still passes — 07-03's and 07-04's gates are unaffected by a full-corpus pass.
- `git diff --stat` for the whole plan touches exactly two files: `packages/ingest/corpusCensus.test.ts` and `.planning/phases/07-event-pages/COVERAGE.md`. Any other file in the diff is a scope deviation to report.
- `git diff --stat packages/corpus/`, `git diff --stat packages/harness/`, `git diff --stat apps/` and `git diff package.json` are all empty — this plan runs what other plans built and changes no pipeline source.
- `git log --oneline` shows one commit per task, each scoped `07-05`.
- `git status --porcelain` shows `.env` untracked and unstaged, no `data/ingest-logs/` entry (matched by `.gitignore`'s `data/*`), and `pnpm vitest run scripts/secrets-boundary.test.ts` passes.
- `ls data/corpus.sqlite.lock` reports no such file.
- All ten live invocations exited 0, and each season's summary line is quoted verbatim in its task's commit message.
</verification>

<success_criteria>
- `event_rankings` is force-refreshed across all five seasons with `record_wins`, `record_losses`, `record_ties` and `ranking_score` populated — zero NULL record columns corpus-wide, zero negative values, at least 47,695 rows, and at least one non-integral ranking score in every season.
- Every rankings season's event-level paths close exactly against its corpus event count once the log's 404-skip lines are counted, and every season reports `0` cache hits — the proof that `--force` was in effect on all five.
- 2024's null-body / empty-rankings split is measured for the first time and recorded, closing 06.1-04 COVERAGE.md note [3].
- `event_alliances` is populated for all five seasons, with every season's five counters closing exactly against its event count and 2022/2024 reading as all-cache-hits against 07-03's already-fetched rows.
- The events with no official ranking land as zero rows and zero errors: all ten invocations exit 0 and the corpus-wide no-ranking-event count sits in the 150–400 band around 06.1-04's measured 259.
- `2025bc`, `2026wvrox` and `2022ispr` each exist in `events` and each hold exactly zero `event_alliances` rows — D-17's absent case proven reachable from real data rather than from a defensive branch nothing exercises.
- `alliance_number` is contiguous 1..N for every event corpus-wide, every `picks` array length falls in 1–4 with the histogram recorded, and any sub-three-pick alliance is enumerated and flagged to 07-14's D-16 rule.
- 07-03's 2022 and 2024 alliance rows are proven untouched by this pass — their maximum `fetched_at` is strictly earlier than the minimum across the three freshly-fetched seasons.
- `packages/ingest/corpusCensus.test.ts` exists, is gated on corpus availability with a loud skip naming `data/corpus.sqlite`, makes no network call, and stands afterwards as a repeatable census gate for a write path that cannot be imported.
- Every corpus claim is proven by a fresh read-only query executed after the writing processes exited, never by a run's own tally, and every wall-clock figure comes from `ingest_runs` rather than console timing.
- `COVERAGE.md` carries the real measured cost of both endpoints across all five seasons, reconciled against 06.1-04's 47,695 / 1,322 / 259 baseline, with the capability matrix untouched and every `OPT-OUT` row still reasoned.
- Every run-level probe-edge half this plan owns (EVNT-02 `empty`/`ordering`/`adjacency`, EVNT-05 `empty`/`ordering`/`adjacency`) is asserted by a passing case in `corpusCensus.test.ts`, each cross-referenced to its storage, ingest-logic and rendered owners, and this plan's four backstop markers are the only truths not machine-checked.
- No secret value from `.env` appears in any log, test name, assertion message, document or commit message; the pre-commit `grep -qF` sweep reported a match count of zero.
</success_criteria>

<output>
Create `.planning/phases/07-event-pages/07-05-SUMMARY.md` when done.
</output>
