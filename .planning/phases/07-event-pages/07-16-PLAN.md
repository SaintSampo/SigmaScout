---
phase: 07-event-pages
plan: 16
type: execute
wave: 11
depends_on: ["07-09", "07-10", "07-15"]
files_modified:
  - packages/core/algorithms/sigma1/index.ts
  - packages/core/algorithms/sigma1/sigma1.test.ts
  - packages/core/algorithms/sigma1/params.test.ts
  - packages/core/algorithms/sigma1/carryover.test.ts
  - packages/core/algorithms/sigma1/rp/distribution.test.ts
  - data/algorithm-versions/vpr@2.0.0+tracer-check.json
  - data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json
  - packages/harness/cli.ts
  - packages/harness/manifests.ts
  - packages/harness/manifestSchemas.ts
  - packages/harness/promote.ts
  - packages/harness/publish.ts
  - packages/harness/publishedAlgorithms.ts
  - packages/harness/percentiles.ts
  - packages/harness/fixtures/extract-digest-slice.ts
  - packages/harness/algorithmIdentity.test.ts
  - packages/harness/promotedOverrides.test.ts
  - packages/harness/cli.promotedStaleness.test.ts
  - packages/harness/manifests.test.ts
  - packages/harness/publish.test.ts
  - packages/harness/predictions.test.ts
  - packages/harness/metricHistory.test.ts
  - packages/harness/stateSnapshot.test.ts
  - packages/harness/report.test.ts
  - packages/harness/artifact.test.ts
  - packages/harness/payloadBudget.test.ts
  - packages/harness/baselineFingerprint.test.ts
  - packages/harness/rounding.test.ts
  - packages/harness/percentiles.test.ts
  - packages/harness/browserSafeSchemas.test.ts
  - scripts/replayRig.ts
  - scripts/replayRig.test.ts
  - apps/worker/src/scheduled.ts
  - apps/worker/src/env.ts
  - apps/worker/src/stateStore.ts
  - apps/worker/wrangler.toml
  - apps/worker/migrations/0001_algorithm_state.sql
  - apps/worker/test/liveAlgorithmTier.test.ts
  - apps/worker/test/liveWindows.test.ts
  - apps/worker/test/readScopedStateSql.test.ts
  - apps/worker/test/scheduled.test.ts
  - apps/worker/test/scheduled.replay.test.ts
  - apps/worker/test/stateStore.test.ts
  - apps/web/src/lib/searchParams.ts
  - docs/worker-operations.md
autonomous: true
requirements: [EVNT-02, EVNT-03, EVNT-04, EVNT-05, EVNT-06]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every artifact key the publisher would write after this plan carries `vpr@{version}` in its algorithm segment, because `artifactKey` interpolates `algorithm.id` and `algorithm.id` now resolves to `vpr` through exactly one chain: the registry entry in `packages/core/algorithms/sigma1/index.ts` supplies the base id, and `applyPromotedOverrides` replaces it with the module built from the committed promoted version file, whose own `id` field reads `vpr` (D-04, D-05). Proven by a dry-run publish whose emitted key list contains the new segment and no occurrence of the old one — not by reading the source."
    - "The published algorithms manifest names the SAME id the publisher writes under, structurally rather than by coincidence: `buildAlgorithmsManifest` reads the third entry's `id` straight out of the promoted version file (`promoted.id`), the same file `applyPromotedOverrides` pins, so a manifest id and an artifact key segment cannot disagree without the version file disagreeing with itself. This is the single highest-severity integrity surface in the plan (T-07-16-01) and it is closed by construction, not by a check."
    - "**Nothing the deployed browser reads changes in this plan.** `PUBLISHED_ALGORITHM_IDS` still reads `opr, epa, sigma1`; `DEFAULT_ALGORITHM` still holds the pre-rename id; `AlgorithmSelect.tsx`'s label map and every `apps/web` runtime path are byte-identical. A fresh visitor therefore still requests the `sigma1@` keys that 07-10 published and that 07-17 leaves in place — there is no wave in which the client reads a prefix nothing has written (outline assumption 7). Proven by `git diff --numstat` reporting zero across `apps/web/src` and `apps/web/e2e`, with `apps/web/src/lib/searchParams.ts` the one exception and its diff confined to comment lines (EVNT-02..EVNT-06)."
    - "The two tiers are carried by two named constants, not by one constant meaning different things in different weeks: `PUBLISHED_ALGORITHM_IDS` states which ids currently have live objects in R2 that a browser may request, and the new `PIPELINE_ALGORITHM_IDS` states which ids the publisher and the Worker WRITE under. Through waves 11 and 12 those sets genuinely differ, and both statements are true at once. 07-18 collapses them back to one by deleting `PIPELINE_ALGORITHM_IDS` and setting `PUBLISHED_ALGORITHM_IDS` to the renamed triple — a deletion its own plan owns and this plan's assertion exclusion list makes visible."
    - "The rename moves no predicted number. `promote.ts`'s reproducibility digest hashes `[matchKey, pRedWin, redScore, blueScore]` and deliberately does not include `algorithmId` (CONTEXT.md D-05's own verified correction), so `digest.test.ts` stays green against the committed version files under their new names with no fixture regeneration and no tolerance widening. A rename commit that changed a prediction would fail that gate loudly."
    - "`packages/harness/algorithmIdentity.test.ts` exists and fails on any identity-shaped occurrence of the pre-rename id anywhere in the tracked tree outside a short, individually-reasoned exclusion list. This is this plan's half of the standing D-05 assertion: it proves the SOURCE carries no reachable pre-rename identity. The client half is 07-18's (it deletes the `apps/web/**` exclusion line), and the live half — zero `sigma1@` objects in R2 and zero `algorithm_id = 'sigma1'` rows in D1 — is 07-19's. Three halves, one assertion, no duplicate and no gap."
    - "The exclusion list is file-scoped and every entry carries a one-line reason in the test source. Isolated in-code citations of a measured figure taken under the old name are exempted line-by-line with a `[pre-rename]` marker that is only honoured on a comment line, so the escape hatch can never exempt an assignment, a registry entry, a TOML var or a JSON field."
    - "Historical measurement records keep the name they were measured under: `docs/models/*.md`, `docs/first-paint-measurement.md`, `docs/publish-budget.md`'s measurement tables, `data/baselines/*.json` and the sketch-skill sources are not rewritten. What was measured under the old id was measured under the old id, and rewriting those files would make the project's own accuracy record say something that never happened."
    - "The four harness-only experiment ids rename with the published one, so the registry never carries two names for one model. `HARNESS_ONLY_ALGORITHM_IDS` still rejects them from any published manifest by EXACT string equality — the membership test is `Set.has`, never a prefix or substring test, which is what keeps `vpr` itself from being swept into the harness-only set by its own name."
    - "The two committed version files move with `git mv`, so `git log --follow` still reaches their promotion commits. Their JSON bodies change in exactly one field — the `id` — and every parameter value, `codeVersion`, `paramSetName` and `version` string is byte-identical, which is what makes the digest gate above a real check rather than a tautology."
    - "This plan writes nothing to R2 and nothing to D1, runs no live TBA request, and needs no credential. Every command in it is a local test, typecheck, grep or dry run. `--dry-run` is the only publish invocation, and it is asserted to emit zero `putObject` calls."
    - "`PIPELINE_ALGORITHM_IDS` and `PUBLISHED_ALGORITHM_IDS` both keep the published algorithm in the THIRD position. D-03 fixes the order (`opr, epa, …`) and `AlgorithmSelect.useAlgorithmOptions` walks that constant to render the ribbon, so a rename that re-sorted the list would silently reorder a shipped control (EVNT-02..EVNT-06 `ordering` probe, rename-level half; the primary halves belong to 07-11 and 07-01)."
    - "`vpr` and `vpr-adapt` are adjacent by prefix, and every membership test that distinguishes them is exact equality: `applyPromotedOverrides`' two branches, `HARNESS_ONLY_ALGORITHM_IDS.has`, `parseLiveAlgorithmIds`' `includes` over a string array, and the sweep test's own word-bounded pattern. A `startsWith`/`includes`-on-substring test anywhere in that chain would resolve the harness-only variant as the published algorithm and publish a research module's numbers under the site's headline name (EVNT-02..EVNT-06 `adjacency` probe, rename-level half; the primary halves belong to 07-11, 07-12, 07-13 and 07-14)."
    - "An omitted `--algorithm` and an unset `LIVE_ALGORITHM_IDS` both resolve to the renamed pipeline set rather than to nothing: `resolvePublishAlgorithms(undefined)` returns three modules whose ids are the renamed triple, and `parseLiveAlgorithmIds(undefined)` returns the renamed single-element default and still emits its `live-tier-defaulted` warn line. The empty-input path is the one a hurried operator actually takes, and leaving it pointing at the old id would silently publish nothing (EVNT-02..EVNT-06 `empty` probe, rename-level half; the primary halves belong to 07-01, 07-11 and 07-14)."
    - "Every percentile, spread, score and probability this plan's diff can reach is bit-identical before and after, asserted through the existing digest and prediction-stream gates rather than by inspection (EVNT-03 `precision` probe, rename-level half; 07-09 owns the publish-boundary half and 07-01 owns the rendered half)."
    - statement: "That no `sigma1@` object remains in R2 and no `algorithm_id = 'sigma1'` row remains in D1 cannot be proven by anything in this plan — this plan deliberately leaves both in place, because the deployed browser is still reading them. The live half of the D-05 assertion is executed by 07-19 against the real bucket and the real database, after 07-17 has written the replacements and 07-18 has moved the client off the old prefix."
      verification: backstop
    - statement: "That the deployed site is genuinely unaffected through waves 11 and 12 is proven here only by a source-level zero-change gate over `apps/web`. The behavioural proof is 07-18's e2e run against the deployed apex (`apps/web/playwright.config.ts` has no local `webServer` and asserts against the live origin), which is also the first run in which the specs' algorithm queries change. Recorded so a green suite here is never mistaken for evidence about the deployed origin."
      verification: backstop
    - statement: "That the full-repo sweep found everything is asserted by a running test rather than claimed, but the test's own pattern set is the limit of what it can find: it matches identity-shaped occurrences (quoted id, `@`-suffixed key segment, `algorithm=`/`algorithm_id =` value), not every conceivable spelling. `07-PATTERNS.md`'s enumeration is treated as a floor per outline assumption 8, the re-grep result is recorded against it in the SUMMARY, and a spelling neither the enumeration nor the pattern set anticipates would still escape."
      verification: backstop
  prohibitions:
    - statement: "A historical measurement record must never be rewritten to attribute a figure to the new name. Every Brier score, byte count, subrequest tally and D1 row count in `docs/models/`, `docs/first-paint-measurement.md`, `docs/publish-budget.md` and `data/baselines/` was produced by a run that identified itself under the old id, and the whole point of committing those files was that this project's accuracy claims are checkable against the runs that produced them. Sweeping them would make the record say a run happened that never did — the same class of dishonesty as publishing a consistency value under a `±` glyph, applied to provenance instead of uncertainty. Where a doc's forward-looking half genuinely must change, the measured half beside it keeps its old name and says so."
      status: unverified
      verification: test
    - statement: "The standing assertion must never be made green by widening its exclusion list. Every exclusion is a file-scoped entry with a written reason, every line-level exemption is a comment line carrying an explicit marker, and the count of both is bounded and reported. An assertion tuned until it passes is worse than no assertion, because it converts an open question into a false answer that a later reader will trust — and this project's failure log already names the version of that mistake where documentation asserts a state the code is not in."
      status: unverified
      verification: test
    - statement: "No document, comment or commit message in this change may state that the rename is complete. It is not: the browser still requests the old prefix, the old objects are still in the bucket, the old rows are still in D1, and three later plans finish the job. Every doc this plan edits must say which half landed and name the plan that lands the rest. A file that reads as though the transition is done is precisely the failure this project's log names first — a description of a system that does not exist — reintroduced on the commit that was supposed to be careful about exactly that."
      status: unverified
      verification: none
  artifacts:
    - path: "packages/harness/publishedAlgorithms.ts"
      provides: "The two-tier id constants for the transition window — the browser-facing set (unchanged here) and the new publisher-facing set"
      exports: ["PUBLISHED_ALGORITHM_IDS", "PublishedAlgorithmId", "PIPELINE_ALGORITHM_IDS", "PipelineAlgorithmId"]
    - path: "packages/core/algorithms/sigma1/index.ts"
      provides: "The five registry entries renamed to the published identity, with the identity-vs-implementation rule recorded at the rename site"
      exports: ["vpr", "vprDefaults", "vprSeasonSd", "vprNormalCdf", "vprAdaptive"]
    - path: "data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json"
      provides: "The pinned promoted version file, git-mv'd from its pre-rename name with only its `id` field changed"
    - path: "data/algorithm-versions/vpr@2.0.0+tracer-check.json"
      provides: "The tracer-stage promoted version file, same treatment"
    - path: "packages/harness/algorithmIdentity.test.ts"
      provides: "The source half of the standing D-05 assertion — a filesystem walk asserting no identity-shaped pre-rename token survives outside a reasoned exclusion list; 07-18 deletes its client exclusion, 07-19 adds the live half"
      exports: ["IDENTITY_SWEEP_EXCLUSIONS", "PRE_RENAME_MARKER"]
    - path: "packages/harness/cli.ts"
      provides: "The renamed registry keys, the renamed pinned-version-path constant, and the renamed promoted/search-winner loaders"
      exports: ["loadPromotedVpr", "loadSearchWinnerVpr", "warnIfNewerPromotedVpr", "ALGORITHMS", "applyPromotedOverrides"]
    - path: "apps/worker/wrangler.toml"
      provides: "The live-fold tier var pointing at the renamed id (inert until 07-19 redeploys the Worker)"
  key_links:
    - from: "packages/core/algorithms/sigma1/index.ts"
      to: "packages/harness/publish.ts"
      via: "BASE_PUBLISH_ALGORITHMS's third entry, then applyPromotedOverrides, then artifactKey({ algorithmId: algorithm.id })"
      pattern: "artifactKey"
    - from: "data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json"
      to: "packages/harness/manifests.ts"
      via: "promoted.id — the manifest's third entry id is READ from the version file, never written as a literal, so key and manifest cannot disagree"
      pattern: "buildAlgorithmsManifest"
    - from: "packages/harness/publishedAlgorithms.ts"
      to: "apps/web/src/lib/searchParams.ts"
      via: "z.enum(PUBLISHED_ALGORITHM_IDS).catch(DEFAULT_ALGORITHM) — the browser-facing tier this plan deliberately does not move; 07-18 owns it"
      pattern: "PUBLISHED_ALGORITHM_IDS"
    - from: "packages/harness/publishedAlgorithms.ts"
      to: "apps/worker/src/scheduled.ts"
      via: "PIPELINE_ALGORITHM_IDS — parseLiveAlgorithmIds validates wrangler.toml's LIVE_ALGORITHM_IDS against the publisher tier, not the browser tier"
      pattern: "PIPELINE_ALGORITHM_IDS"
    - from: "packages/harness/algorithmIdentity.test.ts"
      to: ".planning/phases/07-event-pages/07-18-PLAN.md"
      via: "the `apps/web/**` exclusion entry, whose deletion is the mechanism that lands the client half of the D-05 assertion"
      pattern: "IDENTITY_SWEEP_EXCLUSIONS"
---

<objective>
**Pipeline-side half of the Sigma1 → VPR rename (D-04/D-05)** — everything that determines what the
publisher *writes*, and nothing the browser reads: the core registry id,
`packages/harness/publishedAlgorithms.ts`'s `PUBLISHED_ALGORITHM_IDS`,
`packages/harness/{manifests,manifestSchemas,cli}.ts`,
`apps/worker/src/{scheduled,stateStore,env,bundleSmoke}.ts` + `wrangler.toml` +
`migrations/0001_algorithm_state.sql`, both `data/algorithm-versions/sigma1@*.json` files (file
rename + internal `id`), and forward-looking docs claims. A **full-repo grep-and-sweep**, treating
PATTERNS.md's ~24-file enumeration as a floor and re-grepping before declaring it done; lands the
standing assertion that no `sigma1` identifier remains reachable outside historical measurement
docs. Scheduled after every publish run that must still emit `sigma1@` keys, so exactly one
publisher identity is in force at any moment. Rated **one-way**; gated at 07-17/07-19.

Purpose: 07-10 has already published a real enriched subset under the current keys, and the
deployed site reads them. This plan changes what the *next* publish run will write, and only that.
Every source of the algorithm identity that flows into an R2 key, a manifest entry, a D1
`algorithm_id` value or the Worker's live-fold tier moves to `vpr` in one commit set; every source
of the identity the browser sends stays exactly where it is until 07-18. Drawing the line there is
not tidiness — it is the entire safety property of the phase's rename sequencing (outline
assumption 7), and it is the reason 07-17's write pass can be purely additive.

Output: a tree in which one publisher identity is in force, the browser is still on the other one
on purpose, and a runnable test says so.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/CLAUDE.md

# The authority — D-04, D-05, D-06, D-18 item 1, D-21
@.planning/phases/07-event-pages/07-CONTEXT.md

# The blast-radius finding wider than CONTEXT.md's list, plus the Runtime State Inventory
@.planning/phases/07-event-pages/07-RESEARCH.md

# The enumerated rename file list — a FLOOR, not an exhaustive enumeration (outline assumption 8)
@.planning/phases/07-event-pages/07-PATTERNS.md

@.planning/phases/07-event-pages/07-VALIDATION.md

# The tier-split reasoning this plan exists to implement — assumptions 6, 7, 8, 10 and 15
@.planning/phases/07-event-pages/07-PLAN-OUTLINE.md

# The plan that wired --include-offseason and restructured publish.ts's season loop
@.planning/phases/07-event-pages/07-09-PLAN.md

# The plan that routed the Teams-page rank header through algorithmDisplayLabel so the
# user-visible relabel follows 07-18's cutover for free, and that left searchParams.ts untouched
@.planning/phases/07-event-pages/07-15-PLAN.md
</context>

## Artifacts this phase produces

Newly-created symbols and names, listed so a source-grounding pass can exclude them from drift
verification. **This plan creates:**

| Kind | Name | Location |
|---|---|---|
| exported constant | `PIPELINE_ALGORITHM_IDS` | `packages/harness/publishedAlgorithms.ts` |
| exported type | `PipelineAlgorithmId` | `packages/harness/publishedAlgorithms.ts` |
| exported registry entry | `vpr` (was the pre-rename export at the same site) | `packages/core/algorithms/sigma1/index.ts` |
| exported registry entry | `vprDefaults` | `packages/core/algorithms/sigma1/index.ts` |
| exported registry entry | `vprSeasonSd` | `packages/core/algorithms/sigma1/index.ts` |
| exported registry entry | `vprNormalCdf` | `packages/core/algorithms/sigma1/index.ts` |
| exported registry entry | `vprAdaptive` | `packages/core/algorithms/sigma1/index.ts` |
| algorithm id value | `vpr` | registry, manifest, artifact key segment, D1 `algorithm_id` |
| algorithm id value | `vpr-defaults`, `vpr-seasonsd`, `vpr-normalcdf`, `vpr-adapt` | `ALGORITHMS` registry, `HARNESS_ONLY_ALGORITHM_IDS` |
| exported function | `loadPromotedVpr(id, versionPath)` | `packages/harness/cli.ts` |
| exported function | `loadSearchWinnerVpr(id, searchArtifactPath, paramSetName)` | `packages/harness/cli.ts` |
| exported function | `warnIfNewerPromotedVpr(versionsDir, pinnedPath)` | `packages/harness/cli.ts` |
| module constant | `PROMOTED_VPR_VERSION_PATH` | `packages/harness/cli.ts` and `packages/harness/manifests.ts` (two deliberate copies, per those files' own existing duplication comments) |
| file path | `data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json` | `data/algorithm-versions/` |
| file path | `data/algorithm-versions/vpr@2.0.0+tracer-check.json` | `data/algorithm-versions/` |
| R2 key shape | `v1/{page}/…/vpr@{version}.json` | R2 (written first by 07-17, never by this plan) |
| test file | `packages/harness/algorithmIdentity.test.ts` | `packages/harness/` |
| exported constant | `IDENTITY_SWEEP_EXCLUSIONS` | `packages/harness/algorithmIdentity.test.ts` |
| exported constant | `PRE_RENAME_MARKER` | `packages/harness/algorithmIdentity.test.ts` |
| comment marker convention | `[pre-rename]` | any comment line citing a figure measured under the old id |

Created by no other means in this plan: **no** new npm dependency, **no** new corpus table or
column, **no** new published schema field, **no** new CLI flag, **no** `PAGE_ARTIFACT_SCHEMA_VERSION`
bump (D-02), **no** new component, and **no** R2 or D1 write of any kind.

Sibling plans in this phase additionally create: `EVENT_TABS`/`DEFAULT_EVENT_TAB`/`EventSearchSchema`,
`lib/api/event.ts`, `REGISTERED_EVENT_TABS`, `BreakdownTab` and `algorithmDisplayLabel` (07-01);
`event_alliances` and the four `event_rankings` columns (07-02); `tbaAllianceResponseSchema`,
`normalizeEventAlliances` and `ingestSeasonAlliancesOnly` (07-03); `teamOwnComponentVarianceSum`
(07-06); `EventAllianceSchema`, `composeEventLocation` and `ROUNDING_RULE.rankingPoints` (07-07);
`EventArtifactIdentityInput`, `EventTeamRankingInput`, `EventAllianceInput`, `findEventArtifact` and
`eventArtifactParams` (07-08); `withEventPercentiles`, `metricsAsOfEvent` and `--include-offseason`
(07-09); `InsightsTab` (07-11); `eventMatchAxis.ts`/`EventMatchTable.tsx`/`QualsTab` (07-12);
`ElimsTab` (07-13); `AlliancesTab` (07-14); `EventHeader`, `eventKeyForSeason` and
`resolveYearChangeTarget` (07-15).

## The tier line, stated file by file

The split is the safety property, so it is drawn here explicitly rather than left to be inferred
from the file list. Three tiers, one rule each.

### Tier P — the publisher writes it. **This plan.**

A file is in tier P when a value in it determines the `{algorithmId}` segment of an R2 key, the
`id` of a published manifest entry, the `algorithm_id` column of a D1 `algorithm_state` row, or
which module the Worker folds live.

| File | Why it is tier P |
|---|---|
| `packages/core/algorithms/sigma1/index.ts` | The five registry entries and their `id:` values are the ultimate source of `algorithm.id` |
| `data/algorithm-versions/*.json` | The promoted override's `id` is what actually reaches the key and the manifest |
| `packages/harness/cli.ts` | `ALGORITHMS` keys, `applyPromotedOverrides`' branches, the pinned version path |
| `packages/harness/manifests.ts` | Builds the manifest entry whose id the browser resolves a version from |
| `packages/harness/manifestSchemas.ts` | `HARNESS_ONLY_ALGORITHM_IDS` — which ids are forbidden from a published manifest |
| `packages/harness/promote.ts` | The default `--id` a future promotion stamps into a new version file |
| `packages/harness/publish.ts` | `BASE_PUBLISH_ALGORITHMS` and `resolvePublishAlgorithms`' default set |
| `packages/harness/fixtures/extract-digest-slice.ts` | `DEFAULT_VERSION_PATH` points at a file this plan renames |
| `scripts/replayRig.ts` | Default `--algorithm` list; drives real folds against the deployed Worker |
| `apps/worker/src/scheduled.ts` | `DEFAULT_LIVE_ALGORITHM_IDS` and the tier validation |
| `apps/worker/wrangler.toml` | `LIVE_ALGORITHM_IDS` — the deployed live-fold tier |
| `apps/worker/src/env.ts`, `stateStore.ts`, `migrations/0001_algorithm_state.sql` | Doc comments describing which id the Worker folds and stores |
| `docs/worker-operations.md` | A runbook: its commands are executed, so a stale id in it is a wrong instruction |

### Tier C — the browser reads it. **07-18, not this plan.**

A file is in tier C when a value in it determines which key the browser *requests* or what a reader
*sees the algorithm called*.

| File | Owner | Why it must not move here |
|---|---|---|
| `apps/web/src/lib/searchParams.ts`'s `DEFAULT_ALGORITHM` value and `z.enum` expression | 07-18 | Flipping it makes a fresh visitor request `vpr@{version}` before 07-17 writes those keys |
| `apps/web/src/components/ribbon/AlgorithmSelect.tsx`'s `ALGORITHM_DISPLAY_LABELS` | 07-18 | D-04's display relabel; also keyed on `PublishedAlgorithmId`, so it moves only when that type moves |
| `packages/harness/publishedAlgorithms.ts`'s `PUBLISHED_ALGORITHM_IDS` **value** | 07-18 | See PD-01 — this is the one genuinely dual-tier constant, and the outline's expectation that it flips here is provably incompatible with the outline's own assumption 7 |
| `apps/web/src/components/{search/SearchBox,teams-table/columns}.tsx` | 07-18 | Type-only consumers of `PublishedAlgorithmId`; they move when the type's members move |
| `apps/web/src/{lib,components}/**` prose naming the model | 07-18 | D-04 display naming. Enumerated for 07-18 under "Handoff to 07-18" below so it is a handoff, not a gap |
| `apps/web/e2e/*.spec.ts` | 07-18 | Their `?algorithm=` queries assert against the DEPLOYED origin, which is still on the old prefix until 07-17 |

### Tier F — frozen history. **No plan.**

`docs/models/*.md`, `docs/first-paint-measurement.md`, `docs/publish-budget.md`'s measurement
tables, `data/baselines/*.json`, `.claude/skills/sketch-findings-sigmascout/sources/**`,
`REBUILD_SPEC.md`, and `.planning/**`. These record what was measured, run or decided under the old
name. Rewriting them is prohibited by this plan's first prohibition.

### The ambiguous items, named and reasoned rather than assumed

1. **`packages/harness/publishedAlgorithms.ts` — the one file that is genuinely both tiers.** See
   PD-01. Resolved by adding a second constant rather than by moving the first.
2. **The `packages/core/algorithms/sigma1/` directory and the implementation symbols**
   (`makeSigma1`, `Sigma1State`, `Sigma1Params`, `Sigma1ParamsSchema`, `SIGMA1_CODE_VERSION`,
   `DEFAULT_SIGMA1_PARAMS`, and every `sigma1/…` import path). See PD-02 — **deliberately not
   renamed**, with the reasoning recorded and surfaced as flagged assumption 1 so the developer can
   reverse it.
3. **The four harness-only experiment ids.** See PD-03 — **renamed**, because leaving them would
   put two names for one model in the same registry, which is the confusion D-04 exists to end.
4. **In-code comments citing a figure measured under the old id.** See PD-05 — kept verbatim,
   exempted line-by-line with a marker that only works on a comment line.

## Scope boundaries (what this plan deliberately does NOT do)

- **No R2 write, no R2 delete, no D1 statement, no live TBA request, no Worker deploy.** 07-17
  writes, 07-19 deletes and deploys. The only publish invocation here is `--dry-run`.
- **No `PUBLISHED_ALGORITHM_IDS` value change** and **no `apps/web` behaviour change of any kind**.
  `git diff --numstat` over `apps/web/src` and `apps/web/e2e` reports zero on every file except
  `apps/web/src/lib/searchParams.ts`, whose diff is confined to comment lines.
- **No `AlgorithmSelect.tsx` edit.** `algorithmDisplayLabel` (07-01) and its backing label map are
  07-18's to relabel. 07-15 already routed the Teams-page rank header through that accessor for
  exactly this reason.
- **No `packages/core/algorithms/sigma1/` directory rename and no implementation-symbol rename.**
  PD-02, flagged assumption 1.
- **No prediction, percentile, spread or score value change.** The digest gate proves it.
- **No `checkpoint:decision`.** PD-08 records why the `one-way` rating on Task 1 does not earn one
  here, following the outline's own deliberate gate-placement decision.
- **No `docs/publish-budget.md` re-measure.** That is 07-19's, against the D-18-enlarged schema.
- **No new npm dependency**, so no package-legitimacy checkpoint and no `T-07-16-SC` row in the
  threat register. Stated rather than omitted.

<inherited_ownership>
Recorded so absences here are not read as silent drops.

- **The 17 probe edges are owned by the plan owning the surface each constrains** — EVNT-02 to
  07-11, EVNT-03 to 07-01, EVNT-04 to 07-12, EVNT-05 to 07-14, EVNT-06 to 07-13 (approved outline,
  assumption 14). **Zero of the 17 are authored here as primary edges, by assignment. This plan owns
  no primary probe edge at all.** That is not an omission: every edge in the coverage report is
  rooted in a tab's data shape — a tier cut, a rank tie, an empty roster, an alliance seed order —
  and this plan renders nothing, orders nothing and computes nothing. It changes one string's value
  in the places that string becomes an identity.

  Four of the seventeen probe *questions* do have a genuine RENAME-LEVEL half that is decided in
  this plan and nowhere else. Each is authored into `must_haves.truths` above with an explicit
  cross-reference to the plan owning its primary half, so nothing is double-counted and nothing is
  dropped. The accounting table below reconciles them.

| Probe category | Rename-level half owned here | Primary half owner(s) | Where authored |
|---|---|---|---|
| `ordering` | Position 3 is preserved in both id constants, so the shipped ribbon's option order is unchanged | 07-11 (Insights row order), 07-01 (Breakdown column order) | `truths`, "both keep the published algorithm in the THIRD position" |
| `adjacency` | `vpr` vs `vpr-adapt` are distinguished by exact equality at every membership test | 07-11, 07-12, 07-13, 07-14 (rendered ties/equal values) | `truths`, "adjacent by prefix" |
| `empty` | Omitted `--algorithm` and unset `LIVE_ALGORITHM_IDS` resolve to the renamed set, not to nothing | 07-01 (empty roster), 07-11 (absent rank), 07-14 (absent alliances) | `truths`, "An omitted `--algorithm`" |
| `precision` | No predicted number moves; proven by the reproducibility digest, not by inspection | 07-09 (publish boundary), 07-01 (rendered boundary) | `truths`, "bit-identical before and after" |
| `boundary` | **None.** There is no threshold, cut or min/max anywhere in an identity rename — the value is a string with no ordering. Cross-referenced rather than manufactured, because inventing a rename-shaped boundary truth would be a resolution with nothing behind it. | 07-01 (tier cuts), 07-03 (RP sort order) | — |

  Reconciliation: 4 rename-level halves authored, 1 category explicitly absent with a reason,
  0 silently dropped. Phase-wide the 17 == 17 equality still reconciles across 07-01, 07-11, 07-12,
  07-13 and 07-14.

- **UI-SPEC `## UI Considerations` rows: none are this plan's.** Every E-row belongs to a tab or to
  the header — E1 to 07-15, E2 and E4 to 07-01, E3 to 07-11, E5 to 07-12, E6 to 07-13, E7 to 07-14,
  with the seven `🧪 backstop` runs 07-20's.
- **UI-SPEC Cross-Cutting Changes table, the D-04/D-05 row, is split.** That row states two things:
  "`AlgorithmSelect.tsx` option label changes from `Sigma1` to `VPR`" (**07-18's**) and "every
  `sigma1@…` path becomes `vpr@…`" — whose *code* half is this plan and whose *published* half is
  07-17's write pass and 07-19's delete pass. This plan changes what a path WOULD be; it creates no
  path. Recorded so the row does not read as unowned.
- **The honest-uncertainty prohibition (D-01) is owned by 07-06** and re-asserted by 07-17. This
  plan renders nothing and redefines no quantity.
- **`COVERAGE.md` is owned by 07-03.** This plan calls no external API.
- **D-19's ROADMAP re-confirmation is 07-19's** (outline assumption 13 — the amendment is already in
  the tree).
</inherited_ownership>

<handoff_to_07_18>
## Enumerated client-tier work this plan deliberately leaves, so 07-18 has no discovery to do

Every file below carries a pre-rename occurrence that is tier C. This list was produced by the same
re-grep Task 3 runs, restricted to `apps/web/`. 07-18 owns all of it; the `apps/web/**` exclusion
entry in `algorithmIdentity.test.ts` is what keeps the assertion green until 07-18 deletes both the
occurrences and the exclusion line in one commit.

**Runtime values and types (the cutover proper):**
`apps/web/src/lib/searchParams.ts` (`DEFAULT_ALGORITHM`'s value and the `z.enum` expression — NOT
its comments, which are this plan's; see PD-04),
`apps/web/src/components/ribbon/AlgorithmSelect.tsx` (`ALGORITHM_DISPLAY_LABELS`, D-04),
plus whatever `PUBLISHED_ALGORITHM_IDS`' value change drags through
`apps/web/src/components/search/SearchBox.tsx` and
`apps/web/src/components/teams-table/columns.tsx` as type-only consumers.

**Model-naming prose (D-04 display naming, no runtime effect):**
`apps/web/src/components/team/MatchTable.tsx`, `apps/web/src/components/team/BonusRpDots.tsx`,
`apps/web/src/lib/bonusRp.ts`, `apps/web/src/lib/metricGroups.ts`, `apps/web/src/lib/metricKeys.ts`,
`apps/web/src/lib/query-client.ts`, `apps/web/src/lib/api/manifests.ts`. Note that several of these
name the *implementation module or its symbols* rather than the model — under PD-02's rule those
stay, and 07-18 should apply the same identity-vs-implementation test rather than a blanket sweep.

**Test and spec files (07-18's, per the outline row):**
every `apps/web/src/**/*.test.{ts,tsx}` carrying an algorithm literal, and the five e2e specs
`touch-scroll.spec.ts`, `static-shell.spec.ts`, `no-page-pan.spec.ts`, `team-page.spec.ts`,
`deep-link.spec.ts` — the last group asserts against the DEPLOYED origin, so they can only be
flipped once 07-17 has written the new keys.
</handoff_to_07_18>

<planner_decisions>

- **PD-01 — `publishedAlgorithms.ts` is the one genuinely dual-tier file, and the transition is
  carried by a SECOND constant rather than by moving the first.** The outline row assigns
  `PUBLISHED_ALGORITHM_IDS` to this plan. Flipping its value here is provably incompatible with the
  outline's own assumption 7, and the proof is mechanical rather than a matter of taste:
  `apps/web/src/lib/searchParams.ts` declares `DEFAULT_ALGORITHM: PublishedAlgorithmId`, and
  `AlgorithmSelect.tsx` declares `ALGORITHM_DISPLAY_LABELS: Readonly<Record<PublishedAlgorithmId,
  string>>`. Changing the constant's members therefore does not compile until both of those move
  too — which is 07-18's entire plan, executed two waves early. And the moment they move, a fresh
  visitor's `?algorithm=` resolves to the new id, `useAlgorithmVersion` looks it up in the manifest
  that is live in R2 (which still names the old id, because no publish run has happened), finds no
  entry, and every artifact query stays disabled. `.github/workflows/deploy.yml` deploys on every
  push to `main`, and 07-17 sits behind a blocking `checkpoint:decision`, so that window is not
  hypothetical and not short.

  So the constant's *value* stays, and a sibling `PIPELINE_ALGORITHM_IDS` is added beside it,
  consumed by exactly the three call sites that are publisher-side: `resolvePublishAlgorithms`'
  default set (`publish.ts`), `parseLiveAlgorithmIds`' validation set (`scheduled.ts`), and
  `replayRig.ts`'s default `--algorithm` list. Nothing in `apps/web` imports it. During waves 11 and
  12 the two constants state two facts that are BOTH true: the old id is what has live objects in
  R2, and the new id is what the publisher writes. 07-18 collapses them.

  The alternative of leaving `PUBLISHED_ALGORITHM_IDS` as the sole constant and having 07-17 pass an
  explicit `--algorithm` CSV was rejected: it makes correctness depend on remembering a flag, and it
  leaves `parseLiveAlgorithmIds` rejecting the very id `wrangler.toml` now names, which turns the
  Worker suite red at the end of this plan.
  — <reversibility rating="reversible">One added export and three repointed call sites; `git revert` restores the single-constant shape exactly.</reversibility>

- **PD-02 — the rename follows the IDENTITY, not the IMPLEMENTATION, and the rule is written down
  once so the sweep is decidable rather than a series of judgment calls.** A name renames when its
  value *is*, *produces* or *resolves* the published algorithm identity. A name stays when it
  describes the implementation module, its math, its state shape or its parameter type. So the five
  registry exports and their `id:` values rename, the pinned version-path constant and the three
  promoted/search-winner loaders rename (they exist to build the published module), and
  `makeSigma1`, `Sigma1State`, `Sigma1Params`, `Sigma1ParamsSchema`, `SIGMA1_CODE_VERSION`,
  `DEFAULT_SIGMA1_PARAMS` and the `packages/core/algorithms/sigma1/` path do not.

  This is a real line with a real cost on both sides, so it is argued rather than asserted. Neither
  D-05 nor the outline row names the module path or the factory; both name the registry *id*. A
  directory rename would change no published byte, no runtime identity and no R2 key, while touching
  roughly 90 additional files including every import path in `packages/harness`, `apps/worker/src`
  and `scripts/`, and roughly 25 files inside the algorithm package itself. Against that, the cost of
  NOT doing it is one genuine oddity — `export const vpr` living in a file called `sigma1/index.ts`
  — which Task 1 is required to explain in a doc comment at the rename site rather than leave for a
  reader to trip over. Surfaced as flagged assumption 1 so the developer can reverse it as a
  follow-up rather than discovering it as a silent scope cut.
  — <reversibility rating="reversible">A naming boundary, not a contract. Widening it later is a mechanical `git mv` plus an import sweep.</reversibility>

- **PD-03 — the four harness-only experiment ids rename with the published one.** `vpr-defaults`,
  `vpr-seasonsd`, `vpr-normalcdf`, `vpr-adapt`. They never reach R2, D1 or the browser, so they are
  not strictly tier P — but leaving them would put two names for one model side by side in the same
  `ALGORITHMS` registry, which is exactly the confusion D-04 exists to end, and it would make the
  standing assertion unauthorable without an exclusion broad enough to hide real misses. Their cost
  is bounded: five id strings, one `Set` literal, one error message, and the harness test fixtures
  that name them. The historical records that measured them (`data/baselines/`,
  `docs/models/opr-baseline-change.md`) keep the old names under tier F.
  — <reversibility rating="reversible">Harness-internal ids that reach no published surface.</reversibility>

- **PD-04 — this plan's `searchParams.ts` edit is comment-only, and that is a real edit rather than
  a cosmetic one.** Outline assumption 6 reserved `searchParams.ts` for this plan and for 07-18
  specifically so no other plan would collide on it; 07-11 and 07-15 both carry `git diff --numstat`
  zero-change criteria on it for that reason. The edit this plan owns is the consequence of PD-01:
  the moment two id constants exist, that file's doc comment describing `PUBLISHED_ALGORITHM_IDS` as
  "the published algorithm ids" is ambiguous, and its `DEFAULT_ALGORITHM` comment names a model that
  the rest of the tree has stopped calling by that name. Leaving both silent would be this project's
  named failure mode in miniature. So the comments say which tier this file is on, why it is
  deliberately on the older one, and which plan moves it — and the VALUES on those same lines do not
  change. The two halves are separated by an acceptance criterion, not by intent: the diff must
  touch comment lines only.
  — <reversibility rating="reversible">Comments.</reversibility>

- **PD-05 — measured figures cited inside code comments keep the old name, exempted by a marker that
  only works on a comment line.** A handful of comments in `percentiles.ts`,
  `fixtures/extract-digest-slice.ts`, `stateStore.ts`, `scheduled.ts` and `wrangler.toml` cite a
  specific byte count, subrequest tally or reproduced bug under the old id. Rewriting them would
  make each one claim a measurement that was never taken. The exemption is the token `[pre-rename]`
  on the same line, honoured by the assertion ONLY when the line's first non-whitespace characters
  are a comment opener (`//`, `*`, `/*` or `#`) — so the marker can never exempt an assignment, a
  TOML var or a JSON field, which is the whole failure mode prohibition 2 names. Where a comment
  mixes a measured figure with a forward-looking rule, the rule is rewritten and the figure keeps its
  attribution in the same sentence.
  — <reversibility rating="reversible">A comment convention with a bounded, asserted usage count.</reversibility>

- **PD-06 — the standing assertion walks the filesystem, not `git ls-files`.** A `child_process`
  call to git inside a Vitest run couples the gate to a git checkout and to the platform's shell;
  a `readdirSync` walk with an explicit ignore list (`node_modules`, `.git`, `dist`, `reports`,
  `corpus`, `.wrangler`, `test-results`, `playwright-report`, `coverage`) is dependency-free,
  deterministic, and readable by the person who has to reason about a failure. The exclusion list is
  a separate, exported, individually-commented constant so a future reader can see at a glance what
  is not being checked — which is the only thing that makes prohibition 2 checkable.
  — <reversibility rating="reversible">A test's implementation strategy.</reversibility>

- **PD-07 — this plan does not run `pnpm publish:artifacts` for real, and its end-to-end proof is a
  `--dry-run`.** The proof obligation is "the next real run would write the new segment", and
  `publish.ts`'s dry-run path builds every artifact and every key and skips only `putObject`. That
  is exactly the assertion needed, at zero cost against production and with no credential in play.
  Running a real single-event publish here would write a `vpr@` key the deployed browser cannot
  request, which is the hazard the whole tier split exists to avoid — the first `vpr@` object in the
  bucket is 07-17's to create, deliberately.
  — <reversibility rating="reversible">A verification choice.</reversibility>

- **PD-08 — the `one-way` rating on Task 1 carries no `checkpoint:decision`, and that is the
  outline's deliberate gate placement rather than an oversight.** CONTEXT.md rates D-05/D-06
  `one-way` because the path-embedded algorithm id is a published contract (Phase 4 D-02) and
  reverting means republishing every artifact under the old key. At THIS commit, though, nothing is
  published: the change is a set of source edits and two `git mv`s that `git revert` undoes
  completely, leaving the bucket, the database and the deployed site exactly as they were.
  Irreversibility is realized at the moment the new keys are written and the old ones deleted —
  07-17 and 07-19, each of which carries its own `checkpoint:decision`. Two gates at the real points
  of no return beat four gates where two of them re-litigate a decision the developer already
  locked. Recorded here so a reviewer counting gates against `one-way` ratings sees a reason rather
  than a gap.
  — <reversibility rating="reversible">A gate-placement decision, already approved at outline time.</reversibility>

</planner_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The published identity — registry entries, the two promoted version files, and every site that resolves them</name>
  <precondition>The pinned promoted version file is where `cli.ts` and `manifests.ts` both say it is: `test -f "data/algorithm-versions/sigma1@2.0.0+tuned-2026-08.json"` succeeds. Both files hardcode that exact path as two deliberate copies, and this task changes both copies plus the file itself; if the pin has moved since Phase 3, HALT and report rather than guessing which copy is authoritative.</precondition>
  <read_first>
    - `packages/core/algorithms/sigma1/index.ts` lines 1130-1200 — `makeSigma1`'s returned module object (`id`, `version`, `initState`, `predict`, `update`, `teamMetrics`, `carrySeason`) and the five exported registry entries below it with their doc comments. This is the file being modified; read its current state.
    - `packages/harness/cli.ts` lines 40-115 — the import block from the algorithm barrel, the doc block above `ALGORITHMS`, and the `ALGORITHMS` record itself with its five prefixed keys.
    - `packages/harness/cli.ts` lines 116-270 — `PROMOTED_SIGMA1_VERSION_PATH`, `ALGORITHM_VERSIONS_DIR`, `ON_SEARCH_ARTIFACT_PATH`, `loadPromotedSigma1`, `loadSearchWinnerSigma1`, `warnIfNewerPromotedSigma1` and its D-12/03-REVIEW WR-03 doc block, and `applyPromotedOverrides`' two id branches.
    - `packages/harness/manifests.ts` lines 150-235 — the duplication doc comment, the second copy of the pinned path constant, and `buildAlgorithmsManifest`'s three-entry construction, especially that the third entry's `id` is `promoted.id` read from the file rather than a literal.
    - `packages/harness/manifestSchemas.ts` lines 20-110 — the `Sigma1ParamsSchema` import, `HARNESS_ONLY_ALGORITHM_IDS`, `AlgorithmManifestEntrySchema`, and the `.check()` block's D-03 rejection message which names all four variant ids in prose.
    - `packages/harness/promote.ts` lines 40-100 and 185-250 — `PromotedVersionSchema`, the `--id` default at the `parseArgs` site, and the `makeSigma1` call that stamps the id into a new version file.
    - `packages/harness/publish.ts` lines 50-115 — the algorithm barrel imports and `BASE_PUBLISH_ALGORITHMS` with its T-04-16 doc comment — and lines 1548-1575, `resolvePublishAlgorithms` and its `Unknown algorithm for publish` error.
    - `packages/harness/fixtures/extract-digest-slice.ts` lines 20-55 — `DEFAULT_VERSION_PATH` and the two doc comments around it (one cites a measurement, one describes the default; they are treated differently).
    - `packages/harness/digest.test.ts` lines 40-130 — `listVersionFiles`' directory glob and how each file's `promoted.id` is used to rebuild a module, which is why renaming both the filename and the internal field keeps this gate meaningful.
    - `packages/harness/percentiles.ts` around line 237 — one measurement citation in a doc comment, the archetype for PD-05's marker.
    - `data/algorithm-versions/sigma1@2.0.0+tuned-2026-08.json` first 10 lines only — enough to see `id`, `codeVersion`, `paramSetName`, `version`. **Do not read the whole parameter block; nothing in this task needs it and it must not change.**
    - `.planning/phases/07-event-pages/07-CONTEXT.md` — the `### Renaming Sigma1 → VPR` section, D-04, D-05 and D-06 in full, including D-05's verified correction that the reproducibility digest does not hash `algorithmId`.
    - `.planning/phases/07-event-pages/07-PATTERNS.md` — the `## VPR Rename Sweep — Enumerated File List` section. A floor, not an enumeration.
  </read_first>
  <files>packages/core/algorithms/sigma1/index.ts, packages/core/algorithms/sigma1/sigma1.test.ts, packages/core/algorithms/sigma1/params.test.ts, packages/core/algorithms/sigma1/carryover.test.ts, packages/core/algorithms/sigma1/rp/distribution.test.ts, data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json, data/algorithm-versions/vpr@2.0.0+tracer-check.json, packages/harness/cli.ts, packages/harness/manifests.ts, packages/harness/manifestSchemas.ts, packages/harness/promote.ts, packages/harness/publish.ts, packages/harness/percentiles.ts, packages/harness/fixtures/extract-digest-slice.ts, packages/harness/promotedOverrides.test.ts, packages/harness/cli.promotedStaleness.test.ts, packages/harness/manifests.test.ts, packages/harness/publish.test.ts, packages/harness/predictions.test.ts, packages/harness/metricHistory.test.ts, packages/harness/stateSnapshot.test.ts, packages/harness/report.test.ts, packages/harness/artifact.test.ts, packages/harness/payloadBudget.test.ts, packages/harness/baselineFingerprint.test.ts, packages/harness/rounding.test.ts, packages/harness/percentiles.test.ts, packages/harness/browserSafeSchemas.test.ts</files>
  <behavior>
    Extend the existing harness suites. Every case below is written before the source edit that
    satisfies it, and every pre-existing case in every touched file must still pass afterwards with
    its assertions unweakened — a rename that had to relax an assertion would be a behaviour change
    wearing a rename's clothes.

    In `packages/harness/manifests.test.ts`:
    - **Test 1 — the manifest names the new id.** `buildAlgorithmsManifest({ generation, computedAt })`
      returns three entries whose `id` values are, in order, the OPR id, the EPA id, and `vpr`.
    - **Test 2 — the manifest id is READ, not written.** The third entry's `id` strictly equals the
      `id` field parsed from `data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json` in the test
      itself, so a future literal reintroduced at the construction site fails this case.
    - **Test 3 — order is position, not sort.** The three ids appear in the same positions they
      occupied before the rename; the third entry is the published algorithm.

    In `packages/harness/promotedOverrides.test.ts`:
    - **Test 4 — the promoted override still fires, under the new id.** `applyPromotedOverrides` over
      a registry containing the renamed base entry returns a module whose `version` carries the
      promoted `paramSetName`, not the bare defaults.
    - **Test 5 — adjacency: the published id and the adaptation variant do not collide.**
      `applyPromotedOverrides` applied to a registry containing both `vpr` and `vpr-adapt` routes
      each to its own branch — the published one to the committed version file, the variant to the
      search-artifact loader — and the assertion reads each returned module's `id` back rather than
      trusting call order. A `startsWith` or `includes` test at either branch fails this case.
    - **Test 6 — an unknown id is untouched.** An entry whose id matches neither branch is returned
      byte-identical, as today.

    In `packages/harness/publish.test.ts`:
    - **Test 7 — the default publish set.** `resolvePublishAlgorithms(undefined)` returns three
      modules whose `id` values are the OPR id, the EPA id and `vpr` — the empty-input path, which is
      the one an operator who omits the flag actually takes.
    - **Test 8 — the artifact key segment.** For each of the four page kinds, the key built for the
      published algorithm contains the substring formed by `vpr`, an at-sign, and the module's
      `version`; and no key in the emitted set contains the pre-rename segment.
    - **Test 9 — an unknown id still throws.** `resolvePublishAlgorithms` with the pre-rename id in
      its CSV throws, and the thrown message lists the three known keys. This is what makes a
      half-applied rename loud instead of silent (T-07-16-01).

    In `packages/harness/manifestSchemas.test.ts` (or `manifests.test.ts` if the schema cases live
    there — read first and put them where the existing D-03 cases are):
    - **Test 10 — the harness-only set rejects by exact equality.** Each of the four renamed variant
      ids is rejected by `AlgorithmsManifestSchema` when placed in a published manifest, and the
      published id itself is ACCEPTED — the case that would fail under any prefix or substring test.

    In `packages/core/algorithms/sigma1/sigma1.test.ts`:
    - **Test 11 — the registry entry's id.** The renamed export's `id` strictly equals `vpr`, and its
      `version` string is unchanged from before the rename (the code version plus the default param
      set name), because `SIGMA1_CODE_VERSION` is an implementation constant PD-02 leaves alone.

    `packages/harness/digest.test.ts` is NOT extended: it already globs the version directory and
    rebuilds each module from its own `promoted.id`, so it becomes the rename's bit-identity gate
    with no edit at all. Its continued green is an acceptance criterion below, not a new case.
  </behavior>
  <action>
    Move the published algorithm identity from the pre-rename id to `vpr`, at every site that
    produces or resolves it, in one coherent change. Work outward from the registry so the compiler
    walks you through the call sites rather than relying on a blind text substitution.

    **1. The registry entries (`packages/core/algorithms/sigma1/index.ts`).** Rename the five
    exported module constants and the `id:` argument each passes to `makeSigma1`: the published entry
    becomes `export const vpr = makeSigma1({ id: "vpr", linkMode: "predictive-variance" })`, and the
    four experiment entries become `vprSeasonSd` with id `vpr-seasonsd`, `vprNormalCdf` with id
    `vpr-normalcdf`, `vprDefaults` with id `vpr-defaults`, and `vprAdaptive` with id `vpr-adapt`
    (PD-03). Leave `makeSigma1` itself, `SIGMA1_CODE_VERSION`, `DEFAULT_SIGMA1_PARAMS`,
    `Sigma1ParamsSchema`, `Sigma1State`, `Sigma1Params` and every other implementation symbol
    untouched, and leave the file's own path untouched (PD-02).

    Add a short doc comment immediately above the published entry recording the identity-vs-
    implementation rule, in prose: that the exported constant and its `id` are the published
    identity D-04 and D-05 renamed, that the factory and the module path around them name the
    Kalman-filter implementation and were deliberately not renamed, and that a reader wanting the
    implementation-side rename should treat it as a separate refactor. Without this comment the file
    reads as a half-finished sweep to the next person who opens it.

    **2. The two committed version files.** `git mv` each of
    `data/algorithm-versions/sigma1@2.0.0+tracer-check.json` and
    `data/algorithm-versions/sigma1@2.0.0+tuned-2026-08.json` to the same names with `vpr` in place
    of the leading segment. Use `git mv`, not a delete-and-create, so `git log --follow` still
    reaches each file's promotion commit. Then change exactly one field in each body — the top-level
    `id` — to `vpr`. `codeVersion`, `paramSetName`, `version` and every parameter value stay
    byte-identical; that is what makes the digest gate below a real check rather than a tautology.

    **3. `packages/harness/cli.ts`.** Update the import list from the algorithm barrel to the renamed
    exports. Rekey `ALGORITHMS` to `vpr`, `"vpr-defaults"`, `"vpr-seasonsd"`, `"vpr-normalcdf"`,
    `"vpr-adapt"`. Rename `PROMOTED_SIGMA1_VERSION_PATH` to `PROMOTED_VPR_VERSION_PATH` and point its
    `join` at the renamed file. Rename the three exported helpers to `loadPromotedVpr`,
    `loadSearchWinnerVpr` and `warnIfNewerPromotedVpr` (PD-02: they exist to build and pin the
    published module, so they carry the identity). Update `applyPromotedOverrides`' two branches to
    compare against the renamed ids, keeping both as strict equality against a string literal —
    never a prefix test, which Test 5 exists to forbid. Rewrite the doc block above `ALGORITHMS` and
    the `warnIfNewerPromotedVpr` block so their prose names the renamed ids; the `pnpm harness
    --algorithm …` example in that block is a forward-looking instruction and must name the new ids.

    **4. `packages/harness/manifests.ts`.** Rename its own second copy of the pinned path constant
    and point it at the renamed file. Update `buildAlgorithmsManifest`'s local binding name for the
    split version and its doc comment. **Do not change the third entry's `id` to a literal** — it
    must stay `promoted.id`, read from the file, because that is the single mechanism preventing a
    manifest entry and an artifact key from naming different algorithms (T-07-16-01). Update the
    duplication doc comment so it names the renamed constant in both files.

    **5. `packages/harness/manifestSchemas.ts`.** Update `HARNESS_ONLY_ALGORITHM_IDS`' four members
    to the renamed variant ids, and update the D-03 rejection message so the ids it lists in prose
    match the set it actually tests. Leave the `Sigma1ParamsSchema` import and the
    `params: Sigma1ParamsSchema.optional()` field alone — that is an implementation type (PD-02) —
    but rewrite the doc comment above that field so it names the published algorithm as VPR while
    still naming the schema by its real symbol.

    **6. `packages/harness/promote.ts`.** Change the `--id` default to `vpr`, so a future promotion
    stamps the right identity into a new version file by default. Update the `makeSigma1` call site's
    surrounding prose only where it names the model rather than the factory.

    **7. `packages/harness/publish.ts`.** Update the algorithm barrel import and
    `BASE_PUBLISH_ALGORITHMS`'s third entry to the renamed export, so its object key and the module's
    own `id` agree — they must, because `resolvePublishAlgorithms` looks up by key and
    `artifactKey` interpolates `algorithm.id`, and a disagreement between them is exactly how a run
    writes a key nothing can resolve. Update the file-header usage line's `--algorithm` example to
    the renamed ids. Leave `--include-offseason` and everything 07-09 added alone.

    **8. `packages/harness/fixtures/extract-digest-slice.ts`.** Point `DEFAULT_VERSION_PATH` at the
    renamed file and update the doc comment that describes it (forward-looking). Leave the doc
    comment that cites a measured figure verbatim and append the PD-05 marker token to that line.

    **9. `packages/harness/percentiles.ts`.** One doc-comment line cites a measured artifact size
    against a specific pre-rename key. Keep the key exactly as measured and append the PD-05 marker
    to that line. Change nothing executable in this file.

    **10. The harness test files.** Update every algorithm-id literal in the listed test files to the
    renamed ids. These are fixtures and expectations, not behaviour: do not change a single numeric
    expectation, tolerance or fixture value while doing it. Where a test file's assertion is about
    the identity itself, prefer asserting against the imported registry entry's `id` over a re-typed
    literal, so the next rename has fewer places to miss.

    Do not touch `packages/harness/publishedAlgorithms.ts`, `apps/worker/`, `scripts/` or
    `apps/web/` in this task — those are Task 2's and Task 3's, and keeping them out of this commit
    is what makes the tier line auditable in the diff.
  </action>
  <verify>
    <automated>pnpm vitest run packages/harness packages/core/algorithms/sigma1</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Each new case was observed failing before its source edit; quote one failure in the SUMMARY.
    - `pnpm vitest run packages/harness packages/core/algorithms/sigma1` passes with 0 skipped and 0 failures.
    - `pnpm vitest run packages/harness/digest.test.ts` is green with no fixture regenerated and no tolerance changed — `git diff --numstat packages/harness/digest.test.ts` reports no change. This is the bit-identity gate: it rebuilds a module from each version file's own `id` and re-derives the prediction-stream digest, so a rename that moved any predicted number fails here.
    - `ls data/algorithm-versions/` lists exactly two files, both beginning `vpr@`, and `git log --follow --oneline -- "data/algorithm-versions/vpr@2.0.0+tuned-2026-08.json"` prints more than one commit — the move preserved history.
    - `git diff -M --stat data/algorithm-versions/` shows both files as renames with a similarity index of at least 99%, and the JSON body diff on each is exactly one changed line.
    - `node -e` (or a test case) reading each renamed version file asserts its `codeVersion`, `paramSetName` and `version` fields are unchanged from the values recorded in the SUMMARY's before-table.
    - `grep -vE '^\s*(\*|//|/\*)' packages/core/algorithms/sigma1/index.ts | grep -c 'id: "vpr"'` is at least 1, and the same comment-filtered grep for the pre-rename quoted id prints `0`. <!-- planner-discipline-allow: sigma1 -->
    - `grep -c 'implementation' packages/core/algorithms/sigma1/index.ts` is at least 1 within the doc comment above the renamed published entry — the identity-vs-implementation rule is recorded at the rename site, not only in this plan.
    - `grep -vE '^\s*(\*|//|/\*)' packages/harness/manifests.ts | grep -c 'promoted.id'` is at least 1 — the manifest id is still read from the version file rather than written as a literal.
    - `grep -vE '^\s*(\*|//|/\*)' packages/harness/manifests.ts | grep -c 'id: "vpr"'` prints `0` — no literal id was introduced at the manifest construction site. <!-- planner-discipline-allow: id: "vpr" -->
    - The comment-filtered grep for the pre-rename quoted id prints `0` in each of `packages/harness/cli.ts`, `packages/harness/manifests.ts`, `packages/harness/manifestSchemas.ts`, `packages/harness/promote.ts`, `packages/harness/publish.ts` and `packages/harness/fixtures/extract-digest-slice.ts`. <!-- planner-discipline-allow: sigma1 -->
    - `grep -cF 'startsWith' packages/harness/cli.ts` prints `0` (it prints `0` today, so the rename must not introduce one), and `applyPromotedOverrides`' two branches are strict `===` comparisons against string literals — proven by Test 5 rather than by inspection.
    - Test 10 shows the published id ACCEPTED and all four variant ids REJECTED by `AlgorithmsManifestSchema`, in the same test file, so the prefix-collision case is proven in both directions.
    - `grep -cF '[pre-rename]' packages/harness/percentiles.ts` equals 1 and `grep -cF '[pre-rename]' packages/harness/fixtures/extract-digest-slice.ts` equals 1, and in both files every line carrying the marker begins, after leading whitespace, with a comment opener.
    - `git diff --numstat packages/harness/publishedAlgorithms.ts apps/worker apps/web scripts` reports no change on any path — the tier line holds inside this task's own commit.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Every source of the published algorithm identity — the registry entry, the two committed version files, the promoted-override chain, the manifest builder and the publisher's module table — resolves to `vpr`, the reproducibility digest is bit-identical under the new name, and nothing outside `packages/core` and `packages/harness` has moved.</done>
  <reversibility rating="one-way">D-05 and Phase 4 D-02 rate the path-embedded algorithm id one-way: once artifacts exist under the new key, reverting means republishing every one of them under the old key. At THIS commit nothing is published — these are source edits and two `git mv`s that `git revert` undoes completely, leaving R2, D1 and the deployed site untouched. The irreversibility is realized at 07-17's write pass and 07-19's delete pass, each of which carries its own `checkpoint:decision`; see PD-08 for why one is not duplicated here.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two-tier id constant, and the Worker's write-side identity</name>
  <precondition>07-17's write pass has not run: `test ! -f .planning/phases/07-event-pages/07-17-SUMMARY.md`. This task's entire correctness argument is that the browser is still reading the old prefix and the new prefix does not yet exist in R2. If 07-17 has already run, the tier ordering has been violated somewhere upstream — HALT and report rather than proceeding, because the two-constant shim this task installs would then be describing a world that no longer exists.</precondition>
  <read_first>
    - `packages/harness/publishedAlgorithms.ts` in full (18 lines) — the whole file, its zero-imports property, its browser-safety rationale and the D-03 comment on the constant. This is the file being modified; the reason it has no imports is load-bearing and must survive the edit.
    - `packages/harness/manifestSchemas.ts` line 27 — the pass-through re-export, so you can see which consumers reach the constant through this module rather than directly.
    - `packages/harness/publish.ts` lines 1548-1575 — `resolvePublishAlgorithms`' default-set branch, the one pipeline consumer in that file.
    - `apps/worker/src/scheduled.ts` lines 60-95 (the import block, including the algorithm barrel and the manifest-schemas import), lines 125-160 (`DEFAULT_LIVE_ALGORITHM_IDS`, its quick-task doc block with measured subrequest figures, and `UnknownLiveAlgorithmIdError`), lines 185-240 (`parseLiveAlgorithmIds` and `buildAlgorithmModules`), and lines 240-275 and 715-730 (the remaining prose references).
    - `apps/worker/src/env.ts` lines 25-50 — the `LIVE_ALGORITHM_IDS` doc comment, which states the unset-branch default by name.
    - `apps/worker/src/stateStore.ts` lines 30-45 and 110-170 — the implementation-type import (PD-02: stays) and the reproduced-bug comment (PD-05: marker).
    - `apps/worker/wrangler.toml` lines 35-55 — the `[vars]` block, its measurement comments and `LIVE_ALGORITHM_IDS`.
    - `apps/worker/migrations/0001_algorithm_state.sql` lines 10-30 — the schema doc comment naming which algorithms accumulate per team.
    - `scripts/replayRig.ts` lines 60-80 and 615-635 — the algorithm barrel import, the module builder, and the default `--algorithm` list.
    - `apps/web/src/lib/searchParams.ts` lines 1-55 — the file header, the `publishedAlgorithms.js` import, the `DEFAULT_ALGORITHM` doc comment and declaration, and the `algorithm:` field's doc comment and `z.enum(...).catch(...)` expression. **Comment lines only are in scope here; the values are 07-18's.**
    - `apps/web/src/components/ribbon/AlgorithmSelect.tsx` lines 1-45 — `ALGORITHM_DISPLAY_LABELS`' `Record<PublishedAlgorithmId, string>` type and `useAlgorithmOptions`' walk over the constant. **Read only; this task must not modify this file.** This is the mechanical proof behind PD-01 and you should see it directly rather than take it on trust.
    - `.planning/phases/07-event-pages/07-PLAN-OUTLINE.md` assumptions 6, 7 and 8 — the tier-split reasoning this task implements.
  </read_first>
  <files>packages/harness/publishedAlgorithms.ts, packages/harness/publish.ts, scripts/replayRig.ts, scripts/replayRig.test.ts, apps/worker/src/scheduled.ts, apps/worker/src/env.ts, apps/worker/src/stateStore.ts, apps/worker/wrangler.toml, apps/worker/migrations/0001_algorithm_state.sql, apps/worker/test/liveAlgorithmTier.test.ts, apps/worker/test/liveWindows.test.ts, apps/worker/test/readScopedStateSql.test.ts, apps/worker/test/scheduled.test.ts, apps/worker/test/scheduled.replay.test.ts, apps/worker/test/stateStore.test.ts, apps/web/src/lib/searchParams.ts</files>
  <behavior>
    Extend the existing Worker and script suites. As in Task 1, every pre-existing case must pass
    afterwards with unweakened assertions.

    In `apps/worker/test/liveAlgorithmTier.test.ts`:
    - **Test 1 — the renamed tier is accepted.** `parseLiveAlgorithmIds("vpr")` returns a
      single-element array holding `vpr` and emits no warn line.
    - **Test 2 — the empty path.** `parseLiveAlgorithmIds(undefined)` returns
      `DEFAULT_LIVE_ALGORITHM_IDS`, which holds `vpr`, AND still emits the existing
      `live-tier-defaulted` warn line. The default branch is the one an operator who forgets a
      `--var` override actually takes; a rename that left it pointing at the retired id would fold
      nothing and say nothing.
    - **Test 3 — the pre-rename id is now a typo.** `parseLiveAlgorithmIds` with the pre-rename id
      throws `UnknownLiveAlgorithmIdError`, and the thrown message lists the accepted ids from
      `PIPELINE_ALGORITHM_IDS`. Being loud here is the point: after this plan, that id in tracked
      config is unambiguously stale.
    - **Test 4 — adjacency at the tier gate.** `parseLiveAlgorithmIds("vpr-adapt")` throws, because
      the harness-only variant is not a published id — the case that would pass under a prefix test.

    In `apps/worker/test/liveWindows.test.ts` and `scripts/replayRig.test.ts`:
    - **Test 5 — the default `--algorithm` set.** `replayRig`'s default list resolves to the three
      pipeline ids, asserted against the imported `PIPELINE_ALGORITHM_IDS` rather than a re-typed
      array literal.

    A new case in whichever harness test file already covers `publishedAlgorithms.ts` (create the
    case there rather than a new file; read first to find it):
    - **Test 6 — the two tiers are deliberately different, and the difference is bounded.**
      `PUBLISHED_ALGORITHM_IDS` and `PIPELINE_ALGORITHM_IDS` have the same length, share their first
      two members in the same positions, and differ in exactly one position — the third. The case
      carries a comment naming 07-18 as the plan that makes them equal and states that the case is
      expected to be REWRITTEN, not deleted, when it does. A transition constant with no test is a
      transition constant nobody deletes.
    - **Test 7 — ordering.** Both constants place the published algorithm third, which is the
      position the shipped ribbon renders it in.
  </behavior>
  <action>
    Install the two-tier id constant and move every publisher-side and Worker-side consumer onto the
    pipeline tier, leaving the browser tier exactly where it is.

    **1. `packages/harness/publishedAlgorithms.ts`.** Leave `PUBLISHED_ALGORITHM_IDS`' members and
    `PublishedAlgorithmId` alone. Add a sibling `export const PIPELINE_ALGORITHM_IDS = ["opr", "epa",
    "vpr"] as const` and its derived `export type PipelineAlgorithmId`. Keep the file's zero-import
    property intact — nothing added here may import anything, because `apps/web` reaches this module
    directly and that is why it was split out of `manifestSchemas.ts` in the first place.

    Write the doc comment on the new constant to carry the whole transition argument, in prose: that
    `PUBLISHED_ALGORITHM_IDS` names the ids that currently have live objects in R2 and that a
    deployed browser may therefore request; that `PIPELINE_ALGORITHM_IDS` names the ids the publisher
    and the Worker write under; that during this phase's rename window both statements are true and
    the sets genuinely differ; that 07-17 writes the new prefix additively, 07-18 collapses the two
    constants into one and deletes this one, and 07-19 deletes the old objects. Name those three
    plans explicitly. A transitional constant whose comment does not say who deletes it is how
    transitional constants become permanent.

    **2. `packages/harness/publish.ts`.** Change `resolvePublishAlgorithms`' default-set branch to
    spread `PIPELINE_ALGORITHM_IDS`, and update its import. Update the function's doc comment so it
    names the pipeline tier rather than "the three published ids", which is now an ambiguous phrase.
    Nothing else in this file changes in this task.

    **3. `scripts/replayRig.ts`.** Same two changes: the default `--algorithm` list and the import.
    Update the algorithm barrel import to Task 1's renamed export if the compiler has not already
    forced it.

    **4. `apps/worker/src/scheduled.ts`.** Set `DEFAULT_LIVE_ALGORITHM_IDS` to a single-element array
    holding `vpr`. Change `UnknownLiveAlgorithmIdError`'s accepted-list message and
    `parseLiveAlgorithmIds`' membership test to read `PIPELINE_ALGORITHM_IDS`, keeping the test as an
    `includes` over a string array — an exact-equality membership test, never a prefix comparison
    (Test 4). Update the algorithm barrel import to the renamed factory usage only if Task 1 forced
    it; `makeSigma1` itself is unchanged (PD-02), so the `buildAlgorithmModules` call site keeps its
    factory name and only its surrounding prose changes.

    In this file's doc blocks, apply PD-05's split treatment. The quick-task block above
    `DEFAULT_LIVE_ALGORITHM_IDS` states a forward-looking rule ("only this algorithm needs real-time
    folding") AND cites measured subrequest counts. Rewrite the rule to name the renamed id; keep
    each measured figure verbatim and mark its line with the PD-05 marker so the assertion in Task 3
    exempts it and a reader can see the figure was measured before the rename.

    **5. `apps/worker/src/env.ts`.** The `LIVE_ALGORITHM_IDS` doc comment names the default the unset
    branch falls back to. Update that name. No type or value in this file changes.

    **6. `apps/worker/src/stateStore.ts`.** The `Sigma1State` type import and its use in the return
    union stay (PD-02 — that is the implementation's state shape). The reproduced-bug comment
    describes a real incident under the old id: keep its wording and mark the line with the PD-05
    marker.

    **7. `apps/worker/wrangler.toml`.** Set `LIVE_ALGORITHM_IDS` to the renamed id. This is inert
    against production until 07-19 runs `pnpm worker:deploy`; say so in the comment above it, and
    name 07-19. Apply the same PD-05 split to the measurement comments in that block as in
    `scheduled.ts`.

    **8. `apps/worker/migrations/0001_algorithm_state.sql`.** One schema doc comment names which
    algorithms accumulate per-team state. Update the name. This file's SQL is unchanged — the
    migration has already run against the remote database and its text must not be edited in any way
    that would change what a fresh `wrangler d1 migrations apply` produces.

    **9. The Worker test files.** Update every algorithm-id literal to the renamed ids. Where a test
    asserts against the id, prefer reading it from the imported constant. Change no numeric
    expectation and no SQL fixture text.

    **10. `apps/web/src/lib/searchParams.ts` — comments only (PD-04).** Two doc comments become
    misleading the moment two id constants exist. Rewrite the `DEFAULT_ALGORITHM` comment so it
    states which tier this file is deliberately on, that the value here is the id the deployed
    browser still requests because those are the objects that exist in R2 today, and that 07-18 flips
    it once 07-17 has written the replacements. Rewrite the `algorithm:` field comment so its
    reference to the id constant says which of the two constants it means and why the browser-facing
    one is the right one for a URL enum. **Do not change `DEFAULT_ALGORITHM`'s value, do not change
    the `z.enum(...).catch(...)` expression, and do not change the import.** The diff on this file
    must touch comment lines only.
  </action>
  <verify>
    <automated>pnpm vitest run apps/worker packages/harness scripts</automated>
    <automated>pnpm typecheck &amp;&amp; pnpm --filter web typecheck &amp;&amp; pnpm --filter web test</automated>
  </verify>
  <acceptance_criteria>
    - Each new case was observed failing before its source edit; quote one failure in the SUMMARY.
    - `pnpm vitest run apps/worker packages/harness scripts` passes with 0 skipped and 0 failures.
    - `pnpm --filter web test` is green and `pnpm --filter web typecheck` exits 0 — the client suite is untouched and stays untouched.
    - `git diff --numstat apps/web` lists exactly one path, `apps/web/src/lib/searchParams.ts`, and `git diff -U0 apps/web/src/lib/searchParams.ts` contains no added or removed line whose first non-whitespace characters are anything other than a comment opener. Every value line in that file is byte-identical.
    - `git diff --numstat apps/web/src/components/ribbon/AlgorithmSelect.tsx` reports no change — D-04's relabel is 07-18's and did not leak in.
    - Test 6 asserts the two id constants share their first two members and differ only at index 2, and its source carries a comment naming 07-18 as the plan that collapses them.
    - `grep -c 'export const PIPELINE_ALGORITHM_IDS' packages/harness/publishedAlgorithms.ts` prints `1`, and `grep -cE '^\s*import ' packages/harness/publishedAlgorithms.ts` prints `0` — the browser-safe zero-import property survived. <!-- planner-discipline-allow: import -->
    - `grep -c '07-18' packages/harness/publishedAlgorithms.ts` is at least 1 — the deletion owner is named in the source, not only in this plan.
    - `grep -vE '^\s*(\*|//|/\*|#|--)' apps/worker/src/scheduled.ts apps/worker/wrangler.toml apps/worker/src/env.ts apps/worker/migrations/0001_algorithm_state.sql scripts/replayRig.ts packages/harness/publish.ts | grep -c 'sigma1'` prints `0` — no executable line, TOML assignment or SQL statement in the pipeline tier still names the retired id. <!-- planner-discipline-allow: sigma1 -->
    - Every line in the files this task touched that carries the PD-05 marker begins, after leading whitespace, with a comment opener — verified by a grep that selects marker lines and rejects any whose first non-whitespace characters are not one of `//`, `*`, `/*`, `#` or `--`.
    - `git diff apps/worker/migrations/0001_algorithm_state.sql` shows changes on comment lines only; no `CREATE`, `INSERT`, `ALTER` or index statement is in the diff.
    - Test 3's thrown message contains all three pipeline ids, asserted against the imported constant joined at runtime rather than a hardcoded sentence.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>The publisher and the Worker write under `vpr`; the browser still requests the retired prefix on purpose, proven by a comment-only diff across the whole of `apps/web`; and the two constants encoding that transition each say in their own source which plan collapses them.</done>
  <reversibility rating="reversible">One added export, three repointed default/validation call sites, and a set of comment rewrites. Nothing published, nothing deployed — `wrangler.toml`'s change is inert until 07-19 runs the deploy. `git revert` restores the single-constant shape and the old live tier exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The full-repo sweep, the standing D-05 assertion, and the forward-looking runbook</name>
  <precondition>Tasks 1 and 2 are committed and the tree is clean: `git status --porcelain` prints nothing. The sweep measures the post-rename tree and its found-versus-enumerated table is only meaningful against a committed state; running it over a dirty tree produces a number that cannot be reproduced or reviewed.</precondition>
  <read_first>
    - `.planning/phases/07-event-pages/07-PATTERNS.md`, the `## VPR Rename Sweep — Enumerated File List` section in full — the floor this sweep is measured against, including its own instruction to re-grep rather than treat the list as exhaustive.
    - `.planning/phases/07-event-pages/07-RESEARCH.md`, the `## Runtime State Inventory` table and the `**Additional finding beyond CONTEXT.md's named blast radius:**` paragraph beneath it — the wider count and the D1 row-data finding that has no code analog.
    - `.planning/phases/07-event-pages/07-VALIDATION.md`, the D-05 row of the Per-Task Verification Map — which states the live half of this assertion and names it a Wave 0 gap.
    - `vitest.config.ts` at the repo root — the `projects` array and the `node` project's `include` globs, which is why the new test file must sit under `packages/**` to be picked up by `pnpm test`.
    - `docs/worker-operations.md` in full — a runbook whose commands get executed, so a stale id in it is a wrong instruction rather than a stale record. Note which of its lines are instructions and which cite measured figures; they get opposite treatment.
    - `docs/publish-budget.md` lines 200-230 and 540-560 — the seed-file command and the re-measure instruction, so you can see which half of that file is a record (tier F, untouched here) and which half 07-19 re-measures.
    - `packages/harness/browserSafeSchemas.test.ts` — the closest existing analog for a repo-shape assertion in this codebase: a test that walks source and asserts a structural property rather than a behaviour. Mirror its structure, its failure-message style and its exclusion-commenting habit rather than inventing a new shape.
    - `packages/harness/publishedAlgorithms.ts` as Task 2 left it, and `packages/harness/algorithmIdentity.test.ts` if a prior attempt created it.
  </read_first>
  <files>packages/harness/algorithmIdentity.test.ts, docs/worker-operations.md</files>
  <action>
    Re-grep the whole tree, close whatever the enumeration missed, and leave behind a test that will
    notice the next time.

    **1. Re-grep, and record found versus enumerated.** Run a case-insensitive search for the
    pre-rename token across every tracked file, excluding `node_modules`, `.git`, `dist`, `reports`,
    `corpus` and the planning directory. Classify every hit into exactly one of the three tiers
    defined in this plan's "The tier line, stated file by file" section: tier P (fix now), tier C
    (07-18's — it must appear in this plan's `<handoff_to_07_18>` list, and if it does not, add it to
    the SUMMARY as a newly-found client-tier file), or tier F (frozen history — leave). Any hit that
    fits none of the three is a genuine surprise: name it in the SUMMARY, decide it explicitly
    against PD-02's identity-versus-implementation rule, and record the decision.

    Record in the SUMMARY a table with three columns — file, tier, and whether PATTERNS.md's
    enumeration named it — so the floor-versus-actual gap that outline assumption 8 warned about is
    measured rather than assumed. Report the counts: files enumerated, files found, files found that
    were not enumerated.

    **2. Write `packages/harness/algorithmIdentity.test.ts`.** A single test file, no new
    dependency, mirroring `browserSafeSchemas.test.ts`' shape.

    Walk the repository from its root with `readdirSync`, skipping the directory names `node_modules`,
    `.git`, `dist`, `reports`, `corpus`, `.wrangler`, `test-results`, `playwright-report` and
    `coverage`. Read every remaining file as UTF-8, skipping anything that is not text.

    Match identity-shaped occurrences only, not every appearance of the token: the id inside double
    quotes, inside single quotes and inside backticks; the id immediately followed by an at-sign
    (the artifact-key and version-file segment); the id as the value of an `algorithm=` query
    parameter; and the id as the right-hand side of an `algorithm_id` SQL comparison or assignment.
    Anchor each pattern so it does not match the four variant ids as a prefix of the published one,
    and add each variant id as its own pattern so a stale variant is caught rather than shadowed.
    Deliberately do NOT match a bare occurrence inside a filesystem path segment: under PD-02 the
    implementation module keeps its directory name, and a pattern that matched it would fail on
    every import line in the repository and would have to be neutered into uselessness.

    Export `IDENTITY_SWEEP_EXCLUSIONS` as an array of path prefixes or globs, each entry on its own
    line with a trailing comment giving its one-line reason. Seed it with exactly these, and no
    others: the planning directory (planning history), the Claude configuration and skill directories
    (conventions and frozen sketch sources), `REBUILD_SPEC.md` (project history), `docs/models/`
    (measurement records), `docs/first-paint-measurement.md` (measurement record),
    `docs/publish-budget.md` (measurement record; 07-19 re-measures it), `data/baselines/` (frozen run
    fingerprints), and `apps/web/` — whose comment must say it is the CLIENT tier, that it is
    deliberately still on the retired id because the deployed browser reads those objects, and that
    **07-18 deletes this entry** as the mechanism that lands the client half of this assertion.

    Export `PRE_RENAME_MARKER` as the marker token from PD-05. A line is exempt when it contains the
    marker AND its first non-whitespace characters are a comment opener — one of a double slash, an
    asterisk, a slash-asterisk, a hash, or a double dash. Implement that as two conditions, not one:
    a marker on a non-comment line must NOT exempt, and the test must carry its own case proving it
    (a fixture string with the marker appended to an assignment is still reported as a violation).
    This is the mechanical form of prohibition 2 and it is the reason the escape hatch is safe.

    Assert three things. First, zero violations outside the exclusions. Second, the total count of
    marker-exempted lines across the tree is at most twelve — a cap, so the hatch cannot be widened
    quietly; if a legitimate thirteenth is needed the cap moves in a visible diff with a reason.
    Third, `IDENTITY_SWEEP_EXCLUSIONS` has the exact length it is seeded with, so an added exclusion
    is a deliberate, reviewed edit rather than a quiet way to make a red gate green.

    The failure message must print every violating file with its line number and the matched text,
    because the value of this gate is entirely in how fast it tells someone what to fix.

    Write the file's header doc comment to state the three-way split explicitly: that this file is
    the SOURCE half of the D-05 assertion, that 07-18 owns the CLIENT half and lands it by deleting
    one exclusion entry, and that 07-19 owns the LIVE half — zero objects under the retired prefix in
    R2 and zero rows carrying it in D1 — which no source-level test can prove. Name all three plans.
    Cross-referencing them here is what keeps this file from reading as either a duplicate of 07-19's
    check or a claim that the rename is finished.

    **3. Close whatever step 1 found in tier P.** Apply the same rules Tasks 1 and 2 used: identity
    renames, implementation stays, measured figures keep their name and get the marker.

    **4. `docs/worker-operations.md`.** This is a runbook. Update every line that is an INSTRUCTION —
    the seed-file name in the `wrangler d1 execute` command, the statement of which algorithm folds
    live, and the replay-rig invocation's `--algorithm` value — so a reader who copies a command gets
    a command that works after this phase. Keep every line that is a RECORD — the measured subrequest
    counts, and the account of the deliberate misconfiguration that was flipped and observed to fail
    — with its original id, marked per PD-05.

    Add a short paragraph, near the live-fold section, stating the transition's actual position:
    which half of the rename has landed, that the deployed Worker is still running the pre-rename
    tier until 07-19 redeploys it, and that the deployed browser is still reading the pre-rename
    prefix until 07-18. This paragraph is required by this plan's third prohibition — a runbook that
    reads as though the rename is complete would send an operator to run a command against a state
    that does not exist yet.

    Do not touch `docs/publish-budget.md`, `docs/models/`, `docs/first-paint-measurement.md` or
    `data/baselines/`. Do not touch anything under `apps/web/`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/harness/algorithmIdentity.test.ts</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck &amp;&amp; pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/harness/algorithmIdentity.test.ts` is green, and the SUMMARY quotes one deliberately-induced failure of it (add a violating line to a scratch file, observe the message names the file, line and matched text, then remove it) — a gate nobody has seen fail is a gate nobody knows works.
    - The test file carries its own case proving the marker does NOT exempt a non-comment line, and that case is green.
    - `pnpm test` is green across every project in `vitest.config.ts`, including `apps/web`.
    - `pnpm typecheck` and `pnpm --filter web typecheck` both exit 0.
    - `grep -c 'export const IDENTITY_SWEEP_EXCLUSIONS' packages/harness/algorithmIdentity.test.ts` prints `1`, and every entry in that array has a comment on its own line — verified by the entry count equalling the comment count within the array literal.
    - `grep -c '07-18' packages/harness/algorithmIdentity.test.ts` is at least 2 (the header's three-way split and the client exclusion's own reason), and `grep -c '07-19' packages/harness/algorithmIdentity.test.ts` is at least 1.
    - `grep -cF 'apps/web' packages/harness/algorithmIdentity.test.ts` is at least 1 — the client exclusion is present, which is what 07-18 deletes.
    - The exclusion-length assertion and the marker-count cap are both present as assertions in the test file, not as comments.
    - Running the step-1 re-grep again after all edits, restricted to tier-P paths (`packages/`, `scripts/`, `apps/worker/`, `data/algorithm-versions/`, `docs/worker-operations.md`) and filtering out comment lines, returns zero identity-shaped hits for the retired id. <!-- planner-discipline-allow: sigma1 -->
    - The SUMMARY carries the found-versus-enumerated table with its three counts, and every file it lists is classified into exactly one of tier P, tier C or tier F.
    - Every tier-C file the re-grep found appears in this plan's `<handoff_to_07_18>` list, or is added to the SUMMARY as newly found with its tier decision written out.
    - `docs/worker-operations.md` contains a paragraph naming both 07-18 and 07-19 as the plans that complete the transition, and `grep -cF '[pre-rename]' docs/worker-operations.md` is at least 1 with every such line beginning, after leading whitespace, with a comment opener or sitting inside a quoted measurement sentence recorded in the SUMMARY.
    - `git diff --numstat docs/publish-budget.md docs/first-paint-measurement.md docs/models data/baselines apps/web/src/components apps/web/e2e` reports no change on any path.
    - `git status --porcelain` is empty after the task's commit.
  </acceptance_criteria>
  <done>The sweep is measured rather than assumed — the tree carries a runnable test that fails on any identity-shaped survival of the retired id outside a short, individually-reasoned, length-asserted exclusion list — and the one runbook whose commands get executed now describes the half-landed state honestly instead of implying a rename that has not finished.</done>
  <reversibility rating="reversible">A new test file and a documentation edit. Reverting removes a gate and restores a stale runbook; it breaks nothing and publishes nothing.</reversibility>
</task>

</tasks>

<threat_model>
ASVS level 1; block on `high`. Two `high` threats are identified, both on the integrity of a
half-applied identity rename, and both are dispositioned `mitigate` with the mitigation named in
this plan's own structure rather than deferred.

No package-manager install occurs in this plan, so there is no `T-07-16-SC` supply-chain row and no
package-legitimacy checkpoint. Stated rather than omitted, since the phase's threat convention
otherwise expects that row.

No task in this plan reads a credential. Nothing here runs `tsx --env-file=.env`, no command needs
the TBA key or an R2 token, and the only publish invocation is `--dry-run`. The secrets-handling
convention in `.claude/CLAUDE.md` is therefore satisfied vacuously, which is the safest way to
satisfy it.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry module -> R2 key namespace | `algorithm.id` is interpolated directly into the `{algorithmId}` segment of every published object key; a wrong value here names a namespace nothing can resolve |
| Committed version file -> published manifest -> browser version resolution | The manifest's third entry id is read out of a tracked JSON file; the browser resolves the version it requests from that entry |
| Registry module -> D1 `algorithm_state.algorithm_id` | `serializeState(algorithm.id, …)` writes the id as row data; the Worker reads state scoped by the same column |
| Tracked config (`wrangler.toml`) -> deployed Worker live-fold tier | `LIVE_ALGORITHM_IDS` selects which algorithm folds per tick once deployed |
| Source tree -> the standing assertion's own exclusion list | The gate's truthfulness is bounded by what it excludes, so the exclusion list is itself a trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16-01 | Tampering (integrity of published identity) | the chain registry `id` -> `applyPromotedOverrides` -> `artifactKey` -> `buildAlgorithmsManifest` | high | mitigate | A half-applied rename that leaves the publisher writing one id while the manifest names another would publish an entire corpus under keys no client can resolve, and would do it silently — every `putObject` would succeed. Closed by construction rather than by check: `buildAlgorithmsManifest` reads the third entry's `id` from `promoted.id`, the same version file `applyPromotedOverrides` pins, so key and manifest cannot disagree unless the file disagrees with itself. Reinforced by Task 1 Test 2 (the manifest id is asserted against the file's own field, not a literal), Test 8 (every emitted key carries the new segment and none carries the old), Test 9 (a stale id throws rather than resolving), and an acceptance criterion forbidding a literal id at the manifest construction site |
| T-07-16-02 | Denial of Service (availability of every page) | `PUBLISHED_ALGORITHM_IDS` as a dual-tier constant | high | mitigate | Flipping the browser-facing id list before 07-17 writes the replacement objects makes `useAlgorithmVersion` find no manifest entry, which disables every artifact query on every route — with `.github/workflows/deploy.yml` deploying on every push to `main` and 07-17 sitting behind a blocking gate, that outage would be live and open-ended. Closed by PD-01's two-constant split: the browser-facing value does not move in this plan, and the mitigation is enforced mechanically by an acceptance criterion requiring `git diff --numstat apps/web` to list exactly one file whose diff touches comment lines only |
| T-07-16-03 | Tampering (state corruption) | D1 `algorithm_state` rows keyed by `algorithm_id` | medium | mitigate | Both the write path (`serializeState(algorithm.id, …)`) and the Worker's read path derive the scope from one value, so they cannot drift within a build. The remaining exposure is temporal — a Worker deployed under one id reading state seeded under another — and it is closed by ordering rather than by code: the Worker is not redeployed until 07-19, and 07-17 reseeds under the new id as part of its own run. `readScopedStateSql.test.ts` (a real SQLite engine, not a fake) already pins the scoping bug this column caused once before and stays green through the rename |
| T-07-16-04 | Repudiation (falsified provenance) | `docs/models/`, `docs/first-paint-measurement.md`, `docs/publish-budget.md`, `data/baselines/` | medium | mitigate | A blanket sweep would rewrite committed accuracy figures, byte counts and D1 read-backs to claim they were produced by runs under a name that did not exist when they ran, destroying the checkability that made committing them worthwhile. Closed by the tier-F exclusion list, by PD-05's line-level marker for in-code citations, and by this plan's first prohibition. The marker is honoured only on a comment line, so it cannot quietly launder an assignment |
| T-07-16-05 | Spoofing (one model under two names) | the `ALGORITHMS` registry and `HARNESS_ONLY_ALGORITHM_IDS` | medium | mitigate | `vpr` and `vpr-adapt` are prefix-adjacent, and a membership test using `startsWith` or substring `includes` anywhere in the resolution chain would route a research variant's numbers into the site's headline algorithm. Every membership test in the chain is exact equality — `applyPromotedOverrides`' two `===` branches, `Set.has`, and an `includes` over a string array — pinned by Task 1 Test 5, Task 1 Test 10 (accept the published id, reject all four variants, in one file) and Task 2 Test 4 |
| T-07-16-06 | Tampering (a gate tuned until it passes) | `IDENTITY_SWEEP_EXCLUSIONS` and the `[pre-rename]` marker | medium | mitigate | The standing assertion's value is exactly its exclusion list's honesty. Closed by three assertions inside the test itself: the exclusion array's length is pinned, the marker-exempted line count is capped at twelve, and the marker is honoured only when the line begins with a comment opener — with the test carrying its own case proving a marker on an assignment does not exempt. Carried as this plan's second prohibition as well, because the mechanical checks prove the shape of the hatch, not the honesty of the person widening it |
| T-07-16-07 | Elevation (a stale id in tracked config folding nothing) | `wrangler.toml`'s `LIVE_ALGORITHM_IDS` | low | accept | The existing `UnknownLiveAlgorithmIdError` makes an unrecognized id a hard throw with the accepted list in the message rather than a silent skip, and Task 2 Test 3 re-pins it under the renamed set. The residual risk is an operator deploying with a `--var` override naming the retired id, which throws loudly on the first tick. Accepted: the failure is immediate, attributable and self-describing, and narrowing it further would mean validating a deploy-time override this repo does not otherwise model |
</threat_model>

## Probe coverage ledger (no silent drops)

**Edge probes — 0 of the 17 authored here as primary edges, by assignment; 0 flagged; 0 dropped.**
Every one of the seventeen is owned by the plan owning the surface it constrains (approved outline,
assumption 14): EVNT-02 to 07-11, EVNT-03 to 07-01, EVNT-04 to 07-12, EVNT-05 to 07-14, EVNT-06 to
07-13. This plan owns no primary edge because it renders nothing, orders nothing and computes
nothing. Four rename-level halves and one explicitly-absent category are accounted for in the table
under `<inherited_ownership>`; each is authored as a cross-referenced truth naming the plan that
owns its primary half, so none is counted twice and none is dropped. Phase-wide the equality still
reconciles: 17 == 17 across 07-01, 07-11, 07-12, 07-13 and 07-14.

**Prohibitions — 11 raw candidates recalled, 3 minted.** Dropped as routine engineering already
owned by an acceptance criterion, a threat row or code review: a partial rename leaving the manifest
and the key disagreeing (T-07-16-01 plus Tests 2, 8 and 9); declaring the sweep done from
PATTERNS.md's list without re-grepping (Task 3's criteria); widening the id list to four members so
the ribbon shows one model twice (PD-01 plus the `apps/web` zero-change gate); leaving the
transitional constant undeleted forever (Test 6 plus the named deletion owner in its own source);
using a rename commit as cover for a behaviour change (the digest gate); a prefix-matching membership
test collapsing the published id and its adaptation variant (T-07-16-05 plus Tests 5, 10 and 4);
deleting old objects here (an explicit scope boundary); and touching the client tier (a
`git diff --numstat` criterion). **Canon-referral drops, breadcrumbs rather than minted
prohibitions:** none — this plan touches no injection, traversal, authentication or PII surface, and
the two identity-integrity concerns are carried as threat rows because they are mechanical rather
than ethical. **Minted (values / honesty / integrity, all descriptor-less and fail-closed):** the
frozen-history prohibition, the never-widen-the-gate prohibition, and the never-claim-completion
prohibition, all in `must_haves.prohibitions` above.

**UI-SPEC `## UI Considerations` lift — 0 rows, by assignment.** Every E-row belongs to a tab or to
the page header; see `<inherited_ownership>` for the owner of each and for the split of the
Cross-Cutting Changes table's D-04/D-05 row.

## Flagged planner assumptions

1. **The `packages/core/algorithms/sigma1/` directory and the implementation symbols are
   deliberately NOT renamed, and this is a decision the developer can reverse.** PD-02 gives the
   rule and its reasoning: the rename follows the published identity, not the implementation. The
   consequence is that after this phase `export const vpr` lives in a file called `sigma1/index.ts`,
   `makeSigma1` builds the VPR module, and `Sigma1State` is VPR's state type. Task 1 is required to
   explain that at the rename site so it reads as a decision rather than an unfinished sweep. It is
   flagged here because neither D-04 nor D-05 nor the outline row asked for a module rename, and
   doing it anyway would multiply this plan's diff by roughly four across ~90 additional files for
   zero movement on any published byte — but it is genuinely arguable the other way, and a
   follow-up `git mv` plus an import sweep would close it cheaply at any later point.

2. **The outline row assigns `PUBLISHED_ALGORITHM_IDS` to this plan, and this plan does not move its
   value. That is a deliberate deviation, argued in PD-01 and surfaced here rather than buried.**
   The outline's own assumption 7 states the invariant that no wave has the client reading a prefix
   nothing has written; moving that constant's value in wave 11 violates that invariant, and the
   violation is mechanical rather than a matter of judgment (`DEFAULT_ALGORITHM`'s type annotation
   and `ALGORITHM_DISPLAY_LABELS`' `Record<PublishedAlgorithmId, string>` both stop compiling, and
   the fix for both is 07-18's plan executed early). The resolution — a second, transitional
   constant consumed by three pipeline call sites — keeps every other line of the outline row intact.
   If the developer prefers the outline's literal assignment, the correct alternative is not to flip
   the constant here but to merge 07-16 and 07-17 into one wave so the write pass follows the rename
   immediately; that is a phase-shape change, not a plan-shape one.

3. **This plan's `searchParams.ts` edit is comment-only, which is narrower than outline assumption 6
   anticipated.** That assumption reserved the file for "07-16 renames the pipeline-visible ids",
   which presupposed the constant flip that flagged assumption 2 declines. What survives is real but
   smaller: the file's doc comments describe a world with one id constant, and after this plan there
   are two. Recorded so the `git diff --numstat` zero-change criteria that 07-11 and 07-15 carry on
   this file are seen to have been honoured by every plan that promised them, and so 07-18's edit is
   still a one-constant flip rather than something this plan pre-empted.

4. **The marker cap of twelve is a bound, not a measurement.** It is set from the eight to ten
   in-code measurement citations this plan's own grep found across `percentiles.ts`,
   `extract-digest-slice.ts`, `stateStore.ts`, `scheduled.ts` and `wrangler.toml`, with a small
   allowance. If the executor's re-grep finds a legitimate thirteenth, the correct action is to raise
   the cap in a visible diff with the reason in the SUMMARY — not to widen a file exclusion, which is
   what prohibition 2 forbids. Flagged because a cap that has to move on first contact looks like a
   failure and is not one.

5. **`07-10-PLAN.md` did not exist when this plan was written.** This plan depends on 07-10 for
   ordering only — it needs the subset publish to have already happened under the retired prefix so
   that the deployed site is enriched before the publisher's identity moves. It reads nothing 07-10
   produces and asserts nothing about its contents. If 07-10's final shape publishes under a
   different key set than the outline describes, this plan's Task 2 precondition is the place that
   assumption fails visibly.

<verification>
- `pnpm test` green across every project in `vitest.config.ts`, with all pre-existing cases in every
  touched file still passing and unweakened.
- `pnpm typecheck` and `pnpm --filter web typecheck` both exit 0.
- `pnpm vitest run packages/harness/digest.test.ts` green with `git diff --numstat
  packages/harness/digest.test.ts` reporting no change — the rename moved no predicted number.
- `pnpm vitest run packages/harness/algorithmIdentity.test.ts` green, and its deliberately-induced
  failure observed and quoted in the SUMMARY.
- `git diff --numstat apps/web` lists exactly one path, `apps/web/src/lib/searchParams.ts`, whose
  diff touches comment lines only.
- `git diff --numstat docs/publish-budget.md docs/first-paint-measurement.md docs/models
  data/baselines` reports no change on any path.
- `ls data/algorithm-versions/` lists exactly two files, both under the renamed prefix, and
  `git diff -M --stat` shows both as renames.
- A dry-run publish for one event emits keys carrying the new algorithm segment and none carrying
  the retired one, and performs zero object writes.
- The tier-P re-grep, comment-filtered, returns zero identity-shaped hits for the retired id.
</verification>

<success_criteria>
- Every value that determines what the publisher writes — the registry entry's id, the two committed
  version files, the promoted-override chain, the manifest builder, the publisher's module table,
  the replay rig's default set and the Worker's live-fold tier — resolves to `vpr`.
- Nothing the deployed browser reads has changed. `PUBLISHED_ALGORITHM_IDS`, `DEFAULT_ALGORITHM`,
  the ribbon's label map and every e2e spec are exactly as 07-15 left them, so the live site keeps
  reading the objects 07-10 published, and 07-17's write pass is purely additive.
- The manifest and the artifact key cannot name different algorithms, because both derive from one
  field in one tracked file.
- No predicted number moved, proven by the reproducibility digest rather than by inspection.
- The tree carries a runnable assertion that no identity-shaped occurrence of the retired id
  survives outside a short exclusion list whose length is itself asserted — with the client half
  landing when 07-18 deletes one line of that list and the live half landing at 07-19.
- Historical measurement records still say what was actually measured, under the name it was
  measured under.
- The one runbook whose commands get executed states which half of the rename has landed and names
  the two plans that finish it, so nothing in the tree reads as though the transition is complete.
- The sweep was re-run rather than taken from a list, and the found-versus-enumerated gap is
  recorded with counts.
</success_criteria>

<output>
Create `.planning/phases/07-event-pages/07-16-SUMMARY.md` when done.

Record in it:
- The found-versus-enumerated sweep table: every file the re-grep hit, its tier (P / C / F), and
  whether `07-PATTERNS.md`'s enumeration named it — plus the three counts (enumerated, found, found
  but not enumerated). This is the measured answer to outline assumption 8.
- Any hit that fit none of the three tiers, with the identity-versus-implementation decision made
  for it and the reasoning.
- Any tier-C file found that is not already in this plan's `<handoff_to_07_18>` list, flagged for
  07-18.
- One observed RED failure per task, quoted, plus the deliberately-induced failure of
  `algorithmIdentity.test.ts` with its full message.
- The before-and-after `codeVersion` / `paramSetName` / `version` values of both renamed version
  files, showing only the `id` changed.
- Confirmation that `digest.test.ts` passed unchanged, and the `git diff --numstat` results for
  `apps/web`, `docs/publish-budget.md`, `docs/first-paint-measurement.md`, `docs/models` and
  `data/baselines`.
- The final count of `[pre-rename]` markers in the tree, against the cap of twelve.
- The dry-run publish's emitted key sample for one event, showing the new algorithm segment.
</output>
