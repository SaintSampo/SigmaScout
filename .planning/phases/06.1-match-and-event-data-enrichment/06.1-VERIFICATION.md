---
phase: 06.1-match-and-event-data-enrichment
verified: 2026-08-27T01:08:22Z
reopened: 2026-08-27T02:52:00Z
reopened_reason: "06.1-UAT.md recorded a major defect in this phase's own deliverable (playoff match rows render bonus-RP dots). Verification had passed before UAT ran; status returned to gaps_found so the phase is not asserted Complete while a known defect is open."
status: gaps_found
score: 8/9 must-haves verified (1 accepted as tracked debt - see overrides); REOPENED 2026-08-27 after UAT found 1 major defect - see gaps G-06.1-26 / G-06.1-28
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "Every page kind's measured maximum stays at or under its committed payload-budget ceiling"
    reason: "teams/{year}'s overage is caused entirely by pre-06.1 Phase-6 commits (06f468ad, bf1e3228) first swept into a live republish by this phase's required single republish; this phase's own team/{teamKey}/{year} artifact is well under its ceiling (340,569 of 375,000 bytes); tracked at .planning/WINDOWS.md ledger #11 for a separate developer decision. The ceiling was NOT raised and packages/harness/payloadBudget.test.ts remains genuinely red."
    accepted_by: "Jacob Williams"
    accepted_at: "2026-08-27T01:25:36Z"
gaps:
  - truth: "Every published page kind's measured maximum stays at or under its committed payload-budget ceiling (plan 06.1-07 must_haves.truths, item 5)"
    status: accepted-override
    reason: |
      packages/harness/payloadBudget.test.ts's internal-consistency test fails: teams/{year}'s
      measured maximum (3,577,069 bytes) exceeds its own committed budgetMaxBytes ceiling
      (3,500,000 bytes) by 77,069 bytes. Independently re-run and confirmed still red at
      verification time (not a stale claim). Root cause independently re-derived (not merely
      accepted from SUMMARY): git log/show confirms commits 06f468ad ("publish phase-group
      metrics") and bf1e3228 ("publish rarity tier on the teams artifact") are dated 2026-08-26,
      tagged feat(06) (Phase 6, pre-06.1), and both commit messages state outright "requires an
      artifact republish." Reading packages/harness/publish.ts's buildTeamsArtifact and
      buildEventArtifact directly confirms neither references any of this phase's own new fields
      (redBonusRp, actualRedBonusRp, rank, totalTeams, or history-row percentile) — both call only
      roundTeamMetricRecord(t.metrics), so the added bytes are exclusively the pre-existing tier
      field these two Phase-6 commits added to teamMetrics() output, which plan 06.1-07's republish
      was simply the first republish to carry live. This phase's own artifact (the singular
      team/{teamKey}/{year} kind, the one every 06.1 field actually lands on) measures 340,569
      bytes against its 375,000-byte ceiling — well under, with 34,431 bytes (9.18%) of headroom
      remaining. The ceiling was deliberately NOT raised (plan 06.1-07's own first prohibition), so
      the test is left genuinely red rather than silently made to pass.
    artifacts:
      - path: "docs/publish-budget.md"
        issue: "teams/{year} row's maxBytes (3,577,069) exceeds its own budgetMaxBytes (3,500,000); machine-readable block correctly left un-edited on the ceiling value, so the test stays honestly red"
      - path: "packages/harness/payloadBudget.test.ts"
        issue: "internal-consistency assertion for the 'teams' page kind fails; 9/10 other assertions in the same file pass"
    missing:
      - "A developer decision on data/scope this project has not yet made: either shrink teams/{year} (e.g. revisit bf1e3228's own documented Common-tier-omission trade) or deliberately raise budgetMaxBytes for that one kind with a recorded reason — tracked as .planning/WINDOWS.md ledger entry #11 (open), not silently absorbed"
  - gap_id: G-06.1-26
    truth: "Bonus-RP dots assert a real per-bonus fact only where bonus RP exists — qualification matches; playoff rows must not render earned/missed dots"
    status: failed
    source: 06.1-UAT.md test 26
    severity: major
    reason: |
      buildTeamSeasonArtifact publishes actualRedBonusRp/actualBlueBonusRp for EVERY played
      match, not only compLevel === 'qm'. Confirmed against live published artifacts:
      frc118/2024 carries actual arrays on all 23 sf and 13 f matches (2024txkat_sf2m1
      act=[true,false]); frc1114/2026 on all 12 sf and 11 f (2026onham_f1m1
      act=[true,true,false]). The predicted side is already correctly gated to qual matches
      (0 arrays on every sf/f row), so predicted and actual disagree on the same rule.
      MatchTable's ActualScoreLine then renders earned/missed dots on playoff rows, asserting
      ranking points that FRC awards in qualification play only.
    artifacts:
      - path: "packages/harness/publish.ts"
        issue: "actual per-bonus flags published for playoff matches; the qual-only gate applied to the predicted side is missing on the actual side"
      - path: "apps/web/src/components/team/MatchTable.tsx"
        issue: "ActualScoreLine renders BonusRpDots for every compLevel with no playoff guard"
    missing:
      - "Gate actual per-bonus flag publication to compLevel === 'qm' in buildTeamSeasonArtifact"
      - "Render playoff rows' bonus dots in the muted 'unknown' state bonusRp.ts already defines, so already-published artifacts render correctly without a republish"
      - "Pin both with a publish.test.ts case and a MatchTable.test.tsx case"
  - gap_id: G-06.1-28
    truth: "The per-event metric line does not carry the visible 'vs season-final field' basis caption"
    status: failed
    source: 06.1-UAT.md test 28
    severity: cosmetic
    reason: |
      Design change requested by the user at UAT. The tiers themselves passed the sensibility
      check (test 28 = pass); the always-visible caption at EventSection.tsx:131-138 was judged
      to be clutter. CORRECTION (planner, 2026-08-27): the title tooltip is an attribute of the
      same span, so removing the span removes the hover explanation too — no explanation
      survives anywhere. This reverses PD-14 and un-mitigates T-06.1-24 (Repudiation, high).
      A decision to make explicitly, not a side effect to absorb silently.
    artifacts:
      - path: "apps/web/src/components/team/EventSection.tsx"
        issue: "lines 131-138 render the visible caption; hasTieredTile may become unused"
      - path: "apps/web/src/components/team/EventSection.test.tsx"
        issue: "two assertions query data-testid event-tier-basis-2024casj and will fail once removed"
    missing:
      - "Remove the visible 'vs season-final field' span"
      - "Remove or repoint the two EventSection.test.tsx assertions"
      - "CORRECTION (planner, 2026-08-27): the title tooltip is an attribute of the same span, so removing the span removes the hover explanation too — nothing survives. Pick a home (TierKeyRow renders once as this colour system's legend) or sign off on removing the explanation entirely"
human_verification:
  - test: "Open a 2024 team page and a 2026 team page on the deployed site. On a played qualification match row, confirm the bonus dots read clearly as solid/hollow at rest, unambiguous to a first-time scout; confirm an upcoming row's predicted dots read as predictions, not results."
    expected: "Dot meaning is unambiguous against the rarity-tier palette without hovering."
    why_human: "Perception/legibility question — no unit test can answer it. Deferred from plan 06.1-07 Task 2's own <human-check> block; per workflow.human_verify_mode: end-of-phase, still open per that plan's own SUMMARY ('remain open ... collected at /gsd-verify-work')."
  - test: "Pick three team-and-event pairs across different seasons and compare each event card's published 'Rank N of M' standing against that event's rankings page on The Blue Alliance."
    expected: "The published number matches the third-party source of truth."
    why_human: "Cross-checks the published artifact against an external source the test suite deliberately does not reach out to. Deferred from plan 06.1-07 Task 2, still open."
  - test: "Open a team whose performance changed sharply mid-season. Confirm the early events' rarity tiers read sensibly given they rank against the season-final field, and confirm the basis caption ('vs season-final field') is visible on the per-event metric line."
    expected: "A reader is not misled into reading an early-event tier as an as-of-that-event rank."
    why_human: "This is the resolution point for plan 06.1-06's judgment-tier prohibition ('MUST NOT apply a rarity-tier colour without a visible basis statement'). The caption's existence and text are test-verified (EventSection.test.tsx); whether it actually prevents misreading is a judgment call this project's own workflow routes to end-of-phase human review, not to an automated gate. Deferred from plan 06.1-07 Task 3, still open."
---

# Phase 06.1: Match and event data enrichment Verification Report

**Phase Goal:** The team page's per-match and per-event detail is backed by real published data
instead of placeholders — bonus RP resolved per bonus, event standing shown per event, and the
per-event metric line tiered like every other metric.
**Verified:** 2026-08-27T01:08:22Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (SC-1) | Every bonus-RP dot on a played qualification match row (registered-RP season) resolves to solid/hollow — none stays `unknown` | ✓ VERIFIED | `apps/web/src/lib/bonusRp.test.ts` (29 tests) + `apps/web/src/components/team/MatchTable.test.tsx` "real bonus-RP dot states" describe block, both re-run and passing (0 skipped). Independently fetched the live production artifact `v1/team/frc118/2026/sigma1@2.0.0+tuned-2026-08.json` and confirmed match `2026txcle_qm2` carries real, non-omitted arrays: `redBonusRp:[0,0,0]`, `blueBonusRp:[0,0,0]`, `actualRedBonusRp:[false,false,false]`, `actualBlueBonusRp:[true,false,false]` — none `null`, none absent. |
| 2 (SC-2) | Each event section states "Rank N of M" sourced from real ranking data, never derived client-side | ✓ VERIFIED | `apps/web/src/components/team/EventSection.test.tsx` re-run passing (5 standing-related cases: populated, absent, rank-only, totalTeams-only, placement). Live artifact fetch confirms event `2026txcle` carries `{rank: 4, totalTeams: 32}` on the production team page for frc118/2026. `event_rankings` corpus table populated for all five seasons (2022–2026), 47,695 rows, 0 rows violating `rank>=1 AND total_teams>=1` (plan 06.1-04). |
| 3 (SC-3) | The per-event metric line carries the same rarity tiers as the season header/Teams table, from a published percentile, not a season-final rank misapplied to an as-of-event value | ✓ VERIFIED | `apps/web/src/components/team/EventSection.test.tsx` "per-event metric tiers and basis caption" describe block passing. Live artifact confirms `metricHistory` rows carry real, non-uniform percentiles per row (e.g. `total: 69.8`, `phaseAuto: 69.5`, `phaseTeleop: 65.1`, `phaseEndgame: 59` on one row) — sourced from `withHistoryPercentiles`, which ranks against the season-final pool (D-06.1-A), never the team's own season-final percentile record (structurally unreachable from `EventSection.tsx`, which is never passed that record). |
| 4 (SC-4) | The D-15 digest-reproducibility gate still passes: every committed algorithm version reproduces its recorded prediction-stream digest bitwise | ✓ VERIFIED | `pnpm vitest run packages/harness/digest.test.ts` re-run independently: 3 tests, 0 skipped (corpus present, not a silent skip), both committed `sigma1@2.0.0` versions reproduced. Confirmed by reading `computePredictionStreamDigest` (packages/harness/promote.ts:97-103) that it hashes exactly `matchKey`, `pRedWin`, `redScore`, `blueScore` — none of which any 06.1 plan touches (all four are computed upstream of the RP step per plan 06.1-02's own read-first instructions). `git diff --stat data/algorithm-versions/` confirmed empty across the whole phase. |
| 5 | No fabricated, inferred, or zero event standing is ever rendered when ranking data is absent (plan 06.1-01 prohibition) | ✓ VERIFIED | `EventSection.test.tsx` asserts `queryByTestId('event-standing-...')` is `null` for absent, rank-only, and totalTeams-only fixtures — a real, non-vacuous test, not merely asserted in prose. |
| 6 | A published `null` actual-bonus-flag pair renders as `unknown`, never coerced to `missed` (plan 06.1-06 prohibition) | ✓ VERIFIED | `bonusRp.test.ts` asserts `bonusStatesFromFlags(null, 3)` deep-equals three unknown states, tested against `null` specifically (not `undefined`). |
| 7 | TEAM-04 / F-06-1 / F-06-3 are genuinely closed, not merely marked closed | ✓ VERIFIED | REQUIREMENTS.md: TEAM-04 already `[x]` complete (Phase 6; extended, not re-opened, by 06.1). F-06-1/F-06-3 are `06-UAT.md` follow-up IDs (not formal REQUIREMENTS.md entries) — confirmed both marked `RESOLVED 2026-08-26` in `.planning/phases/06-team-pages/06-UAT.md` with a resolution narrative naming the exact plans/commits that closed each, and commit `a66bce8d` applies that resolution. No orphaned phase-06.1 requirement rows found in REQUIREMENTS.md. |
| 8 | Every page kind's measured maximum stays at or under its committed payload-budget ceiling; no ceiling raised to fit a measurement (plan 06.1-07 must_haves.truths) | ✗ FAILED | See Gaps below. `teams/{year}` (3,577,069 / 3,500,000 bytes) is over; this phase's own `team/{teamKey}/{year}` artifact (340,569 / 375,000 bytes) is comfortably under. No ceiling was raised (confirmed: `budgetMaxBytes` values for `team`/`teams` unchanged across both 06.1-07 commits). |
| 9 | The judgment-tier "tier colour must carry a visible basis statement" prohibition (plan 06.1-06) is resolved, not merely asserted structurally | ? UNCERTAIN → routed to human verification | The caption's existence, render condition, and exact text are test-verified. Whether it actually prevents a reader's misreading is a judgment call this project's own `human_verify_mode: end-of-phase` workflow explicitly routes to a human checkpoint (plan 06.1-07 Task 3's own `<human-check>` item) — confirmed still open per that plan's own SUMMARY. |

**Score:** 8/9 truths verified (1 failed, not deferred to a later phase — see Gaps; 1 additional judgment item routed to human verification, not counted in the denominator above since it is a distinct human-check item rather than a pass/fail truth)

### Required Artifacts (spot-checked, all three phase items)

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `packages/ingest/rankings.ts` + `event_rankings` corpus table | TBA rankings ingest, idempotent, offseason-inclusive | ✓ VERIFIED | 47,695 rows across 5 seasons, live re-confirmed at HTTPS artifact level |
| `packages/core/algorithms/sigma1/rp/distribution.ts` `RpPmfResult` widening | Per-bonus marginals tallied from existing draws, no new RNG | ✓ VERIFIED | `distribution.test.ts` fixed-seed golden case passing |
| `packages/harness/percentiles.ts` `percentileAgainstSortedPool`/`sortedPoolsByMetric` | Season-final ranking, exact agreement with `percentileRanks` | ✓ VERIFIED | `percentiles.test.ts` passing incl. strict `toBe` cross-check |
| `packages/harness/pageArtifacts.ts` four new `TeamSeasonMatchSchema` fields | Three-state absent/null/array contract | ✓ VERIFIED | `pageArtifacts.test.ts` passing; live artifact shows real (non-null, non-omitted) arrays |
| `apps/web/src/lib/bonusRp.ts`, `MatchTable.tsx`, `EventSection.tsx` | Real data wired to rendering | ✓ VERIFIED | Component tests passing; live artifact data structurally matches what these components consume |
| `docs/publish-budget.md` republish record | One republish, measured, no ceiling moved | ⚠️ PARTIAL | Republish real and measured (object count 54,673 matches prior run exactly); one page kind (`teams`, not `team`) is over its ceiling for reasons independently confirmed external to this phase |

### Data-Flow Trace (Level 4) — live production fetch

Fetched `https://data.sigmascout.org/v1/team/frc118/2026/sigma1@2.0.0+tuned-2026-08.json` directly
(HTTP 200, 340,569 bytes, matching the byte count plan 06.1-07's SUMMARY recorded) and parsed it:

- `events[eventKey="2026txcle"]` → `{rank: 4, totalTeams: 32}` — real, non-zero, non-fabricated.
- `events[...].matches[matchKey="2026txcle_qm2"]` → `redBonusRp:[0,0,0]`, `blueBonusRp:[0,0,0]`,
  `actualRedBonusRp:[false,false,false]`, `actualBlueBonusRp:[true,false,false]` — real per-bonus
  arrays, not the pre-phase aggregate-only shape.
- `metricHistory` rows carry real, row-varying `percentile` values on `total`/`phaseAuto`/
  `phaseTeleop`/`phaseEndgame` (e.g. 69.8 / 69.5 / 65.1 / 59 on one row) — not a static placeholder.

All three of this phase's stated deliverables are **FLOWING** in production, independently of the
SUMMARY narratives' own claims.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| TEAM-04 (extends) | 06.1-01/04/05 | Team page event sections carry real standing | ✓ SATISFIED | Live artifact + tests |
| F-06-1 (06-UAT.md) | 06.1-02/05/06 | Per-bonus RP resolved, not just aggregate | ✓ SATISFIED | Live artifact + tests; F-06-1 marked RESOLVED in 06-UAT.md |
| F-06-3 (06-UAT.md) | 06.1-03/05/06 | Per-event metric line tiered from real percentile | ✓ SATISFIED | Live artifact + tests; F-06-3 marked RESOLVED in 06-UAT.md |
| SC-4 (digest gate) | 06.1-02/05/07 | D-15 gate still green | ✓ SATISFIED | Independently re-run, 3/3 passing |

No orphaned phase-06.1 requirement rows found in REQUIREMENTS.md.

### Anti-Patterns Found

None of TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER found in phase-modified source files. No stub returns,
no hardcoded empty arrays standing in for real data, no console.log-only implementations. The
phase's own "absence vs. null vs. real array" three-state discipline is the opposite of a stub
pattern — it is deliberately more conservative than a naive implementation would be.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full suite health | `pnpm test` | 103/104 files passed, 1421/1422 tests passed | ⚠️ (see gap 8) |
| Typecheck | `pnpm typecheck` | exit 0 | ✓ PASS |
| D-15 digest gate | `pnpm vitest run packages/harness/digest.test.ts` | 3/3 passed, 0 skipped | ✓ PASS |
| Payload budget gate | `pnpm vitest run packages/harness/payloadBudget.test.ts` | 9/10 passed | ✗ FAIL (teams/{year} only) |
| Corpus-backed publish tests | `pnpm vitest run packages/harness/publish.test.ts` | 46/46 passed (10.6s — real corpus work, not a stub) | ✓ PASS |
| Corpus-backed tracer test | `pnpm vitest run packages/harness/eventRank.tracer.test.ts` | passed, not skipped | ✓ PASS |
| Live production artifact fetch | `curl https://data.sigmascout.org/v1/team/frc118/2026/sigma1@2.0.0+tuned-2026-08.json` | HTTP 200, all three phase items present with real data | ✓ PASS |

### Human Verification Required

### 1. Bonus-dot legibility (2024 and 2026 team pages)
**Test:** Open a 2024 and a 2026 team page on the deployed site; check dot solid/hollow legibility at rest and predicted-vs-actual distinction on an upcoming row.
**Expected:** Unambiguous to a first-time scout without hovering.
**Why human:** Perception question against the rarity-tier palette; no unit test can answer it.

### 2. Cross-check standings against The Blue Alliance
**Test:** Pick three team/event pairs across different seasons; compare the published "Rank N of M" against TBA's own rankings page.
**Expected:** Numbers match the third-party source of truth.
**Why human:** The test suite deliberately does not reach out to TBA at verification time.

### 3. Mid-season swing tier sensibility + basis-caption visibility
**Test:** Open a team with a sharp mid-season performance swing; check early-event tiers read sensibly given they rank against the season-final field, and confirm the basis caption is visible.
**Expected:** A reader is not misled into reading an early tier as an as-of-that-event rank.
**Why human:** This is the resolution point for the plan's judgment-tier prohibition — deliberately not an automated gate.

All three items are carried forward from plan 06.1-07's own deferred `<human-check>` blocks
(`workflow.human_verify_mode: end-of-phase`) and were confirmed still open by reading that plan's
own SUMMARY ("Next Phase Readiness" section) rather than assumed resolved.

### Gaps Summary

One genuine, measurable gap: `packages/harness/payloadBudget.test.ts` fails on the `teams/{year}`
page kind's ceiling. This verification **independently re-derived** (not merely accepted) the
SUMMARY's attribution to two pre-06.1 Phase-6 commits (`06f468ad`, `bf1e3228`, both dated
2026-08-26, both tagged `feat(06)`) by reading `buildTeamsArtifact`/`buildEventArtifact` directly
and confirming neither references any of this phase's own new fields — the added bytes are
exclusively the pre-existing `tier` field those two commits added, first swept into a live
republish by this phase's own plan 06.1-07 (which was, correctly, the phase's one authorized
republish and could not skip carrying pre-existing committed-but-unpublished work). This phase's
own artifact (`team/{teamKey}/{year}`) is comfortably under its own ceiling. The gap is honestly
tracked (`.planning/WINDOWS.md` ledger #11, status `open`), the ceiling was correctly *not* raised
to force a green test, and none of the ROADMAP's four stated Success Criteria for this phase
reference the `teams/{year}` ceiling. This is reported as a gap per this project's own standard
("a phase that passes verification while carrying an unmet truth is worse than one that reports
the gap") rather than silently absorbed — but it is not a regression this phase's own code
introduced, and it does not block phase 06.1's own stated goal, all four of whose Success Criteria
are independently confirmed VERIFIED above.

**This looks like an intentional, already-documented deviation.** To accept it and close this gap
without further action, add to this file's frontmatter:

```yaml
overrides:
  - must_have: "Every page kind's measured maximum stays at or under its committed payload-budget ceiling"
    reason: "teams/{year}'s overage is caused entirely by pre-06.1 Phase-6 commits (06f468ad, bf1e3228) first swept into a live republish by this phase's required single republish; this phase's own team/{teamKey}/{year} artifact is well under its ceiling; tracked at .planning/WINDOWS.md ledger #11 for a separate developer decision"
    accepted_by: "<your name>"
    accepted_at: "<ISO timestamp>"
```

Three additional human-check items (bonus-dot legibility, TBA standings cross-check, mid-season
tier/basis-caption sensibility) remain genuinely open per the phase's own end-of-phase
human-verify workflow and should be resolved at `/gsd-verify-work` before this phase is considered
fully closed, independent of the payload-budget gap above.

---

_Verified: 2026-08-27T01:08:22Z_
_Verifier: Claude (gsd-verifier)_
