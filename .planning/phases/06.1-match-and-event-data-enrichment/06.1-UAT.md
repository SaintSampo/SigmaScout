---
status: diagnosed
phase: 06.1-match-and-event-data-enrichment
source: 06.1-01-SUMMARY.md, 06.1-02-SUMMARY.md, 06.1-03-SUMMARY.md, 06.1-04-SUMMARY.md, 06.1-05-SUMMARY.md, 06.1-06-SUMMARY.md, 06.1-07-SUMMARY.md
started: 2026-08-27T01:54:24Z
updated: 2026-08-27T02:45:27Z
---

## Current Test

[testing complete]

## Tests
### 1. [06.1-01 D1] event_rankings corpus table + ingest pipeline (--rankings-only, fetchEventRankings, normalizeEventRankings) populates real 2024 TBA rankings
expected: event_rankings corpus table + ingest pipeline (--rankings-only, fetchEventRankings, normalizeEventRankings) populates real 2024 TBA rankings, idempotently, with null-body/empty/populated cases distinguished at the layer where the distinction is actionable
result: pass
source: automated
coverage_id: D1
plan: 06.1-01
verification:
  - unit: packages/ingest/rankings.test.ts (11 cases)
  - integration: pnpm ingest:rankings --year 2024 (live run: 280/324 events populated, 0 rows with rank<1 or total_teams<1, unchanged row count on --force re-run)

### 2. [06.1-01 D2] TeamSeasonEventSchema.rank/.totalTeams published from event_rankings via buildTeamSeasonArtifact, omitted entirely (not merely undefined) wh
expected: TeamSeasonEventSchema.rank/.totalTeams published from event_rankings via buildTeamSeasonArtifact, omitted entirely (not merely undefined) when absent
result: pass
source: automated
coverage_id: D2
plan: 06.1-01
verification:
  - unit: packages/harness/publish.test.ts#buildTeamSeasonArtifact — TEAM-04/F-06-3 event rank (plan 06.1-01 Task 3)
  - integration: packages/harness/eventRank.tracer.test.ts (corpus-backed, deterministic pair, exact equality against a real 2024 event_rankings row)

### 3. [06.1-01 D3] EventSection renders 'Rank N of M' only when both rank and totalTeams are present; a half-present pair, or an entirely absent pair, renders 
expected: EventSection renders 'Rank N of M' only when both rank and totalTeams are present; a half-present pair, or an entirely absent pair, renders no standing element; the standing element sits inside the same paragraph as startDate, which is unaffected
result: pass
source: automated
coverage_id: D3
plan: 06.1-01
verification:
  - unit: apps/web/src/components/team/EventSection.test.tsx (5 new cases: populated, absent, rank-only, totalTeams-only, placement)

### 4. [06.1-02 D1] rpPmfForMatch tallies per-bonus achievement from the existing Monte Carlo draws, exposed as RpPmfResult.redBonusProbabilities/blueBonusProba
expected: rpPmfForMatch tallies per-bonus achievement from the existing Monte Carlo draws, exposed as RpPmfResult.redBonusProbabilities/blueBonusProbabilities, positionally aligned to ruleModule.bonusNames, omitted entirely on both short-circuit branches
result: pass
source: automated
coverage_id: D1
plan: 06.1-02
verification:
  - unit: packages/core/algorithms/sigma1/rp/distribution.test.ts — 'rpPmfForMatch — per-bonus probabilities (plan 06.1-02 Task 1, F-06-1)' and 'rpPmfForMatch — fixed-seed golden pmf' describe blocks

### 5. [06.1-02 D2] Prediction.redBonusRp/blueBonusRp surfaced by Sigma1's predict() for qualification matches at RP-eligible event types, absent entirely other
expected: Prediction.redBonusRp/blueBonusRp surfaced by Sigma1's predict() for qualification matches at RP-eligible event types, absent entirely otherwise, absent for OPR/EPA, with pRedWin/redScore/blueScore left numerically untouched
result: pass
source: automated
coverage_id: D2
plan: 06.1-02
verification:
  - unit: packages/core/algorithms/sigma1/rp/distribution.test.ts — 'Prediction.redBonusRp / blueBonusRp (plan 06.1-02 Task 2, F-06-1)' describe block

### 6. [06.1-02 D3] The D-15 prediction-stream digest gate is measured passing (not skipped) with this plan's changes in place, and the pipeline-side positional
expected: The D-15 prediction-stream digest gate is measured passing (not skipped) with this plan's changes in place, and the pipeline-side positional-length invariant is pinned per registered season
result: pass
source: automated
coverage_id: D3
plan: 06.1-02
verification:
  - unit: packages/harness/digest.test.ts — 3 passing tests, 0 skipped, both committed sigma1@2.0.0 versions reproduced
  - unit: packages/core/algorithms/sigma1/rp/distribution.test.ts — 'rpPmfForMatch — per-bonus array length matches bonusNames for every registered season (plan 06.1-02 Task 3)'

### 7. [06.1-03 D1] percentileAgainstSortedPool ranks a value from an earlier point in the season against the SEASON-FINAL pool (D-06.1-A), agreeing exactly wit
expected: percentileAgainstSortedPool ranks a value from an earlier point in the season against the SEASON-FINAL pool (D-06.1-A), agreeing exactly with percentileRanks on every value the pool contains, and throws a named error on an empty pool rather than returning 0
result: pass
source: automated
coverage_id: D1
plan: 06.1-03
verification:
  - unit: packages/harness/percentiles.test.ts#percentileAgainstSortedPool (D-06.1-A, plan 06.1-03 Task 1)

### 8. [06.1-03 D2] sortedPoolsByMetric builds one ascending pool per metric name, scoped strictly to teamKeys, omitting a metric name entirely when no team in 
expected: sortedPoolsByMetric builds one ascending pool per metric name, scoped strictly to teamKeys, omitting a metric name entirely when no team in the pool has it (PD-07)
result: pass
source: automated
coverage_id: D2
plan: 06.1-03
verification:
  - unit: packages/harness/percentiles.test.ts#sortedPoolsByMetric (D-06.1-A, plan 06.1-03 Task 1)

### 9. [06.1-03 D3] HISTORY_PERCENTILE_METRIC_KEYS is single-sourced from core (COMPONENT_GROUP_METRIC_KEYS + TOTAL_METRIC_KEY), never a re-declared literal
expected: HISTORY_PERCENTILE_METRIC_KEYS is single-sourced from core (COMPONENT_GROUP_METRIC_KEYS + TOTAL_METRIC_KEY), never a re-declared literal
result: pass
source: automated
coverage_id: D3
plan: 06.1-03
verification:
  - unit: packages/harness/percentiles.test.ts#HISTORY_PERCENTILE_METRIC_KEYS (PD-06, plan 06.1-03 Task 1)

### 10. [06.1-03 D4] MetricValueSchema.percentile is optional and bounded [0,100]; a pre-phase artifact with no percentile key still parses; MetricHistoryRowSche
expected: MetricValueSchema.percentile is optional and bounded [0,100]; a pre-phase artifact with no percentile key still parses; MetricHistoryRowSchema round-trips a row carrying percentiles; the browser import graph stays clean
result: pass
source: automated
coverage_id: D4
plan: 06.1-03
verification:
  - unit: packages/harness/percentiles.test.ts#MetricValueSchema.percentile (F-06-3, plan 06.1-03 Task 2)
  - unit: packages/harness/browserSafeSchemas.test.ts#metricHistorySchema.ts specifically carries zero Node-only imports

### 11. [06.1-04 D1] COVERAGE.md — the rankings endpoint's full capability matrix (8 request-level rows, 13 response-type/field rows), every OPT-OUT carrying a n
expected: COVERAGE.md — the rankings endpoint's full capability matrix (8 request-level rows, 13 response-type/field rows), every OPT-OUT carrying a non-empty reason, offseason-inclusion settled as an explicit row
result: pass
source: automated
coverage_id: D1
plan: 06.1-04
verification:
  - unit: Task 1's automated verify command (awk over every OPT-OUT row's reason cell) — exit 0

### 12. [06.1-04 D2] event_rankings populated for all five seasons 2022-2026 via a real pnpm ingest:rankings --years 2022-2026 run, verified by fresh read-only c
expected: event_rankings populated for all five seasons 2022-2026 via a real pnpm ingest:rankings --years 2022-2026 run, verified by fresh read-only corpus queries independent of the run's own in-memory tally
result: pass
source: automated
coverage_id: D2
plan: 06.1-04
verification:
  - integration: pnpm ingest:rankings --years 2022-2026 (live run 12f17740-e573-4459-bb43-46b7c4b4a193: 1,582 requests, 253.8s, exit 0) + read-only corpus queries: 47,695 rows / 1,322 populated events across 5 seasons, 0 rows with rank<1 OR total_teams<1, 368 offseason events with rows
  - unit: pnpm vitest run packages/harness/eventRank.tracer.test.ts packages/ingest/rankings.test.ts (14 tests)
  - integration: pnpm test (full suite, 104 files / 1359 tests, incl. digest.test.ts corpus-gated reproducibility gate, not skipped)

### 13. [06.1-05 D1] TeamSeasonMatchSchema gains four optional fields (redBonusRp/blueBonusRp bounded [0,1] arrays; actualRedBonusRp/actualBlueBonusRp nullable b
expected: TeamSeasonMatchSchema gains four optional fields (redBonusRp/blueBonusRp bounded [0,1] arrays; actualRedBonusRp/actualBlueBonusRp nullable boolean arrays) with three refines (non-empty when present, equal predicted lengths, equal non-null actual lengths); every existing pageArtifacts.test.ts case still passes unchanged; no new import added to pageArtifacts.ts; browserSafeSchemas.test.ts still passes
result: pass
source: automated
coverage_id: D1
plan: 06.1-05
verification:
  - unit: packages/harness/pageArtifacts.test.ts — 'TeamSeasonMatchSchema — predicted/actual per-bonus RP fields (Phase 06.1, plan 06.1-05 Task 1)' (11 cases)
  - unit: packages/harness/browserSafeSchemas.test.ts (6 tests, unchanged, still passing)

### 14. [06.1-05 D2] actualBonusFlagsForSeason computes the algorithm-independent actual per-bonus flag map once per season, using the exact predicates sigma1/in
expected: actualBonusFlagsForSeason computes the algorithm-independent actual per-bonus flag map once per season, using the exact predicates sigma1/index.ts's update() uses for its own RP-fold skip; publishes null (never undefined) for RP-ineligible event type / no score breakdown / a breakdown that throws on parse (caught, not propagated); returns an empty map for a season with no registered RP rule module
result: pass
source: automated
coverage_id: D2
plan: 06.1-05
verification:
  - unit: packages/harness/publish.test.ts — 'actualBonusFlagsForSeason (Phase 06.1, plan 06.1-05 Task 2, F-06-3/PD-09)' (6 cases, including two real-shaped 2022/2024 raw-breakdown parses cross-checked against each season's own rule module)

### 15. [06.1-05 D3] redBonusRp/blueBonusRp published rounded exactly once at ROUNDING_RULE.probability (never through roundPmf); actualRedBonusRp/actualBlueBonu
expected: redBonusRp/blueBonusRp published rounded exactly once at ROUNDING_RULE.probability (never through roundPmf); actualRedBonusRp/actualBlueBonusRp conditionally spread only inside a played match's row, so absence/null/array stay three distinct published states; the same match built under two different algorithm ids carries identical actual bonus arrays
result: pass
source: automated
coverage_id: D3
plan: 06.1-05
verification:
  - unit: packages/harness/publish.test.ts — 'buildTeamSeasonArtifact — predicted/actual per-bonus RP fields (Phase 06.1, plan 06.1-05 Task 2, F-06-1/F-06-3)' (7 cases, including algorithm-independence)

### 16. [06.1-05 D4] withHistoryPercentiles attaches a percentile to an allowlisted metric only when a season-final pool exists for it, never mutates input, pres
expected: withHistoryPercentiles attaches a percentile to an allowlisted metric only when a season-final pool exists for it, never mutates input, preserves row order, and gives equal percentiles to equal values regardless of array position; sortedPools is built exactly once per (algorithm, season), immediately after metricsByTeamWithPercentiles, from the same teamsThisSeason membership withPercentiles uses
result: pass
source: automated
coverage_id: D4
plan: 06.1-05
verification:
  - unit: packages/harness/publish.test.ts — 'withHistoryPercentiles (Phase 06.1, plan 06.1-05 Task 3, D-06.1-A)' (5 synthetic cases)

### 17. [06.1-05 D5] Against the real 2022 corpus (14,677 matches, 3,062 teams), for every team whose last metricHistory row's total value equals its season-fina
expected: Against the real 2022 corpus (14,677 matches, 3,062 teams), for every team whose last metricHistory row's total value equals its season-final total value, withHistoryPercentiles's row percentile equals the season-final percentile exactly — asserted with a non-vacuity floor of 50 teams, not skipped
result: pass
source: automated
coverage_id: D5
plan: 06.1-05
verification:
  - integration: packages/harness/publish.test.ts — 'withHistoryPercentiles — real-corpus season-final agreement invariant (Phase 06.1, plan 06.1-05 Task 3, D-06.1-A)' — filtered-set size 3,062 (floor 50), 0 mismatches, corpus-gated (existsSync guard, no silent skip), ran (not skipped) on this machine

### 18. [06.1-06 D1] bonusStatesFromProbabilities/bonusStatesFromFlags/bonusDotLabel map the three published per-bonus states (a probability, a boolean, absence)
expected: bonusStatesFromProbabilities/bonusStatesFromFlags/bonusDotLabel map the three published per-bonus states (a probability, a boolean, absence) to exactly one BonusRpState, with the 0.5 threshold boundary and the null-is-not-missed rule each pinned by a dedicated test case
result: pass
source: automated
coverage_id: D1
plan: 06.1-06
verification:
  - unit: apps/web/src/lib/bonusRp.test.ts (29 tests: bonusStatesFromProbabilities, bonusStatesFromFlags, bonusDotLabel, PREDICTED_BONUS_THRESHOLD describe blocks)

### 19. [06.1-06 D2] Every bonus dot on a played qualification match row with published predicted/actual arrays resolves to earned or missed — none unknown — acr
expected: Every bonus dot on a played qualification match row with published predicted/actual arrays resolves to earned or missed — none unknown — across a two-bonus (2024) and a three-bonus (2025) season; absent data (null/undefined arrays) still renders unknown, never hollow; each alliance reads only its own side's arrays
result: pass
source: automated
coverage_id: D2
plan: 06.1-06
verification:
  - unit: apps/web/src/components/team/MatchTable.test.tsx — 'real bonus-RP dot states (plan 06.1-06, F-06-1)' describe block (5 cases)

### 20. [06.1-06 D3] The per-event metric line's tiles carry a rarity tier sourced from the history row's own percentile (never season-final data, which this com
expected: The per-event metric line's tiles carry a rarity tier sourced from the history row's own percentile (never season-final data, which this component never receives); a visible basis caption (event-tier-basis-{eventKey}) renders only when a rendered tile carries a percentile, with visible text naming season-final and a title stating where the team stood at that point
result: pass
source: automated
coverage_id: D3
plan: 06.1-06
verification:
  - unit: apps/web/src/components/team/EventSection.test.tsx — 'per-event metric tiers and basis caption (plan 06.1-06, D-06.1-A/F-06-3)' describe block (6 cases)

### 21. [06.1-07 D1] Dry-run projections for all five seasons against sigma1 confirm the per-team artifact's post-phase maximum stays under the 375,000-byte ceil
expected: Dry-run projections for all five seasons against sigma1 confirm the per-team artifact's post-phase maximum stays under the 375,000-byte ceiling before the real republish is spent; 2026 (not 2024) is the new projected maximum, since three-bonus seasons cost more per row than 2024's two
result: pass
source: automated
coverage_id: D1
plan: 06.1-07
verification:
  - integration: Five real dry-run publish invocations (packages/harness/publish.ts --dry-run), one per season, sigma1, all exit 0 -- projections recorded in docs/publish-budget.md's pre-run projection section
  - unit: pnpm vitest run packages/harness/payloadBudget.test.ts (10/10 pass, against the unmodified committed doc)

### 22. [06.1-07 D2] One full republish across 2022-2026, all three algorithms, exits 0, lands on the prior run's exact object count (54,671 page objects + 2 man
expected: One full republish across 2022-2026, all three algorithms, exits 0, lands on the prior run's exact object count (54,671 page objects + 2 manifests), and a fetched-back real team artifact carries a per-bonus array, an allowlisted history-row percentile, and an event rank/total
result: pass
source: automated
coverage_id: D2
plan: 06.1-07
verification:
  - integration: pnpm publish:seasons live run (generation bbe1552e-0091-40cf-b70c-cf4296ebcf63, 2026-08-27T00:01:39Z-00:24:31Z, exit 0, objects=54671 totalBytes=2714525205)
  - integration: curl fetch of v1/team/frc118/2026/sigma1@2.0.0+tuned-2026-08.json over HTTPS (https://data.sigmascout.org, 340,569 bytes, matches the run's own reported max) -- parsed and confirmed to carry event 2026txcle rank 4/32, match 2026txcle_qm2's predicted+actual per-bonus arrays, and metricHistory row percentiles on total/phaseAuto/phaseTeleop

### 23. [06.1-07 D3] The D-15 digest gate is measured passing, not assumed, with this republish's changes in place; git diff --stat data/algorithm-versions/ is e
expected: The D-15 digest gate is measured passing, not assumed, with this republish's changes in place; git diff --stat data/algorithm-versions/ is empty
result: pass
source: automated
coverage_id: D3
plan: 06.1-07
verification:
  - unit: pnpm vitest run packages/harness/digest.test.ts -- 3 tests, 0 skipped, both committed sigma1@2.0.0 versions reproduced bitwise
  - integration: git diff --stat data/algorithm-versions/ -- confirmed empty

### 24. [06.1-07 D4] docs/publish-budget.md's Latest run paragraph, payload budget table, Team-page delta, Compressed figures, Storage/write-volume, and machine-
expected: docs/publish-budget.md's Latest run paragraph, payload budget table, Team-page delta, Compressed figures, Storage/write-volume, and machine-readable block all carry this run's real measured figures, with no budgetMaxBytes ceiling line touched
result: pass
source: automated
coverage_id: D4
plan: 06.1-07
verification:
  - integration: git diff HEAD~2 -- docs/publish-budget.md -- confirmed zero changed lines matching a \"budgetMaxBytes\": <number> JSON pattern
  - unit: pnpm vitest run packages/harness/payloadBudget.test.ts -- 9/10 pass; the one failure (teams internal-consistency) is a real, investigated, honestly-recorded finding, not a parser or non-vacuity failure

### 25. Auto-coverage confirmation
expected: |
  All 24 deliverables across the 7 plans are deterministically covered by passing tests (listed in the summary presented in chat). Confirm the coverage claim looks right — in particular that nothing you expected to be hand-checked was auto-passed instead.
result: pass

### 26. Bonus-dot legibility (2024 and 2026 team pages)
expected: |
  On the Overview tab of a 2024 team page and a 2026 team page, the per-bonus RP dots on played qualification match rows are unambiguous to a first-time scout WITHOUT hovering: earned vs missed is distinguishable at rest, and 2024's two-dot rows vs 2026's three-dot rows both read cleanly.
  
  Suggested targets (local dev, live data):
    2024: http://localhost:5177/team/118?year=2024&algorithm=sigma1
    2026: http://localhost:5177/team/1114?year=2026&algorithm=sigma1
  
  Note: the "predicted vs actual on an UPCOMING row" half of this check is not testable today — no 2026 event has a published match schedule yet (next event 2026azscor starts 2026-08-28), so every published match row is played. Say so if that half matters to you and we'll record it as blocked.
result: issue
reported: "on the teams page, playoff matches do not have RP. All playoff matches should have RP greyed out"
severity: major

### 27. Standings cross-check against The Blue Alliance
expected: |
  The published "Rank N of M" on the Overview tab matches TBA's own rankings page for the same team+event, across three different seasons:
  
    1. frc118 @ 2022lake  -> expect rank 2 of 41
       https://www.thebluealliance.com/event/2022lake#rankings
    2. frc1114 @ 2023onwat -> expect rank 1 of 30
       https://www.thebluealliance.com/event/2023onwat#rankings
    3. frc33 @ 2025micmp1  -> expect rank 7 of 40
       https://www.thebluealliance.com/event/2025micmp1#rankings
  
  Pages: http://localhost:5177/team/118?year=2022&algorithm=sigma1 (etc.)
result: pass

### 28. Mid-season swing tier sensibility + basis-caption visibility
expected: |
  Open a team with a sharp mid-season performance swing and check that the early-event rarity tiers read sensibly given they rank against the SEASON-FINAL field, and that the basis caption explaining that is actually visible — a reader must not be misled into reading an early tier as an as-of-that-event rank.
  
  Suggested target: frc217, 2024 — its total-metric percentile falls from 79.3 early to 46.5 by season end (65-point swing across 30 history rows), the sharpest in the sample.
    http://localhost:5177/team/217?year=2024&algorithm=sigma1
  
  Cross-check on the History tab too: http://localhost:5177/team/217?year=2024&algorithm=sigma1&tab=history
result: pass

## Summary

total: 28
passed: 27
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-06.1-26
  truth: "Bonus-RP dots assert a real per-bonus fact only where bonus RP exists — i.e. qualification matches; playoff rows must not render earned/missed dots"
  status: failed
  reason: "User reported: on the teams page, playoff matches do not have RP. All playoff matches should have RP greyed out"
  severity: major
  test: 26
  root_cause: "buildTeamSeasonArtifact publishes actualRedBonusRp/actualBlueBonusRp for EVERY played match, not only compLevel === 'qm'. Confirmed against live published artifacts: frc118/2024 carries actual arrays on all 23 sf and 13 f matches (e.g. 2024txkat_sf2m1 act=[true,false]); frc1114/2026 on all 12 sf and 11 f (e.g. 2026onham_f1m1 act=[true,true,false]). Predicted arrays are already correctly gated to qual matches (0 on every sf/f row), so the predicted and actual sides disagree on the same gating rule. actualBonusFlagsForSeason derives flags from each match's raw score_breakdown, where the bonus fields exist structurally on playoff matches but carry no ranking-point meaning — FRC awards bonus RP in qualification play only. MatchTable's ActualScoreLine then calls bonusStatesFromFlags on those arrays, yielding earned/missed dots rather than the muted/dashed 'unknown' style bonusRp.ts already defines for 'the fact is not derivable'."
  artifacts:
    - path: "packages/harness/publish.ts"
      issue: "actualBonusFlagsForSeason / buildTeamSeasonArtifact publish actual per-bonus flags for playoff matches; the qual-only gate applied to the predicted side is missing on the actual side"
    - path: "apps/web/src/components/team/MatchTable.tsx"
      issue: "ActualScoreLine renders BonusRpDots for every compLevel; no guard renders playoff rows in the muted 'unknown' state"
    - path: "apps/web/src/lib/bonusRp.ts"
      issue: "BonusRpState already defines the muted/dashed 'unknown' style needed for the greyed-out playoff rendering — the state exists, it is simply never reached on playoff rows"
  missing:
    - "Gate actual per-bonus flag publication to compLevel === 'qm' in buildTeamSeasonArtifact, matching the predicted side's existing gate"
    - "Render playoff match rows' bonus dots in the muted/greyed 'unknown' state (or omit them) so no playoff row asserts earned/missed"
    - "Republish affected artifacts, or have MatchTable ignore actual arrays on non-qm rows so already-published artifacts render correctly without a republish"
    - "Add a MatchTable test case and a publish.test.ts case pinning 'no earned/missed dot on a playoff row'"
  debug_session: ""

- gap_id: G-06.1-28
  truth: "The per-event metric line does not carry the visible 'vs season-final field' basis caption"
  status: failed
  reason: "User requested during test 28: get rid of this text: vs season-final field"
  severity: cosmetic
  test: 28
  root_cause: "Not a defect — a deliberate design change requested by the user at UAT. EventSection.tsx:131-138 renders the caption inside a hasTieredTile guard. The tiers themselves passed the sensibility check (test 28 = pass); the user judged the always-visible caption to be clutter."
  artifacts:
    - path: "apps/web/src/components/team/EventSection.tsx"
      issue: "lines 131-138 render the visible 'vs season-final field' span; removing it may leave hasTieredTile unused"
    - path: "apps/web/src/components/team/EventSection.test.tsx"
      issue: "two assertions query data-testid event-tier-basis-2024casj and will fail once the element is removed"
  missing:
    - "Remove the visible 'vs season-final field' span from EventSection.tsx"
    - "Remove or repoint the two EventSection.test.tsx assertions on data-testid=event-tier-basis-*"
    - "Drop hasTieredTile if it becomes unused after the span is removed"
    - "Decide where the season-final basis is still explained: after this removal the only remaining explanation is the hover-only title tooltip, which plan 06.1-07's judgment-tier prohibition was specifically concerned about — confirm hover-only is acceptable or move the explanation elsewhere"
  debug_session: ""
