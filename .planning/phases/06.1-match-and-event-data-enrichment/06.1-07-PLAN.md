---
phase: 06.1-match-and-event-data-enrichment
plan: 07
type: execute
wave: 5
depends_on: [06.1-04, 06.1-05, 06.1-06]
files_modified:
  - docs/publish-budget.md
autonomous: true
requirements: [TEAM-04, F-06-1, F-06-3, SC-4]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "packages/harness/digest.test.ts RUNS rather than skips during this phase's verification: data/corpus.sqlite is present on this machine, so a skipped digest suite is a failure signal and the verification asserts a non-zero passing-test count for that file rather than a zero-failure exit code."
    - "The prediction stream's order is the walk-forward chronological order the simulator produces; this phase adds no match, removes none, and reorders none, which digest.test.ts's own corpus-versus-fixture deep-equality assertion proves independently of the digest hash itself."
    - "Exactly one full artifact republish is spent for the whole phase, covering 2022 through 2026 across all three algorithms in a single run — the sequencing reason this phase exists as one phase rather than three."
    - "The republished object count lands on the prior run's count exactly, confirming this phase's new fields changed bytes rather than object count — the same verification pattern docs/publish-budget.md already records for Phase 6's own republish."
    - "Every page kind's measured maximum stays at or under its committed budget ceiling, and no ceiling is raised to accommodate a measurement."
    - "docs/publish-budget.md's machine-readable budget block and its prose tables carry the same numbers after this plan, since packages/harness/payloadBudget.test.ts parses that block as its only input and would go green against a stale or internally inconsistent record."
  artifacts:
    - docs/publish-budget.md
  key_links:
    - "the dry-run projection in Task 1 -> the go/no-go on spending the republish (measuring after the run is 22 minutes too late to change a schema decision)"
    - "docs/publish-budget.md's machine-readable block -> packages/harness/payloadBudget.test.ts (the single source; two files carrying these numbers would be two files that drift)"
    - "the published artifacts in R2 -> the deployed site's team pages (the only place the three items become visible to a real reader, and the only place the manual checks can run)"
  prohibitions:
    - statement: "MUST NOT regenerate, relax, or hand-edit a committed algorithm-version digest, and MUST NOT raise a committed payload budget ceiling, in order to make a failing gate pass — a failing gate is a finding about this phase's changes, not a fixture to refresh."
      status: unverified
      verification: test
    - statement: "MUST NOT record a projected, interpolated, or partial-run figure in docs/publish-budget.md as though it were measured — every number in that document names the run that produced it, and a number without a real run behind it is a claim this project's own failure log is about."
      status: unverified
      verification: judgment
---

<objective>
Spend the phase's one full artifact republish, measure it, and prove the gates still hold.

Purpose: all three of this phase's items ride the same republish — that sequencing fact is why the
ROADMAP made this one phase instead of three. The last full run measured 21 minutes 39 seconds of
wall clock across 54,673 objects, so spending three would cost an hour for no gain. This plan
projects the byte cost first, spends the run once, records the measurement, and confirms the two
standing gates (payload budget and digest reproducibility) are still green with the phase's changes
in place.

Output: published artifacts carrying per-bonus RP, event standings and per-event percentiles for
2022–2026 across all three algorithms; an updated `docs/publish-budget.md` whose numbers all name
the run that produced them; and a measured, not-assumed, D-15 digest gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-match-and-event-data-enrichment/06.1-VALIDATION.md
@.planning/phases/06.1-match-and-event-data-enrichment/06.1-05-SUMMARY.md
@.planning/phases/06.1-match-and-event-data-enrichment/06.1-06-SUMMARY.md
</context>

## Artifacts this phase produces

**This plan creates no new source symbol.** It updates `docs/publish-budget.md` in place — its prose
payload table, its team-page delta section, its storage and write-volume table, and its
machine-readable budget block. Plan 06.1-01's created-symbol table lists the phase's full set of new
symbols.

<planner_decisions>
- **PD-15 — The byte-cost mitigation ladder is pre-approved, in order, and stops short of raising a
  ceiling.** `docs/publish-budget.md` records 70,138 bytes (18.70%) of headroom on the
  budget-critical team artifact. This phase's projected delta is roughly 36,000 bytes of per-bonus
  arrays plus roughly 21,000 bytes of history-row percentiles plus a few hundred bytes of event
  standings, which fits — but "fits" is a projection until Task 1 measures it. If the measurement
  lands over the ceiling, apply in order: **(1)** narrow the history-row percentile attachment to
  the last row per team per event, which is the only row the per-event line reads and which removes
  roughly 97% of that field's cost; **(2)** publish the predicted per-bonus probabilities at three
  decimals instead of four, via a new named entry in the rounding rule rather than an inline
  literal; **(3)** STOP and escalate to the developer with the measurement — do not proceed.
  Raising a committed ceiling to fit a measurement is prohibited by this plan's own
  `must_haves.prohibitions`, and `docs/publish-budget.md` already records that Phase 6 was given
  the same explicit instruction. **Reversibility: costly** — each rung is a published-shape change
  that would need another republish, which is exactly why the projection runs before the run.
- **PD-16 — The manual verifications are `<human-check>` items on this plan's tasks, not blocking
  checkpoints.** This project's `workflow.human_verify_mode` is `end-of-phase`, so the three
  manual-only rows in `06.1-VALIDATION.md` ride the verify blocks and are collected at
  `/gsd-verify-work` rather than halting the wave. **Reversibility: reversible.**
</planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Project the byte cost before spending the run</name>
  <files>docs/publish-budget.md</files>
  <read_first>
    - `docs/publish-budget.md` — the whole file: the payload budget table, the team-page delta section from Phase 6, the storage and write-volume table, the note that the machine-readable block is the single source for these numbers, and the explicit instruction Phase 6 was given not to raise a budget to fit a measurement
    - `packages/harness/payloadBudget.test.ts` — the parser, the fields it reads from the machine-readable block, the internal-consistency checks it makes, and the non-vacuity floor it asserts
    - `packages/harness/publish.ts` lines 575–620 and 1290–1310 — the dry-run uploader's behaviour and the per-page-kind size summary the run prints
    - `packages/harness/publish.ts` lines 1340–1370 — the seasons-range parsing, so the projection slice is specified correctly
    - `packages/harness/percentiles.ts` — the history-row percentile allowlist and its recorded byte reasoning, since narrowing it further is mitigation rung one
    - `packages/harness/rounding.ts` — the rounding rule table, since adding a coarser entry is mitigation rung two
  </read_first>
  <action>
Run a dry-run publish over the slice most likely to breach: the season and algorithm that produced
the recorded maximum. Use the publish CLI's dry-run mode with the seasons flag set to 2024 and the
algorithm flag set to sigma1, so nothing is uploaded and the per-page-kind size summary still
prints. This is the same projection path plan 06-04 used, whose 304,862-byte figure then matched the
real run exactly, because both exercise the identical assembly code over identical underlying data.

**Secrets.** The publish CLI runs through `tsx --env-file=.env`. Never read, print, echo or
interpolate `.env`, the TBA key, or the R2 credential pair into any command, log line, document, or
commit message. This rule exists because it was broken in Phase 4 and a live R2 token had to be
rotated; the git-side protections all passed while the secret escaped anyway, so a green
`scripts/secrets-boundary.test.ts` is not evidence that secrets were handled correctly.

Compare the printed maximum for the per-team page kind against the committed ceiling of 375,000
bytes recorded in the machine-readable budget block. Record the projection, the ceiling, the
resulting headroom, and the delta against the recorded 304,862-byte baseline.

Then widen the projection to confirm no other season is worse: repeat the dry run for the remaining
four seasons against sigma1, one at a time, and record each season's per-team maximum. Seasons with
three bonus ranking points carry more per-bonus bytes per match row than 2024's two, so the 2024
artifact being the historical maximum does not by itself guarantee it is still the maximum after
this phase — measure rather than assume.

**If any season's projection exceeds the ceiling**, apply PD-15's ladder in order and re-project
after each rung. Do not proceed to Task 2 with a projection over the ceiling, and do not raise the
ceiling. If rung three is reached, stop and surface the measurement.

Record the full projection table in the SUMMARY. Do not edit `docs/publish-budget.md`'s numbers in
this task — the document records measured runs, and a projection is not one (this plan's second
prohibition). The only edit permitted here is appending a short projection note clearly labelled as
a pre-run projection with its own date, kept separate from the measured tables.
  </action>
  <verify>
    <automated>pnpm vitest run packages/harness/payloadBudget.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A dry-run publish completes for each of the five seasons against sigma1 and exits 0, with the per-page-kind size summary captured for each.
    - The SUMMARY carries a five-row projection table: season, projected per-team maximum bytes, the 375,000-byte ceiling, and the resulting headroom.
    - Every projected per-team maximum is at or under 375,000 bytes, OR the SUMMARY records which mitigation rung was applied and shows the re-projection that followed it.
    - `packages/harness/payloadBudget.test.ts` passes — its own re-measurement slice now exercises the widened assembly path.
    - The machine-readable budget block's ceiling values are unchanged: `git diff docs/publish-budget.md` shows no modification to any line containing a budget ceiling key.
    - No command, log, document or commit message produced by this task contains a credential value from `.env`.
  </acceptance_criteria>
  <done>The per-team artifact's post-phase size is measured against the committed ceiling for every season before any republish is spent, with a recorded mitigation applied if any season was over.</done>
</task>

<task type="auto">
  <name>Task 2: The one full republish</name>
  <files>docs/publish-budget.md</files>
  <read_first>
    - `docs/publish-budget.md` § "Latest run" — the exact figures and framing Phase 6's republish recorded, including its object-count-unchanged verification and its per-page-kind byte-identical claim
    - `packages/harness/publish.ts` lines 1–20 — the CLI usage block, the seasons flag, and the concurrency and bucket flags
    - `package.json` — the `publish:seasons` script's exact definition
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-05-SUMMARY.md` and `06.1-06-SUMMARY.md` — which fields landed, so the delta attribution in Task 3 names the right causes
    - `.planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md` — D-24 (the publish runs locally against the local corpus) and D-25 (the browser reads from an R2 custom domain with no compute in the path)
  </read_first>
  <action>
Run the full artifact republish across 2022–2026 for all three algorithms in one pass, using the
existing `publish:seasons` script exactly as Phase 6's own authorized republish did. Budget 20–30
minutes of wall clock; the last full run measured 21 minutes 39 seconds producing 54,671 page
objects plus 2 manifests.

**Secrets.** Same constraint as Task 1: the run reaches its credentials only through
`tsx --env-file=.env`. Never read, print, echo or interpolate that file or any value from it.

Capture from the run: the generation stamp, start and end timestamps, wall-clock elapsed, the object
count, the total byte count, and the full per-page-kind size table the summary prints.

Then verify the run rather than trusting it:
- The object count must land on the prior run's 54,671 page objects plus 2 manifests. A different
  count means this phase changed which objects exist, which it should not have — every change is a
  field on an existing object.
- Every page kind other than the per-team page should be byte-identical to the prior run, since this
  phase's fields all land on `TeamSeasonArtifactSchema` alone. The events, event, teams and compare
  artifacts are untouched. A moved byte count on any of those is a finding, not a rounding
  difference — investigate before recording it.
- Fetch one republished team artifact back over HTTPS from the read domain and confirm it parses and
  carries the phase's new fields: a per-bonus array on a played qualification match row, a percentile
  on an allowlisted history-row metric, and a rank and total on an event that has one.

If the run fails partway, re-run it. The publish path is idempotent by key, so a resumed run
overwrites rather than duplicating. Do not record a partial run's figures as the measurement.
  </action>
  <verify>
    <automated>pnpm vitest run packages/harness/digest.test.ts packages/harness/payloadBudget.test.ts</automated>
    <human-check>Open a 2024 team page and a 2026 team page on the deployed site. On a played qualification match row, confirm the bonus dots read clearly as solid or hollow at rest and that the meaning is not ambiguous to a scout seeing the page for the first time; confirm an upcoming row's predicted dots read as predictions rather than as results. This is a perception question about legibility against the rarity-tier palette, which no unit test can answer.</human-check>
    <human-check>Pick three team-and-event pairs across different seasons and compare each event card's standing against that event's rankings page on The Blue Alliance. The published number should be the standing a user would find there. This cross-checks the published artifact against the third-party source of truth, which the test suite deliberately does not reach out to.</human-check>
  </verify>
  <acceptance_criteria>
    - The republish exits 0 and its summary reports 54,673 total objects (54,671 page objects plus 2 manifests) — the same count as the prior run.
    - The per-page-kind table shows a changed byte count for the per-team page kind and unchanged byte counts for the teams, events, event and compare kinds; any other kind moving is investigated and explained in the SUMMARY before proceeding.
    - The per-team page kind's measured maximum is at or under 375,000 bytes.
    - One republished team artifact fetched back over HTTPS parses and is shown to carry a per-bonus array, an allowlisted history-row percentile, and an event rank with its total.
    - `pnpm vitest run packages/harness/digest.test.ts` reports at least one passing test and 0 skipped.
    - `git diff --stat data/algorithm-versions/` is empty.
    - No command, log, document or commit message produced by this task contains a credential value from `.env`.
  </acceptance_criteria>
  <done>One full republish is spent, its object count matches the prior run exactly, only the per-team artifact's bytes moved, and a fetched-back artifact demonstrably carries all three of this phase's additions.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measurement and close the gates</name>
  <files>docs/publish-budget.md</files>
  <read_first>
    - `docs/publish-budget.md` — the whole file again, now with Task 2's figures in hand: the payload budget table, the "Latest run" paragraph, the team-page delta section, the compressed-figures table, the storage and write-volume table, and the machine-readable budget block at the bottom
    - `packages/harness/payloadBudget.test.ts` — the parser's exact field expectations and its internal-consistency checks, so the block's edits satisfy them
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-05-SUMMARY.md` — which fields landed on the per-team artifact, for the delta attribution
    - `packages/harness/percentiles.ts` — the history-row percentile allowlist's own recorded byte reasoning, so the document and the code comment agree on the same measurement
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-01-PLAN.md` § edge probe reconciliation — the phase's probe accounting, referenced by the SUMMARY
  </read_first>
  <action>
Update `docs/publish-budget.md` with Task 2's measured figures, following the document's own
established shape and its rule that every number names the run that produced it.

Replace the "Latest run" paragraph with this phase's run: the date, the start and end timestamps,
the wall-clock elapsed, the object count, the total payload bytes, and one sentence stating what
this republish carried live for the first time — per-bonus predicted probabilities and actual flags
on the match row, event rank and total on the event record, and season-final-ranked percentiles on
the allowlisted history-row metrics.

Update the payload budget table's measured columns for the per-team page kind and leave every other
kind's row at its prior values, with a sentence stating why those rows did not move — this phase's
fields land on the per-team artifact alone.

Add a delta section in the shape of Phase 6's own: the prior 304,862-byte maximum, the new maximum,
the absolute and percentage increase, and an attribution naming the three field groups. State the
remaining headroom under the 375,000-byte ceiling in bytes and percent. If Task 1's mitigation
ladder was applied, say which rung and why, so the narrower allowlist or the coarser rounding is a
recorded decision rather than an unexplained difference from the plan.

Update the storage and write-volume table with the run's byte count and its share of the monthly
Class-A allowance, keeping the prior run's row for comparison as the document already does.

Update the machine-readable budget block so it agrees with the prose exactly — measured columns
only. **Do not change any ceiling value.** That block is the sole input to
`packages/harness/payloadBudget.test.ts`, so an internally inconsistent edit is caught there, but a
raised ceiling would pass and would be the exact failure this plan's first prohibition names.

Leave the manual dashboard read-back open, as the document already does — a human with Cloudflare
account access records those figures, and this run has no dashboard access and must not fabricate
the cross-check.

Finally, run the full gate set and record the results in the SUMMARY: the full test suite across
both vitest projects, the payload budget suite, and the digest suite with its passing-test count
stated explicitly so a reader can see it ran rather than skipped.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm vitest run packages/harness/payloadBudget.test.ts packages/harness/digest.test.ts</automated>
    <human-check>Open a team whose performance changed sharply mid-season. Confirm the early events' rarity tiers read sensibly given that the tiers rank against the season-final field, and confirm the basis caption is visible on the per-event metric line so the reader is told what the colour means. The risk this check guards against is a reader's wrong inference, not a wrong number, which is why it cannot be a unit test.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm test` passes across both vitest projects with 0 unexpected skips.
    - `pnpm vitest run packages/harness/payloadBudget.test.ts` passes against the updated document.
    - `pnpm vitest run packages/harness/digest.test.ts` reports at least one passing test and 0 skipped, and the SUMMARY states the passing-test count.
    - `docs/publish-budget.md`'s "Latest run" paragraph names this phase's run date, its wall clock, and its object count, and names the three field groups it carried.
    - The machine-readable block's measured values for the per-team page kind match the prose table's values for the same kind exactly.
    - Every budget ceiling value in the machine-readable block is byte-identical to its pre-plan value: `git diff docs/publish-budget.md` shows no modification to any ceiling line.
    - The document contains no figure introduced by this plan that is not attributable to a named run — verified by reading the diff.
    - `git log --oneline` shows one commit per task.
  </acceptance_criteria>
  <done>`docs/publish-budget.md` records this phase's real measured republish, both standing gates are green with the digest suite confirmed to have run, and no ceiling was moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell / agent transcript → `.env` | this plan's runs need both the TBA key and the R2 credential pair; Phase 4 leaked the R2 pair into a transcript through exactly this boundary |
| the local machine → R2 → every reader | one command replaces 51,693 published artifacts; a defect here is live for every visitor at once |
| a measured figure → `docs/publish-budget.md` → `payloadBudget.test.ts` | the document is a gate's only input, so a wrong number there disables the gate rather than failing it |
| a failing gate → the temptation to move the gate | the project's own failure log is about quiet inaccuracy nobody measured |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-03 | Information Disclosure | `.env`, `publish:seasons` | high | mitigate | Both runs reach credentials only through `tsx --env-file=.env`. No step reads, prints, echoes or interpolates the file or any value from it; acceptance criteria assert the absence on both tasks. Recorded explicitly because the git-side protections passed during the Phase 4 incident while the secret escaped through the transcript anyway. |
| T-06.1-27 | Tampering | committed budget ceilings and digests | high | mitigate | Raising a ceiling or refreshing a digest to make a gate pass is this plan's first prohibition, and the acceptance criteria assert byte-identical ceiling lines and an empty diff on the committed version files. Task 1 projects the cost BEFORE the run so a schema decision can still change; PD-15's ladder ends in stop-and-escalate rather than in a ceiling move. |
| T-06.1-28 | Repudiation | `docs/publish-budget.md` figures | high | mitigate | Every number names the run that produced it, and a projection is recorded separately from a measurement and labelled as such (this plan's second prohibition). A partial or interrupted run's figures are never recorded; the run is repeated instead. The Cloudflare dashboard read-back stays explicitly open rather than being fabricated. |
| T-06.1-29 | Tampering | the republished artifact set | high | mitigate | The object count is asserted against the prior run's exact count, and every page kind other than the per-team page is asserted byte-identical — so a change that silently added, dropped, or rewrote an unrelated artifact fails the task rather than shipping. One republished artifact is fetched back over HTTPS and shown to carry all three additions. |
| T-06.1-07 | Tampering | prediction-stream digests | high | mitigate | `digest.test.ts` is required to report a non-zero passing-test count rather than merely a zero exit code, because a silent skip and a pass are indistinguishable by exit code and the corpus is present on this machine. |
| T-06.1-30 | Denial of Service | R2 free-tier Class-A operations | low | accept | One full republish spends 54,673 Class-A operations, about 5.47% of one month's free allowance — measured, recorded, and unchanged by this phase since the object count is unchanged. Spending three republishes instead of one is what this phase's single-run sequencing avoids. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package install runs in this plan. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm test` passes across both vitest projects with 0 unexpected skips.
- `pnpm vitest run packages/harness/digest.test.ts` reports a non-zero passing-test count and 0 skipped.
- `pnpm vitest run packages/harness/payloadBudget.test.ts` passes against the updated document.
- `git diff --stat data/algorithm-versions/` is empty; no budget ceiling line in `docs/publish-budget.md` was modified.
- The three `<human-check>` items are collected at `/gsd-verify-work` per this project's end-of-phase human-verify mode.
</verification>

<success_criteria>
- All three of this phase's items are live in the published artifacts for 2022–2026 across all three algorithms, from exactly one republish.
- The per-team artifact's measured maximum stays inside its committed ceiling, with the projection taken before the run rather than after.
- `docs/publish-budget.md` records the run, and every figure in it names the run that produced it.
- The D-15 digest gate is measured passing, not assumed.
</success_criteria>

<output>
Create `.planning/phases/06.1-match-and-event-data-enrichment/06.1-07-SUMMARY.md` when done.
</output>
