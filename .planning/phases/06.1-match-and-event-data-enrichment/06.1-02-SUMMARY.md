---
phase: 06.1-match-and-event-data-enrichment
plan: 02
subsystem: prediction-model
tags: [sigma1, monte-carlo, ranking-points, typescript, vitest]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: event standing display groundwork and phase worktree-disabled setup
provides:
  - "RpPmfResult.redBonusProbabilities / blueBonusProbabilities — per-bonus marginal probabilities tallied from the existing Monte Carlo draws, positionally aligned to RpRuleModule.bonusNames"
  - "Prediction.redBonusRp / blueBonusRp — the published form of the above, surfaced by Sigma1's predict()"
  - "A measured (not assumed) proof that the D-15 prediction-stream digest did not move"
affects: [06.1-03, 06.1-04, 06.1-05, apps/web/src/lib/bonusRp.ts consumers]

# Actuals (#2632)
actuals:
  tokens: 4482
  tasks: 3
  commits: 5

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-bonus tally reads bonusFlags off predictThresholds' already-computed result inside the existing per-draw loop -- zero new draws, zero new RNG stream"
    - "Positional array (not Record<string, number>) for wire-format-adjacent per-bonus data, indexed against RpRuleModule.bonusNames, matching the existing redPmf/redRpPmf 'omitted entirely, never fabricated zero' convention"

key-files:
  created: []
  modified:
    - packages/core/algorithms/sigma1/rp/distribution.ts
    - packages/core/algorithms/sigma1/rp/distribution.test.ts
    - packages/core/algorithms/types.ts
    - packages/core/algorithms/sigma1/index.ts

key-decisions:
  - "PD-04 (plan-level, applied as written): per-bonus data travels as a positional array aligned to bonusNames, not a Record<string, number> -- RESEARCH.md/PATTERNS.md sketched a record, this plan's PLAN.md deliberately overrode it for consumer fit (BonusRpDots already indexes positionally) and payload budget (~33KB saved on the budget-critical frc118/2024/sigma1 artifact)"
  - "PD-05 (plan-level, applied as written): the per-bonus marginals are never passed through roundPmf -- they are independent per-bonus probabilities, not a distribution that sums to 1"
  - "Both RpPmfResult short-circuit branches (non-qm compLevel, zero rpMonteCarloDraws) and Prediction's RP-ineligible-event-type fallback all omit redBonusRp/blueBonusRp entirely -- absence represents unknown, never a fabricated all-zero claim"

patterns-established:
  - "New RpPmfResult fields tallied via one extra counter-array pass INSIDE the existing per-draw loop, never a second loop or second draw sequence -- the discipline any future per-draw aggregate on this Monte Carlo core should follow"

requirements-completed: [F-06-1, SC-4]

coverage:
  - id: D1
    description: "rpPmfForMatch tallies per-bonus achievement from the existing Monte Carlo draws, exposed as RpPmfResult.redBonusProbabilities/blueBonusProbabilities, positionally aligned to ruleModule.bonusNames, omitted entirely on both short-circuit branches"
    requirement: F-06-1
    verification:
      - kind: unit
        ref: "packages/core/algorithms/sigma1/rp/distribution.test.ts — 'rpPmfForMatch — per-bonus probabilities (plan 06.1-02 Task 1, F-06-1)' and 'rpPmfForMatch — fixed-seed golden pmf' describe blocks"
        status: pass
    human_judgment: false
  - id: D2
    description: "Prediction.redBonusRp/blueBonusRp surfaced by Sigma1's predict() for qualification matches at RP-eligible event types, absent entirely otherwise, absent for OPR/EPA, with pRedWin/redScore/blueScore left numerically untouched"
    requirement: F-06-1
    verification:
      - kind: unit
        ref: "packages/core/algorithms/sigma1/rp/distribution.test.ts — 'Prediction.redBonusRp / blueBonusRp (plan 06.1-02 Task 2, F-06-1)' describe block"
        status: pass
    human_judgment: false
  - id: D3
    description: "The D-15 prediction-stream digest gate is measured passing (not skipped) with this plan's changes in place, and the pipeline-side positional-length invariant is pinned per registered season"
    requirement: SC-4
    verification:
      - kind: unit
        ref: "packages/harness/digest.test.ts — 3 passing tests, 0 skipped, both committed sigma1@2.0.0 versions reproduced"
        status: pass
      - kind: unit
        ref: "packages/core/algorithms/sigma1/rp/distribution.test.ts — 'rpPmfForMatch — per-bonus array length matches bonusNames for every registered season (plan 06.1-02 Task 3)'"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-26
status: complete
---

# Phase 06.1 Plan 02: Predicted Per-Bonus RP Marginals Summary

**`rp/distribution.ts`'s Monte Carlo now tallies per-bonus achievement it was already computing and discarding, surfaced on `Prediction` as `redBonusRp`/`blueBonusRp` — measured to leave the committed D-15 digest bitwise unchanged.**

## Performance

- **Duration:** 25 min
- **Tasks:** 3 completed
- **Files modified:** 4 (`rp/distribution.ts`, `rp/distribution.test.ts`, `types.ts`, `sigma1/index.ts`)

## Accomplishments

- `RpPmfResult` widened with optional `redBonusProbabilities`/`blueBonusProbabilities`, tallied inside the existing per-draw Monte Carlo loop from `predictThresholds`' already-computed `bonusFlags` — no new draws, no second RNG stream, no second loop.
- A fixed-seed golden pmf, recorded from the pre-widening production code before any edit, proves the draw sequence is byte-identical after the change (`redPmf: [0.0055, 0, 0.9945, 0, 0]`, `bluePmf: [0.9945, 0, 0.0055, 0, 0]` for the recorded fixture).
- `Prediction` widened with `redBonusRp`/`blueBonusRp`, spread onto Sigma1's returned prediction using the exact same defined-and-non-empty presence test the existing `redRpPmf`/`blueRpPmf` spreads already use — no new convention invented.
- The D-15 reproducibility gate (`packages/harness/digest.test.ts`) was run for real against the live corpus and MEASURED passing (3 tests, 0 skipped) — both committed `sigma1@2.0.0+tracer-check.json` and `sigma1@2.0.0+tuned-2026-08.json` reproduced their committed digest bitwise with this plan's changes in place.
- A new per-season test pins the positional-length invariant (`redBonusProbabilities.length === bonusNames.length`) across all of `RP_REGISTERED_SEASONS`, so registering a future season extends the assertion automatically.

## Task Commits

Each task was committed atomically (TDD RED → GREEN pairs for Tasks 1 and 2):

1. **Task 1: Tally per-bonus achievement from the existing draws**
   - `dc970d6b` (test) — failing tests + fixed-seed golden recorded before the production edit
   - `23b44a93` (feat) — widened `RpPmfResult`/`rpPmfForMatch`
2. **Task 2: Surface the marginals on `Prediction`**
   - `160e8c68` (test) — failing tests for `Prediction.redBonusRp`/`blueBonusRp`
   - `6b3bb908` (feat) — widened `types.ts`'s `Prediction` and `sigma1/index.ts`'s `predict()`
3. **Task 3: Measure the digest gate rather than assume it**
   - `e0a2374a` (test) — ran `digest.test.ts` for real (3 passing, 0 skipped) and added the per-season length invariant test

_TDD RED commits intentionally include only test-file changes and fail against the pre-edit production code; each is immediately followed by the matching GREEN commit._

## Files Created/Modified

- `packages/core/algorithms/sigma1/rp/distribution.ts` — `RpPmfResult` widened with `redBonusProbabilities`/`blueBonusProbabilities`; `rpPmfForMatch`'s per-draw loop gained a per-bonus counter tally read by name from `ruleModule.bonusNames`
- `packages/core/algorithms/sigma1/rp/distribution.test.ts` — fixed-seed golden pmf case; per-bonus presence/absence/determinism/single-bonus-identity cases; `Prediction.redBonusRp`/`blueBonusRp` presence/absence/OPR-EPA-exclusion/regression cases; per-season bonus-array-length case
- `packages/core/algorithms/types.ts` — `Prediction.redBonusRp`/`blueBonusRp` optional fields, documented as per-bonus marginals distinct from `redRpPmf`
- `packages/core/algorithms/sigma1/index.ts` — `predict()`'s returned prediction gained two conditional spreads for the bonus arrays, reusing the existing pmf-spread presence convention; nothing above the `rpResult` assignment was touched

## Decisions Made

- Followed PD-04 and PD-05 exactly as recorded in the PLAN.md frontmatter (positional array over `Record<string, number>`; never through `roundPmf`) — no new decisions required during execution.
- No deviation from the plan's task structure; all `read_first` context (constants.ts, rules.ts, PATTERNS.md's widening sketch) matched the live code, so the widening applied directly with only the array-vs-record shape corrected per the plan's own explicit override of RESEARCH.md/PATTERNS.md.

## Deviations from Plan

None — plan executed exactly as written, including the PLAN's explicit override of RESEARCH.md/PATTERNS.md's `Record<string, number>` sketch in favor of the positional array (PD-04).

## Issues Encountered

None. The golden pmf value used to seed the pre-edit regression test was computed via a temporary, uncommitted scratch script run against the untouched production code, then deleted before any edit — no scratch file was left in the working tree or committed.

## Threat Flags

None — this plan's changes stay entirely downstream of `pRedWin`/`redScore`/`blueScore` (T-06.1-07/T-06.1-08 mitigations verified: digest gate measured passing, golden pmf proved the draw sequence unchanged) and read bonus flags by name from a fixed `bonusNames` array rather than spreading a parsed record (T-06.1-09).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `RpPmfResult`/`Prediction` now carry the predicted per-bonus data plan 06.1-05 (publish boundary) needs to round and ship to the client.
- The positional-array contract's pipeline-side half is now pinned (`distribution.test.ts`'s per-season length case) alongside the already-passing client-side half (`apps/web/src/lib/bonusRp.test.ts`).
- No blockers for downstream 06.1 plans.

---
*Phase: 06.1-match-and-event-data-enrichment*
*Completed: 2026-08-26*

## Self-Check: PASSED

All 4 modified files exist on disk; all 5 task commits (`dc970d6b`, `23b44a93`, `160e8c68`, `6b3bb908`, `e0a2374a`) verified present in `git log --oneline --all`.
