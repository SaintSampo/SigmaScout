---
phase: 06.1-match-and-event-data-enrichment
plan: 04
type: execute
wave: 2
depends_on: [06.1-01]
files_modified:
  - .planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md
autonomous: true
requirements: [TEAM-04]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every capability of TBA's /event/{event_key}/rankings endpoint and of its response types carries an explicit INTEGRATE or OPT-OUT decision in COVERAGE.md, and every OPT-OUT carries a non-empty one-line reason — an opt-out with an empty reason cell is an undecided hole, not a decision."
    - "COVERAGE.md opens with the same explanation Phase 6's COVERAGE.md carries: the api-coverage detector returned detected false over this phase's scope, that result is a false negative, and the matrix is written anyway because the phase genuinely adds one new third-party endpoint to packages/ingest/tbaClient.ts."
    - "COVERAGE.md settles RESEARCH.md's Open Question 1 as its own explicit row — whether offseason events are in the event_rankings ingest scope — with a decision and a reason, rather than leaving it recorded only as an assumption."
    - "A full 2022-2026 rankings ingest completes and its measured cost is recorded in COVERAGE.md with the exact command, the date, the total request count, and the split between populated, null-body and empty-rankings events — figures that can be checked against a real run rather than merely believed."
    - "The corpus holds event_rankings rows for every season 2022 through 2026, and every stored row satisfies rank at least 1 and total_teams at least 1, verified by a read-only query rather than by the ingest's own in-memory tally."
  artifacts:
    - .planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md
  key_links:
    - "COVERAGE.md's offseason row -> ingestSeasonRankingsOnly's event iteration scope -> which event cards on a team page can carry a standing at all (553 of 1,581 corpus events are offseason)"
    - "COVERAGE.md's presence and well-formedness -> the seal-time api-coverage.verify-pre gate (a malformed or partial matrix blocks the phase seal)"
---

<objective>
Widen the rankings ingest from the tracer's single season to all of 2022–2026, and record the
endpoint's capability surface as a committed COVERAGE.md.

Purpose: two things that belong together. The capability matrix decides what this project takes from
a newly-integrated third-party endpoint and what it deliberately leaves; the full ingest run
produces the measured cost figures that matrix should carry. Phase 6's COVERAGE.md set the precedent
of recording real measured request counts alongside the decisions, and this follows it.

Output: `event_rankings` populated for all five seasons, and a committed two-table capability matrix
the seal-time `api-coverage.verify-pre` gate can validate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-team-pages/COVERAGE.md
@.planning/phases/06.1-match-and-event-data-enrichment/06.1-RESEARCH.md
@.planning/phases/06.1-match-and-event-data-enrichment/06.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

**This plan creates:**

| Kind | Name | Location |
|---|---|---|
| file | `COVERAGE.md` | `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` |

No source symbol is created by this plan. Plan 06.1-01's created-symbol table lists the phase's full
set.

<planner_decisions>
- **PD-01 (restated, decided in plan 06.1-01) — offseason events are IN scope.** This plan records
  it as COVERAGE.md's explicit row rather than re-deciding it, so the ingest already written in
  06.1-01 and the matrix written here cannot disagree. **Reversibility: reversible.**
- **PD-08 — The matrix decides the rankings endpoint's surface only.** TBA as a whole was
  integrated in Phase 1 and the media endpoint in Phase 6; each of those phases owns its own
  decisions. Restating them here would create a second, drift-prone record of the same choice.
  **Reversibility: reversible.**
</planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: The rankings endpoint's capability matrix</name>
  <files>.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/06-team-pages/COVERAGE.md` — the whole file: its title line, its "Why this file exists despite a detector result" opening, its scope note, its two-table shape (request-level, then response-type/field-level), its reason-cell style, its bracketed notes convention, and its closing measured-cost section. This is the format to follow exactly.
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-RESEARCH.md` § Code Examples (the live-confirmed response shape and its field list), § Common Pitfalls 3 and 5, § Assumptions Log entries A1–A4, and § Open Questions 1 — the proposed rows, to be re-decided from a full-coverage default rather than copied
    - `packages/ingest/tbaClient.ts` — the eight capabilities its header records as INTEGRATE from Phase 1, plus `fetchEventRankings` as added in plan 06.1-01
    - `packages/ingest/schemas.ts` — `tbaEventRankingSchema` and `tbaEventRankingsResponseSchema` as written in plan 06.1-01, so every field the schema models has a matrix row and no field is decided twice
    - `packages/core/algorithms/opr.ts` — confirm this project computes its own OPR, which is the reason the endpoint's sibling OPR capability is opted out
  </read_first>
  <action>
Write `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` following Phase 6's
COVERAGE.md format exactly.

**Title and opening.** Title it for The Blue Alliance API v3 and the rankings path. Carry the same
"Full coverage by default. Opt-outs are explicit, reasoned decisions." blockquote. Then a section
explaining why the file exists despite a `detected: false` detector result: the api-coverage
detector was run over this phase's scope and returned no signals, that result is a false negative
and the same one Phase 6 recorded, and the phase genuinely adds one new third-party endpoint to
`packages/ingest/tbaClient.ts`. Then the scope note: TBA as a whole was integrated in Phase 1 and
the media endpoint in Phase 6; this matrix decides the capability surface of the rankings endpoint
and its response types only (PD-08).

**Table one — request-level capabilities.** Default INTEGRATE; the matrix is the subtraction record;
every OPT-OUT gets a one-line reason and an INTEGRATE row leaves the reason cell empty. Decide each
of these, re-deriving from a full-coverage default rather than accepting RESEARCH.md's proposals as
given: the rankings GET itself; conditional requests via `If-None-Match` producing a 304;
`Cache-Control` header respect; **offseason events in ingest scope** (the row that settles
RESEARCH.md Open Question 1 — INTEGRATE per PD-01, with the reason naming TEAM-04's unscoped
"attended event" wording, the 553-of-1,581 offseason share of the corpus, TBA computing these
rankings itself from official match results rather than from self-reported score breakdowns, and
the roughly one-minute cost delta); and each sibling event-scoped endpoint a reader might expect
this phase to have taken while it was there — district points, TBA's own OPRs, alliances, and event
insights.

**Table two — response-type and field-level capabilities.** One row per field the response carries,
since each is a separate decision with a separate consumer question. Cover at minimum: `rank`;
`team_key`; the `rankings` array length as the source of the standing's total; the top-level `null`
body as a distinct recorded outcome; the empty `rankings` array as a distinct recorded outcome;
`record`; `matches_played`; `dq`; `qual_average`; `sort_orders`; `extra_stats`; `sort_order_info`;
and `extra_stats_info`. Reasons must be specific to this project — for the two TBA-computed
aggregates a reader might expect us to take, say plainly that this project computes its own and that
ingesting a second differently-computed one is the two-implementations-that-drift anti-pattern
`.planning/research/ARCHITECTURE.md` names; for the season-varying sort-order metadata, say that the
names genuinely differ by season (four in 2022, five in 2024, three in 2026) and that "Rank N of M"
needs none of them.

Add bracketed notes below the tables for any row whose reason needs more than a line, following
Phase 6's `[1]`/`[2]` convention — in particular the null-body versus empty-array distinction and
why it is preserved in the ingest counters rather than in the corpus (PD-02 from plan 06.1-01).

Leave the measured-cost section as a placeholder heading; Task 2 fills it from a real run.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md &amp;&amp; awk '/^\|/ &amp;&amp; /OPT-OUT/ {n=split($0,c,"|"); r=c[n-1]; gsub(/^[ \t]+|[ \t]+$/,"",r); if (r=="") {print "UNDECIDED OPT-OUT ROW: " $0; bad=1}} END {exit bad?1:0}' .planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md` exists and contains exactly two capability tables, each with the three columns `capability`, `decision`, `reason`.
    - Every row's decision cell is one of the two allowed values; the verify command exits 0, proving no OPT-OUT row has an empty reason cell.
    - The opening section explains the `detected: false` false negative and names `packages/ingest/tbaClient.ts` as the file the new endpoint was added to.
    - The scope note states that Phase 1 owns TBA's overall integration and Phase 6 owns the media endpoint, so this matrix is not a second record of either.
    - A row whose capability cell names offseason events exists in the request-level table, carries a decision, and carries a reason of at least 100 characters citing the offseason share of the corpus.
    - The response-type table has at least 13 rows and covers every field `tbaEventRankingSchema` models — cross-checked by reading that schema and confirming no modelled field lacks a row.
    - `grep -c 'INTEGRATE' COVERAGE.md` is at least 5 and `grep -c 'OPT-OUT' COVERAGE.md` is at least 10, confirming the matrix is a real subtraction record rather than a blanket approval.
  </acceptance_criteria>
  <done>The rankings endpoint's full capability surface is decided row by row, every subtraction carries a reason, and RESEARCH.md's one open question is settled as a committed decision rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the ingest to 2022–2026 and record its measured cost</name>
  <files>.planning/phases/06.1-match-and-event-data-enrichment/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/06-team-pages/COVERAGE.md` § "The recurring cost this integration commits to" — the exact shape of the measured-cost section: the throttle note, the measured request count with the run that produced it, the observed per-request timing range, and the budget guidance the numbers support
    - `packages/ingest/cli.ts` — `ingestSeasonRankingsOnly` and its four per-season counters, as written in plan 06.1-01
    - `packages/ingest/tbaClient.ts` — `THROTTLE_INTERVAL_MS` and `TbaRequestCounter`, the source of the per-request floor the cost projection rests on
    - `packages/corpus/db.ts` — `openCorpusReadOnly`, for the verification queries
    - `.planning/phases/06.1-match-and-event-data-enrichment/06.1-01-SUMMARY.md` — the 2024 run's own measured counts, so the five-season figure can be checked against it
  </read_first>
  <action>
Run the full-scope rankings ingest across all five seasons with
`pnpm ingest:rankings --years 2022-2026`. 2024 is already ingested from plan 06.1-01, so its events
will mostly return 304 cache hits — that is expected and is itself a useful signal that the ETag
path works. Expect roughly 1,581 requests total at the existing unconditional 100 ms throttle floor,
so budget 4–10 minutes of wall clock; the measured per-request time on this machine has historically
run 18–117% above the floor depending on live network conditions, so record the real elapsed time
rather than the projection.

**Secrets.** The run uses `pnpm ingest:rankings`, which is `tsx --env-file=.env` under the hood.
Never read, print, echo or interpolate `.env` or the TBA key into any command, log line, document,
or commit message. If the run needs to be inspected, inspect its stdout, which the client is
constructed never to put the key into.

Capture from the run: total elapsed wall clock, the total request count from `TbaRequestCounter`,
the cache-hit count, and the per-season split of populated, null-body and empty-rankings events.

Then verify the corpus independently rather than trusting the run's own tally — an interrupted and
resumed run mixes cache hits with fresh writes, so only a fresh read of stored rows is accurate
(this is exactly the correction Phase 6's media ingest already documents for itself). Query, read
only: distinct event count per season in `event_rankings` joined to `events`; total row count; and
the count of rows violating the positive-integer invariant, which must be zero.

Finally, append the measured-cost section to `COVERAGE.md`, following Phase 6's format: the exact
command, the date, total requests, cache hits, elapsed wall clock, per-season event counts, the
populated versus null-body versus empty-rankings split, and a budget guidance line a future reader
can plan against. Also record how the two absent cases divide in reality — this is the first real
measurement of how often TBA returns a null body versus an empty array, and it is the evidence that
the distinction PD-02 preserves is a real one rather than a theoretical one.

If the run is interrupted, re-run it — the upsert is idempotent and the ETag cache makes a resumed
run cheap. Do not record a partial run's numbers as the measurement.
  </action>
  <verify>
    <automated>pnpm vitest run packages/harness/eventRank.tracer.test.ts packages/ingest/rankings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm ingest:rankings --years 2022-2026` exits 0 and prints a summary line per season carrying all four counters.
    - A read-only corpus query confirms every season 2022 through 2026 has at least 250 distinct events with rows in `event_rankings`.
    - A read-only corpus query confirms `SELECT COUNT(*) FROM event_rankings WHERE rank < 1 OR total_teams < 1` is exactly 0.
    - A read-only corpus query confirms at least one offseason event (an `events` row with `is_offseason = 1`) has rows in `event_rankings`, proving the offseason INTEGRATE decision is in force rather than merely documented.
    - `COVERAGE.md`'s measured-cost section names the exact command, the run date, the total request count, the cache-hit count, and the elapsed wall clock — each a real figure from this run, none projected.
    - `COVERAGE.md` records the populated / null-body / empty-rankings split across all five seasons as three separate numbers.
    - No document, log line, or commit message produced by this task contains the value of `TBA_API_KEY`.
    - `pnpm test` passes across both vitest projects.
  </acceptance_criteria>
  <done>`event_rankings` holds real data for all five seasons including offseason events, verified by read-only query rather than by the ingest's own tally, and COVERAGE.md carries the run's measured cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBA API v3 → `packages/ingest` | 1,581 third-party responses cross here in one run, including the two absent shapes |
| developer shell / agent transcript → `.env` | the run needs the live TBA key |
| a capability decision left undecided → the phase seal | a partial matrix blocks the seal-time gate, and worse, hides a capability nobody chose |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-03 | Information Disclosure | `.env`, `pnpm ingest:rankings` | high | mitigate | The run reaches the key only through `tsx --env-file=.env` and `process.env`; `tbaClient.ts`'s header records that it never logs the key nor embeds it in a returned value. No task step reads, prints or interpolates `.env`. Acceptance criteria assert the absence explicitly. |
| T-06.1-16 | Repudiation | the ingest's own in-memory tally | high | mitigate | Corpus state is verified by a fresh read-only query rather than by the run's counters, because an interrupted-and-resumed run mixes 304 cache hits with fresh writes and its own tally under-reports — the exact correction Phase 6's media ingest already documents. |
| T-06.1-17 | Repudiation | an OPT-OUT with no reason | high | mitigate | The verify command mechanically fails the task if any OPT-OUT row has an empty reason cell. An unreasoned opt-out is an undecided hole wearing a decision's clothes, and the seal-time gate would pass it. |
| T-06.1-05 | Denial of Service | TBA API politeness | low | accept | The run reuses `THROTTLE_INTERVAL_MS` 100 ms unconditional spacing and `TbaRequestCounter`; 1,581 requests is an order of magnitude below Phase 6's 17,231-request media pass, which this service already absorbed. One politeness policy across all ingest paths, no new one introduced. |
| T-06.1-18 | Tampering | offseason ranking trustworthiness | medium | accept | RESEARCH.md assumption A3 — that TBA's ranking computation for an offseason event carries the same trust profile as an in-season one — rests on one live sample plus the structural argument that TBA computes rankings from official match results rather than from self-reported score breakdowns. Accepted with the reasoning recorded in COVERAGE.md's offseason row; the failure mode is a less reliable standing on an offseason event card, not a wrong prediction. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package install runs in this plan. |
</threat_model>

<verification>
- `pnpm test` passes across both vitest projects with 0 unexpected skips.
- The verify command in Task 1 exits 0 against the final `COVERAGE.md`.
- Read-only corpus queries confirm five seasons populated, zero invariant violations, and at least one offseason event carrying rows.
- `git log --oneline` shows one commit per task; the source tree is untouched by this plan.
</verification>

<success_criteria>
- Every capability of the rankings endpoint and its response types is an explicit, reasoned decision in a committed matrix.
- RESEARCH.md's one open question is settled as a row, not carried forward as an assumption.
- `event_rankings` holds real 2022–2026 data, including offseason events, verified independently of the ingest's own tally.
- The matrix carries measured cost figures a future reader can check against a real run.
</success_criteria>

<output>
Create `.planning/phases/06.1-match-and-event-data-enrichment/06.1-04-SUMMARY.md` when done.
</output>
