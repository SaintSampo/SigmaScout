---
phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures
fixed_at: 2026-08-21T20:11:13Z
review_path: .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-REVIEW.md
iteration: 1
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 03.2: Code Review Fix Report

**Fixed at:** 2026-08-21T20:11:13Z
**Source review:** .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 2 (WR-01, WR-02 — Info findings IN-01/IN-02/IN-03 explicitly out of scope per orchestrator instruction)
- Fixed: 2
- Skipped: 0

**Environment:** worked directly in the main checkout on `main` (no worktree isolation, per orchestrator instruction). Tree started clean at `f53315d5`. All verification (typecheck, full test suite) ran in that same main checkout, so the numbers below are reproducible from the tree as committed.

## Fixed Issues

### WR-01: `poolSlices`'s "exact, bit-for-bit" pooling reconstruction is not actually exact, and the reconciliation gate depends on it

**Files modified:** `packages/harness/eventScopeDiagnostic.ts`
**Commit:** `fd748f89`

**What changed:** Took the PREFERRED fix from the three options the reviewer offered — eliminated the lossy `(x*n)/n` round-trip on the path the reconciliation gate actually exercises, rather than loosening `buildReconciliationEntry`'s strict `===` to a tolerance. `poolSlices` now short-circuits when given exactly one slice (the single-season case `buildReconciliationEntry` pools every time, since `poolPredictions` groups by algorithm+season before calling `poolSlices`) and returns that slice's own `brierScore`/`winnerAccuracy`/`scoredCount` unchanged, with no arithmetic performed on them at all. For two-or-more slices (the real multi-season pooling case used by the accuracy-bucket and warm-cut aggregations), the original weighted-mean-of-group-means reconstruction is unchanged — it is mathematically exact in real-number arithmetic but not guaranteed bit-identical to a direct pooled computation, and no caller currently needs that guarantee for the multi-slice case.

Also corrected the doc comment (previously lines 145-153) that claimed the reconstruction is "exact, not an approximation ... bit-for-bit-equivalent" unconditionally. The comment now states the actual guarantee precisely: bit-for-bit exact for the single-slice case (the one that matters for the strict-equality gate), and an exact-in-real-arithmetic-but-not-bit-identical weighted mean for the multi-slice case, with the empirical ~12% mismatch rate from the reviewer's own repro cited as the reason a caller needing `===` must only ever pool a single slice.

**Why this approach:** The reviewer's guidance explicitly preferred this over loosening the gate's equality — it "keeps the strict `===` and the loud-failure discipline fully intact ... while removing the floating-point fragility entirely." Fixing it in `poolSlices` itself (rather than special-casing inside `buildReconciliationEntry`) benefits every current and future caller that happens to pool a single slice, not just the reconciliation path, and the fix is purely subtractive (removes arithmetic) so it cannot introduce a new numeric discrepancy.

**Frozen-data impact:** None. This is a source-code-only change; it was not run against the corpus or `reports/`, and no file under `data/baselines/` or `data/diagnostics/` was touched (verified: `git diff f53315d5 HEAD -- data/baselines/... data/diagnostics/...` is empty). The five already-committed reconciliation results in `data/diagnostics/opr-event-scope-2026-08.json` happened to round-trip cleanly before this fix (per the review), and the fix returns the identical underlying value for the single-slice case (same slice, now returned directly instead of round-tripped) — so this change is behavior-preserving with respect to every number already published.

### WR-02: `poolSlices`/`buildReconciliationEntry` (and related pooling functions) have no direct unit tests

**Files modified:** `packages/harness/eventScopeDiagnostic.ts`, `packages/harness/eventScopeDiagnostic.test.ts`
**Commit:** `235681c4`

**What changed:** Added a synthetic-fixture unit suite in `eventScopeDiagnostic.test.ts` covering exactly what the fix guidance specified:
- `poolSlices`: a single-slice pool returns the slice's own values unchanged — deliberately using `brierScore: 0.1, scoredCount: 3` (a value that provably mismatches under the old `(x*n)/n` round-trip: `(0.1*3)/3 !== 0.1` in IEEE 754 double precision, asserted directly in the test as a runtime sanity check before the real assertion). This is the exact round-trip fragility WR-01 identified, now under a regression test so it cannot silently reappear.
- Multi-slice pooling (2 synthetic slices with known counts/scores) asserts the weighted-mean identity for both `brierScore` and `winnerAccuracy`.
- A slice with `brierScore: null` contributes nothing to the pooled Brier numerator (population count still included, numerator does not).
- A slice with `winnerAccuracy: null` is excluded from the accuracy denominator entirely (not just from the numerator).
- `buildReconciliationEntry`: `matches: true` for a synthetic identical-population case and `matches: false` for a synthetic mismatch (mismatched `scoredCount`).

`buildReconciliationEntry` was previously a private (unexported) function; exported it so the test file can call it directly, matching the pattern already used for `poolSlices`, `assertWarmCutPartition`, `computeWarmCut`, etc. All fixtures are synthetic object literals — no corpus access, no `reports/` dependency, fully deterministic.

**Why this approach:** Followed the fix guidance precisely (poolSlices weighted-mean + null-handling cases, buildReconciliationEntry true/false cases) rather than expanding scope to the other functions the Issue section mentions (`poolPredictions`, `computeAccuracyBuckets`, `summarizeCheckpoint`) — the Fix section's "at minimum" list names only `poolSlices` and `buildReconciliationEntry`, and the orchestrator's guidance block reiterated exactly that scope.

**Frozen-data impact:** None — test-only and one `export` keyword addition; no data files touched, no corpus or `reports/` access.

## Skipped Issues

None — both in-scope findings (WR-01, WR-02) were fixed.

## Verification

- **Typecheck:** `pnpm typecheck` (`tsc --noEmit`) — clean, zero errors, run after each commit's changes.
- **Full test suite:** `pnpm test` (`vitest run`) — **696/696 passed** (up from the pre-existing 690, matching the expected +6 from WR-02's new tests: 4 in the `poolSlices` suite, 2 in the `buildReconciliationEntry` suite). Ran twice: once before splitting into commits (both changes present) and once again after the WR-01 commit was isolated, immediately before the WR-02 commit — both runs green.
- **Frozen-file check:** `git diff f53315d5 HEAD -- <the four frozen files>` — empty, confirming no committed/published figure was touched.
- **Scope check:** `git status --short` clean after both commits; only `packages/harness/eventScopeDiagnostic.ts` and `packages/harness/eventScopeDiagnostic.test.ts` were ever modified across the whole session.
- **Environment:** all of the above ran in the main checkout on `main` (worktree isolation intentionally skipped per the orchestrator's explicit instruction), so these results are reproducible by re-running the same commands against the current `main` tip.

Neither finding involved a logic-bug-classified fix requiring flagged human verification — WR-01 is a subtractive correctness fix (removes arithmetic, provably preserves existing values for the case that mattered) and WR-02 is additive test coverage. Both are marked `fixed`, not `fixed: requires human verification`.

---

# Iteration 2: Info Findings + Extra Item

**Fixed at:** 2026-08-21T21:00:00Z
**Source review:** .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-REVIEW.md
**Iteration:** 2

**Summary:**
- Findings in scope: 4 (IN-01, IN-02, IN-03 — the three Info findings deferred from iteration 1 — plus IX-01, an orchestrator-added item flagged as surfaced by the phase verifier rather than the code reviewer, with no REVIEW.md ID)
- Fixed: 4
- Skipped: 0

**Environment:** worked directly in the main checkout on `main` (no worktree isolation, per orchestrator instruction). Tree started clean at `93957249`. All verification (typecheck, full test suite) ran in that same main checkout, so the numbers below are reproducible from the tree as committed. WR-01 (`fd748f89`) and WR-02 (`235681c4`) from iteration 1 were left untouched, as instructed.

## Fixed Issues

### IN-01: `OprState`'s `lastEventByTeam` doc comment reads ambiguously on first pass

**Files modified:** `packages/core/algorithms/opr.ts`
**Commit:** `a8a376ce`
**Applied fix:** Applied the reviewer's suggested rewording verbatim (it already read cleanly against the current code): "`lastEventByTeam` (D-04): explicitly tracked. Map insertion order alone would record a team's FIRST event, not its MOST RECENT one — wrong once two events interleave — so this field is written explicitly on every `update()` call instead." Comment-only change; no behavior touched.

### IN-02: Magic number `3` for alliance size in the surrogate cold-start fallback

**Files modified:** `packages/core/algorithms/opr.ts`
**Commit:** `a878c638`
**Applied fix:** Per the orchestrator's explicit minimal-scope guidance, added a one-line inline comment at the call site — `fallbackAllianceScore / 3 /* 3 teams per FRC alliance */` — rather than introducing an `ALLIANCE_SIZE` constant or touching other call sites, which the guidance and the reviewer's own "not worth a standalone change today" note both ruled out. Comment-only change; the expression's value is unchanged (`3` is still `3`).

### IN-03: Sidecar line parsing has no defense against a truncated final line

**Files modified:** `packages/harness/baselineFingerprint.ts`, `packages/harness/baselineFingerprint.test.ts`, `packages/harness/eventScopeDiagnostic.ts`, `packages/harness/eventScopeDiagnostic.test.ts`
**Commit:** `7e32fa10`

**What changed:** Took the RE-THROW option per the orchestrator's explicit guidance (not the skip option the reviewer also offered). In both `readSeasonRecordsBySidecar` (`baselineFingerprint.ts:336-357`) and `readSidecarRecordsForAlgorithm` (`eventScopeDiagnostic.ts:119-140`), wrapped the `JSON.parse(line)` call in a `try/catch`. On failure, the catch re-throws a `new Error(...)` naming the sidecar path, the 1-based line number, and an up-to-80-character prefix of the offending line (with an ellipsis if truncated), using the standard `Error` `cause` option to preserve the original `SyntaxError` for anyone inspecting the stack. The pre-existing missing-file handling (`baselineFingerprint.ts`: `console.warn` + early return with zero records; `eventScopeDiagnostic.ts`: throws a distinct "run-dir must cover season" error before this function is even called) and the empty-file/empty-line handling (`line.trim().length === 0 → continue`, and an empty file naturally produces zero readline iterations) are both byte-for-byte unchanged — verified by diff, no lines in either missing/empty-file branch were touched.

Exported both previously-private functions (`readSeasonRecordsBySidecar`, `readSidecarRecordsForAlgorithm`) so they could be exercised directly by a new regression test in each corresponding `*.test.ts`, following the same export-for-testability pattern iteration 1's WR-02 fix used for `buildReconciliationEntry`. Each new test writes a two-line JSONL fixture to a `mkdtempSync`-created temp directory (one well-formed line, one deliberately truncated mid-object on the final line — no trailing newline, mirroring the "process killed mid-write" scenario both files' doc comments already describe), then asserts the rejected promise's message matches both the sidecar path + line number (`/predictions-2022\.jsonl:2/`) and a snippet of the offending line's content (`/2022test_qm2/`). A second `baselineFingerprint.test.ts` test reconfirms the missing-sidecar-file path still returns zero records unchanged. This established `mkdtempSync`/`afterEach(rmSync)` pattern already exists elsewhere in the harness test suite (e.g. `predictions.test.ts`), so no new testing convention was introduced.

**Why this approach:** The orchestrator's guidance was explicit and reasoned: these sidecar readers feed `baselineFingerprint.ts`'s committed provenance digests, so silently skipping a truncated line would change the fingerprinted population and produce a wrong-but-plausible digest — exactly the silent-bad-data failure mode this project's failure log exists to prevent. Re-throwing with the path/line/content-prefix trades a possible mid-run crash (on genuinely corrupted data) for an immediate, diagnosable failure, which is the stated project-wide preference (see WR-01's note in iteration 1: "a spurious loud failure ... is this project's preferred failure mode over silent corruption").

**Frozen-data impact:** None. Source and test changes only — confirmed via `git diff 93957249 --stat -- data/baselines/ data/diagnostics/ data/algorithm-versions/ packages/core/algorithms/sigma1/`, which is empty both before and after this commit. Neither modified function was invoked against real corpus data or `reports/` during this fix; the new tests use only synthetic temp-file fixtures.

## Skipped Issues

None — all four in-scope items (IN-01, IN-02, IN-03, IX-01) were fixed as specified.

## Extra Item (not from REVIEW.md)

### IX-01: stale `carrySeason` justification for OPR in `cli.ts`

**Files modified:** `packages/harness/cli.ts`
**Commit:** `dc630a08`
**Applied fix:** This item was surfaced by the phase verifier (not `gsd-code-reviewer`) and has no REVIEW.md finding ID, as flagged by the orchestrator. `runSeason`'s doc comment (around line 583) previously justified OPR's exclusion from carried-forward `initialStates` with "OPR is season-pooled by design ... a cross-season OPR would be a different algorithm, not a bug fix" — true before this phase, false after it, since this phase made OPR event-scoped. Reworded to argue from the current, correct premise: "OPR is event-scoped by design (Phase 3.2, see `opr.ts`'s file header) — its state does not even survive across events within a season, let alone across a season boundary — so carrying it forward here would contradict the algorithm's own definition, not merely change its behavior." Per the orchestrator's explicit instruction, the surrounding logic (OPR staying out of `initialStates`, the fallback-to-`initState` behavior) and the conclusion were left completely unchanged — this is a comment-only fix correcting the stated reason, not the outcome.

## Verification

- **Typecheck:** `pnpm typecheck` (`tsc --noEmit`) — clean, zero errors, run after all four findings' changes were applied and again as a final check before writing this report.
- **Full test suite:** `pnpm test` (`vitest run`) — **699/699 passed** (up from iteration 1's 696, matching the expected +3 from IN-03's new regression tests: 2 in `baselineFingerprint.test.ts`, 1 in `eventScopeDiagnostic.test.ts`). Ran after each commit's changes and once more as a final pre-report check — all runs green.
- **Frozen-file check:** `git diff 93957249 --stat -- data/baselines/ data/diagnostics/ data/algorithm-versions/ packages/core/algorithms/sigma1/` — empty at every commit, confirming no committed/published figure or CI digest-frozen file was touched.
- **Scope check:** `git status --short` clean after all four commits; the only files ever modified across this iteration were `packages/core/algorithms/opr.ts`, `packages/harness/cli.ts`, `packages/harness/baselineFingerprint.ts`, `packages/harness/baselineFingerprint.test.ts`, `packages/harness/eventScopeDiagnostic.ts`, and `packages/harness/eventScopeDiagnostic.test.ts`.
- **Environment:** all of the above ran in the main checkout on `main` (worktree isolation intentionally skipped per the orchestrator's explicit instruction), so these results are reproducible by re-running the same commands against the current `main` tip.

None of this iteration's four fixes is classified as a logic-error fix per the verification_strategy's "logic bug limitation" — IN-01 and IN-02 are comment-only, IX-01 is comment-only, and IN-03 is additive error-handling (a `try/catch` around an already-existing `JSON.parse` call plus new regression tests) rather than a change to any existing decision-making branch. All four are marked `fixed`, not `fixed: requires human verification`.

---

_Fixed: 2026-08-21T21:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
