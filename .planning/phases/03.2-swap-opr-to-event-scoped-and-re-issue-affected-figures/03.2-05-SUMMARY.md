---
phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures
plan: 05
subsystem: docs
tags: [opr, event-scoped, baseline-change, sc-3, sc-5, documentation, requirements]

# Dependency graph
requires:
  - phase: 03.2-01
    provides: "data/baselines/opr-season-pooled-published-tuned-v3.json and data/baselines/opr-season-pooled-retired-2026-08.json — the two retired-baseline fingerprints this document's retired SC-3 verdict and retired-baseline-provenance section cite"
  - phase: 03.2-03
    provides: "data/baselines/opr-event-scoped-2026-08.json — the single committed source for every current figure this plan re-issues"
  - phase: 03.2-04
    provides: "data/diagnostics/opr-event-scope-2026-08.json — the SC-5 accuracy-by-checkpoint, rank-vs-team-count, and warm-cut figures this plan's SC-5 section reports"
provides:
  - "docs/models/opr-baseline-change.md — the single narrative home for the baseline change: what changed and why, both baselines side by side, the retired baseline's provenance, both SC-3 verdicts (retired dated, current marked), the D-15 attribution sentence, the full SC-5 measurement, named TBA divergences, and Open Items"
  - ".planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md — a committed, measured (not assumed) sweep of every OPR figure/claim across docs/models/, PROJECT.md, REQUIREMENTS.md, and all 20 Phase 1-3 SUMMARYs, with a re-issue/annotate/no-figure-found disposition per row"
  - "docs/models/sigma1-tuning-results.md re-issued: Holdout Head-to-Head and SC-3 Verdict OPR figures replaced, PASS/FAIL re-derived, Phase-2 starting-position OPR column relabelled as retired, provenance footer extended, pointer to opr-baseline-change.md added"
  - "docs/models/sigma1-identifiability.md corrected: the season-scale/ridge-regression description of opr.ts replaced with the event-scoped, no-ridge description; Sigma1's own sampling design and measured rank/condition figures left unchanged; dated correction note added"
  - ".planning/PROJECT.md line 17 re-issued against the committed event-scoped fingerprint (a run that exists, rather than a possibly-deleted gitignored artifact)"
  - ".planning/REQUIREMENTS.md ALGO-01 re-issued to the event-scoped, qualification-matches-only definition, with a dated re-issue note; [x] mark and ID unchanged"
affects: [03.2-06]

actuals:
  tokens: 12800
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Dual-verdict documentation pattern: a superseding measurement (current SC-3 verdict) is stated beside the superseded one (retired SC-3 verdict, dated and unedited), with an explicit standalone attribution sentence naming which direction the baseline moved and forbidding the widened margin from being read as model improvement"
    - "Committed figure inventory as a sweep artifact: a grep-based sweep is written to a file with per-row dispositions (re-issue/annotate/no-figure-found) rather than trusted from a prior planning document's partial list, so a downstream consumer plan can cross-check the sweep was exhaustive"

key-files:
  created:
    - docs/models/opr-baseline-change.md
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md
  modified:
    - docs/models/sigma1-tuning-results.md
    - docs/models/sigma1-identifiability.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "sigma1-tuning-results.md's intro reframed from 'every figure comes from ONE run' to 'every EPA/Sigma1 figure comes from ONE run, OPR's figures are the exception' — since OPR's figures now trace to a later, different committed run (opr-event-scoped-2026-08.json) than EPA/Sigma1's (tuned-v3), and D-12's side-by-side comparison proves this substitution changes nothing except OPR's own numbers"
  - "sigma1-tuning-results.md's Phase-2 starting-position table's OPR column was relabelled '(season-pooled, retired — preserved unedited)' rather than re-issued with new figures — that table's entire purpose is recording what 02-06-SUMMARY.md actually measured at the time, and rewriting it with new numbers would falsify a measurement this phase did not re-run"
  - "sigma1-tuning-results.md's SC-3 Verdict closing prose ('the margins are real but not large... not a blowout') was updated rather than left as-is, since the table directly above it now shows a ~3.5pp margin (not the original 0.39-0.48pp) and leaving the old framing text unedited beside new numbers would have been internally inconsistent and silently understated how much the margin actually widened"
  - "PROJECT.md line 17's harness-validation claim was re-pointed entirely at the new committed fingerprint (25 algorithm-season figures, 84,003 combined-view matches, from data/baselines/opr-event-scoped-2026-08.json) rather than the original Phase 2 3-link-mode run it previously cited — that original run's artifact.json is a gitignored path with no committed provenance, so its figures could not be verified against a committed source under this plan's 'every figure traces to a committed file' constraint"
  - "The figure inventory's sweep scope followed the plan's own illustrative grep command (hyphenated '03-*', not the brace-expansion '0{1,2,3}*' prose) — 03.1-*/03.2-* phase SUMMARYs are inserted phases, not 'Phase 1-3,' and 03.2's own SUMMARYs already describe the new baseline rather than the retired one, so neither is in the annotate scope"

patterns-established:
  - "Baseline-note inline pattern: when a live document's headline table is re-issued but its narrative prose quoted the old baseline's specific numbers, a short 'Baseline note (re-issued by Phase X, date)' paragraph is inserted immediately after the affected prose, naming the attribution requirement explicitly rather than silently editing the prose's own numbers in place"

requirements-completed: []  # ALGO-01/EVAL-01/EVAL-03 (this plan's frontmatter) intentionally left unmarked — REQUIREMENTS.md is a shared cross-wave file the orchestrator owns centrally in this parallel-worktree execution model (matches 03.2-01 through 03.2-04's precedent); ALGO-01's TEXT was re-issued by this plan (its [x] mark and completion status were already true from Phase 1/03.1 and are unchanged)

coverage:
  - id: D1
    description: "A committed figure inventory (03.2-FIGURE-INVENTORY.md) proves the affected-figure sweep was exhaustive rather than assumed — every one of the 12 planning-sweep floor candidates appears with a disposition or an explicit no-figure-found note, plus 8 additional discovered rows"
    requirement: "EVAL-03"
    verification:
      - kind: other
        ref: "03.2-FIGURE-INVENTORY.md — 20 rows total; all 12 floor candidates present with disposition (5 re-issue, 6 annotate, 1 no-figure-found)"
        status: pass
    human_judgment: false
  - id: D2
    description: "docs/models/opr-baseline-change.md carries both SC-3 verdicts (retired dated, current marked) computed from committed fingerprints, with the D-15 attribution sentence stating event-scoped OPR is the weaker opponent and any widened margin is attributable to the baseline change, not to Sigma1 improving"
    requirement: "EVAL-01"
    verification:
      - kind: other
        ref: "node -e verification script from plan Task 1 (all new/retired 2025/2026 opr/epa/sigma1 figures present, matching data/baselines/opr-event-scoped-2026-08.json to four decimal places)"
        status: pass
    human_judgment: false
  - id: D3
    description: "docs/models/opr-baseline-change.md's SC-5 section reports every accuracy bucket, every rank checkpoint, and the D-09 warm-cut partition from the committed diagnostic, plus the named TBA divergences (surrogate, disqualification, Statbotics) and an Open Items section"
    requirement: "ALGO-01"
    verification:
      - kind: other
        ref: "node -e verification script from plan Task 2 (matchstats_helper/np.linalg.pinv/comp_level/Open Items/surrogate/Statbotics all present; all 6 accuracy-bucket Brier figures and all 3 warm-cut Brier figures present, matching data/diagnostics/opr-event-scope-2026-08.json to four decimal places)"
        status: pass
    human_judgment: false
  - id: D4
    description: "sigma1-tuning-results.md, sigma1-identifiability.md, PROJECT.md, and REQUIREMENTS.md are all re-issued against the event-scoped baseline and cross-linked to opr-baseline-change.md, with pnpm test and pnpm typecheck staying green"
    requirement: "ALGO-01"
    verification:
      - kind: other
        ref: "node -e verification script from plan Task 3 (new OPR figures present in tuning-results.md, ALGO-01 text names event+qualification, identifiability.md carries the correction pointer)"
        status: pass
      - kind: integration
        ref: "node node_modules/typescript/bin/tsc --noEmit (clean, no output); node node_modules/vitest/vitest.mjs run (45 files, 625 passed, 21 skipped — corpus-dependent tests skip in this doc-only worktree with no corpus.sqlite hard-link, no failures)"
        status: pass
    human_judgment: false

duration: ~55min hands-on
completed: 2026-08-21
status: complete
---

# Phase 3.2 Plan 05: Write the Record — Baseline Change Narrative and Live-Document Re-Issue Summary

**Wrote `docs/models/opr-baseline-change.md` as the single narrative home for OPR's season-pooled-to-event-scoped baseline swap (both verdicts, the SC-5 cold-start measurement, and the required D-15 attribution sentence), backed by a committed 20-row figure-sweep inventory, and re-issued every live OPR figure in `sigma1-tuning-results.md`, `sigma1-identifiability.md`, `PROJECT.md`, and `REQUIREMENTS.md` — every number traced to one of four committed baseline/diagnostic files, never to memory or a gitignored path.**

## Performance

- **Duration:** ~55 min hands-on wall clock
- **Tasks:** 3/3
- **Files modified:** 6 (2 created, 4 modified)

## Accomplishments

- **Verified all four committed provenance files** (`git ls-files --error-unmatch`) before writing any figure, per the plan's precondition.
- **Swept the repository for every OPR figure and mechanism claim** — `docs/models/*.md`, `PROJECT.md`, `REQUIREMENTS.md`, and all 20 `.planning/phases/{01,02,03}-*/**/*SUMMARY.md` files — and committed the result as `03.2-FIGURE-INVENTORY.md`: 5 live-document rows closed by this plan (Task 3), 11 Phase 1-3 SUMMARY rows flagged `annotate` for plan 03.2-06, and 4 floor candidates re-checked and confirmed to carry no stale figure. Every one of the plan's 12 named floor candidates appears with an explicit disposition.
- **Wrote `docs/models/opr-baseline-change.md`**, the single narrative home for the change (D-14): what changed and why (citing TBA's `matchstats_helper.py`, its `comp_level != "qm"` filter, and `np.linalg.pinv`, plus the ridge-shrinkage arithmetic behind D-06), both baselines side by side (OPR under both definitions, 2022-2026, plus the D-12 EPA/Sigma1 side-by-side proving 20/20 pairs identical), the retired baseline's two-fingerprint provenance and digest-agreement finding, both SC-3 verdicts (retired, dated 2026-08-17; current, marked current — both PASS 8/8), the required D-15 standalone attribution sentence (event-scoped OPR is the weaker opponent; the widened margin is attributable to the baseline changing, not to Sigma1 improving), the full SC-5 measurement (all 6 accuracy buckets, all 6 rank checkpoints, the numerical-condition-squaring caveat, and the D-09 warm-only cut reported beside — never replacing — the all-matches headline), the two named TBA divergences (surrogates, disqualifications) plus the Statbotics reconciliation-target note, and an `## Open Items` section naming four unresolved items with forward pointers.
- **Re-issued `docs/models/sigma1-tuning-results.md`**: replaced every OPR figure in the Holdout Head-to-Head and SC-3 Verdict tables with the event-scoped run's values, re-derived every PASS/FAIL from the new figures (still 8/8), relabelled the Phase-2 starting-position table's OPR column as the retired baseline (preserved unedited, since that table's purpose is recording what Phase 2 actually measured), added baseline-note paragraphs wherever the prose quoted a specific OPR margin so the surrounding narrative stays internally consistent with the re-issued tables, added a pointer to `opr-baseline-change.md` near the top, and extended the provenance footer with the new run's exact command and `runTimestamp`.
- **Corrected `docs/models/sigma1-identifiability.md`**: replaced the stale description of `opr.ts` as a season-scale ridge regression over a ~36,000 x 3,700 design matrix with the event-scoped, no-ridge description, attributed the retained historical figure explicitly to "the retired season-pooled `opr.ts`," and added a dated correction note pointing at `opr-baseline-change.md` — while leaving every one of the document's own Sigma1 sampling-design, rank, and condition-number figures exactly as originally measured (this phase did not re-run that check).
- **Re-issued `.planning/PROJECT.md` line 17** (the harness-validation claim) against the committed event-scoped fingerprint rather than the original Phase 2 run, whose gitignored artifact could not be verified against a committed source. Lines 25 and 64 were checked and confirmed to already carry no season-scope implication for OPR — no edit needed there.
- **Re-issued `.planning/REQUIREMENTS.md`'s ALGO-01 text** from "OPR is computed per team per season as a no-variance baseline" (now literally false) to the event-scoped, qualification-matches-only definition, keeping its `[x]` mark and ID, with a dated re-issue note beneath the Algorithms section.
- **Confirmed `pnpm typecheck` (clean) and the full test suite (45 files, 625 passed, 21 skipped, 0 failed)** stay green after all documentation changes — the 21 skips are corpus-dependent tests skipping in this doc-only worktree, which has no `data/corpus.sqlite` hard-link (this plan needed none).

## Task Commits

Each task was committed atomically:

1. **Task 1: Sweep the affected-figure inventory, then carry the SC-3 restatement end to end** - `2a03c42a` (docs)
2. **Task 2: Complete the baseline-change narrative, including SC-5 and the named divergences** - `7323d725` (docs)
3. **Task 3: Re-issue the live documents — tuning results, identifiability prose, PROJECT, REQUIREMENTS** - `4456c403` (docs)

_No separate plan-metadata commit in this worktree — the orchestrator merges the wave and owns the shared-file (STATE.md/ROADMAP.md) commit centrally._

## Files Created/Modified

- `docs/models/opr-baseline-change.md` - New: the single narrative home for the OPR baseline change
- `.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md` - New: the committed, measured figure-sweep result
- `docs/models/sigma1-tuning-results.md` - Re-issued OPR figures, re-derived PASS/FAIL, relabelled Phase-2 table, extended provenance footer
- `docs/models/sigma1-identifiability.md` - Corrected the stale OPR mechanism description, added a dated correction note
- `.planning/PROJECT.md` - Re-issued line 17's harness-validation claim
- `.planning/REQUIREMENTS.md` - Re-issued ALGO-01's text, added a dated re-issue note

## Decisions Made

See `key-decisions` in the frontmatter above for the five decisions made during execution (the intro-paragraph reframing, the Phase-2 table relabelling, the SC-3 closing-prose update, PROJECT.md's re-pointed source, and the sweep-scope interpretation).

## Deviations from Plan

None — plan executed exactly as written. The precondition check, both node-script verifications per task, and the full `pnpm test`/`pnpm typecheck` equivalent all passed on the first attempt for every task. `pnpm install`/`pnpm test`/`pnpm typecheck` needed the same direct-binary-invocation workaround plans 03.2-01 through 03.2-04 already documented (this worktree's `better-sqlite3` cannot rebuild natively; the shipped prebuild works fine at runtime) — this is environment setup, not a plan deviation, and no committed file was affected by it.

## Issues Encountered

None beyond the documented environment setup above. Every real command in the plan's `<verify>` blocks ran against real committed data and passed without retries.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 03.2-06 is unblocked.** `03.2-FIGURE-INVENTORY.md`'s 11 `annotate` rows are the exact set of Phase 1-3 SUMMARYs plan 03.2-06 needs to add dated "superseded by Phase 3.2" notes to (D-16) — and the plan's own text notes 03.2-06 independently re-runs the sweep, so any omission here is caught twice.
- **The record now states outright which baseline is weaker and why any margin widened** — `docs/models/opr-baseline-change.md`'s D-15 attribution sentence is the load-bearing honesty artifact 04-CONTEXT D-11 requires, and it is now live, cross-linked from `sigma1-tuning-results.md`'s own SC-3 section.
- **Every live document a future reader would consult for "what is OPR" now describes the event-scoped, qualification-matches-only, no-ridge definition** — `sigma1-identifiability.md`, `PROJECT.md`, and `REQUIREMENTS.md` no longer describe a model that has been deleted, closing exactly the failure mode REBUILD_SPEC.md's failure log names.

---
*Phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures*
*Completed: 2026-08-21*

## Self-Check: PASSED

All claimed files found on disk (`docs/models/opr-baseline-change.md`, `03.2-FIGURE-INVENTORY.md`,
`docs/models/sigma1-tuning-results.md`, `docs/models/sigma1-identifiability.md`, `.planning/PROJECT.md`,
`.planning/REQUIREMENTS.md`, this SUMMARY) and all three claimed commit hashes (`2a03c42a`,
`7323d725`, `4456c403`) found in `git log --oneline --all`.
