---
phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures
plan: 06
subsystem: docs
tags: [opr, event-scoped, baseline-change, sc-2, sc-3, d-16, d-17, errata, phase-gate]

# Dependency graph
requires:
  - phase: 03.2-05
    provides: "docs/models/opr-baseline-change.md (the narrative every annotation and the D-11 errata point at) and 03.2-FIGURE-INVENTORY.md's 11 annotate-dispositioned rows (this plan's starting worklist, independently re-verified rather than trusted)"
provides:
  - "Dated 'Superseded by Phase 3.2' notes on 9 Phase 1-3 SUMMARYs (01-02, 01-03, 01-04, 01-06, 02-01, 02-03, 02-05, 02-06, 03-06), each additive-only with original measured numbers left byte-identical, per D-16"
  - "03.2-FIGURE-INVENTORY.md closed: all 10 annotate rows resolved (9 annotated, 1 explicitly left un-annotated with a recorded discrepancy note), final count line added"
  - "04-CONTEXT.md D-11 dated errata (2026-08-21): corrects the misattributed untuned-vs-promoted Sigma1 figures and the stale post-D-06 ridge-term claim, additive beneath the original text, per D-17"
  - "Phase 3.2's closing gate measured green: pnpm typecheck clean, pnpm test 625/625 passed (21 corpus-dependent skips), digest.test.ts passed, data/algorithm-versions/ unchanged since Phase 3 (D-10's bit-freeze held for the whole phase)"
affects: []

actuals:
  tokens: 6600
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Fourth independent re-sweep as a reconciliation step: rather than trusting the committed inventory from the prior plan, re-running the same grep methodology from scratch and recording where it disagrees (03-03-SUMMARY.md) rather than silently accepting or silently overriding the prior list"
    - "Dated errata-beneath-original pattern applied to a decision record (04-CONTEXT.md), mirroring the SUMMARY annotation pattern: the original text stays untouched, a clearly-marked, dated correction is appended immediately beneath it"

key-files:
  created: []
  modified:
    - .planning/phases/01-data-foundation-evaluation-harness/01-02-SUMMARY.md
    - .planning/phases/01-data-foundation-evaluation-harness/01-03-SUMMARY.md
    - .planning/phases/01-data-foundation-evaluation-harness/01-04-SUMMARY.md
    - .planning/phases/01-data-foundation-evaluation-harness/01-06-SUMMARY.md
    - .planning/phases/02-prediction-models-epa-sigma1/02-01-SUMMARY.md
    - .planning/phases/02-prediction-models-epa-sigma1/02-03-SUMMARY.md
    - .planning/phases/02-prediction-models-epa-sigma1/02-05-SUMMARY.md
    - .planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md
    - .planning/phases/03-tuning-ranking-points-versioning/03-06-SUMMARY.md
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md
    - .planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md

key-decisions:
  - "03-03-SUMMARY.md was NOT annotated despite the inventory's original 'annotate' disposition. The independent re-sweep found no season-pooled-specific figure or claim in that file — only a code-identity note about opr.ts's/epa.ts's version-string shape change, the same kind of content that floor-candidate row 20 (03-04-SUMMARY.md) was already correctly dispositioned as clean. Attaching a superseded-by pointer to a file with no stale figure beneath it would itself be a false claim, so the discrepancy is recorded in the inventory instead of silently resolved either way."
  - "The superseded-by note block is placed directly beneath each SUMMARY's H1 title, before the one-line bold description, rather than at the end of the file — so a reader meets the caveat before any figure it qualifies, per the plan's explicit placement instruction."
  - "04-CONTEXT.md's D-11 errata is a beneath-original addition, not an in-place edit, matching D-16's SUMMARY annotation discipline extended to a decision record: the original (now-known-wrong) text stays visible so a reader can see both what was decided and what was later found wrong."

patterns-established:
  - "Reconciliation-note-in-place: when a re-verification step disagrees with a prior committed artifact's disposition for one row, the disagreement is written into that same artifact (not a separate document), directly beside the disputed row, so a future reader hits the correction at the point of use."

requirements-completed: [ALGO-01, EVAL-01]

coverage:
  - id: D1
    description: "Every Phase 1-3 SUMMARY carrying a retired OPR figure has a dated superseded-by note pointing at docs/models/opr-baseline-change.md, with original measured numbers left intact"
    requirement: "ALGO-01"
    verification:
      - kind: other
        ref: "node -e verification script (Task 2): 8 files (plus 02-06 from Task 1 = 9 total) all contain 'Superseded by Phase 3.2' and 'opr-baseline-change.md'; combined git diff -U0 across all 9 shows zero removed content lines"
        status: pass
    human_judgment: false
  - id: D2
    description: "The affected-SUMMARY inventory was verified by an independent second sweep, not inherited on trust"
    requirement: "EVAL-01"
    verification:
      - kind: other
        ref: "Independent grep re-sweep (numeric + mechanism-prose passes) reconciled against 03.2-FIGURE-INVENTORY.md; 9 of 10 annotate rows confirmed, 1 (03-03-SUMMARY.md) could not be confirmed and was left un-annotated with the discrepancy recorded in the inventory"
        status: pass
    human_judgment: false
  - id: D3
    description: "04-CONTEXT.md D-11's misattributed figures and stale ridge claim are corrected as dated errata"
    requirement: "ALGO-01"
    verification:
      - kind: other
        ref: "node -e verification script (Task 3): errata contains 0.7539/0.7819/0.7657/0.7873/0.7618/0.7825 and the opr-baseline-change.md pointer; git diff -U0 on 04-CONTEXT.md shows added lines only"
        status: pass
    human_judgment: false
  - id: D4
    description: "The phase closes green: pnpm test, pnpm typecheck, digest.test.ts all pass, and data/algorithm-versions/ is unchanged from the phase's starting commit"
    requirement: "ALGO-01"
    verification:
      - kind: integration
        ref: "node node_modules/typescript/bin/tsc --noEmit (clean); node node_modules/vitest/vitest.mjs run (45 files, 625 passed, 21 skipped, 0 failed); node node_modules/vitest/vitest.mjs run packages/harness/digest.test.ts (1 passed, 1 skipped); git diff --exit-code data/algorithm-versions/ exits 0; last commit touching that path (7bac21c3) predates every Phase 3.2 commit"
        status: pass
    human_judgment: false

duration: ~50min hands-on
completed: 2026-08-21
status: complete
---

# Phase 3.2 Plan 06: Annotate the Execution Record and Close the Phase Summary

**Independently re-swept the Phase 1-3 SUMMARY set (finding it 9-of-10 correct against the prior planning sweep and recording the one discrepancy in place), applied identical dated "Superseded by Phase 3.2" notes to 9 SUMMARYs with original numbers left byte-identical, corrected 04-CONTEXT D-11's misattributed untuned-vs-promoted Sigma1 figures and stale ridge-term claim as dated errata, and closed the phase gate green.**

## Performance

- **Duration:** ~50 min hands-on wall clock
- **Tasks:** 3/3
- **Files modified:** 11 (9 SUMMARYs annotated, 1 inventory closed, 1 decision record corrected)

## Accomplishments

- **Ran a fourth independent re-sweep of the Phase 1-3 SUMMARY set** — the exact numeric grep (`grep -rniE "opr" --include='*SUMMARY.md' ... | grep -E "0\.[0-9]{3,4}"`) plus the mechanism-prose second pass (`season-pool|per team per season|ridge regression|3,700|36,000|design matrix`) — run independently of `03.2-FIGURE-INVENTORY.md` rather than trusted from it. Reconciled the result: 9 of the inventory's 10 `annotate` rows were confirmed to carry real season-pooled OPR figures or stale mechanism prose; all 4 "no OPR figure found on re-check" floor candidates and the one already-clean row were reconfirmed clean; nothing beyond the inventory's existing rows was found.
- **Found and recorded one discrepancy rather than silently resolving it either way.** Row 14 (`03-tuning-ranking-points-versioning/03-03-SUMMARY.md`) could not be confirmed — the file carries no season-pooled-specific figure or claim, only a code-identity note about `opr.ts`'s/`epa.ts`'s version-string shape change, the same kind of content floor-candidate row 20 (`03-04-SUMMARY.md`) already correctly dispositioned as clean. Per D-16's own discipline (a note is only warranted where a reader actually meets a stale figure it qualifies), `03-03-SUMMARY.md` was **not** annotated. The discrepancy is written into `03.2-FIGURE-INVENTORY.md` itself, beside the disputed row, so a future reader hits the correction at the point of use rather than in a separate document.
- **Annotated `02-06-SUMMARY.md` end to end as the exemplar (Task 1)**, fixing the note's exact wording — the literal marker `Superseded by Phase 3.2`, the date, a one-line statement that OPR is now event-scoped and quals-only, an explicit statement that the original numbers are left intact as the execution record, and a link to `docs/models/opr-baseline-change.md` — placed directly beneath the H1 title, before the one-line bold description. Proved the diff shape mechanically: `git diff -U0` on the file showed added lines only, zero removed content lines.
- **Applied the identical note block to the remaining 8 confirmed-affected SUMMARYs (Task 2):** `01-02`, `01-03`, `01-04`, `01-06` (Phase 1); `02-01`, `02-03`, `02-05` (Phase 2); `03-06` (Phase 3). Every file changed nothing but the note — no typo fixes, no reflowed paragraphs, no figure adjustments. The combined `git diff -U0` across all 9 annotated SUMMARYs (including `02-06` from Task 1) shows zero removed content lines; every one of the underlying 20+ OPR figures cited in the inventory remains byte-identical to what each plan originally measured.
- **Closed `03.2-FIGURE-INVENTORY.md`:** every `annotate` row marked `DONE (03.2-06)` in place, row 14 marked `NOT applied` with the discrepancy pointer, and a final count line recorded (9 of 10 annotate-dispositioned rows closed).
- **Corrected `04-CONTEXT.md` D-11's misattributed figures as dated errata (Task 3), per D-17.** D-11 originally cited 2025: 0.7539 and 2026: 0.7819 as Sigma1 losing holdout winner accuracy to season-pooled OPR — those are the **untuned** `sigma1-defaults` rows from `docs/models/sigma1-tuning-results.md`'s Phase-2 starting-position table. The **promoted, tuned** Sigma1 (`sigma1@2.0.0+tuned-2026-08`) scored **0.7657** (2025) and **0.7873** (2026) against season-pooled OPR's **0.7618** and **0.7825**, winning both — `sigma1-tuning-results.md`'s SC-3 Verdict records 8/8 PASS. The errata states this plainly, explains why the correction *strengthens* D-11's honesty argument (a Sigma1 win against a weaker baseline is riskier to misread when Sigma1 was already winning against the harder one — the reader has more reason to suspect the margin widened only because the opponent weakened), and corrects the stale post-D-06 claim that "the existing ridge term is what keeps it finite" (there is no ridge term after D-06; finiteness now comes from the minimum-norm pseudo-inverse solve). The errata was added beneath D-11's original text, not edited in place — `git diff -U0` on `04-CONTEXT.md` shows added lines only.
- **Closed the phase gate, every check measured, not assumed:** `pnpm typecheck` (`node node_modules/typescript/bin/tsc --noEmit`) — clean, zero errors. `pnpm test` (`node node_modules/vitest/vitest.mjs run`) — 45 files, 625 passed, 21 skipped (corpus-dependent tests, expected — no `data/corpus.sqlite` in this doc-only worktree), 0 failed. `packages/harness/digest.test.ts` alone — 1 passed, 1 skipped (same corpus-dependent reason). `git diff --exit-code data/algorithm-versions/` — exits 0. The last commit touching that path (`7bac21c3`, Phase 3) predates every Phase 3.2 commit, confirming D-10's Sigma1 bit-freeze held for the whole phase, not merely at this instant. All four committed phase data artifacts (`opr-season-pooled-published-tuned-v3.json`, `opr-season-pooled-retired-2026-08.json`, `opr-event-scoped-2026-08.json`, `opr-event-scope-2026-08.json`) confirmed tracked by git.

## Task Commits

Each task was committed atomically:

1. **Task 1: Independently re-sweep, then annotate one SUMMARY end to end and prove the diff shape** - `f4b246f7` (docs)
2. **Task 2: Annotate every remaining affected Phase 1-3 SUMMARY** - `0cb6bb94` (docs)
3. **Task 3: Correct 04-CONTEXT D-11's misattributed figures as dated errata, and close the phase gate** - `2aefab56` (docs)

_No separate plan-metadata commit in this worktree — the orchestrator merges the wave and owns the shared-file (STATE.md/ROADMAP.md) commit centrally, per this plan's explicit instruction._

## Files Created/Modified

- `.planning/phases/01-data-foundation-evaluation-harness/01-02-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/01-data-foundation-evaluation-harness/01-03-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/01-data-foundation-evaluation-harness/01-04-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/01-data-foundation-evaluation-harness/01-06-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/02-prediction-models-epa-sigma1/02-01-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/02-prediction-models-epa-sigma1/02-03-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/02-prediction-models-epa-sigma1/02-05-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md` - Annotated (additive-only, exemplar)
- `.planning/phases/03-tuning-ranking-points-versioning/03-06-SUMMARY.md` - Annotated (additive-only)
- `.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md` - Closed: annotate rows marked done, discrepancy recorded, final count added
- `.planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md` - D-11 dated errata added beneath original text (additive-only)

## Decisions Made

See `key-decisions` in the frontmatter above for the three decisions made during execution: leaving `03-03-SUMMARY.md` un-annotated with a recorded discrepancy, the note's placement directly beneath each SUMMARY's title, and the beneath-original (not in-place) shape of the D-11 errata.

## Deviations from Plan

**1. [Reconciliation finding, not a Rule 1-4 deviation] `03-03-SUMMARY.md` left un-annotated.** The plan's Task 2 `<files>` list did not include `03-03-SUMMARY.md` (it was never in the task's declared file set), and the independent re-sweep confirmed it should stay that way: the file carries no season-pooled-specific OPR figure or claim, only a code-identity version-string note. `03.2-FIGURE-INVENTORY.md`'s original `annotate` disposition for this row could not be confirmed and is recorded as such in place, per Task 1's explicit instruction to mark any inventory row the re-sweep could not confirm. No note was force-added to a file with nothing for it to qualify.

No other deviations — every other task executed exactly as written, and no auto-fixable bug, missing functionality, or blocking issue was encountered in this documentation-only plan.

## Issues Encountered

None. `pnpm install` failed to rebuild `better-sqlite3`'s native binding (no Visual Studio C++ toolchain in this worktree) — the same environment characteristic every prior 03.2 plan documented — but `node_modules` populated successfully otherwise, and direct-binary invocation (`node node_modules/typescript/bin/tsc`, `node node_modules/vitest/vitest.mjs`) worked cleanly for both the typecheck and the full test suite. This is environment setup, not a plan deviation, and no committed file was affected by it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Phase 3.2 is closed.** Every measured gate passed: typecheck, full test suite, the digest reproducibility gate, and the frozen `data/algorithm-versions/` surface, the last confirmed unchanged since Phase 3 across the phase's entire duration.
- **Phase 4 can now read `04-CONTEXT.md` D-11 without inheriting a wrong premise.** The errata is live, dated, and points at `docs/models/opr-baseline-change.md` for the full current record — Phase 4's own scope source no longer misstates whether Sigma1 beat or lost to season-pooled OPR.
- **The execution record is intact and cross-linked.** Every Phase 1-3 SUMMARY that measured a stale OPR figure now tells a future reader where the current numbers live, without a single one of those SUMMARYs' original measured numbers having been touched — the property D-16 exists to guarantee.

---
*Phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures*
*Completed: 2026-08-21*

## Self-Check: PASSED

All 11 modified files found on disk, and all three task commit hashes (`f4b246f7`, `0cb6bb94`,
`2aefab56`) found in `git log --oneline --all`.
