---
phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures
verified: 2026-08-21T19:49:58Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 3.2: Swap OPR to event-scoped and re-issue affected figures Verification Report

**Phase Goal:** OPR means event-scoped OPR everywhere in this project — harness, published figures, and the record — matching what TBA and Statbotics publish, with the season-pooled baseline it replaces preserved as recorded history rather than deleted.
**Verified:** 2026-08-21T19:49:58Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `opr.ts` computes OPR at event scope; no season-pooled ridge OPR path remains reachable from the harness registry or the published set | ✓ VERIFIED | `packages/core/algorithms/opr.ts` (298 lines) implements `PerEventOprState` keyed by `eventKey`, `solveEventOpr` (bare `SingularValueDecomposition(MtM).solve(Mts)`, no ridge term, `OPR_RIDGE_LAMBDA` removed from the file entirely). `update()` line 238 gates on `compLevel !== "qm"` (quals-only). `packages/harness/cli.ts`'s `ALGORITHMS.opr` registry entry imports this exact module (`opr.test.ts`'s `ALGORITHMS.opr` object-identity test, part of the 690-test green suite, proves this — see Spot-Checks). No `solveRidgeOpr`/`IncrementalInverse` symbol exists anywhere outside historical-comment references in unrelated files (`sigma1/covariance.ts`, `sigma1/kalman.ts`) and the intentionally-preserved retired-baseline artifacts/docs. |
| 2 | Full 2022-2026 walk-forward harness re-run against the new baseline; every OPR figure in `docs/models/` and `PROJECT.md` reflects that run; every Phase 1-3 SUMMARY carrying a retired OPR figure has a dated superseded-by note with original numbers intact | ✓ VERIFIED | `data/baselines/opr-event-scoped-2026-08.json` (75 slices, 5 algorithms x 5 seasons x 3 views) is the committed re-run. `docs/models/sigma1-tuning-results.md`, `docs/models/sigma1-identifiability.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` all carry the new event-scoped figures/prose (verified by direct read and cross-check against the JSON — see Data-Flow Trace). 9 Phase 1-3 SUMMARYs (`01-02`, `01-03`, `01-04`, `01-06`, `02-01`, `02-03`, `02-05`, `02-06`, `03-06`) each carry a dated "Superseded by Phase 3.2 (2026-08-21)" note; `git diff -U0 5d3d5ce1..HEAD` across all 9 shows exactly 45 insertions, 0 deletions — additive-only, original figures byte-identical. The one flagged open item (row 14, `03-03-SUMMARY.md`) was independently re-checked by this verifier: the file carries only a version-string code-identity note, no season-pooled OPR figure — correctly left un-annotated. |
| 3 | `docs/models/` records both baselines side by side (retired + new + reason) so a reader can judge SC-3 against either | ✓ VERIFIED | `docs/models/opr-baseline-change.md` § "Both baselines, side by side" table quotes exact per-season Brier/accuracy for both definitions across all 5 seasons, sourced from the three committed fingerprints (`opr-season-pooled-published-tuned-v3.json`, `opr-season-pooled-retired-2026-08.json`, `opr-event-scoped-2026-08.json`) plus a full digest-agreement reconciliation section. |
| 4 | Phase 3's SC-3 verdict re-stated against the new baseline as an explicit measured result, with any change in Sigma1-vs-OPR outcome named outright | ✓ VERIFIED | `opr-baseline-change.md` § "SC-3 Verdict" states both the current (event-scoped, 8/8 PASS) and retired (season-pooled, 8/8 PASS) verdicts side by side, and § "Which baseline is weaker?" states outright, in its own sentence, that event-scoped OPR is the weaker opponent and that Sigma1's widened margin is "attributable entirely to the baseline changing, and NOT to any improvement in Sigma1" — matching D-15's required framing. Verified against `data/baselines/opr-event-scoped-2026-08.json`'s own numbers directly (exact match). |
| 5 | Event-scoped OPR's early-event rank-deficient behavior is measured and documented, not assumed benign | ✓ VERIFIED | `data/diagnostics/opr-event-scope-2026-08.json` (accuracy-by-checkpoint buckets, rank-vs-team-count checkpoints, reconciliation, warm/cold cut) is committed and its numbers reproduce exactly in `opr-baseline-change.md` § "Early-event behavior (SC-5)" (checked directly, e.g. bucket-0 Brier 0.24796... -> 0.2480, all-matches 0.21105... -> 0.2111). The document states the non-monotonic Brier degradation honestly rather than smoothing it, and explicitly declines mitigation (D-08) in its own named section. |

**Score:** 5/5 truths verified

### Requirement Traceability (ALGO-01 text correction)

| Item | Status | Evidence |
|------|--------|----------|
| ALGO-01's `[x]` mark and ID unchanged; text corrected from "per team per season" to "per team per event ... qualification matches only" | ✓ VERIFIED | `.planning/REQUIREMENTS.md` line 28 reads the corrected text with `[x]` intact; line 37-40 carries an explicit "Re-issued 2026-08-21 (Phase 3.2)" note stating this is a text correction, not a new requirement. No new requirement ID was introduced anywhere in this phase's plans (`requirements:` frontmatter across all 6 plans only ever cites `ALGO-01`/`EVAL-01`, both pre-existing). |
| 04-CONTEXT D-11 misattributed figures corrected (D-17) | ✓ VERIFIED | `.planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md` carries a 28-line dated errata block beneath D-11's original (unedited) text, correcting the untuned-vs-promoted Sigma1 mix-up and the stale ridge-term claim, per `git diff -U0` (28 insertions, 0 deletions). |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/core/algorithms/opr.ts` | Event-scoped, quals-only, no-ridge rewrite | ✓ VERIFIED | 298 lines, matches orchestrator's stated line 238/193 anchors exactly; version bumped to `3.0.0+baseline` |
| `data/baselines/opr-season-pooled-published-tuned-v3.json` | Published-run retired fingerprint | ✓ VERIFIED | Tracked, reproduces `sigma1-tuning-results.md`'s original holdout table to 4 decimals |
| `data/baselines/opr-season-pooled-retired-2026-08.json` | D-13's final retired run | ✓ VERIFIED | Tracked; digest agreement measured and recorded (3/5 seasons bitwise, 5/5 aggregate) rather than assumed |
| `data/baselines/opr-event-scoped-2026-08.json` | New baseline, 5 algorithms x 5 seasons x 3 views | ✓ VERIFIED | 75 slices confirmed; combined-view scoredCounts match retired run exactly per season |
| `data/diagnostics/opr-event-scope-2026-08.json` | SC-5 measurement | ✓ VERIFIED | 6 accuracy buckets, 6 rank checkpoints, 5 reconciliation entries (all `matches: true`), warm/cold cut |
| `docs/models/opr-baseline-change.md` | New narrative record (D-14) | ✓ VERIFIED | Exists, ~337 lines, covers all D-14 required content (both baselines, SC-3 restatement, SC-5 measurement, named divergences, open items) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `packages/harness/cli.ts` `ALGORITHMS.opr` | `packages/core/algorithms/opr.ts` `opr` export | direct import + object-identity | ✓ WIRED | `import { opr } from "../core/algorithms/opr.js"` at cli.ts:41; `opr.test.ts`'s own object-identity assertion (`ALGORITHMS.opr` toBe `opr`) passes as part of the green 690-test suite |
| Committed fingerprints | `docs/models/opr-baseline-change.md` narrative numbers | direct JSON read + cross-check | ✓ WIRED | Every headline number in the document (event-scoped per-season figures, SC-3 verdict rows, warm/cold cut) was independently re-derived from the committed JSON by this verifier and matches exactly |
| `data/diagnostics/opr-event-scope-2026-08.json` | `docs/models/opr-baseline-change.md` § Early-event behavior | direct JSON read + cross-check | ✓ WIRED | Bucket and checkpoint figures match the document's tables exactly |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `packages/harness/cli.ts` | 583 | Stale doc comment: "OPR is season-pooled by design (see `opr.ts`'s file header)" — no longer true post-rewrite (OPR is now event-scoped) | Info | Prose-only; does not affect SC-1's code-reachability claim (this is a comment explaining why OPR has no `carrySeason` implementation, and that functional fact is still correct — only the stated *reason* is stale). No test, artifact, or published figure is affected. Worth a one-line fix in a future pass but not phase-blocking. |

No debt markers (`TBD`/`FIXME`/`XXX`), no `TODO`/`HACK`/`PLACEHOLDER` markers, and no empty-implementation patterns found in any file this phase created or modified (`opr.ts`, `baselineFingerprint.ts`, `eventScopeDiagnostic.ts`, `opr-baseline-change.md`).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite green | `pnpm test` | 45 files, 690/690 passed | ✓ PASS |
| Typecheck clean | `pnpm typecheck` | exit 0, no output | ✓ PASS |
| Sigma1 reproducibility gate unaffected | `pnpm vitest run packages/harness/digest.test.ts` | 3/3 passed | ✓ PASS |
| Sigma1 promoted versions bit-frozen across whole phase | `git diff --stat 5d3d5ce1..HEAD -- packages/core/algorithms/sigma1/ data/algorithm-versions/` | no output (zero changes) | ✓ PASS |
| Fingerprint commits precede the deleting rewrite commit (D-13 sequencing prohibition) | `git log --oneline --reverse` ordering check | `49b76e99`, `5fdf1929`, `3fdb6d0e` all precede `37d6fd67` | ✓ PASS |
| Row-14 open item (`03-03-SUMMARY.md`) genuinely carries no season-pooled OPR figure | `grep -n -iE "opr" 03-03-SUMMARY.md` | Only version-string code-identity notes found, no figure/mechanism prose | ✓ PASS — confirms 03.2-06's disposition was correct, no SC-2 gap |
| Retired figures do not leak into live docs unqualified | `grep -rniE "0\.1675|0\.7618|0\.1773|0\.7825|..." docs/models/*.md .planning/PROJECT.md .planning/REQUIREMENTS.md` | All hits are inside the explicitly-labeled retired-baseline comparison rows of `opr-baseline-change.md` and `sigma1-tuning-results.md`'s preserved-original comparison table | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| ALGO-01 (text correction) | all 6 plans | OPR definition corrected to event-scoped/quals-only, `[x]` and ID preserved | ✓ SATISFIED | `.planning/REQUIREMENTS.md` line 28, 37-40 |
| EVAL-01 / EVAL-03 (comparison surface re-baselined) | 03.2-01, 03, 05 | Harness comparison surface re-run and re-published against new baseline | ✓ SATISFIED | `data/baselines/opr-event-scoped-2026-08.json`, `docs/models/opr-baseline-change.md`, `docs/models/sigma1-tuning-results.md` |

No orphaned requirements found — this phase deliberately introduces no new requirement IDs, matching its documented scope.

## Gaps Summary

None. All 5 ROADMAP success criteria are verified directly against committed code and data artifacts, not against SUMMARY.md narrative. The one item the orchestrator flagged as open (row 14 / `03-03-SUMMARY.md`) was independently re-checked and confirmed correct as left un-annotated — no SC-2 gap. One informational-severity stale doc comment (`cli.ts:583`) was found; it does not affect any success criterion, test, or published figure and is noted for a future cleanup pass rather than blocking this phase.

---

_Verified: 2026-08-21T19:49:58Z_
_Verifier: Claude (gsd-verifier)_
