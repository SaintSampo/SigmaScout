---
phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures
plan: 05
type: execute
wave: 5
depends_on: ["03.2-04"]
files_modified:
  - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md
  - docs/models/opr-baseline-change.md
  - docs/models/sigma1-tuning-results.md
  - docs/models/sigma1-identifiability.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ALGO-01, EVAL-01, EVAL-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/models/ records both baselines side by side — the retired season-pooled numbers, the new event-scoped numbers, and the reason for the switch — so a reader can see the baseline moved and judge the SC-3 claim against either (SC-3, D-14)."
    - "Phase 3's SC-3 verdict is restated against the new baseline as eight explicit measured comparisons, computed from data/baselines/opr-event-scoped-2026-08.json, with the retired verdict preserved beside it, dated, as what was measured through Phase 3 (SC-4, D-15)."
    - "The record states outright, in its own sentence, whether the new baseline is a weaker or stronger opponent than the retired one, and attributes any change in Sigma1's margin to that rather than to Sigma1 improving — and if the new verdict is a shortfall it is recorded as a shortfall with a named decision about what to change, never reworded to fit the result (D-15, 03-CONTEXT D-02)."
    - "Every OPR figure in docs/models/ and .planning/PROJECT.md reflects the plan 03.2-03 re-run rather than the retired one, with the provenance footer naming that one run's exact command and runTimestamp (SC-2, D-16)."
    - "REQUIREMENTS.md's ALGO-01 text no longer says OPR is computed per team per season; it states the event-scoped, qualification-matches-only definition the code now implements."
    - "docs/models/sigma1-identifiability.md no longer describes opr.ts as a season-scale ridge regression over a ~36,000 x 3,700 design matrix, which D-03 and D-06 both made false."
    - "A grep sweep across docs/models/, .planning/PROJECT.md, .planning/REQUIREMENTS.md and every Phase 1-3 SUMMARY produced a committed inventory of affected figures, rather than the phase trusting either CONTEXT.md's or RESEARCH.md's partial list."
  artifacts:
    - docs/models/opr-baseline-change.md
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md
  key_links:
    - "Every figure in docs/models/opr-baseline-change.md traces to one of three committed sources: data/baselines/opr-season-pooled-published-tuned-v3.json, data/baselines/opr-event-scoped-2026-08.json, or data/diagnostics/opr-event-scope-2026-08.json — never to a gitignored reports/ path and never to prose."
    - "docs/models/sigma1-tuning-results.md gains a pointer to opr-baseline-change.md, so the project's most-read results document cannot be read without discovering that the baseline moved (D-14)."
  prohibitions:
    - statement: "A widened Sigma1 margin over the new baseline must not be presented, framed, or implied as Sigma1 improving. If event OPR is the weaker opponent the record must say so outright in its own sentence, because that is what keeps the honesty requirement from being satisfied only in the letter (D-15)."
      requirement_id: EVAL-01
      category: transparency
      status: unresolved
---

<objective>
Write the record. Create `docs/models/opr-baseline-change.md` as the single narrative home for the
baseline change, restate SC-3 against the new baseline honestly, and re-issue every live OPR figure
in `docs/models/`, `PROJECT.md` and `REQUIREMENTS.md`.

Purpose: the phase's whole point is that the baseline moved and the record says so. 04-CONTEXT D-11
calls this "not optional bookkeeping." The failure mode this guards against is specific and this
project has already recorded it once: a document describing a model that no longer exists.

Output: one new document carrying the whole story, three re-issued documents, and a committed
inventory proving the sweep was exhaustive rather than assumed.

**Every number in every document this plan writes comes from a committed file**, not from a
gitignored `reports/` path and not from memory:
- retired baseline: `data/baselines/opr-season-pooled-published-tuned-v3.json` (the run behind the
  published tables) and `data/baselines/opr-season-pooled-retired-2026-08.json` (D-13's final run)
- new baseline: `data/baselines/opr-event-scoped-2026-08.json`
- SC-5 measurement: `data/diagnostics/opr-event-scope-2026-08.json`

If any figure cannot be traced to one of those four files, it does not go in the document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-CONTEXT.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-RESEARCH.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-PATTERNS.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-01-SUMMARY.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-03-SUMMARY.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Sweep the affected-figure inventory, then carry the SC-3 restatement end to end</name>
  <files>.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md, docs/models/opr-baseline-change.md</files>
  <precondition>All four committed source files exist and are tracked: `data/baselines/opr-season-pooled-published-tuned-v3.json`, `data/baselines/opr-season-pooled-retired-2026-08.json`, `data/baselines/opr-event-scoped-2026-08.json`, `data/diagnostics/opr-event-scope-2026-08.json`. Assert with `git ls-files --error-unmatch` on all four before writing any figure; halt if any is missing, because the alternative is writing numbers from memory.</precondition>
  <read_first>
    - data/baselines/opr-event-scoped-2026-08.json — all five algorithms' per-season Brier and winner accuracy from the re-run. These are the new numbers.
    - data/baselines/opr-season-pooled-published-tuned-v3.json — the retired numbers, as published.
    - data/baselines/opr-season-pooled-retired-2026-08.json — D-13's final run, and whether its digest matched the published one.
    - docs/models/sigma1-tuning-results.md lines 105-168 — the published § Holdout Head-to-Head and § SC-3 Verdict tables, whose exact column layout and PASS/FAIL wording the restatement copies.
    - .planning/phases/03-tuning-ranking-points-versioning/03-CONTEXT.md § D-02 — SC-3's literal eight-comparison reading, and the rule that a shortfall is recorded as a shortfall with a named decision, never reworded.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-CONTEXT.md § D-14 and § D-15 — what the new document holds and the required framing.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-01-SUMMARY.md — whether the two retired fingerprints' digests agreed.
  </read_first>
  <action>
**Sweep first.** Run a figure sweep and write the result to
`.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md`.
03.2-RESEARCH.md rates the affected-figures list only MEDIUM confidence for exhaustiveness and
explicitly recommends this sweep instead of trusting either its own list or CONTEXT.md's. Sweep at
least: `docs/models/*.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, and every
`.planning/phases/0{1,2,3}*/**/*SUMMARY.md`. A useful starting command is
`grep -rniE "opr" --include='*.md' docs/models .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/phases/01-* .planning/phases/02-* .planning/phases/03-* | grep -E "0\.[0-9]{3,4}"`,
widened with a second pass for prose claims that describe OPR's mechanism without quoting a number
(for example a description of its scope, its regularization, or its design-matrix dimensions).

The planning sweep already found these candidates — treat them as a floor to confirm and extend, not
a ceiling: `docs/models/sigma1-tuning-results.md`, `docs/models/sigma1-identifiability.md`,
`.planning/PROJECT.md` (lines 17, 25, 64), `.planning/REQUIREMENTS.md` (line 28),
`01-02-SUMMARY.md`, `01-04-SUMMARY.md`, `02-01-SUMMARY.md`, `02-03-SUMMARY.md`, `02-05-SUMMARY.md`,
`02-06-SUMMARY.md`, `03-05-SUMMARY.md`, `03-06-SUMMARY.md`.

The inventory file is a table with one row per affected location: file, line reference, the figure or
claim, and its disposition — `re-issue` for live documents (`docs/models/`, `PROJECT.md`,
`REQUIREMENTS.md`) or `annotate` for Phase 1-3 SUMMARYs, per D-16. Plan 03.2-06 consumes the
`annotate` rows and independently re-runs the sweep, so an omission here is caught twice.

**Then carry the SC-3 restatement end to end.** Create `docs/models/opr-baseline-change.md` with its
full heading skeleton and the SC-3 sections completely written. Follow
`docs/models/sigma1-tuning-results.md`'s conventions exactly: an opening paragraph naming the ONE run
every figure traces to with the exact command in a fenced block, section headings, and a closing
provenance footer of the form `*Phase: ...*` / `*Generated: <date>, from <source> (<exact command>,
runTimestamp: <ISO>)*`.

Compute the eight SC-3 comparisons from `data/baselines/opr-event-scoped-2026-08.json`: promoted
Sigma1 versus OPR and versus EPA, on Brier and on winner accuracy, for 2025 and 2026. Render them in
the same six-column table shape the published `## SC-3 Verdict` uses (`# | Season | Comparison |
Tuned Sigma1 | Baseline | Result`), and render the retired verdict as a second table beside it, dated
and labelled as what was measured through Phase 3 against the season-pooled baseline. Both tables
must be self-labelling about which baseline they are measured against; the new one is marked current.

**The D-15 framing sentence is a deliverable, not garnish.** Immediately adjacent to the new verdict
table, state in its own sentence whether the new baseline is a weaker or a stronger opponent than the
retired one — determined by comparing the two committed fingerprints' OPR figures, not assumed — and
state the consequence outright. If event OPR scores worse than season-pooled OPR, the sentence must
say that any widening of Sigma1's margin is attributable to the baseline changing and NOT to Sigma1
improving. If the measurement instead shows event OPR is the stronger opponent, record that plainly
as contradicting this phase's own stated expectation, and say so — 03.2-CONTEXT D-15 asserts event
OPR is weaker, and a measurement that disagrees is a finding, not an error to smooth over.

If any of the eight comparisons fails, record it as a failure with a named decision about what to
change (03-CONTEXT D-02). Do not reword a comparison, drop it, change its denominator, or reach for
the warm-only cut to rescue a number — D-09 explicitly rejects a warm-only headline for exactly that
reason.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='docs/models/opr-baseline-change.md';const t=fs.readFileSync(p,'utf8');const n=require('./data/baselines/opr-event-scoped-2026-08.json');const g=(id,y)=>{const a=n.algorithms.find(x=>x.id===id);const s=a.perSeason.find(x=>x.season===y);return [s.brierScore.toFixed(4),s.winnerAccuracy.toFixed(4)];};let missing=[];for(const id of ['opr','epa','sigma1'])for(const y of [2025,2026])for(const v of g(id,y))if(!t.includes(v))missing.push(id+' '+y+' '+v);if(missing.length)throw new Error('figures absent from the document: '+missing.join(', '));if(!/0\.1675/.test(t)||!/0\.7618/.test(t))throw new Error('retired 2025 figures absent - both baselines must be stated side by side');console.log('all new 2025/2026 opr,epa,sigma1 figures and the retired 2025 figures are present')"</automated>
  </verify>
  <acceptance_criteria>
    - `03.2-FIGURE-INVENTORY.md` exists, is committed, and contains a table with at least 12 rows, each naming a file, a line reference, the figure or claim, and a disposition of `re-issue` or `annotate`.
    - Every one of the twelve planning-sweep candidates listed in the action appears in the inventory, either with a disposition or with an explicit "no OPR figure found on re-check" note.
    - `docs/models/opr-baseline-change.md` exists and contains two SC-3 verdict tables, one labelled against the retired season-pooled baseline and dated as what was measured through Phase 3, one labelled against the event-scoped baseline and marked current.
    - The new verdict table has exactly 8 numbered rows covering Sigma1 versus OPR and versus EPA, on Brier and on winner accuracy, for 2025 and 2026, each with an explicit PASS or FAIL.
    - Every figure in the new verdict table matches `data/baselines/opr-event-scoped-2026-08.json` to four decimal places.
    - The document contains a standalone sentence stating whether the new baseline is a weaker or stronger opponent than the retired one, and attributing any change in Sigma1's margin accordingly.
    - The document's opening names the single run every figure traces to, with the exact `pnpm harness --seasons 2022-2026 --algorithm opr,epa,sigma1,sigma1-defaults,sigma1-adapt --out reports/event-scoped-v1` command in a fenced block, and closes with a provenance footer carrying that run's `runTimestamp`.
    - If any of the eight comparisons is a FAIL, the document records it as a failure with a named decision about what to change, and no comparison was reworded, dropped, or re-based on the warm-only cut.
  </acceptance_criteria>
  <done>The inventory is measured rather than assumed, and the honesty-critical half of the record — both verdicts, the attribution sentence — is written from committed figures.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the baseline-change narrative, including SC-5 and the named divergences</name>
  <files>docs/models/opr-baseline-change.md</files>
  <read_first>
    - data/diagnostics/opr-event-scope-2026-08.json — the six accuracy buckets, six rank checkpoints, and the three-way warm-cut partition this section reports.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-RESEARCH.md § Pitfall 2, § Pitfall 3, § Pitfall 4 — the normal-equations condition squaring, the surrogate/DQ divergence from TBA's literal practice, and the finding that Statbotics no longer publishes a comparable plain OPR.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-CONTEXT.md § D-02, § D-05, § D-06, § D-07, § D-08, § D-09, § D-12 — the decisions this narrative explains.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-03-SUMMARY.md — the D-12 EPA/Sigma1 comparison table and the `sigma1-adapt` override-source finding.
    - docs/models/sigma1-tuning-results.md § Open Items — this codebase's precedent for recording an unresolved or non-ideal finding plainly with a forward pointer.
  </read_first>
  <action>
Fill out the rest of `docs/models/opr-baseline-change.md`. It is the single narrative home for this
change (D-14), so it must be readable start to finish by someone who was not here.

Sections to write, beyond the SC-3 material Task 1 already placed:

**What changed and why.** OPR now means a fit over one event's qualification matches, solved as a
plain minimum-norm pseudo-inverse — which is what TBA computes. Cite the verification directly:
TBA's own `src/backend/common/helpers/matchstats_helper.py` filters `if match.comp_level != "qm":
continue` in both `build_team_mapping` and `build_Minv_matrix`, and solves with `np.linalg.pinv(M)`
with no regularization term anywhere in the file. State the arithmetic behind dropping the ridge:
under season pooling a team had roughly 30-40 alliance observations by mid-season so `lambda = 3`
biased ratings by about 7-9%, while under quals-only event scope a team finishes a regional with
about 12, so the same lambda would shrink every rating by roughly 20% and far more early — freezing
it would have held a constant fixed while roughly tripling its effect.

**Both baselines, side by side.** A table of per-season Brier and winner accuracy for OPR under both
definitions, 2022-2026, from the two committed fingerprints. Alongside it, per D-12, the EPA,
`sigma1`, `sigma1-defaults` and `sigma1-adapt` rows from both runs in adjacent columns — those
algorithms never read OPR and the match stream is unchanged, so they are expected to be identical,
and stating them side by side is what makes unexplained movement visible to a reader. Report whatever
plan 03.2-03 measured, including any movement, and note that no reproduction control was asserted on
them (D-12) so a change in the shared stream during the rewrite would have been indistinguishable
from the OPR swap.

**The retired baseline's provenance.** State that the retired numbers are backed by two committed
fingerprints — the run behind the published tables and D-13's final run of the implementation
immediately before deletion — and report whether their prediction-stream digests agreed. That
comparison, measured in plan 03.2-01, is the strongest available evidence about whether the published
retired figures were reproducible from the code at the moment it was retired.

**Early-event behavior (SC-5).** Report D-07's two halves together from
`data/diagnostics/opr-event-scope-2026-08.json`: the Brier and winner-accuracy curve across the six
completed-quals buckets, and the design matrix's rank against its team-column count at the six
checkpoints. State the cold-start consequence in plain terms — a team with no observations at an
event has rating 0, so the first qualification match of every event predicts 0-0 with `pRedWin`
exactly 0.500 and a flat 0.25 Brier. Report the warm-only cut beside the all-matches headline (D-09),
labelling clearly that the headline is all-matches because EPA and Sigma1 predict the same match set
and comparability is what SC-3 rests on. Note that some of the measured early-event degradation is
attributable to solving the normal equations on the Gram matrix — which squares the effective
condition number — and that this is a deliberate fidelity choice matching TBA's own method, not a
defect. State explicitly that mitigation is out of scope for this phase (D-08) and that this
measurement is the input to a future phase's decision.

**Named divergences from TBA.** Two, both deliberate and both worth one paragraph so a future reader
who diffs SigmaScout's OPR against TBA's does not file a bug. First, surrogates: TBA's
`matchstats_helper.py` has no surrogate handling at all — every listed team's column is included and
every team's rating updated — while this project excludes a surrogate's column and subtracts an offset
(D-07 from Phase 1), so numbers will differ on surrogate-affected events. Second, disqualifications:
this project deliberately keeps a disqualified team's column and rating update, on the reasoning that
OPR models physical score contribution and a disqualification is a ranking ruling. Also state, per
03.2-RESEARCH.md Pitfall 4, that "matching what TBA and Statbotics publish" reduces in practice to
matching TBA for OPR specifically — Statbotics' primary model is EPA and it does not currently publish
a plain per-event OPR comparable to TBA's, so no second independent implementation exists to
reconcile against.

**Open items.** Follow `sigma1-tuning-results.md`'s § Open Items precedent: name each unresolved
thing plainly with a forward pointer, softening nothing. At minimum: cold-start mitigation deferred
per D-08; Sigma1 re-tuning against the new baseline deferred as its own phase per D-10; whether each
championship division should be its own event scope, raised and not opened; and the absence of an
event-boundary hook on `AlgorithmModule` analogous to the existing `carrySeason`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('docs/models/opr-baseline-change.md','utf8');const d=require('./data/diagnostics/opr-event-scope-2026-08.json');const need=['matchstats_helper','np.linalg.pinv','comp_level','Open Items','surrogate','Statbotics'];const miss=need.filter(s=>!t.includes(s));if(miss.length)throw new Error('missing required content: '+miss.join(', '));for(const b of d.accuracyBuckets){const v=b.brierScore.toFixed(4);if(!t.includes(v))throw new Error('SC-5 bucket figure absent: '+b.bucket+' '+v);}const w=d.warmCut;for(const v of [w.allMatches.brierScore.toFixed(4),w.warmOnly.brierScore.toFixed(4),w.coldOnly.brierScore.toFixed(4)])if(!t.includes(v))throw new Error('warm-cut figure absent: '+v);console.log('narrative carries every SC-5 bucket, the warm-cut partition, and all required divergence content')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/models/opr-baseline-change.md` contains a section stating what changed and why, citing TBA's `matchstats_helper.py`, its `comp_level != "qm"` filter, and its `np.linalg.pinv` solve.
    - It contains a per-season side-by-side table of OPR under both definitions for 2022-2026, every figure matching the two committed fingerprints to four decimal places.
    - It contains adjacent retired and new columns for `epa`, `sigma1`, `sigma1-defaults` and `sigma1-adapt`, and a sentence recording that no reproduction control was asserted on them (D-12).
    - It states whether the two retired fingerprints' prediction-stream digests agreed.
    - It contains an SC-5 section reporting all six accuracy buckets and all six rank checkpoints, with every bucket's Brier figure matching `data/diagnostics/opr-event-scope-2026-08.json` to four decimal places.
    - It reports the warm-only cut beside the all-matches headline and labels the headline as the score, with a sentence explaining why the headline stays all-matches (D-09).
    - It states that cold-start mitigation is out of scope (D-08) and names the future phase as the place that decision belongs.
    - It contains paragraphs on the surrogate and disqualification divergences from TBA's literal practice, and a sentence on Statbotics not publishing a comparable plain OPR.
    - It contains an `## Open Items` section with at least four named items, each with a forward pointer.
  </acceptance_criteria>
  <done>The whole story lives in one document, written from committed figures, with every known divergence and every deferred item named rather than left for a future reader to rediscover.</done>
</task>

<task type="auto">
  <name>Task 3: Re-issue the live documents — tuning results, identifiability prose, PROJECT, REQUIREMENTS</name>
  <files>docs/models/sigma1-tuning-results.md, docs/models/sigma1-identifiability.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/models/sigma1-tuning-results.md — the whole file. Specifically § Holdout Head-to-Head (lines 110-124), the Phase-2 starting-position table below it, § SC-3 Verdict (lines 143-168), and the provenance footer (line 344, currently naming `reports/tuned-v3/artifact.json` and `runTimestamp: 2026-08-17T01:11:06.668Z`).
    - docs/models/sigma1-identifiability.md — lines 9, 21 and 25, which describe `opr.ts` as a ridge regression recovering a per-team total "for the season" over a roughly 36,000 x 3,700 design matrix. D-03 and D-06 both invalidate that prose.
    - .planning/PROJECT.md lines 17, 25 and 64 — the harness-validation claim, the baseline-algorithms line, and the v1-algorithms scope row.
    - .planning/REQUIREMENTS.md line 28 — ALGO-01's current text.
    - data/baselines/opr-event-scoped-2026-08.json — the source for every replacement figure.
    - .planning/phases/03.2-.../03.2-FIGURE-INVENTORY.md — the `re-issue` rows this task must close.
  </read_first>
  <action>
Re-issue each live document in place (D-16: live docs are re-issued outright; SUMMARYs are annotated, which is plan 03.2-06's job).

**`docs/models/sigma1-tuning-results.md`.** Replace the OPR rows in § Holdout Head-to-Head and every
OPR figure in the § SC-3 Verdict table with the new run's values from
`data/baselines/opr-event-scoped-2026-08.json`, keeping the table shapes byte-identical in structure.
Re-derive each PASS/FAIL from the new figures rather than carrying the old verdicts forward. Update
the surrounding prose wherever it quotes an OPR margin — the paragraph beneath the Phase-2
starting-position table currently narrates specific OPR comparisons and will be wrong otherwise.
Leave the Phase-2 starting-position table's own Sigma1 rows untouched; that table records what was
measured in Phase 2 and is not an OPR-baseline claim, but its OPR column must be re-issued or
explicitly relabelled as the retired baseline, whichever reads more honestly for that table's purpose.
Add a pointer sentence near the top directing readers to `docs/models/opr-baseline-change.md` for the
full baseline-change narrative (D-14). Replace the provenance footer with the new run's date, exact
command and `runTimestamp`, following the existing footer format exactly.

**`docs/models/sigma1-identifiability.md`.** Correct the prose at lines 9, 21 and 25 so it no longer
describes `opr.ts` as recovering a per-team total for the *season* via ridge regression over a
~36,000 x 3,700 design matrix. The surviving true statement is that OPR recovers a per-team total
from overlapping alliance sums by regression — now scoped to one event's qualification matches, at
roughly 39 team columns rather than 3,700, with no regularization term. Keep the document's own
Sigma1 findings, its sampling design, and all of its own measured rank and condition numbers exactly
as they are: those are Sigma1 identifiability measurements, not OPR figures, and rewriting them would
falsify a measurement this phase did not re-run. Add a dated note stating that the OPR description
was corrected by Phase 3.2 and pointing at `opr-baseline-change.md`.

**`.planning/PROJECT.md`.** Line 17's harness-validation claim quotes a run's slice, match and
prediction-record counts; re-issue it against the new run's artifact so it describes a run that
exists. Line 25 and line 64 describe OPR as a baseline algorithm — update any wording that implies
season scope. Do not restate SC-3's verdict here; that lives in `docs/models/`.

**`.planning/REQUIREMENTS.md` line 28.** ALGO-01 currently reads "OPR is computed per team per season
as a no-variance baseline." That is now literally false. Re-issue it to state the event-scoped,
qualification-matches-only definition — for example "OPR is computed per team per event over
qualification matches only, as a no-variance baseline, matching TBA's definition." Keep its `[x]`
completion mark and its ID. Add a dated one-line note beneath the Algorithms section, or in the
traceability table's notes if one exists, recording that ALGO-01's text was re-issued by Phase 3.2
and pointing at `opr-baseline-change.md`. This is a requirement-text correction, so it must be
visible as one and not look like the requirement always said this.

Close every `re-issue` row in `03.2-FIGURE-INVENTORY.md` by marking it done in place.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const n=require('./data/baselines/opr-event-scoped-2026-08.json');const opr=n.algorithms.find(a=>a.id==='opr');const t=fs.readFileSync('docs/models/sigma1-tuning-results.md','utf8');for(const y of [2025,2026]){const s=opr.perSeason.find(p=>p.season===y);for(const v of [s.brierScore.toFixed(4),s.winnerAccuracy.toFixed(4)])if(!t.includes(v))throw new Error('tuning-results missing new opr '+y+' '+v);}if(!t.includes('opr-baseline-change'))throw new Error('tuning-results lacks the pointer to opr-baseline-change.md');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const line=r.split('\n').find(l=>l.includes('ALGO-01'));if(!/event/i.test(line)||!/qualification/i.test(line))throw new Error('ALGO-01 not re-issued: '+line);const i=fs.readFileSync('docs/models/sigma1-identifiability.md','utf8');if(!i.includes('opr-baseline-change'))throw new Error('identifiability lacks the dated correction pointer');console.log('live documents re-issued and cross-linked')" &amp;&amp; pnpm test &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `docs/models/sigma1-tuning-results.md` § Holdout Head-to-Head and § SC-3 Verdict contain the new run's OPR figures, matching `data/baselines/opr-event-scoped-2026-08.json` to four decimal places.
    - Every PASS/FAIL in its § SC-3 Verdict table was re-derived from the new figures and is consistent with them.
    - It contains a pointer to `opr-baseline-change.md` above the fold, and its provenance footer names the new run's command and `runTimestamp` rather than `reports/tuned-v3`.
    - `docs/models/sigma1-identifiability.md` no longer describes `opr.ts` as season-scoped or ridge-regularized, retains all of its own Sigma1 sampling design and measured rank/condition figures unchanged, and carries a dated correction note pointing at `opr-baseline-change.md`.
    - `.planning/PROJECT.md` line 17's counts describe the new run; lines 25 and 64 carry no season-scope implication for OPR.
    - `.planning/REQUIREMENTS.md`'s ALGO-01 text names event scope and qualification matches, keeps its `[x]` mark and its ID, and is accompanied by a dated note recording the re-issue.
    - Every `re-issue` row in `03.2-FIGURE-INVENTORY.md` is marked done.
    - `pnpm test` and `pnpm typecheck` both exit 0.
  </acceptance_criteria>
  <done>Every live document states the new baseline, cross-links to the narrative, and no longer describes an algorithm that does not exist.</done>
</task>

</tasks>

## Artifacts this phase produces

New paths introduced by THIS plan (exclude from source-drift verification):

| Kind | Name | Location |
|---|---|---|
| Document | `opr-baseline-change.md` | `docs/models/` |
| Planning artifact | `03.2-FIGURE-INVENTORY.md` | `.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/` |

This plan introduces no code symbol, function, type, or CLI flag.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed figure files -> published prose | The one boundary in this plan: a number crossing from a machine-generated artifact into a human-read document |
| retired verdict -> restated verdict | Where a prior measured claim is superseded and could be misrepresented |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.2-23 | Repudiation | the SC-3 restatement | high | mitigate | Both verdicts are stated, each self-labelled with the baseline it was measured against and the new one marked current; the retired verdict keeps its date. The automated verify re-reads every published figure back out of the committed fingerprints, so a transcription drift between artifact and document fails the task. |
| T-03.2-24 | Tampering | framing of a widened margin | high | mitigate | D-15's attribution sentence is a named acceptance criterion with its own standalone-sentence requirement, and the prohibition block forbids presenting a weakened opponent as a Sigma1 improvement. If the measurement contradicts the phase's own expectation, the action requires recording the contradiction rather than smoothing it. |
| T-03.2-25 | Tampering | a shortfall reworded to fit | high | mitigate | The action forbids rewording, dropping, re-basing on the warm-only cut, or changing a denominator, and requires a FAIL to be recorded with a named decision about what to change (03-CONTEXT D-02, D-09). |
| T-03.2-26 | Repudiation | figures with no traceable source | high | mitigate | Every figure must trace to one of four committed files; the plan states that a figure which cannot be traced does not go in the document, and each task's verify re-derives published figures from those files. |
| T-03.2-27 | Tampering | Sigma1 identifiability measurements | medium | mitigate | The action explicitly ring-fences `sigma1-identifiability.md`'s own sampling design and measured rank/condition numbers as a measurement this phase did not re-run, so only the OPR description is corrected. |
| T-03.2-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. 03.2-RESEARCH.md § Package Legitimacy Audit records the not-applicable finding. |
</threat_model>

<verification>
- `pnpm test` and `pnpm typecheck` green (documentation changes must not break the reviews frontmatter lint or any doc-referencing test).
- `docs/models/opr-baseline-change.md` exists with both verdict tables, the attribution sentence, the SC-5 measurement, the named divergences, and an Open Items section.
- `docs/models/sigma1-tuning-results.md`, `docs/models/sigma1-identifiability.md`, `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` all re-issued and cross-linked.
- `03.2-FIGURE-INVENTORY.md` committed with every `re-issue` row closed.

</verification>

<success_criteria>
- Both baselines are recorded side by side with the reason for the switch (SC-3).
- Phase 3's SC-3 verdict is restated against the new baseline as an explicit measured result, with any change named outright and never presented as an improvement Sigma1 did not make (SC-4).
- Every OPR figure in `docs/models/` and `PROJECT.md` reflects the re-run (SC-2, live-document half).
- ALGO-01's requirement text matches the code.
</success_criteria>

<output>
Create `.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-05-SUMMARY.md` when done.
</output>