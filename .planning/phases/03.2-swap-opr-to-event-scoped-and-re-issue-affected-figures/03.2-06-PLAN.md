---
phase: 03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures
plan: 06
type: execute
wave: 6
depends_on: ["03.2-05"]
files_modified:
  - .planning/phases/01-data-foundation-evaluation-harness/01-02-SUMMARY.md
  - .planning/phases/01-data-foundation-evaluation-harness/01-04-SUMMARY.md
  - .planning/phases/02-prediction-models-epa-sigma1/02-01-SUMMARY.md
  - .planning/phases/02-prediction-models-epa-sigma1/02-03-SUMMARY.md
  - .planning/phases/02-prediction-models-epa-sigma1/02-05-SUMMARY.md
  - .planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md
  - .planning/phases/03-tuning-ranking-points-versioning/03-05-SUMMARY.md
  - .planning/phases/03-tuning-ranking-points-versioning/03-06-SUMMARY.md
  - .planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md
  - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md
autonomous: true
requirements: [ALGO-01, EVAL-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Phase 1-3 SUMMARY carrying a retired OPR figure has a dated superseded-by note pointing at docs/models/opr-baseline-change.md, with its original measured numbers left intact as the execution record (SC-2, D-16)."
    - "No SUMMARY's originally measured number was changed: the diff for every annotated SUMMARY consists of added lines only, with zero removed content lines."
    - "The affected-SUMMARY inventory was verified by an independent second sweep run in this plan, not inherited on trust from plan 03.2-05's inventory, from CONTEXT.md's list, or from RESEARCH.md's list — all three of which differ."
    - "04-CONTEXT.md D-11's misattributed figures are corrected in place as dated errata: the cited 2025 0.7539 and 2026 0.7819 are the UNTUNED Sigma1 rows, while promoted Sigma1 scored 0.7657 and 0.7873 against OPR's 0.7618 and 0.7825 and won 8/8 (D-17)."
    - "04-CONTEXT.md D-11's closing claim that 'the existing ridge term is what keeps it finite' is corrected in the same errata: there is no ridge term after D-06, and finiteness now comes from the minimum-norm pseudo-inverse solve."
    - "The phase closes green: `pnpm test`, `pnpm typecheck`, and `pnpm vitest run packages/harness/digest.test.ts` all pass, and `git diff --exit-code data/algorithm-versions/` exits 0 against the phase's starting commit."
  artifacts:
    - .planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md
  key_links:
    - "Every annotation points at docs/models/opr-baseline-change.md, created in plan 03.2-05 — the note is only meaningful if that document exists, so this plan runs after it rather than beside it."
    - ".planning/phases/03.2-.../03.2-FIGURE-INVENTORY.md's `annotate` rows are this plan's worklist, and this plan closes each row it completes."
    - "04-CONTEXT.md D-11 is Phase 4's own scope source and Phase 4 reads it directly, so an uncorrected version keeps propagating a wrong premise into the next phase."
  prohibitions:
    - statement: "No Phase 1-3 SUMMARY's originally measured numbers may be rewritten, recalculated, or replaced. A SUMMARY records what a completed plan actually measured; editing its figures to match a later run would falsify the execution record. Only a dated superseded-by note is added (D-16)."
      requirement_id: ALGO-01
      category: transparency
      status: unresolved
---

<objective>
Annotate the execution record and correct the one document that carries a wrong premise into
Phase 4, then close the phase.

Purpose: D-16 draws a hard line that SC-2's original wording did not. Live documents
(`docs/models/`, `PROJECT.md`, `REQUIREMENTS.md`) get their figures replaced outright — plan 03.2-05
did that. Phase 1-3 SUMMARYs do not: they are the execution record of what a completed plan actually
measured, and rewriting their numbers would falsify it. They get a dated superseded-by note instead,
with their original numbers left exactly as they are. SC-2 was reworded through `/gsd-phase` before
planning and before any new figures were known, so this softer reading cannot be mistaken for fitting
a criterion to a result.

Separately, 04-CONTEXT.md D-11 — this phase's own scope source, which Phase 4 reads directly — cites
Sigma1 losing holdout winner accuracy to season-pooled OPR using the *untuned* Sigma1 rows. The
promoted Sigma1 won those comparisons. An uncorrected D-11 keeps propagating a wrong premise, and the
correction strengthens D-11's argument rather than weakening it (D-17).

Output: annotated SUMMARYs with original numbers intact, a dated errata block in 04-CONTEXT.md, and a
green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-CONTEXT.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md
@.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-05-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Independently re-sweep, then annotate one SUMMARY end to end and prove the diff shape</name>
  <files>.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md, .planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md</files>
  <precondition>`docs/models/opr-baseline-change.md` exists and is tracked by git — every annotation this plan writes points at it, and a note pointing at a file that does not exist is worse than no note. Assert with `git ls-files --error-unmatch docs/models/opr-baseline-change.md` and halt if it fails.</precondition>
  <read_first>
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md — the `annotate` rows plan 03.2-05 produced, which this task verifies rather than trusts.
    - .planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md — the densest carrier of retired OPR figures found during planning (7 numeric hits); read enough of it to place the note where a reader meets it before the figures.
    - docs/models/opr-baseline-change.md — the target of every annotation, so the note's wording can name what the reader will find there.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-CONTEXT.md § D-16 — the annotate-not-rewrite rule and its reasoning.
  </read_first>
  <action>
**Re-sweep independently first.** Three separate lists of affected SUMMARYs exist and they disagree:
03.2-CONTEXT.md names one set, 03.2-RESEARCH.md found four additional files
(`01-02-SUMMARY.md`, `01-04-SUMMARY.md`, `02-01-SUMMARY.md`, `02-03-SUMMARY.md`) and rated its own
list only MEDIUM confidence for exhaustiveness, and this phase's own planning sweep additionally
surfaced `02-05-SUMMARY.md`, `03-05-SUMMARY.md` and `03-06-SUMMARY.md`. Run the sweep again here,
independently, rather than inheriting any of them:

`grep -rniE "opr" --include='*SUMMARY.md' .planning/phases/01-* .planning/phases/02-* .planning/phases/03-tuning-ranking-points-versioning .planning/phases/03.1-* | grep -E "0\.[0-9]{3,4}"`

Run a second pass for prose claims that describe OPR's mechanism without quoting a figure — season
scope, a regularization term, a design-matrix dimension, an incremental solve — since those are
equally superseded and a numeric grep will not surface them.

Reconcile the result against `03.2-FIGURE-INVENTORY.md`. Add any file the re-sweep found that the
inventory lacks, and mark any inventory row the re-sweep could not confirm with a note saying so.
Record the reconciliation outcome in the SUMMARY: which of the three prior lists was closest, and
what this sweep found that none of them did.

**Then annotate one file end to end.** Add the dated superseded-by note to
`.planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md`. Place it high enough that a
reader meets it before the figures it qualifies — directly beneath the document's title or its
frontmatter, not buried at the end.

Fix the note's exact wording now so every remaining file gets the identical block, and record that
wording in the SUMMARY. It must contain: the literal marker string `Superseded by Phase 3.2`, the
date, a one-line statement that OPR became event-scoped and quals-only so every OPR figure below
describes the retired season-pooled baseline, an explicit statement that the original numbers are
left intact as the execution record, and a link to `docs/models/opr-baseline-change.md`.

**Prove the diff shape.** After editing, run `git diff -U0` on that file and confirm the diff
contains only added lines and no removed content lines. This is the mechanical form of D-16's rule:
an annotation adds, it never replaces. If the diff shows any removed content line, the edit
overstepped and must be redone.
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process');const f='.planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md';const d=execSync('git diff -U0 -- '+f).toString();const removed=d.split('\n').filter(l=>/^-[^-]/.test(l));if(removed.length)throw new Error('annotation removed '+removed.length+' content line(s): '+removed.slice(0,3).join(' | '));const t=require('fs').readFileSync(f,'utf8');if(!t.includes('Superseded by Phase 3.2'))throw new Error('marker string absent');if(!t.includes('opr-baseline-change.md'))throw new Error('pointer absent');console.log('annotation is additive only and carries the marker and the pointer')"</automated>
  </verify>
  <acceptance_criteria>
    - The independent re-sweep was run and its result reconciled against `03.2-FIGURE-INVENTORY.md`; the SUMMARY records what it found beyond the three prior lists.
    - `.planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md` contains the literal string `Superseded by Phase 3.2`, a date, and a link to `docs/models/opr-baseline-change.md`.
    - `git diff -U0` on that file shows added lines only, with zero removed content lines.
    - Every numeric OPR figure that was in that file before this task is still present in it afterwards, byte-identical.
    - The exact note wording is recorded in the SUMMARY so the remaining files receive an identical block.
  </acceptance_criteria>
  <done>The inventory is verified by a third independent sweep, and the annotation format is proven to add without replacing on one real file.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate every remaining affected Phase 1-3 SUMMARY</name>
  <files>.planning/phases/01-data-foundation-evaluation-harness/01-02-SUMMARY.md, .planning/phases/01-data-foundation-evaluation-harness/01-04-SUMMARY.md, .planning/phases/02-prediction-models-epa-sigma1/02-01-SUMMARY.md, .planning/phases/02-prediction-models-epa-sigma1/02-03-SUMMARY.md, .planning/phases/02-prediction-models-epa-sigma1/02-05-SUMMARY.md, .planning/phases/03-tuning-ranking-points-versioning/03-05-SUMMARY.md, .planning/phases/03-tuning-ranking-points-versioning/03-06-SUMMARY.md, .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-FIGURE-INVENTORY.md — the reconciled `annotate` worklist from Task 1.
    - .planning/phases/02-prediction-models-epa-sigma1/02-06-SUMMARY.md — the annotated exemplar, for the exact note block to replicate.
    - Each file in this task's `files` list — enough of each to place the note above the figures it qualifies and to confirm which figures it is qualifying.
  </read_first>
  <action>
Apply the identical dated superseded-by note block from Task 1 to every remaining file the
reconciled inventory dispositions as `annotate`. The files list on this task reflects the planning
sweep; if Task 1's independent re-sweep found additional files, annotate those too and add them to
this plan's SUMMARY as an authorized deviation, rather than leaving a known-affected file unannotated
because it was not in a list written before the sweep ran.

For each file: add the note, change nothing else. Do not correct a typo, do not reflow a paragraph,
do not update a stale cross-reference, and above all do not adjust a figure — every edit that is not
the note itself widens the diff and weakens the "original numbers intact" property that is the whole
point of D-16's annotate-not-rewrite rule.

Then close every `annotate` row in `03.2-FIGURE-INVENTORY.md` by marking it done in place, and add a
final line to the inventory recording the total count of annotated files.

Finally, verify the additive property across every annotated file at once, not just per file: the
combined `git diff -U0` over all of them must contain zero removed content lines.
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process');const files=execSync('git diff --name-only -- .planning/phases/01-data-foundation-evaluation-harness .planning/phases/02-prediction-models-epa-sigma1 .planning/phases/03-tuning-ranking-points-versioning').toString().trim().split('\n').filter(Boolean);if(files.length<7)throw new Error('expected at least 7 annotated SUMMARYs, found '+files.length+': '+files.join(', '));const d=execSync('git diff -U0 -- '+files.join(' ')).toString();const removed=d.split('\n').filter(l=>/^-[^-]/.test(l));if(removed.length)throw new Error('annotations removed '+removed.length+' content line(s): '+removed.slice(0,3).join(' | '));const fs=require('fs');for(const f of files){const t=fs.readFileSync(f,'utf8');if(!t.includes('Superseded by Phase 3.2'))throw new Error('marker absent in '+f);if(!t.includes('opr-baseline-change.md'))throw new Error('pointer absent in '+f);}console.log('annotated '+files.length+' SUMMARYs, all additive, all carrying the marker and the pointer')"</automated>
  </verify>
  <acceptance_criteria>
    - Every file the reconciled inventory dispositions as `annotate` contains the literal string `Superseded by Phase 3.2`, a date, and a link to `docs/models/opr-baseline-change.md`.
    - At least 8 SUMMARY files are annotated in total across Tasks 1 and 2.
    - The combined `git diff -U0` across every annotated SUMMARY contains zero removed content lines.
    - No annotated file has any edit other than the note block.
    - Every `annotate` row in `03.2-FIGURE-INVENTORY.md` is marked done, and the inventory records the total annotated-file count.
    - Any file annotated beyond this task's declared `files` list is recorded in the SUMMARY as an authorized deviation with the sweep evidence that surfaced it.
  </acceptance_criteria>
  <done>Every Phase 1-3 SUMMARY carrying a retired OPR figure points a reader at the new record, and not one measured number in the execution record changed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct 04-CONTEXT D-11's misattributed figures as dated errata, and close the phase gate</name>
  <files>.planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md</files>
  <read_first>
    - .planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md line 71 — D-11's full text, including the parenthetical `(2025: 0.7539 vs 0.7618; 2026: 0.7819 vs 0.7825)` and the closing sentence about the ridge term keeping the design matrix finite.
    - docs/models/sigma1-tuning-results.md — the Phase-2 starting-position table (which is where 0.7539 and 0.7819 actually come from: the UNTUNED Sigma1 rows) and the § SC-3 Verdict table recording promoted Sigma1 at 0.7657 and 0.7873 against OPR's 0.7618 and 0.7825, passing 8/8.
    - .planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-CONTEXT.md § D-17 — the errata requirement and its reasoning that the correction strengthens D-11's argument.
    - docs/models/opr-baseline-change.md — the new record the errata points at.
  </read_first>
  <action>
**Add a dated errata block to D-11 in `04-CONTEXT.md`.** Leave D-11's original text in place and add
the errata immediately beneath it, clearly marked as a dated correction rather than a silent edit —
04-CONTEXT.md is a decision record and rewriting its text in place would have the same problem
D-16 identifies for SUMMARYs.

The errata must state three things:

1. **The misattribution.** D-11 cites 2025: 0.7539 and 2026: 0.7819 as Sigma1 losing holdout winner
   accuracy to season-pooled OPR. Those are the **untuned** Sigma1 rows from
   `docs/models/sigma1-tuning-results.md`'s Phase-2 starting-position table. The **promoted** Sigma1
   scored 0.7657 and 0.7873 against season-pooled OPR's 0.7618 and 0.7825 and **won both**, and
   SC-3 was recorded as passing 8/8.
2. **Why the correction strengthens D-11 rather than weakening it.** D-11's argument is that a Sigma1
   win against a weaker new baseline must not be mistakable for a goalpost move. That argument is
   *stronger* when Sigma1 was already winning against the harder baseline, because the risk of
   misreading a new win as progress is higher, not lower.
3. **The ridge claim.** D-11 closes by saying the existing ridge term is what keeps the rank-deficient
   early-event design matrix finite. After D-06 there is no ridge term: finiteness comes from the
   minimum-norm pseudo-inverse solve, which is what TBA itself uses, and rank deficiency becomes a
   well-defined minimum-norm answer rather than a divergence risk. Point at
   `docs/models/opr-baseline-change.md` for the measured early-event behavior (SC-5).

**Close the phase gate.** Run and record the results of:
- `pnpm test`
- `pnpm typecheck`
- `pnpm vitest run packages/harness/digest.test.ts`
- `git diff --exit-code data/algorithm-versions/` against the phase's starting commit, proving D-10's
  Sigma1 bit-freeze held from the first commit of the phase to the last

Also confirm the four committed data artifacts this phase produced are all present and tracked:
`data/baselines/opr-season-pooled-published-tuned-v3.json`,
`data/baselines/opr-season-pooled-retired-2026-08.json`,
`data/baselines/opr-event-scoped-2026-08.json`,
`data/diagnostics/opr-event-scope-2026-08.json`.

Record each result in the SUMMARY as a plain pass or fail. A failure here is a phase-level finding,
not something to fix by narrowing a test.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm typecheck &amp;&amp; pnpm vitest run packages/harness/digest.test.ts &amp;&amp; git diff --exit-code data/algorithm-versions/ &amp;&amp; node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/04-publish-live-update-pipeline/04-CONTEXT.md','utf8');for(const v of ['0.7539','0.7819','0.7657','0.7873','0.7618','0.7825'])if(!t.includes(v))throw new Error('errata missing figure '+v);if(!t.includes('opr-baseline-change.md'))throw new Error('errata lacks pointer to the new record');const {execSync}=require('child_process');for(const p of ['data/baselines/opr-season-pooled-published-tuned-v3.json','data/baselines/opr-season-pooled-retired-2026-08.json','data/baselines/opr-event-scoped-2026-08.json','data/diagnostics/opr-event-scope-2026-08.json'])execSync('git ls-files --error-unmatch '+p);console.log('errata complete and all four committed phase artifacts are tracked')"</automated>
  </verify>
  <acceptance_criteria>
    - `04-CONTEXT.md` D-11's original text is unchanged and a dated errata block sits immediately beneath it.
    - The errata names 0.7539 and 0.7819 as the untuned Sigma1 rows, states the promoted figures 0.7657 and 0.7873 against OPR's 0.7618 and 0.7825, and states that SC-3 passed 8/8.
    - The errata explains why the correction strengthens D-11's argument.
    - The errata corrects the ridge-term claim and points at `docs/models/opr-baseline-change.md`.
    - `git diff -U0` on `04-CONTEXT.md` shows added lines only, with zero removed content lines.
    - `pnpm test`, `pnpm typecheck`, and `pnpm vitest run packages/harness/digest.test.ts` all exit 0.
    - `git diff --exit-code data/algorithm-versions/` exits 0 measured against the phase's starting commit.
    - All four committed data artifacts are tracked by git.
    - The SUMMARY records each gate result as a plain pass or fail.
  </acceptance_criteria>
  <done>Phase 4's scope source no longer carries a wrong premise, and the phase closes with every gate measured rather than assumed.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan introduces no new file, symbol, function, type, directory, or CLI flag. It adds an
annotation block to existing SUMMARY files and a dated errata block to an existing CONTEXT file.

New string introduced, for downstream greps: the literal marker `Superseded by Phase 3.2` now appears
in every annotated Phase 1-3 SUMMARY.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completed execution record -> later-phase edit | The only boundary here: a finished phase's measured output being modified by a later phase |
| 04-CONTEXT.md -> Phase 4 planning | A decision record consumed directly as the next phase's scope source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.2-28 | Tampering | Phase 1-3 SUMMARY measured figures | high | mitigate | Every annotation is verified to be additive by checking that `git diff -U0` contains zero removed content lines, per file in Task 1 and across all annotated files in Task 2. The action forbids any edit other than the note block, including typo fixes, because each one widens the diff and weakens the property. |
| T-03.2-29 | Repudiation | dangling superseded-by pointers | medium | mitigate | Task 1's precondition asserts `docs/models/opr-baseline-change.md` is tracked before any note is written, and both tasks' verifies re-check the pointer string in every annotated file. A note pointing at a nonexistent record is worse than no note. |
| T-03.2-30 | Repudiation | 04-CONTEXT D-11's wrong premise reaching Phase 4 | high | mitigate | The correction is a dated errata appended beneath the original rather than a silent in-place edit, so a reader can see both what was decided and what was later found wrong; the additive diff property is asserted here too. |
| T-03.2-31 | Tampering | an incomplete inventory silently leaving a file unannotated | high | mitigate | Three prior lists disagree, so Task 1 runs a fourth independent sweep including a non-numeric prose pass, reconciles it against the inventory, and Task 2 authorizes annotating files beyond its declared list rather than deferring to a list written before the sweep. |
| T-03.2-32 | Tampering | frozen Sigma1 promoted versions | high | mitigate | Task 3 asserts `git diff --exit-code data/algorithm-versions/` against the phase's starting commit, making D-10's bit-freeze a phase-level checked fact rather than a per-plan one. |
| T-03.2-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. 03.2-RESEARCH.md § Package Legitimacy Audit records the not-applicable finding. |
</threat_model>

<verification>
- At least 8 Phase 1-3 SUMMARYs annotated, every one carrying the marker string and the pointer.
- Combined `git diff -U0` across all annotated SUMMARYs and 04-CONTEXT.md contains zero removed content lines.
- 04-CONTEXT.md D-11 carries a dated errata correcting both the misattributed figures and the ridge claim.
- `pnpm test`, `pnpm typecheck`, `pnpm vitest run packages/harness/digest.test.ts` all green.
- `git diff --exit-code data/algorithm-versions/` exits 0 against the phase's starting commit.
- All four committed phase data artifacts tracked by git.

</verification>

<success_criteria>
- Every Phase 1-3 SUMMARY carrying a retired OPR figure has a dated superseded-by note with its original numbers intact (SC-2, SUMMARY half).
- 04-CONTEXT D-11's misattributed figures and its ridge claim are corrected as dated errata (D-17).
- The phase's frozen surfaces held from first commit to last.
</success_criteria>

<output>
Create `.planning/phases/03.2-swap-opr-to-event-scoped-and-re-issue-affected-figures/03.2-06-SUMMARY.md` when done.
</output>