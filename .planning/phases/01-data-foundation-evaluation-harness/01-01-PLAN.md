---
phase: 01-data-foundation-evaluation-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.json
  - vitest.config.ts
  - .env.example
  - scripts/verify-native-module.ts
  - scripts/recon-tba-fields.ts
  - docs/data/tba-field-recon.md
autonomous: false
requirements: [DATA-01, DATA-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pnpm exec tsc --noEmit` exits 0 with strict mode on and typescript resolved at exactly 5.9.3"
    - "`pnpm vitest run` exits 0, resolving its config from vitest.config.ts"
    - "`require('better-sqlite3')` loads on this dev machine without invoking a node-gyp build step"
    - "`git check-ignore .env` exits 0, so the TBA API key cannot be committed"
    - "docs/data/tba-field-recon.md records, for each of 2022, 2023, 2024, 2025 and 2026, whether a TBA-computed per-match ranking-point field is present in score_breakdown — quoting the observed field name, or recording its absence"
    - "docs/data/tba-field-recon.md records the resolved Statbotics per-season accuracy endpoint and its auth requirement, or records that it could not be resolved and names the dated fallback constant to use instead"
    - "The recon script reads the TBA key from the environment and never writes it into its own output document"
  prohibitions:
    - statement: "MUST NOT consult, copy, or port any pre-v3 implementation, model, or tuned value from git history (tag v2-poc) into this codebase — the clean-slate mandate means only the failure log carries forward"
      status: resolved
  artifacts:
    - path: "package.json"
      provides: "Root manifest, pinned toolchain, pipeline scripts"
      contains: "\"typescript\": \"5.9.3\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace declaration reserving apps/* for later phases"
    - path: "tsconfig.json"
      provides: "Strict TypeScript config for the whole source tree"
      contains: "\"strict\": true"
    - path: "vitest.config.ts"
      provides: "Vitest test discovery across packages/ and scripts/"
    - path: ".env.example"
      provides: "Documented placeholder for TBA_API_KEY (never a real key)"
    - path: "scripts/verify-native-module.ts"
      provides: "better-sqlite3 native-load smoke check"
    - path: "scripts/recon-tba-fields.ts"
      provides: "One-shot TBA + Statbotics field reconnaissance probe"
    - path: "docs/data/tba-field-recon.md"
      provides: "Recorded answers to RESEARCH.md Open Questions 1 and 2"
      contains: "2026"
  key_links:
    - from: "scripts/recon-tba-fields.ts"
      to: "docs/data/tba-field-recon.md"
      via: "script writes its observations into the document"
      pattern: "tba-field-recon"
    - from: "package.json"
      to: "scripts/recon-tba-fields.ts"
      via: "npm script invokes it through tsx"
      pattern: "recon"
---

<objective>
Stand up the Node/TypeScript pipeline toolchain for a repository that currently contains zero source
files, behind a supply-chain gate on the three packages RESEARCH.md flagged `[SUS]`, and resolve the two
Open Questions that later plans would otherwise have to guess at.

Purpose: Every other plan in this phase is blocked on a runnable toolchain. Two of those plans are also
blocked on facts nobody has yet observed — whether TBA exposes a computed per-match ranking-point field
for 2022–2026 (which decides whether D-05's "normalize RP awards" is a field read or season-specific
rule logic), and what the Statbotics per-season accuracy endpoint actually is (which decides whether
D-04's reference row is fetched or a dated constant). Both are cheap to answer now and expensive to
discover mid-implementation.

Output: A pinned, installed, test-runnable toolchain; a verified secrets boundary; and
`docs/data/tba-field-recon.md` recording observed facts that Plans 03 and 05 read as input.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation-evaluation-harness/01-CONTEXT.md
@.planning/phases/01-data-foundation-evaluation-harness/01-RESEARCH.md
@REBUILD_SPEC.md
</context>

## Artifacts this phase produces

New symbols and paths created by Plan 01 (none of these exist yet — the repository has no source files):

- Files: `package.json`, `pnpm-workspace.yaml`, `tsconfig.json`, `vitest.config.ts`, `.env.example`,
  `scripts/verify-native-module.ts`, `scripts/recon-tba-fields.ts`, `docs/data/tba-field-recon.md`
- npm scripts: `typecheck`, `test`, `recon:tba`, `verify:native`, `ingest`, `harness` (the last two point
  at entry files created in Plans 02 and 03; `package.json` is owned exclusively by this plan)
- Environment variable contract: `TBA_API_KEY`

## Flagged assumptions carried into execution

- **A3 (RESEARCH.md):** TBA's `score_breakdown` is assumed to expose a computed per-match ranking-point
  field consistently across 2022–2026. Task 3 exists specifically to confirm or refute this. If refuted
  for any season, record it plainly — Plan 03 branches on this document, and a wrong guess here silently
  corrupts every RP value in the corpus.
- **A4 (RESEARCH.md):** The Statbotics per-season accuracy endpoint is assumed to be publicly fetchable
  without auth. A direct probe returned HTTP 500 during research; treat the endpoint as unresolved until
  Task 3 observes a 200.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm legitimacy of the three [SUS]-flagged packages before any install runs</name>
  <read_first>
    - .planning/phases/01-data-foundation-evaluation-harness/01-RESEARCH.md — the `## Package Legitimacy Audit` table, which records download counts, source repos, and the reason each package was flagged
  </read_first>
  <what-built>
    Nothing is installed yet. RESEARCH.md's Package Legitimacy Audit ran `gsd_run query
    package-legitimacy check` against every package this phase needs and returned a `SUS (too-new)`
    verdict for three of them. The verdict came solely from a "most recent version was published very
    recently" heuristic — all three report large download counts and official source repositories — but
    the protocol requires a human to confirm before an install (and therefore any postinstall script)
    runs.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/better-sqlite3 — confirm the repository link points to
       `github.com/WiseLibs/better-sqlite3` and weekly downloads are in the millions.
    2. Open https://www.npmjs.com/package/@types/better-sqlite3 — confirm it is published from
       `github.com/DefinitelyTyped/DefinitelyTyped`.
    3. Open https://www.npmjs.com/package/ml-matrix — confirm the repository link points to
       `github.com/mljs/matrix`.
    4. For each, confirm nothing on the page suggests a recent ownership transfer or an unexpected
       install script.
  </how-to-verify>
  <resume-signal>Type "approved" to permit the install, or name any package to drop and its replacement</resume-signal>
  <done>A human has confirmed all three packages, or named a substitute, before Task 2 installs anything</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the pinned toolchain and verify the native module and secrets boundary</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.json, vitest.config.ts, .env.example, scripts/verify-native-module.ts</files>
  <read_first>
    - .planning/phases/01-data-foundation-evaluation-harness/01-RESEARCH.md — `## Standard Stack` for exact versions, `Pitfall 2` for the TypeScript pin rationale, `Pitfall 3` for the native-module risk
    - .gitignore — confirm the existing `.env` entry before relying on it
    - .claude/CLAUDE.md — project stack constraints
  </read_first>
  <action>
    Create the root `package.json` for a project named `sigmascout`, `"type": "module"`, `"private": true`,
    with `packageManager` set to the pnpm version corepack activates. Enable pnpm first via `corepack
    enable` then `corepack prepare pnpm@latest --activate` — pnpm is not currently on this machine.

    Pin devDependencies to the exact versions RESEARCH.md verified against the registry, using exact
    specifiers with no caret: `typescript` at `5.9.3`, `tsx` at `4.23.12`, `vitest` at `4.1.10`,
    `@types/better-sqlite3` at `9.6.0`, `ml-matrix` at `6.15.0`, plus `@types/node`. Dependencies:
    `better-sqlite3` at `13.0.3`, `zod` at `4.4.3`. The `typescript` pin is exact deliberately — the npm
    `latest` dist-tag for that package now resolves to a 7.x line that ships no compiler API, and a
    caret range would let a later lockfile refresh cross that boundary and break tooling added in Phase 2+.

    Add npm scripts. Declare the full set now, including entry points whose target files later plans in
    this phase create — `package.json` is owned by this plan alone, so no later plan has to edit it and
    contend with a sibling running in the same wave. The set is: `typecheck` running `tsc --noEmit`,
    `test` running `vitest run`, `verify:native` running `tsx scripts/verify-native-module.ts`,
    `recon:tba` running `tsx scripts/recon-tba-fields.ts`, `ingest` running
    `tsx packages/ingest/cli.ts`, and `harness` running `tsx packages/harness/cli.ts`. The last two
    resolve to files that do not exist until Plans 02 and 03; that is expected and they are not invoked
    by this plan's verification.

    Create `pnpm-workspace.yaml` declaring `packages` globs `apps/*` — reserved for the web app and
    Worker that land in Phases 4 and 5. Phase 1's source tree lives under `packages/` as a single
    compilation unit rather than four separate workspace packages; the isomorphic boundary that actually
    matters (`packages/core` must import nothing Node-only, per ARCHITECTURE.md Anti-Pattern 2) is
    enforced by a fitness test in Plan 04 rather than by package manifests. Module layout is explicitly
    Claude's discretion per CONTEXT.md.

    Create `tsconfig.json` with `strict` true, `target` and `lib` at ES2023 or later, `module` and
    `moduleResolution` set for Node ESM (`NodeNext`), `noUncheckedIndexedAccess` true,
    `verbatimModuleSyntax` true, `noEmit` true, and `include` covering `packages`, `scripts`, and
    config files.

    Create `vitest.config.ts` with `test.include` covering `packages/**/*.test.ts` and
    `scripts/**/*.test.ts`, `test.environment` set to `node`, and `test.globals` false (import
    `describe`/`it`/`expect` explicitly).

    Create `.env.example` documenting `TBA_API_KEY=` with a placeholder value and a comment naming
    thebluealliance.com/account as the source. Never copy the real key from `.env` into it.

    Create `scripts/verify-native-module.ts`: import `better-sqlite3`, open an in-memory database,
    create a one-column table, insert and read back one row, close it, and print the resolved SQLite
    library version. Exit non-zero with a readable message on any failure — this is the Pitfall 3 spike
    that must run before Plan 02 designs a corpus on top of the binding.

    Then run the install, run `pnpm verify:native`, run `pnpm typecheck`, and run `git check-ignore .env`
    to confirm the secrets boundary holds before any pipeline code touches the key.
  </action>
  <verify>
    <automated>corepack enable && pnpm install && pnpm typecheck && pnpm verify:native && git check-ignore .env && pnpm ls typescript | grep -E 'typescript 5\.9\.3'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` completes without a node-gyp, Python, or Visual Studio Build Tools error
    - `pnpm verify:native` prints a SQLite version and exits 0
    - `pnpm typecheck` exits 0
    - `git check-ignore .env` exits 0
    - `pnpm ls typescript` output contains `typescript 5.9.3`
    - package.json contains the exact specifier `"typescript": "5.9.3"` with no caret or tilde
    - tsconfig.json contains `"strict": true`
    - `.env.example` exists and the value assigned to TBA_API_KEY in it differs from the value in `.env`
  </acceptance_criteria>
  <done>The toolchain installs, typechecks, loads the native module, and the API key is confirmed unstageable</done>
</task>

<task type="auto">
  <name>Task 3: Probe TBA and Statbotics for the two unresolved field questions and record the answers</name>
  <precondition>TBA_API_KEY is present in the environment (loaded from the untracked .env at repo root)</precondition>
  <files>scripts/recon-tba-fields.ts, docs/data/tba-field-recon.md</files>
  <read_first>
    - .planning/phases/01-data-foundation-evaluation-harness/01-RESEARCH.md — `## Open Questions` items 1 and 2, `Pitfall 4`, and the `### TBA client with ETag conditional requests` example for the header shape
    - .planning/phases/01-data-foundation-evaluation-harness/COVERAGE.md — the two capabilities marked INTEGRATE that this probe exercises (match-detail, Statbotics year-summary)
  </read_first>
  <action>
    Write `scripts/recon-tba-fields.ts`, a one-shot read-only probe that answers two questions and writes
    its observations to `docs/data/tba-field-recon.md`. It reads `TBA_API_KEY` from the environment and
    sends it as the `X-TBA-Auth-Key` header against base URL `https://www.thebluealliance.com/api/v3`.
    It must never print or persist the key itself.

    Question 1 — per-match ranking-point fields. For each season 2022, 2023, 2024, 2025 and 2026, pick one
    known completed event and fetch `/event/{event_key}/matches`, take the first qualification match with
    a non-null `score_breakdown`, and enumerate the top-level key names present under
    `score_breakdown.red`. Record, per season: the event and match key sampled, whether a TBA-computed
    ranking-point total field is present (RESEARCH.md observed `tba_rpEarned` for 2016/2017 and could not
    confirm 2022–2026), the exact field name if present, and otherwise the full sorted list of key names
    that look ranking-point-related so Plan 03 can derive RP from named bonus booleans instead. Where a
    season has no event data yet, record that plainly rather than omitting the row.

    Question 2 — Statbotics per-season accuracy. Attempt the documented per-year endpoint against
    `https://api.statbotics.io`, trying the v3 year path shape first. Record the URL tried, the HTTP
    status, whether auth was required, and — on success — the field names carrying per-season prediction
    accuracy and mean squared error, plus the observed values for 2022–2026. On failure after a small
    number of shape attempts, record the failure and state that D-04's reference row must instead use a
    dated, manually-sourced constant, naming the date the constant was captured.

    Write the results as a dated markdown document with one section per question and a short
    "What Plans 03 and 05 must do with this" closing line per section. This document is read as input by
    Plan 03 (RP normalization) and Plan 05 (the D-04 reference row); it is observation, not design.
  </action>
  <verify>
    <automated>pnpm recon:tba && test -s docs/data/tba-field-recon.md && grep -q 2026 docs/data/tba-field-recon.md && grep -qi statbotics docs/data/tba-field-recon.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm recon:tba` exits 0
    - docs/data/tba-field-recon.md exists and is non-empty
    - The document contains a row or section for each of 2022, 2023, 2024, 2025, 2026
    - For each season the document either names the observed TBA ranking-point field or explicitly records its absence — no season is silently missing
    - The document records the Statbotics URL attempted and the HTTP status observed
    - The document does not contain the value of TBA_API_KEY (assert programmatically against the environment variable, not by eye)
    - scripts/recon-tba-fields.ts sends the key only as a request header and never writes it to stdout or to a file
  </acceptance_criteria>
  <done>Both RESEARCH.md Open Questions are answered by observation and recorded in a document later plans read</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local toolchain | Third-party package code executes at install time and at runtime |
| `.env` → pipeline process | The TBA API key crosses from an untracked file into process memory |
| TBA / Statbotics API → recon output | Untrusted third-party JSON crosses into a document a human reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pnpm installs (better-sqlite3, @types/better-sqlite3, ml-matrix) | high | mitigate | RESEARCH.md Package Legitimacy Audit ran slopcheck; Task 1 is a blocking human checkpoint that must clear before Task 2 runs any install or postinstall script |
| T-01-02 | Information Disclosure | TBA API key in `.env` | high | mitigate | Task 2 asserts `git check-ignore .env` exits 0; `.env.example` carries a placeholder only, asserted to differ from the real value; Task 3's script reads the key from `process.env` and its acceptance criteria assert the key value is absent from the generated document |
| T-01-05 | Tampering | Statbotics / TBA JSON entering the recon document | low | accept | The recon output is a human-read observation document, not corpus input and not executed; a malformed response is recorded as an observation. Corpus-bound TBA JSON is validated at the fetch boundary in Plans 02 and 03, where it actually matters |
</threat_model>

<verification>
- `pnpm install` succeeds from a clean state with the exact pinned versions
- `pnpm typecheck` and `pnpm test` both exit 0 (the test suite is empty at this point; a green empty run proves discovery and config resolve)
- `pnpm verify:native` proves better-sqlite3 loads and executes SQL on this machine
- `git check-ignore .env` exits 0
- `docs/data/tba-field-recon.md` answers both Open Questions with observed data
</verification>

<success_criteria>
- A repository that had zero source files now installs, typechecks, and runs tests
- No package flagged `[SUS]` was installed without explicit human confirmation
- The TBA API key is provably unstageable and provably absent from generated output
- Plans 03 and 05 have observed answers, not assumptions, for RP field availability and the Statbotics endpoint
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-evaluation-harness/01-01-SUMMARY.md` when done
</output>
