---
phase: 03.1-address-phase-1-3-review-warnings-and-doc-drift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ingest/normalize.ts
  - packages/ingest/normalize.test.ts
  - packages/corpus/schema.sql
  - packages/corpus/db.ts
  - packages/corpus/db.test.ts
  - packages/corpus/integrity.test.ts
  - packages/harness/cli.season-carry.test.ts
  - packages/harness/replay.season.test.ts
  - pnpm-workspace.yaml
autonomous: true
requirements: [TBD]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A played, non-tied match whose TBA `winning_alliance` is empty is normalized with a winner derived from the score comparison, so it reaches the scored population instead of being dropped by `selectMatchesChronological`'s `WHERE m.winner IS NOT NULL` clause (D-01, 01-REVIEW WR-06, success criterion 2)."
    - "A derived winner is distinguishable from a TBA-reported one at read time, from one stored fact rather than a recomputation (D-03, DATA-02's explicit-flag discipline)."
    - "A match written before its event row fails at write time rather than vanishing at read time through `selectMatchesChronological`'s inner join (D-02, 01-REVIEW WR-04, success criterion 2)."
    - "Two `openCorpus` calls racing on the same path cannot both believe they hold the write lock (01-REVIEW WR-03)."
    - "`pnpm-workspace.yaml`'s globs match the repository's real package layout (01-REVIEW WR-02)."
    - "Each of WR-02, WR-03, WR-04 and WR-06 carries a regression test that fails against the pre-fix source (success criterion 1)."
  artifacts:
    - packages/corpus/schema.sql
    - packages/corpus/db.ts
    - packages/corpus/integrity.test.ts
    - packages/ingest/normalize.ts
    - packages/ingest/normalize.test.ts
    - pnpm-workspace.yaml
  key_links:
    - "`normalizeMatch` sets `winnerImputed` -> `upsertMatch` writes `winner_imputed` -> `selectImputedWinnerCount` reads it. One stored fact, read by every reporter, never independently recomputed (D-03)."
    - "`openCorpus`'s `foreign_keys = ON` pragma -> `matches.event_key REFERENCES events(event_key)` -> `selectOrphanMatchCount` asserted at 0 (D-02/D-04)."
    - "`acquireWriteLock`'s atomic `wx` create -> the existing `isProcessAlive` stale-lock branch, reached via the `EEXIST` catch rather than a prior existence probe."
---

<objective>
Close the four Phase-1 review warnings that live in the corpus and ingest layer: WR-06 (a played non-tied match with an empty `winning_alliance` is silently dropped), WR-04 (unenforced `matches.event_key` foreign key plus an inner join that can silently drop matches), WR-03 (TOCTOU race in the single-writer corpus lock), and WR-02 (`pnpm-workspace.yaml`'s glob does not match the real package layout).

Purpose: ROADMAP success criterion 2 — "No match TBA reports as played leaves the scored population silently." Both WR-04 and WR-06 are forward-looking guards, not repairs. The 2026-08-19 measurement against the live 104,772-match corpus found the WR-06 population (played, non-tied, empty `winning_alliance`) at 0 corpus-wide and 0 in both digest slices, and the WR-04 orphan population at 0 corpus-wide. Neither can move a committed digest today, and no published figure changes as a result of either. These measurements are locked inputs — do not re-measure them.

Output: a `winner_imputed` corpus column threaded from `normalizeMatch` to a read function, foreign-key enforcement at corpus open, an atomic write-lock acquisition, a corrected workspace glob, and a regression test per warning that fails against the pre-fix source.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-CONTEXT.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-PATTERNS.md
</context>

<planning_notes>
**Spec-less probe fallback — skipped, recorded here rather than silently.** This phase has no `SPEC.md`, so no `## Edge Coverage` or `## Prohibitions` section was supplied and the probe's nothing-to-probe guard fired: the phase has no requirement IDs of its own (`requirements: TBD` — it hardens Phase 1-3 requirements DATA-02, EVAL-02, EVAL-03, ALGO-01, ALGO-04 and ALGO-06 without owning or widening them, and probing those would author out-of-scope predicates into a deliberately fenced remediation phase). No probe-derived predicates were generated. The `must_haves` above are derived from the phase goal and the five ROADMAP success criteria instead.

**Tracer-first — deliberately not applied, recorded here rather than silently.** `TRACER_MODE` is on by default, and the reference's own instruction is to apply it as intended rather than force it. This phase is nine largely-independent warning fixes plus doc reconciliation across four separate subsystems (corpus/ingest, scoring, the OPR solver, the Sigma1 tuning pipeline). There is no single end-to-end path all nine share, so a "thinnest slice through every layer" would be an artificial construct proving nothing about the other eight fixes. Each plan instead leads with the task its siblings depend on, and every task carries a real runnable `<verify>` — the property tracer-first exists to buy.

**Planner discretion exercised in this plan** (recorded per CONTEXT.md's Claude's-Discretion grant): `openCorpus` gains an explicit schema-version guard rather than letting a pre-column corpus fail with SQLite's own column-not-found message at first insert. Rationale: D-02's own spirit is fail at the boundary where the bug is. A named error at open time, naming the file and the exact remedy, costs about six lines and turns a cryptic mid-ingest failure into an actionable one. The same predicate is exported as one helper so the open guard and the integrity test's skip guard cannot drift.
</planning_notes>

<artifacts_this_phase_produces>
Symbols and files this plan creates (new — not pre-existing, so drift verification must not flag them):

| Symbol / path | Kind | Home |
|---|---|---|
| `winnerImputed` | new required field on the `CorpusMatch` interface | `packages/ingest/normalize.ts` |
| `winner_imputed` | new column on the `matches` table | `packages/corpus/schema.sql` |
| `hasWinnerImputedColumn` | new exported function returning boolean | `packages/corpus/db.ts` |
| `selectOrphanMatchCount` | new exported function returning number | `packages/corpus/db.ts` |
| `selectImputedWinnerCount` | new exported function returning number | `packages/corpus/db.ts` |
| `packages/corpus/integrity.test.ts` | new test file | `packages/corpus/` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive the winner TBA left empty, and record that derivation as one stored corpus fact (D-01, D-03, 01-REVIEW WR-06)</name>

  <reversibility rating="costly">Adds a column to `packages/corpus/schema.sql`. `schema.sql` is applied with `CREATE TABLE IF NOT EXISTS`, and this codebase has no `ALTER TABLE` migration path anywhere under `packages/corpus/` — per established project practice (STATE.md, 2026-08-13 precedent) the local `data/corpus.sqlite` is deleted and re-ingested rather than migrated. The corpus is gitignored and disposable by design, and `digest.test.ts`'s committed `digest-slice.json` fixture keeps the bitwise-reproducibility gate green through the rebuild window.</reversibility>

  <files>packages/ingest/normalize.ts, packages/ingest/normalize.test.ts, packages/corpus/schema.sql, packages/corpus/db.ts, packages/corpus/db.test.ts, packages/harness/cli.season-carry.test.ts, packages/harness/replay.season.test.ts</files>

  <read_first>
    - `packages/ingest/normalize.ts` — the file being modified. Read the header doc comment (lines 1-21), the `CorpusMatch` interface (lines 33-53), and `normalizeMatch` (lines 89-128) in full. The header comment's treatment of the `replayed` flag is the documentation convention the new derivation must mirror.
    - `packages/corpus/schema.sql` — the file being modified. The `replayed` column's declaration is the exact boolean-flag style to copy. Note that `red_dqs`, `blue_dqs`, `red_surrogates` and `blue_surrogates` are `TEXT` JSON-array columns, a different pattern — do not follow those.
    - `packages/corpus/db.ts` — `openCorpus` (lines 62-83) and `upsertMatch` (lines ~164-230), including its INSERT column list, its `ON CONFLICT ... DO UPDATE SET` list, and its `.run({...})` parameter object. All three must gain the new field together.
    - `packages/ingest/normalize.test.ts` — the existing tie and unplayed-match describe blocks are the fixture shape for the new cases.
    - `packages/corpus/db.test.ts`, `packages/harness/cli.season-carry.test.ts` (its `match(overrides: Partial<CorpusMatch>)` factory near line 77), `packages/harness/replay.season.test.ts` (the same factory near line 53) — each builds a full `CorpusMatch` literal and will fail `pnpm typecheck` until the new required field is added to its defaults.
    - `.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` section WR-06 — the finding's own Fix prescription.
  </read_first>

  <behavior>
    - Played match (both scores present and non-negative), `winning_alliance` empty, `redScore` greater than `blueScore`: `normalizeMatch` returns winner red and the imputation flag true.
    - Played match, `winning_alliance` empty, `blueScore` greater than `redScore`: returns winner blue and the imputation flag true.
    - Played match with `winning_alliance` reported as red: returns winner red and the imputation flag false — a TBA-reported winner is never overwritten or re-derived.
    - Played match, `winning_alliance` empty, equal scores: returns winner tie and the imputation flag false — the existing tie fallback is unchanged, and a tie is not an imputation.
    - Unplayed match: returns a null winner and the imputation flag false — unchanged from today.
    - A `winning_alliance` value that is neither red, blue, nor empty on a played non-tied match: the score-comparison derivation applies and the imputation flag is true.
    - Round trip: `upsertMatch` of a `CorpusMatch` whose imputation flag is true, followed by a read of the `winner_imputed` column, returns 1; a false flag returns 0.
    - A second `upsertMatch` of the same match key with a false flag overwrites a stored 1 with 0 — the flag tracks the current row, unlike `replayed`, which is deliberately sticky.
  </behavior>

  <action>
Add a required `winnerImputed: boolean` field to the `CorpusMatch` interface in `packages/ingest/normalize.ts`, positioned immediately after the existing `winner` field so the flag sits beside the field it qualifies.

In `normalizeMatch`, extend the existing played block: after the branch that accepts a red or blue `winning_alliance` and after the equal-scores tie branch, add a final else branch that assigns the winner from the score comparison (red when `redScore` exceeds `blueScore`, otherwise blue) and marks the imputation. Track the flag in a local initialised false and set true only inside that new branch, then return it as `winnerImputed`. Every other returned field is unchanged.

Document the derivation in the file's existing header doc comment (lines 1-21), as a new bullet placed next to the bullet that already explains the `replayed` flag. State that TBA's empty `winning_alliance` on a played non-tied match is treated as a reporting gap rather than as a statement about the match, cite D-01 and 01-REVIEW WR-06, and state that the measured population of this case is zero corpus-wide as of 2026-08-19 so this is a forward-looking guard rather than a repair.

Add `winner_imputed INTEGER NOT NULL DEFAULT 0` to the `matches` table in `packages/corpus/schema.sql`, immediately after the `winner TEXT` column, with an inline comment in the same style the `replayed` column uses, stating that the value is synthesized (TBA has no such field) and marks a winner derived from the score comparison rather than reported by TBA.

Wire the column through `upsertMatch` in `packages/corpus/db.ts` in all three places the existing `replayed` column appears: the INSERT column list plus its named value placeholder, the conflict-update assignment list (assigning from `excluded`), and the `.run({...})` parameter object, converting the boolean with the same one-or-zero form `hasScoreBreakdown` already uses. Unlike `replayed`, this flag is not sticky — write it straight from the incoming `CorpusMatch` on every upsert, because it describes the row as currently normalized.

Add the new field with a false default to the `CorpusMatch` literal inside the fixture factory in each of `packages/corpus/db.test.ts`, `packages/harness/cli.season-carry.test.ts` and `packages/harness/replay.season.test.ts`. These are mechanical one-line additions required for `pnpm typecheck` to stay green in the same commit — the change is one coherent thread-through of one column, so splitting it would leave a broken intermediate state.

Add the `normalizeMatch` behavior cases above to `packages/ingest/normalize.test.ts` as a new describe block named for D-01 and WR-06, following the existing tie block's fixture style. Add the round-trip and overwrite cases to `packages/corpus/db.test.ts`.

Do not touch `selectMatchesChronological`'s winner-not-null clause — D-01's whole point is that fewer played matches ever reach that clause with a null winner, not that the clause changes.
  </action>

  <verify>
    <automated>npx vitest run packages/ingest/normalize.test.ts packages/corpus/db.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>Your local `data/corpus.sqlite` predates the `winner_imputed` column. Because `schema.sql` is applied with `CREATE TABLE IF NOT EXISTS`, an existing corpus is never migrated — delete `data/corpus.sqlite` plus its `-wal` and `-shm` siblings and re-run `pnpm ingest` when you next need the local corpus. Nothing in CI is affected: the corpus is gitignored, so CI has never had it, and `digest.test.ts` replays from the committed `packages/harness/fixtures/digest-slice.json` fixture instead. No published number changes.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -n "winnerImputed" packages/ingest/normalize.ts` shows the field declared on the `CorpusMatch` interface and returned from `normalizeMatch`.
    - `grep -n "winner_imputed" packages/corpus/schema.sql` returns exactly one line, and that line contains `INTEGER NOT NULL DEFAULT 0`.
    - `grep -c "winner_imputed" packages/corpus/db.ts` returns at least 3 — the INSERT column list, the conflict-update list, and at minimum one read path added in Task 2.
    - `npx vitest run packages/ingest/normalize.test.ts` passes, and the new describe block covers all six `normalizeMatch` behavior cases listed in `<behavior>`.
    - Regression proof required by success criterion 1: before committing, temporarily revert `normalizeMatch`'s new derivation branch (leaving the interface and the tests in place), confirm the played-non-tied-empty-`winning_alliance` cases go red, then restore. Record the observed failure output in the plan SUMMARY.
    - `npx tsc --noEmit` exits 0 — all three fixture factories carry the new field.
    - `npx vitest run` (full suite) exits 0, including `packages/harness/digest.test.ts`, which re-derives every committed `data/algorithm-versions/*.json` digest bitwise from the committed fixture. Per D-13 a committed digest is never hand-edited; if one moves, stop and report rather than adjusting the expected value.
  </acceptance_criteria>

  <done>A played, non-tied match whose TBA `winning_alliance` is empty normalizes to a score-derived winner carrying an imputation flag of true; that fact is stored in the `winner_imputed` column and survives a write/read round trip; a TBA-reported winner is provably never re-derived; and the whole suite plus typecheck is green with every committed digest unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the event foreign key at corpus open and report both quirk populations (D-02, D-04, 01-REVIEW WR-04)</name>

  <files>packages/corpus/db.ts, packages/corpus/integrity.test.ts</files>

  <read_first>
    - `packages/corpus/db.ts` — the file being modified. `openCorpus` (lines 62-83) with its existing `journal_mode` and `busy_timeout` pragma calls; `readEtag` and `writeEtag` (lines ~335-347) as the minimal single-purpose-query style the new read functions follow; `selectMatchesChronological` (lines ~274-333) for the inner join on `events` this fix protects.
    - `packages/corpus/schema.sql` — the `event_key TEXT NOT NULL REFERENCES events(event_key)` declaration that is currently decorative, and the `winner_imputed` column added in Task 1.
    - `packages/harness/digest.test.ts` lines 28-60 — the exact corpus-present-guard shape to copy: module-level path constant plus an `existsSync` availability constant, with guarded blocks that skip with an explicit named message rather than passing silently. Its top-of-file doc comment (lines 1-27) is the framing to mirror.
    - `.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` section WR-04 — the finding's own Fix prescription.
  </read_first>

  <behavior>
    - `openCorpus` on a fresh path returns a handle whose `PRAGMA foreign_keys` reads back as 1.
    - Inserting a match row whose `event_key` has no matching `events` row, through a handle returned by `openCorpus`, throws a SQLite foreign-key constraint error rather than succeeding silently.
    - `openCorpus` on a database file whose `matches` table lacks the `winner_imputed` column throws a named error containing the database path and the remedy, rather than surfacing SQLite's own column-not-found error later at first insert.
    - `selectOrphanMatchCount` returns the count of `matches` rows with no matching `events` row — 0 on a well-formed corpus.
    - `selectImputedWinnerCount` returns the count of `matches` rows whose `winner_imputed` is 1.
    - `hasWinnerImputedColumn` returns true for a corpus created by the current `schema.sql` and false for one created without the column.
    - The corpus-backed integrity check asserts the orphan count is exactly 0 and only reports, never asserts, the imputed-winner count.
    - With `data/corpus.sqlite` absent, the corpus-backed integrity check skips with a message naming the missing path — never a silent pass.
  </behavior>

  <action>
In `packages/corpus/db.ts`, add a `foreign_keys = ON` pragma call to `openCorpus` as a third `db.pragma(...)` call immediately after the existing `busy_timeout` line and before the schema is applied. This is 01-REVIEW WR-04's own prescribed fix verbatim.

Export a new `hasWinnerImputedColumn(db: Corpus): boolean` that runs `PRAGMA table_info(matches)` and tests whether any returned row's `name` equals `winner_imputed`. In `openCorpus`, after the schema is applied, call it and throw when it returns false. The error message must name the database path, state that the schema is applied with `CREATE TABLE IF NOT EXISTS` so an existing database is never migrated, instruct deleting the file along with its `-wal` and `-shm` siblings and re-running `pnpm ingest`, and note that the corpus is gitignored and disposable by design. Follow `acquireWriteLock`'s existing throw style: descriptive, names the offending path, states what the caller should do. One exported predicate, used by both this guard and the integrity test's skip guard, so the two cannot drift.

Export two single-purpose read functions beside `readEtag` and `writeEtag`, matching their minimal style:
- `selectOrphanMatchCount(db: Corpus): number` — a left join from `matches` to `events` on `event_key`, counting rows where the `events` side is null.
- `selectImputedWinnerCount(db: Corpus): number` — a count of `matches` rows whose `winner_imputed` equals 1.

Give each a doc comment citing D-04 and stating its asymmetry explicitly: an orphan match is never legitimate, so its count is asserted at zero forever; an imputed winner is valid TBA data the project wants surfaced, so its count is reported and never asserted — asserting a number there would eventually turn correct behavior into a red suite and create pressure to hand-edit an expected value.

Create `packages/corpus/integrity.test.ts` with module-level guard constants copied from `packages/harness/digest.test.ts` lines 39-45. Give the file a top-of-file doc comment in `digest.test.ts`'s framing: what it checks, why the two checks are asymmetric, and that it skips with an explicit named message when the corpus is absent rather than passing silently. It must contain:
- A temp-path block needing no corpus (always runs) proving the foreign-key pragma reads back as enabled, that an orphan insert throws, and that `hasWinnerImputedColumn` distinguishes a current-schema database from one whose `matches` table was created without the column.
- A corpus-backed block guarded on both corpus availability and `hasWinnerImputedColumn`, asserting the orphan count equals 0 and logging the imputed-winner count with a labelled message and no assertion on that value. When the guard is false it must skip with a message naming `data/corpus.sqlite` and the reason — absent, or predating the column.
- Open the corpus with `openCorpusReadOnly` for the corpus-backed block so the test never takes the write lock.

Import the Vitest helpers explicitly (`import { describe, expect, it } from "vitest"`) — the repo runs Vitest with `globals: false`.
  </action>

  <verify>
    <automated>npx vitest run packages/corpus/integrity.test.ts packages/corpus/db.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "foreign_keys" packages/corpus/db.ts` shows the pragma inside `openCorpus`, positioned after the `busy_timeout` pragma.
    - `npx vitest run packages/corpus/integrity.test.ts` passes, and its output contains the labelled imputed-winner count line, or — with the corpus absent — the named skip message including `data/corpus.sqlite`.
    - The orphan-insert test proves a real constraint: it must fail against the pre-fix source. Before committing, temporarily remove the foreign-key pragma, confirm the orphan-insert case goes red (the insert succeeds where the test expects a throw), then restore. Record the observed output in the plan SUMMARY.
    - The orphan count is asserted with an `expect(...).toBe(0)` call, and the imputed-winner count's value appears in no `expect` call anywhere in the file. Verify by running `grep -n "selectImputedWinnerCount" packages/corpus/integrity.test.ts` and reading every hit.
    - `npx vitest run` (full suite) exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>Foreign-key enforcement is on at corpus open and proven by a test that fails without it; a stale corpus fails loudly at open with an actionable message; the orphan count is asserted at zero and the imputed-winner count is reported without assertion; and both corpus-backed checks skip with a named message when `data/corpus.sqlite` is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make write-lock acquisition atomic and fix the workspace glob (01-REVIEW WR-03, WR-02)</name>

  <files>packages/corpus/db.ts, packages/corpus/db.test.ts, pnpm-workspace.yaml</files>

  <read_first>
    - `packages/corpus/db.ts` lines 29-60 — `isProcessAlive` and `acquireWriteLock`, the functions being modified. The existing stale-lock branch and its error message must be preserved exactly; only the initial existence-then-write sequence changes.
    - `pnpm-workspace.yaml` — the file being modified. It currently lists a single `apps/*` entry under `packages:`, plus an `allowBuilds` block.
    - `.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` sections WR-03 and WR-02 — both findings state their fix verbatim, and WR-02's is a literal YAML block. CONTEXT.md's Claude's-Discretion note records that neither was separately discussed because neither has a competing policy choice: implement exactly as the review prescribes.
  </read_first>

  <behavior>
    - With no lock file present, `acquireWriteLock` creates it containing the current process id and returns.
    - With a lock file present whose recorded pid is alive, `acquireWriteLock` throws the existing message naming that pid and the lock path — behavior unchanged.
    - With a lock file present whose recorded pid is not alive, `acquireWriteLock` reclaims it and returns — behavior unchanged.
    - With a lock file present whose contents are not a parseable pid, `acquireWriteLock` reclaims it and returns — behavior unchanged.
    - When acquisition throws because the owner is alive, the lock file's contents still hold the original owner's pid — the throwing path never writes.
  </behavior>

  <action>
Rewrite `acquireWriteLock` in `packages/corpus/db.ts` so its first action is an exclusive-create write attempt — `writeFileSync` with a `wx` flag inside a `try` — returning immediately on success. In the `catch`, rethrow anything whose error `code` is not `EEXIST`; on `EEXIST` fall into the existing stale-lock-detection logic: read the recorded pid, throw the existing already-open-for-writing error unchanged when `isProcessAlive` reports it alive, and otherwise reclaim by overwriting the lock file with the current pid. Preserve `isProcessAlive` and the existing error message text exactly. Only the ordering changes, so that the success path is a single atomic syscall attempt rather than a probe followed by a separate write. This is 01-REVIEW WR-03's own prescribed fix.

Add the behavior cases above to `packages/corpus/db.test.ts` against a temp-directory lock path. The regression case that must fail against the pre-fix source is the no-clobber assertion: with a lock file already present whose owner is alive, assert both that acquisition throws and that the lock file's contents still hold the original pid afterwards. Cover the unparseable-contents reclaim case too, since the `EEXIST` path now reaches the pid parse from a different direction than before.

In `pnpm-workspace.yaml`, add a `packages/*` entry to the `packages:` list beneath the existing `apps/*` entry, leaving the `allowBuilds` block untouched. This is 01-REVIEW WR-02's own prescribed fix verbatim.
  </action>

  <verify>
    <automated>npx vitest run packages/corpus/db.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c 'packages/\*' pnpm-workspace.yaml</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "wx" packages/corpus/db.ts` shows the exclusive-create flag inside `acquireWriteLock`, and `grep -n "EEXIST" packages/corpus/db.ts` shows the catch discriminating on that code.
    - `grep -c 'packages/\*' pnpm-workspace.yaml` returns 1, and `grep -c 'apps/\*' pnpm-workspace.yaml` still returns 1 — the existing entry is retained, not replaced.
    - `npx vitest run packages/corpus/db.test.ts` passes with the four lock behavior cases present.
    - Regression proof required by success criterion 1: before committing, temporarily restore the probe-then-write ordering, confirm the no-clobber case goes red (the lock file's contents get overwritten by the second acquirer), then restore the atomic form. Record the observed output in the plan SUMMARY.
    - `pnpm install --frozen-lockfile` completes without a workspace-resolution error after the glob change (run it once; if the lockfile requires updating because the new glob matches a package manifest, stop and report rather than regenerating it silently — no `packages/*` directory carries its own `package.json` today, so the expected outcome is no lockfile change).
    - `npx vitest run` (full suite) exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>Lock acquisition is a single atomic exclusive-create with an `EEXIST` fallback into the unchanged stale-lock path, proven by a test that fails against the probe-then-write ordering; the workspace globs list both `apps/*` and `packages/*`; and install, typecheck, and the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBA JSON to `normalizeMatch` | Third-party match payloads cross here; `winning_alliance` is the specific field whose absence this plan now interprets. |
| `normalizeMatch` output to the corpus row | The point a synthesized fact (`winnerImputed`) becomes an on-disk fact indistinguishable from a fetched one unless flagged. |
| Concurrent processes to `data/corpus.sqlite` | Two `openCorpus` callers race for the single-writer lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Tampering | `normalizeMatch` winner derivation (`packages/ingest/normalize.ts`) | medium | mitigate | A derived winner is stored under its own `winner_imputed` column so it is never indistinguishable from a TBA-reported one; `normalize.test.ts` pins that a reported `winning_alliance` is never overwritten by the derivation. |
| T-03.1-02 | Denial of Service | `openCorpus` foreign-key enforcement (`packages/corpus/db.ts`) | medium | mitigate | Turning enforcement on can abort an ingest that previously completed. Measured orphan population is 0 corpus-wide (2026-08-19), and `integrity.test.ts` asserts it stays 0, bounding the abort surface to genuinely new ordering bugs. |
| T-03.1-03 | Elevation of Privilege | `acquireWriteLock` (`packages/corpus/db.ts`) | low | mitigate | Atomic exclusive-create removes the check-then-write window, so two concurrent writers can no longer both believe they hold the lock; the alive-owner path is proven non-clobbering by test. |
| T-03.1-04 | Information Disclosure | `integrity.test.ts` reported counts | low | accept | The reported output is integer counts only — no team keys, match keys, or credentials. No API key is in scope anywhere in this plan. |
| T-03.1-05 | Repudiation | `openCorpus` stale-schema guard | low | mitigate | A pre-column corpus fails at open with a named, actionable error rather than a cryptic insert-time SQLite message, so the operator can attribute the failure. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency. Severity is low because no install surface exists; verify with `git diff --stat package.json pnpm-lock.yaml` showing no dependency change before commit. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits 0 across the whole suite, including `packages/harness/digest.test.ts` — every committed `data/algorithm-versions/*.json` digest reproduces bitwise. Per D-13 a committed digest is never hand-edited; if one moves, stop and report.
- `npx vitest run packages/corpus/integrity.test.ts` reports either the orphan-zero assertion plus the labelled imputed-winner count, or a named skip citing `data/corpus.sqlite`.
- All four warnings covered by this plan (01-REVIEW WR-02, WR-03, WR-04, WR-06) have a regression test whose pre-fix red state was observed and recorded in the SUMMARY.
</verification>

<success_criteria>
- 01-REVIEW WR-02, WR-03, WR-04 and WR-06 are each fixed in the tree and each carries a regression test proven red against the pre-fix source.
- A played, non-tied match with an empty `winning_alliance` is resolved under D-01's recorded policy and flagged under D-03's stored column, rather than dropped by a `WHERE` clause.
- Foreign-key enforcement is on at corpus open, and the orphan population is asserted at zero by a corpus-guarded test that skips with a named message when the corpus is absent.
- No committed digest moves; no published figure changes.
</success_criteria>

<output>
Create `.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-01-SUMMARY.md` when done. Record: the pre-fix red output observed for each of the four warnings, the exact `openCorpus` stale-schema error text chosen, and whether the local corpus was rebuilt or left stale.
</output>
