---
phase: 03.1-address-phase-1-3-review-warnings-and-doc-drift
plan: 05
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02", "03.1-03", "03.1-04"]
files_modified:
  - scripts/reviewFrontmatterLint.ts
  - scripts/reviewFrontmatterLint.test.ts
  - package.json
  - .planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md
  - .planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md
  - .planning/phases/01-data-foundation-evaluation-harness/01-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements: [TBD]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every Phase 1-3 planning artifact's recorded status matches HEAD: no artifact claims an open finding that is closed, or a closed finding that is open (D-14, success criterion 5)."
    - "Each of the nine warnings is recorded as resolved in its own review file, with the commit that fixed it (success criterion 1's second half)."
    - "A `*-REVIEW.md` whose frontmatter resolution counts disagree with how many findings in its body carry a resolution subsection fails a mechanical check (D-16)."
    - "The linter runs in CI, not only by hand, so this class of drift cannot silently reappear (D-16)."
    - "The correction follows `02-REVIEW.md`'s append-never-rewrite shape: original finding text is unchanged and resolutions are appended per finding (D-14)."
  artifacts:
    - scripts/reviewFrontmatterLint.ts
    - scripts/reviewFrontmatterLint.test.ts
    - .planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md
    - .planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md
    - .planning/phases/01-data-foundation-evaluation-harness/01-VERIFICATION.md
    - .planning/STATE.md
  key_links:
    - "The linter's parsed `resolution.resolved` count -> the number of resolution subsections in the same file's body. One fact, two representations, checked against each other."
    - "Each resolution block's recorded commit -> the actual fix commit from plans 03.1-01 through 03.1-04, which is why this plan is wave 3."
    - "`03-SECURITY.md`'s verified status -> STATE.md's Blockers/Concerns entry, which currently contradicts it."
---

<objective>
Close the four stale planning artifacts identified by the 2026-08-19 milestone audit, and add the narrow mechanical guard that keeps this class of drift from silently reappearing.

Purpose: ROADMAP success criterion 5 — "Every Phase 1-3 planning artifact's recorded status matches HEAD," covering the three REVIEW.md frontmatter blocks, `01-VERIFICATION.md`'s human-verification item, and STATE.md's Blockers/Concerns section. It also closes success criterion 1's second half: each of the nine warnings must be recorded as resolved in its review file with its commit.

Per D-15, the corrections trust the 2026-08-19 audit's re-confirmation rather than re-verifying every claim against the code first. That is a deliberate trade made acceptable by D-16: the linter becomes the ongoing guard, so a one-time transcription step is not the only thing standing between the docs and reality.

Per D-16 the linter is scoped deliberately tight — one mechanical invariant only. A full cross-artifact consistency checker (review-to-resolutions, verification-to-UAT, STATE blockers-to-security) was rejected: it would have caught all four cases here, but it means parsing prose in STATE.md and is a meaningful tooling build that risks becoming its own phase. Relying on existing GSD tooling was also rejected — `gsd-tools audit-open` was run during the discussion and reported all artifact types clear while two REVIEW.md files carried an issues-found status, so it does not detect this class of drift.

Output: a dependency-free review-frontmatter linter wired into the test suite, and four corrected artifacts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-CONTEXT.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-PATTERNS.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-01-SUMMARY.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-02-SUMMARY.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-03-SUMMARY.md
@.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-04-SUMMARY.md
</context>

<planning_notes>
**Spec-less probe fallback — skipped, recorded here rather than silently.** This phase has no `SPEC.md`, so neither `## Edge Coverage` nor `## Prohibitions` was supplied and the probe's nothing-to-probe guard fired: the phase has no requirement IDs of its own (`requirements: TBD`; the IDs it names belong to Phases 1-3 and are hardened, not owned, here). No probe-derived predicates were generated. The `must_haves` above are derived from the phase goal and the five ROADMAP success criteria instead.

**Tracer-first — deliberately not applied.** See `03.1-01-PLAN.md`'s planning notes for the full rationale.

**Why this plan is wave 3.** Every resolution block must record the commit that fixed its finding, so this plan cannot run until plans 03.1-01 through 03.1-04 have landed their commits. That is a genuine ordering dependency, not a file conflict.

**Planner discretion exercised in this plan** (CONTEXT.md grants the linter's form explicitly):

- **The linter is both a standalone script and a Vitest test, sharing one implementation.** `scripts/reviewFrontmatterLint.ts` exports the checking function and also runs as a script under `tsx` for a human; `scripts/reviewFrontmatterLint.test.ts` imports the same exported function and runs it over the real `.planning/phases/*/[0-9]*-REVIEW.md` files. Rationale: the repo's `vitest.config.ts` already includes `scripts/**/*.test.ts` and `scripts/secrets-boundary.test.ts` is the existing precedent for a policy check living there, so wiring the test costs nothing and makes the guard a CI gate rather than a command someone must remember. The script form gives a readable local run. One implementation, so the two forms cannot disagree.
- **Dependency-free parsing.** The frontmatter blocks in these files are simple flat and one-level-nested key-value YAML, and the repo has no YAML or frontmatter parser today. Per CLAUDE.md's stated stance against adding a dependency preemptively, the linter hand-rolls a small parser for exactly the keys it needs rather than pulling in a YAML library.

- **A deliberate reading of D-14 that the executor must not "correct".** D-14's phrasing says `01-REVIEW.md`'s `critical: 1` / `status: issues_found` become resolved. Read literally against the 02-REVIEW precedent D-14 itself names as the shape to follow, that would introduce new drift rather than remove it, for two reasons. First, `findings:` counts record what the review *found* at review time; `02-REVIEW.md` left its own `findings:` block untouched and added a separate `resolution:` block instead. Second, three info-level findings in `01-REVIEW.md` and two in `03-REVIEW.md` remain genuinely open by design (explicitly out of scope for this phase), so flipping `status` to a resolved value would make an artifact claim a closed finding that is open — the exact failure success criterion 5 forbids. Therefore: keep `findings:` untouched, add a `resolution:` block in `02-REVIEW.md`'s exact shape with accurate resolved/open counts and open ids, and leave `status: issues_found` in place because info findings remain open — matching `02-REVIEW.md`, which also still reads `issues_found` for exactly that reason. This satisfies D-14's intent (the frontmatter stops misrepresenting HEAD) without manufacturing a new false claim.
</planning_notes>

<artifacts_this_phase_produces>
Symbols and files this plan creates (new — not pre-existing, so drift verification must not flag them):

| Symbol / path | Kind | Home |
|---|---|---|
| `scripts/reviewFrontmatterLint.ts` | new module and script | `scripts/` |
| `scripts/reviewFrontmatterLint.test.ts` | new test file | `scripts/` |
| `lintReviewFile` | new exported function | `scripts/reviewFrontmatterLint.ts` |
| `ReviewLintProblem` | new exported result type | `scripts/reviewFrontmatterLint.ts` |
| `lint:reviews` | new npm script | `package.json` |
| `resolution:` frontmatter block | new frontmatter key | `01-REVIEW.md`, `03-REVIEW.md` |
| Per-finding resolution subsections | new body sections | `01-REVIEW.md`, `03-REVIEW.md` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A review file whose frontmatter and body disagree about resolutions fails a mechanical check (D-16)</name>

  <files>scripts/reviewFrontmatterLint.ts, scripts/reviewFrontmatterLint.test.ts, package.json</files>

  <read_first>
    - `.planning/phases/02-prediction-models-epa-sigma1/02-REVIEW.md` lines 52-64 — the `findings:` block followed by the `resolution:` block with its `resolved`, `open`, `open_ids`, `resolved_at` and `note` keys, then `status:`. This is the exact frontmatter shape the linter parses and the shape Task 2 writes.
    - `.planning/phases/02-prediction-models-epa-sigma1/02-REVIEW.md` lines 79-90 — the Resolution Summary table, and the per-finding resolution subsections further down the body. The subsection heading form is what the linter counts.
    - `.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` lines 44-50 — the `findings:` block and `status:` line with no `resolution:` key, the pre-correction state.
    - `.planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md` lines 65-72 — the same pre-correction state.
    - `scripts/secrets-boundary.test.ts` — the existing precedent for a policy check living under `scripts/` and running in the Vitest suite. Follow its structure and its explicit Vitest import.
    - `vitest.config.ts` — confirms the include glob already covers `scripts/**/*.test.ts`, `environment: "node"`, and `globals: false`.
    - `package.json` lines 7-18 — the existing scripts block, to add the new entry in the same style (the project runs one-off TypeScript through `tsx`).
    - `.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-CONTEXT.md` D-16 — the recorded scope and both rejected alternatives.
  </read_first>

  <behavior>
    - Given a file whose frontmatter has no `resolution:` key and whose body has no resolution subsections, the check reports no problem — a fresh, unresolved review is valid.
    - Given a file whose frontmatter has no `resolution:` key but whose body contains one or more resolution subsections, the check reports a problem naming the file and the body count.
    - Given a file whose `resolution.resolved` count equals the number of resolution subsections in its body, and whose `resolved` plus `open` equals `findings.total`, and whose `open_ids` list length equals `open`, the check reports no problem.
    - Given a file whose `resolution.resolved` count disagrees with the number of resolution subsections in its body, the check reports a problem naming both numbers.
    - Given a file whose `resolution.resolved` plus `resolution.open` does not equal `findings.total`, the check reports a problem naming all three numbers.
    - Given a file whose `open_ids` list length does not equal `resolution.open`, the check reports a problem naming both.
    - Given a file whose `resolution.open` is zero while `status` still reads the issues-found value, the check reports a problem — a status contradicting its own resolution counts is the drift this linter exists to catch.
    - Given a file with no frontmatter delimiters at all, the check reports a problem naming the file rather than throwing.
    - Run over every real review file in the repository at HEAD, the check reports zero problems after Task 2 lands.
    - The script form exits with a non-zero code when any problem is found and prints each problem on its own line; it exits zero and prints a count when clean.
  </behavior>

  <action>
Create `scripts/reviewFrontmatterLint.ts` as a dependency-free module that both exports its checking function and runs as a script.

Export `lintReviewFile(path: string, contents: string): ReviewLintProblem[]`, returning one entry per violation rather than throwing, so a run can report every problem at once. Export the `ReviewLintProblem` type carrying at least the file path and a human-readable message.

Hand-roll the parsing — do not add a YAML dependency. Split the leading frontmatter block on its delimiter lines. From it, extract: the `total` value nested under `findings`, and the `resolved`, `open` and `open_ids` values nested under `resolution`, plus the top-level `status` value. Treat an absent `resolution:` key as distinct from a present one whose counts are zero. Parse `open_ids` from its inline bracketed-list form. From the body (everything after the frontmatter), count occurrences of the per-finding resolution subsection heading, matching the heading form `02-REVIEW.md` uses at the start of a line and tolerating a trailing parenthetical date.

Implement each behavior case above as its own check with its own message, and make every message name the file and the specific numbers that disagree — the point of the linter is that a human reading its output knows which fact to correct without opening the file.

Add a script entry point guarded so it only runs when the module is executed directly, not when imported by the test. It should glob the review files under `.planning/phases/`, run the check over each, print each problem on its own line, print a clean count when there are none, and set a non-zero exit code when any problem was found.

Give the file a top-of-file doc comment recording D-16's scope and both rejected alternatives verbatim in substance: a full cross-artifact consistency checker was rejected because it means parsing prose in STATE.md and is a meaningful tooling build that risks becoming its own phase, and relying on existing GSD tooling was rejected because `gsd-tools audit-open` reported all artifact types clear while two review files carried an issues-found status, so it does not detect this class of drift. State plainly what this linter does not check, so a future reader does not mistake its narrowness for completeness.

Create `scripts/reviewFrontmatterLint.test.ts` covering every behavior case above. Build the synthetic cases from inline string fixtures rather than temp files where possible, since `lintReviewFile` takes contents directly. Add one case that runs the check over every real review file found under `.planning/phases/` and asserts zero problems — that is the case which enforces Task 2's corrections and keeps future reviews honest. Import the Vitest helpers explicitly.

Add a `lint:reviews` entry to `package.json`'s scripts block, invoking the module through `tsx` in the same style as the existing `identifiability` and `tune` entries.
  </action>

  <verify>
    <automated>npx vitest run scripts/reviewFrontmatterLint.test.ts</automated>
    <automated>npx tsx scripts/reviewFrontmatterLint.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export function lintReviewFile" scripts/reviewFrontmatterLint.ts` returns a match.
    - `grep -c "^import" scripts/reviewFrontmatterLint.ts` shows imports only from `node:` builtins — no YAML or frontmatter library was added. Confirm with `git diff --stat package.json` showing a scripts-block change and no dependency change.
    - `grep -n "lint:reviews" package.json` returns a match.
    - `npx vitest run scripts/reviewFrontmatterLint.test.ts` passes with a case for each of the ten behavior items.
    - `npx tsx scripts/reviewFrontmatterLint.ts` exits 0 after Task 2 lands, and its output names the number of files checked.
    - Regression proof required by success criterion 1's spirit: run the script against `01-REVIEW.md` and `03-REVIEW.md` as they stand before Task 2's corrections and record the reported problems in the plan SUMMARY. That output is the evidence the linter would have caught the drift this phase is fixing.
    - The real-files case is present in the test file and is not skipped — verify by reading it.
    - `npx vitest run` (full suite) exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>A dependency-free linter checks exactly one mechanical invariant — that a review file's frontmatter resolution counts agree with its body's resolution subsections and with its own findings total and status — runs both as a script and as a CI-gating Vitest test from one shared implementation, reports every problem with the file and the disagreeing numbers named, and its output against the pre-correction review files is recorded as evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the four stale artifacts with HEAD (D-14, D-15)</name>

  <files>.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md, .planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md, .planning/phases/01-data-foundation-evaluation-harness/01-VERIFICATION.md, .planning/STATE.md</files>

  <precondition>The fix commits from plans 03.1-01 through 03.1-04 exist in `git log`, so each resolution block can name a real short SHA. Assert by running `git log --oneline -20` and confirming a commit for each of the nine warnings before writing any resolution block; halt and report if any is missing.</precondition>

  <read_first>
    - `.planning/phases/02-prediction-models-epa-sigma1/02-REVIEW.md` — read the frontmatter (lines 52-64), the Resolution Summary section and its finding/status/commit table (lines 79-90), and at least two per-finding resolution subsections in the body. This is the precedent D-14 names, and this task must match it in shape: original finding text unchanged, a resolution subsection appended under each finding, opening with a resolved-status line naming the short SHA and the commit subject, followed by a plain-language account of what changed and why alternatives were rejected.
    - `.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` — the file being modified. Read its frontmatter and the full CR-01 and WR-01 through WR-06 findings so each appended resolution answers the finding actually written.
    - `.planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md` — the file being modified. Read its frontmatter and WR-01 through WR-03 in full (lines 112-320).
    - `.planning/phases/01-data-foundation-evaluation-harness/01-VERIFICATION.md` — the frontmatter `human_verification` list (lines 35-42) and the Human Verification Required section in the body (around lines 202-226), plus the verdict line at line 51 and the status line at line 48, all of which assert the item holds the phase at a human-needed status.
    - `.planning/phases/01-data-foundation-evaluation-harness/01-UAT.md` — its frontmatter status and the tests that closed the report visual check. Identify the specific test entries by reading the file; the audit records them as tests 30 and 31, both passing, recorded 2026-08-19T03:12Z.
    - `.planning/STATE.md` lines 159-169 — the Blockers/Concerns list, specifically the final entry asserting that a security threat remains formally open until the security phase re-runs.
    - `.planning/phases/03-tuning-ranking-points-versioning/03-SECURITY.md` lines 1-10 — the frontmatter reading a verified status with zero open threats, updated 2026-08-19. This is the source of truth STATE.md currently contradicts.
    - `.planning/v1.0-MILESTONE-AUDIT.md` frontmatter `tech_debt:` block — the authoritative enumeration of exactly these four items and their evidence.
    - The four SUMMARY files from plans 03.1-01 through 03.1-04, for the commit SHAs, the policies chosen, and the alternatives rejected that each resolution block must record.
  </read_first>

  <action>
Correct four artifacts. Follow `02-REVIEW.md`'s append-never-rewrite discipline throughout: never edit an original finding's text, and never delete a recorded historical claim — append the correction beside it.

**`01-REVIEW.md`.** Add a `resolution:` block to the frontmatter, positioned between the existing `findings:` block and the `status:` line, in `02-REVIEW.md`'s exact key shape (`resolved`, `open`, `open_ids`, `resolved_at`, `note`). Set `resolved` to 7 (CR-01 plus WR-01 through WR-06), `open` to 3, and `open_ids` to the three info-level ids. Leave the `findings:` counts exactly as they are — they record what the review found, and `02-REVIEW.md` set that precedent. Leave `status:` reading its current value, because the three info findings genuinely remain open and are explicitly out of scope for this phase; `02-REVIEW.md` reads the same value for the same reason. Do not flip it to a resolved value: doing so would replace one false claim with another, which is exactly what success criterion 5 forbids. Record that reasoning in the `note` field.

Add a Resolution Summary section to the body, matching `02-REVIEW.md`'s placement and its finding/status/commit table shape, with one row per finding. CR-01's row credits quick task 260819-2x6 (commit e70b31df, already in history). Each of WR-01 through WR-06 gets the short SHA from this phase's own plans. Append a per-finding resolution subsection under each of the seven resolved findings, following the precedent's structure: a resolved-status opening line naming the short SHA and the commit subject, then a plain-language account of what changed, which policy was chosen, and which alternative was rejected and why. Pull that content from the four plan SUMMARY files rather than re-deriving it. Note in WR-01's and WR-04's and WR-06's subsections that the measured populations were zero, so these are forward-looking guards rather than repairs and no published figure changed. Leave the three info findings untouched with no resolution subsection.

**`03-REVIEW.md`.** Apply the same treatment: a `resolution:` frontmatter block with `resolved` at 3, `open` at 2, and the two info-level ids; the `findings:` counts and `status:` line unchanged for the same reason; a Resolution Summary table; and a per-finding resolution subsection under WR-01, WR-02 and WR-03 sourced from plan 03.1-04's SUMMARY. Leave both info findings untouched.

**`01-VERIFICATION.md`.** Change the `human_verification` entry's status field from its outstanding value to a resolved one, and add sibling fields recording how it closed: the closing artifact (`01-UAT.md`), the specific test entries that closed it (identify them by reading the UAT file — the audit records them as tests 30 and 31), the recorded timestamp, and a note that the verification ran roughly sixteen minutes before the UAT closed the same item, which is why it went stale. Do not delete the entry, its `why_human` text, or its `carried_from` provenance.

In the body's Human Verification Required section, append a dated correction paragraph beneath the existing text rather than rewriting it, stating that the item is now closed by the UAT artifact and naming it. The existing body text asserting no UAT artifact exists is a historical statement that was true when written — leave it and let the appended correction supersede it, matching the append-never-rewrite discipline. Update the frontmatter `status` and the body's status and verdict lines only if the human-verification section being empty of outstanding items is what the verification decision tree requires for a passed status; the body's own verdict line states that rule explicitly, so read it and follow what it says rather than guessing.

**`STATE.md`.** In the Blockers/Concerns list, correct the final entry's trailing claim that the security threat remains formally open until the security phase re-runs. It was re-run: `03-SECURITY.md` reads a verified status with zero open threats as of 2026-08-19. Follow the entry's own existing convention — it already uses strikethrough plus a bolded resolution marker for the superseded portion — so strike the stale sentence and append the correction naming `03-SECURITY.md` and its recorded status, rather than deleting the sentence.

Also append a Roadmap Evolution or Blockers note recording that this phase closed the nine review warnings, so STATE.md itself reflects HEAD.

Per D-15 these corrections trust the 2026-08-19 audit's re-confirmation rather than re-verifying each claim against the code first — that is the recorded trade, made acceptable by Task 1's linter. Do not spend the plan's budget re-deriving what the audit already established. The one thing you must verify directly is the set of commit SHAs, because those are new facts this phase created rather than facts the audit recorded.

Per the SUMMARY-and-VERIFICATION note in CONTEXT.md's Specific Ideas, write these resolution accounts in plain language rather than review jargon.
  </action>

  <verify>
    <automated>npx tsx scripts/reviewFrontmatterLint.ts</automated>
    <automated>npx vitest run scripts/reviewFrontmatterLint.test.ts</automated>
    <automated>git log --oneline -25</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsx scripts/reviewFrontmatterLint.ts` exits 0 and reports zero problems across all three review files.
    - `grep -c "^#### Resolution" .planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` returns 7, and the same command against `03-REVIEW.md` returns 3.
    - `grep -n "resolution:" .planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md .planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md` shows the new frontmatter block in both files.
    - Every resolution subsection names a short SHA that resolves: for each, run `git show --oneline -s <sha>` and confirm it exists and its subject matches the one recorded.
    - `grep -n "outstanding" .planning/phases/01-data-foundation-evaluation-harness/01-VERIFICATION.md` no longer matches the `human_verification` status field (it may still match historical prose, which is left intentionally).
    - `grep -n "secure-phase 3" .planning/STATE.md` shows the stale claim struck through with an appended correction naming `03-SECURITY.md`, not deleted.
    - The original text of every finding in both review files is byte-identical to its pre-task state — confirm with `git diff` that the only additions under each finding are appended resolution subsections, and that no line inside an original finding body was modified.
    - The three info-level findings in `01-REVIEW.md` and the two in `03-REVIEW.md` have no resolution subsection — verify by reading, since they are explicitly out of scope.
    - `npx vitest run` (full suite) exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>All nine warnings are recorded as resolved in their own review file with a verifiable commit SHA, in `02-REVIEW.md`'s append-never-rewrite shape; both review files carry accurate resolution frontmatter with the still-open info findings honestly counted; `01-VERIFICATION.md`'s human-verification item records its closure by the UAT artifact; STATE.md's security claim is corrected against `03-SECURITY.md`; and the linter reports zero problems across all three review files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts to a future reader or agent | A stale recorded status is consumed as fact by later phases, audits, and agents. |
| Filesystem to the linter | The linter reads arbitrary markdown from `.planning/` and parses it without a schema library. |
| The linter's verdict to CI | A green suite is taken as evidence that no review-status drift exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-22 | Repudiation | Stale recorded status in `01-REVIEW.md`, `03-REVIEW.md`, `01-VERIFICATION.md` and STATE.md | high | mitigate | All four are corrected against HEAD, each resolution names a verifiable commit SHA checked with `git show`, and the append-never-rewrite discipline preserves the original claims so the correction is auditable rather than a silent overwrite. |
| T-03.1-23 | Spoofing | A resolution block naming a commit that does not exist or does not do what it claims | medium | mitigate | Every recorded short SHA must resolve through `git show --oneline -s` with a matching subject, and the plan's precondition halts the task if any of the nine fix commits is missing. |
| T-03.1-24 | Tampering | Overwriting an original finding's text while appending a resolution | medium | mitigate | `git diff` review is an acceptance criterion: the only permitted change inside a finding is an appended subsection, never a modified line. |
| T-03.1-25 | Denial of Service | A malformed or missing frontmatter block crashing the linter | low | mitigate | The linter returns a problem entry rather than throwing for a file with no frontmatter delimiters, and that behavior is a required test case, so a stray markdown file cannot break the suite. |
| T-03.1-26 | Repudiation | Mistaking the linter's narrow scope for a full consistency guarantee | medium | mitigate | The linter's top-of-file doc comment states plainly what it does not check, and the deferred full cross-artifact checker is recorded in CONTEXT.md's Deferred Ideas rather than implied to be covered. |
| T-03.1-27 | Information Disclosure | The linter reading and printing planning-file contents | low | accept | It reads only `.planning/phases/*/[0-9]*-REVIEW.md`, which are committed, and prints only counts, ids and file paths — never file bodies. No credential is in scope. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages: the linter deliberately hand-rolls its parsing rather than adding a YAML dependency, per CLAUDE.md's stated stance. Severity is low because no install surface exists. Verify with `git diff --stat package.json pnpm-lock.yaml` showing a scripts-block change only and no dependency change. |
</threat_model>

<verification>
- `npx tsx scripts/reviewFrontmatterLint.ts` exits 0 with zero problems across all three review files.
- `npx vitest run` exits 0 across the whole suite, including the linter's real-files case, and `npx tsc --noEmit` exits 0.
- Every short SHA recorded in a resolution block resolves via `git show --oneline -s` with a matching subject.
- `git diff` confirms no original finding text was modified in either review file — only appended subsections and new frontmatter keys.
- The pre-correction linter output against `01-REVIEW.md` and `03-REVIEW.md` is recorded in the SUMMARY as evidence the guard catches this class of drift.
</verification>

<success_criteria>
- All nine warnings are recorded as resolved in their review file with the commit that fixed each (success criterion 1's second half).
- Every Phase 1-3 planning artifact's recorded status matches HEAD: no artifact claims an open finding that is closed, or a closed finding that is open (success criterion 5).
- The narrow review-frontmatter invariant is enforced by a dependency-free check that runs in CI.
- Out-of-scope info-level findings remain honestly recorded as open.
</success_criteria>

<output>
Create `.planning/phases/03.1-address-phase-1-3-review-warnings-and-doc-drift/03.1-05-SUMMARY.md` when done. Record: the linter's output against the two review files before correction; the nine short SHAs recorded, one per warning; the final `resolution:` counts written into each review file; the specific UAT test entries that closed the verification item; and confirmation that no original finding text was modified.
</output>
