---
phase: 03.1-address-phase-1-3-review-warnings-and-doc-drift
verified: 2026-08-20T23:45:00Z
status: gaps_found
score: 4/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  previous_verified: 2026-08-20T23:00:00Z
  gaps_closed:
    - "01-REVIEW.md WR-01 resolution subsection now names commit d73d5aba and the widened `!Number.isFinite(denom)` condition."
    - "01-REVIEW.md WR-02 resolution subsection now states pnpm-workspace.yaml has both globs commented out (c09b4b57), records this as a resolution-by-redefinition, and flags the original forward-looking concern as arguably reopened under the accepted trade-off."
    - "01-REVIEW.md WR-03 resolution subsection now names commit 280b1e7e and states full closure required both commits, not 8dec3a05 alone."
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "Every Phase 1-3 planning artifact's recorded status matches HEAD — no artifact claims an open finding that is closed, or a closed finding that is open (ROADMAP success criterion 5)."
    status: failed
    reason: "A newly-discovered instance of the same drift class, in a different artifact than the one closed by this gap-closure commit. STATE.md's Blockers/Concerns section (line 187) states, in the present tense with no strikethrough/correction, that epa.ts's predict() 'sums a team's own learned foulsCommitted into its own score rather than the opponent's... not fixed by plan 02-04.' This is false at HEAD: predict() (packages/core/algorithms/epa.ts:349-364, confirmed by direct read) explicitly EXCLUDES an alliance's own FOULS_COMMITTED_COMPONENT and ADDS the opposing alliance's — the D-04 cross-attribution fix. The fix landed same-day in commit a0ec5d54 (`fix(02): EPA predict attributes foulsCommitted to the opposing alliance (D-04)`), and WINDOWS.md's own ledger entry — the exact one STATE.md's line cites as its source ('logged to WINDOWS.md entry 3') — records status `fixed`, `resolved_at: 2026-08-14T06:40:51.008Z`, with a reason describing this exact fix. STATE.md's line was never updated to reflect that its own cited source closed the item. This was not part of the three items this gap-closure commit (e66f7c22) addressed, and was not caught by the prior verification pass (which scoped its check to the three 01-REVIEW.md subsections)."
    artifacts:
      - path: ".planning/STATE.md"
        issue: "Line 187 (Blockers/Concerns section) asserts a present-tense, currently-active bug in epa.ts's predict() that does not exist at HEAD, and asserts it was 'not fixed by plan 02-04' without noting a same-day, cited-source-confirmed fix (a0ec5d54) closed it."
    missing:
      - "An appended, dated correction to STATE.md's line 187 — in the same strikethrough-and-append style the two adjacent bullets in the same section already use — stating that WINDOWS.md ledger entry 3 records this as `fixed` (resolved_at 2026-08-14T06:40:51.008Z, commit a0ec5d54), and that predict() at HEAD already implements the correct D-04 cross-alliance attribution."
      - "A pass over STATE.md's Blockers/Concerns section (and any other cross-phase ledger this project maintains, e.g. WINDOWS.md itself, which is internally consistent and does not need correction) confirming no other bullet describes a since-fixed issue in present tense."
deferred: []
behavior_unverified_items: []
human_verification: []
---

# Phase 03.1: Address Phase 1-3 Review Warnings and Doc Drift Verification Report

**Phase Goal:** Every open code-review warning from Phases 1-3 is fixed under a recorded policy, and every Phase 1-3 planning artifact's stated status matches the tree it describes
**Verified:** 2026-08-20T23:45:00Z
**Status:** gaps_found
**Re-verification:** Yes — after gap closure (prior pass 2026-08-20T23:00:00Z, `gaps_found`, 4/5)

**Verdict in one line:** The three targeted corrections to `01-REVIEW.md` (WR-01/WR-02/WR-03) are accurate, complete, and correctly append-only — each independently re-derived against HEAD, not trusted from the correction text. Criteria 1-4 still hold, unchanged (the gap-closure commit was docs-only; no source touched). But adversarially re-checking "no OTHER artifact in the Phase 1-3 set is now stale" surfaced a fourth, previously-unflagged instance of the exact same drift class in `STATE.md`'s Blockers/Concerns section — unrelated to the three items this pass closed, and not something this gap-closure commit touched.

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | All nine open review warnings are resolved in the tree, each carrying a regression test that fails against the pre-fix source, and each review file records the resolution with its commit | ✓ VERIFIED (was ⚠️ FAILED partial) | All nine fixes remain genuinely present in the tree (unchanged since prior pass — no source file touched by the gap-closure commit). The recording half, previously failing for three of nine, now holds: `01-REVIEW.md`'s WR-01, WR-02, WR-03 `#### Resolution` subsections each carry a `**Correction (2026-08-20, phase 03.1 code-review gate).**` paragraph, independently re-verified against HEAD below. |
| 2 | No match TBA reports as played leaves the scored population silently: FK enforcement on at corpus open, a played non-tied match with empty `winning_alliance` resolved under a recorded policy | ✓ VERIFIED (regression check) | `packages/corpus/db.ts:125` still sets `PRAGMA foreign_keys = ON`, unchanged. No source file in this area was touched by the gap-closure commit (docs-only). Orchestrator's pre-verification run confirms `data/algorithm-versions/` byte-identical and full suite green at the code-freeze commit 896fb09b, which e66f7c22 builds on with zero source diff. |
| 3 | A malformed prediction cannot reach a published number: non-finite or out-of-[0,1] `pRedWin` fails loudly at the scoring boundary | ✓ VERIFIED (regression check) | `assertValidPRedWin` wiring unchanged; not touched by the gap-closure commit (confirmed via `git show --stat` on the correction commit — zero non-`01-REVIEW.md` files). |
| 4 | Neither long-running numerical guarantee rests on coincidence: incremental OPR solve proven finite/tolerant at season scale; `isValidParamSet` enforced at every Sigma1 candidate-generation and promotion boundary | ✓ VERIFIED (regression check) | Re-read `packages/core/algorithms/opr.ts:417` directly this pass: guard is still `!Number.isFinite(residual) \|\| !Number.isFinite(denom) \|\| denom <= 0`, matching both the WR-01 correction's claim and the CR-01 fix. `Sigma1ParamsSchema`'s `.check(...)` and `makeSigma1`'s new `Sigma1ParamsSchema.parse(...)` call (added by `11087dd8`, confirmed by direct `git show`) are unaffected by the docs-only gap-closure commit. |
| 5 | Every Phase 1-3 planning artifact's recorded status matches HEAD — covering the three REVIEW.md frontmatter blocks, `01-VERIFICATION.md`'s human-verification item, and STATE.md's Blockers/Concerns section | ✗ FAILED (was ✗ FAILED, different underlying cause) | The three `01-REVIEW.md` corrections that failed this truth last pass are now accurate (see Correction Verification below). `02-REVIEW.md` and `03-REVIEW.md`'s resolution subsections re-checked and remain accurate — none of the four 03.1-code-review-gate commits (`d73d5aba`, `280b1e7e`, `11087dd8`, `c09b4b57`) touch any file those two review files' resolutions describe (`tune.ts`, `promote.ts`, `cli.ts`, `params.ts`, `searchSpace.ts`, `epa.ts`'s D-05 fallback region, `replay.ts`) — confirmed by reading each commit's `--stat`. `01-VERIFICATION.md`'s human-verification item remains correctly closed. **But** `STATE.md`'s Blockers/Concerns section (line 187) is stale in a way this pass discovered independently — see Gaps Summary. |

**Score:** 4/5 truths fully verified (0 present-but-behavior-unverified, 0 overrides). Truth 1 moved from partial-fail to fully verified this pass. Truth 5 remains failed, now for a different, newly-found reason (STATE.md, not 01-REVIEW.md).

### Correction Verification (adversarial re-derivation against HEAD)

Each of the three claimed corrections was independently checked against the actual code/config at HEAD, not trusted from the correction paragraph's own text.

| Correction | Claim | Independently checked against HEAD | Verdict |
|---|---|---|---|
| WR-01 (OPR guard) | Guard widened to `!Number.isFinite(residual) \|\| !Number.isFinite(denom) \|\| denom <= 0` by `d73d5aba` | Direct read: `packages/core/algorithms/opr.ts:417` — exact match, byte for byte | ✓ ACCURATE |
| WR-02 (pnpm-workspace.yaml) | Both `apps/*` and `packages/*` globs commented out by `c09b4b57`; `allowBuilds` untouched; described as resolution-by-redefinition with the original forward-looking concern noted as arguably reopened | Direct read: `pnpm-workspace.yaml` — both globs commented (lines 7-9), `allowBuilds` active (lines 10-12), inline note matches commit message intent | ✓ ACCURATE |
| WR-03 (corpus lock TOCTOU) | Full closure required both `8dec3a05` (fast path) and `280b1e7e` (stale-lock reclaim path) | Direct read: `packages/corpus/db.ts:59-98` — fast path is atomic `wx` create (line 61), reclaim path unlinks then recursively retries the same atomic create (lines 88-98), matching the correction's description of both commits' contributions | ✓ ACCURATE |
| Commits exist, correctly authored after `f719750c` | `git log` | `d73d5aba`, `c09b4b57`, `280b1e7e` all present in history | ✓ CONFIRMED |
| Append-only discipline preserved | Original text left in place, correction appended | Direct read of all three subsections — original paragraphs unchanged above each `**Correction**` paragraph | ✓ CONFIRMED |
| `reviewFrontmatterLint.ts` still agrees with frontmatter | Corrections are prose paragraphs, not `#### Resolution` headings, so resolution count stays 7 | `npx tsx scripts/reviewFrontmatterLint.ts` → `4 review file(s) checked, 0 problems`; direct count of `#### Resolution` in `01-REVIEW.md` = 7, matching `resolution.resolved: 7` | ✓ CONFIRMED |

### Cross-Check: No Other Review File Went Stale

Adversarially checked whether any of the four phase-03.1-code-review-gate commits (`d73d5aba`, `280b1e7e`, `11087dd8`, `c09b4b57`) touch code paths `02-REVIEW.md` or `03-REVIEW.md`'s resolution subsections describe.

| Gate-fix commit | Files touched (`git show --stat`) | Overlaps a 02/03-REVIEW.md resolution? |
|---|---|---|
| `d73d5aba` (CR-01, OPR guard) | `opr.ts`, `opr.test.ts` | No — neither review file's resolutions describe `opr.ts`'s guard condition |
| `280b1e7e` (WR-01, lock reclaim) | `packages/corpus/db.ts`, `db.test.ts` | No — `03-REVIEW.md` doesn't touch corpus locking |
| `11087dd8` (WR-02, `makeSigma1` validation) | `packages/core/algorithms/sigma1/index.ts` (isolated to `makeSigma1`, lines ~1084-1101), `sigma1.test.ts` | No — confirmed by reading the diff directly: touches only the `makeSigma1` factory, not the D-05 fallback region (`predictedComponentTotals`, lines 255-270/494-500) that `02-REVIEW.md`'s CR-01/WR-03 describe, nor `tune.ts`/`promote.ts`/`cli.ts` that `03-REVIEW.md`'s WR-01 describes |
| `c09b4b57` (WR-03, workspace globs) | `pnpm-workspace.yaml` | No — only `01-REVIEW.md`'s WR-02 describes this file |

**Conclusion: no additional REVIEW.md drift found.** `02-REVIEW.md` and `03-REVIEW.md`'s existing resolution subsections remain accurate against HEAD.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.planning/phases/01-data-foundation-evaluation-harness/01-REVIEW.md` | 7 resolution subsections, resolution frontmatter, accurate against HEAD, with the three prior-pass corrections applied | ✓ VERIFIED (was ⚠️ PARTIAL) | All 7 resolution subsections (CR-01, WR-01 through WR-06) now accurately describe HEAD; the three corrected ones independently re-derived above. |
| `.planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md` | 3 resolution subsections, accurate against HEAD | ✓ VERIFIED (regression check) | Re-confirmed no overlap with the gate-fix commits; unchanged since prior pass. |
| `.planning/phases/02-prediction-models-epa-sigma1/02-REVIEW.md` | 4 resolution subsections (CR-01, WR-01, WR-02, WR-03), accurate against HEAD | ✓ VERIFIED (newly checked this pass) | Read in full this pass (not previously read directly). CR-01's fix (`dc6b841b`) and WR-01's fix (`c5975de6`) both independently confirmed unaffected by the later gate-fix commits, which touch a different function (`makeSigma1`) in the same file, not the D-05 fallback region CR-01/WR-03 describe. |
| `.planning/phases/01-data-foundation-evaluation-harness/01-VERIFICATION.md` | Human-verification item corrected, status `passed` | ✓ VERIFIED (regression check) | `human_verification[0].status: resolved`, `passed` verdict, unchanged. |
| `.planning/STATE.md` | Blockers/Concerns section accurate against HEAD | ✗ FAILED (newly found this pass) | The previously-checked T-03-18b correction remains accurate. But line 187 (foulsCommitted/epa.ts predict() concern) is stale — see Gaps Summary. This line was not part of what the prior pass checked or what this gap-closure commit addressed. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `.planning/STATE.md` | 187 | Present-tense description of a bug in `epa.ts`'s `predict()` that does not exist at HEAD; explicitly says "not fixed by plan 02-04" without noting the same-day fix (`a0ec5d54`) that WINDOWS.md's own cited ledger entry records as `fixed` | 🛑 Blocker | This is exactly the drift class ROADMAP success criterion 5 exists to prevent — a planning artifact claims an open finding that is in fact closed. |

### Human Verification Required

None. The correction accuracy, the cross-check for additional drift, and the newly-found STATE.md gap are all directly verifiable by reading the code and the planning artifacts side by side.

### Gaps Summary

**The gap-closure commit did its job — the three items it targeted are genuinely fixed.** Each of `01-REVIEW.md`'s WR-01, WR-02, and WR-03 `#### Resolution` subsections now carries an accurate, dated, append-only correction, independently re-derived against HEAD rather than trusted from the correction text. `pnpm-workspace.yaml`, `opr.ts`'s guard, and `db.ts`'s stale-lock reclaim path all match what their respective corrections claim, exactly. `reviewFrontmatterLint.ts` still reports 0 problems, and the resolution counts remain internally consistent. Criteria 1-4 all still hold — nothing regressed, since the gap-closure commit touched only prose in `01-REVIEW.md`.

**However, being adversarial about "no OTHER artifact in the Phase 1-3 set is now stale" (as instructed) surfaced a new instance of the same drift class in a different file.** `STATE.md`'s Blockers/Concerns section, line 187, states in the present tense that `epa.ts`'s `predict()` currently sums a team's own `foulsCommitted` into its own score rather than the opponent's, and explicitly says this was "not fixed by plan 02-04." This is false at HEAD: `predict()` (lines 349-364, confirmed by direct read) implements exactly the D-04 cross-alliance attribution the line describes as missing — excluding the alliance's own `FOULS_COMMITTED_COMPONENT` and adding the opposing alliance's instead. The fix landed same-day as the deviation was logged, in commit `a0ec5d54` (`fix(02): EPA predict attributes foulsCommitted to the opposing alliance (D-04)`). `WINDOWS.md`'s own ledger — the exact source STATE.md's line cites ("logged to WINDOWS.md entry 3") — independently records this as `status: fixed`, `resolved_at: 2026-08-14T06:40:51.008Z`, with a `reason` field describing this same fix. STATE.md's line was simply never updated to reflect what its own cited source already says.

This is not a regression introduced by the gap-closure commit (`e66f7c22` touched only `01-REVIEW.md`) and is not one of the three items the previous verification pass flagged — it is a fourth, independent instance of the phase's own success-criterion-5 failure mode, found by extending the same adversarial check to a different artifact this pass was specifically asked to widen scope to. It requires the same narrow, mechanical fix as the three already-closed items: an appended, dated correction to STATE.md line 187, in the same strikethrough-and-append style the two adjacent bullets in the same section already use.

No other STATE.md line, and no other Phase 1-3 planning artifact checked this pass (`02-REVIEW.md`, `03-REVIEW.md`, `01-VERIFICATION.md`), was found stale.

---

_Verified: 2026-08-20T23:45:00Z_
_Verifier: Claude (gsd-verifier) — re-verification after gap closure_
