---
phase: 03-tuning-ranking-points-versioning
plan: 07
type: execute
wave: 6
depends_on: ["03-02", "03-03"]
files_modified:
  - packages/core/algorithms/sigma1/rp/constants.ts
  - packages/core/algorithms/sigma1/index.ts
  - packages/core/algorithms/sigma1/rp/distribution.ts
  - packages/core/algorithms/sigma1/sigma1.test.ts
autonomous: true
requirements: [ALGO-08]
gap_closure: true

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Sigma1's shipped update() and predict() complete without throwing for a match whose TBA event_type is not in the RP tier table, including offseason (99) — the RP step is skipped, never crashed through."
    - "Both documented CLI invocations that reach an offseason match with a score breakdown — a season replay with the offseason flag, and single-event mode on an offseason event key — run to completion against the real corpus."
    - "A match at any mapped event_type still takes the full RP path, so the guard cannot silently disable RP prediction everywhere and pass as a fix."
    - "Every committed algorithm version still reproduces its recorded prediction-stream digest bitwise after the guard is added — the fix is a proven no-op for the already-tested population, not an asserted one."
    - "An alliance with no evaluable RP for a match carries no ranking-point distribution at all, rather than a distribution claiming it will certainly earn zero."
    - statement: "An event_type absent from the RP tier table (the empty/unknown-key case) produces a defined skip with the skipped-match counter incremented, never a throw and never a coerced zero-RP result"
      verification: backstop
    - statement: "The RP pmf is indexed by integer RP total in a fixed 0..maxRp order and is seeded per match key, so two matches with equal inputs produce byte-identical pmfs regardless of their position in the replay stream"
      verification: backstop
    - statement: "ALGO-04 boundary — a searched hyperparameter sitting exactly on its search-space bound is accepted and reported as on-bound rather than silently clamped or rejected"
      verification: backstop
    - statement: "ALGO-04 adjacency — two candidate parameter sets scoring exactly equal resolve to a single deterministic winner, never an order-dependent one"
      verification: backstop
    - statement: "ALGO-04 empty — a search stage with zero surviving parameters after the screen produces an explicit reported outcome rather than an empty joint search that appears to succeed"
      verification: backstop
    - statement: "ALGO-04 ordering — candidates that tie on the search objective are ranked by a stated, stable tiebreak so a re-run selects the same winner"
      verification: backstop
    - statement: "ALGO-04 precision — the search objective is compared at full float precision with no rounding step that could make two genuinely different candidates compare equal"
      verification: backstop
    - statement: "ALGO-05 boundary — an adaptation factor driven to its stability bound is clamped at that bound and the filter stays stable, rather than diverging"
      verification: backstop
    - statement: "ALGO-05 precision — innovation statistics accumulate without precision loss or overflow across a full season of matches for a single team"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT resolve a digest mismatch by re-promoting a version, regenerating the committed fixture slice, or relaxing the digest assertion — a digest that moves because of this guard is a finding about the guard, not a fixture to refresh."
      status: unverified
      verification: flagged
  artifacts:
    - path: "packages/core/algorithms/sigma1/rp/constants.ts"
      provides: "The eligibility predicate the RP subsystem's callers were missing"
      contains: "isRpEligibleEventType"
    - path: "packages/core/algorithms/sigma1/index.ts"
      provides: "The guarded update() RP fold and predict() RP pmf step"
    - path: "packages/core/algorithms/sigma1/sigma1.test.ts"
      provides: "The CR-01 regression tests: unmapped event_type does not throw, mapped event_type still folds"
  key_links:
    - from: "packages/core/algorithms/sigma1/index.ts"
      to: "packages/core/algorithms/sigma1/rp/constants.ts"
      via: "update() and predict() both gate the RP step on the same exported eligibility predicate"
      pattern: "isRpEligibleEventType"
    - from: "packages/core/algorithms/sigma1/rp/constants.ts"
      to: "packages/core/algorithms/sigma1/rp/constants.ts"
      via: "the predicate reads the same tier table eventTierFor throws on, so the two can never disagree about which event types are eligible"
      pattern: "EVENT_TYPE_TIERS"
---

<objective>
Close CR-01: Sigma1's shipped `update()`/`predict()` RP path crashes on any TBA `event_type` the
RP tier table does not map, and two documented CLI invocations reach that crash with real corpus
data. Give the RP subsystem the structural exclusion the rest of the codebase already applies at
its call sites, and prove the fix changes nothing for every match that already worked.

Purpose: `03-VERIFICATION.md` failed Success Criterion 4 ("every match has predicted ranking
points...") on two separable grounds. This plan closes the first and only code defect among them.
The verifier did not take the code review's word for it — it wrote and ran a standalone
reproduction against the current tree and confirmed `sigma1.update()` throws on a synthetic
offseason match with a real score breakdown. The corpus contains 552 offseason events and 4,757
offseason 2024 matches carrying a score breakdown, so this is a large, real, reachable population,
not a hypothetical one.

Output: `isRpEligibleEventType` in `rp/constants.ts`; guards at the two call sites in
`sigma1/index.ts`; regression tests in `sigma1.test.ts`; a recorded before/after digest comparison
proving the guard is a no-op.

The shape of the answer, so the executor is not surprised: `EVENT_TYPE_TIERS` maps
`{0,1,2,3,4,5,100}`. A live corpus query at planning time found the corpus contains exactly these
event types plus `99` — so `99` (offseason) is the ONLY unmapped value present today, and the
guard is provably a no-op for every other match in the corpus. Both committed digest slices
(`2022alhu`, `2022azfl`, `2022azva`) are `event_type: 0`. The digest CANNOT legitimately move.
If it does, the guard was written wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tuning-ranking-points-versioning/03-CONTEXT.md
@.planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md
@.planning/phases/03-tuning-ranking-points-versioning/03-VERIFICATION.md
@.planning/phases/03-tuning-ranking-points-versioning/03-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce the crash, add the eligibility guard end to end, and prove both directions with tests</name>
  <read_first>
    - `.planning/phases/03-tuning-ranking-points-versioning/03-REVIEW.md`, the full `## Critical Issues` / CR-01 section — it names both call sites, both reachable CLI invocations, and the exact shape of the prescribed fix. This plan implements that fix; do not invent a different one.
    - `packages/core/algorithms/sigma1/rp/constants.ts` — the whole file. Load-bearing: `EVENT_TYPE_TIERS` (the `Readonly<Record<number, EventTier>>` mapping `0/1/100 -> base`, `2/5 -> districtChampionship`, `3/4 -> championship`), and `eventTierFor`, whose doc comment states it throws for an unmapped value *by design* rather than defaulting to `base`, because a silent default would mispredict every District-Championship-and-above match for an unknown event type. The new predicate must preserve that contract, not weaken it.
    - `packages/core/algorithms/sigma1/index.ts` lines 660-705 — `predict()`'s RP block: `rpRuleModuleForSeason(season)`, the two `predictAllianceRpMoments(...)` calls (which take no `eventType` and therefore cannot throw), and the unconditional `rpPmfForMatch({ ..., eventType: match.eventType, ... })` call, whose result is spread into the returned `Prediction` via `...(rpResult.redPmf.length > 0 ? { redRpPmf: rpResult.redPmf } : {})`.
    - `packages/core/algorithms/sigma1/index.ts` lines 789-860 — `update()`'s RP fold: the `let rpSkippedMatchCount = state.rpSkippedMatchCount;` declaration, the `if (usedFallback) { rpSkippedMatchCount += 1; } else { ... }` branch, the `JSON.parse(result.scoreBreakdownRaw!)` and the two `ruleModule.parse(rawJson, side, result.eventType)` calls inside the `else`.
    - `packages/core/algorithms/sigma1/rp/distribution.ts` around `rpPmfForMatch` (from its `export function` line through its return) — the two existing short-circuits: `compLevel !== "qm"` returns `degenerateZeroPmf()` for both alliances, and `params.rpMonteCarloDraws === 0` returns `{ redPmf: [], bluePmf: [] }`. The per-draw loop calls `ruleModule.predictThresholds(values, eventType)`, which is the second throw site.
    - `packages/core/algorithms/sigma1/rp/2024.ts` — confirm `parse()` and `predictThresholds()` each call `eventTierFor(eventType)` as their first statement, so both throw sites are genuinely inside the season module and not somewhere else.
    - `packages/core/algorithms/sigma1/sigma1.test.ts` — the existing fixture helpers this task reuses rather than rebuilds: the match-result builder around line 38 (`eventType: 0`), `rawBreakdown2024Uniform(perComponentValue)` at line 53, `UNIFORM_PER_COMPONENT`, and the two `rpSkippedMatchCount: 0` state literals at lines 272 and 612.
  </read_first>
  <files>packages/core/algorithms/sigma1/rp/constants.ts, packages/core/algorithms/sigma1/index.ts, packages/core/algorithms/sigma1/rp/distribution.ts, packages/core/algorithms/sigma1/sigma1.test.ts</files>
  <action>
**Step 1 — reproduce before fixing.** Run `pnpm harness --season 2024 --algorithm sigma1 --include-offseason --out reports/cr01-repro` against the local corpus and capture the failure verbatim (the thrown message from `eventTierFor` naming an unmapped `event_type 99`, plus which match key it died on). Record that captured output in the SUMMARY. This is the honesty discipline this project already applies (03-CONTEXT.md `<specifics>`: CR-01's Phase-2 fix was re-run in full rather than asserted from cached output) — the fix is only credible if the failure it fixes was observed on this tree, not inherited from a report. If the command unexpectedly succeeds, STOP and report that in the SUMMARY rather than proceeding: the premise of this plan would be wrong and the executor must not invent a fix for a bug that is not there.

**Step 2 — the predicate.** Add to `packages/core/algorithms/sigma1/rp/constants.ts`, immediately after `eventTierFor`:

`export function isRpEligibleEventType(eventType: number): boolean` returning `EVENT_TYPE_TIERS[eventType] !== undefined`.

Give it a doc comment at this file's established density that states: it is the caller-side counterpart to `eventTierFor`'s deliberate throw — the two read the SAME table, so they can never disagree about which event types are eligible, and adding a new event type to `EVENT_TYPE_TIERS` automatically makes it eligible with no second edit. Name CR-01 and cite that the rest of the codebase already applies this exclusion structurally at its own boundaries (`reconciliation.test.ts`'s `sampleQualMatches`, `promote.ts`'s `selectMatchesChronological(..., { excludeOffseason: true })`, `tune.ts`'s `boundedSeasonStream`, which hardcodes `includeOffseason: false`), and that Sigma1's own live `update()`/`predict()` were the one place relying on every caller remembering to filter upstream. Do NOT weaken `eventTierFor` — it keeps throwing; this predicate exists so the throw is never reached from Sigma1's own path.

**Step 3 — guard `update()`'s RP fold.** Change the branch condition from `usedFallback` alone to `usedFallback || !isRpEligibleEventType(result.eventType)`, leaving the `else` body byte-identical. Import the predicate alongside the existing `rp/constants.js` imports.

**DECISION — the skipped-match counter (state this in the source comment, not just here):** `rpSkippedMatchCount` DOES increment for an ineligible `eventType`, sharing the counter with the no-score-breakdown case. The field's meaning is therefore "matches whose RP fold was skipped, for any reason", not "matches missing a breakdown". Rationale: the counter exists so a silently-empty RP state is diagnosable, and both skip reasons produce exactly that condition — splitting it into two counters would add a state field and a versioned-shape change for a distinction nothing currently consumes (the field is read only in `carrySeason`, `index.ts:996`). Extend the existing D-09 comment above the branch to say both skip reasons and why they share one counter.

**CONSISTENCY between `update()` and `predict()`:** `predict()` is pure and carries no counter, so consistency here means the *predicate* is shared, not the bookkeeping — the same `isRpEligibleEventType(eventType)` decides both, so any match whose fold `update()` skips is exactly a match whose `predict()` emits no pmf. Say this explicitly in both comments so a future reader does not add a second, drifting eligibility rule on one side.

**Step 4 — guard `predict()`'s RP pmf step.** Wrap only the `rpPmfForMatch({ ... })` call:

`const rpResult = isRpEligibleEventType(match.eventType) ? rpPmfForMatch({ ...unchanged argument object... }) : { redPmf: [], bluePmf: [] };`

Leave `redRpMoments`/`blueRpMoments` and everything above them untouched — `predictAllianceRpMoments` takes no `eventType` and cannot throw, and moving it would risk perturbing the score-side path this plan must not touch.

**DECISION — why `{ redPmf: [], bluePmf: [] }` and not `degenerateZeroPmf()`:** the empty arrays make the existing `...(rpResult.redPmf.length > 0 ? ... : {})` spread omit `redRpPmf`/`blueRpPmf` from the `Prediction` entirely, which is `types.ts`'s documented "omitted entirely, never an empty array" convention and is already how the zero-draws fast path behaves. A degenerate `P(RP=0)=1` would be a positive claim that the alliance certainly earns no ranking points — false for an offseason qualification match, which does award RP under whatever rules that event ran. Absence of a prediction is the honest representation of "this subsystem has no rules for this event tier"; certainty of zero is not. Record this reasoning in the comment.

**Step 5 — the caller precondition on `rpPmfForMatch`.** Add one sentence to `rpPmfForMatch`'s doc comment in `rp/distribution.ts` naming `isRpEligibleEventType` as a caller precondition and stating that `predictThresholds` will throw for an ineligible `eventType`. Do NOT add a second guard inside `rpPmfForMatch` — one load-bearing guard, documented, beats two that can drift apart about which is authoritative.

**Step 6 — regression tests in `sigma1.test.ts`.** Add a `describe` block named for CR-01 with four tests, reusing the existing `rawBreakdown2024Uniform(UNIFORM_PER_COMPONENT)` fixture and match-result builder rather than new fixtures:

1. `update()` on a match with `eventType: 99`, `compLevel: "qm"` and a real `scoreBreakdownRaw` does not throw, and the returned state's `rpSkippedMatchCount` is the prior value plus one.
2. That same call leaves the score side working: the returned state's team beliefs still changed (the score fold ran normally), proving the guard skipped only the RP fold and did not short-circuit `update()` wholesale.
3. `predict()` on an upcoming match with `eventType: 99` and `compLevel: "qm"` does not throw, and the returned `Prediction` has neither a `redRpPmf` nor a `blueRpPmf` property present (assert on property presence, not on an empty array).
4. **Positive control, non-negotiable:** for every value in `[0, 1, 2, 3, 4, 5, 100]`, `update()` takes the RP path — `rpSkippedMatchCount` does NOT increment — and `predict()` returns a `Prediction` that DOES carry `redRpPmf` and `blueRpPmf`. Without this test, an `isRpEligibleEventType` that always returned `false` would silently disable RP prediction for the entire project and still pass tests 1-3.

Then run `pnpm typecheck` and `pnpm test`. If any pre-existing test now fails, that is a finding to investigate and report, not a test to adjust.
  </action>
  <verify>
    <automated>pnpm typecheck && npx vitest run packages/core/algorithms/sigma1/sigma1.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function isRpEligibleEventType" packages/core/algorithms/sigma1/rp/constants.ts` outputs `1`
    - `grep -c "isRpEligibleEventType" packages/core/algorithms/sigma1/index.ts` outputs at least `3` (the import plus one call site in `update()` and one in `predict()`)
    - `eventTierFor` still throws for an unmapped value: `grep -c "throw new Error" packages/core/algorithms/sigma1/rp/constants.ts` is unchanged from its pre-task value (the predicate is added alongside the throw, never replacing it)
    - A test exists asserting `sigma1.update()` on a match with `eventType: 99`, `compLevel: "qm"` and a non-null `scoreBreakdownRaw` does NOT throw, and it passes
    - A test exists asserting that same call returns a state whose `rpSkippedMatchCount` equals the input state's value plus 1, and it passes
    - A test exists asserting `sigma1.predict()` with `eventType: 99` and `compLevel: "qm"` returns an object where `"redRpPmf" in prediction` and `"blueRpPmf" in prediction` are both `false`, and it passes
    - A parameterized positive-control test covers all of `0, 1, 2, 3, 4, 5, 100` and asserts each still produces a `Prediction` carrying `redRpPmf` and `blueRpPmf` and an `update()` that does not increment `rpSkippedMatchCount`, and it passes
    - `pnpm typecheck` exits 0
    - `npx vitest run packages/core/algorithms/sigma1/sigma1.test.ts` exits 0 with no test skipped
    - The SUMMARY quotes the verbatim pre-fix crash output captured in Step 1, including the offending match key
  </acceptance_criteria>
  <done>The guard exists, is shared by both call sites, is proven to skip unmapped event types and proven NOT to skip mapped ones, and the crash it fixes was observed on this tree before being fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the fix is a no-op — committed digests bitwise unchanged, and both crashing CLI invocations now complete</name>
  <read_first>
    - `packages/harness/digest.test.ts` — the whole file, in particular the `describe("promoted algorithm version reproducibility (D-15/SC-5)")` block at line 94, the per-version `describe(file)` at line 113, the `it("re-runs on its own recorded slice and reproduces its committed digest bitwise")` at line 137, and the `it("corpus-derived and fixture-derived slice match lists are identical (fixture is not stale, T-03-17)")` at line 177.
    - `data/algorithm-versions/sigma1@2.0.0+tuned-2026-08.json` and `data/algorithm-versions/sigma1@2.0.0+tracer-check.json` — the `digest` blocks whose `predictionStreamSha256` values must not move.
    - `packages/harness/cli.ts` lines 720-770 — the `--include-offseason` flag parse at 727/742 and its threading into `runSeasonsMode`.
    - `packages/harness/cli.ts` lines 614-707 — `runEventMode`. Note carefully: it DOES read `is_offseason` at line ~655, but only to TAG each prediction record's `isOffseason` field for score slicing at line 678. It never filters offseason matches out of `selectMatchesChronological(db, { eventKey })` at line 649, so `WalkForwardSimulator.runAll` at line 667 replays them all. Do not mistake that read for a filter.
    - `.planning/phases/03-tuning-ranking-points-versioning/03-VERIFICATION.md` — the `gaps[0].missing[0]` entry, which is exactly what this plan closes.
  </read_first>
  <files>packages/harness/digest.test.ts</files>
  <action>
No production code changes in this task. This is the proof step, and it is the reason the guard is
allowed to ship: D-15/D-16 make "unchanged" mean bitwise, and `digest.test.ts` is the CI gate that
enforces it.

**Step 1 — record the committed digests before running anything.** Read both files under
`data/algorithm-versions/` and write down each `digest.predictionStreamSha256`. At planning time
these are `d1203147feb7b130a085c1a992f83d2577221d8efcfcad6ac22360e1ad4bf8a6` for
`sigma1@2.0.0+tuned-2026-08` (2022 slice, events `2022alhu`/`2022azfl`/`2022azva`, 265 matches) and
`b5f3d21c41d6af2b1fea7c562769c2ac9afde8a44dd654a73ff176e378cb7d8e` for `sigma1@2.0.0+tracer-check`
(2022 slice, events `2022alhu`/`2022azfl`, 180 matches). Confirm the on-disk values match these; if
they do not, say so in the SUMMARY rather than proceeding on the planned values.

**Step 2 — run the gate.** `npx vitest run packages/harness/digest.test.ts`. It must report 3 passing
tests and 0 skipped, against the live corpus (present locally at `data/corpus.sqlite`) rather than
falling back to the fixture, so the assertion exercises the real path.

Why this must pass unchanged, so the executor knows what a failure would mean: a live corpus query
at planning time confirmed all three digest-slice events (`2022alhu`, `2022azfl`, `2022azva`) are
`event_type: 0`, which `EVENT_TYPE_TIERS` maps to `base`. `isRpEligibleEventType(0)` is therefore
`true` for every match in both slices, both guards take their existing branch, and not one byte of
the prediction stream can change. **A moved digest means the guard is wrong** — most likely the
predicate's polarity is inverted or it is reading the wrong field. Per this plan's prohibition, fix
the guard; do not re-promote, do not regenerate `packages/harness/fixtures/digest-slice.json`, and
do not touch the assertion.

**Step 3 — re-run the two invocations that crashed.** Both against the real corpus:

1. `pnpm harness --season 2024 --algorithm sigma1 --include-offseason --out reports/cr01-fixed` — the exact command captured failing in Task 1 Step 1. 2024 carries 4,757 offseason matches with a score breakdown, so this genuinely exercises the guarded path thousands of times rather than incidentally.
2. `pnpm harness --event 2024wvrox --algorithm sigma1 --out reports/cr01-fixed-event` — `2024wvrox` is a real corpus offseason event (`event_type: 99`, `is_offseason: 1`) with 154 matches, all carrying a score breakdown; it is the largest such event in the corpus. Note this path calls `tbaFetch` before replaying, so it needs network access and `TBA_API_KEY` in `.env`. If either is unavailable, record that honestly in the SUMMARY as "not run, and why" — do NOT claim it passed, and do NOT substitute a different command and describe it as this one.

For each run that completes, report the produced artifact path and confirm the run covered offseason matches (invocation 1's artifact will carry offseason-tagged score slices; invocation 2's records are tagged `isOffseason: true` wholesale).

**Step 4 — full suite.** `pnpm test` and `pnpm typecheck`, both green. Report the total test count so a future reader can see nothing was quietly excluded.

**Step 5 — clean up.** Remove the `reports/cr01-*` output directories after recording their results; `reports/` is gitignored per the failure log's repo-hygiene rule, so nothing generated here belongs in a commit.
  </action>
  <verify>
    <automated>npx vitest run packages/harness/digest.test.ts && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/harness/digest.test.ts` exits 0, reporting 3 passed and 0 skipped
    - `data/algorithm-versions/sigma1@2.0.0+tuned-2026-08.json` still contains the string `d1203147feb7b130a085c1a992f83d2577221d8efcfcad6ac22360e1ad4bf8a6` and the file is unmodified in `git status`
    - `data/algorithm-versions/sigma1@2.0.0+tracer-check.json` still contains the string `b5f3d21c41d6af2b1fea7c562769c2ac9afde8a44dd654a73ff176e378cb7d8e` and the file is unmodified in `git status`
    - `packages/harness/fixtures/digest-slice.json` is unmodified in `git status`
    - `pnpm harness --season 2024 --algorithm sigma1 --include-offseason --out reports/cr01-fixed` exits 0 and writes an artifact; the SUMMARY records the exit status and artifact path
    - `pnpm harness --event 2024wvrox --algorithm sigma1 --out reports/cr01-fixed-event` exits 0 and writes an artifact — OR the SUMMARY records that it could not run, names the missing prerequisite (network or `TBA_API_KEY`), and does not claim a result
    - `pnpm test` exits 0 and the SUMMARY records the total passing test count
    - `pnpm typecheck` exits 0
    - `git status` shows no modification to any file under `data/algorithm-versions/` or `reports/`
  </acceptance_criteria>
  <done>Both committed digests reproduce bitwise with the guard in place, the full suite is green, and the season-replay invocation that crashed before now runs to completion against real offseason data.</done>
</task>

</tasks>

<threat_model>
Scope note: this plan changes offline algorithm code with no network surface, no authentication,
no user input, and no secrets. The threats below are the ones that genuinely apply — integrity of
a reproducibility guarantee and availability of a documented code path — not a transplanted
web-application register. ASVS L1; `block_on: high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus `event_type` (third-party TBA enum) → RP tier dispatch | An out-of-range enum value crosses into code documented to throw on it |
| corpus `score_breakdown_raw` (third-party JSON string) → `JSON.parse` in `update()`'s RP fold | Untrusted third-party text is parsed and folded into Kalman state |
| this source change → committed prediction-stream digests | A code edit can silently invalidate the SC-5 reproducibility guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-18 | Denial of Service | `sigma1/index.ts` `update()` RP fold and `predict()` RP pmf step, on an unmapped `event_type` | high | mitigate | This IS CR-01. An uncaught `eventTierFor` throw aborts the entire harness run — every season and every algorithm in the batch, not just the offending match. Task 1 adds `isRpEligibleEventType` at both call sites so the RP step degrades to a counted skip; Task 2 re-runs both documented invocations that reached it. |
| T-03-19 | Tampering | The guard silently perturbing a committed prediction-stream digest, voiding SC-5's bitwise reproducibility claim | high | mitigate | Task 2 records both committed `predictionStreamSha256` values before running, re-runs `digest.test.ts` against the live corpus, and asserts the version files and the fixture slice are untouched in `git status`. Both slices are `event_type: 0`, so the guard is provably inert there — a moved digest is a defect in the guard, and this plan's prohibition forbids resolving it by re-promotion or fixture refresh. |
| T-03-20 | Tampering | `JSON.parse(result.scoreBreakdownRaw!)` on third-party corpus text reaching the RP fold | medium | mitigate | Inherited, unchanged by this plan and re-affirmed: the parsed value is immediately handed to each season module's own Zod schema, which rejects a shape missing its required fields, and 03-02's `RpParsedResult` records are built with `Object.create(null)` and a fixed allowlist loop, so a `__proto__` key in the raw payload cannot pollute. This plan adds a guard in front of that parse and widens no input surface. |
| T-03-21 | Spoofing | A guard broad enough to swallow unrelated exceptions, masking a real rule defect as a skip | medium | mitigate | The guard is a positive membership test against `EVENT_TYPE_TIERS`, not a `try`/`catch`. `eventTierFor` keeps throwing, and Task 1's positive-control acceptance criterion proves all seven mapped event types still take the full RP path — so a predicate that over-skips fails the tests rather than passing quietly. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs occur in this plan. No dependency is added, removed, or version-changed; `packages/core` gains no import beyond a sibling module in its own directory. A package legitimacy audit is therefore not applicable. |
</threat_model>

<assumption_delta_decision>
**Signal:** the detector fired `kind: chosen` on 03-01-PLAN.md's "parameterized Sigma1 tuned,
promoted, and reproduced end to end" — a constant became a parameter.

**Noun now primary:** an algorithm version = code version + a named, committed parameter set
(D-13), already the phase's established identity.

**Decision: `no-change`.**

Rationale: this plan introduces no parameter and promotes no derived value to a chosen one.
`isRpEligibleEventType` is *derived* from the existing `EVENT_TYPE_TIERS` table — it reads the same
data `eventTierFor` already reads, adds no constant, and exposes no knob. Nothing here is tunable,
versioned, or searchable, so no configuration decision has become a modeling decision. Recorded
explicitly rather than omitted.
</assumption_delta_decision>

<verification>
- `pnpm typecheck` exits 0
- `pnpm test` exits 0 (full suite), with the total count recorded
- `npx vitest run packages/harness/digest.test.ts` exits 0 with 3 passed, 0 skipped, against the live corpus
- Both committed `predictionStreamSha256` values are byte-identical to their pre-task values, and `git status` shows no change under `data/algorithm-versions/` or `packages/harness/fixtures/`
- `pnpm harness --season 2024 --algorithm sigma1 --include-offseason` completes, having previously crashed on this same tree
- The pre-fix crash output is quoted verbatim in the SUMMARY
</verification>

<success_criteria>
- Sigma1's shipped `update()` and `predict()` handle an unmapped `event_type` as a defined, counted skip instead of an uncaught exception
- The two documented CLI invocations that reached the crash run to completion against the real corpus
- Every mapped `event_type` still takes the full RP path, proven by a positive control rather than assumed
- Every committed algorithm version reproduces its digest bitwise, proving the guard is a no-op for the already-tested population
</success_criteria>

<artifacts_this_phase_produces>
Symbols and paths CREATED by this plan (new to the codebase — not drift):

- `packages/core/algorithms/sigma1/rp/constants.ts` — new export `isRpEligibleEventType(eventType: number): boolean`
- `packages/core/algorithms/sigma1/sigma1.test.ts` — new `describe` block of CR-01 regression tests (added to an existing file; no new test file)

Modified, no new symbols: `packages/core/algorithms/sigma1/index.ts` (guard expressions and comments
at two existing call sites), `packages/core/algorithms/sigma1/rp/distribution.ts` (one added doc-comment
sentence naming the caller precondition on `rpPmfForMatch`; no signature or behavior change).
</artifacts_this_phase_produces>

<flagged_assumptions>
- **ALGO-06's edge-probe row `unclassified` remains UNRESOLVED.** The deterministic probe returned
  `"unclassified — review manually"` for ALGO-06, and the rules forbid auto-classifying it into
  `backstop` or `dismissed`. It is surfaced here rather than dropped. This is the same row
  `03-01-PLAN.md` flagged and `docs/models/sigma1-tuning-results.md`'s Open Items still records as
  open. It is deliberately NOT closed by this gap-closure plan: ALGO-06 is a VERIFIED requirement
  (`03-VERIFICATION.md` Requirements Coverage), and this run's contract is to close ALGO-08 only.
  Re-running that probe is a scope expansion onto already-satisfied work.
- **The seven ALGO-04/ALGO-05 edge predicates are authored as `backstop` markers, not as new work.**
  Both requirements are already SATISFIED per `03-VERIFICATION.md`. The predicates state the
  contract each edge implies so the probe items are not silently dropped, but this plan adds no
  task to re-verify them and claims no new evidence for them.
- **Edge-probe accounting (no silent drops).** 11 applicable probe items. Authored here: 2 as
  covered `truths` (ALGO-08 `empty` → the unmapped-event_type skip; ALGO-08 `ordering` → fixed pmf
  index order and per-match seeding), 7 as `backstop` markers (ALGO-04 ×5, ALGO-05 ×2), 1 as the
  flagged assumption above (ALGO-06 `unclassified`) = 10. The remaining item, ALGO-08 `adjacency`
  (threshold exactly met), is authored in `03-08-PLAN.md`, where the thresholds it concerns are the
  subject. 10 + 1 = 11.
</flagged_assumptions>

<output>
Create `.planning/phases/03-tuning-ranking-points-versioning/03-07-SUMMARY.md` when done
</output>
