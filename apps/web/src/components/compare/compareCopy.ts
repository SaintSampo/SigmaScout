/**
 * Page level copy for `/methodology/compare` (sketch 017, variant A). Lives
 * here rather than in the route file so the route module exports only `Route`.
 * No hyphen or dash characters in any string.
 */

/** The page title, matching the methodology hub card that links here. */
export const COMPARE_PAGE_TITLE = "Algorithm accuracy";

/** The opening paragraph. Static, so it renders from first paint in every query state. */
export const COMPARE_LEAD =
  "How often OPR, EPA and SPR picked the match winner in each season, and how good their win odds were. Every prediction was made before the match was played.";

export const COMPARE_LEAD_TESTID = "compare-lead";
